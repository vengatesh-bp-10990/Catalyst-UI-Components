(self["webpackChunk_zoho_lyte_ui_component"] = self["webpackChunk_zoho_lyte_ui_component"] || []).push([["lyte"],{

/***/ 47851461:
/*!************************************************!*\
  !*** ./node_modules/@slyte/component/index.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Component": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.Component),
/* harmony export */   "ComponentRegistry": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.ComponentRegistry),
/* harmony export */   "CustomElements": () => (/* reexport safe */ _src_utils_custom_elements_js__WEBPACK_IMPORTED_MODULE_4__.CustomElements),
/* harmony export */   "Directive": () => (/* reexport safe */ _src_directives_lyte_directive_js__WEBPACK_IMPORTED_MODULE_1__.Directive),
/* harmony export */   "Helper": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.Helper),
/* harmony export */   "IdeOverride": () => (/* reexport safe */ _src_ide_lyte_ide_override_js__WEBPACK_IMPORTED_MODULE_7__["default"]),
/* harmony export */   "LyteCustomElement": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.LyteCustomElement),
/* harmony export */   "LyteYield": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.LyteYield),
/* harmony export */   "Nimble": () => (/* reexport safe */ _src_compiler_client_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_5__.ClientCompiler),
/* harmony export */   "NimblePlus": () => (/* reexport safe */ _src_compiler_client_lyte_client_compile_advanced_js__WEBPACK_IMPORTED_MODULE_6__.ClientCompilerAdvanced),
/* harmony export */   "RawComponent": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.RawComponent),
/* harmony export */   "Sanitizer": () => (/* reexport safe */ _src_utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_3__.Sanitizer),
/* harmony export */   "V3Registry": () => (/* reexport safe */ _src_utils_v3_registry_js__WEBPACK_IMPORTED_MODULE_9__.V3Registry),
/* harmony export */   "_LC": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__._LC),
/* harmony export */   "_addLyteAction": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__._addLyteAction),
/* harmony export */   "_setIgnoreDisconnect": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__._setIgnoreDisconnect),
/* harmony export */   "addBindings": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.addBindings),
/* harmony export */   "appendChild": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.appendChild),
/* harmony export */   "arrayUtils": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.arrayUtils),
/* harmony export */   "customElementPrototype": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.customElementPrototype),
/* harmony export */   "defProp": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.defProp),
/* harmony export */   "freeze": () => (/* reexport safe */ _src_utils_freeze_js__WEBPACK_IMPORTED_MODULE_0__.freeze),
/* harmony export */   "getNearestApp": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.getNearestApp),
/* harmony export */   "insertAfter": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.insertAfter),
/* harmony export */   "insertBefore": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.insertBefore),
/* harmony export */   "loadComponent": () => (/* reexport safe */ _src_utils_load_component_js__WEBPACK_IMPORTED_MODULE_8__.loadComponent),
/* harmony export */   "makeSet": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.makeSet),
/* harmony export */   "nextTick": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.nextTick),
/* harmony export */   "objectUtils": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.objectUtils),
/* harmony export */   "render": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.render),
/* harmony export */   "replaceWith": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.replaceWith),
/* harmony export */   "set": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.set),
/* harmony export */   "shouldIgnoreDisconnect": () => (/* reexport safe */ _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__.shouldIgnoreDisconnect)
/* harmony export */ });
/* harmony import */ var _src_lyte_component_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./src/lyte-component.js */ 26591616);
/* harmony import */ var _src_utils_v3_registry_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./src/utils/v3-registry.js */ 10494547);
/* harmony import */ var _src_compiler_client_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./src/compiler/client/lyte-client-compile.js */ 52516253);
/* harmony import */ var _src_compiler_client_lyte_client_compile_advanced_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./src/compiler/client/lyte-client-compile-advanced.js */ 43638185);
/* harmony import */ var _src_ide_lyte_ide_override_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./src/ide/lyte-ide-override.js */ 88544617);
/* harmony import */ var _src_utils_load_component_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./src/utils/load-component.js */ 35495860);
/* harmony import */ var _src_utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./src/utils/lyte-sanitizer.js */ 20426923);
/* harmony import */ var _src_utils_custom_elements_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./src/utils/custom-elements.js */ 4585556);
/* harmony import */ var _src_utils_freeze_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./src/utils/freeze.js */ 22956926);
/* harmony import */ var _src_directives_lyte_directive_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./src/directives/lyte-directive.js */ 43280731);












/***/ }),

/***/ 32483767:
/*!**************************************************************************!*\
  !*** ./node_modules/@slyte/component/src/compatibility/compatibility.js ***!
  \**************************************************************************/
/***/ (() => {

if(!window.__lyteConfig){
    window.__lyteConfig = {
        _definedBeforeBridging : [],
        _alreadyDefinedBeforeBridging : [],
        v4 : true,
        _pendingV3Components : {},
        _firstRegisteredComp : {}
    }
}
window.__lyteConfig.v4 = true;

/***/ }),

/***/ 41325797:
/*!***********************************************************************!*\
  !*** ./node_modules/@slyte/component/src/compiler/cli/expHandlers.js ***!
  \***********************************************************************/
/***/ ((module) => {



var errorFlag;
var errorCount = [0];
var expHandlers = {
	checkForError : function(errorMsg,node,isIgnore) {
		this.message = errorMsg;
		this.node = node;
		return this;
	},

	handleCallExpression : function(node,isIgnore,escape){
		var output = "";
		if(node.callee.type == "MemberExpression") {
			throw new this.checkForError("Only helper function calls are allowed",node.callee);
		}
		if(node.callee.name){
			output += node.callee.name+"(";
		}else{
			output += this.getValueBasedOnType(node.callee,isIgnore)+"(";
		}
		output += this.handleCallArguments(node,isIgnore).toString();
		output += ")";
		return output;
	},

	handleCallArguments : function(node,isIgnore,escape){
			var output = [];
			var forArg = node.arguments;
			for(var k=0;k<forArg.length;k++){
				if(forArg[k].type == "CallExpression"){
						output.push(this.handleCallExpression(forArg[k],isIgnore));
				}else {
					output.push(this.getValueBasedOnType(forArg[k],isIgnore,escape));//.name;
				}
			}
			return output.toString();
	},
	
	handleMemberExpression : function(node,isIgnore,output){
		var val ="";
		if(node.object.name){
			val += node.object.name;
		}else if(node.object.type == "CallExpression"){
			val += this.getValueBasedOnType(node.object,isIgnore);
			//helperFnParams.push(handleCallArguments(node.object,true).toString());
		}else {
			val += this.getValueBasedOnType(node.object,isIgnore);
		}
		if(node.computed ) {			
			val += "["+this.getValueBasedOnType(node.property,isIgnore)+"]"
		}			
		else if(node.property.name){
			val += "." + node.property.name;
		}
		return val;
	},

	getValueBasedOnType : function(node,isIgnore,switchcase,output,escape){
		switch(node.type){
			case "Identifier":
				return node.name;
			break;
			case "ThisExpression" : 
				return "this";
			break;
			case "Literal":
				if(escape){
					return node.raw.replace(/"/g,'\\"');
				}
				if(typeof node.value != "boolean" && typeof node.value != "number") {
					// console.log(node.raw.replace(/\\([\s\S])|(")/g,"\\$1$2"));
					if(switchcase) {
						return node.value.replace(/\ /g,'&#32;');
					} else if(!isIgnore) {
					 	return node.raw.replace(/\"/g,'&quot;');
					} else {
						return node.raw
					}
					// return "'"+node.value+"'";
				}
				return node.value;
			break;
			case "ObjectExpression":
				return this.handleObjectExpression(node,isIgnore,output);
				break;
			default : 
				return this.handleConditionStmt(node,isIgnore,output);
		}
	},

	handleObjectExpression: function(node,isIgnore,output){
		var obj = []; 
		var prop = node.properties;
		prop.forEach(function(item ,index){
			obj.push(this.getValueBasedOnType(item.key,isIgnore)+":"+this.getValueBasedOnType(item.value,isIgnore));
		});
		return "{"+obj.toString()+"}";
	},
	
	handleConditionStmt : function(node,isIgnore,output){ 
		var output = ""; 
		if(node.type == "LogicalExpression" || node.type == "BinaryExpression" || node.type =="AssignmentExpression"){
			output += "expHandlers(";
			output += this.handleConditionStmt(node.left,isIgnore);
			output += ",'" + this.checkOperator(node.operator,isIgnore)+"',";
			output += this.handleConditionStmt(node.right,isIgnore);
			output += ")";
		} else if(node.type == "CallExpression"){
			output += this.handleCallExpression(node,isIgnore);
		} else if(node.type == "Literal"){
			output += this.getValueBasedOnType(node,isIgnore);
		} else if(node.type === "MemberExpression"){
			var memberVal = this.handleMemberExpression(node,isIgnore);
			output += memberVal;
		} else if(node.type == "UpdateExpression"){
			var val = this.getValueBasedOnType(node.argument,isIgnore);
			output += "expHandlers("+val+",";
			output += this.getValueBasedOnOperation(node,isIgnore)+",";
			if(node.prefix == false) {
				output += "\"postfix\"";
			} else if(node.prefix == true) {
				output += "\"prefix\"";
			}
			output += ')';
		} else if(node.type == "Identifier") {
			output += node.name;
		} else if(node.type == "ThisExpression") {
			output += "this";
		} else if(node.type == "UnaryExpression") {
			output += "expHandlers("+this.getValueBasedOnType(node.argument,isIgnore)+",'" + node.operator +"')";
		}
		
		return output;
	},

	checkOperator : function(operator,isIgnore) {
		if(operator == ">") {
			operator = isIgnore ? ">" : '&gt;';
		} else if(operator ==">="){
			operator = isIgnore ? ">=" : '&gt;=';
		}
		return operator;
	},

	getValueBasedOnOperation : function(node,isIgnore,output) {
		if(node.operator) {
			return ("'"+node.operator+"'")
		}
	},

	handleExpression : function(node,isIgnore,output) {
		var fnbody = node.body;
		for(var i=0; i<fnbody.length; i++) {
			var type = fnbody[i].type;
			switch(type) {
				case "BlockStatement" : 
					output = this.handleExpression(fnbody[i],isIgnore);
					break;
				case "ExpressionStatement" :
					var exfnBodyType = fnbody[i].expression.type;
					if(exfnBodyType == "CallExpression"){
						output = this.handleCallExpression(fnbody[i].expression,isIgnore);
					} else if(exfnBodyType == "Identifier" || exfnBodyType === "ThisExpression") {
						output = this.getValueBasedOnType(fnbody[i].expression,isIgnore);
					} else if(exfnBodyType == "MemberExpression") {
						output = this.handleMemberExpression(fnbody[i].expression,isIgnore);
					} else {
						output = this.handleConditionStmt(fnbody[i].expression,isIgnore);
					}
				    break;
				case "IfStatement" :
					output = this.handleConditionStmt(fnbody[i].test,isIgnore);
					break;
				case "SwitchStatement" :
					output = this.handleConditionStmt(fnbody[i].discriminant,isIgnore);
					break;					
			}
		}
		return output;
	}	
}
if(true) {
	module.exports = expHandlers;	
}

/***/ }),

/***/ 93957260:
/*!*****************************************************************************!*\
  !*** ./node_modules/@slyte/component/src/compiler/cli/lyte-base-compile.js ***!
  \*****************************************************************************/
/***/ ((module) => {

// function BaseCompiler(){
    let Compile = {};
    Compile.regex = {
        get lyteTemplateType1() { return /if|for|switch/g; },
        get getArrayArgRegex() { return /\[(?=([\s]*[\w-_.]+)([\(\[]))/g; },
        get getArrayArgRegexCLI() { return /\[(?=([\s]*[\w-_]+)([\(\[]))/g; },
        get matchInsideParentheses() { return /\(.*\)$/; },
        get startsAndEndWithNumber() { return /^[0-9]$/; },
        get matchInsideSquareBrackets() { return /\[.*\]/; },
        get getHelperRegex() { return /\((?:[^\)]*|(?:(?:"(?:[^"\\]|\\.)*?")|(?:'([^'\\]|\\.)*?')|[\w\s!@#$%^&*)([\]+=.,_-]*?)*?)\)$/; },
        get matchAllEmptyCharacter() { return /\s/g; },
        get endsWithCloseParantheses() { return /\)$/g; },
        get startWithMustacheWithContent() { return /^{{(?=[\s]*[\w-_\(\$)]+)/; },
        get dynamicValueCheck() { return /[\w!@#\$%\^\&*\)\(+=.,_-]+[\s]*[(]{0,1}(?:"([^"]|\\")*?"|'([^']|\\')*?'|[\w\s!@#\$%\^\&*\)\(\[\]+=.,_-]*?)*?[)]{0,1}[\s]*(?=}})/g; },
        get validMustacheCheck() { return /{{[^}]*?(?:(?:('|")[^\1]*?\1)[^}]*?)*}}$/; },
        get mustacheCountCheck() { return /{{[a-zA-Z0-9_.\[\]\(\)]*(?![\\])}}/g; },
        get validateArrayIndexing() { return /[a-zA-z0-9]*\[.*\]/g; },
        get mustache() { return /{{[^}]*?(?:(?:[^\1]*?\1)[^}]*?)*}}/g; },
        get lyteTemplateType2() { return /(for|forIn|if|switch)$/; },
        get splitTextNodesMustache() { return /{{[^}]*?(?:(?:('|")[^\1]*?\1)[^}]*?)*}}/g; },
        get tableTags() { return /^(TR|TD|TH|TBODY|THEAD|TFOOT)$/; },
        get tableTagsWithSelect() { return /^(SELECT|TR|TABLE|TBODY|THEAD|TFOOT)$/; },
        get eventListRegexFull() { return /^(onfocus|onfocusin|onfocusout|onresize|onscroll|onclick|ondblclick|onmousedown|onmouseup|onmousemove|onmouseover|onmouseout|onchange|onselect|onsubmit|onkeydown|onkeypress|onkeyup|oncontextmenu|ondragstart|ondrag|ondragenter|ondragleave|ondragover|ondrop|ondragend|onload|onunload|onabort|onerror|onscroll|onreset|onblur|onafterprint|onbeforeprint|ononbeforeunload|onerror|onhashchange|onload|onmessage|onoffline|ononline|onpagehide|onpageshow|onpopstate|onstorage|onunload|oninput|oninvalid|onsearch|ononmousewheel|onwheel|ondrag|ondragend|ondragenter|ondragleave|ondragover|ondragstart|ondrop|onscroll|ondrag|ondragend|ondragenter|ondragleave|ondragover|ondragstart|ondrop|onscroll|ondrag|ondragend|ondragenter|ondragleave|ondragover|ondragstart|ondrop|onscroll|onabort|oncanplay|oncanplaythrough|oncuechange|ondurationchange|onemptied|onended|onloadeddata|onloadedmetadata|onloadstart|onpause|onplay|onplaying|onprogress|onratechange|onseeked|onseeking|onstalled|onsuspend|ontimeupdate|onvolumechange|onwaiting|onpaste)$/; },
        get eventListRegexPart() { return /^(onfocus|onfocusin|onfocusout|onresize|onscroll|onclick|ondblclick|onmousedown|onmouseup|onmousemove|onmouseover|onmouseout|onchange|onselect|onsubmit|onkeydown|onkeypress|onkeyup|oncontextmenu)$/; },
        get keyWordsRegex() { return /((const)|(continue)|(default)|(delete)|(do)|(else)|(enum)|(export)|(extends)|(finally)|(for)|(function)|(if)|(import)|(in)|(instanceof)|(new)|(return)|(super)|(switch)|(try)|(typeof)|(var)|(void)|(while)|(with))(?!\w)/g; },
        get lyteHelperRegex() { return /__LyteHelper(\w*?)__/g; },
        get tableElementRegex() { return /^(TBODY|THEAD|TFOOT)$/; },
        get getTransitionOrderRegex() { return /^f|fI|e|s|cD|cM|i|r|yield$/g; },
        get getChildRegex() { return /^f|fI|e|s|cD|i|r|yield$/g; },
        get startWithMustacheRegex() { return /^{{/; },
        get endWithMustacheRegex() { return /}}$/; },
        get baseCaseMustacheCheck() { return /{{.*}}/; },
        get commentedMustacheCheck() { return /\\{{.*}}/; }
    
    }
    Compile._registryNameList = [];
    Compile.getTrimmedContent = function(content, position, node) {
        let dummyContent = content;
        if(node) {
            position = [];
            let parentNode = node.parentNode;
            while(true) {
                position.$unshift(this.getArrayIndex(parentNode.childNodes,node));
                parentNode = parentNode.parentNode;
                node = node.parentNode;
                if(!parentNode) {
                    break;
                }
            }
        }
        for(let i=0;i<position.length;i++) {
            for(let j=content.childNodes.length-1;j>position[i];j--) {
                content.childNodes[j].remove();
            }
            content = content.childNodes[position[i]];
        }
        return dummyContent;
    }
    Compile.getArrayIndex = function(array,value) {
        for(let i=0;i<array.length;i++) {
            if(array[i] === value) {
                return i
            };
        }
    }

    Compile.splitMixedText = function(str,componentName,errors){
            // console.log("str",str);
        var stack = [],
        start = 0,
        flag = false,
        helper = {
            name: "concat",
            args: []
        },
        strStack = [],lastAdded,helperStarted;
        for (var i = 0; i < str.length; i++) {
            var j = i;
            if(helperStarted && str[i] === "'"){
                if(lastAdded === "'" && str[i-1] !== "\\"){
                    strStack.pop();
                    lastAdded = undefined;
                }
                else if(!strStack.length){
                    lastAdded = str[i];
                    strStack.push(lastAdded);
                }
            }
            else if(helperStarted && str[i] === "\""){
                if(lastAdded === "\"" && str[i-1] !== "\\"){
                    strStack.pop();
                    lastAdded = undefined;
                }
                else if(!strStack.length){
                    lastAdded = str[i];
                    strStack.push(lastAdded);
                }
            }
            else if( ((helperStarted && !strStack.length) || !helperStarted) && str[i-1] !== "\\" && str[i] === "{" && str[++i] === "{"){
                stack.push('{{');
                helper.args.push("'" + str.substr(start, j - start) + "'");
                start = i + 1;
                helperStarted = true;
            }
            else if( ((helperStarted && !strStack.length) || !helperStarted) && str[i] === "}" && str[++i] === "}" && stack.length){
                stack.pop(start);
                helperStarted = false;
                var toPush = str.substr(start, j - start);
                if((toPush.startsWith("'") && toPush.endsWith("'")) || (toPush.startsWith('"') && toPush.endsWith('"'))){
                    console.warn("Rendering string value inside dynamic data (mustache) is depriciated","for the value ",toPush ,"on the attribute value ",str);
                }
                var actObj  =  this.getArray(toPush);  
                if(actObj && actObj == "timeoutError"){
                    errors.push(new Error("(LC:timeout8)Syntax error in node "+node.node+" for the value: "+" - "+node.text));
                    return;
                }else if(actObj === false){
                    errors.push(new Error("(LC:14)Syntax Error in node "+node.node+"for the value:"+node.text));
                    return;
                }
                if(actObj){
                    toPush = actObj;
                    helper.args.push({type:"sq",value:actObj});
                    flag = true;
                }
                if(!actObj){
                    actObj = this.getHelper(toPush);  
                    if(actObj){
                        if(actObj == "timeoutError"){
                            errors.push(new Error("(LC:timeout5)Syntax Error in node "+"  for : "+str));
                            return;
                        }
                        else if(actObj.name == "lbind"){
                            errors.push(new Error("(LC:lbind5) Syntax Error : lbind are not allowed with prepending or appending with texts "+" for : "+str));
                            return;
                        }
                        else if(actObj == "lbindError" || actObj.args == "lbindError"){
                            errors.push(new Error("(LC:lbind6)Syntax Error : lbind are not allowed inside helpers on node "+node.node+" for: "+ str));
                            return;
                        }
                        else if(actObj.name == "lbind" && node.tagName == "TEMPLATE"&& node.hasAttribute("is") && this.regex.lyteTemplateType1.test(node.attr.is)){
                            errors.push(new Error("(LC:lbind7)Syntax Error : lbind not allowed as value in if/for/switch node"+"for: "+str));
                            return;
                        }
                        
                        else if(actObj.name == "action"){
                            errors.push(new Error("(LC:9)Syntax Error : action are not allowed with prepending or appending with texts "+"for :"+str));
                            return;
                        }
                        else if(actObj.name == "method"){
                            errors.push(new Error("(LC:10)Syntax Error : method are not allowed with prepending or appending with texts "+"for :"+str));
                            return;
                        }
                        toPush = actObj;
                        if(!this.fromCLI && this.regex.getArrayArgRegex.test(toPush)){
                            flag = true;
                        }
                        helper.args.push({ type: "helper", value: toPush })
                        if (this.fromCLI) {
                            if (this.defaultImpDetails.helpers.indexOf(actObj.name) == -1 && this.defaultHelpers.indexOf(actObj.name) == -1) {
                                this.defaultImpDetails.helpers.push(actObj.name);
                            }
                            this.getImportedHelpers(actObj.args, this.defaultImpDetails.helpers);
                        }
                    }
                    else if(actObj === false){
                        errors.push(new Error("(LC:13)Syntax error in node "+node.node+" for the attribute:"+node.attributes[x].nodeName+"- "+node.attributes[x].text));
                        return;
                    }
                    else{
                        if((toPush.startsWith("'") && toPush.endsWith("'")) || (toPush.startsWith('"') && toPush.endsWith('"'))){
                            console.warn("Rendering string value inside dynamic data (mustache) is depriciated","for the value ",toPush ,"on the attribute value ",str);
                        }
                        helper.args.push(toPush);
                    }
                    if(this.fromCLI){
                        start = i + 1;  
                    }
                }
                if (!this.fromCLI) {
                    start = i + 1;//checkWithMentor 3055 it should come up one step
                }
                // var actObj = getHelper(toPush, componentName);
                // if (actObj) {
                //     toPush = actObj;
                //     helper.args.push({
                //         type: "helper",
                //         value: toPush
                //     });
                // } else {
                //     helper.args.push(toPush);
                // }
                // start = i + 1;
            }

        }
        if (start < str.length) {
            helper.args.push("'" + str.substr(start, str.length - start) + "'");
        }
        if (flag) {
            helper._t = "hq";
            return helper;
        }
        else {
            return helper;
        }
    }

    Compile.getHelper = function(dynamicValue) {
        let helperValue = this.regex.getHelperRegex.exec(dynamicValue);
        if(helperValue){
            let actObj = this.getHelperInfo(dynamicValue,helperValue);
            if(this.regex.getArrayArgRegex.test(dynamicValue)){
                actObj._t = 'hq';
            }
            // actObj._importHelperArray = importHelperArray;
            return actObj;
        }
        return undefined;
    }
    

    Compile.getHelperInfo = function(dynamicValue, helperValue){
        let helperFunc = {};
        helperFunc.name = dynamicValue.substr(0, helperValue.index).replace(this.regex.matchAllEmptyCharacter, '');
        helperValue = (helperValue) ? helperValue[0].trim() : helperValue;
        let args = this.getHelperArgs(helperValue.substr(1,helperValue.length-2));
        if(helperFunc.name == "lyteViewPort" && helperValue == "()"){
            helperFunc.args = [];
            return helperFunc;
        }
        if (args === false) {
            return false;
        }
        helperFunc.args = args;
        return helperFunc;
    }

    Compile.getHelperArgs = function(str){
        let stack = [],
        escapeQuote = false,
        stringStarted = false,
        bracketStack = [],
         args = [],
          from = 0;
        let lastPushed;
        for(let i=0; i<str.length; i++){
            if(!stack.length && str.charAt(i) === ","){
                let toPush = str.substr(from,i-from);
                toPush = toPush.trim();
                if(toPush && toPush.startsWith("\"") && toPush.endsWith("\"")){
                    toPush = toPush.slice(1,-1);
                    toPush = "'" + toPush + "'";
                }
                try{
                    toPush = this.getHelperArgValue(toPush);
                }
                catch(err){
                    console.log("errr",err);
                    if(err.message == "lbindError"){
                        return "lbindError"
                    }
                }
                args.push(toPush);
                from = i + 1;
            }
            else if(!bracketStack.length && stringStarted && str.charAt(i) === "\\" && /\\|\'|\"/g.test(str.charAt(i+1))){
                str = str.substr(0,i)  + str.substr(i+1,str.length);
            }
            else if(str.charAt(i) === "\\" && /\\/g.test(str.charAt(i+1))){
                // str = str.substr(0,i)  + str.substr(i+1,str.length);
                // continue;
                i++;
            }
            else if(str.charAt(i) === "\\" && /\'|\"/g.test(str.charAt(i+1))){
                escapeQuote = true
            }
            else if(str.charAt(i) === "("){
                if(stack[stack.length - 1] != "'" && stack[stack.length - 1] != "\""){
                    stack.push(str.charAt(i));
                    lastPushed = str.charAt(i);
                    bracketStack.push("(");
                }
            }
            else if(str.charAt(i) === ")"){
                if(stack[stack.length-1] === "("){
                    stack.pop();
                    bracketStack.pop();
                }
            }
            else if(str.charAt(i) === "'"){// && str.charAt(i-1) !== "\\"
                if(escapeQuote){
                    escapeQuote = false;
                    continue;
                }
                else if(stack[stack.length-1] === "'"){
                    stringStarted = false
                    stack.pop();
                }
                else if(stack[stack.length-1] !== "\""){
                    stack.push(str.charAt(i));
                    lastPushed = str.charAt(i);
                    stringStarted = true;
                }
            }
            else if(str.charAt(i) === "\""){// && str.charAt(i-1) !== "\\"
                if(escapeQuote){
                    escapeQuote = false;
                    continue;
                }
                else if(stack[stack.length-1] === "\""){
                    stringStarted = false
                    stack.pop();
    //                  str.replaceAt(i, "'");
                }
                else if(stack[stack.length-1] !== "'"){
                    stack.push(str.charAt(i));
                    lastPushed = str.charAt(i);
                    stringStarted = true;
    //                  str.replaceAt(i, "'");
                }
            }
        }
        if (stack.length) {
            return false;
        }
        let toPush = str.substr(from, str.length - from);
        toPush = toPush.trim();
        if (toPush && toPush.startsWith("\"") && toPush.endsWith("\"")) {
            toPush = toPush.slice(1, -1);
            toPush = "'" + toPush + "'";
        }
        try{
            toPush = this.getHelperArgValue(toPush);
        }
        catch(err){
            console.log("errr",err);
            if(err.message == "lbindError"){
                return "lbindError"
            }
            return false;
        }
        args.push(toPush);
        return args;
    }
    
    Compile.getHelperArgValue = function(argValue) {
        switch (argValue) {
            case "undefined":
                return undefined
            case "true":
                return true;
            case "false":
                return false;
            case "null":
                return null;
            case "":
                return undefined;
            default:
                if (argValue && argValue.startsWith("'") && argValue.endsWith("'")) {
                    return (this.fromCLI)?this.decodeHtmlEntities.decode(argValue):argValue;
                }else if(this.fromCLI && this.regex.getArrayArgRegexCLI.test(argValue) && !this.regex.endsWithCloseParantheses.test(argValue)){// //array //if array inside helepr or array inside array it should work/\w+\[.*\]/g
                    let arg = this.getArrayArg(argValue);
                    return arg.args[0];
                } 
                else if(!this.fromCLI && this.regex.getArrayArgRegex.test(argValue) && !this.regex.endsWithCloseParantheses.test(argValue)){
                    let arg = this.getArrayArg(argValue);
                    return arg.args[0];
                }
                else if(/\([\w\s,')(]*/.test(argValue)) {
                    let arg = this.getHelper(argValue);
                    if(arg === false){
                        throw new Error(argValue);
                    }
                    if(arg.name == "lbind"){
                        throw new Error("lbindError");
                    }
                    return {"type" : "helper" , "value" : arg}
                } else if (!isNaN(argValue)) {
                    return parseInt(argValue);
                } else {
                    return argValue;
                }
        }
    }
    Compile.getMustache = function(value,componentName,syn){
        value=(value && typeof value === "string") ? value.trim() : value;
            if(this.regex.startWithMustacheWithContent.test(value)){
            let arr = value.match(this.regex.mustacheCountCheck);
                if(arr && arr.length > 1){
                    console.log("length>1",value)
                    return undefined;
                }
                // console.log(value);
                if(syn && !syn.bool){
                    console.log("syntax error",syn)
                    return {bool:false,err:syn.err};
                }
                if(!this.regex.validMustacheCheck.test(value)){
                    return undefined;
                }
                let dynamic = value.match(this.regex.dynamicValueCheck); // /* */
                if(dynamic && dynamic.length > 1){
                    return undefined;
                }
                else{
                    dynamic = (dynamic) ? dynamic[0].trim(): dynamic;                
                }
                // console.log("dynamic value before returning ", dynamic);
                return dynamic;
            }
            return undefined;
    }
    Compile.syntaxCheckWorkerNew = function(val){
        var parsingMustache;
        var retObj = {
            "mustache":0,
            bool : true,
            err : null
        }
        for(let i=0;i<val.length;i++){
            if(parsingMustache){
                if(val[i] == "\\"){
                    i=i+1;
                }
                else if(val[i] == "'"){
                    if(lastString != '"'){//val[i-1] != "\\"  && 
                        if(!stringStarted){
                            stringStarted = true;
                            lastString = "'";
                        }else{
                            stringStarted = false;
                            lastString = undefined;
                        }
                    }               
                }
                else if(val[i] == '"'){
                    if(lastString != "'"){//val[i-1] != "\\" &&
                        if(!stringStarted){
                            stringStarted = true;
                            lastString = '"'
                        }else{
                            stringStarted = false;
                            lastString = undefined;
                        }
                    }
                }
                else if(val[i] == "{" && !stringStarted){
                    retObj.bool = false;
                    retObj.err = " unexpected '{' bracket ";
                    return retObj;
                }
                else if(val[i] == "{" && stringStarted && val[i-1] == "{"){
                    retObj.mustache ++;
                }
                else if(val[i] == "}" && val[i-1] == "}" && !stringStarted){
                    if(curlyBracket.length){
                        retObj.bool = false;
                        retObj.err = " error unmatched '(' bracket ";
                        return retObj;
                    }
                    if(squareBracket.length){
                        retObj.bool = false;
                        retObj.err = " unmatched '['bracket ";
                        return retObj;
                    }
                    parsingMustache = false;
                }
                else if(val[i] == "[" && !stringStarted){
                    squareBracket.$push("[");
                }
                else if(val[i] == "]" && !stringStarted){
                    if(squareBracket.length){
                        squareBracket.$pop();
                    }else{
                        retObj.bool = false;
                        retObj.err = " unmatched ']' bracket ";
                        return retObj;
                    }
                }
                else if(val[i] == "(" && !stringStarted){
                    curlyBracket.$push('(');
                }
                else if(val[i] == ")" && !stringStarted){
                    if(curlyBracket.length){
                        curlyBracket.$pop();
                    }else{
                        retObj.bool = false;
                        retObj.err = " unmatched ')' bracket "; 
                        return retObj;
                    }
                }
            }    
            else if((val[i] == "{" && val[i-1] == "{" && val[i-2] != "\\")){
                //start parsing
                var curlyBracket =[] ,squareBracket =[] ,stringStarted ,lastString;
                var parsingMustache = true;
            }
        }
        if(parsingMustache){
            retObj.bool = false;
            retObj.err = " unmatched '{' bracket";
            return retObj;
        }
        return retObj;
    }
    Compile.getArray = function(mustacheVal){
        // becomses true if it is arr[arr[0]] or arr[helper()]
        if(this.regex.getArrayArgRegex.test(mustacheVal) && !this.regex.endsWithCloseParantheses.test(mustacheVal)){
            let arg = this.getArrayArg(mustacheVal);
            if(arg && arg.args[0]){
                arg.args[0].value._t = "sq";
                return arg.args[0].value;
            }else{
                return false;
            }
        }
    }
    Compile.splitMultiDimensionArray = function(val,arrayExtra){
        var openSq = 0,openSqPos = 0,lastString,stringStarted,openSPos,resultStack = [],extra = false,extraPos;
        for(var i=0 ; i<val.length;i++){
            if(val[i] == "\\"){
                i=i+1;
            }
            else if(val[i] == "'"){
                if(lastString != '"'){//val[i-1] != "\\"  &&
                    if(!stringStarted){
                        stringStarted = true;
                        lastString = "'";
                    }else{
                        stringStarted = false;
                        lastString = undefined;
                    }
                }               
            }
            else if(val[i] == '"'){
                if(lastString != "'"){//val[i-1] != "\\" &&
                    if(!stringStarted){
                        stringStarted = true;
                        lastString = '"'
                    }else{
                        stringStarted = false;
                        lastString = undefined;
                    }
                }
            }
            else if(val[i] == "[" && !stringStarted){
                openSq = openSq + 1;
                if(openSq == 1){
                    openSqPos = i;
                }
            }
            else if(val[i] == "]" && !stringStarted){
                openSq = openSq - 1;
                if(!extra && openSq == 0){
                    resultStack.push(val.substring(openSqPos+1,i));//need to handlw []
                    openSq = 0;
                    if(val[i+1]=='.'){
                        extra = true;
                        extraPos = i+1;
                    }
                }
            }
        }
        if(extra){
            arrayExtra.push(val.substring(extraPos,i));//need to handlw []
        }
        return resultStack;
    }
    Compile.getArrayArg = function(argStr){
        //argStr may be 0 or '0' or 'abc' or a() or a[] or a(b[]) or a[b()]
        // var arg = {"type":"sq","value":{"args":[],"name":""}},arrayName;
        if(this.regex.matchInsideParentheses.test(argStr)){
            //if arr comes inside helper
            let arg = this.getHelper(argStr);  
            if(arg === false){
                return false;
            };
            return {"args":[{ "type": "helper", "value": arg }]}
        }
        else if(this.regex.validateArrayIndexing.test(argStr)){
            let arg = {'args':[]}, ret = [], temp,arrayExtra=[],retNew = [];
            let arrayArg = this.regex.matchInsideSquareBrackets.exec(argStr);
            let arrayName = argStr.substr(0,arrayArg.index);
            // arrayArg = arrayArg[0];
            //splitting multi dimensional array a[one][two][three] into array
            var arr = this.splitMultiDimensionArray(argStr, arrayExtra);
            for(var i=0; i<arr.length; i++){
                if(this.regex.getArrayArgRegex.test(arr[i]) || this.regex.matchInsideParentheses.test(arr[i])){
                    temp = this.getArrayArg(arr[i]);
                    ret.push(temp.args[0]);
                }
                else{
                    ret.push(arr[i])
                }
            }
            for(var i=0; i<arrayExtra.length; i++){
                if(this.regex.getArrayArgRegex.test(arrayExtra[i])){
                    temp = this.getArrayArg(arrayExtra[i]);
                    retNew.push(temp.args[0])
                }else{
                    retNew.push(arrayExtra[i])
                }
            }
            ret.forEach(function(item,index){
                if (item && item && typeof item == "string" && item.startsWith("\"") && item.endsWith("\"")) {
                    item = item.slice(1, -1);
                    item = "'" + item + "'";
                }else if(typeof item == "string" && Compile.regex.startsAndEndWithNumber.test(item)){
                    item = "'" + item + "'";
                }
                arg.args.push(item)
            })
            arg.name = arrayName;
            if(retNew && retNew.length > 0){
                arg.extra = retNew;
            }
            return {"args":[{"type" : "sq" , "value" : arg}]};
        } 
    }
    Compile.getDV = function(value){
        var result = [],ref = result,arr = [],data = "",strStack = [],arrayStack = [],refStack = [],strLast,str;
        for(var i=0;i<value.length;i++){
            if(value[i] === "."){
                if(data.length){
                    ref.$push(data);
                }
                data = "";
                continue;
            }
            else if(value[i] === "["){
                arrayStack.$push(i)
                if(data.length){
                    ref.$push(data);
                }
                while(value[i+1] === "\s"){
                    i++;
                }
                if(value[i+1] === "\"" || value[i+1] === "'"){
                    strStack.$push(value[i+1]);
                    strLast = value[i+1];
                    i++;
                }
                else if(arr.length){
                    ref.$push([]);
                    refStack.$push(ref);
                    ref = ref[ref.length-1];
                }else{
                    arr.$push([]);
                    refStack.$push(ref);
                    ref = arr[arr.length-1];
                }
                data = "";
                continue;
            }
            else if((value[i] === "\"" || value[i] === "'" ) && value[i++] === strLast){
                while(value[i] === "\s" && value[i] != "]"){
                    i++;
                }
                strStack.$pop();
                str = true;
            }
            if(value[i] === "]"){
                arrayStack.$pop();
                if(data.length){
                    if(str === true){
                        ref.$push(data);    
                    }
                    else if(!isNaN(parseInt(data))){
                        if(refStack.length){
                            ref = refStack.$pop();
                            if(arr.length && Array.isArray(ref[ref.length-1]) && !ref[ref.length-1].length){
                                ref.$pop();
                            }
                            ref.$push(data);
                            if(!arrayStack.length && arr.length){
                                arr.$shift();
                            }
                        }
                    }
                    else{
                        ref.$push(data);
                    }
                }
                if(!arrayStack.length && arr.length){
                    result.$push(arr.$shift());
                    ref = result;
                }
                else if(refStack.length && !arr.length){ 
                    ref = refStack.$pop();
                }
                data = "";
                str = "";
                continue;
            }
            data = data.concat(value[i]);
        }
        if(data.length){
            result.$push(data);
        }
        if(strStack.length || arrayStack.length){
            console.log("check the syntax",strStack,arrayStack);
        }
        return result;
    }
    // return Compile;
// }

if(  true && typeof module.exports === "object" ) {
    module.exports = Compile;
}else{
    window.BaseCompiler = Compile;
}
// BaseCompiler();


/***/ }),

/***/ 92997744:
/*!*******************************************************************************!*\
  !*** ./node_modules/@slyte/component/src/compiler/cli/lyte-client-compile.js ***!
  \*******************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

// import { Lyte } from "@slyte/core";
// import { Compile } from '@slyte/component/src/lyte-base-compile.js';
// import { expHandlers } from "../expHandlers/js";
// import { _LC, doCompile} from "./lyte-component.js";
// import { _LC,doCompile ,expHandlers,Espree } from "./lyte-component";
// function ClientCompiler(Lyte, _LC, Compile,doCompile ,expHandlers,Espree) {
    // if(!document){
    //     var document = undefined;
    // }
    // if(!window){
    //     var window = {};
    // }
    let Compile = {};
    Compile.fromCLI = undefined;
    Compile.defaultImpDetails = {
        components: [],
        helpers: [],
        alreadyImported: false,
        found : {}
    };
    Compile.htmlStr = {},
    Compile.pushDir = function(dirName){
        if(this.defaultDirectives.indexOf(dirName) != -1){
            let actDirName = this.dirMap[dirName];
            if(actDirName && this.impObj.defaultDirectives.indexOf(actDirName) == -1){
                this.impObj.defaultDirectives.push(actDirName);
            }
            // else if(this.impObj.defaultDirectives.indexOf(dirName) == -1){
            //     this.impObj.defaultDirectives.push(dirName);
            // }
        }
        else if(this.impObj.directives.indexOf(dirName) == -1){
            this.impObj.directives.push(dirName);
        }
    }
    Compile.dirMap = {
        "shadow-supported" : "lyte-shadow",
        "shadow" : "lyte-shadow",
        "turbo-supported" : "lyte-turbo",
        "unbound" : "lyte-turbo",
        "turbo" : "lyte-turbo",
        "view-in" : "lyte-view",
        "view-out" : "lyte-view",
        "prop" : "lyte-save",
        "node" : "lyte-save",
        "data" : "lyte-save",
        "hide-tag" : "lyte-hide",
        "hide-tag-supported" : "lyte-hide",
        "elemental" : "lyte-elemental"
    }
    Compile.impObj = {
        "svgLoader": [],
        "components": [],
        "helpers": [],
        "directives" : [],
        "defaultDirectives" : [],
        "defaultComponents" : [],
        "registryMap": {},
        "lyteRegistries" : {},
        "options" : {
            "internalSlicer" : {
                $bundlePurifier : false,
                $bundleEncoder : false,
                $bundleElemental : false
            }
        }
    };
    Compile.debug = true;
    Compile.needDummyComponentsDiv = true;
    Compile.defaultComponents = ["link-to"];
    Compile.defaultSrcComponents = ["lyte-event-listener","import-shadow-style"];
    Compile.defaultHelpers = ["unbound", "action", "lbind", "method", "unescape", "escape", "debugger", "log", "ifEquals", "if", "negate", "ifNotEquals", 'concat', 'encAttr', 'expHandlers'];
    Compile.defaultDirectives = ["shadow","shadow-supported","turbo","turbo-supported","view-in","view-out","unbound","elemental","data","prop","node","hide-tag","hide-tag-supported"];
    Compile.addUnbound = function(node,template,tagName,parentElementName){
        let hasUnbound,unboundVal,hasFastRender,fastRenderVal;
        if(node.hasAttribute("@unbound")){
            hasUnbound = true;
            unboundVal  = node.getAttribute("@unbound");
            node.removeAttribute("@unbound");
        }
        if(node.hasAttribute("@turbo")){
            hasFastRender = true;
            fastRenderVal  = node.getAttribute("@turbo");
            node.removeAttribute("@turbo");
        }
        if(hasUnbound || hasFastRender){
            let unboundHtml;
            if(unboundVal || fastRenderVal){
                unboundHtml = "<" + tagName;
                if(hasUnbound){
                    if(unboundVal){
                        unboundHtml = unboundHtml + " @unbound=" + unboundVal;
                    }else{
                        unboundHtml = unboundHtml +" @unbound";
                    }
                }
                if(hasFastRender){
                    if(fastRenderVal){
                        unboundHtml = unboundHtml + " @turbo=" + fastRenderVal;
                    }else{
                        unboundHtml = unboundHtml +" @turbo";
                    }
                }
                unboundHtml = unboundHtml + ">" + "</" + tagName + ">";
            }else if(hasUnbound){
                unboundHtml = "<"+ tagName +" @unbound></"+ tagName +">";
            }else if(hasFastRender){
                unboundHtml = "<"+ tagName +" @turbo></"+ tagName +">";
            }
            let root = "div";
            if(parentElementName){
                root = parentElementName;
            }
            let div = document.createElement(root);
            div.innerHTML = unboundHtml;
            template = div.firstChild;
        }else{
            template = document.createElement(tagName);
        }
        return template;
    }
    
    Compile.getImpDetails = function (node) {
        var obj = {};
        var name = node.attr.name;
        var from = node.hasAttribute("from") ? node.attr.from : false;
        try {
            name = JSON.parse(name);
        } catch (e) {

        }
        if (!Array.isArray(name)) {
            name = [name];
        }
        obj.name = name;
        if (from) {
            obj.from = from;
        }
        return obj;
    }
    Compile.impDetails = function (node, impObj) {
        var nodeType = node.tag;
        var obj = {};
        let compArr = impObj.components;
        let helpersArr = impObj.helpers;
        let pushToObj = function (obj, arr) {
            let names = obj.name;
            let length = names.length;
            for (let i = 0; i < length; i++) {
                impObj.registryMap[names[i]] = obj.from;
            }
            // arr.push(...names);
            names.forEach(function(name){
                arr.push(name);
            })
        }
        switch (nodeType) {
            case "import-component": {
                obj = this.getImpDetails(node);
                pushToObj(obj, compArr);
            }
            break;
        case "import-helper": {
            obj = this.getImpDetails(node);
            pushToObj(obj, helpersArr);
        }
        break;
        }
    }
    Compile.checkForMixedCase = function(val) {
        let seenDoubleQuotes = false,
            seenSingleQuotes = false,
            mustacheStarted = false,
            mixedCase = false;
        let slashCount = 0;
        let mustacheSpots = [],res = "",mus={};
        for (var i = 0; i < val.length; i++) {
            //remove spaces other than present in double and single quotes
            if (mustacheStarted && ((seenDoubleQuotes || seenSingleQuotes) || val[i].match(/\S/))) {
                res += val[i];
            }
            if (val[i - 1] != "\\" && val[i] == "{" && val[i + 1] == "{" && !seenDoubleQuotes && !seenSingleQuotes) {
                if(mustacheStarted){
                    return {
                        bool:false,
                        errorIndex:i-mus.index[0]+1,
                        err:"Invalid dynamic value inside dynamic value `{{`"
                    };
                }
                if (!mixedCase && i != 0) {
                    mixedCase = true
                }
                mus = {}
                mus.index = [i];
                // spot[0] = i;
                mustacheStarted = true;
            }
            if (val[i] == "}" && val[i + 1] == "}" && !seenDoubleQuotes && !seenSingleQuotes && mustacheStarted) {
                if (!mixedCase && i + 1 != val.length - 1) {
                    mixedCase = true
                }
                // spot[1] = i + 1;
                mus.index[1] = i + 1;
                mus.orgMus = val.substring(mus.index[0],mus.index[1]+1);
                mus.mus = "{"+res+"}";
                res = "";
                mustacheSpots.push(mus);
                mustacheStarted = false;
            }
            if (val[i] == "\\") {
                slashCount++;
            }
            if (val[i - 1] != "\\" || (val[i - 1] == "\\" && slashCount % 2 == 0)) {
                if (val[i] == "'" && !seenDoubleQuotes && mustacheStarted) {
                    seenSingleQuotes = !seenSingleQuotes;
                }
                if (val[i] == '"' && !seenSingleQuotes && mustacheStarted) {
                    seenDoubleQuotes = !seenDoubleQuotes;
                }
            }
            if (val[i] != "\\") {
                slashCount = 0;
            }
    
        }
        return {
            bool:true,
            mixedCase: mixedCase,
            mustacheSpots: mustacheSpots
        };
    }
    Compile.processAttr = function (componentName, node, deepNodes, deepN, is, strict, errors, warnings) {
        if (node.hasAttributes && node.hasAttributes()) { //template has no attributes
            let add = false,
                toBeRemoved = [],
                toBeAdded = [],
                tempObj = {};
            var attr = {};
            let nodeAttrType = node.attrType;
            for (let i = 0; i < node.attributes.length; i++) {
                let fnFlag;
                let eventListRegex = this.regex.eventListRegexFull;
                if (eventListRegex.test(node.attributes[i].nodeName) && strict) {
                    fnFlag = true;
                }
                if (node.attributes[i].nodeValue.indexOf("{{") !== -1) {
                    let actObj, actValue;
                    let val = node.attributes[i].nodeValue;
                    if (this.fromCLI) {
                        if (nodeAttrType[node.attributes[i].nodeName] == "Mixed") {
                            actObj = this.splitMixedText(val, componentName, errors);
                        } else if (nodeAttrType[node.attributes[i].nodeName] == "helper") {
                            actValue = this.getMustache(val, {
                                bool: true
                            });
                            if (actValue) {
                                actObj = this.getArray(actValue);
                                if (actObj && actObj == "timeoutError") {
                                    errors.push(new Error("(LC:timeout7)Syntax error in node " + node.node + " for the attribute: " + node.attributes[i].nodeName + " - " + node.attributes[i].text));
                                    continue;
                                } else if (actObj === false) {
                                    errors.push(new Error("(LC:12)Syntax error in node " + node.node + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].text));
                                    continue;
                                }
                            }
                            if (!actObj && actValue) {
                                actObj = this.getHelper(actValue);
                                actValue = actValue.replace('____lyteinternal____', '.{}');
                                if (actObj && actObj.args == "lbindError" || actObj == "lbindError") {
                                    errors.push(new Error("(LC:lbind4)Syntax Error : lbind are not allowed inside helpers on node " + node.node + " for: " + node.attributes[i].text));
                                    continue;
                                } else if (actObj && actObj.name == "lbind" && node.tagName == "TEMPLATE" && node.hasAttribute("is") && this.regex.lyteTemplateType1.test(node.attr.is)) {
                                    errors.push(new Error("(LC:lbind3)Syntax Error : lbind not allowed as value in if/for/switch node" + "for: " + val));
                                } else if (actObj && actObj == "timeoutError") {
                                    errors.push(new Error("(LC:timeout4)Syntax error in node " + node.node + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].text));
                                    continue;
                                } else if (actObj === false) {
                                    errors.push(new Error("(LC:7)Syntax error in node " + node.node + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].text));
                                    continue;
                                }
                                if (actObj) {
                                    let arr = [];
                                    Compile.getAllHelperNames(actObj,arr);
                                    let self = this;
                                    arr.forEach(function(hName){
                                        if(hName == "unescape"){
                                            self.defaultImpDetails.found.purifier = true;
                                        }else if(hName == "escape"){
                                            self.defaultImpDetails.found.encoder = true;
                                        }
                                    })
                                    if (this.defaultImpDetails.helpers.indexOf(actObj.name) == -1 && this.defaultHelpers.indexOf(actObj.name) == -1) {
                                        this.defaultImpDetails.helpers.push(actObj.name);
                                    }
                                    this.getImportedHelpers(actObj.args, this.defaultImpDetails.helpers);
                                }
                            }
                        } else if (nodeAttrType[node.attributes[i].nodeName] == "Multi-dimensional array") {
                            actObj = this.getArray(val);
                            if (actObj && actObj == "timeoutError") {
                                errors.push(new Error("(LC:timeout7)Syntax error in node " + node.node + " for the attribute: " + node.attributes[i].nodeName + " - " + node.attributes[i].text));
                                continue;
                            } else if (actObj === false) {
                                errors.push(new Error("(LC:12)Syntax error in node " + node.node + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].text));
                                continue;
                            }
                            if (!actObj) {
                                actValue = this.getMustache(val, {
                                    bool: true
                                });
                                if (actValue && typeof actValue === 'object') {
                                    if (actValue.bool === false) {
                                        continue;
                                    }
                                }
                            }
                        } else {
                            actValue = this.getMustache(val, {
                                bool: true
                            });
                            if (actValue && typeof actValue === 'object') {
                                if (actValue.bool === false) {
                                    // errors.push(new Error("Syntax error in node "+actValue.err+" in "+node.node+" for the attribute: "+node.attributes[i].nodeName+" - "+node.attributes[i].text +" in the component "+componentName));
                                    continue;
                                }
                            }
                        }
                    } else {
                        node.attributes[i].nodeValue = node.attributes[i].nodeValue.replace('.{}', '____lyteinternal____').trim();
                        val = node.attributes[i].nodeValue;
                        let res = this.checkForMixedCase(val);
                        if (res.bool) {
                            let {
                                mustacheSpots
                            } = res;
                            if (mustacheSpots.length && mustacheSpots.length > 0) {
                                for (let j = 0; j < mustacheSpots.length; j++) {
                                    let x = mustacheSpots[j].mus
                                    if(x.match(/\&|\||\!|\=|\+|-|\*|\/|<|>|\?|:|%/gm)){
                                        x = this.trimAttr(x, errors, node, node.attributes[i], componentName);
                                    }
                                    val = val.replace(mustacheSpots[j].orgMus,function(){return x});
                                }
                            }
                            node.attributes[i].nodeValue = val;
                        }
                        let syn = this.syntaxCheckWorkerNew(node.attributes[i].nodeValue);
                        if (!syn.bool) {
                            errors.push(new Error("(LC:syn2)Syntax error in node " + syn.err + " in " + node.nodeName + " for the attribute: " + node.attributes[i].nodeName + " - " + node.attributes[i].nodeValue + " in the component " + componentName));
                            continue;
                        }
                        var splittedMus = val.split("{{");
                        var splittedMusLen = splittedMus.length;
                        if (syn && syn.mustache > 0) {
                            splittedMusLen = splittedMusLen - syn.mustache;
                        }
                        var ind = val.indexOf("}}");
                        if ((splittedMusLen > 2 || !this.regex.startWithMustacheRegex.test(val) || !this.regex.endWithMustacheRegex.test(val)) || val[ind + 2] && this.regex.baseCaseMustacheCheck.test(val) && !this.regex.commentedMustacheCheck.test(val)) {
                            actObj = this.splitMixedText(val, componentName, errors, node, i);
                        } else {
                            actValue = this.getMustache(val, componentName, syn);
                            if (actValue && actValue == "timeoutError") {
                                errors.push(new Error("(LC:timeout3)Syntax error in node " + actValue.err + " in " + node.nodeName + " for the attribute: " + node.attributes[i].nodeName + " - " + node.attributes[i].nodeValue));
                                continue;
                            }
                            console.log("actValue response " + actValue + "val given to mustache" + val)
                            if (actValue && typeof actValue === 'object') {
                                if (actValue.bool === false) {
                                    errors.push(new Error("(LC:6)Syntax error in node " + actValue.err + " in " + node.nodeName + " for the attribute: " + node.attributes[i].nodeName + " - " + node.attributes[i].nodeValue));
                                    continue;
                                }
                            }
                            if (actValue) {
                                actObj = this.getArray(actValue);
                                if (actObj && actObj == "timeoutError") {
                                    errors.push(new Error("(LC:timeout7)Syntax error in node " + node.nodeName + " for the attribute: " + node.attributes[i].nodeName + " - " + node.attributes[i].nodeValue));
                                    continue;
                                } else if (actObj === false) {
                                    errors.push(new Error("(LC:12)Syntax error in node " + node.nodeName + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].nodeValue));
                                    continue;
                                }
                            }
                            if (!actObj && actValue) {
                                actValue = actValue.replace('____lyteinternal____', '.{}');
                                actObj = this.getHelper(actValue);
                                if (actObj && actObj.args == "lbindError" || actObj == "lbindError") {
                                    errors.push(new Error("(LC:lbind4)Syntax Error : lbind are not allowed inside helpers on node " + node.nodeName + " for: " + node.attributes[i].nodeValue));
                                    continue;
                                } else if (actObj && actObj.name == "lbind" && node.tagName == "TEMPLATE" && node.hasAttribute("is") && this.regex.lyteTemplateType1.test(node.getAttribute("is"))) {
                                    errors.push(new Error("(LC:lbind3)Syntax Error : lbind not allowed as value in if/for/switch node" + "for: " + val));
                                } else if (actObj && actObj == "timeoutError") {
                                    errors.push(new Error("(LC:timeout4)Syntax error in node " + node.nodeName + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].nodeValue));
                                    continue;
                                } else if (actObj === false) {
                                    errors.push(new Error("(LC:7)Syntax error in node " + node.nodeName + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].nodeValue));
                                    continue;
                                }
                            }
                        }
                    }
                    if (actObj && (actObj.name === "action" || actObj.name === "method") && this.regex.eventListRegexPart.test(node.attributes[i].name)) {
                        add = true;
                        if (!this.fromCLI) {
                            attr[node.attributes[i].name.substr(2)] = {
                                name: node.attributes[i].name.substr(2),
                                helperInfo: actObj,
                                globalEvent: true
                            };
                        }
                        let actArgs = this.deepCopyObject(actObj.args);
                        let actName = actArgs.splice(0, 1)[0];
                        if (typeof actName != 'string' || !actName.startsWith("'")) {
                            errors.push(new Error(" action or method must start with string on " + node.nodeName + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].nodeValue));
                        } else {
                            actName = actName.startsWith("'") ? actName.replace(/'/g, '') : actName;
                        }
                        "came in 2033"
                        //node.setAttribute(node.attributes[i].name.substr(2),componentName+" => "+ actString);
                        toBeRemoved.push(node.attributes[i].name);
                    } else {
                        if (actObj && (actObj.name === "action" || actObj.name === "method")) {
                            if (actObj.args && (typeof actObj.args[0] === 'object' || !actObj.args[0].startsWith("'"))) {
                                warnings.push({
                                    message: " action or method must start with string on " + node.nodeName + " for the attribute:" + node.attributes[i].nodeName + "- " + node.attributes[i].nodeValue
                                });
                            }
                        }
                        if (actObj || actValue) {
                            add = true;
                            let attrToPush = {};
                            if (node.attributes[i].name.startsWith("lbind:")) {
                                toBeRemoved.push(node.attributes[i].name);
                                toBeAdded.push({
                                    "name": node.attributes[i].name.substring(6),
                                    "value": node.attributes[i].nodeValue
                                });
                                attrToPush.isLbind = true;
                                attrToPush.name = node.attributes[i].name.substring(6);
                            } else {
                                attrToPush.name = node.attributes[i].name;
                            }
                            if (actObj) {
                                if (actObj.name === "lbind") {
                                    attrToPush.dynamicValue = actObj.args[0];
                                    attrToPush.isLbind = true;
                                } else {
                                    attrToPush.helperInfo = actObj;
                                }
                            } else {
                                attrToPush.dynamicValue = actValue;
                                //                              LN to do
                                //                              attrToPush.dynamicValue = getDynamicValue(actValue);
                            }
                            add = true;
                            attr[attrToPush.name] = attrToPush;
                        }
                    }
                    node.attributes[i].nodeValue = node.attributes[i].nodeValue.replace('____lyteinternal____', '.{}');
                    if (fnFlag && !(actObj && (actObj.name === "action"))) {
                        let err = new Error("(LC:22)Strict-Mode: Please specify action in " + node.nodeName + " for attribute-" + node.attributes[i].nodeName);
                        err.strict = true;
                        errors.push(err);
                    }
                } else {
                    if (fnFlag) {
                        let err = new Error("(LC:23)Strict-Mode: Please specify action in " + node.nodeName + " for attribute-" + node.attributes[i].nodeName);
                        err.strict = true;
                        errors.push(err);
                    }
                }
                if (node.attributes[i].name.startsWith("@")) {
                    add = true;
                    node._special = true;
                    let attrToPush = {};
                    attrToPush.name = node.attributes[i].name;
                    attrToPush.dynamicValue = node.attributes[i].nodeValue;
                    attr[attrToPush.name] = attrToPush;
                    attr._special = true;
                    let directiveName = attrToPush.name;
                    directiveName = directiveName.substring(1) 
                    if(directiveName == "prop" || directiveName == "node" || directiveName == "data"){
                        let obj = {
                            nodeName : node.localName,
                            propName : attrToPush.dynamicValue
                        }
                        this.getParentFor(node,obj,{dirName : directiveName, dirValue : attrToPush.dynamicValue});
                    }
                }
            }
            
            if (attr && add) {
                let dummyAttr = {};
                let keys = Object.keys(attr);
                if (keys.indexOf("style") !== -1) {
                    dummyAttr.style = attr.style;
                }
                if (keys.indexOf("type") !== -1 && (node.tagName === "BUTTON" || node.tagName === "INPUT")) {
                    dummyAttr.t = attr.type;
                }
                if (node.tagName === "INPUT" && node.getAttribute("type") === "number") {
                    dummyAttr.value = attr.value;
                }
                if (keys.indexOf("placeholder") !== -1 && node.tagName === "TEXTAREA") {
                    node.removeAttribute("placeholder");
                    dummyAttr.placeholder = attr.placeholder;
                }
                let obj = {
                    "t": "a",
                    p: deepN.slice()
                };
                if (node._special) {
                    obj.trans = true;
                }
                if (Object.keys(dummyAttr).length) {
                    obj.a = dummyAttr;
                }
                deepNodes.push(obj);
            }
        }
        return attr;
    }
    Compile.getParentFor = function(node,propObj,propDetails){
        let newNode = node;
        while(newNode){
            let parentNode = newNode._parent;
            if(parentNode){
                if(parentNode && parentNode.tagName && parentNode.tagName == "TEMPLATE" && (parentNode.getAttribute("is") == "for" || parentNode.getAttribute("is") == "forIn")){
                    propObj.parent = {
                        nodeId : parentNode.getAttribute("node-id"),
                        nodeType  : parentNode.getAttribute("is")
                    }
                    if(propDetails){
                        if(!parentNode._dirObj){
                            parentNode._dirObj = {};
                        }
                        if(!parentNode._dirObj[propDetails.dirName]){
                            parentNode._dirObj[propDetails.dirName] = []
                        }
                        parentNode._dirObj[propDetails.dirName].push(propDetails.dirValue);
                    }
                    propObj = propObj.parent;
                }
            }
            newNode = parentNode;
        }
    }
    Compile.getImportedHelpers = function (array, helperArr) {
        for (let i = 0; i < array.length; i++) {
            if (array[i] && typeof array[i] === "object") {
                let helperName = array[i].value.name;
                if (helperArr.indexOf(helperName) == -1 && this.defaultHelpers.indexOf(helperName) == -1) {
                    helperArr.push(helperName);
                }
                array[i] = this.getImportedHelpers(array[i].value.args, helperArr);
            }
        }
    }
    Compile.elifHold = false;
    Compile.getArgString = function (name, array) {
        let retString;
        for (let i = 0; i < array.length; i++) {
            // console.log("array content"+JSON.stringify(array[i]));
            if (array[i] && typeof array[i] === "object") {
                array[i] = this.getArgString(array[i].value.name, array[i].value.args);
            }
        }
        if (name) {
            retString = name + "(" + array.toString() + ")";
        } else {
            retString = array.toString();
        }
        return retString;
    }
    Compile.trimAttr = function (tempVal, errors, node, attrNode, componentName, check = {}) {
        let trimmedVal;
        trimmedVal = tempVal.replace(this.regex.keyWordsRegex, '__LyteHelper$1__')
        try {
            let expOutput;
            // if (check.fromParser) {
            expOutput = this.expHandlers.handleExpression(this.Espree.parse(trimmedVal), true);
            // } else {
            //     expOutput = expHandlers.handleExpression(Espree.parse(trimmedVal), true);
            // }
            if (expOutput && (expOutput !== trimmedVal)) {
                expOutput = expOutput.replace(this.regex.lyteHelperRegex, '$1');
                tempVal = "{{" + expOutput + "}}";
            }
        } catch (e) {
            trimmedVal = trimmedVal.replace(this.regex.lyteHelperRegex, '$1');
            tempVal = trimmedVal;
            if (check.fromParser && !e.message.match(/^Unexpected token \.\d+$/gm)) {
                capturedError = {
                    bool: false,
                    err: e.message,
                    warning: null,
                    errorIndex: e.index
                };
            } else if (!check.fromParser && e.message === "Only helper function calls are allowed") {
                if (attrNode) {
                    e.message = "(LC:1)Syntax error in node " + node.nodeName + " for the attribute: " + attrNode.nodeName + " - " + attrNode.nodeValue + ". Only helper function calls are allowed"
                    errors.push(new Error(e));
                } else {
                    e.message = "(LC:2)Syntax Error in node " + node.nodeName + " for :" + node.nodeValue + ". Only helper function calls are allowed"
                    errors.push(new Error(e));
                }
            }
        }
        return tempVal;
    }
    // window.pendingComponents = [];

    Compile.getDynamicNodes = function(
        fileName,
        resolve,
        templateToRender,
        fromCLIFlag,
        errorObj,
        elementalComponent
    ) {

        this.fromCLI = fromCLIFlag;
        if( fromCLIFlag ) {
            __webpack_require__.g.window = {};
        }
        else if(typeof WorkerGlobalScope !== 'undefined' && self instanceof WorkerGlobalScope) {
            globalThis.window = {};
        }
        window.pending = true;
        let returnValue, missingComp;
        let s, comp, errors = [],
            warnings = (this.fromCLI) ? errorObj.warnings : [];
        if (templateToRender && !this.fromCLI) {
            comp = templateToRender;
        } else {
            if (typeof WorkerGlobalScope !== 'undefined' && self instanceof WorkerGlobalScope) {
                globalThis.document = templateToRender;
            }
            else{
                __webpack_require__.g.document = templateToRender;
            }
            if(elementalComponent){
                comp = document.querySelector("template[@elemental='" + fileName + "']")    
            }else{
                comp = document.querySelector("template[tag-name='" + fileName + "']")
            }
        }
        let elementals = [];
        //console.log("components generating dynamicNodes"+fileName)
        if (!comp) {
            missingComp = fileName;
            errors.push(new Error("Cannot find template " + fileName));
            returnValue = {
                componentName: fileName,
                errors: errors,
                warning: warnings
            };
        } else {
            if(!elementalComponent){
                var nextElement = comp.nextElementSibling;
                if(nextElement && nextElement.tagName == "TEMPLATE" && nextElement.hasAttribute("view-port-template")) {
                    var viewPortIf = document.createElement("template");
                    viewPortIf.setAttribute("lyte-if", "{{lyteViewPort}}");
                    viewPortIf.setAttribute("vpc","true");
                    viewPortIf.content.appendChild(document.createElement("dummy-port-element"));
                    viewPortIf.content.appendChild(nextElement.content);
                    viewPortIf.content.appendChild(document.createElement("dummy-port-element"));
                    var falseCase = document.createElement("template");
                    falseCase.setAttribute("lyte-else","");
                    falseCase.content.appendChild(comp.content);
                    comp.innerHTML = "";
                    comp.content.appendChild(viewPortIf);
                    this.pushDir("view-in")
                }
                if(comp.parent){
                    elementals = comp.parent.querySelectorAll("[@elemental]");
                }
                let elementalsList = [];
                elementals.forEach(function(elemental){
                    let elementalName = elemental.getAttribute("@elemental");
                    if(elementalsList.indexOf(elementalName) == -1){
                        elemental.setAttribute("is","elemental");
                        elemental.setAttribute("value",elementalName);
                        elemental.removeAttribute("elemental");
                        debugger
                        comp.content.prepend(elemental);
                        elementalsList.push(elementalName);
                    }else{
                        errors.push(new Error("Duplicate Elemental template found '" + elementalName + "'"));  
                    }
                })
            }
            if(elementals.length){
                this.pushDir("elemental");  
            }
            s = comp.content;
        }
        if (s) {
            if (!this.fromCLI) {
                this.splitTextNodes(s, warnings);
            }
            let dynamicNodes = [];

            var attrStrict = comp.getAttribute("use-strict");
            var strict = (attrStrict == false ||  attrStrict == "false") ? attrStrict : attrStrict == undefined ? window.useStrict: attrStrict;
            if(strict == "false") {
                strict = false;
            } else if(strict == "true") {
                strict = true;
            }
            
            if (strict) {
                if (s.querySelector("script")) {
                    let e = new Error("Security: Script tags should not be included")
                    e.strict = true;
                    errors.push(e)
                }
                if (s.querySelector("style")) {
                    let e = new Error("Security: Style tags should not be included")
                    e.strict = true;
                    errors.push(e)
                }
            }
            let d = [];
            try {
                this.newGetDeepNodes(fileName, comp, d, [], undefined, true, errors, warnings);
                this.processTemplate(s, dynamicNodes, fileName, strict, errors, warnings);
                this.getTransitionOrder(dynamicNodes);
            } catch (e) {
                console.log("Error from getDynamicNodes:-\n", e);
            }
            let obb = {};
            this.getTemplateDirectChild(dynamicNodes, obb);
            if (obb && (obb.hc || obb.trans)) {
                let newobb = {
                    "type": "dc",
                    "trans": obb.trans,
                    hc: obb.hc,
                    p: obb.dc
                }
                dynamicNodes.push(newobb);
            }
            let attr = comp.attributes;
            for(let i = 0; i< attr.length; i++){
                let attrName = attr[i].name;
                if(attrName.startsWith("@")){
                    let dirName = attrName.split("@")[1];
                    this.pushDir(dirName);
                }
            }
            if (errors.length) {
                returnValue = {
                    componentName: fileName,
                    errors: errors,
                    warnings: warnings
                };
            } else {
                let template = comp.outerHTML;
                if(this.fromCLI){
                    template = "_template = " + JSON.stringify(template+"____cssContent")+";";
                    dynamicNodes ="_dynamicNodes = " + JSON.stringify(dynamicNodes)+";";
                }
                else{
                    template = JSON.stringify(template);
                }
                returnValue = {
                    componentName: fileName,
                    dynamicNodes,
                    template,
                    _templateAttributes: d[0],
                    warnings: warnings
                    // ,
                    // ...(this.fromCLI ? {
                    //     importDetails: this.impObj,
                    //     needToImpDetails: this.defaultImpDetails
                    // } : {})
                };
                if(this.fromCLI){
                    if(Compile.defaultImpDetails.found){
                        if(Compile.defaultImpDetails.found.purifier){
                            this.impObj.options.internalSlicer.$bundlePurifier = true;
                        }
                        if(Compile.defaultImpDetails.found.encoder){
                            this.impObj.options.internalSlicer.$bundleEncoder = true;
                        }
                        if(elementals.length){
                            this.impObj.options.internalSlicer.$bundleElemental = true;
                        }
                    }
                    this.impObj.defaultDirectives.forEach(function(defDirective){
                        if(defDirective.startsWith("lyte-")){
                            let slicerName = "$"+defDirective.substring(5,defDirective.length);
                            Compile.impObj.options.internalSlicer[slicerName] = true;
                            // console.log("slicerName",slicerName);
                        }
                    })
                    returnValue.importDetails = Compile.deepCopyObject(this.impObj);
                    returnValue.needToImpDetails = Compile.deepCopyObject(this.defaultImpDetails);
                }
            }
        }
        window.pending = false;
        if(this.fromCLI){
            if (typeof WorkerGlobalScope !== 'undefined' && self instanceof WorkerGlobalScope) {
                globalThis.window && (delete globalThis.window);
                globalThis.document && (delete globalThis.document);
            }
            else{
                __webpack_require__.g.window && (delete __webpack_require__.g.window);
                __webpack_require__.g.document && (delete __webpack_require__.g.document);
            }
        }
        Compile.defaultImpDetails = this.defaultImpDetails = {
            components: [],
            helpers: [],
            alreadyImported: false,
            found : {}
        };
        Compile.impObj = this.impObj = {
            "svgLoader": [],
            "components": [],
            "helpers": [],
            "directives" : [],
            "defaultDirectives" : [],
            "defaultComponents" : [],
            "registryMap": {},
            "lyteRegistries" : {},
            "options" : {
                "internalSlicer" : {
                    $bundlePurifier : false,
                    $bundleEncoder : false,
                    $bundleElemental : false,
                    $save : false,
                    $turbo : false,
                    $shadow : false,
                    $view : false,
                    $promise : false,
                    $hide : false
                }
            }
        };
        // console.log(fileName,this.impObj.options.internalSlicer.$bundleEncoder);
        this.fromCLI = undefined;
        return returnValue;
    }
    Compile.splitTextNodes = function (node, warnings, svg, checker = {}, errorObj, impObj) {
        var nodeName = node.localName;
        if (checker.fromMain) {
            if (typeof WorkerGlobalScope !== 'undefined' && self instanceof WorkerGlobalScope) {
                globalThis.document = node;
            }
            else{
                __webpack_require__.g.document = node;
                
            }
            impObj = this.impObj;
            checker.fromMain = undefined;
        }
        if(checker.fromCLI){
            this.htmlStr= {
                innerHTML:"innerObj",
                outerHTML:"outerObj"
            }
        }else{
            this.htmlStr= {
                innerHTML:"innerHTML",
                outerHTML:"outerHTML"
            }
        }
        let compVal = this.defaultImpDetails.components;
        if (nodeName) {
            let tag = nodeName.toLowerCase();
            if (tag === "slyte-svg-loader") {
                let src = node.getAttribute("src"),
                    arr = Compile.impObj.svgLoader;
                if (src && !arr.includes(src)){ arr.push(src) };
            }
        }
        if (nodeName && nodeName.indexOf("-") != -1 && compVal.indexOf(nodeName) == -1 && this.defaultComponents.indexOf(nodeName) == -1) {
            if (nodeName == "lyte-import" || nodeName == "import-component" || nodeName == "import-helper") {
                this.defaultImpDetails.alreadyImported = true;
            } else if(this.defaultSrcComponents.indexOf(nodeName) != -1 && impObj.defaultComponents.indexOf(nodeName) == -1){
                impObj.defaultComponents.push(nodeName);
            } else {
                compVal.push(nodeName);
            }
        }
        if (node.tagName === "svg") {
            svg = true;
        }
        if (node.hasAttribute) {
            if (node.hasAttribute("is") && this.regex.lyteTemplateType2.test(node.getAttribute("is")) && node.getAttribute("_jsp") != "true" && !node.hasAttribute("lyte-for") && !node.hasAttribute("lyte-for-in") && !node.hasAttribute("lyte-if") && !node.hasAttribute("lyte-switch") && !node.hasAttribute("_new")) {
                warnings.push({
                    message: "Deprecated Syntax : '" + this.errorNodeDetails(node) + "' Change it to " + "'lyte-" + node.getAttribute("is") + "' syntax. It will not be supported in upcoming versions.\n"
                })
            }
            if (svg && node.tagName == "template" && node.hasAttribute("is") && this.regex.lyteTemplateType2.test(node.getAttribute("is"))) {
                var template = document.createElement("template");
                template[this.htmlStr["innerHTML"]] = node[this.htmlStr["innerHTML"]]
                var attrs = node.attributes;
                for (var i = 0; i < attrs.length; i++) {
                    template.setAttribute(attrs[i].nodeName, attrs[i].nodeValue);
                }
                node.replaceWith(template);
                node = template;
            }
            if(node.hasAttribute("lyte-registry")){
                let val = node.getAttribute("lyte-registry");
                if(val.indexOf("{{") == -1){
                    let tagName = node.tagName;
                    if(tagName != "TEMPLATE"){
                        if(!impObj.lyteRegistries[tagName]){
                            impObj.lyteRegistries[tagName] = [val];
                        }else{
                            impObj.lyteRegistries[tagName].push(val);
                        }
                    }
                }
            }
            if (node.hasAttribute("lyte-for")) {
                let errorinfo = this.getErrorInfo(node);
                var lyteFor = node.getAttribute("lyte-for");
                var lyteForOptions = {};
                if (node.hasAttribute("lyte-options")) {
                    try {
                        lyteForOptions = JSON.parse(node.getAttribute("lyte-options"));
                    } catch (e) {
                        lyteForOptions = {};
                    }
                }
                var lyteForArr = lyteFor.split(" ");
                var template;
                if (node.tagName === "TEMPLATE") {
                    template = node;
                } else if (svg && node.tagName == "template") {
                    template = this.addUnbound(node,template,"template");
                    node.removeAttribute("lyte-for");
                    node.removeAttribute("lyte-options");
                    template[this.htmlStr["innerHTML"]] = node[this.htmlStr["innerHTML"]];
                    node.replaceWith(template);
                    node = template;
                } else {
                    template = this.addUnbound(node,template,"template");
                    node.removeAttribute("lyte-for");
                    node.removeAttribute("lyte-options");
                    template[this.htmlStr["innerHTML"]] = node[this.htmlStr["outerHTML"]];
                    node.replaceWith(template);
                    node = template;
                }
                node.removeAttribute("lyte-for");
                node.removeAttribute("lyte-options");
                if (checker.fromParser) {
                    if (!template.attrType) {
                        template.attrType = {}
                    };

                    let syn = checker.syntaxCheckWorkerNew(lyteForArr[0], checker);
                    template.setAttribute("items", syn.text);
                    template.attrType.items = syn.type;

                    syn = checker.syntaxCheckWorkerNew(lyteForArr[2] || "item", checker);
                    template.setAttribute("item", syn.text);
                    template.attrType.item = syn.type;

                    syn = checker.syntaxCheckWorkerNew(lyteForArr[3] || "index", checker);
                    template.setAttribute("index", syn.text);
                    template.attrType.index = syn.type;
                } else {
                    template.setAttribute("items", lyteForArr[0]);
                    template.setAttribute("item", lyteForArr[2] || "item");
                    template.setAttribute("index", lyteForArr[3] || "index");
                }
                template.setAttribute("is", "for");
                template.setAttribute("_new", "true");
                template.setAttribute("errorinfo", JSON.stringify(errorinfo));
                if (lyteForOptions.unbound) {
                    template.setAttribute("unbound", lyteForOptions.unbound);
                }

                let nxtSibl = node.nextElementSibling;
                if(nxtSibl && (nxtSibl.hasAttribute("lyte-await") || nxtSibl.hasAttribute("lyte-fail"))){ // may be wait
                    let trueCase = document.createElement("template");
                    trueCase.setAttribute("case", "true");
                    trueCase[this.htmlStr.innerHTML] = template[this.htmlStr.innerHTML];
                    // trueCase.innerHTML = template.innerHTML;
                    template.innerHTML = "";
                    template.content.appendChild(trueCase);
                    template.setAttribute("has-child",true);
                    // this.appendBreak(template)
                    this.ifNextElementSibling(nxtSibl, template, svg, undefined, checker, errorObj, impObj, node, "for");
                    nxtSibl = node.nextElementSibling;
                    if(nxtSibl){ // may be fail
                        this.ifNextElementSibling(nxtSibl, template, svg, undefined, checker, errorObj, impObj, node, "for");
                    }
                }
            } else if (node.hasAttribute("lyte-for-in")) {
                let errorinfo = this.getErrorInfo(node);
                var lyteFor = node.getAttribute("lyte-for-in");
                var lyteForOptions = {};
                if (node.hasAttribute("lyte-options")) {
                    try {
                        lyteForOptions = JSON.parse(node.getAttribute("lyte-options"));
                    } catch (e) {
                        lyteForOptions = {};
                    }
                }
                var lyteForArr = lyteFor.split(" ");
                var template;
                if (node.tagName === "TEMPLATE") {
                    template = node;
                    node.removeAttribute("lyte-for-in");
                    node.removeAttribute("lyte-options");
                } else if (svg && node.tagName == "template") {
                    template = this.addUnbound(node,template,"template");
                    node.removeAttribute("lyte-for-in");
                    node.removeAttribute("lyte-options");
                    template[this.htmlStr["innerHTML"]] = node[this.htmlStr["innerHTML"]]
                    node.replaceWith(template);
                    node = template;
                } else {
                    template = this.addUnbound(node,template,"template");
                    node.removeAttribute("lyte-for-in");
                    node.removeAttribute("lyte-options");
                    template[this.htmlStr["innerHTML"]] = node[this.htmlStr["outerHTML"]];
                    node.replaceWith(template);
                    node = template;
                }
                if (checker.fromParser) {
                    if (!template.attrType) {
                        template.attrType = {}
                    };
                    let syn = checker.syntaxCheckWorkerNew(lyteForArr[0], checker);
                    template.setAttribute("object", syn.text);
                    template.attrType.object = syn.type;

                    syn = checker.syntaxCheckWorkerNew(lyteForArr[2] || "value", checker);
                    template.setAttribute("value", syn.text);
                    template.attrType.value = syn.type;

                    syn = checker.syntaxCheckWorkerNew(lyteForArr[3] || "key", checker);
                    template.setAttribute("key", syn.text);
                    template.attrType.key = syn.type;
                } else {
                    template.setAttribute("object", lyteForArr[0]);
                    template.setAttribute("value", lyteForArr[2] || "value");
                    template.setAttribute("key", lyteForArr[3] || "key");
                }
                template.setAttribute("is", "forIn");
                template.setAttribute("_new", "true");
                template.setAttribute("errorinfo", JSON.stringify(errorinfo));
                if (lyteForOptions.unbound) {
                    template.setAttribute("unbound", lyteForOptions.unbound);
                }
                let nxtSibl = node.nextElementSibling;
                if(nxtSibl && (nxtSibl.hasAttribute("lyte-await") || nxtSibl.hasAttribute("lyte-fail"))){ // may be wait
                    let trueCase = document.createElement("template");
                    trueCase.setAttribute("case", "true");
                    trueCase.innerHTML = template.innerHTML;
                    template.innerHTML = "";
                    template.content.appendChild(trueCase);
                    template.setAttribute("has-child",true);
                    this.ifNextElementSibling(nxtSibl, template, svg, undefined, checker, errorObj, impObj, node, "forIn");
                    nxtSibl = node.nextElementSibling;
                    if(nxtSibl){ // may be fail
                        this.ifNextElementSibling(
                            nxtSibl,
                            template,
                            svg,
                            undefined,
                            checker,
                            errorObj,
                            impObj,
                            node,
                            "forIn"
                        );
                    }
                }
            } else if (node.hasAttribute("lyte-if")) {
                var modifiedIf = this.handleLyteIf(node, svg, checker, errorObj, impObj);
                node.replaceWith(modifiedIf);
                node = modifiedIf;
            } else if (node.hasAttribute("lyte-switch")) {
                // handleLyteSwitch(node,svg);//af check
                var modifiedSwitch = this.handleLyteSwitch(node, svg, checker, errorObj, impObj);
                node.replaceWith(modifiedSwitch);
                node = modifiedSwitch;
            } else if (svg && node.tagName == "template" && node.hasAttribute("is") && this.regex.lyteTemplateType2.test(node.getAttribute("is"))) {
                var template = document.createElement("template");
                template[this.htmlStr["innerHTML"]] = node[this.htmlStr["innerHTML"]]
                var attrs = node.attributes;
                for (var i = 0; i < attrs.length; i++) {
                    template.setAttribute(attrs[i].nodeName, attrs[i].nodeValue);
                }
                node.replaceWith(template);
                node = template;
            } else if (node.hasAttribute("is") && node.getAttribute("is") == "if") {
                var modifiedIf = this.jspIfToSwitch(node, checker);
                node.replaceWith(modifiedIf);
                node = modifiedIf;
            }
            for(let i=0; i<node.attributes.length; i++){
                let attrName = node.attributes[i].name;
                if(attrName.startsWith("@")){
                    let dirName = attrName.substring(1);
                    this.pushDir(dirName);
                    if(dirName == "view-in"){
                        // debugger
                        let attrVal = node.attributes[i].value;
                        if(attrVal.startsWith("{{")){
                            attrVal = attrVal.substring(2, attrVal.length-2);
                            node.setAttribute("lyte-if", "{{lyteViewPort(" + attrVal + ")}}");
                        }else if(attrVal === ""){;
                            node.setAttribute("lyte-if", "{{lyteViewPort()}}");
                        }
                        else{
                            node.setAttribute("lyte-if", "{{lyteViewPort(" + attrVal + ")}}");
                        }
                        node.removeAttribute(attrName);
                        if(node.nextElementSibling && node.nextElementSibling.hasAttribute("@view-out")){
                            node.nextElementSibling.removeAttribute("@view-out");
                            node.nextElementSibling.setAttribute("lyte-else","");
                            var modifiedIf = this.handleLyteIf(node, svg, checker, errorObj, impObj);
                            node.replaceWith(modifiedIf);
                            node = modifiedIf;
                        }else{
                            errors.push(new Error("(LC:26) view-out directive not found"));
                        }
                    }
                }
            }
            for(let i=0; i<node.attributes.length; i++){
                let attrName = node.attributes[i].name;
                if(attrName.startsWith("@")){
                    let dirName = attrName.substring(1);
                    this.pushDir(dirName);
                }
            }
        }

        if (node && node.childNodes && node.childNodes.length) {
            for (var i = node.childNodes.length - 1; i >= 0; i--) {
                var chlNode = node.childNodes[i];
                if (chlNode.hasAttribute) {
                    if (chlNode.hasAttribute("lyte-if-else") || chlNode.hasAttribute("lyte-else")) {
                        if (!(chlNode.previousElementSibling && (chlNode.previousElementSibling.hasAttribute("lyte-if") || chlNode.previousElementSibling.hasAttribute("lyte-else-if")))) {
                            warnings.push({
                                message: "Element with lyte-else must be preceded by a element having lyte-if or lyte-else :\n \t" + chlNode.cloneNode().outerHTML
                            })
                        }
                    } else {
                        if (checker.ide && node.childNodes[i].getAttribute("case") == "false") {
                            window.elifHold = true;
                        }
                        this.splitTextNodes(node.childNodes[i], warnings, svg, checker, errorObj, impObj);
                    }
                } else {
                    this.splitTextNodes(node.childNodes[i], warnings, svg, checker, errorObj, impObj);
                }
            }
        }
        if (node.tagName === "TEMPLATE") {
            this.splitTextNodes((node.content) ? node.content : this.createDocFragment(node), warnings, svg, checker, errorObj, impObj);
        }
        if (node.tagName == "LYTE-IMPORT") {
            for (let c = 0; c < node.children.length; c++) {
                this.impDetails(node.children[c], impObj);
            }
            if (node.previousSibling && node.previousSibling.nodeValue == " " && node.nextSibling && node.nextSibling.nodeValue == " ") {
                node.previousSibling.remove();
            }
            node.remove();
            return;
        }
        if (!checker.fromParser && node.nodeType === node.TEXT_NODE) {
            var nodeValue = node.nodeValue;
            if (nodeValue) {
                let mustacheValues = nodeValue.match(this.regex.splitTextNodesMustache); //'
                if (!mustacheValues) {
                    return;
                }
                var newNodeArray = [];
                for (var i = 0; i < mustacheValues.length; i++) {
                    var mustacheStartIndex = nodeValue.indexOf(mustacheValues[i]);
                    var mustacheEndIndex = mustacheStartIndex + mustacheValues[i].length;
                    if (mustacheStartIndex) {
                        newNodeArray.push(document.createTextNode(nodeValue.substring(0, mustacheStartIndex)));
                    }
                    newNodeArray.push(document.createTextNode(nodeValue.substring(mustacheStartIndex, mustacheEndIndex)));
                    nodeValue = nodeValue.substring(mustacheEndIndex);
                }
                newNodeArray.push(document.createTextNode(nodeValue));
                node.replaceWith.apply(node, newNodeArray);
            }
        }
    }


    //This method will run through all the nodes of the template and put the dynamicNode positions 
    //in deepNodes and helper node positions in helperNodes
    //By helper nodes, we mean all the for and if helpers which are present in the component template. 
    //The template will contain the dynamicNodes. 
    //For template contains - _forTemplate, which will contain the content and _dynamicNodes
    //If template contains - _trueCase, _falseCase, which will contain the content and _dynamicNodes.
    Compile.processTemplate = function(node, deepNodes, componentName, strict, errors, warnings, nearByParent) {
        let isBreak = node.querySelector('template[is=break]');
        if (isBreak) {
            this.getTrimmedContent(node, undefined, isBreak);
        }
        let isContinue = node.querySelector('template[is=continue]');
        if (isContinue) {
            this.getTrimmedContent(node, undefined, isContinue);
        }

        this.helperNodes = [];
        if (node.hasChildNodes()) {
            let runningIndex = 0;
            let oldParent;
            if (nearByParent == "svg" || nearByParent == "foreignObject") {
                oldParent = nearByParent;
            }
            for (let i = 0; i < node.childNodes.length; i++) {
                let deepN = [];
                deepN.push(i);
                let index = 0;
                let tagName = node.childNodes[i].tagName;
                if (tagName) {
                    if (tagName == "svg") {
                        nearByParent = "svg";
                    } else if (tagName == "foreignObject" || tagName == "FOREIGNOBJECT") {
                        nearByParent = "foreignObject";
                    }
                    let is = node.childNodes[i].getAttribute("is");
                    if (tagName === "TEMPLATE" && is) {
                        //                        index = helperNodes.push(node.childNodes[i]);
                        //We will be adding an attribute index1 in the helper templates, 
                        //This is done because we will be storing all the helper nodes as such in an array _helperNodes in the component template. 
                        //In order to lookup to the dynamicNodes of the for template or if template, we need to have a reference of which 
                        //helper we are calling. 
                        //By this way, we will be adding an index1 Attribute which will contain index startign from 0. 
                        //This index refreshes for each component registration. 
                        //                        node.childNodes[i].setAttribute("index1", index-1);
                        this.newGetDeepNodes(
                            componentName,
                            node.childNodes[i],
                            deepNodes,
                            deepN,
                            is,
                            strict,
                            errors,
                            warnings,
                            nearByParent
                        );
                    } else {
                        this.newGetDeepNodes(componentName, node.childNodes[i], deepNodes, deepN, undefined, strict, errors, warnings, nearByParent);
                    }
                    node.childNodes[i].removeAttribute("errorinfo");
                } else {
                    this.newGetDeepNodes(componentName, node.childNodes[i], deepNodes, deepN, undefined, strict, errors, warnings, nearByParent);
                }
                nearByParent = oldParent;
            }
        }

    }
    Compile.errorNodeDetails = function (node) {
        if (node.hasAttribute("errorinfo")) {
            let errorinfo = JSON.parse(node.getAttribute("errorinfo"));
            return "`" + errorinfo.details.tagName + "`" + " with attribute " + errorinfo.details.name + "=\"" + errorinfo.details.value.replace(/@mus@/g, "{{") + "\"";
        }
        let str = node.cloneNode(true);
        str.innerHTML = ".....";
        return str.outerHTML;
    }
    Compile.getAllHelperNames = function(obj,arr){
        if(obj){
            if(obj.name){
                arr.push(obj.name);
            }
            if(obj.args && obj.args.length){
                obj.args.forEach(function(item){
                    if(item && typeof item == "object" && item.value){
                        Compile.getAllHelperNames(item.value,arr);
                    }
                })
            }
        }
    }
    //This method is the place where the deepNodes and helperNodes gets updated with the 
    //Values of the positions of dynamicNodes and helperNodes. 
    Compile.newGetDeepNodes = function(
        componentName,
        node,
        deepNodes,
        deepN,
        is,
        strict,
        errors,
        warnings,
        nearByParent
    ) {
        if (!Object.getPrototypeOf(node).querySelectorAllChildren) {
            let obj = node;
            while (obj.parent) {
                if (Object.getPrototypeOf(obj).querySelectorAllChildren && this.fromCLI) {
                    obj.refreshNode(true);
                    break;
                }
                obj = obj.parent;
            }
        }
        node._parent = node.parentNode;
        let toBePushed;
        let errorMsg = "Usage of block helpers (for|forIn|if|switch) inside the tags (TABLE|TR|SELECT) will not work in IE11. \n If your app is supported for IE11, please use lyte ui components lyte-table and lyte-dropdown instead of table and select tags respectively";
        if (node.nodeType == 8) {
            deepN.pop();
            return;
        }
        if (node.tagName && node.tagName.indexOf("-") !== -1 && node.tagName !== "LYTE-YIELD") {
            toBePushed = {
                "t": "cD",
                p: deepN.slice()
            };
        }
        if (node.tagName === "LYTE-YIELD") {
            toBePushed = {
                "t": "i",
                p: deepN.slice()
            };
        } else
        if (is === "registerYield" || is === "yield") {
            let dynamicNodes = [];
            if (node.tagName != "TEMPLATE") {
                errors.push(new Error("Syntax error in node '" + this.errorNodeDetails(node) + "' in the component " + componentName + ". Yield must be given in template tag only"));
            } else {
                this.processTemplate(node.content, dynamicNodes, componentName, strict, errors, warnings, nearByParent); //Lyte._ie ? node : 
                this.getTransitionOrder(dynamicNodes);
                toBePushed = {
                    "t": "r",
                    p: deepN.slice(),
                    "dN": dynamicNodes
                };
                this.getTemplateDirectChild(dynamicNodes, toBePushed);
            }
        } else if (is === "insertYield") {
            deepNodes.push({
                "t": "i",
                p: deepN.slice()
            });
        } else
        if (is === "for") {
            let template = node;
            node._forTemplate = {};
            if(node.hasAttribute("has-child")){
                let casesArr = {};
                let cases = node.content.querySelectorAll("[case]");
                var currentCaseTemplate;
                var rtObj = {};
                for(var i=0;i<cases.length;i++){
                    let currentCase = cases[i];
                    currentCaseTemplate = cases[i];
                    let caseName = currentCase.getAttribute("case");
                    currentCase.setAttribute("case", caseName );
                    currentCase = currentCase.content;
                    let dynamicNodes = [];
                    currentCase._parent = node;
                    this.processTemplate(currentCase, dynamicNodes, componentName,strict,errors,warnings,nearByParent);
                    casesArr[caseName] = {dN: dynamicNodes};
                    this.replaceCaseNode(currentCaseTemplate,componentName,errors,node,rtObj,nearByParent);
                }
                toBePushed = {"t": "f", p: deepN.slice(),"c":casesArr};
                if(rtObj.svg){
                    toBePushed.svg = true;
                }
                if(node._dirObj){
                    let str = JSON.stringify(node._dirObj);
                    node.setAttribute("dir-obj",str);
                }
                node = this.replaceParentNode(node, is, toBePushed,componentName,errors,true,nearByParent);
            }
            else if (template) {
                node._forTemplate.content = template.content;
                node._forTemplate.content._parent = template;
                let dynamicNodes = [];
                this.processTemplate(node._forTemplate.content, dynamicNodes, componentName, strict, errors, warnings, nearByParent); //Lyte._ie ? node._forTemplate : 
                this.getTransitionOrder(dynamicNodes);
                toBePushed = {
                    "t": "f",
                    p: deepN.slice(),
                    "dN": dynamicNodes
                };
                this.getTemplateDirectChild(dynamicNodes, toBePushed);
                if(node._dirObj){
                    let str = JSON.stringify(node._dirObj);
                    node.setAttribute("dir-obj",str);
                }
                node = this.replaceParentNode(node, "for", toBePushed, componentName, errors, true, nearByParent);
            }
        } else if (is === "forIn") {
            let template = node;
            node._forInTemplate = {};
            if(node.hasAttribute("has-child")){
                let casesArr = {};
                let cases = node.content.querySelectorAll("[case]");
                var currentCaseTemplate;
                var rtObj = {};
                for(var i=0;i<cases.length;i++){
                    let currentCase = cases[i];
                    currentCaseTemplate = cases[i];
                    let caseName = currentCase.getAttribute("case");
                    currentCase.setAttribute("case", caseName );
                    currentCase = currentCase.content;
                    let dynamicNodes = [];
                    currentCase._parent = node;
                    this.processTemplate(currentCase, dynamicNodes, componentName,strict,errors,warnings,nearByParent);
                    toBePushed = {"t": "fI", p: deepN.slice(),"c":casesArr};
                    casesArr[caseName] = {dN: dynamicNodes};
                    this.replaceCaseNode(currentCaseTemplate,componentName,errors,node,rtObj,nearByParent);
                }
            }
            else if (template) {
                node._forInTemplate.content = template.content;
                node._forInTemplate.content._parent = template;
                let dynamicNodes = [];
                this.processTemplate(node._forInTemplate.content, dynamicNodes, componentName, strict, errors, warnings, nearByParent);//Lyte._ie ? node._forInTemplate :
                this.getTransitionOrder(dynamicNodes);
                toBePushed = {
                    "t": "fI",
                    p: deepN.slice(),
                    "dN": dynamicNodes
                };
                this.getTemplateDirectChild(dynamicNodes, toBePushed);
                if(node._dirObj){
                    let str = JSON.stringify(node._dirObj);
                    node.setAttribute("dir-obj",str);
                }
            }
            node = this.replaceParentNode(node, "forIn", toBePushed, componentName, errors, true, nearByParent);
        } else if (is === "switch" || is === "if" || is == "case") {
            // if(node.parentElement && /^(SELECT|TR|TABLE)$/.test(node.parentElement.tagName)) {
            //     warnings.push({message: errorMsg});
            // }
            if(node.getAttribute("__vp") == "c-new" || node.getAttribute("__vp") == "c-old" ){
                this.pushDir("view-in")
            }
            let casesArr = {},
                defaultArr = {};
            let defaultCase = node.content.querySelector("[default]");
            let cases = node.content.querySelectorAll("[case]");
            let currentCaseTemplate;
            let tobj = {};
            let hasDynamicCase;
            let casesOrder = [];
            let prevCaseName;
            var rtObj = {};
            for (let i = 0; i < cases.length; i++) {
                let dynamicCaseName = false;
                let casesDeepNodes = []
                let currentCase = cases[i];
                currentCaseTemplate = cases[i];
                let caseName = currentCase.getAttribute("case");
                let actCaseName = caseName;
                tobj[actCaseName] = {};
                if (is === "switch" && caseName == "") {
                    caseName = "\"\"";
                }
                if (is === "switch") { //check
                    cases[i].setAttribute("is", "case");
                }
                if (currentCase.tagName === "TEMPLATE" && (!currentCase.getAttribute("is") || currentCase.getAttribute("is") == 'case')) {
                    currentCase.setAttribute("case", caseName);
                    currentCase = currentCase.content;
                } else {
                    let temp = document.createElement('template');
                    let clone = currentCase.cloneNode(true);
                    temp.content.appendChild(clone);
                    temp.setAttribute("case", caseName);
                    clone.removeAttribute('case');
                    currentCaseTemplate = temp;
                    currentCase = temp.content;
                    cases[i].replaceWith(temp);
                }
                this.processAttr(componentName, currentCaseTemplate, casesDeepNodes, [i], is, strict, errors, warnings);
                if (casesDeepNodes.length && cases[i].getAttribute("case").startsWith("{{")) {
                    dynamicCaseName = true;
                    hasDynamicCase = true;
                    var actName = cases[i].getAttribute("case");
                    cases[i].dcn = true;
                    caseName = "lc_id_" + i;
                    currentCaseTemplate.setAttribute("lc-id", caseName);
                    tobj[caseName] = tobj[actCaseName];
                    delete tobj[actCaseName];
                }
                casesOrder.push(caseName);
                let dynamicNodes = [];
                currentCase._parent = node;
                this.processTemplate(currentCase, dynamicNodes, componentName, strict, errors, warnings, nearByParent);
                this.getTransitionOrder(dynamicNodes);
                this.getTemplateDirectChild(dynamicNodes, tobj[caseName]);
                var rtObj = {};
                if (is === "if") {
                    this.replaceCaseNode(currentCaseTemplate, componentName, errors, node, rtObj, nearByParent);
                }
                if (caseName === "") {
                    caseName = '""';
                }
                casesArr[caseName] = {
                    dN: dynamicNodes,
                    cdp: casesDeepNodes[0]
                };
                dynamicNodes.forEach((dyn) => {
                    dyn.cn = caseName;
                })
                if (dynamicCaseName) {
                    casesArr[caseName].dcn = true;
                }
                if (is === "if") {
                    continue;
                }
                var prevCase = casesArr[prevCaseName];
                if (dynamicCaseName && prevCase && prevCase.additional && prevCase.additional.next) {
                    prevCase.additional.next = cases[i].getAttribute("lc-id");
                }
                let isBreak = currentCase.querySelector("template[is=break]");
                if (!isBreak) {
                    if (cases[i + 1]) {
                        casesArr[caseName].additional = {
                            "next": cases[i + 1].getAttribute("case")
                        };
                    } else if (defaultCase) {
                        casesArr[caseName].additional = {
                            "default": true
                        };
                    }
                } else {
                    isBreak.remove();
                }
                this.replaceCaseNode(currentCaseTemplate, componentName, errors, node, rtObj, nearByParent);
                prevCaseName = caseName;
            }
            if (defaultCase) {
                let dCase;
                let dCaseTemplate;
                tobj["default"] = {}
                if (defaultCase.tagName === "TEMPLATE" && !defaultCase.getAttribute("is")) {
                    dCase = defaultCase.content;
                    dCaseTemplate = defaultCase;
                } else {
                    let temp = document.createElement('template');
                    let clone = defaultCase.cloneNode(true);
                    temp.content.appendChild(clone);
                    temp.setAttribute("default", '');
                    clone.removeAttribute('default');
                    dCase = temp.content;
                    dCaseTemplate = temp;
                    defaultCase.replaceWith(temp);
                }
                let isBreak = dCaseTemplate.content.querySelector("template[is=break]");
                if (isBreak) {
                    isBreak.remove();
                }
                let dynamicNodes = [];
                dCase._parent = node;
                this.processTemplate(dCase, dynamicNodes, componentName, strict, errors, warnings, nearByParent);
                this.getTransitionOrder(dynamicNodes);
                this.getTemplateDirectChild(dynamicNodes, tobj.default);
                dynamicNodes.forEach(function (dyn) {
                    dyn.cn = "default";
                })
                defaultArr = {
                    dN: dynamicNodes
                };
                this.replaceCaseNode(dCaseTemplate, componentName, errors, node, rtObj, nearByParent);
            }
            is == "if" ? is = "e" : is = "s";
            if (is === "s") {
                this.getSwitchOrder(casesArr, tobj, defaultArr);
                let keys = Object.keys(casesArr);
                for (let i = 0; i < keys.length; i++) {
                    this.getTemplateDirectChild(casesArr[keys[i]].dN, tobj[keys[i]]);
                }
            }
            // console.log("the value of d org",defaultArr)
            toBePushed = {
                "t": is,
                p: deepN.slice(),
                "c": casesArr,
                "d": defaultArr,
                dc: tobj,
                "hd": hasDynamicCase,
                "co": casesOrder
            };
            if (rtObj.svg) {
                toBePushed.svg = true;
            }
            is == "e" ? is = "if" : is = "switch";
            let keys = Object.keys(tobj);
            for (let x = 0; x < keys.length; x++) {
                if (tobj[keys[x]].hc) {
                    toBePushed.hc = true;
                    toBePushed.trans = true;
                }
            }
            node = this.replaceParentNode(node, is, toBePushed, componentName, errors, undefined, nearByParent);
        } else if (is === "component") {
            node._componentTemplate = {};
            node._componentTemplate.content = node.content;
            let dynamicNodes = [];
            this.processTemplate(node._componentTemplate.content, dynamicNodes, componentName, strict, errors, warnings, nearByParent);
            this.getTransitionOrder(dynamicNodes);
            toBePushed = {
                "t": "cM",
                p: deepN.slice(),
                "dN": dynamicNodes
            };
            this.getTemplateDirectChild(dynamicNodes, toBePushed);
        }  else if(is === "elemental"){
            node._componentTemplate = {};
            node._componentTemplate.content = node.content;
            let dynamicNodes = [];
            this.processTemplate(node._componentTemplate.content, dynamicNodes, componentName, strict, errors, warnings, nearByParent);
            this.getTransitionOrder(dynamicNodes);
            toBePushed = {
                "t": "eM",
                p: deepN.slice(),
                "dN": dynamicNodes
            };
            this.getTemplateDirectChild(dynamicNodes, toBePushed);
        }  else if (node.nodeType == 3) {
            if (node.nodeValue.indexOf("{{") !== -1) {
            let syn = undefined
                if (!this.fromCLI) {
                    node.nodeValue = node.nodeValue.replace('.{}', '____lyteinternal____');
                    let val = node.nodeValue;
                    //need option
                    if (typeof Espree !== 'undefined' && typeof expHandlers !== 'undefined') {
                        let res = this.checkForMixedCase(val);
                        if (res.bool) {
                            const {
                                mustacheSpots
                            } = res;
                            tempVal = [];
                            if (mustacheSpots.length && mustacheSpots.length > 0) {
                                for (let j = 0; j < mustacheSpots.length; j++) {
                                    let x = mustacheSpots[j].mus
                                    if(x.match(/\&|\||\!|\=|\+|-|\*|\/|<|>|\?|:|%/gm)){
                                        x = this.trimAttr(x, errors, node, undefined, componentName);
                                    }
                                    val = val.replace(mustacheSpots[j].orgMus,function(){return x});
                                }
                            }
                            node.nodeValue = val;
                        }
                    }

                    syn = this.syntaxCheckWorkerNew(node.nodeValue);
                    if (!syn.bool) {
                        errors.push(new Error("Syntax error in node " + syn.err + " in " + node.nodeName + " for the node value: " + node.nodeValue + " in the component " + componentName));
                        return;
                    }
                }
                let mustache = this.getMustache(node.nodeValue, componentName, syn),
                    dynamicValue, helperFunc;
                if (mustache && mustache == "timeoutError") {
                    errors.push(new Error("(LC:timeout1) Syntax error in node " + mustache.err + " in " + node.nodeName + " for the attribute: " + node.nodeName + " - " + node.nodeValue));
                    return;
                }
                if (mustache && typeof mustache === 'object') {
                    if (mustache.bool === false) {
                        errors.push(new Error("Syntax error in node " + mustache.err + " in " + node.nodeName + " for the attribute: " + node.nodeName + " - " + node.nodeValue + " in the component " + componentName));
                    }
                }
                if (mustache) {
                    helperFunc = this.getArray(mustache);
                    if (helperFunc && helperFunc == "timeoutError") {
                        errors.push(new Error("(LC:timeout6)Syntax error in node " + node.nodeName + " for the value: " + " - " + node.nodeValue));
                        return;
                    } else if (helperFunc === false) {
                        errors.push(new Error("(LC:11)Syntax Error in node " + node.nodeName + "for the value:" + node.nodeValue));
                        return;
                    }
                }
                if (mustache) {
                    if (!helperFunc) {
                        mustache = mustache.replace('____lyteinternal____', '.{}');
                        helperFunc = this.getHelper(mustache);
                        if (helperFunc && helperFunc.args == "lbindError" || helperFunc == "lbindError") {
                            errors.push(new Error("(LC:lbind2) Syntax Error : lbind are not allowed inside helpers in " + node.nodeName + " for : " + node.nodeValue + " in the component " + componentName));
                        } else if (helperFunc && helperFunc.name == "lbind") {
                            errors.push(new Error("(LC:lbind1) Syntax Error : lbind are not allowed to render in dom " + node.nodeName + " for: " + node.nodeValue + " in the component " + componentName));
                        } else if (helperFunc === false) {
                            errors.push(new Error("Syntax Error in node " + node.nodeName + "for :" + node.nodeValue + " in the component " + componentName));
                            return;
                        }
                        if (this.fromCLI && helperFunc) {
                            let arr = [];
                            Compile.getAllHelperNames(helperFunc,arr);
                            let self = this;
                            arr.forEach(function(hName){
                                if(hName == "unescape"){
                                    self.defaultImpDetails.found.purifier = true;
                                }else if(hName == "escape"){
                                    self.defaultImpDetails.found.encoder = true;
                                }
                            })
                            if (this.defaultImpDetails.helpers.indexOf(helperFunc.name) == -1 && this.defaultHelpers.indexOf(helperFunc.name) == -1) {
                                this.defaultImpDetails.helpers.push(helperFunc.name);
                            }
                            this.getImportedHelpers(helperFunc.args, this.defaultImpDetails.helpers);
                        }
                    }
                } else {
                    errors.push(new Error("Syntax Error in node " + node.nodeName + "for :" + node.nodeValue + " in the component " + componentName));
                }
                node.nodeValue = node.nodeValue.replace('____lyteinternal____', '.{}');
                let dynamic = mustache;
                if (helperFunc) {
                    deepNodes.push({
                        t: "tX",
                        p: deepN.slice() /*, helperInfo: helperFunc*/
                    });
                } else if (dynamic) {
                    deepNodes.push({
                        t: "tX",
                        p: deepN.slice() /*, dynamicValue: dynamic*/
                    });
                    //                  LN to do
                    //                  deepNodes.push({type: "text", position:deepN.slice(), dynamicValue: getDynamicValue(dynamic)});                    
                }
            }
            deepN.pop();
            return;
        }
        var attr = this.processAttr(componentName, node, deepNodes, deepN, is, strict, errors, warnings);
        if (node.hasChildNodes() && node.tagName !== "TEMPLATE") {
            let oldParent;
            if (nearByParent == "svg" || nearByParent == "foreignObject") {
                oldParent = nearByParent;
            }
            for (let i = 0; i < node.childNodes.length; i++) {
                deepN.push(i);
                let tagName = node.childNodes[i].tagName;
                if (tagName) {
                    if (tagName == "svg") {
                        nearByParent = "svg";
                    } else if (tagName == "foreignObject" || tagName == "FOREIGNOBJECT") {
                        nearByParent = "foreignObject";
                    }
                    let is = node.childNodes[i].getAttribute("is");
                    if (tagName && is) {
                        this.newGetDeepNodes(componentName, node.childNodes[i], deepNodes, deepN, is, strict, errors, warnings, nearByParent);
                    } else {
                        this.newGetDeepNodes(componentName, node.childNodes[i], deepNodes, deepN, undefined, strict, errors, warnings, nearByParent);
                    }
                    node.childNodes[i].removeAttribute("errorinfo")
                } else {
                    this.newGetDeepNodes(componentName, node.childNodes[i], deepNodes, deepN, undefined, strict, errors, warnings, nearByParent);
                }
                nearByParent = oldParent;
            }
        }
        if (attr && attr._special && toBePushed && toBePushed.t == "cD") {
            toBePushed._igTs = true;
        }
        if (attr && attr._special && toBePushed && toBePushed.t == "cM") {
            toBePushed._igTs = true;
        }
        if (attr && attr._special && toBePushed && toBePushed.t == "i") {
            toBePushed._igTs = true;
        }
        if (toBePushed) {
            deepNodes.push(toBePushed);
        }
        deepN.pop();
    }

    Compile.getArrayIndex = function (array, value) {
        for (let i = 0; i < array.length; i++) {
            if (array[i] === value) {
                return i
            };
        }
    }
    Compile.getTrimmedContent = function (content, position, node) {
        let dummyContent = content;
        if (node) {
            position = [];
            let parentNode = node.parentNode;
            while (true) {
                position.unshift(this.getArrayIndex(parentNode.childNodes, node));
                parentNode = parentNode.parentNode;
                node = node.parentNode;
                if (!parentNode) {
                    break;
                }
            }
        }
        for (let i = 0; i < position.length; i++) {
            for (let j = content.childNodes.length - 1; j > position[i]; j--) {
                content.childNodes[j].remove();
            }
            content = content.childNodes[position[i]];
        }
        return dummyContent;
    }
    Compile.createDocFragment = function (template) {
        var childNodes = template.cloneNode(true).childNodes;
        var frag = document.createDocumentFragment();
        var len = childNodes.length;
        for (var i = 0; i < len; i++) {
            frag.appendChild(childNodes[0]);
        }
        return frag;
    }
    Compile.deepCopyObject = function (obj) {
        var current, copies = [{
                source: obj,
                target: Object.create(Object.getPrototypeOf(obj))
            }],
            keys, propertyIndex, descriptor, nextSource, indexOf, sourceReferences = [obj];
        var cloneObject = copies[0].target,
            targetReferences = [cloneObject];
        while (current = copies.shift()) {
            keys = Object.getOwnPropertyNames(current.source);
            for (propertyIndex = 0; propertyIndex < keys.length; propertyIndex++) {
                descriptor = Object.getOwnPropertyDescriptor(current.source, keys[propertyIndex]);
                if (!descriptor.value || typeof descriptor.value != "object") {
                    Object.defineProperty(current.target, keys[propertyIndex], descriptor);
                    continue;
                }
                nextSource = descriptor.value;
                descriptor.value = Array.isArray(nextSource) ? [] : Object.create(Object.getPrototypeOf(nextSource));
                indexOf = sourceReferences.indexOf(nextSource);
                if (indexOf != -1) {
                    descriptor.value = targetReferences[indexOf];
                    Object.defineProperty(current.target, keys[propertyIndex], descriptor);
                    continue;
                }
                sourceReferences.push(nextSource);
                targetReferences.push(descriptor.value);
                Object.defineProperty(current.target, keys[propertyIndex], descriptor);
                copies.push({
                    source: nextSource,
                    target: descriptor.value
                });
            }
        }
        return cloneObject;
    }

    Compile.replaceCaseNode = function (node, componentName, errors, templateNode, rtObj, nearByParent) {
        var tagName;
        var template;
        if (nearByParent) {
            if (nearByParent == "svg") {
                rtObj.svg = true;
                var template = node.cloneNode();
                template.innerHTML = "<svg>" + node.innerHTML + "</svg>";
                template.setAttribute("depth", 1);
                node.replaceWith(template);
                return;
            }
        }
        for (var i = 0; i < node.content.children.length; i++) {
            if (node.content.children[i].tagName !== "TEMPLATE") {
                if (this.regex.tableTags.test(node.content.children[i].tagName)) {
                    tagName = node.content.children[i].tagName;
                }
                break;
            }
        }
        if (tagName) {
            if (tagName === "TR") {
                template = node.cloneNode();
                template.innerHTML = "<table><tbody>" + node.innerHTML + "</tbody></table>";
                template.setAttribute("depth", 2);

            } else if (this.regex.tableElementRegex.test(tagName)) {
                template = node.cloneNode()
                template.innerHTML = "<table>" + node.innerHTML + "</table>";
                template.setAttribute("depth", 1);
            } else {
                template = node.cloneNode();
                template.innerHTML = "<table><tbody><tr>" + node.innerHTML + "</tr></tbody></table>";
                template.setAttribute("depth", 3);
            }
            if (template.content.childNodes.length > 1) {
                template.content.childNodes.forEach(function (item) {
                    if (item.nodeName != "TABLE") {
                        if (item.nodeType == 1) {
                            errors.push(new Error("Syntax Error : Unexpected node - " + item.nodeName + "tag" + "inside " + tagName + " tag in the component " + componentName))
                        } else {
                            errors.push(new Error("Syntax Error : Unexpected text node - " + item.nodeValue + "present inside " + tagName + " tag in the component " + componentName))
                        }
                    }
                })
            }
            if (this.fromCLI) {
                template.refreshNode();
            }
            node.replaceWith(template);
        }
    }
    Compile.getParent = function (node) {
        var newNode = node;
        while (newNode) {
            if (newNode._parent) {
                if (newNode._parent.tagName && newNode._parent.tagName != "TEMPLATE") {
                    return newNode._parent;
                }
            }
            newNode = newNode._parent;
        }
    }
    Compile.getTemplateDirectChild = function (dynamicNodes, info) {
        var stack = [];
        info.dc = [];
        // info["hc"] = false;
        for (var i = 0; i < dynamicNodes.length; i++) {
            var dyn = dynamicNodes[i]
            var x = false;
            if ((dyn.trans || dyn.hc || dyn._comp || dyn.t == "cD" || dyn.t == "i" || dyn.t == "cM") && !dyn._igTs) {
                stack.forEach((item, index) => {
                    if (Compile.subArray(dyn.p, item)) {
                        x = true;
                    }
                });

                if (!x) {
                    stack.push(dyn.p);
                    // dyn["dirctChld"] = true;
                    info.dc.push(dyn.in)
                }
                info.hc = true;
                info.trans = true;
            }

        }
        if (!info.hc) {
            for (var i = 0; i < dynamicNodes.length; i++) {
                if (dynamicNodes[i].in != undefined && dynamicNodes[i].t != "cD" && dynamicNodes[i].t != "i") {
                    dynamicNodes[i].in = undefined;
                }
            }
            delete info.dc;
        }
    }
    Compile.getSwitchOrder = function (casesArr, tobj, defaultArr) {
        var keys = Object.keys(casesArr);
        var ln = keys.length;
        var club = [];
        for (var i = 0; i < ln; i++) {
            if (tobj[keys[i]].trans && casesArr[keys[i]].additional) {
                var stack = [];
                var flag = false;
                casesArr[keys[i]].dN.forEach((dyn) => {
                    club.push(dyn);
                })
                stack.push(keys[i]);
                while (keys[i] && casesArr[keys[i]].additional) {
                    flag = true;
                    var caseName;
                    if (casesArr[keys[i]].additional.next) {
                        caseName = casesArr[keys[i]].additional.next;
                    } else if (casesArr[keys[i]].additional.default) {
                        caseName = "default";
                        defaultArr.dN.forEach((dyn) => {
                            club.push(dyn);
                        })
                        i++;
                        continue;
                    }
                    if (tobj[caseName].trans) {
                        casesArr[caseName].dN.forEach((dyn) => {
                            club.push(dyn);
                        })
                    }
                    stack.push(caseName);
                    i++;
                }
                if (flag) {
                    this.getTransitionOrder(club, true);
                    // for(var i=0;i<stack.length;i++){
                    //     getTemplateDirectChild(club,tobj[stack[i]]);
                    // }
                }
            }
        }
    }
    Compile.getTransitionOrder = function (a, switchcase) {
        var count = 0;
        var previousTransition;
        for (var i = a.length - 1; i >= 0; i--) {
            if ((a[i].trans || this.regex.getTransitionOrderRegex.test(a[i].t)) && !a[i]._igTs) {
                a[i].in = count;
                count++;
                if (previousTransition) {
                    if (this.subArray(previousTransition.p, a[i].p)) {
                        a[i].chld = this.getChild(a, i, switchcase);
                    } else if (previousTransition) {
                        a[i].sibl = [previousTransition.in];
                    }
                }
                previousTransition = a[i]
            }
        }
    }
    Compile.subArray = function (sub, master) {
        //master//small //sub //big
        var brk = false;
        for (var i = 0; i < master.length; i++) {
            if (master[i] == sub[i]) {
                continue;
            } else {
                brk = true;
                break;
            }
        }

        if (!brk && master.length != sub.length) {
            return true;
        } else {
            return false;
        }
    }
    Compile.getChild = function (a, i, switchcase) {
        var childArr = [];
        var x = i;
        var previous;
        var caseName;
        if (switchcase) {
            caseName = a[i].cn;
        }
        while (a[i]) {
            i++;
            if (switchcase) {
                if (a[i] && a[i].cn != caseName) {
                    break;
                }
            }
            if (a[i] && (a[i].trans || a[i]._comp || this.regex.getChildRegex.test(a[i].t)) && !a[i]._igTs) {
                if (previous != undefined) {
                    if (this.subArray(a[i].p, previous)) {
                        //ignoring child's child
                        continue;
                    }
                }
                if (this.subArray(a[i].p, a[x].p)) {
                    childArr.push(a[i].in);
                } else {
                    break;
                }
                previous = a[i].p;
            }
        }
        if (a[i]) {
            a[x].sibl = [a[i].in];
        }
        return childArr;
    }
    Compile.svgFix = function (node, type, toBePushed, parentElementName, componentName, errors) {
        var templateNode = node.cloneNode();
        var newElementName;
        if (!node.content || !node.content.children || !node.content.children[0] || !node.content.children[0].tagName) {
            errors.push(new Error("Syntax Error in the node " + this.errorNodeDetails(node)));
            return node;
        } else {
            newElementName = node.content.children[0].tagName;
        }
        templateNode.innerHTML = "<svg>" + node.innerHTML + "</svg>"
        templateNode.setAttribute("depth", 1);
        return this.inner(node, templateNode, type, newElementName, parentElementName, toBePushed);
    }
    Compile.inner = function (node, templateNode, type, newElementName, parentElementName, toBePushed) {
        var newElement = this.addUnbound(node,newElement,newElementName,parentElementName);
        var parentElement = document.createElement(parentElementName);
        switch (type) {
            case "for":
                newElement.setAttribute("is", "for");
                newElement.setAttribute("lyte-for", "true");
                if (node.hasAttribute("unbound")) {
                    newElement.setAttribute("unbound", node.getAttribute("unbound"));
                }
                // newElement.setAttribute("items", node.getAttribute("items"));
                // newElement.setAttribute("item", node.getAttribute("item"));
                // newElement.setAttribute("index", node.getAttribute("index"));
                break;
            case "if":
            case "switch":
                newElement.setAttribute("is", type);
                newElement.setAttribute("lyte-" + type, "true");
                //newElement.setAttribute("value", node.getAttribute("value"));
                break;
            case "forIn":
                newElement.setAttribute("is", "forIn");
                newElement.setAttribute("lyte-forin", "true");
                if (node.hasAttribute("unbound")) {
                    newElement.setAttribute("unbound", node.getAttribute("unbound"));
                }
                // newElement.setAttribute("object", node.getAttribute("object"));
                // newElement.setAttribute("key", node.getAttribute("key"));
                // newElement.setAttribute("value", node.getAttribute("value"));
                break;
        }
        var attrs = node.attributes;
        for (var i = 0; i < attrs.length; i++) {
            if(!attrs[i].nodeName.startsWith("@")){
                newElement.setAttribute(attrs[i].nodeName, attrs[i].nodeValue);
            }    
        }
        if (parentElementName !== "SELECT" && (type === "for" || type === "forIn")) {
            this.removeAllAttributes(templateNode);
            toBePushed.actualTemplate = templateNode.outerHTML;
            toBePushed.tagName = parentElementName;
            if (templateNode.hasAttribute("depth")) {
                newElement.setAttribute("depth", templateNode.getAttribute("depth"));
            }
        } else {
            this.removeAllAttributes(node);
            toBePushed.actualTemplate = node.outerHTML;
        }
        node.replaceWith(newElement);
        return newElement;
    }
    Compile.replaceParentNode = function (node, type, toBePushed, componentName, errors, forHold, nearByParent) {
        var parentNode = this.getParent(node);
        if (parentNode && nearByParent) {
            if (nearByParent == "svg") {
                var parentElementName = parentNode.tagName;
                toBePushed.svg = true;
                return this.svgFix(node, type, toBePushed, parentElementName, componentName, errors);
            }
            return node;
        }
        if (!parentNode) {
            return node;
        }
        var parentElementName = parentNode.tagName;
        var newElementName;
        var templateNode = node.cloneNode();
        var childTagName;
        if (forHold) {
            for (var i = 0; i < node.content.children.length; i++) {
                if (node.content.children[i].tagName !== "TEMPLATE") {
                    if (this.regex.tableTags.test(node.content.children[i].tagName)) {
                        childTagName = node.content.children[i].tagName;
                    }
                    break;
                }
            }
        }
        if (parentNode && (this.regex.tableTagsWithSelect.test(parentElementName))) {
            switch (parentElementName) {
                case "SELECT":
                    newElementName = "option";
                    break;
                case "TR":
                    if (!forHold) {
                        templateNode.innerHTML = "<table><tbody><tr>" + node.innerHTML + "</tr></tbody></table>"
                        templateNode.setAttribute("depth", 3);
                        newElementName = "td";
                    } else {
                        if (childTagName && childTagName == "TR") {
                            errors.push(new Error("Syntax Error : Unexpected node - " + childTagName + "tag inside " + parentElementName + " tag in the component " + componentName))
                        } else {
                            templateNode.innerHTML = "<table><tbody><tr>" + node.innerHTML + "</tr></tbody></table>"
                            templateNode.setAttribute("depth", 3);
                            newElementName = "td";
                        }
                    }
                    break;
                case "TABLE":
                    if (!forHold) {
                        templateNode.innerHTML = "<table>" + node.innerHTML + "</table>";
                        templateNode.setAttribute("depth", 1);
                        newElementName = "tbody";
                    } else {
                        if (childTagName && (childTagName == "TBODY" || childTagName == "THEAD" || childTagName == "TFOOT")) {
                            templateNode.innerHTML = "<table>" + node.innerHTML + "</table>";
                            templateNode.setAttribute("depth", 1);
                        } else if (childTagName && childTagName == "TR") {
                            templateNode.innerHTML = "<table><tbody>" + node.innerHTML + "</tbody></table>";
                            templateNode.setAttribute("depth", 2);
                        } else {
                            templateNode.innerHTML = "<table><tbody><tr>" + node.innerHTML + "</tr></tbody></table>"
                            templateNode.setAttribute("depth", 3);
                        }
                        newElementName = "tbody";
                    }
                    break;
                case "TBODY":
                case "THEAD":
                case "TFOOT":
                    if (!forHold) {
                        templateNode.innerHTML = "<table><tbody>" + node.innerHTML + "</tbody></table>";
                        templateNode.setAttribute("depth", 2);
                        newElementName = "tr";
                    } else {
                        if (childTagName && (childTagName == "TR")) {
                            templateNode.innerHTML = "<table><tbody>" + node.innerHTML + "</tbody></table>";
                            templateNode.setAttribute("depth", 2);
                        } else {
                            templateNode.innerHTML = "<table><tbody><tr>" + node.innerHTML + "</tr></tbody></table>"
                            templateNode.setAttribute("depth", 3);
                        }
                        newElementName = "tr";
                    }
                    break;
            }
            if (templateNode.content.childNodes.length > 1) {
                templateNode.content.childNodes.forEach(function (item) {
                    if (item.nodeName != "TABLE") {
                        if (item.nodeType == 1) {
                            errors.push(new Error("Syntax Error : Unexpected node - " + item.nodeName + "tag inside " + parentElementName + " tag in the component " + componentName))
                        } else {
                            errors.push(new Error("Syntax Error : Unexpected text node - " + item.nodeValue + "present inside " + parentElementName + " tag in the component " + componentName))
                        }
                    }
                })
            }
            return this.inner(node, templateNode, type, newElementName, parentElementName, toBePushed);
        }

        return node;
    }
    Compile.removeAllAttributes = function (node) {
        let ls = node.querySelectorAll("errorinfo");
        for (let i = 0; i < ls.length; i++) {
            ls.removeAttribute("errorinfo");
        }
        var arr = Array.from(node.attributes);
        for (let i = 0; i < arr.length; i++) {
            if (arr[i].nodeName != "depth" && arr[i].nodeName != "is") {
                node.removeAttribute(arr[i].nodeName);
            }
        }
    }

    Compile.siblingNullCheck = function (nextSibling) {
        if (nextSibling.previousSibling.nodeValue === null || nextSibling.nextSibling.nodeValue === null) {
            return null;
        } else {
            return 1;
        }
    }
    Compile.jspIfToSwitch = function (element, check = {}) {
        var temp = document.createElement("template");
        if (element.hasAttribute("is") && element.getAttribute("is") == "if") {
            temp.setAttribute("is", "switch");
            temp.setAttribute("l-c", true);
        }
        if(element.hasAttribute("_jsp")){
            temp.setAttribute("_jsp",element.getAttribute("_jsp"));
        }
        this.getCase(temp, element, check);
        return temp;
    }

    Compile.appendBreak = function (temp) {
        var breakTemp = document.createElement("template");
        breakTemp.setAttribute("is", "break");
        temp.content.appendChild(breakTemp);
    }
    Compile.ifNextElementSibling = function(nextSibling,temp, svg, hasBreak, check, errorObj, impObj, parentNode, parentNodeType){
        var falseTemp;
        var type;
        var appendBreak;
        if(nextSibling.hasAttribute("lyte-else")) {
            type = "lyte-else";
        } else if(nextSibling.hasAttribute("lyte-else-if")) {
            type = "lyte-else-if";
        } else if(nextSibling.hasAttribute("lyte-await")){
            impObj.options.internalSlicer.$promise = true;
            type = "__lyteAwait__"
        } else if(nextSibling.hasAttribute("lyte-fail")){
            impObj.options.internalSlicer.$promise = true;
            type = "__lyteFail__"
        }
        if(type) {
            if(type === "__lyteAwait__" || type == "__lyteFail__"){
                if(type == "__lyteAwait__"){
                    nextSibling.removeAttribute("lyte-await");
                }
                else if(type == "__lyteFail__"){
                    nextSibling.removeAttribute("lyte-fail");
                }
                if(svg && nextSibling.tagName=="template"){
                    falseTemp = document.createElement("template");
                    falseTemp.setAttribute("case", type);
                    falseTemp.innerHTML = nextSibling.innerHTML
                } else if(nextSibling.tagName === "TEMPLATE") {
                    falseTemp = nextSibling.cloneNode();
                    falseTemp.innerHTML = nextSibling.innerHTML;
                } else {
                    falseTemp = document.createElement("template");
                    falseTemp.setAttribute("case", type);
                    falseTemp.innerHTML = nextSibling.outerHTML;
                }
                falseTemp.setAttribute("case", type);
                falseTemp.setAttribute("has-child", true);
                if(nextSibling.nextSibling && nextSibling.previousSibling && this.siblingNullCheck(nextSibling) && (nextSibling.nextSibling.nodeValue.trim() === "") && ("" === nextSibling.previousSibling.nodeValue.trim())) {
                    nextSibling.nextSibling.remove();
                }
                nextSibling.remove();
                debugger
                if(parentNodeType == "if" || parentNodeType == "switch"){
                    appendBreak = true;
                }
                parentNode.setAttribute("has-child",true);
                // var breakTemp = document.createElement("template");
                // breakTemp.setAttribute("is", "break");
                // falseTemp.parent.appendChild(breakTemp);
            }
            else if(type === "lyte-else") {
                nextSibling.removeAttribute("lyte-else");
                if(svg && nextSibling.tagName=="template"){
                    falseTemp = document.createElement("template");
                    // falseTemp.setAttribute("case", "false");
                    falseTemp[this.htmlStr.innerHTML] = nextSibling[this.htmlStr.innerHTML];
                } else if(nextSibling.tagName === "TEMPLATE") {
                    falseTemp = nextSibling.cloneNode();
                    falseTemp[this.htmlStr.innerHTML] = nextSibling[this.htmlStr.innerHTML];
                } else {
                    falseTemp = document.createElement("template");
                    // falseTemp.setAttribute("case", "false");
                    falseTemp[this.htmlStr.innerHTML] = nextSibling[this.htmlStr.outerHTML];
                }
                falseTemp.setAttribute("default", "");
                if(nextSibling.nextSibling && nextSibling.previousSibling && this.siblingNullCheck(nextSibling) && (nextSibling.nextSibling.nodeValue.trim() === "") && ("" === nextSibling.previousSibling.nodeValue.trim())) {
                    nextSibling.nextSibling.remove();
                }
                nextSibling.remove();
            }
            else {
                // falseTemp = document.createElement("template");
                // falseTemp.setAttribute("case", "false");
                // falseTemp.innerHTML = this.handleLyteIf(nextSibling,svg).outerHTML;
                falseTemp = this.handleLyteIf(nextSibling, svg, check, errorObj, impObj).content;
                if(nextSibling.nextSibling  && nextSibling.previousSibling && this.siblingNullCheck(nextSibling)  && (nextSibling.nextSibling.nodeValue.trim() === "") && ("" === nextSibling.previousSibling.nodeValue.trim())) {
                    nextSibling.nextSibling.remove();
                }
                nextSibling.remove();
    
            }
            if(hasBreak || appendBreak){
                this.appendBreak(falseTemp);
            }
            temp.content.appendChild(falseTemp);
            // if(appendBreak){
            //     this.appendBreak(temp);
            // }
        }
        return falseTemp;
    }
    Compile.getCase = function (temp, element, check = {}) {
        var ifStmt, trueCase, falseCase;
        ifStmt = element.getAttribute("value");
        if (element.getAttribute("_jsp") == "true") {
            trueCase = element.content.children[0];
            falseCase = element.content.children[1];
        } else {
            trueCase = element.content.querySelector("[case=true]");
            falseCase = element.content.querySelector("[case=false]");
        }
        if (!trueCase && falseCase) {
            //create empty true case
            let tempCase = document.createElement("template");
            tempCase.setAttribute("case", "true");
            trueCase = tempCase;
        }
        if (trueCase || falseCase) {
            if (trueCase && trueCase.getAttribute("case") == "true") {
                let caseTemp;
                if (trueCase.tagName != "TEMPLATE") {
                    trueCase.removeAttribute("case");
                    caseTemp = this.createCase(ifStmt, trueCase, check);
                } else {
                    caseTemp = this.createCase(ifStmt, trueCase.content, check);
                }
                temp.content.appendChild(caseTemp);
                this.appendBreak(caseTemp);
            }
            if (falseCase && falseCase.getAttribute("case") == "false") {
                var elif = false;
                if (falseCase.tagName != "TEMPLATE") {
                    falseCase.removeAttribute("case");
                    let tempCase = document.createElement("template");
                    tempCase.setAttribute("default", "");
                    tempCase.content.appendChild(falseCase);
                    temp.content.appendChild(tempCase);
                } else {
                    if (falseCase.content.children && falseCase.content.children.length && falseCase.content.children[0].getAttribute("lyte-elif") == "true") {
                        elif = falseCase.content.children[0];
                        this.getCase(temp, elif, check);
                    } else {
                        let tempCase = document.createElement("template");
                        tempCase.setAttribute("default", "");
                        tempCase.content.appendChild(falseCase.content);
                        temp.content.appendChild(tempCase);
                    }
                }
            }
        } else {
            temp[this.htmlStr["innerHTML"]] = element[this.htmlStr["innerHTML"]];
        }
    }
    Compile.createCase = function (value, content, check = {}) {
        let tempCase = document.createElement("template");
        tempCase.setAttribute("is", "case");
        if (check.fromParser) {
            let syn = check.syntaxCheckWorkerNew(value, check);
            tempCase.setAttribute("case", syn.text);
            if (!tempCase.attrType) {
                tempCase.attrType = {};
            }
            tempCase.attrType.case = syn.type;
        } else {
            tempCase.setAttribute("case", value);
        }
        tempCase.content.appendChild(content);
        return tempCase;
    }
    Compile.checkAttr = function (obj, key) {
        return (obj.hasAttribute(key)) ? key : false;
    }

    Compile.getErrorInfo = function (obj) {
        let key = this.checkAttr(obj, "lyte-if") || this.checkAttr(obj, "lyte-switch") || this.checkAttr(obj, "lyte-for") || this.checkAttr(obj, "lyte-for-in"),
            errorinfo = undefined;
        if (!key && (this.regex.lyteTemplateType2.test(obj.getAttribute("is")))) {
            key = obj.getAttribute("is");
        }
        if (key) {
            errorinfo = {
                name: "errorinfo",
                details: {
                    tagName: obj.tagName,
                    name: key,
                    value: obj.getAttribute(key).replace(/{{/g, "@mus@")
                }
            }
        }
        return errorinfo;
    }
    Compile.handleLyteIf = function (element, svg, check = {}, errorObj, impObj) {
        var ifStmt;
        var temp = document.createElement("template");
        let errorinfo = this.getErrorInfo(element);
        let ifNode = false;
        if (element.hasAttribute("lyte-if")) {
            ifNode = true;
            ifStmt = element.getAttribute("lyte-if");
            element.removeAttribute("lyte-if")
            temp.setAttribute("is", "switch");
            if(element.hasAttribute("__vp")){
                temp.setAttribute("__vp", element.getAttribute("__vp"));
            }
            if (check.fromParser) {
                if (!temp.attrType) {
                    temp.attrType = {};
                }
                let syn = check.syntaxCheckWorkerNew(ifStmt, check);
                temp.setAttribute("value", syn.text);
                temp.attrType.value = syn.type;
            } else {
                temp.setAttribute("value", ifStmt);
            }
            temp.setAttribute("l-c", true);
            temp.setAttribute("_new", "true");
            temp.setAttribute("errorinfo", JSON.stringify(errorinfo));
        } else if (element.hasAttribute("lyte-else-if")) {
            ifStmt = element.getAttribute("lyte-else-if");
            element.removeAttribute("lyte-else-if");
        }
        try {
            if (check.ide && window.elifHold) {
                temp.setAttribute("el", true);
                window.elifHold = false;
            }
        } catch (e) {
            console.log("ide");
        }
        var trueTemp;
        if (svg && element.tagName == "template") {
            trueTemp = document.createElement("template");
            trueTemp[this.htmlStr["innerHTML"]] = element[this.htmlStr["innerHTML"]]
        } else if (element.tagName === "TEMPLATE") {
            trueTemp = element.cloneNode();
            trueTemp[this.htmlStr["innerHTML"]] = element[this.htmlStr["innerHTML"]];
        } else {
            trueTemp = document.createElement("template");
            trueTemp[this.htmlStr["innerHTML"]] = element[this.htmlStr["outerHTML"]];
        }
        if (trueTemp.hasAttribute("is")) {
            var wrapperTemp = document.createElement("template");
            wrapperTemp.content.appendChild(trueTemp);
            trueTemp = wrapperTemp;
        }
        temp.content.appendChild(trueTemp);
        if (check.fromParser) {
            if (!trueTemp.attrType) {
                trueTemp.attrType = {};
            }
            let syn = check.syntaxCheckWorkerNew(ifStmt, check);
            trueTemp.setAttribute("case", syn.text);
            trueTemp.attrType.case = syn.type;
        } else {
            trueTemp.setAttribute("case", ifStmt);
        }
        trueTemp.setAttribute("is", "case");
        if (temp.getAttribute("is") == "switch" && temp.hasAttribute("value")) {
            if (temp.getAttribute("value").startsWith("{{")) {
                temp.removeAttribute("value");
            }
        }
        var breakTemp = document.createElement("template");
        breakTemp.setAttribute("is", "break");
        trueTemp.content.appendChild(breakTemp);
        // Else case handling
        // if (element.nextElementSibling) {
        //     var nextSibling = element.nextElementSibling;
        //     var falseTemp;
        //     var type;
        //     if (nextSibling.hasAttribute("lyte-else")) {
        //         type = "lyte-else";
        //     } else if (nextSibling.hasAttribute("lyte-else-if")) {
        //         type = "lyte-else-if";
        //     }
        //     if (type) {
        //         if (type === "lyte-else") {
        //             nextSibling.removeAttribute("lyte-else");
        //             if (svg && nextSibling.tagName == "template") {
        //                 falseTemp = document.createElement("template");
        //                 // falseTemp.setAttribute("case", "false");
        //                 falseTemp[this.htmlStr["innerHTML"]] = nextSibling[this.htmlStr["innerHTML"]]
        //             } else if (nextSibling.tagName === "TEMPLATE") {
        //                 falseTemp = nextSibling.cloneNode();
        //                 falseTemp[this.htmlStr["innerHTML"]] = nextSibling[this.htmlStr["innerHTML"]];
        //             } else {
        //                 falseTemp = document.createElement("template");
        //                 // falseTemp.setAttribute("default", "");
        //                 falseTemp[this.htmlStr["innerHTML"]] = nextSibling[this.htmlStr["outerHTML"]];
        //             }
        //             falseTemp.setAttribute("default", "");
        //             if (nextSibling.nextSibling && nextSibling.previousSibling && this.siblingNullCheck(nextSibling) && (nextSibling.nextSibling.nodeValue.trim() === "") && ("" === nextSibling.previousSibling.nodeValue.trim())) {
        //                 nextSibling.nextSibling.remove();
        //             }
        //             nextSibling.remove();
        //         } else {
        //             falseTemp = this.handleLyteIf(nextSibling, svg, check, errorObj).content;

        //             if (nextSibling.nextSibling && nextSibling.previousSibling && this.siblingNullCheck(nextSibling) && (nextSibling.nextSibling.nodeValue.trim() === "") && ("" === nextSibling.previousSibling.nodeValue.trim())) {
        //                 nextSibling.nextSibling.remove();
        //             }
        //             nextSibling.remove();

        //         }
        //         temp.content.appendChild(falseTemp);
        //     }
        // }
        //Else case handling
        let nxtSibl = element.nextElementSibling;
        if(nxtSibl) { //may be else
            this.ifNextElementSibling(nxtSibl, temp, svg, undefined, check, errorObj, impObj, element, "if");
            nxtSibl = element.nextElementSibling;
            if(nxtSibl && (nxtSibl.hasAttribute("lyte-await") || nxtSibl.hasAttribute("lyte-fail"))){ // may be wait
                this.ifNextElementSibling(nxtSibl, temp, svg, undefined, check, errorObj, impObj, element, "if");
                nxtSibl = element.nextElementSibling;
                if(nxtSibl){ // may be fail
                    this.ifNextElementSibling(nxtSibl, temp, svg, undefined, check, errorObj, impObj, element, "if");
                }
            }
        }
        if (ifNode && element.nextSibling && element.previousSibling && this.siblingNullCheck(element) && (element.nextSibling.nodeValue.trim() === "") && ("" === element.previousSibling.nodeValue.trim())) {
            element.nextSibling.remove();
        }
        return temp;
    }

    Compile.handleLyteSwitch = function (node, svg, check, errorObj, impObj) {
        var template;
        var switchValue = node.getAttribute("lyte-switch");
        let errorinfo = this.getErrorInfo(node);
        var childElements;
        var type;
        if (node.tagName === "TEMPLATE") {
            template = node;
            template.removeAttribute("lyte-switch");
            childElements = Array.from(template.content.children);
            type = "template";
        } else if (svg && node.tagName == "template") {
            template = document.createElement("template");
            var attrs = node.attributes;
            for (var j = 0; j < attrs.length; j++) {
                template.setAttribute(attrs[j].nodeName, attrs[j].nodeValue);
            }
            // falseTemp.setAttribute("case", "false");
            template.removeAttribute("lyte-switch");
            // template.innerHTML = node.innerHTML
            type = "template";
            childElements = Array.from(node.children);
        } else {
            node.removeAttribute("lyte-switch");
            template = document.createElement("template");
            if (check.ide) {
                template.setAttribute("nt", true);
            }
            childElements = Array.from(node.children);
        }
        template.setAttribute("_new", "true");

        for (var i = 0; i < childElements.length; i++) {
            var child = childElements[i];
            if (child.hasAttribute("lyte-case")) {
                var isChildTemplate = child.tagName === "TEMPLATE";
                if (svg && child.tagName === "template") {
                    var temp = document.createElement("template");
                    temp[this.htmlStr["innerHTML"]] = child[this.htmlStr["innerHTML"]];
                    var attrs = child.attributes;
                    for (var j = 0; j < attrs.length; j++) {
                        temp.setAttribute(attrs[j].nodeName, attrs[j].nodeValue);
                    }
                    child = temp;
                    isChildTemplate = true;
                }
                //var childTemplate = isChildTemplate ? child : document.createElement("template");
                var childTemplate = child;
                var hasBreak = child.hasAttribute("lyte-break");
                var caseValue = child.getAttribute("lyte-case");

                child.removeAttribute("lyte-case");
                child.removeAttribute("lyte-break");
                if (child.hasAttribute("lyte-if")) {
                    isChildTemplate = false;
                    var oldChild = child;
                    child = this.handleLyteIf(child, svg, check, errorObj, impObj);
                    oldChild.remove();
                } else if (child.hasAttribute("lyte-switch")) {
                    isChildTemplate = false;
                    this.handleLyteSwitch(child, svg, check, errorObj, impObj);
                }
                if (isChildTemplate) {
                    template.content.appendChild(child, true);
                    child.setAttribute("case", caseValue);
                    child.removeAttribute("lyte-case");
                } else {
                    childTemplate = document.createElement("template");
                    childTemplate.setAttribute("case", caseValue);
                    child.removeAttribute("lyte-case");
                    childTemplate.content.appendChild(child, true);
                    template.content.appendChild(childTemplate);
                }
                if (hasBreak) {
                    var breakTemp = document.createElement("template");
                    breakTemp.setAttribute("is", "break");
                    childTemplate.content.appendChild(breakTemp);
                    child.removeAttribute("lyte-break");
                }
            }
        }
        var defaultCase; // = type ? node.content.querySelector("[lyte-default]") : node.querySelector("[lyte-default]");
        if (svg && node.tagName == "template") {
            defaultCase = type ? node.querySelector("[lyte-default]") : node.querySelector("[lyte-default]");
        } else {
            defaultCase = type ? node.content.querySelector("[lyte-default]") : node.querySelector("[lyte-default]");
        }
        if (defaultCase) {
            var isDefCaseTemp;
            if (defaultCase.tagName === "TEMPLATE") {
                isDefCaseTemp = true;
            } else if (svg && defaultCase.tagName === "template") {
                var temp = document.createElement("template");
                temp[this.htmlStr["innerHTML"]] = defaultCase[this.htmlStr["innerHTML"]];
                var attrs = defaultCase.attributes;
                for (var j = 0; j < attrs.length; j++) {
                    temp.setAttribute(attrs[j].nodeName, attrs[j].nodeValue);
                }
                defaultCase = temp;
                isDefCaseTemp = true;
            }
            defaultCase.removeAttribute("lyte-default");
            if (defaultCase.hasAttribute("lyte-if")) {
                isDefCaseTemp = false;
                var oldDefault = defaultCase;
                defaultCase = this.handleLyteIf(defaultCase, svg, check, errorObj, impObj);
                oldDefault.remove();
            } else if (defaultCase.hasAttribute("lyte-switch")) {
                isDefCaseTemp = false;
                this.handleLyteSwitch(defaultCase, svg, check, errorObj, impObj);
            }
            if (isDefCaseTemp) {
                template.content.appendChild(defaultCase, true);
                defaultCase.setAttribute("default", "");
            } else {
                var defTemp = document.createElement("template");
                defTemp.setAttribute("default", "");
                defTemp.content.appendChild(defaultCase, true);
                template.content.appendChild(defTemp);
            }
        }
        let nxtSibl = node.nextElementSibling;
        if(nxtSibl && (nxtSibl.hasAttribute("lyte-await") || nxtSibl.hasAttribute("lyte-fail"))){ // may be wait
            this.ifNextElementSibling(nxtSibl, template, svg, true, check, errorObj, impObj, node, "switch");
            nxtSibl = node.nextElementSibling;
            if(nxtSibl){ // may be fail
                this.ifNextElementSibling(nxtSibl, template, svg, true, check, errorObj, impObj, node, "switch");
            }
        }
        if (check.fromParser) {
            if (!template.attrType) {
                template.attrType = {};
            }
            let syn = check.syntaxCheckWorkerNew(switchValue, check);
            template.setAttribute("value", syn.text);
            template.attrType.value = syn.type;
        } else {
            template.setAttribute("value", switchValue);
        }

        template.setAttribute("is", "switch");
        if (!type) {
            template.setAttribute("errorinfo", JSON.stringify(errorinfo));
            node[this.htmlStr["innerHTML"]] = template[this.htmlStr["outerHTML"]];
        } else {
            node = template;
            node.setAttribute("errorinfo", JSON.stringify(errorinfo));
        }
        return node;
    }
    

    Compile.getTemplateFromString = function (str, outlet) {
        var div = document.createElement("div");
        div.innerHTML = str;
        div.remove();
        return div.firstChild;
    }

    Compile.addComponentTemplate = function (html, componentName) {
        var temp = document.createElement("template");
        if (componentName) {
            var s = document.createElement("template");
            s.setAttribute("tag-name", componentName);
            s.setAttribute("registry-name", this.constructor.name);
            s.innerHTML = html;
            temp.content.appendChild(s);
        } else {
            temp.innerHTML = html;
        }
        Compile.componentsDiv.appendChild(temp.content);
    }
    Compile.compileComponent = function (obj,options={}) {
        let componentName = obj.name
        let templateStr = obj.template;
        let jsStr = obj.js.toString();
        let observedAttributes = obj.observedAttributes;
        let compiledStr = "";

        if (typeof componentName !== "string" || componentName == "") {
            return {
                errors: [{
                    "message": "Invalid Component Name - " + componentName
                }]
            }
        } else if (typeof templateStr !== "string" || templateStr == "") {
            return {
                errors: [{
                    "message": "Invalid Template String - " + templateStr
                }]
            }
        } else if (typeof jsStr !== "string" || jsStr == "") {
            return {
                errors: [{
                    "message": "Invalid Javascript String - " + jsStr
                }]
            }
        } else {
            let errorObj = { mustacheSyntax:{arr:[],obj:{}}, warnings: [] }
            this.decodeHtmlEntities = htmlParser.decodeHtmlEntities;
            let obj = htmlParser.htmlToJsObjectLyteRendered.toObject(templateStr, {
                enhanced: true,
                fromCLI: true,
                Compile: Compile
            },errorObj);
            if (obj.querySelector("[tag-name]")) {
                let returnedObj = this.getDynamicNodes(componentName, undefined, obj, true, errorObj);
                if(options.checkMustacheSyntax){
                    console.error(errorObj.mustacheSyntax);
                }
                if (returnedObj.errors && returnedObj.errors.length) {
                    console.error("Errors in the component ", componentName, " during compilation!", returnedObj.errors);
                    return {
                        componentName: componentName,
                        errors: returnedObj.errors
                    };
                }
                if (returnedObj.warnings && returnedObj.warnings.length) {
                    console.warn("Warnings in the component ", componentName, " during compilation!", returnedObj.warnings);
                }

                var firstStr = /class \w+ extends .*?{/g.exec(jsStr);
                var strArr = jsStr.split(firstStr[0]);
                compiledStr = firstStr[0] + "\nstatic _template = " + JSON.stringify(JSON.parse(returnedObj.template)) + ";\n" + "static _dynamicNodes = " + JSON.stringify(returnedObj.dynamicNodes) + ";\n" + (returnedObj._templateAttributes ? ("static _templateAttributes = " + returnedObj._templateAttributes + ";\n") : "") + "static _observedAttributes = " + (observedAttributes ? JSON.stringify(observedAttributes) : "[]") + ";\n";
                compiledStr = strArr[0] + compiledStr + strArr[1];


            } else {
                return {
                    errors: [{
                        "message": "Missing attribute `tag-name` in the component " + componentName
                    }]
                }
            }
            return compiledStr;
        }
    }
// }
if(  true && typeof module.exports === "object" ) {
    module.exports = Compile;
}
else{
    window.ClientCompilerInternal = Compile;
}
// export default ClientCompiler

/***/ }),

/***/ 43638185:
/*!*******************************************************************************************!*\
  !*** ./node_modules/@slyte/component/src/compiler/client/lyte-client-compile-advanced.js ***!
  \*******************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


var _espree_espree_js__WEBPACK_IMPORTED_MODULE_0___namespace_cache;
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ClientCompilerAdvanced": () => (/* binding */ ClientCompilerAdvanced)
/* harmony export */ });
/* harmony import */ var _cli_expHandlers_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../cli/expHandlers.js */ 41325797);
/* harmony import */ var _cli_expHandlers_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_cli_expHandlers_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _espree_espree_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../../../espree/espree.js */ 42925032);
/* harmony import */ var _lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-client-compile.js */ 52516253);



var ClientCompilerAdvanced = _lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_1__.ClientCompiler;
ClientCompilerAdvanced.expHandlers = (_cli_expHandlers_js__WEBPACK_IMPORTED_MODULE_2___default());
ClientCompilerAdvanced.Espree = /*#__PURE__*/ (_espree_espree_js__WEBPACK_IMPORTED_MODULE_0___namespace_cache || (_espree_espree_js__WEBPACK_IMPORTED_MODULE_0___namespace_cache = __webpack_require__.t(_espree_espree_js__WEBPACK_IMPORTED_MODULE_0__, 2)));


/***/ }),

/***/ 52516253:
/*!**********************************************************************************!*\
  !*** ./node_modules/@slyte/component/src/compiler/client/lyte-client-compile.js ***!
  \**********************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ClientCompiler": () => (/* binding */ ClientCompiler)
/* harmony export */ });
/* harmony import */ var _cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../cli/lyte-base-compile.js */ 93957260);
/* harmony import */ var _cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../cli/lyte-client-compile.js */ 92997744);
/* harmony import */ var _cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../lyte-component.js */ 26591616);
// import expHandlers from "../expHandlers.js";
// import * as Espree from "espree";



/**
     *  @param1 actualComponent - Actual component where we are gonna render the dynamicTemplate.
     *  @param2 templateToRender - clonedNode / String (Which will be converted to DOM) - that needs to be rendered. 
     *  @returnVal rendered dom
*/
;(_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default().compileDynamicTemplate) = function(actualComponent, templateToRender) {
    if (!actualComponent || !actualComponent.hasAttribute("lyte-rendered")) {
        console.error("Provided element is not a component / not rendered yet")
        return;
    }
    if (!templateToRender) {
        console.error("No template is provided for appending")
        return;
    }
    if (typeof templateToRender === "string") {
        var temp = document.createElement("div");
        temp.innerHTML = templateToRender;
        templateToRender = temp.childNodes[0];
    }
    var templateToRenderAct = document.createElement("template");
    //templateToRenderAct.content.appendChild(templateToRender);
    // if (Lyte._ie) {
    //     templateToRenderAct.appendChild(templateToRender);
    // } else {
        templateToRenderAct.content.appendChild(templateToRender);
    // }
    var dynamicNodes = this.getDynamicNodes(actualComponent.tagName, undefined, templateToRenderAct);
    let gen = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.doCompile(templateToRenderAct.content, dynamicNodes.dynamicNodes, actualComponent.localName, actualComponent.constructor, undefined, actualComponent.component.constructor);
    let gnxt = gen.next(), gval;
    while(gnxt.done == false){
        gval = gnxt.value;
        if(typeof gval == "function"){
            gval();
        }
        gnxt = gen.next()
    }
    var returnVal = actualComponent.renderNodes(templateToRenderAct, dynamicNodes.dynamicNodes, undefined, {}, true, {}, templateToRenderAct.outerHTML)
    return returnVal;

}
;(_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default().compileTemplate) = function(templateToRender){
    if (typeof templateToRender === "string") {
        var temp = document.createElement("div");
        temp.innerHTML = templateToRender.trim();
        templateToRender = temp.childNodes[0];
    }
    var tagName = templateToRender.getAttribute("tag-name");
    var dynamicNodes = this.getDynamicNodes(tagName, undefined, templateToRender);  
    return {
        _template : templateToRender.outerHTML,
        _dynamicNodes : dynamicNodes.dynamicNodes
    }
}
/**
 * @param1 func - function to execute without losing bindings
 * @param2 scope - scope with which the function must be executed 
 */
;(_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default().doDomProcessing) = function(func, scope) {
    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.ignoreDisconnect = true;
    if (scope) {
        func.call(scope);
    } else {
        func();
    }
    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.ignoreDisconnect = false;
}

;(_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default().getComponentTemplate) = function (componentName, registryName) {
    let dummyLyteComponentsDiv = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.getComponentsDiv(_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.dummyLyteComponentsDiv, registryName);
    var temp = dummyLyteComponentsDiv.querySelector("template[tag-name=" + componentName + "]");
    if (temp) {
        if (temp.content) {
            return temp.content;
        } else {
            var div = document.createElement("div")
            div.innerHTML = temp.outerHTML;
            return div.querySelector("template");
        }
    } else {
        console.error("No such component is registered");
        return;
    }
}
;(_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default().modifyTemplate) = function(componentName,template,compClass){
    // this.constructor._registeredCommonClass['client-comp1'].activeInstances
    var registryClass = compClass._registryClass;
    if(registryClass._reRegisteredComponents.indexOf(componentName) == -1){
        registryClass._reRegisteredComponents.push(componentName);
    }
    if(registryClass._registeredCommonClass[componentName]) {
        // var comp = registryClass._registeredCommonClass[componentName];
        var compClassDef = compClass; //this._registeredComponents[componentName];
        if(compClassDef.activeInstances > 0) {
            console.warn("There are active instances of the component " + componentName + " and hence cannot be unregistered");
        }
        else{
            if(this.getDynamicNodes) {
                var returnVal = this.getDynamicNodes(componentName, undefined, template);
                if(returnVal.errors){
                    console.error("Error in the component",returnVal.componentName,returnVal.errors);
                    return false;
                }else{  
                    // var docTemplate = _LC.lyteComponentsDiv.querySelector("template[tag-name="+componentName+ "]")//check
                    var lyteComponentsDiv = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.getComponentsDiv(_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.lyteComponentsDiv, registryClass.name);
                    var docTemplate = lyteComponentsDiv.querySelector("template[tag-name="+componentName+ "]");
                    if(docTemplate) {
                        // var docTemplateCopy = Array.from(_LC.lyteComponentsDiv.querySelectorAll("template[tag-name="+componentName+ "]"));
                        var docTemplateCopy = Array.from(
                            lyteComponentsDiv.querySelectorAll("template[tag-name="+componentName+ "]")
                        );
                        // Array.from(document.querySelectorAll("template[tag-name="+componentName+ "]")).forEach(el => el.remove());	
                        docTemplateCopy.forEach(function(el){
                            el.remove()
                        });
                    }
                    if(compClassDef._depthTemp) {
                        compClassDef._depthTemp.remove();    
                    }
                    compClassDef._depthTemp = document.createElement("template");
                    // compClassDef.originalDef._template =returnVal.template;
                    // compClassDef.originalDef._dynamicNodes = returnVal.dynamicNodes;
                    // compClassDef.originalDef._templateAttributes = returnVal._templateAttributes;
                    compClassDef._template = returnVal.template;
                    compClassDef._dynamicNodes = returnVal.dynamicNodes;
                    compClassDef._templateAttributes = returnVal._templateAttributes;
                    // debugger;
                    compClassDef._registered = false;
                    compClassDef.__isRegistered = false;
                    if(registryClass.lazyRegister){
                        var lazyScheduler = registryClass.lazyScheduler;
                        if(lazyScheduler.currentTask && lazyScheduler.currentTask.id === registryClass.name+"$"+componentName){
                            lazyScheduler.currentTask.addEventListener("done", function(){
                                registryClass.registerComponent(componentName,compClassDef)//,compClassDef.originalOpt,compClassDef,this
                            });
                        }
                        else{
                            registryClass.registerComponent(componentName,compClassDef)//,compClassDef.originalOpt,compClassDef,this
                        }
                    }
                    else{
                        registryClass.registerComponent(componentName,compClassDef)//,compClassDef.originalOpt,compClassDef,this
                    }
                }
            }
        }
    }
    else{
    console.warn(componentName , "is not registered");
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.core._constructor._registerComponent = function(a,b,c,d,e){
    if(d._reRegisteredComponents.indexOf(a) != -1){
        c._template = JSON.parse(c._template);
    }
    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.core._registerComponent.call(this,a,b,c,d,e);
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.registerComponent = function(componentName, componentClass ,registryInstance){
    let registry = componentClass._registryClass;
    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.core.registerComponent.call(this,componentName, componentClass, registryInstance);
    if(componentClass.__isRegistered){
        registry._reRegisteredComponents.splice(registry._reRegisteredComponents.indexOf(componentName),1);
    }
}
// var ClientCompilerAdvanced = function(){
//     var args = Array.from(arguments);
//     args.$push(expHandlers,Espree)
//     ClientCompiler.apply(this,args);
// }
// debugger
var ClientCompiler = Object.assign((_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_2___default()), (_cli_lyte_client_compile_js__WEBPACK_IMPORTED_MODULE_0___default()));
// debugger
// try {
//     module.exports = ClientCompiler;
// }
// catch{
    
// }


/***/ }),

/***/ 43280731:
/*!************************************************************************!*\
  !*** ./node_modules/@slyte/component/src/directives/lyte-directive.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Directive": () => (/* binding */ Directive)
/* harmony export */ });
/* harmony import */ var _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../lyte-component.js */ 26591616);
/* harmony import */ var _core_src_service_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../core/src/service.js */ 70549578);
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../core/index.js */ 21046606);
//ignorei18n_start



/*convert to custom class*/
class Directive extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_0__.Service {
    static register(compName, options){
        var _registryClass = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.getRegistryClass(options);
        if(!_registryClass){
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive._pendingDirectives.push({
                compClass : this,
                compName : compName,
                options : options
            });
            return;
            // Lyte.error("registry class for the directive '"+compName+"' not registered with Lyte")
            // return;
        }
        this._refHash = options.refHash;
        compName = compName || _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.String.dasherize(this.name);
        this._registryClass = _registryClass;
        _registryClass.registerDirective(compName, this);
    }
    static actualRegistration(name,directiveClass,registryInstance){
        if(registryInstance.registeredDirectives.indexOf(name) != -1){
            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.warn("Directive name "+ name + " already registered");
            return;
        }
        registryInstance.registeredDirectives.$push(name);
        registryInstance._registeredDirectivesClass[name] = directiveClass;
    }
}
Directive.__lMod = "Directive";
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.bundled = true;
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.appendHooks = function(state,comp,domAppendedFirstTime,initial,dependentPromises) {
    var self = this;
    if(comp._transitionAppend && comp._transitionAppend.length){
        comp._transitionAppend.forEach(function(node){
            node._specialAttributeDetails.forEach(function(attr){
                if(comp.$registry._defaultDirectives.indexOf(attr.hookName) != -1){
                    if(node._defaultDirectives){
                        node._defaultDirectives.push(attr.hookName)
                    }else{
                        node._defaultDirectives = [attr.hookName];
                    }
                    return;
                }
                else if(comp.$registry.registeredDirectives.indexOf(attr.hookName) == -1){
                    _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.warn("Directive names "+attr.hookName+" not found in the registry - " + comp.$registry.constructor.name);
                    return;
                }
                if(!node._directiveIns){
                    let dIns;
                    let lIns = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.getAppOrAddon(comp.$registry);
                    let directiveClass = comp.$registry._registeredDirectivesClass[attr.hookName];
                    lIns.scopedInstance(directiveClass,[],function(ins){
                        dIns = ins
                    },[])
                    if(dIns.onConfigChange){
                        let config = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getTransitionArg(node,attr.hookName);
                        dIns.config = config;
                        let obj = {
                            type : "init",
                            oldValue : null,
                            newValue : config
                        }
                        dIns.onConfigChange.apply(dIns,[obj])
                    }
                    node._directiveIns = dIns;
                }
                var directiveIns = node._directiveIns;
                if(directiveIns && !directiveIns[state]){
                    return;
                }
                var ev = {};
                ev.node = node;
                ev.state  = state;
                // ev.domAppendedFirstTime = domAppendedFirstTime;
                ev.initial = initial;
                ev.dependentPromises = dependentPromises;
                // ev.component = comp.component;
                self.appendHooksCall(state, comp.component, ev, attr);
            }.bind(comp));
        }.bind(comp))
    }
}

_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getTransitionAttr = function(comp,directiveName){
    let found;
    if(comp._specialAttributeDetails && comp._specialAttributeDetails.length){
        comp._specialAttributeDetails.forEach(function(attr){
            if(attr.hookName == directiveName){
                found = attr
            }
        })
        return found;
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.appendHooksCall = function(state,comp,ev,attr){
    var val, node = ev.node;
    var transIns = node._directiveIns;
    if(transIns){
        val = this.getTransitionArg(node,attr.hookName);
        transIns.config = val
        transIns.component = comp;
        transIns[state].call(transIns,ev);
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeHooks = function(state,comp,content,node,ownpromise,chld,sibl,keepAlive,executed){
    var promise;
    var self = this;
    if(content._specialAttributeDetails && content._specialAttributeDetails.length){
    content._specialAttributeDetails.forEach(function(attr){
        if(comp.$registry.registeredDirectives.indexOf(attr.hookName) == -1){
            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.warn("Directive names "+attr.hookName+" not found in the registry" + comp.$registry.constructor.name);
            return;
        }
        if(!content._directiveIns){
            let dIns;
            let lIns = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.getAppOrAddon(comp.$registry);
            let directiveClass = comp.$registry._registeredDirectivesClass[attr.hookName];
            lIns.scopedInstance(directiveClass,[],function(ins){
                dIns = ins
            },[])
            content._directiveIns = dIns;
        }
        var directiveIns = content._directiveIns;
        if(directiveIns && !directiveIns[state]){
            return;
        }
        if(directiveIns){
            if(!directiveIns[state]){
                if(executed){
                    executed.flag = false;
                }
                return;
            }
            maxResolveTime = directiveIns.maxResolveTime;
        }
        var ev = {};
        var maxResolveTime;
        ev.node = content;
        ev.state  = state;
        ev.childPromise = chld;
        // ev.component = this.component;
        // ev.hooks = currentDirective;
        if(sibl && sibl[0]){
            ev.previousSiblingPromise = sibl[0];
        }
        if(state == "onRemove"){
            var res, rej;
            promise = new Promise(function(resolve, reject) { 
                res = resolve, rej=reject;
                ev.done = resolve;
                self.removeHooksCall(state,comp.component,ev,attr,content);
            });
            // node._allPromise.$push(promise);
            content._promise = promise;
            content._promise._state = "pending";
            promise._res = res;
            promise._rej = rej;
            return promise;
        }else if(state == "afterRemove"){
            if(executed){
                executed.flag = true;
                executed.maxResolveTime = maxResolveTime;
            }
            if(ownpromise){
                var p = [ownpromise];
                Promise.all(p).then(function(){
                    if(!keepAlive){
                        content.remove();
                    }
                    if(comp.component){
                        self.removeHooksCall(state,comp.component,ev,attr,content);
                    }
                    content._promise._state = "resolved";
                    if(!keepAlive){
                        content._promise._node = null;
                    }
                });
            }else{
                // if(!keepAlive){
                //     content.remove();
                // }
                self.removeHooksCall(state, comp.component, ev, attr, content);
                // content._promise._state = "resolved";
                // if(!keepAlive){
                //     content._promise._node.remove();
                // }
            }
        }
        else{
            self.removeHooksCall(state,comp.component,ev,attr,content);
        }
    });
    }
    if(promise){
        return promise;
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeHooksCall = function(state,comp,ev,attr,content){
    var val ,transIns = content._directiveIns;
    if(transIns){
        val = this.getTransitionArg(ev.node,attr.hookName);
        transIns.config = val
        transIns.component = comp;
        transIns[state].call(transIns,ev);
    }
}
// _LC.directive.setSpecialNodes = function(comp,helperNode,dynamicN,info,options){
//     if(helperNode && (helperNode._hooksPresent || helperNode._defaultSetSpecialNode)){
//         if(helperNode._specialNodes){
//             if(helperNode.getAttribute("is") == "for"){
//                 helperNode._specialNodes[options.itemIndex][info.in] = dynamicN;
//             }else if(helperNode.getAttribute("is") == "forIn"){
//                 helperNode._specialNodes[options.itemIndex][info.in] = dynamicN;
//             }
//             else if(helperNode && /if|switch/g.test(helperNode.getAttribute("is"))){
//                 helperNode._specialNodes[info.in] = dynamicN;  
//             }
//             else if(helperNode && helperNode.tagName == "LYTE-YIELD"){
//                 helperNode._specialNodes[info.in] = dynamicN;
//             }
//         }
//     }else if(dynamicN && dynamicN._hooksPresent){
//         comp._specialNodes[info.in] = dynamicN;
//         comp._hooksPresent = true;
//         comp.hc = true;
//     }else if(dynamicN && dynamicN._defaultSetSpecialNode){
//         comp._specialNodes[info.in] = dynamicN;
//         comp._defaultSetSpecialNode = true;
//     }
//     if(info.chld){
//         dynamicN._chld = info.chld;
//     }
//     if(info.dc){
//         dynamicN.dc = info.dc;
//         dynamicN.hc = info.hc;
//     }
//     if(info.sibl){
//         dynamicN._sibl = info.sibl;
//     }
// }
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeFromDom = function(content) {
    if(Array.isArray(content)){
        for(let s=0;s<content.length; s++ ) {
            if(Array.isArray(content[s])){
                for(let i=0;i<content[s].length; i++ ) {
                    content[s][i].remove();
                }
            }else{
                content[s].remove();
            }
        }
    }else if(content instanceof Object){
        for (var ind in content) {
            for (let i = 0; i < content[ind].length; i++) {
                content[ind][i].remove();
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeFromDomNormal = function(node,content){
    if(node._forContent) {
        if(node.getAttribute("is") === "for") {
            if(node._rmLaterHelper) {
                for(let i=0;i<node._rmLaterHelper.length;i++) {
                    for(let j=0;j<node._rmLaterHelper[i].length;j++) {
                            this.removeFromDomNormal(node._rmLaterHelper[i][j]);
                    }
                    node._rmLaterHelper[i] = [];
                }
            }
            // for(let s=0;s<node._forContent.length;s++) {
            //     for(let i=0;i<node._forContent[s].length; i++ ) {
            //         node._forContent[s][i].remove();
            //     }
            // }
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.removeForContent(node);
        }else{
            if(node._rmLaterHelper) {
                let keys = Object.keys(node._rmLaterHelper);
                for(let i=0;i<keys.length;i++) {
                    for(let j=0;j<node._rmLaterHelper[keys[i]].length;j++) {
                        this.removeFromDomNormal(node._rmLaterHelper[keys[i]][j]);
                    }
                    node._rmLaterHelper[keys[i]] = [];
                }
            }
            for(var ind in node._forContent) {
                for(let i=0;i<node._forContent[ind].length; i++ ) {
                    node._forContent[ind][i].remove();
                }
            }
        }
    }
    else if(node._caseContent || node._yieldContent){
        if(node._rmLaterHelper) {
            for(let j=0;j<node._rmLaterHelper.length;j++) {
                this.removeFromDomNormal(node._rmLaterHelper[j]);
            }
            node._rmLaterHelper = [];
        }
        for(let i=0;i<node._caseContent.length; i++ ) {
            node._caseContent[i].remove();
        }
        // node._caseContent = []; // not removed from pendignvewport elements
    } else if(node._renderedComponent) {

        for(let key in node._renderedComponent) {
            if(node._renderedComponent[key]) {
                node._renderedComponent[key].remove();
                node._renderedComponent[key] = null;
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeLaterHelper = function(node,content,normalRemove){
    if(node._forContent) {
        if(node.getAttribute("is") === "for") {
            if(node._rmLaterHelper) {
                for(let i=0;i<node._rmLaterHelper.length;i++) {
                    for(let j=0;j<node._rmLaterHelper[i].length;j++) {
                            this.removeLaterHelper(node._rmLaterHelper[i][j]);
                    }
                    node._rmLaterHelper[i] = [];
                }
            }
        }else{
            if(node._rmLaterHelper) {
                let keys = Object.keys(node._rmLaterHelper);
                for(let i=0;i<keys.length;i++) {
                    for(let j=0;j<node._rmLaterHelper[keys[i]].length;j++) {
                        this.removeLaterHelper(node._rmLaterHelper[keys[i]][j]);
                    }
                    node._rmLaterHelper[keys[i]] = [];
                }
            }
        }
    }
    else if(node._caseContent || node._yieldContent){
        if(node._rmLaterHelper) {
            for(let j=0;j<node._rmLaterHelper.length;j++) {
                this.removeLaterHelper(node._rmLaterHelper[j]);
            }
            node._rmLaterHelper = [];
        }
    }
    //  else if(node._renderedComponent) {
        // for(let key in node._renderedComponent) {
        //     if(node._renderedComponent[key]) {
        //         node._renderedComponent[key].remove();
        //         node._renderedComponent[key] = null;
        //     }
        // }
    // }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.rmOtherNodes = function(node,caseContentArr,totalProms,keepAlive){
    if(!keepAlive){
        if(totalProms && totalProms.length>0){
            //remove all _rmLaterHelper
            this.removeLaterHelper(node);
            Promise.all(totalProms).then(function(){
                this.removeFromDom(caseContentArr)
                totalProms = null;
            }.bind(this));
        }else{
            this.removeFromDomNormal(node,caseContentArr);
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getDirectChildPos = function(node){
    var directChildArr = [];
    if(node && (node.getAttribute("is") == "if")){
        let pCase = node._previousCase;
        if(pCase && node.dc && node.dc[pCase]){
            directChildArr = node.dc[pCase].dc;
        }
    }
    else if(node.getAttribute("is") == "switch"){
        let pCase = node._previousCase;
        let dr = node.dc;
        if(pCase && dr && dr[pCase] && dr[pCase].dc){
            dr[pCase].dc.forEach(function(item,index){
                directChildArr.$push(item);
            });
            var dummyScope = node._cases[pCase];
            while(dummyScope){
                if(dummyScope.additional) {
                    if(dummyScope.additional.next) {
                        let nxtName = dummyScope.additional.next
                        dummyScope = node._cases[dummyScope.additional.next];
                        let chldArr = dr[nxtName].dc
                        if(chldArr){
                            for(let i=0;i<chldArr.length;i++){
                                directChildArr.$push(chldArr[i]);
                            }
                            // dr[nxtName].dc.forEach(function(item,index){
                            //     directChildArr.$push(item);
                            // })
                        }
                    } else {
                        dummyScope = node._default;
                        let chldArr = dr["default"].dc; 
                        if(chldArr){
                            for(let i=0;i<chldArr.length;i++){
                                directChildArr.$push(chldArr[i]);
                            }
                            // dr["default"].dc.forEach(function(item,index){
                            //     directChildArr.$push(item);
                            // })  
                        }
                    }
                } else {
                    break;
                }
            }
        }
    }
    else if(node.dc && node.dc.length){
        directChildArr = node.dc;
    }
    return directChildArr;
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.transitionRemoveHelpers = function(thisRef, node, totalProms ,content ,specialNodes,obj,nodeStack,keepAlive){
    var childProms = node._childPromise;
    var directChildArr = this.getDirectChildPos(node);
    if(content){
        if((node._hooksPresent||node.hc) && specialNodes){
            this.removeHookNodes(thisRef,specialNodes,node,totalProms,childProms,directChildArr,obj,nodeStack,keepAlive);
            if(childProms && childProms.length){
                this.rmOtherNodes(node,content,childProms,keepAlive);
            }
        }
    }
}
//need comp
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeHookNodes = function(
    thisRef,
    hookNodes,
    node,
    totalProms,
    childProms,
    directChildArr,
    obj,
    nodeStack,
    keepAlive
) {
    var parentPrev;
    if(obj && obj.previousPromise){
        parentPrev = obj.previousPromise;
    }
    if(hookNodes && hookNodes.length > 0){
        for(var j=0; j<hookNodes.length; j++){
            var chld = [];
            var sibl = [];
            var ele = hookNodes[j];
            if(ele){
                if(ele.tagName =="TEMPLATE"){
                    var is = ele.getAttribute("is");
                    if(is == "if" || is == "switch"){
                        this.checkOngoingPromises(ele);
                        this.transitionRemoveHelpers(thisRef,ele,totalProms,ele._caseContent,ele._specialNodes,obj,nodeStack,keepAlive)
                        this.setParentChildPromise(ele,childProms,directChildArr,j,nodeStack,node);
                        continue;
                    }else if(is == "for"){
                        let hNode = ele._specialNodes;
                        this.checkOngoingPromises(ele);
                        if(ele.hc && hNode){
                            for(let k=hNode.length-1; k>=0; k--){
                                this.transitionRemoveHelpers(thisRef,ele,totalProms,ele._forContent,hNode[k],obj,nodeStack,keepAlive)
                            }
                        }
                        this.setParentChildPromise(ele,childProms,directChildArr,j,nodeStack,node);
                        continue;
                    }else if(is == "forIn"){
                        let hNode = ele._specialNodes;
                        this.checkOngoingPromises(ele);
                        var keysArr = Object.keys(ele._specialNodes);
                        if(ele.hc && hNode){
                            for(let k=keysArr.length-1; k>=0; k--){
                                this.transitionRemoveHelpers(thisRef,ele,totalProms,ele._forContent,hNode[keysArr[k]],obj,nodeStack,keepAlive)
                            }
                        }
                        this.setParentChildPromise(ele,childProms,directChildArr,j,nodeStack,node);
                        continue; 
                    }else if(is == "component"){
                        this.checkOngoingPromises(ele);
                        for(let key in ele._renderedComponent) {
                            if(ele._renderedComponent[key]) {
                                var comp = ele._renderedComponent[key];
                                if(comp._hooksPresent && key == ele._currentComponent){
                                    comp._childPromise =[];
                                    this.removeHookNodes(thisRef,[comp],ele,totalProms,ele._childPromise,[0],obj,nodeStack,keepAlive);
                                    if(childProms && comp._childPromise && ele._childPromise){
                                        for(let i=0; i<comp._childPromise.length; i++){
                                            ele._childPromise.$push(comp._childPromise[i]);
                                        }
                                        // comp._childPromise.forEach(function(proms){
                                        //     ele._childPromise.$push(proms);
                                        // })
                                    }
                                }else{
                                    ele._renderedComponent[key].remove();
                                    ele._renderedComponent[key] = null;
                                }
                            }
                        }
                        this.setParentChildPromise(ele,childProms,directChildArr,j,nodeStack,node);
                        continue;
                    }else if(is == "yield"){
                        continue; 
                    }
                }
                if(ele.component){
                    if(ele.hc){
                        this.checkOngoingPromises(ele);
                        this.transitionRemoveHelpers(thisRef,ele,totalProms,[ele],ele._specialNodes,obj,nodeStack,keepAlive)
                        // if(ele.tagName == "LYTE-YIELD"){
                        //     ele._fakeRemove = true;
                        //      ele.constructor.prototype.disconnectedCallback.apply(ele)
                        //     ele._deleted = true;
                        // }
                    }
                    if(!ele._specialAttributeDetails){
                        this.setParentChildPromise(ele,childProms,directChildArr,j,nodeStack,node);
                        continue;
                    }
                    this.getDirectTransChild(ele,chld);
                }
                //dont c _chld for comp 
                else{
                    this.getChildTransition(hookNodes,j,chld);
                }
                this.getSiblTransition(hookNodes,j,sibl,node,parentPrev);
                if(ele._specialAttributeDetails){
                    var ownpromise;
                    this.removeHooks('beforeRemove',thisRef,ele,node,undefined,chld,sibl);
                    ownpromise = this.removeHooks('onRemove',thisRef,ele,node,undefined,chld,sibl);
                    if(ownpromise){
                        var executed = {"flag" : false};
                        ownpromise._node = ele;
                        ele._ownPromise = ownpromise;
                        this.removeHooks('afterRemove',thisRef,ele,node,ownpromise,chld,sibl,keepAlive,executed);
                        totalProms && totalProms.$push(ownpromise);
                        if(childProms && directChildArr && directChildArr.length && directChildArr.indexOf(j) != -1){
                            childProms.$push(ownpromise);
                        }
                        if(obj){
                            obj.previousPromise = ownpromise;
                        }
                        var maxResolveTime = 5000;
                        if(executed && executed.maxResolveTime != undefined && typeof executed.maxResolveTime == "number"){
                            maxResolveTime = executed.maxResolveTime;
                        }
                        if(executed && !executed.flag){
                            this.setState(ownpromise,ele);
                        }
                        this.transitionDefaultResolve(ownpromise,maxResolveTime);
                    }else{
                        if(!keepAlive){
                            ele.remove();
                        }
                        this.removeHooks('afterRemove',thisRef,ele,node,undefined,chld,sibl,keepAlive);
                    }
                }
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.setParentChildPromise = function(ele,childProms,directChildArr,j,nodeStack,node){
    if(childProms && ele._childPromise && ele._childPromise.length && directChildArr && directChildArr.length && directChildArr.indexOf(j) != -1){
        ele._childPromise.forEach(function(proms){
            childProms.$push(proms);
        })
    }
    nodeStack.$push(node);
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.checkOngoingPromises = function(ele){
    if(ele._totalPromise && ele._totalPromise.length){
        this.resolveOngoingPromise(ele._totalPromise);
        ele._totalPromise = [];
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.resolveOngoingPromise = function(totalProms){
    totalProms.forEach(function(promise){
        if(promise && promise._state == "pending"){
            promise._res();
            promise._state = "resolved";
            let nodeStr ="";
            if(promise._node){
                nodeStr = _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.errorNodeDetails(promise._node);
            }
            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.warn("Ongoing previous transition was resolved by lyte "+nodeStr);
        }
    })
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.setState = function(ownpromise,ele){
    ownpromise.then(function(){
        ele.remove();
        ownpromise._state = "resolved";
    })
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.transitionDefaultResolve = function(promise,maxResolveTime){
    var timeOutId = setTimeout(function(){
        promise._res();
        promise._state = "resolved";
        clearTimeout(timeOutId);
    },maxResolveTime);
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getSiblTransition = function(hookNodes,j,sibl,pnode,parentPrev){
    if(hookNodes[j]._sibl && hookNodes[j]._sibl.length){
        hookNodes[j]._sibl.forEach(function(indNew){
            let node = hookNodes[indNew];
            if(node){
                if(node.tagName == "TEMPLATE" && node.hc && node._childPromise){
                    if(node._childPromise && !node._childPromise.length){
                        this.getNxtSiblTrans(node,pnode,sibl,hookNodes);
                    }else if(sibl && node._childPromise && node._childPromise.length){
                        let ln = node._childPromise.length;
                        sibl.$push(node._childPromise[ln-1]);
                    }
                }else if(sibl && node._hooksPresent && node._ownPromise){
                    sibl.$push(node._ownPromise);
                }
                else if(sibl && node.component && node._childPromise){
                    if(node._childPromise && !node._childPromise.length){
                        this.getNxtSiblTrans(node,pnode,sibl,hookNodes);
                    }else{
                        let ln = node._childPromise.length;
                        sibl.$push(node._childPromise[ln-1]);
                    }
                }else if(sibl && parentPrev){
                    sibl.$push(parentPrev);
                }
            }
        }.bind(this))
        if(sibl && !sibl.length && parentPrev){
            sibl.$push(parentPrev);
        }
    }  else if(parentPrev){
        sibl.$push(parentPrev);
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getNxtSiblTrans = function(node,template,sibl,hookNodes){
    while(node && node._sibl && !sibl.length){
        let ind = node._sibl[0];
        node = hookNodes[ind];
        if(node && node.tagName == "TEMPLATE" && node.hc){
            if(node._childPromise && !node._childPromise.length){
                this.getNxtSiblTrans(node,template,sibl,hookNodes);
            }else if(node._childPromise){
                let ln = node._childPromise.length;
                sibl.$push(node._childPromise[ln-1]);
            }
        }
        else if(node && node.component){
            if(node._specialAttributeDetails){
                sibl.$push(node);
            }else if(node.hc && node._childPromise){
                let ln = node._childPromise.length;
                sibl.$push(node._childPromise[ln-1]);
            }
            else{
                this.getNxtSiblTrans(node,template,sibl,hookNodes);
            }
        }
        else if(node && node._hooksPresent && node._ownPromise){
            sibl.$push(node._ownPromise);
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getChildTransition = function(hookNodes,j,chld){
    if(hookNodes[j]._chld){
        hookNodes[j]._chld.forEach(function(indNew){
            let node = hookNodes[indNew];
            if(node && chld&& node.tagName == "TEMPLATE" && node.hc && node.getAttribute("is") != "yield" && node.getAttribute("is") != "registerYield"){
                this.getDirectTransChild(node,chld);
            }else if(node && chld && node.component){
                if(node._specialAttributeDetails && node._ownPromise){
                    chld.$push(node._ownPromise);
                }else if(node.hc){
                    this.getDirectTransChild(node,chld);
                }
            }
            else if(node && chld && node._ownPromise){
                chld.$push(node._ownPromise);
            }
        }.bind(this))
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getDirectTransChild = function(template,chld){
    let promiseArr = template._childPromise;
    if(promiseArr){
        let ln = promiseArr.length
        for(let i=ln-1; i>=0; i--){
            chld.$push(promiseArr[i]);
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.pushChildPromise = function(ele,childProms){
    ele._childPromise.forEach(function(proms){
        childProms.$push(proms);
    })
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.setTransitionNodes = function(specialNodes,comp,dependentPromises,direct,stack){
    if(specialNodes && specialNodes.length){
        var ln = specialNodes.length;
        // var stack = [];
        for(var i=ln-1; i>=0; i--){
            var spNode = specialNodes[i];
            if(spNode && spNode.tagName == "TEMPLATE"){
                var is = spNode.getAttribute("is");
                switch(is){
                    case "for": {
                        if(spNode._specialNodes){
                            for(let j=0; j<spNode._specialNodes.length; j++){
                                this.setTransitionNodes(spNode._specialNodes[j],comp,dependentPromises,false,stack);
                            }
                        }
                    } break;
                    case "forIn": {
                        if(spNode._specialNodes){
                            var keys = Object.keys(spNode._specialNodes);
                            for(let j=0; j<keys.length; j++){
                                this.setTransitionNodes(spNode._specialNodes[keys[j]],comp,dependentPromises,false,stack);
                            }
                        }
                    }break;
                    case "switch":
                    case "if": {
                        if(spNode._specialNodes){
                            this.setTransitionNodes(spNode._specialNodes,comp,dependentPromises,false,stack);
                        }
                    }
                    break;
                    case "component": {
                        if(comp._transitionAppend && spNode._specialAttributeDetails){
                            comp._transitionAppend.$push(spNode);
                        }
                        var name = spNode._currentComponent;
                        if(name && spNode._renderedComponent[name]){
                            stack.$push(spNode._renderedComponent[name]);
                        }
                    }
                    break;
                    default: {
                        if(spNode._specialNodes){
                            this.setTransitionNodes(spNode._specialNodes,comp,dependentPromises,false,stack);
                        }
                    }
                }
            }else{
                if(spNode && spNode.component){
                    if(spNode._specialAttributeDetails && comp._transitionAppend){
                        comp._transitionAppend.$push(spNode);
                    }
                    // comp._transitionAppend.$push(spNode._specialNodes,comp);
                    if(spNode.tagName == "LYTE-YIELD"){
                        this.setTransitionNodes(spNode._specialNodes,comp,dependentPromises,false,stack);
                    }else if(stack){
                        stack.$push(spNode);
                    }
                }else if(comp._transitionAppend){
                    comp._transitionAppend.$push(spNode);
                }
                
            }
        }
        if(direct){
            this.appendHooks('beforeAppend',comp,undefined,undefined,dependentPromises)
            if(stack && stack.length){
                let self = this;
                stack.forEach(function(childComp){
                    var newStack = [];
                    self.setTransitionNodes(childComp._specialNodes,childComp,dependentPromises,true,newStack);
                })
                stack = null;
            }
            this.appendHooks('onAppend',comp,undefined,undefined,dependentPromises)
            this.appendHooks('afterAppend',comp,undefined,undefined,dependentPromises)
            comp._transitionAppend = [];
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.checkFakeForAndRemove = function(fakeRemove,node,i){
    if(fakeRemove){
        if(!node._rmLaterHelper){
            node._rmLaterHelper = [];
        }
        node._rmLaterHelper[i] = node._helpers[i];
        node._helpers[i] = [];
    }else{
        node._helpers[i] = [];
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.checkFakeIfAndRemove = function(fakeRemove,node){
    if(fakeRemove){
        node._rmLaterHelper = node._helpers;
        node._helpers = [];
    }else{
        node._helpers = [];
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeForContent = function(comp,direct,fakeRemove,node,totalProms){
    if(direct && node.hc){
        for(let i=node._forContent.length-1; i>=0; i--){
            var forContent = Array.from(node._forContent[i]);
            var specialNodes = node._specialNodes[i];
            var obj = {};
            var nodeStack = [];
            obj.previousPromise = previousPromise;
            this.transitionRemoveHelpers(comp,node,totalProms,forContent,specialNodes,obj,nodeStack);
            if(!totalProms || !totalProms.length){
                this.rmOtherNodes(node,forContent);
            }
            for(var j=0; j<nodeStack.length; j++){
                if(nodeStack[j] && nodeStack[j]._specialNodes && nodeStack[j]._specialNodes.length){
                    nodeStack[j]._specialNodes = [];
                }
            }
            if(node._specialNodes){
                for(let i=0; i<node._specialNodes.length; i++){
                    node._specialNodes[i] = []
                }
            }
            
        }
    }
    else{
        var forContent = node._forContent;
        if(forContent){
            if(!fakeRemove){
                this.rmOtherNodes(node,node._forContent,totalProms);
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeForInContent = function(comp,direct,fakeRemove,node,totalProms){
    if(direct && node.hc){
        var keys = Object.keys(node._specialNodes);
        var ln = keys.length;
        for(let i=ln-1; i>=0; i--){
            let key = keys[i];
            var forContent = Array.from(node._forContent[key]);
            var specialNodes = node._specialNodes[key];
            var obj = {};
            var nodeStack = [];
            // var yieldChild = node._yieldChild;
            obj.previousPromise = previousPromise;
            this.transitionRemoveHelpers(comp,node,totalProms,forContent,specialNodes,obj,nodeStack);
            if(!totalProms || !totalProms.length){
                this.rmOtherNodes(node,forContent);
            }
            for(var j=0; j<nodeStack.length; j++){
                if(nodeStack[j] && nodeStack[j]._specialNodes && nodeStack[j]._specialNodes.length){
                    nodeStack[j]._specialNodes = [];
                }
            }
            if(node._specialNodes) {
                let keys = Object.keys(node._specialNodes);
                for(let i=0;i<keys.length;i++) {
                    for(let j=0;j<node._specialNodes[keys[i]].length;j++) {
                        this.removeHelpers(node._specialNodes[keys[i]][j],undefined,undefined, totalProms, fakeRemove, previousPromise);
                    }
                    node._specialNodes[keys[i]] = [];
                }
            }
        }
    }
    else{
        var caseContent = node._forContent;
        if(caseContent){
            if(!fakeRemove){
                this.rmOtherNodes(node,node._forContent,totalProms);
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeIfContent = function(comp,direct,fakeRemove,node,totalProms,previousPromise){
    if(direct && node.hc){
        var caseContent = Array.from(node._caseContent);
        var specialNodes = node._specialNodes;
        // var yieldChild = node._yieldChild;
        var obj = {};
        var nodeStack = [];
        obj.previousPromise = previousPromise;
        this.transitionRemoveHelpers(comp,node,totalProms,caseContent,specialNodes,obj,nodeStack);
        if(!totalProms || !totalProms.length){
            this.rmOtherNodes(node,caseContent);
        }
        _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.removeDynamicNodes(node,comp);
        if(!direct){
            for(let tempName in node._tempList) {
                _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.removeDynamicNodes(node._tempList[tempName],comp);
            }
        }
        for(var j=0; j<nodeStack.length; j++){
            if(nodeStack[j] && nodeStack[j]._specialNodes && nodeStack[j]._specialNodes.length){
                nodeStack[j]._specialNodes = [];
            }
        }
        // nodeStack.forEach(function(item){
        //     if(item._specialNodes && item._specialNodes.length){
        //         item._specialNodes = [];
        //     }
        // })
        if(node._specialNodes){
            node._specialNodes = []
        }
    }
    else{
        var caseContent = node._caseContent;
        if(caseContent){
            if(!fakeRemove){
                this.rmOtherNodes(node,caseContent,totalProms);
            }
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.removeDynamicNodes(node,comp);
            if(!direct && node._tempList){
                for(let tempName in node._tempList) {
                    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.removeDynamicNodes(node._tempList[tempName],comp);
                }
            }
        } else if(node._yieldContent){
            for(let i=0;i<node._yieldContent.length; i++ ) {
                node._yieldContent[i].remove();
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeForIndexContent = function(comp,node,totalProms,previousPromise,index){
    var forContent = Array.from(node._forContent[index]);
    var specialNodes = node._specialNodes[index];
    var nodeStack = [];
    var obj = {};
    obj.previousPromise = previousPromise;
    // var yieldChild = node._yieldChild && node._yieldChild[index];
    var stackPromise = [];
    this.transitionRemoveHelpers(comp,node,stackPromise,forContent,specialNodes,obj,nodeStack);
    node._ongoingpromise[index] = stackPromise;
    if(stackPromise && totalProms && stackPromise.length){
        node._ongoingpromise[index] = stackPromise;
        stackPromise.forEach(function(proms){
            totalProms.$push(proms);
        });
    }
    if(!totalProms || !totalProms.length){
        // this.rmOtherNodes(node,forContent);
        if(node._helpers[index]) {
            for(let j=0;j<node._helpers[index].length;j++) {
                this.removeFromDomNormal(node._helpers[index][j]);
            }
        }
        if(node._forContent[index]) {
            for(let i=0;i<node._forContent[index].length; i++ ) {
                node._forContent[index][i].remove();
            }
        }
    }
    if(nodeStack){
        nodeStack.forEach(function(item){
            if(item._specialNodes && item._specialNodes.length && item!=node){
                item._specialNodes = [];
            }
        })
    }
    if(node._specialNodes && node._specialNodes[index]){
        node._specialNodes[index] = []
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.updateRenderedComp = function(comp,activeComponent,node,keepAlive){
    activeComponent._totalPromise = [];
    var obj = {};
    var nodeStack = [];
    var stackPromise = [];
    this.removeHookNodes(comp,[activeComponent],node,stackPromise,node._childPromise,activeComponent.dc,obj,nodeStack,keepAlive);
    node._totalPromise = stackPromise;
    if(stackPromise && activeComponent._totalPromise && stackPromise.length){
        stackPromise.forEach(function(proms){
            activeComponent._totalPromise.$push(proms);
        });
    }
    if(keepAlive){
        if(node._childPromise && node._childPromise.length){
            this.appendInKeepAliveDom(node,node._childPromise,activeComponent);
        }else{
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.hDiv.content.appendChild(activeComponent) 
        }
        
    }else{
        if(nodeStack){
        nodeStack.forEach(function(item){
            if(item._specialNodes && item._specialNodes.length && item!=node){
                item._specialNodes = [];
            }
        })
        }
        if(node._childPromise && !node._childPromise.length){
            activeComponent.remove();    
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.updateDynamicComp = function(comp,update,component,activeComponent,node,newComponent,placeHolder,placeHolderParent){
    let returnVal;
    if(!update) {
        if(comp._dependentPromise){
            component._dependentPromise = comp._dependentPromise;
        }
        returnVal = {"toAppendMain" : component, "lastNode" : node};
    } else {
        if(node._specialAttributeDetails && node._specialAttributeDetails.length > 0){
            component._specialAttributeDetails = node._specialAttributeDetails;
            comp._transitionAppend.$push(component);
        }
        let dependentPromises = [];
        if(activeComponent && activeComponent._totalPromise && activeComponent._totalPromise.length){// && this._transitionAppend.length
            for(let i=activeComponent._totalPromise.length-1;i>=0;i--){
                dependentPromises.$push(activeComponent._totalPromise[i])
            }
        }
        this.appendHooks('beforeAppend',comp,undefined,undefined,dependentPromises)
        if(!newComponent && component.hc){
            var stack = [];
            this.setTransitionNodes(component._specialNodes,component,dependentPromises,true,stack);
        }
        _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.ignoreDisconnect = true;
        _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.insertBeforeNative(placeHolderParent,component, placeHolder);
        _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.ignoreDisconnect = false;
        this.appendHooks('onAppend',comp,undefined,undefined,dependentPromises)
        this.appendHooks('afterAppend',comp,undefined,undefined,dependentPromises)
        comp._transitionAppend = [];
    }
    if(!update && node._specialAttributeDetails && node._specialAttributeDetails.length > 0 && comp._transitionAppend){
        component._specialAttributeDetails = node._specialAttributeDetails;
        comp._transitionAppend.$push(component);
    }
    return returnVal;
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.onGoingForPromise = function(node,ind){
    if(node._ongoingpromise[ind]){
        this.resolveOngoingPromise(node._ongoingpromise[ind]);
        node._ongoingpromise[ind] = null;
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.removeRenderedComponent = function(comp,node,previousPromise){
    var obj = {};
    var nodeStack = [];
    obj.previousPromise = previousPromise;
    directiveObj.transitionRemoveHelpers(comp,renderedComponent,totalProms,[renderedComponent],renderedComponent._specialNodes,obj,nodeStack);
    if(!totalProms || !totalProms.length){
        node._renderedComponent[key].remove();
        node._renderedComponent[key] = null;
    }
    if(nodeStack){
    nodeStack.forEach(function(item){
        if(item._specialNodes && item._specialNodes.length){
            item._specialNodes = [];
        }
    })
    }
    if(node._specialNodes){
        node._specialNodes = []
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.insertInDom = function(comp,placeHolder,lastNode,toAppendMain,domAppendedFirstTime,initial,dependentPromises){
    // var dependentPromises = [];
    // this.getAllDependentPromises(comp,dependentPromises,node._totalPromise);
    this.appendHooks('beforeAppend', comp, domAppendedFirstTime, initial, dependentPromises)
    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.insertInDom(placeHolder,lastNode,toAppendMain);
    this.appendHooks('onAppend',comp,domAppendedFirstTime,initial,dependentPromises)
    this.appendHooks('afterAppend',comp,domAppendedFirstTime,initial,dependentPromises)
    comp._transitionAppend = [];
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.appendInDom = function(comp,content,domAppendedFirstTime,initial,dependentPromises){
    this.appendHooks('beforeAppend',comp,domAppendedFirstTime,initial,dependentPromises)
    _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.appendInDom(comp,content);
    this.appendHooks('onAppend',comp,domAppendedFirstTime,initial,dependentPromises)
    this.appendHooks('afterAppend',comp,domAppendedFirstTime,initial,dependentPromises)
    comp._transitionAppend = [];
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getDependentPromises = function(comp,dependentPromises,sourcePromises){
    if(sourcePromises && sourcePromises.length){
        for(let i=sourcePromises.length-1;i>=0;i--){
            dependentPromises.$push(sourcePromises[i])
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getAllDependentPromises = function(comp,dependentPromises,sourcePromises){
    if(sourcePromises && sourcePromises.length && comp._transitionAppend && comp._transitionAppend.length){
        for(let i=sourcePromises.length-1;i>=0;i--){
            dependentPromises.$push(sourcePromises[i])
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.instanciatePromises = function(comp){
    comp._childPromise = [];
    comp._transitionAppend = [];
    comp._totalPromise = [];
    comp._transitionHelperNodes = [];
    comp._specialNodes = [];
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.instanciateForPromises = function(node){
    node._totalPromise = []
    node._childPromise = []
    if(!node._ongoingpromise){
        node._ongoingpromise = []
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.destroyPromises = function(comp){
    comp._specialNodes = null;
    comp._childPromise = null;
    comp._totalPromise = null;
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.appendInKeepAliveDom = function(node,childProms,activeComponent){
    Promise.all(childProms).then(function(){
        if(node._dontAppend){
            activeComponent.remove();
        }else{
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.ignoreDisconnect = true;
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.hDiv.content.appendChild(activeComponent) 
            _lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.ignoreDisconnect = false;
        }
        childProms = null;
    }.bind(this));
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.destroyHelperPromises = function(arr){
    for(let i=0; i<arr.length; i++){
        let helperArr = arr[i];
        for (let key in helperArr) {
            let node = helperArr[key];
            if(node._totalPromise && node._totalPromise.length){
                let isKeepAlive = node.hasAttribute("lyte-keep-alive");
                if(isKeepAlive){
                    node._dontAppend = true;
                }
                this.resolveOngoingPromise(node._totalPromise);
                node._totalPromise = null;
            }
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.updateSpecialNodeRef = function(comp,node,toAppend,helperNode){
    if(node._specialNodes && node._specialNodes.length){
        node.dc = toAppend.dc;
        node.hc = true
        if(helperNode){
            helperNode.hc = true
        }else{
            comp.hc = true
        }
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.spliceSpecialNodes = function(node,firstIndex,secondIndex){
    node._specialNodes.$splice(firstIndex, secondIndex);
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.infoCD = function(comp,info,dynamicN,helperNode,options){
    this.infoI(comp,info,dynamicN,helperNode,options);
    if(comp._dependentPromise){
        dynamicN._dependentPromise = comp._dependentPromise;
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.infoI = function(comp,info,dynamicN,helperNode,options){
    if(!info._igTs){
        dynamicN._defaultSetSpecialNode = true;
        this.setSpecialNodes(comp,helperNode, dynamicN, info, options); 
    }
}
// _LC.directive.infoA = function(comp,info,dynamicN,helperNode,attr,yieldComp,options){
//     this.setSpecialNodes(comp,helperNode,dynamicN,info,options);
//     if(dynamicN._specialAttributeDetails && dynamicN._specialAttributeDetails.length){
//         dynamicN._specialAttributeDetails.push(attr);
//     }else{
//         dynamicN._specialAttributeDetails = [attr];
//     }
//     // if(attr.stringValue){
//         // dynamicN.removeAttribute(attr.name);
//         // dynamicN.setAttribute(attr.hookName,attr.stringValue);
//     // }
//     if(yieldComp){
//         yieldComp._transitionAppend.$push(dynamicN);
//     }
//     else if(comp._transitionAppend.indexOf(dynamicN)==-1 && dynamicN && dynamicN.getAttribute("is") != "component"){
//         comp._transitionAppend.$push(dynamicN);
//     }
// }
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.infoF = function(comp, info, dynamicN, helperNode, type, options) {
    if(!info._igTs){
        if(type == "cM"){
            dynamicN._defaultSetSpecialNode = true;
        }
        this.setSpecialNodes(comp,helperNode, dynamicN, info, options); 
    }
}
_lyte_component_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.infoE = function(comp,helperNode,dynamicN,info,options){
    this.setSpecialNodes(comp,helperNode,dynamicN,info,options);
}

//ignorei18n_end

/***/ }),

/***/ 98862259:
/*!*******************************************************************!*\
  !*** ./node_modules/@slyte/component/src/directives/lyte-hide.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Hide": () => (/* binding */ Hide)
/* harmony export */ });
/* harmony import */ var _core_src_service_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../core/src/service.js */ 70549578);
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../index.js */ 26591616);
/* harmony import */ var _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../core/src/lyte-utils.js */ 70416156);
//ignorei18n_start




class Hide extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_0__.Service {
    static register(){
        _index_js__WEBPACK_IMPORTED_MODULE_1__.ComponentRegistry.addFakeDirective(this,"hide");
    }
    constructor(){
        super()
        this.yield = {
            self : this,
            connect : function(comp){
                if(comp._callee && _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getTransitionArg(comp._callee, "hide-tag")){
                    this.self.template(comp, _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.hiddenYieldsDiv);
                }
            },
            disConnect : function(comp){
                if(Array.from(_index_js__WEBPACK_IMPORTED_MODULE_1__._LC.hiddenYieldsDiv.content.childNodes).indexOf(comp)!=-1){
                    _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.hiddenYieldsDiv.content.removeChild(comp)
                }
                if(comp._hiddenTemplate && comp._hiddenTemplate.length){
                    this.self.callDisconnectedCallback(comp);
                }
            }
        };
    }
    appendTemp(){
        document.body.appendChild(_index_js__WEBPACK_IMPORTED_MODULE_1__._LC.hiddenComponentsDiv);
        document.body.appendChild(_index_js__WEBPACK_IMPORTED_MODULE_1__._LC.hiddenYieldsDiv);
    }
    static triggerGlobalEvent(target, eventName, ev, hasHandled, ignore){
        if(target && target._hiddenBoundEvents && target._hiddenBoundEvents[eventName] && !ev.cancelBubble && (ignore || ev.currentTarget == document.body)){
            let actObj = target._hiddenBoundEvents[eventName];
            let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.deepCopyObject)(actObj);
            _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.skipArgProcessing(cloneActObj, eventName, target);
            _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.throwAction.call(target.parentElement._callee, target.parentElement._callee, eventName, cloneActObj, undefined, undefined, target, ev, hasHandled);
        }
    }
    connect(comp, content){
        let tempParent = comp._callee;
        if(tempParent){
            this.template(comp, _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.hiddenComponentsDiv ,content)
        }else{
            console.error(' Error:Parent node of current template is not of type TEMPLATE . Cannot hide this template');
        }
    }
    rmHelpersSpecificIndex(node, index){
        if(typeof index=='number' && node._hiddenTemplate[index] && node._hiddenTemplate.length){
            for(let i in node._hiddenTemplate[index]){
                node._hiddenTemplate[index][i].disconnectedCallback();
            }
            if(node.getAttribute("is") == "for"){
                node._hiddenTemplate.splice(index, 1); 
            }else{
                delete node._hiddenTemplate[index];
            }
        }
    }
    forRmHelpers(node){
        if(node._hiddenTemplate && node._hiddenTemplate.length) {
            for(let i=0;i<node._hiddenTemplate.length;i++) {
                for(let j=0;j<node._hiddenTemplate[i].length;j++) {
                    node._hiddenTemplate[i][j].disconnectedCallback();
                }
                node._hiddenTemplate[i] = [];
            }
            node._hiddenTemplate = [];
        }
    }
    ifRmHelpers(node){
        if(node._hiddenTemplate && node._hiddenTemplate.length){
            this.callDisconnectedCallback(node);
        }
    }
    setTxtNode(comp, yld){
        if(_index_js__WEBPACK_IMPORTED_MODULE_1__._LC.directive.getTransitionArg(comp, "hide-tag")){
            var emptyTextNode = document.createTextNode("");
            _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.replaceWithPf(yld, emptyTextNode);
        } 
    }
    static shouldStop(node, event){
        let stopBubble = false;
        if(node._hiddenBoundEvents && node._hiddenBoundEvents[event.type] && node._hiddenBoundFlag){
            stopBubble = true;
            node._hiddenBoundFlag = false;
        }
        return stopBubble;
    }
    addHiddenBoundEvents(compNode, content){
        if(!compNode._boundEvents){return;}
        for(let i=0; i<content.childNodes.length ;i++){
            if(compNode._boundEvents){
                content.childNodes[i]._hiddenBoundEvents = compNode._boundEvents;
                if(content.childNodes[i]._boundEvents){
                    content.childNodes[i]._hiddenBoundFlag = true;
                }
            }
        }
    }
    callDisconnectedCallback(node){
        for(let i=0; i<node._hiddenTemplate.length; i++){
            node._hiddenTemplate[i].disconnectedCallback();
        }
        node._hiddenTemplate=[];
    }
    template(compNode, hiddenDomDiv, content){
        _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.setIgnoreDisconnect(true);
        compNode._ignoreDisconnect=true;
        compNode.__lyteIgnore=true;
        let parentElement=compNode._callee;
        let parentEle=compNode.parentElement;
        let domContent=document.createDocumentFragment();
        let len=compNode.childNodes.length;
        let firstChild=document.createTextNode("");
        let lastChild=document.createTextNode("");
        compNode._firstChild=firstChild;
        compNode._lastChild=lastChild;
    
        for(let i=0;i<len;i++){
            compNode.childNodes[0]._hiddenBoundEvents=compNode._boundEvents;
            if(compNode.childNodes[0]._boundEvents){
                compNode.childNodes[0]._hiddenBoundFlag=true;
            }
            domContent.append(compNode.childNodes[0]);
        }
        let elePos=Array.from(parentEle.childNodes).indexOf(compNode);
        parentEle.insertBefore(firstChild,parentEle.childNodes[elePos]);
    
    
        elePos=Array.from(parentEle.childNodes).indexOf(compNode);
        if(domContent.childNodes.length){
            parentEle.insertBefore(domContent,parentEle.childNodes[elePos]);
            elePos=Array.from(parentEle.childNodes).indexOf(compNode);
        }
    
        if(content){
            this.addHiddenBoundEvents(compNode, content);
            parentEle.insertBefore(content,parentEle.childNodes[elePos]);
        }
    
        elePos=Array.from(parentEle.childNodes).indexOf(compNode);
        parentEle.insertBefore(lastChild,parentEle.childNodes[elePos]);
        let parentHelper = compNode._parentHelper;
        if(parentHelper){
            if(parentHelper.tagName == "LYTE-YIELD"){
                parentHelper._hiddenTemplate.push(compNode);
            }
            else if(parentHelper._forContent){
                if(compNode._cx){
                    let ind = compNode._cx.itemIndex;
                    // let eleArr = parentHelper._forContent[ind];
                    // eleArr.push(content.childNodes[i]);
                    if(!parentHelper._hiddenTemplate[ind]){
                        parentHelper._hiddenTemplate[ind] = [];
                    }   
                    parentHelper._hiddenTemplate[ind].push(compNode);
                }
            } else if(parentHelper._caseContent) {
                // compNode._parentHelper._caseContent.push(content.childNodes[i]);
                parentHelper._hiddenTemplate.push(compNode);
            } 
            // else if(parentHelper._yieldContent) {
            //     compNode._parentHelper._yieldContent.push(content.childNodes[i]);
            // } else if(parentHelper._renderedComponent) {
    
            // }
        } 
        else if(compNode._callee){
            compNode._callee._hiddenTemplate.push(compNode);
        }
        hiddenDomDiv.content.appendChild(compNode);
        compNode._ignoreDisconnect=false;
        _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.setIgnoreDisconnect(false);
        compNode.__lyteIgnore=false;
    }
    didConnect(regIns){
        _index_js__WEBPACK_IMPORTED_MODULE_1__.ComponentRegistry.hide.define.remove(_index_js__WEBPACK_IMPORTED_MODULE_1__.LyteYield);
        let commonCls = regIns._registeredCommonClass;
        for(let cls in commonCls){
            _index_js__WEBPACK_IMPORTED_MODULE_1__.ComponentRegistry.hide.define.remove(commonCls[cls]);
            _index_js__WEBPACK_IMPORTED_MODULE_1__.ComponentRegistry.hide.define.getContent(commonCls[cls]);
        }
    }
}

Hide.__lMod = "Save";

Hide.define = {
    remove : function(classDef){
        const $remove = Element.prototype.remove;
        Object.defineProperty(classDef.prototype,"remove",{
            configurable : true,
            writable : true,
            value : function(){
                if(this._firstChild == undefined || this._firstChild.parentElement == null){
                    $remove.call(this);
                    this._firstChild = undefined;
                    this._lastChild = undefined;
                    return;
                }
                firstNode = this._firstChild;
                parent = firstNode.parentElement;
                lastNode = this._lastChild;
                while(firstNode.nextSibling != lastNode){
                    parent.removeChild(firstNode.nextSibling);
                }
                this._firstChild = undefined;
                this._lastChild = undefined;
                parent.removeChild(firstNode);
                parent.removeChild(lastNode);
                return;
            }
        })
    },
    getContent : function(customCrmComponent){
        Object.defineProperty(customCrmComponent.prototype,"getContent",{
            configurable : true,
            writable : true,
            value : function(){
                arr = [];
                if(this._firstChild == undefined){
                    console.error("Cannot access template through getContent");
                }
                firstNode = this._firstChild;
                lastNode = this._lastChild;
                firstNode = firstNode.nextSibling;
                while(firstNode!=lastNode){
                    arr.push(firstNode);
                    firstNode=firstNode.nextSibling;
                }
                return arr;
            }
        });
    }
};
Hide.register();

//ignorei18n_end

/***/ }),

/***/ 49088325:
/*!********************************************************************!*\
  !*** ./node_modules/@slyte/component/src/directives/lyte-turbo.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../index.js */ 26591616);
/* harmony import */ var _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../core/src/service.js */ 70549578);
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../core/index.js */ 21046606);
/* harmony import */ var _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../compiler/cli/lyte-base-compile.js */ 93957260);
/* harmony import */ var _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_3__);
//ignorei18n_start




_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getFastCompRegistry = function(obj){
    let registryMap = new WeakMap();
    var currentReg = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getCurrentRegistryIns();
    if(currentReg){
        if(this.iterateRegistries([currentReg],obj,registryMap)){
            return;
        }
    }else{
        let defRegIns = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDefaultRegistryIns();
        if(defRegIns){
            this.iterateRegistries([defRegIns],obj,registryMap);    
        }else{
            let defaultReg = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDefaultRegistry();
            let defaultRegInsArr = defaultReg._instanceList;
            this.iterateRegistries(defaultRegInsArr,obj,registryMap);
        }
        
    }
}
function noop() {}
/*convert to custom class and register*/
class Turbo extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__.Service {
    static register(){
        _index_js__WEBPACK_IMPORTED_MODULE_0__.ComponentRegistry.addFakeDirective(this,"turbo");
    }
    renderComponent(dynNode, preDefObj, self) {
    	var componentName = dynNode.componentName;
    	var staticAttrs = dynNode.staticAttrs;
        var constr = _index_js__WEBPACK_IMPORTED_MODULE_0__.ComponentRegistry._registeredCommonClass[componentName];
        if(!constr) {
          return "";
        }
        if(!_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDefaultRegistry()){
            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.error("Component's Default Registry not registered!");
            return;
        }
        var obj = {compName : componentName, type: "component"};
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getFastCompRegistry(obj);
        if(!_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.verifyDetails(obj)){
            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.error(self.localName + " : Component Not found in any registry.");
            return
        }
        var lIns = obj.lIns ,compClass = obj.compClass ,regIns = obj.regIns;
        var component;
        lIns.scopedInstance(compClass,[],function(ins){
            component = preDefObj.component = ins
        },[regIns])
        component._registryClass = compClass._registryClass;
        component.$registry = regIns;
        component.data = {};
        var appScp = this.$app || this.$registry.$app || this.$registry.$addon;
        if(component.$registry.constructor.lazyRegister){
            self.doRegistration(componentName, constr, constr.componentClass, component.$registry.constructor, component.$registry);
        }        
        if((appScp && appScp.$mutate)|| (appScp && appScp.$mutate)){
            component.data = appScp.$mutate.create(component.data);
        }
        let cmpData = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getCmpData(component.data);
    	let data = compClass._data ? compClass._data.apply(component) : {};
    	var def = "default";
    	for(let key in data) {
            cmpData[key] = data[key][def];
        }
        cmpData.errors = {};
        component.__data = data;
        var obsAttrs = regIns.constructor._registeredComponentClass[componentName]._observedAttributes;
        preDefObj.data = preDefObj.data || {};
		for(var key in staticAttrs) {
			if(obsAttrs.indexOf(key) !== -1) {
				preDefObj.data[key] = staticAttrs[key];
			}
		}
		let initProperties = preDefObj.data;
        if(initProperties) {
            for(let key in initProperties) {
                let actVal;
                if(component.__data[key] && component.__data[key].type !== _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDataType(initProperties[key]) && (initProperties[key] !== undefined  || component.__data[key].type === "boolean")) {
                    actVal = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.typeCast(initProperties[key], component.__data[key].type);
                } else {
                     actVal = initProperties[key];
                }
                let error = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.handleValidation(cmpData, key, actVal, component);
                if(!error) {
                    cmpData[key] = actVal;    
                }
                
            }
        }
        if(true) {
            component._methods = {};
			for(var key in compClass._methods) {
			    component._methods[key] = compClass._methods[key];
            }
            component.$node = {"querySelector" : noop, "localName" : componentName};
        	if(component.init) {
            	component.init.apply(component);
            }
            let initCallbacks;
            if(compClass._callBacks && (initCallbacks = compClass._callBacks.init)) {
			    for(let i=0;i<initCallbacks.length;i++) {
			        initCallbacks[i].value.apply(component);
			    }
			}
            preDefObj.unbound = true;
            delete component.$node.querySelector;
            if(component.$registry.constructor.lazyRegister && !constr.__isRegistered){
                var sval = component.$registry.constructor.lazyScheduler.dequeueTask(componentName, compClass._registryClass.name);
                if(sval && component.$registry.constructor.lazyScheduler.isGenerator(sval.handler)){
                    var gnxt = sval.handler.next(), gval;
                    while(gnxt.done == false){
                        gval = gnxt.value;
                        if(typeof gval == "function"){
                            gval();
                        }
                        gnxt = sval.handler.next()
                    }
                }
            }
        	return this.renderFast(compClass._dynamicNodes, compClass._sta, component, preDefObj, self);
        }
		return "";
    }
    renderFast(dynamicNodes, arr, comp, compPreDef, self) {
        if(!arr){
            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.error(comp.$node.localName+" component not supporting fast render. please add @turbo-supported in the component's template tag.");
            return "";
        }
        var fastRenderIndex;
        var prevComp;
        if(comp) {
            prevComp = self.component;
            self.component = comp;
        }
        // var compData = self.component.data;
        var compData = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getCmpData(self.component.data);
        var str = "";
        var dynamicCompile = arr;
        var dynamicCompileNodes = arr.cc;
        let removeFirstChar = false;
        for(var i=0;i<dynamicCompileNodes.length;i++) {
            let app = self.component.getAppOrAddon();
            if(dynamicCompileNodes[i] != undefined) {
                var inte = dynamicCompileNodes[i];
                var dynNode = dynamicNodes[inte];
                if(dynNode.dynamicValue) {
                    let locVal = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDD(compData, dynNode.newDynamicValue);
                    locVal = (locVal == undefined || locVal == null) ? "" : locVal; 
                    // str = str + ZSEC.Encoder.encodeForHTML(locVal);
                    str = str + Turbo.Security.escape(locVal);
                } else if(dynNode.helperInfo) {
                    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.ffr = true;
                    let processDetails = {};
                      let helperVal = self.processHelper(self, {name : dynNode.helperInfo.name, args : self.processArgs(self,dynNode, [], undefined, undefined, undefined, true, undefined, undefined, processDetails)}, undefined);
                    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.ffr = false;
                     helperVal = (helperVal == undefined || helperVal == null) ? "" : helperVal;
                    //  str = str + ( (dynNode.helperInfo.name === "unescape") ? helperVal : ZSEC.Encoder.encodeForHTML(helperVal) );
                    str = str + ((dynNode.helperInfo.name === "unescape") ? helperVal : Turbo.Security.escape(helperVal));
                } else if(dynNode.t){
                    switch(dynNode.t) {
                        case "f" : {
                            let prevDynamic = dynamicNodes[inte-1].a.items;
                            var items;
                            if(prevDynamic.dynamicValue) {      
                                items = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDD(compData, prevDynamic.newDynamicValue);
                            } else if(prevDynamic.helperInfo) {
                                let processDetails = {};
                                items = self.processHelper(self, {name : prevDynamic.helperInfo.name, args : self.processArgs(self,prevDynamic, [],undefined, undefined, undefined, true, undefined, undefined, processDetails)}, undefined);
                            }
                            var itemKey = dynNode._args.item || "item";
                            var indexKey = dynNode._args.index || "index";
                            let renderedStr = "";
                            if(items && items.length) {
                                var initialItemValue = compData[itemKey];
                                var initialIndexValue = compData[indexKey];
                                for(var indexInd=0;indexInd<items.length;indexInd++) {
                                    let item = items[indexInd];
                                    compData[itemKey] = item;
                                    compData[indexKey] = indexInd;
                                    renderedStr += this.renderFast(dynNode.dN, dynNode._sta, undefined, compPreDef, self);
                                }
                                compData[itemKey] = initialItemValue;
                                compData[indexKey] = initialIndexValue;
                            }
                            str += renderedStr;
                        }
                        break;
                        case "fI" : {
                            let prevDynamic = dynamicNodes[inte-1].a.object;
                            var object;
                            if(prevDynamic.dynamicValue) {
                                object = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDD(compData, prevDynamic.newDynamicValue);
                            } else if(prevDynamic.helperInfo) {
                                let processDetails = {};
                                object = self.processHelper(self, {name : prevDynamic.helperInfo.name, args : self.processArgs(self,prevDynamic, [], undefined, undefined, undefined, true, undefined, undefined, processDetails)}, undefined);
                            }
                            var valueKey = dynNode._args.value || "value";
                            var keyKey = dynNode._args.key || "key";
                            let renderedStr = "";
                            let objKeys;
                            if(object && (objKeys = Object.keys(object))) {
                                var initialValueValue = compData[valueKey];
                                var initialKeyValue = compData[keyKey];
                                for(var keyIndex=0;keyIndex < objKeys.length;keyIndex++) {
                                    let key = objKeys[keyIndex];
                                    compData[valueKey] = object[key];
                                    compData[keyKey] = key;
                                    renderedStr += this.renderFast(dynNode.dN, dynNode._sta, undefined, compPreDef, self);
                                }
                                compData[itemKey] = initialValueValue;
                                compData[indexKey] = initialKeyValue;
                            }
                            str += renderedStr;
                        }
                        break;
                        case "e" : 
                        case "s" : {
                            var value;
                            if((dynNode.t == "s" || dynNode.t == "e") && !dynNode.hd){
                                let prevDynamic = dynamicNodes[inte-1].a.value;
                                if(prevDynamic.dynamicValue) {
                                    value = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDD(compData, prevDynamic.newDynamicValue);
                                } else if(prevDynamic.helperInfo) {
                                    let processDetails = {};
                                    value = self.processHelper(self, {name : prevDynamic.helperInfo.name, args : self.processArgs(self,prevDynamic, [], undefined, undefined, true, undefined, undefined, processDetails)}, undefined);
                                }
                            }
                            var currentCaseName;
                            if(value) {
                                currentCaseName = dynNode.t === "e" ? "true" : value.toString();
                            } else {
                                if(dynNode.t=== "e")  {
                                    currentCaseName = "false";
                                } else {
                                    switch(value) {
                                    case undefined : 
                                        currentCaseName = "undefined";
                                        break;
                                    case null : 
                                        currentCaseName = "null";
                                        break;
                                    case false: 
                                        currentCaseName = "false";
                                        break;
                                    case "": 
                                        currentCaseName = '""';
                                        break;
                                    }
                                }
                            }
                            let scope;
                            if(dynNode.hd){
                                var casesList = dynNode.co;
                                var indexStart = 0;
                                for(var j=indexStart; j<casesList.length; j++){
                                    var caseName = casesList[j];
                                    if(dynNode.c[caseName].dcn){
                                        let prevDynamic = dynNode.c[caseName].cdp.a.case;
                                        if(prevDynamic.dynamicValue) {
                                            value = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDD(compData, prevDynamic.newDynamicValue);
                                        } else if(prevDynamic.helperInfo) {
                                            let processDetails = {};
                                            value = self.processHelper(self, {name : prevDynamic.helperInfo.name, args : self.processArgs(self,prevDynamic, [], undefined, undefined, undefined, true, undefined, undefined, processDetails)}, undefined);
                                        }
                                        if(value){
                                            scope  = dynNode.c[caseName];
                                            break;
                                        }
                                    }
                                }
                            }
                            else{
                                scope = dynNode.c[currentCaseName];
                            }
                            if(!scope) {
                                if((scope = dynNode.d) && scope._sta) {
                                    str += this.renderFast(scope.dN, scope._sta, undefined, compPreDef, self);
                                }
                            } else {
                                while(scope) {
                                    str += this.renderFast(scope.dN, scope._sta, undefined, compPreDef, self);
                                    if(scope.additional) {
                                        if(scope.additional.next) {
                                            scope = dynNode.c[scope.additional.next];
                                        } else {
                                            scope = dynNode.d;
                                        }
                                        
                                    } else {
                                        break;
                                    }
                                }
                            }
                        }
                        break;
                        case "a" : {
                            let nextDynamic;
                            let fastRenderProp;
                            let dynLength = dynamicNodes.length;
                            for(let k=inte+1;k<dynLength;k++) {
                                let locDyn = dynamicNodes[k];
                                if(locDyn.t !== "r" && locDyn.t !== "cD" && locDyn.t !== "i" && locDyn.t !== "cM") {
                                    break;
                                } else {
                                    if(dynamicNodes[k].t === "cD" || dynamicNodes[k].t === "i" || dynamicNodes[k].t === "cM") {
                                        if(dynamicNodes[k].p.toString() == dynamicNodes[inte].p.toString()) {
                                            if(dynamicNodes[k].t == "cD") {
                                                if(_index_js__WEBPACK_IMPORTED_MODULE_0__.ComponentRegistry._registeredCommonClass[dynamicNodes[k].componentName]) {
                                                    nextDynamic = dynamicNodes[k];
                                                    break;
                                                }
                                            } else {
                                                nextDynamic = dynamicNodes[k];
                                                break;
                                            }
                                        }
                                    }
                                }
                            }
                            let setAttr;
                            if(nextDynamic) {
                                setAttr = nextDynamic.t !== "cM";
                                fastRenderIndex = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRC++;
                                fastRenderProp = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex] = {"data" : {}};
                            }
                            if(!nextDynamic || nextDynamic.t !== "cM") {
                                str = str.substring(0, str.length - 11);
                            }
                            let attr = dynNode.a;
                            let actionObj = {};
                            for(let key in attr) {
                                if(attr[key].dynamicValue) {
                                    // let nodeValue = this.get(attr[key].dynamicValue);
                                    let nodeValue = attr[key].newDynamicValue ? _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDD(compData, attr[key].newDynamicValue) : self.get(attr[key].dynamicValue);
                                    
                                    if(fastRenderProp) {
                                        fastRenderProp.data[attr[key].camelCase] = nodeValue;
                                        if(typeof nodeValue == "string" && setAttr) {
                                            nodeValue = this.formatValue(key, nodeValue, self);
                                            str = str + nodeValue + " ";
                                        }
                                    } else {
                                        nodeValue = this.formatValue(key, nodeValue, self);
                                        str = str + nodeValue + " ";
                                    }
                                } else if(attr[key].helperInfo) {
                                    if(attr[key].helperInfo.name === "action") {
                                        let actionName = attr[key].helperInfo.args[0];
                                        let boundName;
                                        if(actionName.startsWith('"') || actionName.startsWith("'")) {
                                            boundName = actionName.substring(1, actionName.length - 1);
                                        } else {
                                            _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte.warn("Deprecation warning. Action name " + actionName + " must be in quotes");
                                            boundName = actionName;
                                        }
                                        let processDetails = {};
                                        let actArgs = self.processArgs(self, attr[key], [], undefined, "__lyteEvent__", "__lyteNode__", undefined, undefined, undefined, processDetails);
                                        let actualAttrName = attr[key].globalEvent ? attr[key].name : (attr[key].name.indexOf("-") !== -1)? attr[key].name : attr[key].name.substr(2);
                                        actionObj[actualAttrName] = {"name" : boundName, "args" : attr[key].helperInfo.args, "actArgs" : actArgs, "globalEvent" : attr[key].globalEvent ? true : false, "skipArgProcessing" : true};
                                    }  else if (attr[key].helperInfo.name === "method") {
                                        var methodsObj = compPreDef ? compPreDef._methods : self.component._methods; 
                                        if(fastRenderProp) {
                                            let methods = fastRenderProp._methods = fastRenderProp._methods || {};
                                            let parentComp = compPreDef ? compPreDef.component : self.component;
                                            let processDetails = {};
                                            let actArgs = self.processArgs(self, attr[key], [], undefined, "__lyteEvent__", "__lyteNode__", undefined, undefined, undefined, processDetails);
                                            var methodFunc = function() { //eslint-disable-line no-loop-func
                                                let node = self.$node;
                                                let args = actArgs.slice(1);
                                                let functionName = actArgs[0];
                                                let customArgs = Array.from(arguments);
                                                let mainArgs = args.$concat(customArgs);
                                                return parentComp._methods[functionName].apply(parentComp, mainArgs);
                                            }
                                            methods[attr[key].camelCase] = methodFunc;
                                        }
                                    } else {
                                        let processDetails = {};
                                        let nodeValue = self.processHelper(self, {name : attr[key].helperInfo.name, args : self.processArgs(self,attr[key], [],undefined, undefined, undefined, true, undefined, undefined, processDetails)}, undefined);
                                        if(fastRenderProp) {
                                            fastRenderProp.data[attr[key].camelCase] = nodeValue;
                                            if(typeof nodeValue == "string" && setAttr) {
                                                nodeValue = this.formatValue(key, nodeValue, self);
                                                str = str + nodeValue + " ";
                                            }
                                        } else {
                                            nodeValue = this.formatValue(key , nodeValue, self);
                                            str = str + nodeValue + " ";                                        
                                        }
                                    }
                                }
                            }
                            if(Object.keys(actionObj).length) {
                                let locIndex = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRC++;
                                str = str + "lyteaction=" + locIndex + " ";
                                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[locIndex] = actionObj;
                                actionObj.componentName = self.component.$node.localName || "undefined";
                                //str = str + "lyteaction=" + ZSEC.Encoder.encodeForHTMLAttribute(JSON.stringify(actionObj)) + " ";
                            }
                            if(fastRenderProp && nextDynamic.t !== "cM" && nextDynamic.t !== "i") {
                                str = str + "_lyteprop=" + fastRenderIndex + " ";
                            }
                            if(!dynNode.attrHandling) {
                                dynamicCompile[i+1] = dynamicCompile[i+1].substr(1);
                                dynNode.attrHandling = true;
                            }
                        }
                        break;
                        case "i" : {
                            let preDefObj = fastRenderIndex !== undefined ? _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex] : undefined;
                            if(preDefObj) {
                                delete _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex];
                                // _LC.$pushFrc(fastRenderIndex);
                            }
                            fastRenderIndex = undefined;
                            let yieldObj;
                            let yieldName = dynNode.yieldName || preDefObj.data.yieldName;
                            if(preDefObj) {
                                delete preDefObj.data.yieldName;
                            }
                            let yieldCallee;
                            if(!compPreDef || !compPreDef.yields) {
                                yieldObj = self._yields[yieldName];
                                if(yieldObj) {
                                    yieldCallee = yieldObj._callee;
                                } else {
                                    break;
                                }
                            } else {
                                yieldObj = compPreDef.yields[yieldName];
                                yieldCallee = compPreDef._yieldCallee;
                            }
                            if(yieldObj) {
                                let parentScope;
                                let locComp;
                                if(yieldCallee instanceof HTMLElement) {
                                    parentScope = yieldCallee;
                                } else {
                                    parentScope = self;
                                    locComp = self.component;
                                    self.component = yieldCallee.component;
                                }
                                let contextSwitch = {};
                                let contextSwitchArray = [];
                                var psCmpData = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getCmpData(parentScope.component.data);
                                if(yieldObj._cx) {
                                    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.adCx(yieldObj, contextSwitchArray);
                                }
                                if(preDefObj) {
                                    for(var key in preDefObj.data) {
                                        contextSwitch[key] = psCmpData[key];
                                        psCmpData[key] = preDefObj.data[key];
                                    }
                                }
                                var componentScope = parentScope.component;
                                if(prevComp && prevComp.$node == yieldCallee) {
                                    componentScope = prevComp
                                }
                                // if(componentScope == self.component) {
                                //     componentScope = prevComp;
                                // }
                                str = str + this.renderFast(yieldObj.dN || yieldObj._dynamicNodes, yieldObj._sta, componentScope, yieldCallee, parentScope);
                                for(let key in contextSwitch) {
                                    psCmpData[key] = contextSwitch[key];
                                }
                                if(yieldObj._cx) {
                                    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.rmCx(yieldObj, contextSwitchArray); 
                                }
                                if(locComp) {
                                    parentScope.component = locComp;
                                }
                            }
                            //str = str + self.renderComponent(dynNode, preDefObj, compData);
                        }
                        break;
                        case "cM" : {
                            if(fastRenderIndex !== undefined) {
                                let preDefObj = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex];
                                let componentName = preDefObj.data.componentName;
                                if(componentName) {
                                    if(_index_js__WEBPACK_IMPORTED_MODULE_0__.ComponentRegistry._registeredCommonClass[componentName]) {
                                        str = str + "<" + componentName + " _lyteprop="+ fastRenderIndex+">";
                                        fastRenderIndex = undefined;
                                        str = str + this.renderComponent({"componentName" : componentName}, preDefObj, self);
                                        str = str + "</" + componentName + ">";
                                    } else {
                                        str = str + "component not registered";
                                        // Handle case where component has not been registered yet. 
                                    }   
                                }
                            }
                        }
                        break;
                        case "cD" : {
                            let preDefObj = fastRenderIndex !== undefined ? _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex] : {};
                            fastRenderIndex = undefined;
                            str = str + this.renderComponent(dynNode, preDefObj, self);
                        }
                        break;
                        case "r" : {    
                            let preDefObj;
                            if(fastRenderIndex !== undefined) {
                                preDefObj = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex];
                            } else {
                                fastRenderIndex = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRC++;
                                preDefObj = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.fRP[fastRenderIndex] = {};
                            }
                            preDefObj.yields = preDefObj.yields || {};
                            preDefObj.yields[dynNode._args["yield-name"]] = dynNode;
                            if(!preDefObj._yieldCallee) {
                                preDefObj._yieldCallee = compPreDef ? compPreDef : {"component" : self.component};
                            }
                        }
                    }
                } else {
                    str = str + dynamicCompile[i];
                }
            } else {
                str = str + dynamicCompile[i];
            }
        }
        if(comp) {
            self.component = prevComp;
        }
        return str;
    }
    formatValue(key, nodeValue, self) {
        let type = typeof nodeValue;
        let app = self.component.getAppOrAddon();
        if(nodeValue === "") {
            type = "undefined";
        }
        let retVal;
        let isSpecialAttr = (key == "src") || (key == "href");
        switch(type) {
        case "boolean" : 
            return nodeValue ? key : "";
        case "object" : 
            retVal = ((typeof Record != "undefined" && nodeValue instanceof Record) ? JSON.stringify(nodeValue.$.toJSON()) : JSON.stringify(nodeValue));
            // return key + "=" + (isSpecialAttr ? retVal : ZSEC.Encoder.encodeForHTMLAttribute(retVal));
            return key + "=\"" + (isSpecialAttr ? retVal : Turbo.Security.escape(retVal)) + "\"";
        case "undefined" : 
            return key
        default : 
            {
                // return key + "=" + (isSpecialAttr ? nodeValue : ZSEC.Encoder.encodeForHTMLAttribute(nodeValue) );
                return key + "=\"" + (isSpecialAttr ? nodeValue : Turbo.Security.escape(nodeValue) ) + "\"";
            }
        }
    }
    static caseTx(dynNewCompile,mustache,info,j){
        if(dynNewCompile) {
            var newActObj;
            if(mustache){
                newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_3___default().getArray(mustache);  
                if(!newActObj && mustache){
                    newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_3___default().getHelper(mustache,true);  
                }
                info.newHelperInfo = newActObj;
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.replaceWithPf(dynNewCompile, document.createTextNode("__**--Lyte"+j+"__**"));
            }   
        }
    }
    static caseI(dynNewCompile, info, j){
        // if(dynNewCompile) {
            dynNewCompile.appendChild(document.createTextNode("__**--Lyte"+j+"__**"));
            let yieldAttr = dynNewCompile.attributes["yield-name"];
            info.yieldName = yieldAttr ? yieldAttr.nodeValue : undefined;
        // }
    }
    static caseCD(dynNewCompile, info, j){
        // if(dynNewCompile) {
            dynNewCompile.appendChild(document.createTextNode("__**--Lyte"+j+"__**"));
            info.componentName = dynNewCompile.localName;
            var attrs = Array.from(dynNewCompile.attributes);
            info.staticAttrs = {};
            for(var i=0;i<attrs.length;i++) {
                info.staticAttrs[_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.String.toCamelCase(attrs[i].nodeName)] = attrs[i].nodeValue;
            }
            delete info.staticAttrs._lyteprop;
        // }
    }
    static setAttribute(node,componentName,actString){
        // dynNewCompile.setAttribute(node.attributes[i].name.substr(2),componentName+" => "+ actString);
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.setAttribute(node, componentName, actString);

    }
    static removeAttributeObj(dynNewCompile, attr, j){
        // if(dynNewCompile) {
            for(let key in attr) {
                dynNewCompile.removeAttribute(key);
            }
            dynNewCompile.setAttribute("_lyteAttr" , "__**--Lyte" + j + "__**", "");
        // }
    }
    static removeAttribute(node, name, value){
        node.removeAttribute(name, value);
    }
    static removeAttributeArr(dynNewCompile, toBeRemoved){
        if(toBeRemoved.length){
            for(let i=0; i<toBeRemoved.length;i++){
                dynNewCompile.removeAttribute(toBeRemoved[i]);
                // if(dynNewCompile) {
                //     dynNewCompile.removeAttribute(toBeRemoved[i]);
                // }
            }
        }
    }
    static getDynNewCompile(newCompile,pos){
        return _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDynamicNode(newCompile,pos);
    }
    static replaceWithPf(dynNewCompile,j){
        return _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.replaceWithPf(dynNewCompile, document.createTextNode("__**--Lyte"+j+"__**"));
    }
    static cMBefore(dynN, info){
        let dynNewCompile;
        // if(!dynNewCompile && (type == "r" || dynN.hasAttribute("unbound"))) { //af check
            if(_core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte._ms) {
                dynNewCompile = document.createElement("div");
                dynNewCompile.innerHTML = info.actualTemplate ? info.actualTemplate : dynN.outerHTML;
                dynNewCompile = dynNewCompile.childNodes[0];
                constr.splitTextNodes(dynNewCompile);
            } else {
                dynNewCompile = dynN.cloneNode(true);
            }
        // }

        let depthTemp;
        let dnNode;
        var actualTemplateNewCompile = dynNewCompile ? (_core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte._ie ? dynNewCompile : dynNewCompile.content) : undefined;
        // if(info.actualTemplate) {
        //     if(dynNewCompile) {
        //         actualTemplateNewCompile = _LC.getContentForIE(info.actualTemplate, constr);
        //     }
        // }
        return dynNewCompile//{dynNewCompile : dynNewCompile, actualTemplateNewCompile : actualTemplateNewCompile}
    }
    static cMBefore2(dynNewCompile, info, constr){
        var actualTemplateNewCompile = dynNewCompile ? (_core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte._ie ? dynNewCompile : dynNewCompile.content) : undefined;
        if(info.actualTemplate) {
            return _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getContentForIE(info.actualTemplate, constr);
        }
        return actualTemplateNewCompile;
    }
    static cMAfter(actualTemplateNewCompile, info, type, j, dynNewCompile){
        if(actualTemplateNewCompile) {
            let staticTemp = info.actualTemplate ? actualTemplateNewCompile : dynNewCompile;
            info._sta = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.processStatic(staticTemp);                      
        }
        if(dynNewCompile) {
            info._args = {};
            if(type === "r") {
                //dynNewCompile.parentNode.parentNode.insertBefore(document.createTextNode("__**--Lyte"+j+"__**"), dynNewCompile.parentNode);
                //dynNewCompile.parentNode.setAttribute("_lyteyield", "");
                //_LC.replaceWithPf(dynNewCompile, document.createTextNode(""));
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.replaceWithPf(dynNewCompile, document.createTextNode("__**--Lyte"+j+"__**"));
            } else if(type === "cM") {
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.replaceWithPf(dynNewCompile, document.createTextNode((dynNewCompile.hasAttribute("_lyteattr") ? dynNewCompile.getAttribute("_lyteattr"): "" )+ "__**--Lyte"+j+"__**"));
            } else {
                //dynNewCompile.replaceWith(document.createTextNode("__**--Lyte"+j+"__**"))
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.replaceWithPf(dynNewCompile, document.createTextNode("__**--Lyte"+j+"__**"));
            }
            Array.from(dynNewCompile.attributes).forEach(function(item) { //eslint-disable-line no-loop-func
                info._args[item.nodeName] = item.nodeValue;
            });
        }
    }
    static caseES(dynNewCompile, info, constr){
        var actualTemplateNewCompile = dynNewCompile ? (_core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte._ie ? dynNewCompile : dynNewCompile.content) : undefined;
        if(info.actualTemplate) {
            // dynN = _LC.getContentForIE(info.actualTemplate, constr, undefined);
            if(dynNewCompile) {
                let dummy = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getContentForIE(info.actualTemplate, constr);
                actualTemplateNewCompile = _core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte._ie ? dummy : dummy.content;
            }
        }
        return actualTemplateNewCompile;
    }
    static getNewCompile(componentClass){
        let newCompile;
        if(_core_index_js__WEBPACK_IMPORTED_MODULE_2__.Lyte._ie) {
            newCompile = document.createElement("div");
            newCompile.innerHTML = componentClass._template.outerHTML;
            newCompile = newCompile.childNodes[0];
        } else {
            newCompile = componentClass._template.cloneNode(true);
        }
        return newCompile;
    }
}
Turbo.__lMod = "Turbo";
Turbo.Security = {
    "_eM" : {
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#x27;',
        '`': '&#x60;',
        '=': '&#x3D;'
    }, 
    "_eR" : /[&<>"'`=]/g,
    "_eF" : function(str) {
        return this._eM[str];
    }, 
    "escape" : function(string) {
        if (typeof string !== 'string') {
            string = '' + string;
        }
        return string.replace(this._eR, this._eF.bind(this));
    }
};
Turbo.register();
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Turbo);
//ignorei18n_end

/***/ }),

/***/ 12867660:
/*!*******************************************************************!*\
  !*** ./node_modules/@slyte/component/src/directives/lyte-view.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../index.js */ 26591616);
/* harmony import */ var _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../core/src/service.js */ 70549578);
//ignorei18n_start



_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.pendingViewPortElements = [];
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isInViewPort = function(el) {
    var rect = el.getBoundingClientRect();
    return (((rect.top >= 0 && rect.top <= (window.innerHeight || document.documentElement.clientHeight)) 
    || (rect.bottom >= 0 && rect.bottom <= (window.innerHeight || document.documentElement.clientHeight)))
    &&
    ((rect.left >= 0 && rect.left <= (window.innerWidth || document.documentElement.clientWidth))
    || (rect.right >= 0 && rect.right <= (window.innerWidth || document.documentElement.clientWidth))));

}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC._doneRaf = {};
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC._isInViewPort = function(node){
    node._isViewPortPending = true;
    var fchild = node._caseContent[0];
    if(fchild && _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isInViewPort(fchild)) {
        node._isInView = true;
        if(node._custom){
            let viewOutIns = node._viewOut;
            if(viewOutIns && !node._renderPromise._resolved && viewOutIns.beforeRemove){
                node._promiseObject.resolvedByLyte = true;
                viewOutIns._beforeRemoveCalled = true;
                viewOutIns.beforeRemove.apply(viewOutIns,[node._promiseObject]);
            }else if(!node._renderPromise._reject){
                return true;
            }    
        }
        // if(node._inView && !node._renderPromise._resolved && !node._disable.inView){
        //     node._inView.apply(this,[node._promiseObject]);
        // }
        else{
            return true;
        }
    }
}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC._executeIfTemplates = function(node){
    if(node._currentScope == "default"){
        node._vpNode._attributes.case = true;
    }else{
        node._tempList[node._currentScope]._attributes.case = true;
    }
    var isYieldContext,contextSwitchInfo;
    if (node._cx || node.nodeType === 2 && node.ownerElement._cx) {
        contextSwitchInfo = node._cx || node.ownerElement._cx;
        var contextSwitchArray = [];
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.changeContext(contextSwitchInfo.node, contextSwitchArray, contextSwitchInfo);
    } else if (node.tagName === "LYTE-YIELD" && node._callee._cx) {
        isYieldContext = true;
        contextSwitchInfo = node._callee._cx;
        var contextSwitchArray = [];
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.changeContext(contextSwitchInfo.node, contextSwitchArray, contextSwitchInfo, true);
    }
    node._callee.updateSwitchHelper("if", node, node._cx, true, true);
    if (contextSwitchInfo) {
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.removeContext(contextSwitchInfo.node, contextSwitchArray, contextSwitchInfo, isYieldContext);
    }
    node._isViewPortPending = false;
    if(node.getAttribute("__vp") == "c-new"){
        node._callee.setData("lyteViewPort",false);//af chcek
    }
}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortTemplate = function(node){
    if((node.tagName == "TEMPLATE" || node._origTemplate) && node.getAttribute("is") == "switch"){
        return true;
    }
}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort = function() {
    var pve = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.pendingViewPortElements;
    if(!pve.length) {
        return;
    }
    var arr = Array.from(pve);
    var inViewPortElements = [];
    for(var i=arr.length-1;(i >= 0);i--) {
        let ele = arr[i];
        if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortTemplate(ele)){
            // let actTemp = _LC.getActViewPortTemplate(ele);
            if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC._isInViewPort(ele,ele._callee)){
                inViewPortElements.push(pve.splice(i,1)[0]);
                ele._inPve = false;
                ele._viewPortRendered = true;
            }
            // actTemp._inPve = ele._inPve;
            // actTemp._viewPortRendered = ele._viewPortRendered;
        }
        else if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isInViewPort(ele._dummyViewPortF) || _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isInViewPort(ele._dummyViewPortL)) {
            ele._dummyViewPortF = ele._dummyViewPortL = undefined;
            inViewPortElements.push(pve.splice(i,1)[0]);
        }
    }

    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortPending = false;

    for(var j=0;j<inViewPortElements.length;j++) {
        let ele = inViewPortElements[j];
        if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortTemplate(ele)){
            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC._executeIfTemplates(ele);
        }else{
            ele.setData("lyteViewPort", false);
        }
    }
    if(inViewPortElements.length) {
        requestAnimationFrame(function() {
            requestAnimationFrame(function() {
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();
            });
        });    
    }
}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortSettings = {
    debounce : 0,
    executePendingViewPortElements : function() {
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();
    }
}
window.addEventListener("resize", function() {
    requestAnimationFrame(function() {
        requestAnimationFrame(function() {
            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();     
        })    
    });
});

document.addEventListener("scroll", function() {
    var debounceTime = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortSettings.debounce
    if(debounceTime) {
        if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortScrollDebounce) {
            clearTimeout(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortScrollDebounce);
        }
        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortScrollDebounce = setTimeout(function() {
                requestAnimationFrame(function() {
                    requestAnimationFrame(function() {
                        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();        
                    }) 
                });
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortScrollDebound = undefined;
        },debounceTime);
    } else {
        requestAnimationFrame(function() {
            requestAnimationFrame(function() {
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();        
            }) 
        });
        
    }
}, true);
/*convert to custom class and register*/
class View extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__.Service {
    constructor(regIns){
        super()
        regIns.viewPortSettings = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortSettings;
        regIns.isInViewPort = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isInViewPort;
        if(!regIns.registeredHelpers.lyteViewPort){
            regIns.registeredHelpers.lyteViewPort = this.constructor.viewHelper;
        }
    }
    static register(){
        _index_js__WEBPACK_IMPORTED_MODULE_0__.ComponentRegistry.addFakeDirective(this,"view");
        var defHelpers = _index_js__WEBPACK_IMPORTED_MODULE_0__.ComponentRegistry._defaultHelpers;
        defHelpers["lyteViewPort"] = this.viewHelper;
    }
    static viewHelper(node,config){   
        let compName = node._callee.localName;
        let switchNode = node._parentSwitch; 
        if(switchNode._viewPortRendered){
            return true;
        }
        switchNode._vpNode = node;
        let disableViewPort = config;
        let pve = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.pendingViewPortElements;
        let promise;
        switchNode._disable = {};
        if(config && typeof config == "object"){
            switchNode._custom = true;
            disableViewPort = config.disableViewPort;
            if(config.viewIn || config.viewOut){
                promise = true;
                switchNode._viewIn = config.viewIn ? Object.assign(config.viewIn) : undefined;
                switchNode._viewOut = config.viewOut ? Object.assign(config.viewOut) : undefined;
                let _res,_rej;
                switchNode._renderPromise = new Promise(function(res,rej){
                    _res = res;
                    _rej = rej;
                });
                switchNode._promiseObject = {
                    resolvedByLyte : false,
                    spouse : switchNode._viewIn,
                    resolve : function(){
                        if(!switchNode._viewOut._afterAppendCalled && switchNode._viewOut.beforeRemove && switchNode._renderPromise && !switchNode._viewOut._beforeRemoveCalled){
                            switchNode._viewOut.beforeRemove.apply(switchNode._viewOut,[switchNode._promiseObject]);
                        }
                        if(switchNode._renderPromise){
                            _res();
                        }
                    },
                    reject : function(){
                        switchNode._renderPromise._reject = true;
                        _rej();
                    }
                }
                switchNode._renderPromise.then(function(){
                    if(!switchNode._callee.component){
                        return;
                    }
                    switchNode._renderPromise._resolved = true;
                    let ind = pve.indexOf(switchNode);
                    if(ind != -1){
                        pve.splice(ind,1)
                    }
                    let viewInIns = switchNode._viewIn;
                    if(viewInIns && viewInIns.beforeAppend){
                        viewInIns.beforeAppend.apply(viewInIns);
                    }
                    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC._executeIfTemplates(switchNode);
                    if(viewInIns && viewInIns.afterAppend){
                        viewInIns.afterAppend.apply(viewInIns);
                    }
                    switchNode._renderPromise = null;
                },function(){
                    Lyte.error("Promise rejected - Viewport cannot be toggled.");
                })
            }
        }
        if(!disableViewPort){
            if(!_index_js__WEBPACK_IMPORTED_MODULE_0__._LC._doneRaf[compName]){
                requestAnimationFrame(function () {
                    requestAnimationFrame(function () {
                        let inViewTemplates = [];
                        var arr = Array.from(pve);
                        for(var i=arr.length-1;(i >= 0);i--) {
                            let temp = arr[i];
                            if(temp.tagName == "TEMPLATE" && temp._callee && !temp._callee._destroyed){
                                // var actTemp = _LC.getActViewPortTemplate(temp);
                                let switchNode = temp; 
                                let viewOutIns = switchNode._viewOut;
                                if(viewOutIns && viewOutIns.afterAppend){
                                    viewOutIns.afterAppend.apply(viewOutIns,[switchNode._promiseObject]);
                                    viewOutIns._afterAppendCalled = true;
                                }
                                if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortTemplate(temp) && !temp._callee._destroyed){
                                    if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC._isInViewPort(temp,temp._callee)){
                                        inViewTemplates.push(pve.splice(i,1)[0]);
                                        temp._inPve = false;
                                        temp._viewPortRendered = true;
                                    }
                                }
                                // actTemp._inPve = temp._inPve;
                                // actTemp._viewPortRendered = temp._viewPortRendered;
                            }
                        }
                        for(let i=0; i<inViewTemplates.length; i++){
                            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC._executeIfTemplates(inViewTemplates[i]);
                        }
                        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC._doneRaf[compName] = false;
                        if(inViewTemplates.length) {
                            requestAnimationFrame(function() {
                                requestAnimationFrame(function() {
                                    _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();
                                });
                            });    
                        }
                    });
                });
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC._doneRaf[compName] = true;
            }
            if(pve.indexOf(switchNode) == -1){
                switchNode._inPve = true;
                pve.push(switchNode); 
            }
            return false;
        }else if(pve.length){
            // let fakeTemp = _LC.getFakeTemplate(switchNode);
            let ind = pve.indexOf(switchNode);
            if(ind != -1){
                pve[ind]._inpve = false;
                pve[ind]._viewPortRendered = true;
                pve.splice(ind,1);
            }
        }
        return true;
    }
    rmCaseContent(node){
        var pve = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.pendingViewPortElements;
        if(node._inPve && pve.length){
            let ind = pve.indexOf(node);
            if(ind != -1){
                pve.splice(ind,1);
            }
        }
        if(node.getAttribute("__vp") == "c-old"){
            let ind = pve.indexOf(node._callee);
            if(ind != -1){
                pve.splice(ind,1);
            }
        }
    }
    connectedCallback(comp){
        var isViewPort;
        if(comp._initProperties && comp._initProperties.hasOwnProperty('lyteViewPort')){
            isViewPort = comp._initProperties.lyteViewPort;
        }
        else{
            isViewPort = comp.component.data.lyteViewPort;
        }
        if(isViewPort && comp.component.constructor._template.getAttribute("__vp") != "c-new") {
            if(!_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortPending) {
                requestAnimationFrame(function() {
                    requestAnimationFrame(function() {
                        _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executePendingViewPort();
                    });
                });
                _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.isViewPortPending = true;
            }
            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.pendingViewPortElements.$push(comp);
        }
    }
    actualConnectedCallback(comp,content){
        if(comp.component.data.lyteViewPort) {
            comp._vpc = true;
            // if(Lyte._ie) {
            //     for(var i=0;i<content.childNodes.length;i++) {
            //         if(content.childNodes[i].tagName) {
            //             comp._dummyViewPortF = content.childNodes[i];
            //             break;
            //         }
            //     }
            //     for(var i=content.childNodes.length-1;i>=0;i--) {
            //         if(content.childNodes[i].tagName) {
            //             comp._dummyViewPortL = content.childNodes[i];
            //             break;
            //         }
            //     }
            // } else {
                comp._dummyViewPortF = content.children[0];
                comp._dummyViewPortL = content.children[content.children.length - 1];
            // }
            // comp.appendChild(content);
        }
    }
    disconnectedCallback(comp){
        var pve = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.pendingViewPortElements;
        if(comp.component.data.lyteViewPort) {
            let index = pve.indexOf(comp);
            if(index != -1) {
                pve.splice(index, 1);
            }
        }
        if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortSettings.handlePendingItems && comp._vpc) {
            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executeVpe = true;
        }
        if(pve.length){
            for(let i=0; i<pve.length; i++){
                if(pve[i] && pve[i].tagName == "TEMPLATE" && pve[i].getAttribute("is") == "switch" && pve[i]._callee == comp){
                    pve.splice(i,1);
                    i--;
                }
            }
        }
    }
    actualDisconnected(){
        if(_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executeVpe){
            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.viewPortSettings.executePendingViewPortElements();
            _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.executeVpe = false;
        }
    }
}
View.__lMod = "View";
View.register();
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (View);
//ignorei18n_end

/***/ }),

/***/ 88544617:
/*!********************************************************************!*\
  !*** ./node_modules/@slyte/component/src/ide/lyte-ide-override.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
function IdeOverride(_LC){
    _LC.core._constructor.prototype.executeBlockHelpers = function(updateHelpers, node) {
        var originalUpdateHelpers = Array.from(updateHelpers);
        _LC.core.executeBlockHelpers.call(this,updateHelpers);
        if(node){
            for(var i=0;i<originalUpdateHelpers.length;i++) {
                var lastNode = originalUpdateHelpers[i].lastNode;
                if(lastNode._placeHolder) {
                    lastNode = lastNode._placeHolder;
                }
                lastNode.__lyteHelper = node;
            }
        }   
    }
    _LC.core._constructor.prototype.updateForHelper = function(node, options, contextSwitchInfo, establishBindings, staticTempArr,yieldComp, initialRender,_yields) {
        if(options){
            if(options.type !== "remove" && node._forContent && node._forContent.length === 1 && node._forContent[0].length === 1 && node._forContent[0][0].__lyteHelper === node &&
            node._forContent[0][0].nodeType === 3) {
                node._forContent[0][0].remove();
                node._forContent=[];
            }
        }
        var ret =  _LC.core.updateForHelper.call(this, node, options, contextSwitchInfo, establishBindings, staticTempArr,yieldComp, initialRender,_yields)
        if(node.tagName !== "TEMPLATE") {
            node  = node._origTemplate;
        }
        node._placeHolder.__lytehelper = node._placeHolder._helper;
        node._placeHolder._actTemplate = node;
        if(options && options.type !== "remove") {
            if(!node._fRender){
                node._forContent.forEach(function(arr){
                    arr.forEach(function(ele){
                        ele.__lyteHelper = node;	
                    })  
                })
            } 
        }
        var lastNode = node;
        var items =  node._attributes.items; //node._items;
        if(!items || items.length === 0){
            var emptyTextNode = document.createTextNode("");
            node._forContent[0]=[emptyTextNode];
            emptyTextNode.__lyteHelper = node; 
            // _LC.unbound = initialUnbound;
            if(options.type !== "default") { 
                lastNode._placeHolder.parentNode.insertBefore(emptyTextNode, lastNode._placeHolder)
            }
        }
        return ret;
    }
    _LC.core._constructor.prototype.updateForInHelper = function(node, options, contextSwitchInfo, establishBindings,yieldComp,initialRender,_yields) {
        if(options){
            if(options.type !== "delete" && node._forContent && node._forContent.helperLink && node._forContent.helperLink[0].__lyteHelper === node &&
                node._forContent.helperLink[0].nodeType === 3) {
                node._forContent.helperLink[0].remove();
                delete node._forContent.helperLink;
            }
        }
        var ret = _LC.core.updateForInHelper.call(this,node, options, contextSwitchInfo, establishBindings,yieldComp,initialRender,_yields);
        if(node.tagName !== "TEMPLATE") {
            node  = node._origTemplate;
        }
        node._placeHolder.__lytehelper = node._placeHolder._helper;
        node._placeHolder._actTemplate = node;
        if(node._currentObject && options && options.type !== "delete") {
            node._keysArray.forEach(function(itemKey){
                node._forContent[itemKey].forEach(function(item) {		
                    item.__lyteHelper = node;		        
                });
            })
        }
        var lastNode = node;
            if((!node._keysArray || node._keysArray.length === 0)){
                var emptyTextNode = document.createTextNode("");
                node._forContent.helperLink = [emptyTextNode];
                emptyTextNode.__lyteHelper = node;
                if(options.type !== "default") {   
                    lastNode._placeHolder.parentNode.insertBefore(emptyTextNode, lastNode._placeHolder)
                }  	
            }
        return ret; 
    }
    _LC.core._constructor.prototype.updateSwitchHelper = function(type,node, contextSwitchInfo, update, establishBindings,yieldComp,_yields,templateCaseNode,helperNode){
        var ret = _LC.core.updateSwitchHelper.call(this,type,node, contextSwitchInfo, update, establishBindings,yieldComp,_yields,templateCaseNode,helperNode);
        if(node.tagName !== "TEMPLATE") {
            node  = node._origTemplate;
        }
        node._placeHolder.__lytehelper = node._placeHolder._helper;
        node._placeHolder._actTemplate = node;
        node._caseContent.forEach(function(item) {		
            item.__lyteHelper = node;		
        })
        return ret;
    }
}
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (IdeOverride);

/***/ }),

/***/ 26591616:
/*!*************************************************************!*\
  !*** ./node_modules/@slyte/component/src/lyte-component.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Component": () => (/* binding */ Component),
/* harmony export */   "ComponentRegistry": () => (/* binding */ ComponentRegistry),
/* harmony export */   "Helper": () => (/* binding */ Helper),
/* harmony export */   "LyteCustomElement": () => (/* binding */ LyteCustomElement),
/* harmony export */   "LyteYield": () => (/* binding */ LyteYield),
/* harmony export */   "RawComponent": () => (/* binding */ RawComponent),
/* harmony export */   "_LC": () => (/* binding */ _LC),
/* harmony export */   "_addLyteAction": () => (/* binding */ _addLyteAction),
/* harmony export */   "_setIgnoreDisconnect": () => (/* binding */ _setIgnoreDisconnect),
/* harmony export */   "addBindings": () => (/* binding */ addBindings),
/* harmony export */   "appendChild": () => (/* binding */ appendChild),
/* harmony export */   "arrayUtils": () => (/* binding */ arrayUtils),
/* harmony export */   "customElementPrototype": () => (/* binding */ customElementPrototype),
/* harmony export */   "defProp": () => (/* binding */ defProp),
/* harmony export */   "getNearestApp": () => (/* binding */ getNearestApp),
/* harmony export */   "insertAfter": () => (/* binding */ insertAfter),
/* harmony export */   "insertBefore": () => (/* binding */ insertBeforeFn),
/* harmony export */   "makeSet": () => (/* binding */ makeSet),
/* harmony export */   "nextTick": () => (/* binding */ nextTick),
/* harmony export */   "objectUtils": () => (/* binding */ objectUtils),
/* harmony export */   "render": () => (/* binding */ render),
/* harmony export */   "replaceWith": () => (/* binding */ replaceWith),
/* harmony export */   "set": () => (/* binding */ set),
/* harmony export */   "shouldIgnoreDisconnect": () => (/* binding */ shouldIgnoreDisconnect)
/* harmony export */ });
/* harmony import */ var _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../core/index.js */ 70416156);
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../../core/index.js */ 21046606);
/* harmony import */ var _core_src_JsonPath_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../../core/src/JsonPath.js */ 72493624);
/* harmony import */ var _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./compiler/cli/lyte-base-compile.js */ 93957260);
/* harmony import */ var _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(_compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var _data_src_ValidationError_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../../data/src/ValidationError.js */ 53275862);
/* harmony import */ var _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../core/src/service.js */ 70549578);
/* harmony import */ var _core_src_Utils_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ../../core/src/Utils.js */ 97229384);
/* harmony import */ var _utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./utils/lyte-sanitizer.js */ 20426923);
/* harmony import */ var _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./utils/lyte-errors.js */ 59935577);
/* harmony import */ var _compatibility_compatibility_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./compatibility/compatibility.js */ 32483767);
/* harmony import */ var _compatibility_compatibility_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_compatibility_compatibility_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _core_src_IdleTaskScheduler_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../core/src/IdleTaskScheduler.js */ 76043102);




 //slicer store


//@Slicer.$bundlePurifierStart

//@Slicer.$bundlePurifierEnd



// debugger
// debugger 



// if(!window.__lyteConfig){
//     window.__lyteConfig = {
//         _definedBeforeBridging : [],
//         _alreadyDefinedBeforeBridging : [],
//         v4 : true,
//         _pendingV3Components : {},
//         _firstRegisteredComp : {}
//     }
// }
// window.__lyteConfig.v4 = true;
let ltCf = window.__lyteConfig;
if(!ltCf.customElementsDefine){
    ltCf.customElementsDefine = customElements.constructor.prototype.define; 
    Object.defineProperty(customElements.constructor.prototype,"define",{
        value : function(name, compClass, options, _lyteOptions){
            if(customElements.get(name)){
                ltCf._alreadyDefinedBeforeBridging.push({name : name, compClass : compClass, options : options, _lyteOptions : _lyteOptions, scope : this});
            }else{
                ltCf._definedBeforeBridging.push({name : name, compClass : compClass, options : options ,_lyteOptions : _lyteOptions, scope : this});
                ltCf.customElementsDefine.apply(this,arguments);
            }
        },
        enumerable: false
    })
}
/*convert to custom class*/
class ComponentRegistry extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__.Service {
    //@Slicer.$bundlePurifierEnd
    isComponentRegistry() {
        return true;
    }
    getDirectiveObj(){
        if(this._directive){
            return this._directive;
        }
        return false;
    }
    static getDirectiveIns(name){
        return this[name] ? this[name] : false;
    }
    // lookups(){
    //     return [{lazyScheduler:new IdleTaskScheduler({delayInit: true})}];
    // }
    constructor(opt){
        super(opt);
        var registryClass = this.constructor;
        if(!registryClass._registered){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC001", registryClass.name);
            //@Slicer.developmentEnd
            return;
        }
        // //@Slicer.afzalStart
        // console.log("aaffff")
        // //@Slicer.$afzalEnd
        /* registering component in app module - for router */
        let app = _LC.getNearestParentApp(this);
        app.$.modules.triggerEvent("add", "component", this);
        if(_LC.getDefaultRegistry() == registryClass){
            //set default registry instance and it can be overrided with app's getRegistryInstance.
            _LC.setDefaultRegistryIns(this)
        }
        registryClass._instanciated = true;
        
        this.registeredComponents = {};
        this.registeredCeComponents = {}
        this._registeredComponents = {};
        this._registeredCeComponents = {};
        this._defaultDirectives = [];
        this.registeredDirectives = [];
        this._registeredDirectivesClass = {};
        ComponentRegistry.registerAllComponent(registryClass._registeredComponentClass,this);
        ComponentRegistry.registerAllCeComponent(registryClass._registeredCeComponentClass,this);
        registryClass._instanceList.push(this);
        //@Slicer.$bundlePurifierStart
        this._ourSanitizerInstance_ = (this.getDefaultSanitizer && this.getDefaultSanitizer()) || ComponentRegistry._sanitizerInstance;
        //@Slicer.$bundlePurifierEnd
        // _LC.setAddedRegistries(this,registryClass.name);
        this.render = _LC.render;     
        // this.renderHTML = _LC.renderHTML;
        this.set = _LC.set;
        this.get = _LC.get;
        let currentAppOrAddon = _LC.getAppOrAddon(this);
        this.setDefaultDirectives(currentAppOrAddon);
        for(let dName in registryClass._directivesList){
            let dClass = registryClass._directivesList[dName];
            dClass.actualRegistration(dName,dClass,this);
        }
        _LC.updateDomApis(this);
        this.replaceWith = _LC.replaceWith;
        this.throwEvent = _LC.throwEvent;
        this.chI = _LC.chI;
        this.chromeBugFix = _LC.chromeBugFix;
        this.lyteComponentsDiv = _LC.lyteComponentsDiv;
        this.String = _LC.String;
        this.addAction = _LC.addAction;
        this.getLyteComponentsDiv = function(){
            return _LC.getComponentsDiv(_LC.lyteComponentsDiv,registryClass.name);
        }
        this._getLyteComponent = function(){
            return _LC;
        }
        this.__data = this.data ? this.data.apply(this) : {};
    }
    setDefaultDirectives(app){ 
        let defaultDirectives = ["directive","shadow","shadow-style","shadow-supported","turbo","turbo-supported","unbound","view","elemental","save","hide-tag","hide-tag-supported"];
        let self = this;
        defaultDirectives.forEach(function(item){
            let directiveIns;
            if(self.constructor[item]){
                app.scopedInstance(self.constructor[item],[self],function(ins){
                    directiveIns = ins;
                },[])
                self["_"+item] = directiveIns;
            }
            self._defaultDirectives.push(item);
        })
    }
    static register(options){
        if(options){
            if(options.app == true){
                if(!_LC.gotDefaultRegistryFrom){
                    _LC.setDefaultRegistry(this);
                    _LC.gotDefaultRegistryFrom = "cli";
                }
            }
            _LC.map.registry.set(this, options);
        }
        this.lazyScheduler = new _core_src_IdleTaskScheduler_js__WEBPACK_IMPORTED_MODULE_3__.IdleTaskScheduler({delayInit: true, perf: true});
        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.extendEventListeners)(this);
        
        this._directivesList = {};
        this._definedComponents = {};
        this._registeredComponentClass = Object.assign({},ComponentRegistry._defaultComponents);
        this._registeredCeComponentClass = Object.assign({},ComponentRegistry._defaultCeComponents);
        this._registered = true;
        this.prototype.registeredHelpers = Object.assign({},ComponentRegistry._defaultHelpers,this._beforeRegisteredHelpers ? this._beforeRegisteredHelpers : {});
        this.registeredHelpers = this.prototype.registeredHelpers;
        this.appendChild = _LC.appendChild;//because of ui comps
        this.insertAfter = _LC.insertAfter;
        this.insertBefore = _LC.insertBefore;
        this.replaceWith = _LC.replaceWith;
        this.shouldIgnoreDisconnect = _LC.shouldIgnoreDisconnect;
        this._beforeRegisteredHelpers = {};
        this._instanceList = [];
        var registryClassDef = this;
        ComponentRegistry._registeredRegistries.push({
            name : registryClassDef.name,
            class : registryClassDef
        });
        if((_compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().needDummyComponentsDiv)){
            _LC.setComponentsDiv(_LC.dummyLyteComponentsDiv,this.name);
        }else{
            _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default()._registryNameList.$push(this.name);
        }
        _LC.setComponentsDiv(_LC.lyteComponentsDiv,this.name);
        if(registryClassDef.idePlugins){
            registryClassDef.idePlugins.forEach(function(plugin){
                plugin(_LC)
            })
        }
        if(this.arrayOverride){
            _LC.arrayOverride();
        }
        let lazyDir = this._lazyRegisterDirectives;
        if(lazyDir){
            for(let key in lazyDir){
                this[key] = lazyDir[key];
                _LC.updateDirectivesInReg(ComponentRegistry, lazyDir[key], key);
            }
        }
        // if(registryClassDef.Compile.needDummyComponentsDiv){
        //     _LC.setComponentsDiv(_LC.dummyLyteComponentsDiv,this.name);
        // }
        /*convert to custom class*/
        this.Component = (function() {
            class newC extends ComponentRegistry._componentApis {
                constructor(){
                    super();
                }
                static register(compName, options){
                    compName = compName || _LC.String.dasherize(this.name);
                    this._registryClass = registryClassDef;
                    registryClassDef.registerComponent(compName, this);
                    if(options){
                        _LC.map.component.set(this, options);
                    }
                }
                static unregisterComponent(){
                    _LC.unregisterComponent.apply(this,arguments);
                }
            }newC.__lMod = "Component";newC._registryClass = registryClassDef;newC._render = function(){
                return _LC._render.apply(this,arguments);
            };
            return newC;
        })();
        this.registerDirective = function(name,directiveClass){
            var registry = this;
            if(registry._directivesList[name]){
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC002", "Directive", name, registry.name);
                //@Slicer.developmentEnd
                return;
            }
            registry._directivesList[name] = directiveClass;
            directiveClass._registryClass = registry;
            if(registry._instanciated){
                registry._instanceList.forEach(function(ins){
                    directiveClass.actualRegistration(name,directiveClass,ins);
                })
            }
        }
        /*convert to custom class*/
        this.CustomElements = (function() {
            class newCE extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__.Service {
                constructor(){
                    super();
                }
                static register(compName){
                    compName = compName || _LC.String.dasherize(this.name);
                    this._registryClass = registryClassDef;
                    registryClassDef.registerCeComponent(compName, this, this.options);
                }
            }newCE._registryClass = registryClassDef;
            return newCE;
        })();
        let pendingGroups = [{ list: Component._pendingComponents},{ list: Helper._pendingHelpers, useHelper: true},{ list: RawComponent._pendingComponents},{ list: _LC.directive._pendingDirectives}];
        pendingGroups.forEach(({ list, useHelper }) => {
            for (let i = 0; i < list.length; i++) {
                let obj = list[i];
                if (obj.options.refHash === options.hash) {
                    if (useHelper) {
                        obj.compClass.register(obj.compName, obj.helper, obj.options);
                    } else {
                        obj.compClass.register(obj.compName, obj.options);
                    }
                    list.splice(i, 1);
                    break; // stop after first match
                }
            }
        });
    }
    static getHash(){
        return _LC.map.registry.get(this).hash;
    }
    static getRefHash(){
        return _LC.map.registry.get(this).refHash;
    }
    static generateHash(){
        return this.getHash() + "_Component_" + _LC.counter++;
    }
    static registerCeComponent(name,CompClass, options){
        var registry = this;
        if(registry._registeredCeComponentClass[name]){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC002", "Raw Component", name, registry.name);
            //@Slicer.developmentEnd
            return;
        }
        CompClass.activeInstances = 0;
        registry._registeredCeComponentClass[name] = CompClass;//af move below
        CompClass._compName = name;
        CompClass._options = options;
        if(registry._instanciated){
            registry._instanceList.forEach(function(ins){
                registry.registerAllCeComponent({[name] : CompClass},ins);
            })
        }
    }
    static registerComponent(name,CompClass){
        var registry = this;
        if(registry._registeredComponentClass[name] && registry._reRegisteredComponents.indexOf(name) == -1){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC002", "Component", name, registry.name);
            //@Slicer.developmentEnd
            return;
        }
        CompClass.activeInstances = 0;
        registry._registeredComponentClass[name] = CompClass;//af move below
        CompClass._compName = name;
        if(registry._instanciated){
            registry._instanceList.forEach(function(ins){
                registry.registerAllComponent({[name] : CompClass},ins);
            })
        }
    }
    static unregisterComponent(){
        _LC.unregisterComponent.apply(this,arguments);
    }
    static registerHelper(name,helper){
        //@Slicer.developmentStart
        if(ComponentRegistry._defaultHelpers[name]){    
            console.warn("Deprecated : Default helpers of Lyte can't be overrided - " + name + "");
        }else if(this._registered && this.prototype.registeredHelpers[name] || this._beforeRegisteredHelpers && this._beforeRegisteredHelpers[name]){
            console.warn("Deprecated : Helper named - " + name + " is already registered");
        }
        //@Slicer.developmentEnd
        if(this == ComponentRegistry){
            //register helpe in global
            //@Slicer.developmentStart
            if(ComponentRegistry._defaultHelpers[name]){    
                console.warn("Deprecated : Default helpers of Lyte can't be overrided - " + name + "");
            }else{
            //@Slicer.developmentEnd
                ComponentRegistry._defaultHelpers[name] = helper;
                //update in all reigstries
                ComponentRegistry._registeredRegistries.forEach(function(obj){
                    let _registryClass = obj.class;
                    if(_registryClass._registered){
                        _registryClass.prototype.registeredHelpers[name] = helper;
                    }else if(_registryClass._beforeRegisteredHelpers){
                        _registryClass._beforeRegisteredHelpers[name] = helper;
                    }else{
                        _registryClass._beforeRegisteredHelpers = {[name] : helper};
                    }
                })
            //@Slicer.developmentStart
            }
            //@Slicer.developmentEnd
        }
        if(this._registered){
            this.prototype.registeredHelpers[name] = helper;
        }else if(this._beforeRegisteredHelpers){
            this._beforeRegisteredHelpers[name] = helper;
        }else{
            this._beforeRegisteredHelpers = {[name] : helper};
        }
    }
    static registerCustomPropHandler(){
        _LC.registerCustomPropHandler.apply(this,arguments);
    }
}
ComponentRegistry.__lMod = "ComponentRegistry";
ComponentRegistry._registeredRegistries = [];
// static _definedComponents = {};
ComponentRegistry._definedCeComponents = {};
ComponentRegistry._registeredCommonClass = {};
ComponentRegistry._registeredCommonCeClass = {};
ComponentRegistry.customPropHandlers = [];
ComponentRegistry.customPropRegex = "";
ComponentRegistry._defaultHelpers = {};
ComponentRegistry._defaultComponents = {};
ComponentRegistry._defaultCeComponents = {};
ComponentRegistry.globalComponents = ["lyte-event-listener", "import-shadow-style"];
ComponentRegistry._unRegisteredComponents = [];
ComponentRegistry._reRegisteredComponents = [];
// static _directivesList = {};
ComponentRegistry._registeredDirectivesClass = {};
ComponentRegistry._lazyRegisterDirectives = {};
ComponentRegistry._preLoadedDirectives = {};
ComponentRegistry.registerPerf = {};
//@Slicer.$bundlePurifierStart
ComponentRegistry._sanitizerInstance = new _utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_6__.Sanitizer({ 
    "GLOBAL_TAGS": [], 
    "GLOBAL_ATTRIBUTES": [],
    "FORBID_TAGS":[],
    "FORBID_ATTR":[], 
    REUSE_CONFIG : true
});
ComponentRegistry._observedAttributes = [];
/*convert to custom class*/
ComponentRegistry._componentApis = (function() {
    class apis extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__.Service {
        constructor(){
            super();
            this.$ = {
                validate : function(){
                    return _LC.validate.apply(this, arguments);
                },
                getError : function(path){
                    return _LC.getErrorStructure(this.ins,path)
                },
                _ins : this
            }
        }
        set(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd 
                return _LC.componentSet.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err)
            }
            //@Slicer.catchAllErrorsEnd 
        }
        get(){
            return _LC.componentGet.apply(this, arguments);
        }
        throwEvent(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd 
                return _LC.throwEvent.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err)
            }
            //@Slicer.catchAllErrorsEnd
        }
        executeMethod(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd
                return _LC.executeMethod.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err);
            }
            //@Slicer.catchAllErrorsEnd
        }
        getData(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd
                return _LC.componentGetData.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err);
            }
            //@Slicer.catchAllErrorsEnd
        }
        setData(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd 
                return _LC.componentSetData.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err);
            }
            //@Slicer.catchAllErrorsEnd
        }
        getMethods(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd
                return _LC.componentGetMethods.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err);
            }
            //@Slicer.catchAllErrorsEnd
        }
        setMethods(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd
                return _LC.componentSetMethods.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err);
            }
            //@Slicer.catchAllErrorsEnd
        }
        hasAction(){
            //@Slicer.catchAllErrorsStart
            try{
            //@Slicer.catchAllErrorsEnd 
                return _LC.componentHasAction.apply(this, arguments);
            //@Slicer.catchAllErrorsStart
            }catch(err){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error(this,err);
            }
            //@Slicer.catchAllErrorsEnd
        }
        getActions(){
            return _LC.componentGetActions.apply(this, arguments);
        }
        getObservers(){
            return _LC.componentGetObservers.apply(this, arguments);
        }
        getApp(){
            return _LC.getApp.apply(this, arguments);
        }
        getAppOrAddon(){
            return _LC.getAppOrAddon.apply(this, arguments);
        }
    }
    return apis;
})();
ComponentRegistry.registerAllComponent = function(compClassList, registryInstance) {
    for(var compName in compClassList){
        let compClass = compClassList[compName];
        // if (registryInstance.constructor._reRegisteredComponents.indexOf(compName) == -1 && registryInstance.registeredComponents[compName]) {
        //     _Lyte.error("Component " + componentName + " already registered");
        //     return;
        // }
    if (registryInstance.constructor._definedComponents[compName] && registryInstance.constructor._reRegisteredComponents.indexOf(compName) == -1 && !registryInstance.constructor._unRegisteredComponents[compName]) {
            //same component class for same reigstry which has multiple registry instance
            registryInstance.registeredComponents[compName] = true;
            continue;
        }
        compClass._compName = compName;
        compClass._data = compClass.prototype.data;
        compClass._actions = compClass.actions();
        compClass._methods = compClass.methods();
        compClass.__observers = _LC.executeObservers(compClass);
        compClass.addRegistries = compClass.prototype.addRegistries;
        if(compClass._dynamicNodes && compClass._dynamicNodes.length && compClass._dynamicNodes[compClass._dynamicNodes.length-1].type == "dc"){
            compClass.dc = compClass._dynamicNodes.$pop();
        }
        compClass._callBacks = {};
        compClass._observers = [];
        _LC.updateCustomCrmComponent(compClass);
        compClass._observedMethodAttributes = compClass._observedMethodAttributes || {};
        _LC.registerComponent(compName, compClass,registryInstance);
        registryInstance.registeredComponents[compName] = true;
        registryInstance._registeredComponents[compName] = compClass;
    }
};
ComponentRegistry.registerAllCeComponent = function(compClassList, registryInstance) {
    for(var compName in compClassList){
        let compClass = compClassList[compName];
        if (registryInstance.constructor._definedCeComponents[compName]) {
            //same component class for same reigstry which has multiple registry instance
            registryInstance.registeredComponents[compName] = true;
            continue;
        }
        compClass._compName = compName;
        compClass.addRegistries = compClass.prototype.addRegistries;
        _LC.registerCeComponent(compName, compClass,registryInstance);
        registryInstance.registeredCeComponents[compName] = true;
        registryInstance._registeredCeComponents[compName] = compClass;
    }
};
/*convert to custom class*/
ComponentRegistry.Component = (function() {
    class newC extends ComponentRegistry._componentApis {
        constructor(){
            super();
        }
        static register(compName){
            compName = compName || _LC.String.dasherize(this.name);
            if(ComponentRegistry.globalComponents.indexOf(compName) != -1){
                let compClass = this;
                compClass._registryClass = ComponentRegistry;
                ComponentRegistry._defaultComponents[compName] = compClass;
                compClass._compName = compName;
                compClass.activeInstances = 0;   
                ComponentRegistry._registeredRegistries.forEach(function(reg){
                    let registry = reg.class;
                    registry._registeredComponentClass[compName] = compClass;
                    if(registry._instanciated){
                        registry._instanceList.forEach(function(ins){
                            registry.registerAllComponent({[compName] : compClass},ins);
                        })
                    }
                })
            }
            //@Slicer.developmentStart
            else{
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC003" , compName);
            }
            //@Slicer.developmentEnd
        }
    }newC.__lMod = "Component";newC._registryClass = ComponentRegistry;
    return newC;
})();
ComponentRegistry.addFakeDirective = function(baseClass,name){
    this._lazyRegisterDirectives[name] = baseClass;
    this[name] = baseClass;
    _LC.updateDirectivesInReg(this,baseClass,name);
};
/*convert to custom class*/
class Component extends ComponentRegistry._componentApis {
    constructor(){
        super();
    }
    // static _registryClass = registryClassDef;
    static register(compName, options){
        var _registryClass = _LC.getRegistryClass(options);
        if(!_registryClass){
            Component._pendingComponents.push({
                compClass : this,
                compName : compName,
                options : options
            });
            return;
            // //@Slicer.developmentStart
            // RegistryError.error("LC004","Component",compName)
            // //@Slicer.developmentEnd
            // return
        }
        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.extendEventListeners)(this)
        _LC.map.component.set(this, options);
        this._hash = options.hash;
        this._refHash = options.refHash
        compName = compName || _LC.String.dasherize(this.name);
        this._registryClass = _registryClass;
        _registryClass.registerComponent(compName, this);
    }
    static unregisterComponent(){
        _LC.unregisterComponent.apply(this,arguments);
    }
}
Component.__lMod = "Component";
Component._pendingComponents = [];
Component._render = function(){
    return _LC._render.apply(this,arguments);
};
/*convert to custom class*/
class RawComponent extends _core_src_service_js__WEBPACK_IMPORTED_MODULE_1__.Service {
    constructor(){
        super();
    }
    // static _registryClass = registryClassDef;
    static register(compName, options){
        var _registryClass = _LC.getRegistryClass(options);
        if(!_registryClass){
            RawComponent._pendingComponents.push({
                compClass : this,
                compName : compName,
                options : options
            });
            return;
            // //@Slicer.developmentStart
            // RegistryError.error("LC004","Raw Component",compName)
            // //@Slicer.developmentEnd
            // return;
        }
        this._refHash = options.refHash
        compName = compName || _LC.String.dasherize(this.name);
        this._registryClass = _registryClass;
        _registryClass.registerCeComponent(compName, this, this.options);
    }
}
RawComponent.__lMod = "RawComponent";
RawComponent._pendingComponents = [];
class Helper {
    static register(name, helper, options){
        var _registryClass = _LC.getRegistryClass(options);
        if(!_registryClass){
            Helper._pendingHelpers.push({
                compClass : this,
                compName : name,
                helper : helper,
                options : options
            });
            return;
            // //@Slicer.developmentStart
            // RegistryError.error("LC004","Helper",name)
            // //@Slicer.developmentEnd
            // return;
        }
        _LC.map.helper.set(this, options);
        //@Slicer.developmentStart
        if(ComponentRegistry._defaultHelpers[name]){    
            console.warn("Deprecated : Default helpers of Lyte can't be overrided - " + name + "");
        }else if(_registryClass._registered && _registryClass.prototype.registeredHelpers[name] || _registryClass._beforeRegisteredHelpers && _registryClass._beforeRegisteredHelpers[name]){
            console.warn("Deprecated : Helper named - " + name + " is already registered");
        }
        //@Slicer.developmentEnd
        if(_registryClass._registered){
            _registryClass.prototype.registeredHelpers[name] = helper;
        }else if(_registryClass._beforeRegisteredHelpers){
            _registryClass._beforeRegisteredHelpers[name] = helper;
        }else{
            _registryClass._beforeRegisteredHelpers = {[name] : helper};
        }
    }
}
Helper.__lMod = "Helper";
Helper._pendingHelpers = [];
function arrayUtils(){
    return _LC.aF.apply(_LC, arguments);
}
function objectUtils(){
    return _LC.oF.apply(_LC, arguments);
}
function set(){
    return _LC.set.apply(_LC.String, arguments);
}
ComponentRegistry._registeredComponentClass = {};
ComponentRegistry._registeredCeComponentClass = {};
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$ = {
    assetsDiv : document.createElement("div"),
    shadowDiv : document.createElement("div")
}
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv.setAttribute("id", "lyteAssetsDiv");
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv.setAttribute("id", "lyteShadowDiv");

// ComponentRegistry.Compile = Compile;
let orgArrFns = ["push", "pop", "splice", "shift", "unshift", "concat"];
for(let i=0; i<orgArrFns.length; i++){
    let arrFnName = orgArrFns[i];
    // Array.prototype["$"+orgArrFns[i]] = Array.prototype[orgArrFns[i]]
    Object.defineProperty(Array.prototype, "$"+arrFnName, {
        enumerable: false, 
        value : Array.prototype[arrFnName]
    })
}
var _LC = {
    "counter" : 0,
    "_definedComponents" : {},
    "registeredElementals" : {},
    validateRenderData : function(data, _lyteOptions){
        if(_lyteOptions && _lyteOptions.setInnerHTML){
            return true
        }
        //@Slicer.developmentStart
        if( !(data === undefined || data === null || (typeof data == "object" && !Array.isArray(data)) ) ){
            _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Invalid data passed to render the component from route.");
            // return false;
        }
        //@Slicer.developmentEnd
        return true;
    },
    _render : function(object){
        let compClass = this;
        ltCf.fromV4Render = true;
        let oldLyteV4 = ltCf.lyteV4;
        let outlet = object.outlet;
        let registryInstance = object.registryInstance;
        let data = object.data;
        let options = object.options;
        if(compClass._registered){
            if(_LC.validateRenderData(data)){
                if(data && data.lyteV4){
                    ltCf.lyteV4 = true;
                }
                if(registryInstance){
                    if(!_LC.isValidReg(registryInstance)){
                        //@Slicer.developmentStart
                        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC006");
                        //@Slicer.developmentEnd
                        ltCf.fromV4Render = false;
                        ltCf.lyteV4 = oldLyteV4
                        return;
                    }
                    options = options || {};
                    if(object._route){
                        options._route = object._route;
                    }
                    let ele = registryInstance.render(compClass,data,outlet,options);
                    ltCf.fromV4Render = false;
                    ltCf.lyteV4 = oldLyteV4
                    return ele;
                }else{
                    let registryClass = compClass._registryClass;
                    if(registryClass._instanciated && registryClass._instanceList.length){
                        // if(registryClass._instanceList.length > 1){
                        //     Lyte.error("Registry not instanced for ",compClass.name);
                        // }else{
                            options = options || {};
                            if(object._route){
                                options._route = object._route;
                            }
                            let ele = registryClass._instanceList[0].render(compClass,data,outlet,options);
                            ltCf.fromV4Render = false;
                            ltCf.lyteV4 = oldLyteV4
                            return ele;
                        // }
                    }
                    //@Slicer.developmentStart
                    else{
                        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC007");
                    }
                    //@Slicer.developmentEnd
                }
            }
        }
        //@Slicer.developmentStart
        else{
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC008");
        }
        //@Slicer.developmentEnd
    },
    toBeRegistered : [],
    getRegistryClass : function(options){
        let registryClass;
        ComponentRegistry._registeredRegistries.forEach(function(obj){
            let reg = obj.class;
            if(_LC.map.registry.get(reg).hash == options.refHash){
                registryClass = reg;
                return
            }

        })
        return registryClass;
    },
    "setCurrentRegistryIns" : function(currentReg){
        _LC._currentRegistryInstance = currentReg;
    },
    "setCurrentRegistry" : function(reg){
        _LC._currentRegistry = reg;
    },
    "getCurrentRegistryIns" : function(){
        return _LC._currentRegistryInstance;
    },
    "getCurrentRegistry" : function(){
        return _LC._currentRegistry;
    },
    "getDecidedRegistry" : function(){
        return _LC._decidedRegistry;
    },
    "setDecidedRegistry" : function(reg){
        _LC._decidedRegistry = reg;
    },
    "setDefaultRegistry" : function(reg){
        _LC._defaultRegistry = reg;
    },
    "setDefaultRegistryIns" : function(regIns){
        _LC._defaultRegistryIns = regIns;
    },
    "getDefaultRegistryIns" : function(){
        return _LC._defaultRegistryIns;
    },
    "getDefaultRegistry" : function(){
        return _LC._defaultRegistry;
    },
    "setAddedRegistries" : function(scope,scopeName){
        let addedRegistries = scope.addRegistries ? scope.addRegistries.apply(scope) : [];
        if(addedRegistries){
            let validReg = _LC.validateArrRegistry(addedRegistries,scopeName);
            scope.addedRegistries = validReg ? addedRegistries : [];
        }else{
            scope.addedRegistries = [];
        }
    },
    "validateArrRegistry" : function(arr , moduleName){
        for(let i=0; i<arr.length; i++){
            if(!_LC.validateRegistry(arr[i],moduleName)){
                return false;
            }
        }
        return true;
    },
    "isValidReg" : function(reg){
        return reg && reg.isComponentRegistry && reg.isComponentRegistry()
    },
    "validateRegistry" : function(reg,moduleName){
        if(!_LC.isValidReg(reg)){
            let name =  reg ? reg.constructor ? reg.constructor.name : reg : reg ;
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC005", name, moduleName);
            //@Slicer.developmentEnd
            return false;
        }
        return true;
    },
    "getApp" : function(node){
        if(node){
            return node.$app ? node.$app : node.$addon.$app;
        }else{
            return this.$app ? this.$app : this.$addon.$app;
        }
    },
    "getNearestParentApp" : function(node){
        let app;
        if(!node){
            node = this;
        }
        while(node){
            if(node.$app){
                app = node.$app;
                break;
            }
            else if(node.$addon){
                if(node.$addon.$app){
                    app = node.$addon.$app
                    break;
                }
            }
            node = node.$addon;
        }
        return app;
    },
    "getAppOrAddon" : function(node){
        // node can be component or registry
        node = node ? node : this;
        if(node){
            return node.$app ? node.$app : node.$addon;
        }
    },
    "migratedv2" : function(app){
        if((app.constructor._migration && app.constructor._migration.version == "2") || (app.constructor.__slyteApp)){
            return true;
        }
    },
    "updateDirectivesInReg" : function(self,dClass,name){
        self._registeredRegistries.forEach(function(regObj){
            let regClass = regObj.class;
            if(regClass._instanciated){
                regClass._instanceList.forEach(function(regIns){
                    let directiveIns;
                    let app = _LC.getAppOrAddon(regIns);
                    app.scopedInstance(dClass,[regIns],function(ins){
                        directiveIns = ins;
                    },[])
                    regIns["_"+name] = directiveIns;
                    regIns._defaultDirectives.push(name);
                    directiveIns.didConnect && directiveIns.didConnect.apply(directiveIns, [regIns])
                })
            }
        })
    },
    "updateDomApis" : function(regIns){
        regIns.appendChild = _LC.appendChild;
        regIns.insertAfter = _LC.insertAfter;
        regIns.insertBefore = _LC.insertBefore
    },
    "getMyScope" : function(node){
        if(node.$app){
            return node.$app;
        }else if(node.$addon){
            return node.$addon;
        }
    },
    "setComponentsDiv" : function(template,name){
        let div = createElement("div");
        div.setAttribute("id",name);
        template.appendChild(div);
        return div;
    },
    "getComponentsDiv" : function(template,name){
        return template.querySelector("#"+name);
    },
    "validateRegistryClass" : function(name, returnClass){
        let regFound,regClass;
        ComponentRegistry._registeredRegistries.forEach(function(regObj){
            if(regObj.name == name){
                regFound = true;
                regClass = regObj.class;
            }
        })
        return returnClass ? regClass : regFound;
    },
    "getRegFromAttr" : function(self, localName){
        var initProp = self._initProperties;
        if((initProp && initProp.lyteRegistry) || (self.hasAttribute("lyte-registry"))){
            var reg = (initProp && initProp.lyteRegistry) || self.getAttribute("lyte-registry");
            if(typeof reg == "function"){
                if(_LC.validateRegistryClass(reg.name)){
                    if(reg._instanceList.length){
                        reg = reg._instanceList[0];
                    }else{
                        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC012", reg.name ,localName);    
                    }
                }else{
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC011", reg.name, localName);
                    return;
                }
            }else if(typeof reg == "string"){
                let regClass = _LC.validateRegistryClass(reg,true)
                if(regClass){
                    if(regClass._instanceList.length){
                        reg = regClass._instanceList[0];
                    }else{
                        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC014", reg, localName);    
                    }
                }else{
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC013",reg, localName);
                    return;
                }
            }
            if(localName == "dynamicComponent" && !_LC.isValidReg(reg)){
                let name =  reg ? reg.constructor ? reg.constructor.name : reg : reg ;
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC015", name, localName);
                //@Slicer.developmentEnd
                return false;
            }
            return reg;
        }
    },
    "getCompRegistry" : function(obj,self){
        var reg = this.getRegFromAttr(self, self.localName);
        if(reg){
            var registryMap = new WeakMap();
            if(this.checkInRegistry(reg,obj,registryMap)){
                return;
            }
        }
        var currentReg = _LC.getCurrentRegistryIns();
        if(currentReg){
            this.traverseRegistries([currentReg],obj,self);
        }else{
            let defRegIns = _LC.getDefaultRegistryIns();
            if(defRegIns){
                this.traverseRegistries([defRegIns],obj,self);    
            }else{
                let defaultReg = _LC.getDefaultRegistry();
                if(defaultReg){
                    let defaultRegInsArr = defaultReg._instanceList;
                    this.traverseRegistries(defaultRegInsArr,obj,self);
                }
            }
        }
    },
    "verifyDetails" : function(obj){
        var lIns = obj.lIns ,compClass = obj.compClass ,regIns = obj.regIns;
        if(!lIns || !compClass || !regIns){
            return false;
        }
        return true;
    },
    "isValidateLyteReg" : function(reg,registryMap){
        if(!_LC.isValidReg(reg)){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC006", reg.constructor.name);
            //@Slicer.developmentEnd
            return false;
        }else if(registryMap.get(reg)){
            //@Slicer.developmentStart
            // RegistryError.error("LC007", reg.constructor.name);
            //@Slicer.developmentEnd
            return false;
        }
        registryMap.set(reg,reg);
        return true;
    },
    "checkInRegistry" : function(registry,obj,registryMap){
        if(this.isValidateLyteReg(registry,registryMap)){
            obj.compClass = obj.type == "component" ? registry.constructor._registeredComponentClass[obj.compName] : obj.type == "helper" ? registry.registeredHelpers[obj.compName] : registry.constructor._registeredCeComponentClass[obj.compName];
            if(obj.compClass){
                obj.regIns = registry;
                obj.lIns = registry.$app ? registry.$app : registry.$addon;
                return true;
            }else{
                if(registry.addedRegistries){
                    let addedReg = registry.addedRegistries;
                    for(let i=0; i<addedReg.length; i++){
                        if(this.checkInRegistry(addedReg[i],obj,registryMap)){
                            return true;
                        }
                    }
                }
            }
        }
    },
    "iterateRegistries" : function(baseRegistryArr,obj,registryMap){
        for(let i=0; i<baseRegistryArr.length; i++){
            if(this.checkInRegistry(baseRegistryArr[i],obj,registryMap)){
                return true;
            }
        }
    },
    "traverseRegistries" : function(baseRegistryArr,obj,self){
        let registryMap = new WeakMap();
        if(this.iterateRegistries(baseRegistryArr,obj,registryMap)){
            return
        }
        let parentComp = self._callee || self.getCallee && self.getCallee(self.parentNode);
        if(!parentComp){
            return;
        }
        while(parentComp){
            let parentCompReg = parentComp.$registry;
            registryMap = new WeakMap();
            if(this.checkInRegistry(parentCompReg,obj,registryMap)){
                return;
            }
            if(parentComp.component.addedRegistries){
                let pcAddedReg = parentComp.component.addedRegistries;
                for(let i=0; i<pcAddedReg.length; i++){
                    registryMap = new WeakMap();
                    if(this.checkInRegistry(pcAddedReg[i],obj,registryMap)){
                        return;
                    }
                }
            }
            parentComp = parentComp._callee || parentComp.getCallee && parentComp.getCallee(parentComp.parentNode);
        }
        
    },
    "getCommonClass" : function(componentName){
        var customCrmComponent = ComponentRegistry._registeredCommonClass[componentName];
        return customCrmComponent;
    },
    "getCommonCustomClass" : function(componentName){
        var customCrmComponent = ComponentRegistry._registeredCommonClass[componentName];
        return customCrmComponent;
    },

    "registerCeComponent" : function(componentName, componentClass, registryInstance){
        let customLyteCustomComponent;
        let registry = componentClass._registryClass;
        let registryName = registry.name;
        if(registryInstance.registeredCeComponents[componentName] && ComponentRegistry._registeredCommonCeClass[componentName]._reg[registryName]) {
            customLyteCustomComponent = this.getCommonCustomClass(componentName,registryName);
        }else{
            customLyteCustomComponent = class extends LyteCustomElement {
                constructor(){
                    super()
                }
            }
            customLyteCustomComponent._observedAttributes = componentClass.observedAttributes || [];
            customLyteCustomComponent._deepWatchProperties = componentClass._deepWatchProperties || {};
            registryInstance.registeredCeComponents[componentName] = true;
            if (document.readyState === "complete" || document.readyState === "interactive") {     
                customElements.define(componentName, customLyteCustomComponent, undefined, {v4raw : true});
            }
            else{
                _LC.toBeRegistered.$push({name:componentName, def: customLyteCustomComponent, _lyteOptions : {v4raw : true} });//af check
            }
            ComponentRegistry._definedCeComponents[componentName] = customLyteCustomComponent;
            componentClass._instanciated = true;
            componentClass._registered = true;
            ComponentRegistry._registeredCommonCeClass[componentName] = customLyteCustomComponent;
            registry._registeredCeComponentClass[componentName] = componentClass;
        } 
    },
    "validate" : function() {
        var compInstance = this._ins;
        var data = compInstance.data;
        for(var key in data) {
            _LC.handleValidation(data, key, data[key], compInstance);
        }
    },
    "registerComponent" : function(componentName,componentClass,registryInstance) {
        let customCrmComponent;
        let alreadyRegistered;
        let registry = componentClass._registryClass;
        let registryName = registry.name;
        if(registryInstance.registeredComponents[componentName] && ComponentRegistry._registeredCommonClass[componentName]._reg[registryName]) {
            //think this place wont be called need to remove this check
            customCrmComponent = this.getCommonClass(componentName, registryName);
        }
        else if(ComponentRegistry._unRegisteredComponents[componentName]){
            customCrmComponent = this.getCommonClass(componentName);
            delete ComponentRegistry._unRegisteredComponents[componentName];
            alreadyRegistered = true;
        }
         else {
            customCrmComponent = class extends customElementPrototype {
                constructor(){
                    super()
                }
            }
            registryInstance.registeredComponents[componentName] = true;
            customCrmComponent._reg = {[registryName] : {}};
            customCrmComponent._depthTemp = document.createElement("template");//af check
            customCrmComponent.prototype.throwAction = this.throwAction;
            customCrmComponent._compName = componentName;
            //@Slicer.$hideStart
            ComponentRegistry.hide && ComponentRegistry.hide.define.remove(customCrmComponent);
            ComponentRegistry.hide && ComponentRegistry.hide.define.getContent(customCrmComponent);
            //@Slicer.$hideEnd
            Object.defineProperty(customCrmComponent.prototype, "setData", {
                configurable : true, 
                writable : true,
                value : function(arg0, arg1 ,options) {
                    return this.component.setData(arg0, arg1, options);
                }
            });

            Object.defineProperty(customCrmComponent.prototype, "getData", {
                configurable : true, 
                writable : true,
                value : function(arg0) {
                    return this.component.getData(arg0);
                }
            });

            Object.defineProperty(customCrmComponent.prototype, "setMethods", {
                configurable : true, 
                writable : true, 
                value : function(arg0, arg1) {
                    return this.component.setMethods(arg0, arg1);
                }
            })
            customCrmComponent.prototype.get = this.nodeGet;
            customCrmComponent.prototype.set = this.nodeSet;
        }
        if(componentClass.ssr){
            customCrmComponent._ssr = componentClass.ssr();
            if(customCrmComponent._ssr.config){
                customCrmComponent._config = customCrmComponent._ssr.config;
            }
        }

        customCrmComponent.componentClass = componentClass;
        var Component;
        if(ComponentRegistry._registeredCommonClass[componentName]){
            Component = ComponentRegistry._registeredCommonClass[componentName].component;
            Component.list.push(componentClass);
        }else{
            Component = {list : [componentClass]};
        }
        componentClass._v4 = true;
        customCrmComponent.component = Component;
        componentClass._depthTemp = document.createElement("template");
        customCrmComponent._observedAttributes = componentClass._observedAttributes || [];
        customCrmComponent._deepWatchProperties = componentClass._deepWatchProperties || {};
        // if(!componentClass._registered){
        var currRegClass = registryInstance.constructor;
        if(currRegClass && !currRegClass.lazyRegister){
            customCrmComponent._registerComponent(componentName,customCrmComponent,componentClass,registry,registryInstance);
        }
        else{
            if(currRegClass.lazyScheduler.tasks.get(componentName, registryName)){
                currRegClass.lazyScheduler.deleteTask(componentName, registryName);
            }
            currRegClass.lazyScheduler.enqueueTask(customCrmComponent._registerComponentFn(componentName,customCrmComponent,componentClass,registry,registryInstance), [], componentName, registryName);
        }
        // }
        customCrmComponent._bindsIds = [];
        if(!_LC._definedComponents[componentName]) {
            if (document.readyState === "complete" || document.readyState === "interactive"  || window.preLoadLyteComponents) {     
                customElements.define(componentName, customCrmComponent, undefined, {v4 : true});
            }
            else{
                _LC.toBeRegistered.$push({name:componentName, def: customCrmComponent, _lyteOptions : {v4 : true}});
            }
        }
        _LC._definedComponents[componentName] = customCrmComponent;
        componentClass._instanciated = true;
        componentClass._registered = true;
        ComponentRegistry._registeredCommonClass[componentName] = customCrmComponent;
        registry._registeredComponentClass[componentName] = componentClass;
        if(alreadyRegistered) {
            var pendingComps = customCrmComponent._pendingComponents || [];
            pendingComps.forEach(function(item) {
                delete item.__lyteIgnore 
                item.actualConstructor();
                var arr = Array.from(item.attributes);
                arr.forEach(function(attr) {
                    if(item.constructor.observedAttributes.indexOf(attr.name) != -1) {
                        item.attributeChangedCallback(attr.name, null, attr.value);
                    }
                    
                });
                item.connectedCallback();
            });
            customCrmComponent._pendingComponents = [];
        }
        return customCrmComponent;
    },
    // "_lyteInstance": Lyte,
    "shadow" : {
        "getHostElement" : function(node){
            if(node){
                let host = node.host;
                if(host && host.tagName){
                    return host;
                }
            }
        },
        "stringToStyle" : function(str){
            var wrapper = document.createElement("wrapper");
            wrapper.innerHTML = str;
            return wrapper.firstChild;
        },
    },
    "deep" : {
        "set" : function(properties, deepTriggerObj, childTriggerObj){
            if(deepTriggerObj){
                properties["*"] = deepTriggerObj;
            }
            if(childTriggerObj){
                properties["{}"] = childTriggerObj;
            }
        },
        "delete" : function(comp, node, dynArr, pathName, deepNodeArr){
            if(node._deepValues){
                dynArr.forEach(function(dynVal,indd){ //do the same for helpernodes too
                    deepNodeArr.$push(undefined);
                    if(dynVal != pathName && (node._deepValues.indexOf(dynVal) != -1)){
                        var prop = comp.getProperty(dynVal);
                        let nodes = prop._dynamicNodes;
                        let index = nodes.indexOf(node);
                        deepNodeArr.$push(index);
                        if(nodes && nodes.length){
                            nodes.$splice(index, 1);
                        }
                    }
                });
            }
        },
        "add" : function(comp, node, dynArr, pathName, multipleDeepNodeArr){
            if(node._deepValues){
                dynArr.forEach(function(dynVal,indd){ //do the same for helpernodes too
                    if(dynVal != pathName && (node._deepValues.indexOf(dynVal) != -1)){
                        var prop = comp.getProperty(dynVal);
                        makeArray(prop, "_dynamicNodes");
                        if(prop._dynamicNodes.indexOf(node) == -1){
                            let index = multipleDeepNodeArr[indd];
                            prop._dynamicNodes.$splice(index,0,node);
                        }
                    }
                });
            }
        },
        "getAllKeys" : function(loopDeep ,actualData, arr, prevPath, sourceReferences, deepValues){
            if(typeof actualData == "object"){
                for(let key in actualData){
                    let actData = actualData[key];
                    if(sourceReferences.indexOf(actData) == -1){
                        let newPath = prevPath + "." + key;
                        arr.push(newPath);
                        deepValues.push(newPath);
                        if(typeof actData == "object"){
                            sourceReferences.push(actData);
                        }
                        if(loopDeep){
                            this.getAllKeys(loopDeep, actData, arr, newPath, sourceReferences, deepValues);
                        }
                    }
                }
            }
        },
        "replace" : function(val){
            val = val.replace('.{}','____lyteinternalobj____');
            val = val.replace('.*','____lyteinternalstar____');
            return val;
        },
        "replaceBack" : function(val){
            val = val.replace('____lyteinternalobj____','.{}');
            val = val.replace('____lyteinternalstar____','.*');
            return val;
        },
        "data" : function(argStr, observer, scpData){
            let result;
            let item = argStr.slice(0,-2);
            if(observer && observer.path.startsWith(item)){
                result = observer;
                observer.item = item;
                observer.data = _LC.get(scpData,item);
                observer.type = "deepChange";
            }else{
                let dta = _LC.get(scpData,item);
                result = {
                    newValue : dta,
                    data : dta,
                    oldValue : null,
                    type : "deepChange",
                    item : item
                }
            }
            return result;
        }
    },
    "directive" : {
        "_pendingDirectives" : [],
        
        setTagDirectives : function(compClass,comp){
            if(compClass._lyteOptions && compClass._lyteOptions.attributes && compClass._lyteOptions.attributes.length){
                let attr = compClass._lyteOptions.attributes;
                for(let i=0; i<attr.length; i++){
                    if(typeof attr[i] == "object"){
                        if(attr[i].name.startsWith("@")){
                            comp._tagDirectives.push(attr[i]);
                            compClass._lyteOptions.attributes.splice(i,1);
                            i--;
                        }
                    }else{
                        if(attr[i].startsWith("@")){
                            comp._tagDirectives.push(attr[i]);
                            compClass._lyteOptions.attributes.splice(i,1);
                            i--;
                        }
                    }
                }
            }
        },
        checkSameAttrName : function(arr,directiveName,arg){
            let duplicateFound;
            arr.forEach(function(attr){
                if(attr.hookName == directiveName && arg){
                    attr.stringValue = arg;
                }
            })
            return duplicateFound;
        },
        isDirectiveNode : function(node){
            if(node.hookNode || (node.nodeName && node.nodeName.startsWith("@")) || node._isDirectiveNode){
                return true;
            }
            return false;
        },
        getActualTransitionArg : function(attr,node){
            if(attr.hasOwnProperty("dynamicValue") || attr.hasOwnProperty("helperInfo")){
                if(node.hasOwnProperty("_transitionArgs")){
                    if(node._transitionArgs.hasOwnProperty(attr.hookName)){
                        return node._transitionArgs[attr.hookName];
                    }
                }
            }else if(attr.hasOwnProperty("stringValue")){
                if(attr.hasOwnProperty("stringValue")){
                    if(attr.stringValue === ""){
                        return true
                    }if(attr.stringValue === "true"){
                        return true
                    }else if(attr.stringValue === "false"){
                        return false
                    }else{
                        return attr.stringValue;
                    }
                }
            }
        },
        getTransitionArg : function(node,directiveName,returnAttr){
            let transitionArg;
            if(node.component && node.component.constructor._options){
                this.setAttrFromRender(node);
            }
            var self = this;
            if(node._specialAttributeDetails){
                node._specialAttributeDetails.forEach(function(attr){
                    if(directiveName == attr.hookName){
                        transitionArg = returnAttr ? attr : self.getActualTransitionArg(attr,node);
                        return;
                    }
                })
            }
            return transitionArg;
        },
        setAttrFromRender : function(comp,tagDirectives){
            if(tagDirectives.length){
                if(!comp._specialAttributeDetails){
                    comp._specialAttributeDetails = [];
                }
                var self = this;
                tagDirectives.forEach(function(directiveName){
                    if(self.checkSameAttrName(comp._specialAttributeDetails,directiveName,directiveName.value)){
                        return;
                    }
                    let attr;
                    if(typeof directiveName == "string"){ 
                        attr = {
                            hookName : directiveName.slice(1,directiveName.length),
                            hookNode : true,
                            name : directiveName,
                            stringValue : ""
                        }   
                    }else{
                        attr = {
                            hookName : directiveName.name.slice(1,directiveName.name.length),
                            hookNode : true,
                            name : directiveName.name,
                            stringValue : directiveName.value
                        }
                    }
                    comp._specialAttributeDetails.push(attr)
                })
            }
        },
        setNodeArgs : function(node,nodeValue){
            // node.ownerElement._transitionArgs = nodeValue;
            let nodeName = node.nodeName;
            node.ownerElement.removeAttribute(nodeName);
            // node.nodeName = node.nodeName.slice(1,node.nodeName.length);
            if(!node.ownerElement._transitionArgs){
                node.ownerElement._transitionArgs = {};
            }
            let actNodeName = nodeName
            if(nodeName.startsWith("@")){
                actNodeName = nodeName.slice(1,nodeName.length);
            }
            node.ownerElement._transitionArgs[actNodeName] = nodeValue;
            node.ownerElement.setAttribute("lyte-directive-" + actNodeName,"");
        },
        infoA : function(comp,info,dynamicN,helperNode,attr,yieldComp,options,isDefaultDirective){
            this.setSpecialNodes(comp,helperNode,dynamicN,info,options);
            if(dynamicN._specialAttributeDetails && dynamicN._specialAttributeDetails.length){
                dynamicN._specialAttributeDetails.push(attr);
            }else{
                dynamicN._specialAttributeDetails = [attr];
            }
            if(!isDefaultDirective){
                if(yieldComp){
                    yieldComp._transitionAppend.$push(dynamicN);
                }
                else if(comp._transitionAppend.indexOf(dynamicN)==-1 && dynamicN && dynamicN.getAttribute("is") != "component"){
                    comp._transitionAppend.$push(dynamicN);
                }
            }
        },
        setSpecialNodes : function(comp,helperNode,dynamicN,info,options){
            if (helperNode && (helperNode._hooksPresent || helperNode._defaultSetSpecialNode || dynamicN.hasAttribute("lyte-directive-prop") || dynamicN.hasAttribute("lyte-directive-node") || dynamicN.hasAttribute("lyte-directive-data"))) {
                if(helperNode._specialNodes){
                    if(helperNode.getAttribute("is") == "for"){
                        helperNode._specialNodes[options.itemIndex][info.in] = dynamicN;
                    }else if(helperNode.getAttribute("is") == "forIn"){
                        helperNode._specialNodes[options.itemIndex][info.in] = dynamicN;
                    }
                    else if(helperNode && /if|switch/g.test(helperNode.getAttribute("is"))){
                        helperNode._specialNodes[info.in] = dynamicN;  
                    }
                    else if(helperNode && helperNode.tagName == "LYTE-YIELD"){
                        helperNode._specialNodes[info.in] = dynamicN;
                    }
                }
            }else if(dynamicN && dynamicN._hooksPresent && comp._specialNodes){
                comp._specialNodes[info.in] = dynamicN;
                comp._hooksPresent = true;
                comp.hc = true;
            }else if(dynamicN && dynamicN._defaultSetSpecialNode && comp._specialNodes){
                comp._specialNodes[info.in] = dynamicN;
                comp._defaultSetSpecialNode = true;
            }
            if(info){
                if(info.chld){
                    dynamicN._chld = info.chld;
                }
                if(info.dc){
                    dynamicN.dc = info.dc;
                    dynamicN.hc = info.hc;
                }
                if(info.sibl){
                    dynamicN._sibl = info.sibl;
                }
            }
        },
        getExactTransitionArg : function(attr,node){
            if(attr.hasOwnProperty("dynamicValue") || attr.hasOwnProperty("helperInfo")){
                if(node.hasOwnProperty("_transitionArgs")){
                    if(node._transitionArgs.hasOwnProperty(attr.hookName)){
                        return node._transitionArgs[attr.hookName];
                    }
                }
            }else if(attr.hasOwnProperty("stringValue")){
                if(attr.hasOwnProperty("stringValue")){
                    if(attr.stringValue === ""){
                        if(node.id){
                            attr.stringValue = node.id;
                        }
                    }
                    return attr.stringValue;
                }
            }
        },
    },
    "fRP" : {},
    "fRC" : 0,
    // "frcUnused" : [],
    "customPropRegex" : "",
    // "pushFrc" : function(fastRenderIndex) {
    //     this.frcUnused.$push(fastRenderIndex);
    //     delete _LC.fRP[fastRenderIndex];
    // },
    // "getFrc" : function() {
    //     let frcIndex;
    //     if(this.frcUnused.length) {
    //         frcIndex = this.frcUnused.$shift()
    //     } else {
    //         frcIndex = this.fRC++;
    //     }
    //     return frcIndex;
    // },
    "customPropHandlers" : [],
    "_registeredComponents" : {},
    // "_reRegisteredComponents" : [],
    // "toBeRegistered" : [],
    "getCmpData":function(data){
        return data && data.__target__ ? data.__target__ : data;
    },
    "updateCustomCrmComponent" : function(componentClass){
        var def = componentClass.__observers;
        for(let key in def) {
            if (def[key] && def[key].type === "observer") {
                def[key].fnName = key;
                componentClass._observers.$push(def[key]);
            } else if (def[key] && def[key].type === "callBack") {
                var props = def[key].properties;
                for (var k = 0; k < props.length; k++) {
                    if (!componentClass._callBacks[props[k]]) {
                        componentClass._callBacks[props[k]] = [];
                    }
                    def[key].fnName = key;
                    componentClass._callBacks[props[k]].$push(def[key]);
                }
                if (def[key].observes) {
                    def[key].observes.fnName = key;
                    componentClass._observers.$push(def[key].observes);
                }
            }
        }
        // delete orgDef.observers;
        // for (let key in orgDef) {
        //     componentClass.prototype[key] = orgDef[key];
        // }
        // return orgDef;
    },
    // "dataFromMixin" : function(mixinsToBeUsed,actionsFromMixin,methodsFromMixin,newDefinition){
    //     var mixinslen = mixinsToBeUsed.length;
    //     for(let i=0; i<mixinslen ; i++) {
    //         for(let item in mixinsToBeUsed[i]){
    //             if(item === "actions") {
    //                 Object.assign(actionsFromMixin, mixinsToBeUsed[i][item]);    
    //             } else if(item === "methods") {
    //                 Object.assign(methodsFromMixin, mixinsToBeUsed[i][item]);    
    //             } else {
    //                 newDefinition[item] = mixinsToBeUsed[i][item];
    //             }
    //         }
    //     }
    //     return{"actionsFromMixin":actionsFromMixin,"methodsFromMixin":methodsFromMixin,"newDefinition":newDefinition}
    //   },
    "throwEvent" : function(eventName){
        let self = this.$node ? this.$node : this;
        const evt = self._actions? self._actions[eventName] : undefined; 
        let customArgs = [];        
        if(arguments.length > 1){        
            for(let i=1;i<arguments.length; i++){            
                customArgs.$push(arguments[i]);                
                }        
            }
            //wait for release
            //eventName = _LC.String.toCamelCase(eventName);
            _LC.throwAction.call(self, self._callee, eventName, undefined, true, customArgs, self);
        if(this.$node) {
            self.dispatchEvent(evt? evt : new CustomEvent(eventName, {"detail" : customArgs}));
        }
    },
    //this and scope reference should be either a node or a route.
    "throwAction" : function(scope,eventName,actObj,isCustom,customArgs, node, event, hasHandled, fromEv){
        let actionsObj;
        let stopBubble = false;
        let app = _LC.getNearestParentApp(this.component);
        if(this._route && isCustom) {
            // scope = Lyte.Router.getRouteInstance(this._route);
            scope = app.$.modules.router[0].getRouteInstance(this._route)//.__lp.fns;
            var fns = scope.__lp.fns;
            actionsObj = fns.actions || (fns.actions = {});
        } else if(this.routeName) {
            //process for the parent route and get the current component and proceed;
            let parentRoute = this.parent;
            if(parentRoute) {
                if(parentRoute.component && parentRoute.component.component) {
                    scope = parentRoute.component;
                    if(scope.component){
                        actionsObj = scope.component.constructor._actions; 
                    }else{
                        scope = app.$.modules.router[0].getRouteInstance(scope._route);
                        actionsObj = scope.actions || (scope.actions = {});
                    }
                } else {
                    scope = parentRoute;
                    // actionsObj =  scope.actions || (scope.actions = {});            
                    var fns = scope.__lp.fns;
                    actionsObj = fns.actions || (fns.actions = {});
                }
            }
        } else if(scope){
            if(scope.component && !scope._didDestroyCalled){
                actionsObj = scope.component.constructor._actions
            }else{
                return;
            }
        }
        if(!scope) {
            //Only warning is thrown because, we can have a eventListener for the dom directly. 
            //@Slicer.developmentStart
            if(!hasHandled) {
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Nothing handled the action "+ eventName + ".");    
            }
            //@Slicer.developmentEnd
            return;
        }
        actObj = (actObj) ? actObj : this._actions && this._actions[eventName]? this._actions[eventName].processAction : void 0;     
        //wait for release
        /* 
        var dasherizedEventName = _LC.String.dasherize(eventName);
        actObj = (actObj) ? actObj : this._actions && this._actions[dasherizedEventName]? this._actions[dasherizedEventName].processAction : void 0;     
        */
        let args = customArgs ? customArgs : [];
        let processDetails = {};
        if(actObj){
            var contextSwitchArray = [];
            if(node) {
                _LC.adCx(node, contextSwitchArray);
            }
            let concatArgs;
            if(actObj.skipArgProcessing) {
                // concatArgs = deepCopyObject(actObj.args);
                // concatArgs.$shift();
                // var eventIndex = concatArgs.indexOf("__lyteEvent__");
                // var nodeIndex = concatArgs.indexOf("__lyteNode__");
                // if(eventIndex !== -1) {
                //     concatArgs[eventIndex] = event;
                // } 
                // if(nodeIndex !== -1) {
                //     concatArgs[nodeIndex] = target;
                // }
                concatArgs = actObj.args;
            } else {
                concatArgs = this.processArgs(scope,{"helperInfo" : actObj}, undefined, undefined, event, node, undefined, undefined, undefined , processDetails);
            }
            args.$splice.apply(args, [0,0].$concat(concatArgs) );
            if(node) {
                _LC.rmCx(node, contextSwitchArray);
            }
            if(actionsObj[actObj.name]){
                if(!isCustom){  
                    //args.$unshift(window.event);
                    let parent = node.parentNode,val;
                     //@Slicer.developmentStart
                     _LC.debugPerf(app, this.__renderId, "Action "+actObj.name, this.constructor._compName);
                     //@Slicer.developmentEnd
                    //@Slicer.catchAllErrorsStart
                     try{
                    //@Slicer.catchAllErrorsEnd 
                        val = actionsObj[actObj.name].apply(this.component, args);
                    //@Slicer.catchAllErrorsStart
                     }catch(err){
                        app.appError?app.appError.error(this.component,err):_utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this.component,err);
                     }
                     //@Slicer.catchAllErrorsEnd
                     //@Slicer.developmentStart
                     _LC.debugPerf(app, this.__renderId, "Action "+actObj.name, this.constructor._compName);
                     //@Slicer.developmentEnd
                    if(event.currentTarget !== document.body && !_LCSD.getHostElement(event.currentTarget) && !fromEv) {
                        val = false;
                    }
                    hasHandled = true;
                    //@Slicer.$hideStart
                    stopBubble = ComponentRegistry.hide && ComponentRegistry.hide.shouldStop(node, event);
                    //@Slicer.$hideEnd
                    if(val !== false && !event.cancelBubble && !stopBubble){
                        if(actObj.from && node.getAttribute(event.type) && node._boundEvents && node._boundEvents[event.type]) {
                            let actions = node._callee.component.constructor._actions;
                            let actObj = node._boundEvents[event.type];
                            let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj);
                            // cloneActObj.args.$shift();
                            _LC.skipArgProcessing(cloneActObj, event, node);
                            _LC.throwAction.call(node._callee,node._callee,event.type,cloneActObj, undefined, undefined, node, event, hasHandled);
                        } else {
                            if(_LC.hasLyteEvents(node, eventName)) {
                                let eventStopped = _LC.handleLyteEvents(node, event);
                                val = eventStopped ? false : true;       
                            }
                            if(val === false) {
                                return;
                            }
                            if(_LC.isCustomElement(node)){
                                scope = parent;
                            }
                            if(parent  &&  !(event.currentTarget !== document.body && !_LCSD.getHostElement(event.currentTarget))){
                                let eventStopped;
                                while(parent && !_LCSD.getHostElement(parent) && (!parent.getAttribute(eventName) || parent.hasAttribute("disabled") ) && parent.tagName != "BODY"){
                                    if(parent._hiddenBoundEvents && parent._hiddenBoundEvents[eventName]){
                                        break;
                                     }
                                    if(_LC.hasLyteEvents(parent, eventName)) {
                                        eventStopped = _LC.handleLyteEvents(parent, event);
                                        if(eventStopped) {
                                            break;
                                        }
                                    }
                                    parent = parent.parentNode;
                                }
                                if(eventStopped || !parent || parent.tagName === "BODY"){
                                    return;
                                }
                                if(!parent._callee){
                                    parent._callee = parent.getCallee ? parent.getCallee(parent) : _LC.getCallee(parent);
                                }
                                if(parent && event.type === eventName && !event.cancelBubble){
                                   if(parent._evBoundEvents && parent._evBoundEvents[eventName]) {
                                       let actObj = parent._evBoundEvents[eventName];
                                       let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj);
                                    //    cloneActObj.args.$shift();
                                        _LC.skipArgProcessing(cloneActObj, event, parent);
                                       _LC.throwAction.call(parent,parent,eventName,cloneActObj,undefined,undefined,parent,event, hasHandled);  
                                   } else if(parent && parent._boundEvents && parent._boundEvents[eventName]) {
                                       let actObj = parent._boundEvents[eventName];
                                       let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj);
                                       _LC.skipArgProcessing(cloneActObj, event, parent);
                                       _LC.throwAction.call(parent._callee,parent._callee,eventName,cloneActObj,undefined,undefined,parent,event, hasHandled);  
                                   }
                                   //@Slicer.$hideStart
                                   ComponentRegistry.hide && ComponentRegistry.hide.triggerGlobalEvent(parent, eventName, event, hasHandled, true);
                                   //@Slicer.$hideEnd
                                }
                            }
                        }
                    }
                }            
                else{                
                    //@Slicer.developmentStart  
                    _LC.debugPerf(app, this.__renderId, "Action "+actObj.name, this.constructor._compName);
                    //@Slicer.developmentEnd 
                    //@Slicer.catchAllErrorsStart
                    try{
                    //@Slicer.catchAllErrorsEnd
                        actionsObj[actObj.name].apply(this._callee.component, args);
                    //@Slicer.catchAllErrorsStart
                    }catch(err){
                        app.appError ? app.appError.error(this.component,err) : _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this._callee.component,err);
                    }
                    //@Slicer.catchAllErrorsEnd     
                    //@Slicer.developmentStart
                    _LC.debugPerf(app, this.__renderId, "Action "+actObj.name, this.constructor._compName);
                    //@Slicer.developmentEnd
                    hasHandled = true;                                           
                } 
            }
            //@Slicer.developmentStart
            else{
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this, "LC004" , actObj.name);
            }
            //@Slicer.developmentEnd
        } else if(isCustom) {
            var eventsObj = actionsObj[eventName]  || actionsObj[_LC.String.toCamelCase(eventName)] || actionsObj[_LC.String.dasherize(eventName)];
            if(eventsObj) {
                var scopeS = _LC.isCustomElement(scope) ? scope.component : scope;   
                let val = eventsObj.apply(scopeS, args);
                //let val = eventsObj.apply(_LC.isCustomElement(scope)? scope.component : scope, args);
                hasHandled = true;
                if(val !== false) {
                    _LC.throwAction.call(scope, scope._callee, eventName, actObj, isCustom, customArgs, undefined, undefined, hasHandled);
                }
            } else {
                _LC.throwAction.call(scope, scope._callee, eventName, actObj, isCustom, customArgs, undefined, undefined, hasHandled);
            }
        }
        
    },
    "isControlHelper" : function(ownerElement) {
        return (ownerElement.tagName === "TEMPLATE" && ownerElement.getAttribute("is") && ownerElement.getAttribute("is") !== "component") || (ownerElement.hasAttribute("lyte-for") || ownerElement.hasAttribute("lyte-if") || ownerElement.hasAttribute("lyte-switch") || ownerElement.hasAttribute("lyte-forin"));
    },
    "isCustomElement" : function(node, isTemplate) {
        return node.hasAttribute && ((( node.tagName ==="TEMPLATE" || node.attributes["lyte-for"] || node.attributes["lyte-if"] || node.attributes["lyte-switch"] || node.attributes["lyte-forin"] ) && isTemplate )  || (node.nodeName && node.nodeName.indexOf('-') !== -1 && (ComponentRegistry._registeredCommonClass[node.localName] || node.tagName === "LYTE-YIELD")));
    },
    "componentSet" : function(key, value, options, forceExecute, fromParent) {
        if(!forceExecute && this.get(key) === value) {
            _LC.clearError(this.data, key);
            //@Slicer.developmentStart
            if(!fromParent){
                if(typeof value == "object"){
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("The data passed to update '" + key + "' is the same reference of the actual data. So component bindings won't be updated.");
                }
            }
            //@Slicer.developmentEnd
            return;
        }
        
        //temporary fix
        return _LC.set(this.data, key, value, options, undefined, fromParent);
    },
    "componentGet" : function(key) {
        return key ? _LC.get(this.data, key) : this.data;
    },
    "nodeGet" : function(key) {
        return key ? this.component.get(key) : this.component.data;
    },
    "nodeSet" : function(key, value, options, fromParent) {
        this.component.set(key, value, options, undefined, fromParent);
    },
    "registerMixin" : function(name,mixin){
        _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Mixin.register.call(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte, name, mixin);
    },
    "sendtoclient": function(prop,server,node){
        var data = server ? prop.component.data : prop;
        var newData = {};
        var data_prop = node;
        for (var i in data){
            if(server){
                var def = prop.component.__data[i];
                if(i == "bindIds"){ 
                    newData[i] = {value : data[i] }
                }
                    if(def && /^(object|array)$/.test(def.type) && prop._attributeDetails && prop._attributeDetails.hasOwnProperty(_LC.String.dasherize(i))){
                        newData[i] = { "fP" : true };
                    }
                    else if(data[i] instanceof Date){
                        newData[i] = { "value" : data[i] , "type" : "Date" }
                    }
                    else if(data_prop[i]){
                        newData[i] = { "value" : data[i] , "type" : data_prop[i].type }
                    }else{
                        var type = typeof(data[i])
                        if(data[i] == undefined){
                            type = undefined;
                        }
                        newData[i] = {"value" : data[i] ,"type":type}
                    }
            }else{
                if(data[i].type=="Date"){
                    newData[i] = new Date(data[i].value);
                }
                else if(!data[i].fP){
                    newData[i]= data[i].value;
                }
            }
        }
        return newData;        	
    },
    "typeCast" : function(value, dataType, obj) {
        if(value === null) {
            return value;
        }
        try {
            switch(dataType) {
            case "string" : 
                return typeof value === "object" ? JSON.stringify(value) : value.toString(); 	
            break;
            case "number" :
                {
                if(value == "") {
                    return undefined;
                }
                let val = +value;
                if(isNaN(val)) {
                    throw "TypeCast exception";
                } 
                return val;
                }
            break;
            case "array" : 
            case "object" :
                return JSON.parse(value);
                break;
            case "boolean" :
                return ( (!value && value !== "") || value=== "false") ? false : true; 
            break;
            default : 
                return value;
            }
        } catch(e) {
            if(obj) {
                obj.isError = true;
            }
            return value;
        }
        
    },
    "getDataType" : function(value) {
        var type = typeof value;
        if(type === "object") {
            if(Array.isArray(value)) {
                return "array";
            }
        }
        return type;
    },
    "handleValidation" : function(object, property, value, component ,init) {
        let error = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.validateData)(object, property, value, component ,this._lyteInstance ,init);
        let cmpData = _LC.getCmpData(component.data);
        if(error) {
            _LC.set(component.data.errors, property, error);
            if(component.$node.callback) {
                component.$node.callback("onError", property, error);
            }
        } else {
            _LC.clearError(object, property);
        }
        return error;
    },
    "clearError" : function(data, property) {
        if(data.errors && data.errors[property]) {
            _LC.oF(data.errors, "delete", property);
        }
    },
    "createLyteId" : function(comp){
        comp.__counter++;
        return "__lyteId" + comp.__counter;
    },
    "apdNode" : function(node, comp) {
        _LC.tDiv.content.appendChild(node);
        let id = this.createLyteId(comp);
        comp.__h[id] = node;
        node.__lyteId = id;
    },
    "update":function(object, property, value, options, fromStore,oldValue,setterScope, actualProperty, fromParent ,FromUtils, storeRecord, fromAttr){
        let fromComponent = object.__component__;
        let updateAttr = true;
        let dataType, dataDef, estObjBind = false;
        if(!oldValue){
            oldValue = object[property];
            if(fromComponent && fromComponent.tagName !== "LYTE-YIELD") {
                dataDef = fromComponent.component.__data[property];
                if(!options || (typeof options == "object" && options.skipValidation != true)){
                    if(dataDef && (dataType = dataDef.type)) {
                        updateAttr = !dataDef.hideAttr;
                        if(dataType !== _LC.getDataType(value) && (value !== undefined || dataType === "boolean")) {
                            value = _LC.typeCast(value, dataType);
                        }
                    }
                }
                if(value === oldValue && (!options || !options.force)) {
                    _LC.clearError(object, property);
                    return;
                }
                if(!options || (typeof options == "object" && options.skipValidation != true)){
                    let error = _LC.handleValidation(object, property, value, fromComponent.component);

                    if(error) {
                        // //@Slicer.developmentStart
                        // if(fromComponent.component.data.errors && Object.keys(fromComponent.component.data).length){
                        //     ComponentError.error("Error in data passed to component '"+fromComponent.component.$node.localName+"' for the property - "+property);
                        // }
                        // //@Slicer.developmentEnd
                        return false;
                    }
                }
        }
            //object[property] = value;
            if(!object.hasOwnProperty(property) && !(Array.isArray(object))) {
                _LC.oF(object, "add", property, value, true )
            }else if(_LC.freeze && _LC.freeze.prop(object.__component__, property)){
                return;
            } else {
                object[property] = value;
            }
        }
        let toBeExecuted = fromComponent ? true : false;
        let dasherizedAttr = _LC.String.dasherize(property);
        if(fromComponent && actualProperty && ( (typeof value === "string" && fromComponent.getAttribute(dasherizedAttr) !==  value) || fromComponent.hasAttribute(dasherizedAttr) )) {
            // (customPropRegex && this.customPropRegex)
            let cpr;
            if(fromComponent.tagName == "LYTE-YIELD"){
                cpr = fromComponent._callee.component._registryClass.customPropRegex;
            }else{
                cpr = fromComponent.component._registryClass.customPropRegex;
            }
             if((!cpr.exec(property) || fromComponent.hasAttribute(dasherizedAttr) ) && updateAttr) {
                 if(value && typeof value === "object") {
                     let jsonString;
                     try{
                        jsonString = JSON.stringify(value);
                        // fromComponent.attributes.getNamedItem(dasherizedAttr).__lyteIgnore = true;
                        // fromComponent.setAttribute(dasherizedAttr, jsonString);
                        // fromComponent.attributes.getNamedItem(dasherizedAttr).__lyteIgnore = false;
                        let attrNode1 = fromComponent.attributes.getNamedItem(dasherizedAttr);
                        attrNode1.__lyteIgnore = true;
                        if(!fromComponent._connectedCalled && fromComponent.hasAttribute(dasherizedAttr)){
                            fromComponent.setAttribute(dasherizedAttr, jsonString);
                            attrNode1.__lyteIgnore = false;
                            return;
                        }else{
                            fromComponent.setAttribute(dasherizedAttr, jsonString);
                            attrNode1.__lyteIgnore = false;
                        }
                     } catch(e) {

                     }
                 } else {
                     let attributeString = _LC.typeCast(value, "string");
                     if(fromComponent.getAttribute(dasherizedAttr) !== attributeString) {
                         let detAttr = fromComponent.attributes.getNamedItem(dasherizedAttr);
                         if(detAttr) {
                             detAttr.__lyteIgnore = true;
                         }
                         attributeString = attributeString || "";
                         fromComponent.setAttribute(dasherizedAttr, attributeString);
                         if(detAttr) {
                            detAttr.__lyteIgnore = false;
                         }
                     }
                 }
             }
        }
        if(value && typeof value !== "string" && typeof value !== "boolean" && typeof value !== "number" ) {
            //newValue is of type object 
            
            if(oldValue && typeof oldValue === "object" && oldValue._bindings) {
                //Both oldValue and newValue are objects. 
                if(!value._bindings) {
                    defProp(value, "_bindings", {
                        enumerable: false, 
                        writable: true, 
                        value : new Set(),
                        configurable: true
                    });
                }
                let fcmpData = fromComponent ? (_LC.getCmpData(fromComponent.component.data)) : undefined;
                //for changing only child component
                if(fromComponent && fcmpData === object && property.indexOf('.')=== -1) {
                    let bindings = fromComponent.getProperty(property);
                    this.removeSelectedBindingDeep(bindings, oldValue);
                    addBindings(value,bindings);
                    this.establishBindings(bindings, value);
                    //For removing binding in the object due to forIn Helper ( actual object binding and not the _dynamicNodes binding).
                    if(bindings._forHelpers) {
                        let bindfor = bindings._forHelpers.toArrayLyte();
                        for(var i=0;i<bindfor.length;i++){
                            let item = bindfor[i];
                            if(item._propBindingObject) {
                                this.removeSelectedBindingDeep(item._propBindingObject, oldValue);
//                                  value._bindings.add(item._propBindingObject);
//                                  this.establishBindings(item._propBindingObject, value);
                            }
                        }
                    }
                    let stack = [];
                    this.affectChanges(bindings,undefined,oldValue,setterScope,object[property],stack,undefined,undefined,undefined,undefined,undefined,options);
                    this.executeObserver(stack);
                } else {
                    //To change only the bindings present in the object and not all the bindings present in the oldValue.
                    if(object._bindings) {
                        let oldbind = object._bindings.toArrayLyte();
                        for(let i=0; i<oldbind.length;i++){
                            let item = oldbind[i][property];
                            if(item) {
                                this.removeSelectedBindingDeep(item, oldValue);
                                addBindings(value,item);
                                this.establishBindings(item, value);
                                //For removing binding in the object due to forIn Helper ( actual object binding and not the _dynamicNodes binding).
                                if(item._forHelpers) {
                                    let forbind = item._forHelpers.toArrayLyte();
                                    for(let j=0;j<forbind.length;j++){
                                        let itemBinding = forbind[j];
                                        if(itemBinding._propBindingObject) {
                                            this.removeSelectedBindingDeep(itemBinding._propBindingObject, oldValue);
                                        }
                                    }
                                }
                                let stack = [];
                                this.affectChanges(item,undefined,oldValue,setterScope,object[property],stack,undefined,undefined,undefined,undefined,undefined,options);
                                this.executeObserver(stack);
                            }
                        }
                    }
                
                
                }
            } else {
                //newValue is object and oldValue is string. Hence establish bindings from oldValue's object and place it in the newValue. 
                if(object._bindings) {
                    makeSet(value, "_bindings");
                    let objbind = object._bindings.toArrayLyte();
                    for(let i=0;i<objbind.length;i++){
                        let item = objbind[i];
                        if(item[property]) {
                            addBindings(value,item[property]);
                            this.establishBindings(item[property], value);
                            let stack = [];
                            this.affectChanges(item[property],undefined,oldValue,setterScope,object[property],stack,undefined,undefined,undefined,undefined,undefined,options);
                            this.executeObserver(stack);
                        }else{
                            if(item["{}"]){
                                this.affectChanges(item["{}"],undefined,oldValue,setterScope,object[property],undefined,undefined,undefined,undefined,true,undefined,options);
                            }
                            if(item["*"]){
                                this.affectChanges(item["*"],undefined,oldValue,setterScope,object[property],undefined,undefined,undefined,undefined,true,undefined,options);
                            }  
                        }
                    }
                }
            }
            dataDef = dataDef || (fromComponent && fromComponent.tagName !== "LYTE-YIELD" ? fromComponent.component.__data[property] : undefined); 
            if((fromStore && (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object)) || (object && object.hasOwnProperty("_scp") && object._scp.size) || (dataDef && (/^(object|array)$/.test(dataDef.type)) && (dataDef.watch || dataDef.hasOwnProperty("items") || dataDef.hasOwnProperty("properties")))){
                estObjBind = true;
            }
        } else {
            //newValue is string
            
            if(oldValue && typeof oldValue === "object" && oldValue._bindings && object._bindings) {
                //newValue is string and oldValue is object 
                let objbind = object._bindings.toArrayLyte();
                for(let i=0;i<objbind.length;i++){
                    let item = objbind[i];
                    if(item[property]) {
                        //oldValue._bindings.delete(item[property]);
                        //if(oldValue._bindings.size === 0) {
                        //  delete oldValue._bindings;
                        //  break;
                        //}
                    this.removeSelectedBindingDeep(item[property], oldValue);
                        if(item[property]._forHelpers) {
                            let forbind = item[property]._forHelpers.toArrayLyte();
                            for(let j=0;j<forbind.length;j++){
                                let itemBinding =forbind[j];
                                if(itemBinding._propBindingObject) {
                                    this.removeSelectedBindingDeep(itemBinding._propBindingObject, oldValue);
                                }
                            }
                        }
                    }
                }
            }
            
            //when newValue and oldValue , both are string, no need to change bindings. 
            if(object._bindings) {
                let objbind = object._bindings.toArrayLyte();
                for(let i=0;i<objbind.length;i++){
                    let item = objbind[i];
                    if(item[property]) {
                        let stack = [];
                        this.affectChanges(item[property],undefined,oldValue,setterScope,object[property],stack,undefined,undefined,undefined,undefined,undefined,options);
                        this.executeObserver(stack);
                    }else{
                        if(item["{}"]){
                            this.affectChanges(item["{}"],undefined,oldValue,setterScope,object[property],undefined,undefined,undefined,undefined,true,undefined,options);
                        }
                        if(item["*"]){
                            this.affectChanges(item["*"],undefined,oldValue,setterScope,object[property],undefined,undefined,undefined,undefined,true,undefined,options);
                        }
                    }
                    if(item["$data"]){
                        this.affectChanges(item["$data"],undefined,oldValue,setterScope,object[property],undefined,undefined,undefined,undefined,undefined,property,options);
                    }
                }
            }
        }
        if(object._scp){
            if(!(0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.cmpData)(oldValue, value)){
                this.callDeepObservers(object, { type:"deepChange", oldValue:oldValue, newValue : value }, property);
            }
            if(oldValue && (Array.isArray(oldValue) || typeof oldValue == "object")){
                var keys = Array.from(object._scp.keys());
                keys.forEach(function(id){
                    var nestObj = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.nestScp[id];
                    var isCyclic = nestObj.cyclic;
                    (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.removeNestScp)(oldValue, id);
                    if(isCyclic){
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.bindObj)(nestObj._data, undefined, id, [], new Map());
                    }
                });
            }
        }
        if(((0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object) && object.$.__scpObj) || (object.hasOwnProperty("__component__") && object.__component__.__scpObj)){
            var _scpObj, kmpKey;
            if((0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object)){
                _scpObj = object.$.__scpObj;
                kmpKey = object;
            }else{
                _scpObj = object.__component__.__scpObj;
                kmpKey = object.__component__;
            }
            if(_scpObj.hasOwnProperty(property)){
                var _sId = _scpObj[property].split("_");
                (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.removeNestScp)(oldValue, _sId[0], _sId[1], undefined, kmpKey, undefined, (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(value) || Array.isArray(value) && value.model && value.add ? value : undefined);
                delete _scpObj[property];
            }
        }
        if (!FromUtils) {
            _LC.callObjectObservers(object, { type: "change", "oldValue": oldValue, "newValue": value, item: property });
        }
        var customDtype = false;
        if(dataDef && dataDef.type instanceof Function){
            dataDef = dataDef.type;
            customDtype = true;
        }
        if(estObjBind || customDtype){
            // establishObjectBinding(object, property, fromStore, true);
            (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.establishObjectBinding)(
                object,
                property,
                fromStore,
                true,
                storeRecord,
                (dataDef && dataDef.watch)?dataDef.watch:undefined
            );
        }
        if(toBeExecuted && !fromParent && fromComponent._attributeDetails && fromComponent._callee) {
            //let syntaxValue = fromComponent.getAttributeNode(property).syntaxValue;
            let attrDetail = fromComponent._attributeDetails[_LC.String.dasherize(property)];
            let syntaxValue;
            if(!attrDetail && fromComponent._attributeDetails["$data"]){
                let obj = {};
                _LC.$unbound(fromComponent._attributeDetails["$data"].helperInfo, obj);
                if(!obj.unbound){
                    attrDetail = fromComponent._attributeDetails["$data"];
                    syntaxValue = property;
                }
            }
            if(attrDetail && attrDetail.isLbind) {
                syntaxValue = attrDetail.dynamicValue;
            }
            if(syntaxValue) {
                let contextSwitchArray;
                if(fromComponent._cx) {
                    contextSwitchArray = [];
                    _LC.changeContext(fromComponent._cx.node, contextSwitchArray, fromComponent._cx )
                }
                let fclData = _LC.getCmpData(fromComponent._callee.component.data);
                let obj = _LC.getNew(fclData, syntaxValue);
                if(!obj.context){
                    return;
                }
                let exec = false;
                if(obj.context === fclData) {
                    if(fromComponent._callee._properties[obj.lastKey] && fromComponent._callee._properties[obj.lastKey].__fromComponent) {
                        exec = true;
                    }
                } else {
                    exec = true;
                }
                //self.setData(this._lbind,this.value);
                if(exec) {
                    let lastKeyIndex = +obj.lastKey;
                    if(Array.isArray(obj.context) && typeof lastKeyIndex == "number") {
                        let callReplaceAt = lastKeyIndex < obj.context.length;
                        if(obj.context[lastKeyIndex] !== value || !callReplaceAt){
                            _LC.aF(obj.context, callReplaceAt ? "replaceAt" : "insertAt", lastKeyIndex, value);
                        }
                    } else {
                        _LC.set(obj.context, obj.lastKey, value, options);
                    }
                }
                if(contextSwitchArray) {
                    _LC.removeContext(fromComponent._cx.node, contextSwitchArray, fromComponent._cx )
                }
            }
        }
    },
    "getContentForIE" : function getContentForIE(content, constr, newlyCreated, info) {
        if(typeof content === "string") {
            newlyCreated = true;
            var div = createElement("div");
            div.innerHTML = content;
            content = div.childNodes[0];
            constr.splitTextNodes(content);
        }
        if(content.getAttribute && content.getAttribute("depth")) {
            if(info) {
                info._content = div;
            }
            var itr = parseInt(content.getAttribute("depth"));
            content =  newlyCreated ? content.content : content.content.cloneNode(true);
            // content = Lyte._ie ? content : (newlyCreated ? content.content : content.content.cloneNode(true));
            for(var i=0;i<itr;i++) {
                content = content.childNodes[0];
            }
        }
        return content;
    },
    "replaceWithPf" : function(node1, node2) {
        // if(Lyte._rwpf) {
        //     _LC.insertBeforeNative(node1.parentNode,node2, node1);
        //     node1.remove();
        // } else {
            node1.replaceWith(node2);
        // }
    },
    "getCallee" : function(callee, self){
        while(callee &&  !_LC.isCustomElement(callee) && callee.tagName !== "LYTE-YIELD") {
            if(callee.tagName === "BODY") {
                callee = null;
                break;
            }
            let hostEle = _LCSD.getHostElement(callee)
            if(!callee.parentNode && hostEle){
                callee = hostEle;
            }
            else{
                callee = callee.parentNode;
            }
        }
        if(callee && callee.tagName === "LYTE-YIELD") {
        return callee._registerYield? callee._registerYield._callee : undefined;
        }
        return ((self ===  callee) ? undefined : callee);
    },
    "set" : function(object, property, value, options, fromStore, fromParent, FromUtils, skipTypeError) {
        let lastIndex = -1;
        var s_rec,check={},recDottedProp;
        if((0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object) && fromStore){
            s_rec=object;
        }
        if(!(typeof property === "object") || !property){
            property = property+"";
            lastIndex = property.lastIndexOf('.');
        }
        let actualProperty = property;
        if(lastIndex !== -1) {
            let outerPropertyPath = property.substring(0, lastIndex);
            property = property.substring(lastIndex + 1);
            object = _LC.get(object, outerPropertyPath);
            recDottedProp = true;
        }
        if(object && object.__ltPrx__){
            object = object.__target__;
            // object[property] = value;
            // return;
        }
        let val1 = object[property] && object[property].__target__ ? object[property].__target__ : object[property];
        let val2 = value && value.__target__ ? value.__target__ : value;
        if(typeof property === "string" && val1 === val2 && (!options || !options.force)) {
            if(object.__component__) {
                _LC.clearError(object, property);
            } else if((0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object)) {
                if(!object.$.isCloned){
                    _data_src_ValidationError_js__WEBPACK_IMPORTED_MODULE_8__.ValidationError.clrRecErr(object.$, property);
                }
                object.$.emit("set", [object, property]);
                if(object.$.schema){
                    object.$.schema.emit("set", [object.$.schema._name, object, property]);
                    object.$.schema.db.emit("set", [object.$.schema._name, object, property]);
                }
                else if(object.$.model){
                    object.$.model.emit("set", [object.$.model._name, object, property]);
                    object.$.model.db.emit("set", [object.$.model._name, object, property]);
                }

            }
            return;
        }
        let oldValues = [];
        if(object._setterScope){
            var setterScope = object._setterScope;
        }
        var checkSim = false, watch = [];
        if(object._scp && object._scp.size){
            var keys = Array.from(object._scp.keys()), obj;
            keys.forEach(function(id){
                var _obj = object._scp.get(id);
                obj = _obj.paths;
                var recObj = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.nestScp[id], model, field, rec, attr, wobj = {};
                var pathArr = Object.keys(obj), aPath;
                if(pathArr.length > 1){
                    aPath = "*";
                }
                else if(pathArr.length == 1){
                    aPath = pathArr[0];
                }
                wobj.path = aPath;
                wobj.id = id;
                if(pathArr.length > 1){
                    wobj.paths = pathArr;
                }
                wobj.attr = attr;
                wobj.data = recObj.data;
                wobj.PropsInfo = recObj.PropsInfo||undefined;
                var db = recObj.db;
                if(recObj.model){
                    var mMap = recObj.model;
                    wobj.Error = recObj.Error;
                    var mKeys = Array.from(recObj.model.keys());
                    for(var i=0; i<mKeys.length; i++){
                        var mName = mKeys[i];
                        var pkMap = mMap.get(mName);
                        if(pkMap){
                            var pkArr = Array.from(pkMap.keys());
                            var pkLen = pkArr.length;
                            for(var j=0; j<pkLen; j++){
                                var mPk = pkArr[j];
                                var attrMap = pkMap.get(mPk);
                                var attrArr = Array.from(attrMap.keys());
                                var attrLen = attrArr.length;
                                for(k=0; k<attrLen; k++){
                                    var mAttr = attrArr[k];
                                    var mObj = Object.assign({}, wobj);
                                    if(db.getSchema){
                                        var mRec = recObj.db.cache.getEntity({schema:db.getSchema(mName), pK:mPk});
                                    }
                                    else{
                                        var mRec = recObj.db.cache.getEntity({model:db.getModel(mName), pK:mPk});
                                    }
                                    if(mRec){
                                        model = mRec.$.schema ? mRec.$.schema : mRec.$.model;
                                        field = model.fieldList[mAttr];
                                        if((field && field.watch == true) || field.type instanceof Function){
                                            mObj.data = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(mRec[mAttr]);
                                            mObj.rec = mRec;
                                            mObj.isRec = true;
                                            mObj.attr = mAttr;
                                            mObj.dtype = model.fieldList[mAttr];
                                            mObj._cmpErr=mRec.$.error;
                                        }
                                        watch.$push(mObj);
                                    }
                                }
                            }
                        }
                    }
                } 
                else{
                    watch.$push(wobj);
                }         
            });
            watch.forEach(function(val){
                if((!options || (typeof options == "object" && options.skipValidation != true)) ){
                    var id = val.id,path=val.path.split("."),dtype=val.dtype,errs;
                    if(val.isRec){
                        // path.shift();
                        // path = path.length == 1 && path[0] == property ? []:path;
                        check.Prop = dtype;
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkNestedProp)(id,path,dtype,val,object,property,value,check,fromStore); 
                    }
                    if(val.PropsInfo){
                        val.PropsInfo.forEach(function(props){
                            props.path = val.path;
                            props.attr = val.attr;
                            dtype = props.dtype;
                            check.Prop = dtype;
                            (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkNestedProp)(id,path,dtype,props,object,property,value,check,fromStore);
                        })
                    }
                }
            })
            //@Slicer.developmentStart
            if (check.value && check.value.code) {
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC019", check.Prop.type.name);
                return { code: "LC019", message: _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.getErrorMessage("LC019", check.Prop.type.name)};
            }
            //@Slicer.developmentEnd
        }
        actualProperty = actualProperty === property ? actualProperty : undefined;
        var shareObj = {};
        if(typeof property === "object"){
            if(value && value.hasOwnProperty("skipTypeError")){
                skipTypeError = value.skipTypeError;
            }
            if((0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object) && !fromStore && !object.$.isCloned) {
                    // for(let key in property){
                    //     if(Array.isArray(object[key])){
                    //         oldValues.$push({key:key,oldValue:object[key].slice(0)});    
                    //     }
                    //     else{
                    //         oldValues.$push({key:key,oldValue:object[key]});
                    //     }
                    // }
                    for(let key in property) {
                        let locValue = property[key];
                        let dataType = object.$.schema ? object.$.schema.fieldList[key] : object.$.model.fieldList[key];
                        dataType = dataType ? dataType.type : undefined;
                        if(dataType && (locValue !== undefined || dataType === "boolean") && dataType !== _LC.getDataType(locValue)) {
                            property[key] = _LC.typeCast(locValue, dataType);
                        }
                    }
                    var db = object.$.db;
                    let record;
                    if(db){
                        record = db.$.__sD(object.$, property, undefined, options, undefined, undefined, shareObj);
                    }
                    // if(record.$.isError){
                    //     return record;
                    // }
                    // for(let i=0; i<oldValues.length; i++){
                    //     _LC.update(object,oldValues[i].key,object[oldValues[i].key],fromStore,(oldValues[i].oldValue === undefined)?null:oldValues[i].oldValue ,setterScope, actualProperty, fromParent);
                    // }
            } else {
                //object[property] =  value;
                var errAttr = [];
                for(let key in property){
                    //_LC.update(object,key,property[key],fromStore,undefined,setterScope, actualProperty, fromParent);
                    //value is option here
                    var ret = _LC.set(object, key, property[key], value, fromStore, fromParent, undefined, true);
                    if(ret && ret.code){
                        errAttr.push(property);
                    }
                }
                if(!skipTypeError && errAttr.length){
                    var fromComponent = object.__component__;
                    //@Slicer.developmentStart
                    if(fromComponent && fromComponent.component.data.errors && Object.keys(fromComponent.component.data.errors).length){
                        var eKeys = Object.keys(fromComponent.component.data.errors), kStr;
                        if(eKeys.length){
                            kStr = eKeys.toString();
                            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC006", fromComponent.component.$node.localName, kStr);
                        }
                        return { code: "LC006", message: _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.getErrorMessage("LC006", fromComponent.component.$node.localName, kStr), properties:kStr};
                    }
                    //@Slicer.developmentEnd
                }
            }
        }
        else{
            if(options && options.hasOwnProperty("skipTypeError")){
                skipTypeError = options.skipTypeError;
            }
            if((0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(object) && (!fromStore || (fromStore && recDottedProp)) && !object.$.isCloned) {
                let old = object[property];
                let dataType = object.$.schema ? object.$.schema.fieldList[property] : object.$.model.fieldList[property];
                dataType = dataType ? dataType.type : undefined;
                if(dataType && (value !== undefined || dataType === "boolean") && dataType !== _LC.getDataType(value)) {
                    value = _LC.typeCast(value, dataType);
                }
                var db = object.$.db;
                let record;
                if(db){
                    record = db.__sD(object.$, property,value, options, undefined, undefined, shareObj);
                }
                // if(record.$.isError){
                //     return record;
                // }
                //Commented because update will happend when "set" is called from setData of store. 
                //_LC.update(object,property,value,fromStore,(old === undefined) ? null : old,setterScope , actualProperty);    
            } else {
                var ret = _LC.update(object,property,value,options,fromStore,undefined,setterScope,actualProperty, fromParent, FromUtils, s_rec);
                if(ret == false){
                    var fromComponent = object.__component__;
                    //@Slicer.developmentStart
                    if( !skipTypeError && fromComponent && fromComponent.component.data.errors && Object.keys(fromComponent.component.data.errors).length){
                        var eKeys = Object.keys(fromComponent.component.data.errors);
                        if(eKeys.length){
                            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC006", fromComponent.component.$node.localName,property);
                        }
                    }
                    //@Slicer.developmentEnd
                    return {code: "LC006", message: _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.getErrorMessage("LC006", fromComponent.component.$node.localName, property)};                 
                }
                 
            }
        }
        if(watch && watch.length){
            watch.forEach(function(obj){
                if(obj.isRec){
                  var db = obj.rec.$.db;
                  if(db){
                      db.__dVC(obj.rec, obj.attr, obj.rec[obj.attr], obj);
                  }
                }
            });
        }
        // return true;
    },
    "adCx" : function(node, contextSwitchArray) {
        let isYield = node.tagName === "LYTE-YIELD";
        if(node._cx && (!isYield || node._cx.node.tagName !== "LYTE-YIELD")) {
            _LC.changeContext(node._cx.node, contextSwitchArray, node._cx, isYield);
        } else if(isYield && node._callee._cx) {
            _LC.changeContext(node._callee._cx.node, contextSwitchArray, node._callee._cx, true);
        }
    }, 
    "rmCx" : function(node, contextSwitchArray) {
        let isYield = node.tagName === "LYTE-YIELD";
        if(node._cx && (!isYield || node._cx.node.tagName !== "LYTE-YIELD")) {
            _LC.removeContext(node._cx.node, contextSwitchArray, node._cx, isYield);
        } else if(isYield && node._callee._cx) {
            _LC.removeContext(node._callee._cx.node, contextSwitchArray, node._callee._cx, true);
        }
    },
    "changeContext" : function(node, contextSwitchArray, contextSwitchInfo, proceedFurther) {
        if(!contextSwitchInfo) {
            return;
        }
        let isYield = node.tagName === "LYTE-YIELD";
        if(node._cx && (!isYield || node._cx.node.tagName !== "LYTE-YIELD")) {
            _LC.changeContext(node._cx.node, contextSwitchArray, node._cx, node.tagName === "LYTE-YIELD" || proceedFurther);
        } else if((node.tagName === "LYTE-YIELD" || proceedFurther) && node._callee && node._callee._cx) {
            _LC.changeContext(node._callee._cx.node, contextSwitchArray, node._callee._cx);
        }
        if(isYield) {
            let insertYield = node._registerYield;
            let callee = insertYield._callee;
            if(callee && callee._cx) {
                _LC.changeContext(callee._cx.node, contextSwitchArray, callee._cx);
            }
        }
        let indexValue, itemValue;
        if(contextSwitchInfo.type) {
            if(contextSwitchInfo.type === "for") {
                indexValue = node.getAttribute("index");
                itemValue = node.getAttribute("item");
                if(node._items.length === 0) {
                    return;
                }
            } else {
                indexValue = node.getAttribute("key");
                itemValue = node.getAttribute("value");
                if(Object.keys(node._items).length === 0) {
                    return;
                }
            }
            let callee = node._callee;
            let cmpData = _LC.getCmpData(callee.component.data);
            let initialItemValue = cmpData[itemValue];
            let initialIndexValue = cmpData[indexValue];
            let initialItemProp = callee._properties[itemValue];
            let initialIndexProp = callee._properties[indexValue];
            let initialPropDetails = {};
            
            let items = contextSwitchInfo.type === "for" ? node._currentItems : node._currentObject;
            cmpData[itemValue] = items[contextSwitchInfo.itemIndex];
            cmpData[indexValue] = contextSwitchInfo.itemIndex;
            callee._properties[itemValue] = node._items[contextSwitchInfo.itemIndex].itemProperty;
            callee._properties[indexValue] = node._items[contextSwitchInfo.itemIndex].indexProperty;
            let dummyObject = {"initialItemValue" : initialItemValue , "initialIndexValue" : initialIndexValue, "initialItemProp" : initialItemProp, "initialIndexProp" : initialIndexProp, "initialPropDetails" : initialPropDetails};
            contextSwitchArray.$push(dummyObject);
        } else {
            //handling for yield
            let dummyObject = {};
            let callee = node._registerYield._callee;
            let cmpData = _LC.getCmpData(callee.component.data);
            Object.keys(contextSwitchInfo.node._properties).forEach(function(key) {
                dummyObject[key] = {};
                dummyObject[key].value = cmpData[key];
                dummyObject[key].property = callee._properties[key];
                callee._properties[key] = contextSwitchInfo.node._properties[key];
                let ctxtData = _LC.getCmpData(contextSwitchInfo.node.component.data);
                cmpData[key] = ctxtData[key];
            }); 
            contextSwitchArray.$push(dummyObject);
        }
    },
    "removeContext" : function(node, contextSwitchArray, contextSwitchInfo, proceedFurther, oneLevel) {
        if(!contextSwitchInfo) {
            return;
        }
        let isYield = node.tagName === "LYTE-YIELD";
        if(!oneLevel) {
            if(node._cx && (!isYield || node._cx.node.tagName !== "LYTE-YIELD")) {
                _LC.removeContext(node._cx.node, contextSwitchArray, node._cx, node.tagName === "LYTE-YIELD" || proceedFurther);
            } else if((node.tagName === "LYTE-YIELD" || proceedFurther) && node._callee && node._callee._cx) {
                _LC.removeContext(node._callee._cx.node, contextSwitchArray, node._callee._cx)
            }
        }
        if(isYield) {
            let insertYield = node._registerYield;
            let callee = insertYield._callee;
            if(callee && callee._cx) {
                _LC.removeContext(callee._cx.node, contextSwitchArray, callee._cx);
            }
        }
        let indexValue, itemValue;
        if(contextSwitchInfo.type) {
            if(contextSwitchInfo.type === "for") {
                indexValue = node.getAttribute("index");
                itemValue = node.getAttribute("item");
                if(node._items.length === 0) {
                    return;
                }
            } else {
                indexValue = node.getAttribute("key");
                itemValue = node.getAttribute("value");
                if(Object.keys(node._items).length === 0) {
                    return;
                }
            }
            let callee = node._callee;
            let items = node._attributes.items;
            let removedObject;
            let cmpData = _LC.getCmpData(callee.component.data);
            if(contextSwitchInfo.__prop){
                removedObject = contextSwitchArray.$pop();
            }else{
                removedObject = contextSwitchArray.$shift();
            }
            
            cmpData[itemValue] = removedObject.initialItemValue;
            cmpData[indexValue] = removedObject.initialIndexValue;
            callee._properties[itemValue] = removedObject.initialItemProp;
            callee._properties[indexValue] = removedObject.initialIndexProp;
        } else {
            let callee = node._registerYield._callee;
            let removedObject = contextSwitchArray.$shift();
            let cmpData = _LC.getCmpData(callee.component.data);
            Object.keys(contextSwitchInfo.node._properties).forEach(function(key) {
                cmpData[key] = removedObject[key].value;
                callee._properties[key] = removedObject[key].property;
            });
        }
    },
    "objectUtils" : function(){
        return _LC.oF.apply(_LC, arguments);
    },
    "sortCommands" : function(array1, arrayB) {
        var retVal = {};
        if(array1 && array1.__target__){
            array1 = array1.__target__;
        }
        var arrayA = array1.slice();
        retVal.origianlArray = array1;
        var commands = [];
        
        for (let i = 0; i < arrayB.length; i++) {
            // var targetIndex = arrayA.findIndex((element) => element === arrayB[i]);
            let arrBi = arrayB[i];
            if(arrayB[i] && arrayB[i].__target__){
                arrBi = arrayB[i].__target__;
            }
            var targetIndex = arrayA.indexOf(arrBi);
        
            if (targetIndex === -1) {
            commands.push({
                type: 'Add',
                element: arrBi, 
                toIndex : i
            });
            arrayA.splice(i, 0, arrBi);
            } else {
            if (targetIndex !== i) {
                commands.push({
                type: 'Move',
                element: arrBi,
                fromIndex: targetIndex,
                toIndex: i
                });
                arrayA.splice(targetIndex, 1);
                arrayA.splice(i, 0, arrBi);
            }
            }
        }
        
        for (let i = arrayA.length - 1; i >= arrayB.length; i--) {
            commands.push({
            type: 'Remove',
            element: arrayA[i]
            });
            arrayA.splice(i, 1);
        }
        retVal.commands = commands;
        retVal.changedArray = arrayB;
        return retVal;
    },
    "oF" : function() {
        //@Slicer.catchAllErrorsStart 
        try{
        //@Slicer.catchAllErrorsEnd 
        let object = arguments[0];
        if(object && object.__target__){
            object = object.__target__;
        }
        let functionName = arguments[1];
        let property = arguments[2];
        let newValue = arguments[3];
        if(newValue && newValue.__target__){
            newValue = newValue.__target__;
        }
        let fromComponent = arguments[4];
        let fromStore = arguments[5];
        if(functionName === "add" && !fromComponent && !object._propNodes) {
            let obj = {type:"change","oldValue":object[property],"newValue":newValue,"item":property};
            _LC.set(object, property, newValue,undefined, fromStore,undefined,true);
            _LC.callObjectObservers(object,obj);
            return;
        }
        let options = {};
        options.type = functionName;
        options.property = property;
        if(!/^(add|delete)$/.test(functionName)) {
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC001", functionName);
            //@Slicer.developmentEnd
            return;
        }
        let bindings = object._bindings;
        if(functionName === "delete") {
            let obj = {type:"change","oldValue":object[property],"newValue":newValue,"item":property};
            _LC.set(object, property, undefined, undefined, fromStore,undefined,true);
            _LC.callObjectObservers(object,obj);
        } else {
            object[property] = newValue;
        }
        if(bindings) {
            let bind = bindings.toArrayLyte();
            for(let i=0;i<bind.length;i++) {
                let binding = bind[i];
                let forHelpers = binding._forHelpers;
                if(forHelpers) {
                    let helperBind = forHelpers.toArrayLyte();
                    for(let j=0;j<helperBind.length;j++) {
                        let forHelper = helperBind[j];
                        if(forHelper.getAttribute("is") != "forIn"){
                            continue;
                        }
                        let itemValue = forHelper.getAttribute("key");
                        //Need to check
//                            _LC.removeSelectedBindingDeep({[itemValue] :                        forHelper._items[property].itemProperty}, {[itemValue] : object[property]});
                        let contextSwitchArray = [];
                        if(functionName === "add") {
                            _LC.adCx(forHelper, contextSwitchArray);
                        }
                        forHelper._callee.updateForInHelper(forHelper, options);
                        if(functionName === "add") {
                            _LC.rmCx(forHelper, contextSwitchArray);
                        }
                        
                    }
                }
            }
        }
        if(functionName === "delete") {
            delete object[property];
        }
        //@Slicer.catchAllErrorsStart
        }catch(err){
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(err)
        }
        //@Slicer.catchAllErrorsEnd 
    },
    "arrayUtils" : function(){
        return _LC.aF.apply(_LC, arguments);
    },
    "aF" : function() {
        //@Slicer.catchAllErrorsStart
        try{
        //@Slicer.catchAllErrorsEnd 
        var argumentsArr = Array.from(arguments);
        var fromOverride = false;
        if(argumentsArr[0] && typeof argumentsArr[0] == "object" && argumentsArr[0].fromOverride){
            argumentsArr.$shift();
            fromOverride = true;
        }
        let array = argumentsArr[0];
        if(array && array.__target__){
            array = array.__target__;
        }
        let initialArrLength = array.length;
        let callLengthObserver = true;
        let functionName = argumentsArr[1];
        let value = arguments[3],check={};
        if(value && value.__target__){
            value = value.__target__;
        }
        if(/^(replaceAt|removeAt|shift)$/.test(functionName) && !array.length) {
            //@Slicer.developmentStart
            _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn(functionName + " operation cannot be performed on empty array");
            //@Slicer.developmentEnd
            return;
        }
        let commands;
        if(functionName == "sort") {
            var originalArray = arguments[0];
            var sortFunction = arguments[2];
            var addedArguments = arguments[3] || [];
            var dummyArray = originalArray.slice();
            addedArguments.forEach(function(item) {
                dummyArray.push(item);
            });
            if(typeof arguments[2] == "function") {
                dummyArray.sort(sortFunction);
            } else {
                var obj = arguments[2];
                var key = obj.sortBy;
                var order = obj.sortOrder;
                function sorting(item1, item2) {
                    var item1 = key ? item1[key] : item1;
                    var item2 = key ? item2[key] : item2;
                    if(item1 > item2) {
                        return order ? 1 : -1;
                    } else if(item1 < item2) {
                        return order ? -1 : 1;
                    } else {
                        return 0;
                    }
                }
                dummyArray.sort(sorting)
            }
            commands = this.sortCommands(originalArray, dummyArray).commands;
        }
        let commArgs = arguments[2], oldVal, obsObj, watch = [];
        if(commArgs && commArgs.__target__){
            commArgs = commArgs.__target__;
        } 
        if(array._scp && /^(replaceAt|splice|removeAt|remove|insertAt)$/.test(functionName)){
            array._scp.forEach(function(_obj, id){
                var rec, attr, wobj = {};
                var recObj = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.nestScp[id];
                var obj = _obj.paths;
                var pathArr = Object.keys(obj), aPath;
                if(pathArr.length > 1){
                    aPath = "*";
                }
                else if(pathArr.length == 1){
                    aPath = pathArr[0];
                }
                wobj.path = aPath;
                wobj.id = id;
                if(pathArr.length > 1){
                    wobj.paths = pathArr;
                }
                wobj.attr = attr;
                wobj.data = recObj.data;
                wobj.reInit = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(recObj.data) || (Array.isArray(recObj.data) && (recObj.data.schema || recObj.data.model) && recObj.data.add);
                wobj.PropsInfo = recObj.PropsInfo || undefined;
                wobj.index=commArgs;
                if(recObj.model){
                    var mMap = recObj.model;
                    wobj.Error = recObj.Error;
                    var mKeys = Array.from(recObj.model.keys());
                    for(var i=0; i<mKeys.length; i++){
                        var mName = mKeys[i];
                        var pkMap = mMap.get(mName);
                        if(pkMap){
                            var pkArr = Array.from(pkMap.keys());
                            var pkLen = pkArr.length;
                            for(var j=0; j<pkLen; j++){
                                var mPk = pkArr[j];
                                var attrMap = pkMap.get(mPk);
                                var attrArr = Array.from(attrMap.keys());
                                var attrLen = attrArr.length;
                                for(k=0; k<attrLen; k++){
                                    var mAttr = attrArr[k];
                                    var mObj = Object.assign({}, wobj);
                                    if(recObj.db.getSchema){
                                        var mRec = recObj.db.cache.getEntity({schema:recObj.db.getSchema(mName), pK:mPk});
                                    }
                                    else{
                                        var mRec = recObj.db.cache.getEntity({model:recObj.db.getModel(mName), pK:mPk});
                                    }
                                    if(mRec){
                                        var model = mRec.$.schema ? mRec.$.schema : mRec.$.model;
                                        var field = model.fieldList[mAttr];
                                        if(field && (field.properties || field.items || field.watch == true)){
                                            mObj.data = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(mRec[mAttr]);
                                            mObj.rec = mRec;
                                            mObj.isRec = true;
                                            mObj.attr = mAttr;
                                            mObj.dtype = model.fieldList[mAttr];
                                            mObj._cmpErr=mRec.$.error;
                                            mObj.key=mAttr;
                                        }
                                        // mObj.reInit = true;
                                        watch.push(mObj);
                                    }
                                }
                            }
                        }
                    }
                } 
                else{
                    wobj.reInit = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.isEntity)(recObj.data) || (Array.isArray(recObj.data) && (recObj.data.schema || recObj.data.model ) && recObj.data.add);
                    watch.push(wobj);
                }
            });
            watch.forEach(function(val){
                var id = val.id,
                path=val.path == "" ? [] : val.path.split("."),
                dtype=val.dtype || undefined;
                if(val.isRec){
                    // path.shift();
                    // path = path.length == 1 && path[0] == property ? []:path;
                    check.Prop = dtype;
                    (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkNestedProp)(id,path,dtype,val,array,path,value,check);
                }
                if(val.PropsInfo){
                    val.PropsInfo.forEach(function(props){
                        props.path = val.path ;
                        dtype = props.dtype;
                        props.index = val.index;
                        check.Prop = dtype;
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkNestedProp)(id,path,dtype,props,array,path,value,check);
                    })
                }
            })
            //@Slicer.developmentStart
            if (check.value && check.value.code) {
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC019", check.Prop.type.name);
                // return { code:"LC019", message: ComponentError.getErrorMessage("LC019", check.Prop.type.name) };
            }
            //@Slicer.developmentEnd
        }  
        switch(functionName) {
        case "replaceAt" : 
            {
            let index = parseInt(argumentsArr[2]);
            if(index > array.length) {
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("index provided for replaceAt is greater than array length");
                return [];
            }
            //let args = Array.prototype.slice.call(argumentsArr, 3);
            let args = argumentsArr[3];
            if(!(Array.isArray(args))) {
                args = [args];
            }
            else if(fromOverride){
                args = [args];
            }
            let deletedItems = array.$splice.apply(array, [index, 1].$concat(args));
            let options = {"firstIndex" : index, "secondIndex" : args.length, "type" : "replace"};
            //All references updated by now
            if(options.secondIndex == 1){
                callLengthObserver = false;
            }
            //remove binding from previous object
            if(array._bindings) {
                let objbind = array._bindings.toArrayLyte();
                for(let i=0;i<objbind.length;i++){
                    let item = objbind[i];
                    if(item._forHelpers) {
                        let helperbind = item._forHelpers.toArrayLyte();
                        for(let j=0;j<helperbind.length;j++){
                            let helper = helperbind[j];
                            if(helper.getAttribute("is") != "for"){
                                continue;
                            }
                            /*if(helper.hasAttribute("unbound")) {
                                continue;
                            }*/
                            let finalIndex = index + deletedItems.length;
                            let itemValue = helper.getAttribute("item");
                            for(let i=index, j=0;i<finalIndex;i++,j++) {
                                _LC.removeSelectedBindingDeep({[itemValue] : helper._items[i].itemProperty}, {[itemValue] : deletedItems[j]});
                            }
                            let contextSwitchArray = [];
                            _LC.adCx(helper, contextSwitchArray);
                            helper._callee.updateForHelper(helper, options);
                            _LC.rmCx(helper, contextSwitchArray);
                            
                        }
                    }
                    for(let key in item) {
                        let parsedKey = parseInt(key);
                        let cond = (options.secondIndex == 1) ? (parsedKey == options.firstIndex) : (parsedKey >= options.firstIndex);
                        if(!isNaN(parsedKey) && cond) {
                            let diff = parsedKey - options.firstIndex;
                            let oldObject;
                            if(diff < 1) {
                                oldObject = deletedItems[diff];
                            } else {
                                oldObject = array[options.firstIndex - 1  + options.secondIndex + diff];
                            }
                            this.removeSelectedBindingDeep(item[key], oldObject);
                            if(item[key]._forHelpers) {
                                let bindfor = item[key]._forHelpers.toArrayLyte();
                                for(var j=0;j<bindfor.length;j++){
                                    let item1 = bindfor[j];
                                    if(item1._propBindingObject) {
                                        this.removeSelectedBindingDeep(item1._propBindingObject, oldObject);
//                                          value._bindings.add(item._propBindingObject);
//                                          this.establishBindings(item._propBindingObject, value);
                                    }
                                }
                            }
                            if(array[parsedKey] && typeof array[parsedKey] === "object") {
                                makeSet(array[parsedKey], "_bindings");
                            this.establishBindings({"dummy" : item[key]},{"dummy" : array[parsedKey]});
                            }
                            this.affectChanges(item[key],undefined,oldObject,undefined,array[parsedKey]);
                            if(options.secondIndex == 1){
                                break; 
                            }
                        }
                    }
                }
            }
            obsObj = {type:"array",insertedItems:args, removedItems:deletedItems, index:index};
            _LC.bindWatchObj(watch, array, args, deletedItems, index);
            _LC.callArrayObservers(array,obsObj,callLengthObserver,initialArrLength);
            return deletedItems[0];
            }
            break;
        case "splice" : {
            let index = parseInt(argumentsArr[2]);
            if(index > array.length) {
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("index provided is greater than array length");
                return [];
            }
            let toBeDeleted = argumentsArr.length > 3 ? argumentsArr[3] : array.length - index;
            let args;
            let skip = false;
            if(argumentsArr.length > 4){
                args = argumentsArr[4]
            }else{
                args = []
                skip = true;
            }
            if(!(Array.isArray(args))) {
                args = [args];
            }
            else if(!skip && fromOverride){
                args = [args];
            }
            let deletedItems = array.$splice.apply(array, [index, toBeDeleted].$concat(args));
            let options = {"firstIndex" : index, "secondIndex" : args.length, "thirdIndex" : toBeDeleted, "type" : "splice"};
            //All references updated by now
            if(options.secondIndex == options.thirdIndex){
                callLengthObserver = false
            }
            //remove binding from previous object
            if(array._bindings) {
                let objbind = array._bindings.toArrayLyte();
                for(let i=0;i<objbind.length;i++){
                    let item = objbind[i];
                    if(item._forHelpers) {
                        let helperbind = item._forHelpers.toArrayLyte();
                        for(let j=0;j<helperbind.length;j++){
                            let helper = helperbind[j];
                            /*if(helper.hasAttribute("unbound")) {
                                continue;
                            }*/
                            if(helper.getAttribute("is") != "for"){
                                continue;
                            }
                            let finalIndex = index + deletedItems.length;
                            let itemValue = helper.getAttribute("item");
                            for(let i=index, j=0;i<finalIndex;i++,j++) {
                                _LC.removeSelectedBindingDeep({[itemValue] : helper._items[i].itemProperty}, {[itemValue] : deletedItems[j]});
                            }
                            let contextSwitchArray = [];
                            _LC.adCx(helper, contextSwitchArray);
                            helper._callee.updateForHelper(helper, options);
                            _LC.rmCx(helper, contextSwitchArray);
                            
                        }
                    }
                    for(let key in item) {
                        let parsedKey = parseInt(key);
                        var cond = options.secondIndex == options.thirdIndex ? key >= options.firstIndex && key < (options.firstIndex+options.secondIndex) : parsedKey >= options.firstIndex;
                        if(!isNaN(parsedKey) && cond) {
                            let diff = parsedKey - options.firstIndex;
                            let oldObject;
                            if(diff < options.thirdIndex) {
                                oldObject = deletedItems[diff];
                            } else {
                                oldObject = array[options.firstIndex - options.thirdIndex  + options.secondIndex + diff];
                            }
                            this.removeSelectedBindingDeep(item[key], oldObject);
                            if(item[key]._forHelpers) {
                                let bindfor = item[key]._forHelpers.toArrayLyte();
                                for(var j=0;j<bindfor.length;j++){
                                    let item1 = bindfor[j];
                                    if(item1._propBindingObject) {
                                        this.removeSelectedBindingDeep(item1._propBindingObject, oldObject);
//                                          value._bindings.add(item._propBindingObject);
//                                          this.establishBindings(item._propBindingObject, value);
                                    }
                                }
                            }
                            if(array[parsedKey] && typeof array[parsedKey] === "object") {
                                makeSet(array[parsedKey], "_bindings");
                            this.establishBindings({"dummy" : item[key]},{"dummy" : array[parsedKey]});
                            }
                            this.affectChanges(item[key],undefined,oldObject,undefined,array[parsedKey]);
                        }
                    }
                }
            }
            obsObj = {type:'array',index:index,insertedItems:args, removedItems:deletedItems};
            _LC.bindWatchObj(watch, array, args, deletedItems, index);
            _LC.callArrayObservers(array,obsObj,callLengthObserver ,initialArrLength);
            return deletedItems;
        }
        break;
        case "push" : 
            {
            let toPush = argumentsArr[2];
            if(fromOverride && argumentsArr.length > 3){
                toPush = argumentsArr.$splice(2,argumentsArr.length);
            }
            else if(!(Array.isArray(toPush))) {
                toPush = [toPush];
            }
            else if(fromOverride){
                toPush = [toPush];
            }
            _LC.aF(array, 'insertAt', array.length, toPush);
            }
            break;
        case "pop" : 
               return _LC.aF(array, 'remove', array.length -1)[0];
            break;
        case "shift" : 
        case "shiftObject" :
              return _LC.aF(array, 'remove', 0)[0];
              break;
        case "removeAt" : 
        case "remove" : 
            {
            let index = parseInt(argumentsArr[2]);
            if(index > array.length) {
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("index provided for removeAt is greater than array length");
                return [];
            }
            let length = argumentsArr[3] ? parseInt(argumentsArr[3]) : 1;
            let options = {"firstIndex" : index, "secondIndex" : length, "type" : "remove"};
            let deletedItems = array.$splice(index,length);
            if(array._bindings) {
                let objbind = array._bindings.toArrayLyte();
                for(let i=0;i<objbind.length;i++){
                    let item = objbind[i];
                    if(item._forHelpers) {
                        let helperbind = item._forHelpers.toArrayLyte();
                        for(let j=0;j<helperbind.length;j++){
                            let helper = helperbind[j];
                            /*if(helper.hasAttribute("unbound")) {
                                continue;
                            }*/
                            if(helper.getAttribute("is") != "for"){
                                continue;
                            }
                            let finalIndex = index + deletedItems.length;
                            let itemValue = helper.getAttribute("item");
                            for(let i=index, j=0;i<finalIndex;i++,j++) {
                                _LC.removeSelectedBindingDeep({[itemValue] : helper._items[i].itemProperty}, {[itemValue] : deletedItems[j]});
                            }
                            let contextSwitchArray = [];
                            _LC.adCx(helper, contextSwitchArray);
                            helper._callee.updateForHelper(helper, options);
                            _LC.rmCx(helper, contextSwitchArray);
                            
                        }
                       }
                        for(let key in item) {
                        let parsedKey = parseInt(key);
                        if(!isNaN(parsedKey) && parsedKey >= options.firstIndex) {
                            let diff = parsedKey - options.firstIndex;
                            let oldObject;
                            if(diff < options.secondIndex) {
                                oldObject = deletedItems[diff];
                            } else {
                                oldObject = array[options.firstIndex - options.secondIndex + diff];
                            }
                            this.removeSelectedBindingDeep(item[key], oldObject);
                            if(item[key]._forHelpers) {
                                let bindfor = item[key]._forHelpers.toArrayLyte();
                                for(var j=0;j<bindfor.length;j++){
                                    let item1 = bindfor[j];
                                    if(item1._propBindingObject) {
                                        this.removeSelectedBindingDeep(item1._propBindingObject, oldObject);
//                                          value._bindings.add(item._propBindingObject);
//                                          this.establishBindings(item._propBindingObject, value);
                                    }
                                }
                            }
                            if(array[parsedKey] && typeof array[parsedKey] === "object") {
                                makeSet(array[parsedKey], "_bindings");
                            this.establishBindings({"dummy" : item[key]},{"dummy" : array[parsedKey]});
                            }
                            this.affectChanges(item[key],undefined,oldObject,undefined,array[parsedKey]);
                        }
                    }
                }
            }
            obsObj = {type:"array",removedItems:deletedItems,index:index};
            _LC.bindWatchObj(watch, array, undefined, deletedItems, index);
            _LC.callArrayObservers(array,obsObj,callLengthObserver ,initialArrLength); 
            return deletedItems;
            }
            break;
        case "removeObject" : 
                commArgs = [commArgs];
        case "removeObjects" :
            if(!(Array.isArray(commArgs))) {
                commArgs = [commArgs];
            }
            else if(fromOverride){
                commArgs = [commArgs];
            }
            if(commArgs == array){
                commArgs = Array.from(commArgs);// both array are same instance so cloning
            }
            for(var i=0;i<commArgs.length;i++) {
                var indVal = commArgs[i];
                if(indVal && indVal.__target__){
                    indVal = indVal.__target__;
                }
                let inde = array.indexOf(indVal);
                if(inde !== -1) {
                   _LC.aF(array, 'removeAt', inde);                         
                }
            }
            //Lyte.arrayUtils(array, 'removeObject', actObj);
            //Lyte.arrayUtils(array, 'removeObjects', []);
        break;
        case "unshift" : 
        case "unshiftObject" : 
        case "unshiftObjects" : 
            //_LC.aF.apply(_LC, [array, 'insertAt', 0].$concat(Array.prototype.slice.call(argumentsArr, 2)));
            {
            let toPush = argumentsArr[2];
            if(!(Array.isArray(toPush))) {
                toPush = [toPush];
            }
            else if(fromOverride){
                toPush = [toPush];
            }
            _LC.aF(array, 'insertAt', 0, toPush);
            }
            break;
        case "insertAt" : 
            {
            let index = parseInt(argumentsArr[2]);
            //let args = Array.prototype.slice.call(argumentsArr, 3);
            let args = argumentsArr[3];
            let len = args.length;
            if(!(Array.isArray(args))) {
                args = [args];
            }
            else if(fromOverride){
                args = [args];
            }
            for(let i=index;i>array.length;i--) {
                args.$unshift(undefined);
                index--;
            }
            let options = {"firstIndex" : index, "secondIndex" : args.length, "type" : "insert"};
            array.$splice.apply(array, [index, 0].$concat(args));
            if(array._bindings) {
                let arrbind = array._bindings.toArrayLyte();
                for(let i=0;i<arrbind.length;i++){
                    let item = arrbind[i];
                    if(item._forHelpers) {
                        let forbind = item._forHelpers.toArrayLyte();
                        for(let j=0;j<forbind.length;j++){
                            /*if(forbind[j].hasAttribute("unbound")) {
                                continue;
                            }*/
                            let helper = forbind[j];
                            if(helper.getAttribute("is") != "for"){
                                continue;
                            }
                            let contextSwitchArray = [];
                            _LC.adCx(helper, contextSwitchArray);
                            helper._callee.updateForHelper(helper, options);
                            _LC.rmCx(helper, contextSwitchArray);
                            
                        }
                    }
                    for(let key in item) {
                        let parsedKey = parseInt(key);
                        if(!isNaN(parsedKey) && parsedKey >= options.firstIndex) {
                            this.removeSelectedBindingDeep(item[key], array[parsedKey+options.secondIndex]);
                            if(item[key]._forHelpers) {
                                let bindfor = item[key]._forHelpers.toArrayLyte();
                                for(var j=0;j<bindfor.length;j++){
                                    let item1 = bindfor[j];
                                    if(item1._propBindingObject) {
                                        this.removeSelectedBindingDeep(item1._propBindingObject, oldObject);
//                                          value._bindings.add(item._propBindingObject);
//                                          this.establishBindings(item._propBindingObject, value);
                                    }
                                }
                            }
                            if(array[parsedKey] && typeof array[parsedKey] === "object") {
                                makeSet(array[parsedKey], "_bindings");
                            this.establishBindings({"dummy" : item[key]},{"dummy" : array[parsedKey]});    
                            }
                            this.affectChanges(item[key],undefined,array[parsedKey + options.secondIndex],undefined,array[parsedKey]);
                        }
                    }
                }
            }
            let position = parseInt(argumentsArr[2]);
            obsObj = {type:"array",insertedItems:(!(Array.isArray(argumentsArr[3]))) ? [argumentsArr[3]]: argumentsArr[0].slice(position,position+len),index:position};
            _LC.bindWatchObj(watch, array, args, undefined, position);
            _LC.callArrayObservers(array,obsObj,callLengthObserver ,initialArrLength);                
            }
            break;
        case "concat" : 
            //_LC.aF.apply(_LC, [array, 'insertAt',array.length].$concat(argumentsArr[2]));
            _LC.aF(array, 'insertAt', array.length, argumentsArr[2]);
            break;
        case "sort" : 
            let optionsArray = [];
            commands.forEach(function(command) {
                switch(command.type) {
                    case "Remove" : {
                        let removedItems = array.splice(command.toIndex, 1);
                        optionsArray.push({"firstIndex" : command.toIndex, "secondIndex" : 1, "type" : "remove", "removedItem" : removedItems[0]});
                    }
                    break;
                    case "Move" : {
                        let removedItems = array.splice(command.fromIndex, 1);
                        array.splice(command.toIndex, 0, command.element);
                        optionsArray.push({"firstIndex" : command.fromIndex, "secondIndex" : 1, "type" : "remove", "removedItem" : removedItems[0]});
                        // optionsArray.push({"firstIndex" : command.toIndex, "secondIndex" : 1, "thirdIndex" : 0, "type" : "splice"});
                        optionsArray.push({"firstIndex" : command.toIndex, "secondIndex" : 1, "type" : "insert"});
                    }
                    break;
                    case "Add" : {
                        array.splice(command.toIndex, 0, command.element);
                        // optionsArray.push({"firstIndex" : command.toIndex, "secondIndex" : 1, "thirdIndex" : 0, "type" : "splice"});
                        optionsArray.push({"firstIndex" : command.toIndex, "secondIndex" : 1, "type" : "insert"});
                    }
                    break;
                }
            });
            if(array._bindings) {
                let arrbind = array._bindings.toArrayLyte();
                for(let i=0;i<arrbind.length;i++){
                    let item = arrbind[i];
                    if(item._forHelpers) {
                        let forbind = item._forHelpers.toArrayLyte();
                        for(let j=0;j<forbind.length;j++){
                            /*if(forbind[j].hasAttribute("unbound")) {
                                continue;
                            }*/
                            let helper = forbind[j];
                            if(helper.getAttribute("is") != "for"){
                                continue;
                            }
                            let contextSwitchArray = [];
                            _LC.adCx(helper, contextSwitchArray);
                            for(let j=0;j<optionsArray.length;j++) {
                                let optionItem = optionsArray[j];
                                // Removal of bindings
                                let itemValue = helper.getAttribute("item");
                                if(optionItem.type == "remove") {
                                    this.removeSelectedBindingDeep({[itemValue] : helper._items[optionItem.firstIndex].itemProperty}, {[itemValue] : optionItem.removedItem});
                                }
                                helper._callee.updateForHelper(helper, optionItem);
                            }
                            _LC.rmCx(helper, contextSwitchArray);
                            
                        }
                    }
//                         for(let key in item) {
//                             let parsedKey = parseInt(key);
//                             if(!isNaN(parsedKey) && parsedKey >= options.firstIndex) {
//                                 this.removeSelectedBindingDeep(item[key], array[parsedKey+options.secondIndex]);
//                                 if(item[key]._forHelpers) {
//                                     let bindfor = item[key]._forHelpers.toArrayLyte();
//                                     for(var j=0;j<bindfor.length;j++){
//                                         let item1 = bindfor[j];
//                                         if(item1._propBindingObject) {
//                                             this.removeSelectedBindingDeep(item1._propBindingObject, oldObject);
// //                                          value._bindings.add(item._propBindingObject);
// //                                          this.establishBindings(item._propBindingObject, value);
//                                         }
//                                     }
//                                 }
//                                 if(array[parsedKey] && typeof array[parsedKey] === "object") {
//                                 	makeSet(array[parsedKey], "_bindings");
//                                 this.establishBindings({"dummy" : item[key]},{"dummy" : array[parsedKey]});    
//                                 }
//                                 this.affectChanges(item[key],undefined,array[parsedKey + options.secondIndex],undefined,array[parsedKey]);
//                             }
//                         }
                }
                if(Array.isArray(arguments[3]) && arguments[3].length) {
                    obsObj = {type:"array",insertedItems:arguments[3]};
                    var indices = [];
                    arguments[3].forEach(function(item) {
                        indices.push(array.indexOf(item));
                    });
                    obsObj.indices = indices;
                    _LC.callArrayObservers(array,obsObj,callLengthObserver ,initialArrLength);
                }
            }
            break;
        default: 
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC002", functionName);
            //@Slicer.developmentEnd
            return;
        }
        return array;
    //@Slicer.catchAllErrorsStart
    }catch(err){
        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(err);
    }
    //@Slicer.catchAllErrorsEnd 
    },
    "callDeepObservers" : function (data, args, property ,callLengthObserver){
        var self = this;
        var keys = Array.from(data._scp.keys());
        keys.forEach(function(id){
            var _scp = data._scp.get(id), 
            scp = _scp.paths,
            pathArr = Object.keys(scp), 
            aPath;
            if(pathArr.length == 1){
                aPath = pathArr[0];
            }
            else if(pathArr.length > 1){
                aPath = pathArr;
            }
            var recObj = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.nestScp[id], 
            attr, 
            rec, 
            isRec = false;
            if(recObj.model){
                var mMap = recObj.model;
                var db = recObj.db;
                var mKeys = Array.from(recObj.model.keys());
                for(var i=0; i<mKeys.length; i++){
                    var mName = mKeys[i];
                    var pkMap = mMap.get(mName);
                    if(pkMap){
                        var pkArr = Array.from(pkMap.keys());
                        var pkLen = pkArr.length;
                        for(var j=0; j<pkLen; j++){
                            var mPk = pkArr[j];
                            var attrMap = pkMap.get(mPk);
                            var attrArr = Array.from(attrMap.keys());
                            var attrLen = attrArr.length;
                            for(k=0; k<attrLen; k++){
                                var mAttr = attrArr[k];
                                if(db.getSchema){
                                    var mRec = db.cache.getEntity({schema:db.getSchema(mName), pK:mPk});
                                }
                                else{
                                    var mRec = db.cache.getEntity({model:db.getModel(mName), pK:mPk});
                                }
                                self.deepObsBind(mRec, true, mAttr, args, pathArr, aPath, data, property, callLengthObserver)                                    
                            }
                        }
                    }
                }
            } 
            else{
                rec = recObj.data;
                self.deepObsBind(rec, isRec, attr, args, pathArr, aPath, data, property, callLengthObserver)
            }
        });
    },
    "deepObsBind":function(rec, isRec, attr, args, pathArr, aPath, data, property, callLengthObserver){
        var type = args.type
        var watchPath , propPath;
        if(rec._bindings){
            var newArgs = Object.assign({},args);
            var objbind = rec._bindings.toArrayLyte();
            for (var _i65 = 0; _i65 < objbind.length; _i65++) {
                propPath = aPath;
                var binding = objbind[_i65], obj = undefined;
                watchPath = objbind[_i65]._path
                if(isRec){
                    if(binding.hasOwnProperty(attr) && binding[attr].hasOwnProperty("*")){
                        obj = binding[attr]["*"];
                        watchPath = binding[attr]._path;
                    } 
                }
                else if(binding.hasOwnProperty("*")){
                    obj = binding["*"];
                }
                if(obj){
                    var obsbind = obj._observers ? obj._observers.toArrayLyte() : [], path;
                    if(property){
                        if(pathArr.length > 1){
                            var len = pathArr.length, nPathArr = [];
                            for(var i=0;i<len;i++){
                                nPathArr.push(pathArr[i] ? pathArr[i]+"."+property : property);
                            }
                            propPath = nPathArr;
                        }
                        else if(pathArr.length == 1){
                            propPath = aPath ? aPath.concat("." + property) : property;
                        }
                    }
                    for (var j = 0; j < obsbind.length; j++) {
                        var observer = obsbind[j];
                        newArgs.path = propPath;
                        newArgs.type = "deepChange";
                        newArgs.data = rec;
                        var argPath = obj._path.split('.');
                        argPath.$pop();
                        newArgs.item = argPath.join('.'); 
                        var exactPath = newArgs.index!=undefined?(propPath!=""?propPath+".":propPath)+newArgs.index:propPath
                        if(observer.callee && observer.callee.component){
                            //  observer.observer.value.call(observer.callee.component, newArgs);
                            var calldeep = true;
                            var _data = observer.callee.component.__data[newArgs.item];
                            var _watch = _data ? _data.watch : undefined;
                            if(_data && typeof _watch != "boolean"){
                                var watchArr = obsbind[j].observer.Jpath[watchPath];
                                calldeep = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkWatchPath)(exactPath , watchArr);
                            }
                            if(calldeep){
                                observer.observer.value.call(observer.callee.component, newArgs);
                            }
                        }
                        else{
                            var scope = data._setterScope ? data._setterScope : window;
                            if(Array.isArray(scope)){
                                var sLen = scope.length;
                                for(var k=0;k<sLen;k++){
                                    var itm = scope[k];
                                    var calldeep = true;
                                    if(newArgs.data && newArgs.hasOwnProperty("item")){
                                        var watchArr = newArgs.data.$.schema._fldGrps.watch[newArgs.item].watch
                                        if(Array.isArray(watchArr)){
                                            watchArr = obsbind[j].observer.Jpath[watchPath];
                                            calldeep = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkWatchPath)(exactPath , watchArr);
                                        }
                                    }
                                    if(calldeep){
                                        observer.observer.value.call(itm, newArgs);
                                    }
                                }
                            }
                            else{
                                var calldeep = true;
                                if(newArgs.data && newArgs.hasOwnProperty("item")){
                                    var watchArr = newArgs.data.$.schema._fldGrps.watch[newArgs.item].watch
                                    if(Array.isArray(watchArr)){
                                        watchArr = obsbind[j].observer.Jpath[watchPath];
                                        calldeep = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.checkWatchPath)(exactPath , watchArr);
                                    }
                                }
                                if(calldeep){
                                    observer.observer.value.call(scope, newArgs);
                                }
                            }
                        }
                    }
                }
                if (!callLengthObserver && type == "array" && binding.length) {
                    this.affectChanges(binding.length,undefined,initialArrLength,undefined,array.length,undefined,callLengthObserver);
                }
            }
        }
    },
    "callArrayObservers" : function(array, args ,callLengthObserver ,initialArrLength) {
        if(array._scp && array._scp.size){
            this.callDeepObservers(array, Object.assign({},args) ,undefined, true);          
        }
        if(array._bindings) {
            let objbind = array._bindings.toArrayLyte();
            for(let i=0;i<objbind.length;i++){
                let binding = objbind[i];
                let path = objbind[i]._path;
                if(binding._observers) {
                    let obsbind = binding._observers.toArrayLyte();
                    for(let j=0;j<obsbind.length;j++){
                        let observer = obsbind[j];
                        if(observer.isArrayObserver) {
                            if(args){
                                args.item = path;
                            }
                            if(observer.callee && observer.callee.component){
                                observer.observer.value.call( observer.callee.component, args);
                            }
                            else{
                                var scope = array._setterScope ? array._setterScope : window;
                                if(Array.isArray(scope)){
                                    var sLen = scope.length;
                                    for(var k=0;k<sLen;k++){
                                        var itm = scope[k];
                                        observer.observer.value.call( itm, args);                                                        
                                    }
                                }
                                else{
                                    observer.observer.value.call(scope, args);
                                }
                            }
                        }
                    }
                }
                if(binding.length) {
                    this.affectChanges(binding.length,undefined,initialArrLength,undefined,array.length,undefined,callLengthObserver);
                }
            }
        }
    },
    "callObjectObservers" : function(obj, args) {
        if(obj._bindings) {
            let objbind = obj._bindings.toArrayLyte();
            if(args.oldValue !== args.newValue){
                for(let i=0;i<objbind.length;i++){
                    let binding = objbind[i];
                    let path = objbind[i]._path;
                    if(binding["{}"]){
                        this.affectObservers(binding["{}"])
                    }
                    if(binding._objectObservers && binding._observers) {
                        let obsbind = binding._observers.toArrayLyte();
                        for(let j=0;j<obsbind.length;j++){
                            let observer = obsbind[j];
                            if(observer.isObjectObserver) {
                                if(args){
                                    if(args.item){
                                        args.item = path + "." + args.item;
                                    }else{
                                        args.item = path;
                                    }
                                }
                                observer.observer.value.call( observer.callee && observer.callee.component ? observer.callee.component : obj._setterScope? obj._setterScope : window , args);
                            }
                        }
                    }
                }
            }
        }
    },
    "bindWatchObj" : function(watch, data, insItems, remItems, pos){
        if(watch && watch.length){
            watch.forEach(function(wObj){
                if(wObj.isRec){
                    var db = wObj.rec.$.db;
                    if(db){
                        db.__dVC(wObj.rec, wObj.attr, data, wObj);
                    }
                }
                if(remItems){
                    remItems.forEach(function(itm){
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.removeNestScp)(itm, wObj.id, undefined, undefined, undefined, undefined, wObj.reInit ? wObj.data : undefined);
                    });
                }
                if(insItems){
                    insItems.forEach(function(itm,idx){
                        if(itm && ( Array.isArray(itm) || typeof itm == "object" )){
                            var pth = wObj.path.split(',');
                            pth.$push(pos+idx);
                            (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.bindObj)(itm, undefined, wObj.id,  pth && pth.length ? pth : [], undefined, undefined, true);
                        }
                    });
                }
                if(pos !== undefined){
                    var nestObj = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.nestScp[wObj.id];
                    if(nestObj && nestObj.cyclic){
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.removeNestScp)(nestObj._data, wObj.id);
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.bindObj)(nestObj._data, undefined, wObj.id, [], undefined, undefined, true);
                    }
                    else{
                        var insLen = insItems ? insItems.length : 0, newInd = pos+insLen;
                        var arr = data.slice(newInd);
                        arr.forEach(function(itm){
                            (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.removeNestScp)(itm, wObj.id);
                        });
                        arr.forEach(function(itm,idx){
                            if(itm && ( Array.isArray(itm) || typeof itm == "object" )){
                                var pth = wObj.path.split(',');
                                pth.$push(newInd+idx);
                                (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.bindObj)(itm, undefined, wObj.id,  pth && pth.length ? pth : [], undefined, undefined, true);
                            }
                        });
                    }
                }
            });
        }
    },
    "establishUpdateBindings" : function(bindings, property, actualData) {
        let objbind = bindings.toArrayLyte();
        for(let i=0;i<objbind.length;i++){
            let item = objbind[i];
            if(item[property]) {
                addBindings(actualData,item[property]);
                this.establishBindings(item[property], actualData);
            }

        }
    },
    "establishSelectedBinding" : function(property, actualData,node) {
        if(!property) {
            return;
        }
        let propName = property._path;
        let props = propName.split('.');
        let currentProp = node.getProperty(props[0]);
        let currentValue = actualData[props[0]];
        for(let i=0;i<props.length;i++) {
            if(!currentValue || typeof currentValue !== "object") {
                break;
            } 
                addBindings(currentValue,currentProp);
                currentProp = currentProp[props[i+1]];
                currentValue = currentValue[props[i+1]];
        }
    },
    "establishBindings": function(properties, actualData, deepTriggerObj) {
        if(properties._helperNodes) {
            let path = properties._path;
            let arr = properties._helperNodes.toArrayLyte();
            for(let s=0;s<arr.length;s++) {
                let nodes = arr[s]._dynamicProperty ? arr[s]._dynamicProperty[path]: undefined;
                if(nodes) {
                    for(let j=0;j<nodes.length;j++) {
                        let node = nodes[j];
                        let helper = node.ownerElement;
                        if(helper && helper.tagName === "TEMPLATE" && helper.getAttribute("is") === "for") {
                            if(helper._items) {
                                let item = helper.getAttribute("item");
                                for(let i=0;i<helper._items.length;i++) {
                                    let data = actualData[i];
                                    let item = helper.getAttribute("item");
                                    if(data) {
                                        if(typeof helper._items[i] === "object") {
                                            this.establishBindings(helper._items[i].itemProperty, {[item] : data}, deepTriggerObj);
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
        let childTriggerObj;
        for(let i in properties) {
            let actData = actualData[i];
            if(i == "*"){
                deepTriggerObj =  properties["*"];
                continue;
            }
            else if(i == "{}"){
                childTriggerObj =  properties["{}"];
                continue;
            }
            if(!actData || typeof actData === "string" || typeof actData === "number" || typeof actData === "boolean") {
                addBindings(actualData,properties);
                this.deep.set(properties, deepTriggerObj, childTriggerObj); // handles new value addition
            } else {
                addBindings(actData,properties[i]);
                this.deep.set(properties, deepTriggerObj, childTriggerObj); // handles new value addition
                if(typeof properties[i] === "object") {
                    this.establishBindings(properties[i], actData, deepTriggerObj);
                }
            }
        }
    },
    "removeSelectedBindingDeep" : function(binding, actualData, fromDisConnect) {
        var del = "delete";
        if(!actualData && !fromDisConnect) {
            return;
        }
        if(actualData && actualData._bindings) {
            deleteBindingCheckSize(actualData, "_bindings", binding);
        }
        for(let i in binding) {
            let actData;
            if(actualData){
                actData = actualData[i];
            }   
            if(actData && actData._bindings) {
                deleteBindingCheckSize(actData, "_bindings", binding[i]);
            }
            if(typeof binding[i] === "object") {
                this.removeSelectedBindingDeep(binding[i], actData ,fromDisConnect);
            }
        }
        if(fromDisConnect){
            let dynNodes = binding._dynamicNodes;
            let helperNodes = binding._helperNodes;
            if(dynNodes && dynNodes.length){
                binding._dynamicNodes.$splice(0,dynNodes.length);   
            }
            if(helperNodes && helperNodes.size){
                helperNodes.clear();
            }
        }
        if(binding && binding._forHelpers) {
            let objbind = binding._forHelpers.toArrayLyte();
            for(let i=0;i<objbind.length;i++){
                let fH = objbind[i];
                if(fH.getAttribute("is") === "for") {
                    let item = fH.getAttribute("item");
                    let items = fH._attributes.items;
                    let itemCases = fH._items;
                    for(let i=0;i<itemCases.length;i++) {
                        this.removeSelectedBindingDeep(itemCases[i].itemProperty, items[i], fromDisConnect);
                    }
                } else {
                    if(fH._propBindingObject) {
                        if(actualData && actualData._bindings) {
                            deleteBindingCheckSize(actualData, "_bindings", fH._propBindingObject);
                            this.removeSelectedBindingDeep(fH._propBindingObject, actualData, fromDisConnect);
                        }
                    }
                }
            }
        }
    },
    "removeAllBindings" : function(properties, data) {
        var del = "delete";
        for(let key in properties) {
            if(data[key] && data[key]._bindings) {
//                    data[key]._bindings[del](properties[key]);
//                    if(!data[key]._bindings.size) {
//                        delete data[key]._bindings;
//                    }
                deleteBindingCheckSize(data[key], "_bindings", properties[key]);
            }
            if(data[key] && typeof data[key] !== "string") {
                _LC.removeAllBindings(properties[key], data[key]);
            }
        }
    },
    "affectObservers" : function(item, contextAlreadySwitched,oldValue,setterScope,newValue,stack,callLengthObserver,additionalInfo, fromChildBindings){
        if(item._observers) {
            let objbind = item._observers.toArrayLyte();
            let cond = callLengthObserver == false ? false : oldValue != undefined || newValue != undefined;
            if(cond){
                for(let i=0;i<objbind.length;i++){
                    let observer = objbind[i];
                    let obsObj = {type:"change",oldValue:oldValue,newValue:newValue, item: item._path};
                    if(observer.callee && observer.callee.component){
                        if(stack && observer.isObjectObserver){
                            obsObj.observer = observer;
                            stack.$push(obsObj);
                        }else{
                            observer.observer.value.call(  observer.callee.component, obsObj);                      
                        }
                    }
                    else{
                        var scope = setterScope ? setterScope : window;
                        if(Array.isArray(scope)){
                            var sLen = scope.length;
                            for(var k=0;k<sLen;k++){
                                var itm = scope[k];
                                observer.observer.value.call( itm, obsObj);
                            }
                        }
                        else{
                            observer.observer.value.call( scope, obsObj);                      
                        }
                    }
                }
            }
        }
    },
    "affectChanges" : function(item, contextAlreadySwitched,oldValue,setterScope,newValue,stack,callLengthObserver,additionalInfo, fromChildBindings, dontCallObserver, exactProp, options) {
        if(item._dynamicNodes) {
            for(let i=0;i<item._dynamicNodes.length;i++) {
                let node = item._dynamicNodes[i];
                if(node._deepValues && fromChildBindings && node._deepValues.indexOf(item._path) != -1){
                    continue;
                }
                item._dynamicNodes[i]._callee.updateNode(item._dynamicNodes[i], item._path, oldValue, newValue, exactProp, options);
            }
        }
        let propPath = item._path;
        if(item._helperNodes) {
            let nodes = [],itemHelperNodes = item._helperNodes.toArrayLyte();
            for(let s=0;s<itemHelperNodes.length;s++){
                let node = itemHelperNodes[s];
                let callee = node._callee;
                if(node.getAttribute("is") == "case"){
                    callee = node._parentSwitch._callee;
                }
                let _LCSV = callee.getDirectiveIns("save");
                if(!item._helperNodes.has(node) || (fromChildBindings && node._deepValues && node._deepValues.indexOf(item._path) != -1)) {
                    continue;
                }
                if(itemHelperNodes[s].getAttribute("is") === "for" && itemHelperNodes[s]._items) {
                    let innerContextSwitchArray = [];
                    _LC.adCx(itemHelperNodes[s], innerContextSwitchArray);
                    let  indexValue = itemHelperNodes[s].getAttribute("index");
                    let itemValue = itemHelperNodes[s].getAttribute("item");
                    let callee = itemHelperNodes[s]._callee;
                    let cmpData = _LC.getCmpData(callee.component.data);
                    let initialItemValue = cmpData[itemValue];
                    let initialIndexValue = cmpData[indexValue];
                    let initialItemProp = callee._properties[itemValue];
                    let initialIndexProp = callee._properties[indexValue];
                    let items = itemHelperNodes[s]._attributes.items;
                    
                    for(let i=0;i<itemHelperNodes[s]._items.length;i++) {
                        cmpData[itemValue] = items[i];
                        cmpData[indexValue] = i;
                        callee._properties[itemValue] = itemHelperNodes[s]._items[i].itemProperty;
                        if(itemHelperNodes[s]._items[i]._dynamicProperty[propPath]) {
                            nodes = itemHelperNodes[s]._items[i]._dynamicProperty[propPath];
                            for(let i=0;i<nodes.length;i++) {
                                nodes[i]._callee.updateNode(nodes[i], propPath, oldValue, newValue, exactProp, options);
                            }
                        }
                    }
                    cmpData[itemValue] = initialItemValue;
                    cmpData[indexValue] = initialIndexValue;
                    callee._properties[itemValue] = initialItemProp;
                    callee._properties[indexValue] = initialIndexProp;
                    
                    _LC.rmCx(itemHelperNodes[s], innerContextSwitchArray);                                    
                } else if(itemHelperNodes[s].getAttribute("is") === "forIn" && itemHelperNodes[s]._items) {
                    let innerContextSwitchArray = [];
                    _LC.adCx(itemHelperNodes[s], innerContextSwitchArray);
                    let  indexValue = itemHelperNodes[s].getAttribute("key");
                    let itemValue = itemHelperNodes[s].getAttribute("value");
                    let callee = itemHelperNodes[s]._callee;
                    let cmpData = _LC.getCmpData(callee.component.data);
                    let initialItemValue = cmpData[itemValue];
                    let initialIndexValue = cmpData[indexValue];
                    let initialItemProp = callee._properties[itemValue];
                    let initialIndexProp = callee._properties[indexValue];
                    let object = itemHelperNodes[s]._attributes.object;
                    for(let key in itemHelperNodes[s]._items) {
                        cmpData[itemValue] = object[key];
                        cmpData[indexValue] = key;
                        callee._properties[itemValue] = itemHelperNodes[s]._items[key].itemProperty;
                        if(itemHelperNodes[s]._items[key]._dynamicProperty[propPath]) {
                            nodes = itemHelperNodes[s]._items[key]._dynamicProperty[propPath];
                            for(let i=0;i<nodes.length;i++) {
                                nodes[i]._callee.updateNode(nodes[i], propPath, oldValue, newValue, exactProp, options);
                            }
                        }
                    }
                    cmpData[itemValue] = initialItemValue;
                    cmpData[indexValue] = initialIndexValue;
                    callee._properties[itemValue] = initialItemProp;
                    callee._properties[indexValue] = initialIndexProp;
                    _LC.rmCx(itemHelperNodes[s], innerContextSwitchArray);    
                } else {
                    nodes = itemHelperNodes[s]._dynamicProperty[item._path] || [];
                    let contextSwitchArray = [];
                    if(nodes.length) {
                        _LC.adCx(itemHelperNodes[s], contextSwitchArray);
                        for(let i=0;i<nodes.length;i++) {
                            nodes[i]._callee.updateNode(nodes[i], item._path, oldValue, newValue, exactProp, options);
                        }
                        _LC.rmCx(itemHelperNodes[s], contextSwitchArray);    
                    }
                    
                }
                
            }
        }
        if(!dontCallObserver){
            this.affectObservers(item, contextAlreadySwitched,oldValue,setterScope,newValue,stack,callLengthObserver,additionalInfo, fromChildBindings, dontCallObserver);
        }
        if(Array.isArray(item)){
            for(var i=0;i<item.length;i++){
                for(let key in item[i]) {
                    this.affectChanges(item[i][key], true,oldValue?(oldValue[i]?oldValue[i][key]:oldValue[i]):oldValue,setterScope,newValue?(newValue[i]?newValue[i][key]:newValue[i]):newValue, undefined, undefined, undefined, true);
                }
            }
        }
        else{
            for(let key in item) {
                var oldV = oldValue ? oldValue[key] : oldValue, newV = newValue ? newValue[key] : newValue;
                if(key == "*" && item[key].hasOwnProperty("_observers")){
                    oldV = oldValue;
                    newV = newValue
                }
                this.affectChanges(item[key], true, oldV, setterScope, newV,stack, undefined, undefined, true);
            }
        }
    },
    "executeObserver": function(stack){
        stack.forEach(function(obj){
            obj.observer.observer.value.call( obj.observer.callee && obj.observer.callee.component ? obj.observer.callee.component : setterScope ? setterScope : window ,{type:obj.type,oldValue:obj.oldValue,newValue:obj.newValue, item: obj.item});   
        });
    },
    "skipArgProcessing" : function(cloneActObj,ev,target) {
        if(cloneActObj.skipArgProcessing) {
            let args = cloneActObj.args = cloneActObj.actArgs;
            args.$shift();
            var eventIndex = args.indexOf("__lyteEvent__");
            var nodeIndex = args.indexOf("__lyteNode__");
            if(eventIndex !== -1) {
                args[eventIndex] = ev;
            } 
            if(nodeIndex !== -1) {
                args[nodeIndex] = target;
            }
        } else {
            cloneActObj.args.$shift();
        }
    },
    // getDV added
    // "getDV" : function(value){    
    //     var result = [],ref = result,arr = [],data = "",strStack = [],arrayStack = [],refStack = [],strLast,str;
    //     for(var i=0;i<value.length;i++){
    //         if(value[i] === "."){
    //             if(data.length){
    //                 ref.$push(data);
    //             }
    //             data = "";
    //             continue;
    //         }
    //         else if(value[i] === "["){
    //             arrayStack.$push(i)
    //             if(data.length){
    //                 ref.$push(data);
    //             }
    //             while(value[i+1] === "\s"){
    //                 i++;
    //             }
    //             if(value[i+1] === "\"" || value[i+1] === "'"){
    //                 strStack.$push(value[i+1]);
    //                 strLast = value[i+1];
    //                 i++;
    //             }
    //             else if(arr.length){
    //                 ref.$push([]);
    //                 refStack.$push(ref);
    //                 ref = ref[ref.length-1];
    //             }else{
    //                 arr.$push([]);
    //                 refStack.$push(ref);
    //                 ref = arr[arr.length-1];
    //             }
    //             data = "";
    //             continue;
    //         }
    //         else if((value[i] === "\"" || value[i] === "'" ) && value[i++] === strLast){
    //             while(value[i] === "\s" && value[i] != "]"){
    //                 i++;
    //             }
    //             strStack.$pop();
    //             str = true;
    //         }
    //         if(value[i] === "]"){
    //             arrayStack.$pop();
    //             if(data.length){
    //                 if(str === true){
    //                     ref.$push(data);    
    //                 }
    //                 else if(!isNaN(parseInt(data))){
    //                     if(refStack.length){
    //                         ref = refStack.$pop();
    //                         if(arr.length && Array.isArray(ref[ref.length-1]) && !ref[ref.length-1].length){
    //                             ref.$pop();
    //                         }
    //                         ref.$push(data);
    //                         if(!arrayStack.length && arr.length){
    //                             arr.$shift();
    //                         }
    //                     }
    //                 }
    //                 else{
    //                     ref.$push(data);
    //                 }
    //             }
    //             if(!arrayStack.length && arr.length){
    //                 result.$push(arr.$shift());
    //                 ref = result;
    //             }
    //             else if(refStack.length && !arr.length){ 
    //                 ref = refStack.$pop();
    //             }
    //             data = "";
    //             str = "";
    //             continue;
    //         }
    //         data = data.concat(value[i]);
    //     }
    //     if(data.length){
    //         result.$push(data);
    //     }
    //     if(strStack.length || arrayStack.length){
    //         console.log("check the syntax",strStack,arrayStack);
    //     }
    //     return result;
    // },
    "ccDelay" : [],
    "callCC" : function() {
        this.ccDelay.forEach(function(item) {
            if(item.component) {
                item.actualConnectedCallback();
            }
        });
        this.ccDelay = [];
    },
    "getDD":function(context,dataArr){
        var self = context;
        dataArr.forEach(function(item,index){
            if(Array.isArray(item)){
                if(context == undefined){
                    return undefined;
                }
                var inner = _LC.getDD(self,item);
                if(inner == undefined){
                    return undefined;
                }
                context = context[inner];
            }else{
                if(context == undefined){
                    return undefined;
                }
                context = context[item];
            }
        });
        return context;
    },

    "processStatic" : function(temp) {
        let arr = temp.innerHTML.split("__**");
        let newArr = [];
        for(var i=0;i<arr.length;i++) {
            if(arr[i].startsWith("--Lyte")) {
                newArr.$push(parseInt(arr[i].substring(6)));
            } else {
                newArr.$push(undefined);
            }
        }
        arr.cc = newArr;
        return arr;
    },
    "findLastNodeL" : function(lastNode1,count,node){
        var totalNodeIndex = 0;
        if(count != undefined){
            totalNodeIndex = count; 
        }
        var helperNode;
        switch(node.getAttribute("is")) {
            case "for" : 
                if(node._helpers[totalNodeIndex]){
                    helperNode = node._helpers[totalNodeIndex][0];   
                }
                if(!lastNode1) {
                    if(node._forContent[0]){
                        lastNode1 = node._forContent[0][0];
                    }
                }
                break;
            case "forIn" : 
                if(node._helpers[node._keysArray[0]]){
                    helperNode = node._helpers[node._keysArray[0]][0];
                }
                if(!lastNode1) {
                    if(node._forContent[node._keysArray[0]]){
                        lastNode1 = node._forContent[node._keysArray[0]][0];
                    }
                }                   
                break;
            case "if" : 
            case "switch" : 
                helperNode = node._helpers[totalNodeIndex];
                if(!lastNode1) {
                    lastNode1 = node._caseContent[0];
                }
            break;
            case "component" : 
                lastNode1 = lastNode1._renderedComponent[lastNode1._currentComponent] || lastNode1;
                return lastNode1;
        }
        if(!lastNode1){
            lastNode1 = node._placeHolder;
        }
        if(helperNode && (lastNode1 == helperNode._placeHolder)) {
            lastNode1 =  this.findLastNodeL(undefined, undefined, helperNode);
        }
        return lastNode1;
    },
    "processAction" : function(node) {
        Array.from(node.querySelectorAll('[lyteaction]')).forEach(function(item) { 
            let locIndex = item.attributes.lyteaction.value;
            item._boundEvents = _LC.fRP[locIndex];
            delete _LC.fRP[locIndex];
            // _LC.$pushFrc(locIndex);
            // item._boundEvents = JSON.parse(item.attributes.lyteaction.value);
            var _cx = item._boundEvents._cx;
            let boundEvents = item._boundEvents;
            delete boundEvents._cx;
            let componentName = boundEvents.componentName;
            delete boundEvents.componentName;
            for(var key in item._boundEvents) {
                if(key.indexOf("-") !== -1) {
                    item._actions = item._actions || {};
                    item._actions[key] = new CustomEvent(key);
                    item._actions[key].processAction = item._boundEvents[key];
                    item._boundEvents[key].actArgs.$shift();
                    let nodeIndex = item._boundEvents[key].actArgs.indexOf("__lyteNode__");
                    if(nodeIndex !== -1) {
                        item._boundEvents[key].actArgs[nodeIndex] = item;
                    }
                    item._boundEvents[key].args = item._boundEvents[key].actArgs;
                    delete item._boundEvents[key];
                } else {
                    let actArgs = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(item._boundEvents[key].args);
                    let actName = actArgs.$splice(0,1)[0];
                    actName = actName.startsWith("'")? actName.replace(/'/g,''):  actName;
                    let actString = getArgString(actName, actArgs);
                    item.setAttribute(key.startsWith("on") ? key.substr(2) : key ,componentName+" => "+ actString);
                    if(!item._boundEvents[key].globalEvent) {
                        item.addEventListener(key,globalEventHandler);
                    }
                }
            }
            item._boundEvents._cx = _cx;
            item.removeAttribute("lyteaction");
            //item._boundEvents = item.
        });
    },
    "getCtxVal" : function(context,val){
        if(context != undefined){
            
            return context[val];
        }else{
            return undefined;
        }
    },
    
    "get" : function(context, path, ac,cache, deepValues) {
        if(!ac) {
            ac = [];
        }
        if(cache && cache.hasOwnProperty(path)){// cache.cacheData[path]
            var nodeValue = cache[path]._data; /* put as local variable due to error. plz cross check - christo */
            if(cache[path]._dyn){
                cache[path]._dyn.forEach(function(item){
                    ac.$push(item);
                });
            }
            return nodeValue;
        }
        else{
        try{
            if(path.search(/^\$\./g)!=-1){
                return (0,_core_src_JsonPath_js__WEBPACK_IMPORTED_MODULE_9__.Jwalk)(context,path);
            }
            // else if(path == "$data"){
            //     return context;
            // }
            let arr = path.match(/([^[\]]+|\[\])/g);
            let initialContext = context;
            ac.$push(arr[0]);
            let locArr = arr[0].split('.'); 
            for(let k=0;k<locArr.length;k++) {
                if(locArr[k] == "*" || locArr[k] == "{}"){
                    let prevPath = path.slice(0, locArr[k] == "*" ? -2 : -3);
                    deepValues.push(path);
                    _LC.deep.getAllKeys(locArr[k] == "*" ? true : false, context, ac, prevPath, [], deepValues);
                }
                context = _LC.getCtxVal(context,locArr[k])
            }
            for(let i=1;i<arr.length;i++) {
                let locVal = arr[i];
                //this is context switching
                if(locVal.startsWith(".")) {
                    //direct context switching
                    let locArr = locVal.substring(1).split('.');
                    for(let k=0;k<locArr.length;k++) {
                        context = _LC.getCtxVal(context,locArr[k])
                    }
                    // ac[ac.length -1] = ac[ac.length - 1] + locVal;
                    ac[0] = ac[0] + locVal;
                } else if(locVal.startsWith("'") || locVal.startsWith('"') || !isNaN(locVal)) {
                    if(!isNaN(locVal)) {
                        // ac[ac.length-1] = ac[0] + "." + locVal;
                        ac[0] = ac[0] + "." + locVal;
                        context = _LC.getCtxVal(context,locVal)
                    } else {
                        // ac[ac.length-1] = ac[0] + "." + locVal.substring(1, locVal.length -1);
                        ac[0] = ac[0] + "." + locVal.substring(1, locVal.length -1);
                        context = _LC.getCtxVal(context,locVal.substring(1, locVal.length -1))
                    } 
                } else {
                    let length = ac.length;
                    let val = _LC.get(initialContext, locVal, ac, deepValues);
                    ac[0] = ac[0] + "." + val;
                    context = _LC.getCtxVal(context,val)
                }
                }
                if(cache){
                    cache[path] = {};
                    cache[path]._data = context;  
                    cache[path]._dyn = ac;
                }
            return context;
        } catch(e) {
            return undefined;
        }
        }
    },
    "getNew" : function(context, path) {
        try{
            let arr = path.match(/([^[\]]+|\[\])/g);
            let initialContext = context;
            let locArr = arr[0].split('.'); 
            if(arr.length < 2) {
                if(locArr.length <2) {
                    return {"context": initialContext, "lastKey" : locArr[0]};
                } else {
                    for(var k=0;k<locArr.length-1;k++) {
                        context = context[locArr[k]];
                    }
                    return {"context" : context, "lastKey" : locArr[k]}
                }
            }
            for(let k=0;k<locArr.length;k++) {
                context = context[locArr[k]];
            }
            for(var i=1;i<arr.length - 1;i++) {
                let locVal = arr[i];
                //this is context switching
                if(locVal.startsWith(".")) {
                    //direct context switching
                    let locArr = locVal.substring(1).split('.');
                    for(let k=0;k<locArr.length;k++) {
                        context = context[locArr[k]];
                    }
                } else if(locVal.startsWith("'") || locVal.startsWith('"') || !isNaN(locVal)) {
                    context = context[locVal.substring(1, locVal.length -1)];
                } else {
                    let val = this.get(initialContext, locVal);
                    context = context[val];
                }
                }
            let lastKey = arr[i];
            if(lastKey.startsWith(".")) {
                //direct context switching
                let locArr = lastKey.substring(1).split('.');
                let k=0
                for(;k<locArr.length - 1;k++) {
                    context = context[locArr[k]];
                }
                lastKey = locArr[k];
            } else if(lastKey.startsWith("'") || lastKey.startsWith('"') || !isNaN(lastKey)) {
                lastKey = lastKey.substring(1, lastKey.length -1);
            } else {
                lastKey = this.get(initialContext, lastKey);
            }
            return {"context" : context, "lastKey" : lastKey };
        } catch(e) {
            return undefined;
        }
    },
    "componentGetData" : function(key){
        if(key) {
            return this.get(key);
        } else {
            return this.data;
        }
    },
    "componentSetData" : function(arg0, arg1 ,options) {
        var arr = [];
        if(typeof arg0 === "string") {
            options = options || {}
            var ret = this.set(arg0, arg1 ,Object.assign(options,{skipTypeError:true}));
            if(ret && ret.code){
                arr.push(arg0);
            }
        } else if(typeof arg0 === "object") {
            var ret, arr=[];
            arg1 = arg1 || {};
            for(let key in arg0) {
                ret = this.set(key, arg0[key],Object.assign(arg1,{skipTypeError:true}));
                if(ret && ret.code){
                    arr.push(key);
                }
            }
        }
        if(arr.length){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this, "LC006", this.$node.localName, arr.toString());
            //@Slicer.developmentEnd
            return {code:"LC006", message:_utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.getErrorMessage("LC006", this.$node.localName, arr.toString())};
        }
        return true;
    },
    "componentToggleData" : function(dataName) {
        this.set(dataName,!this.get(dataName));
    },
    "componentGetMethods" : function(key) {
        if(key) {
            return this._methods[key];
        } else {
            return this._methods;
        }
    }, 
    "componentHasAction" : function(key) {
        if(key && this.$node._actions[key]) {
            return true;
        } else {
            return false;
        }
    }, 
    "componentGetActions" : function(key) {
        if(key) {
            return this.constructor._actions[key];
        } else {
            return this.constructor._actions;
        }
    }, 
    "componentGetObservers" : function() {
        return this.constructor._observers;
    },
    "componentsetActions" : function(arg0,arg1) {
        let actions = this.$node.constructor._actions;
        if(typeof arg0 === "string") {
            actions[arg0] = arg1;
        } else if(typeof arg0 === "object") {
            for(let key in arg0) {
                actions[key] = arg0[key];
            }
        }
    },
    "componentSetMethods" : function(arg0, arg1) {
        if(typeof arg0 === "string") {
            this._methods[arg0] = arg1;
        } else if(typeof arg0 === "object") {
            for(let key in arg0) {
                this._methods[key] = arg0[key];
            }
        }
    },
    "String" : {
        "cache_c":{},
        "cache_d":{},
        "upperCaseFirstLetter" : function(string) {
            return string.charAt(0).toUpperCase() + string.slice(1);
        },
        "lowerCaseFirstLetter" : function(string) {
            return string.charAt(0).toLowerCase() + string.slice(1);
        }, 
        "toCamelCase" : function(string) {
            if(!this.cache_c[string]){
                this.cache_c[string] = string.replace(/(-\w)/g, function(m){return m[1].toUpperCase();});
            }
            return this.cache_c[string];
        }, 
        "dasherize" : function(string) {
            if(!this.cache_d[string]){
                this.cache_d[string] = string.replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();
            }
            return this.cache_d[string];
        }
    },
    "appendChild" : function(outlet, component) {
        _LC.setIgnoreDisconnect(true);
        outlet.appendChild(component);
        _LC.setIgnoreDisconnect(false);
    },
    "replaceWith" : function() {
        var argumentsArr = Array.from(arguments);
        var oldNode = argumentsArr.$shift();
        _LC.setIgnoreDisconnect(true);
        var parentNode = oldNode.parentNode;
        for(var i=0,node;node=argumentsArr[i];i++) {
            _LC.insertBeforeNative(parentNode, node, oldNode);
        }
        oldNode.remove();
        _LC.setIgnoreDisconnect(false);
    },	
    // "removeIfCaseContent" : function(comp,node,direct){
    //     if(!direct){
    //         for(let tempName in node._tempList) {
    //             this.removeDynamicNodes(node._tempList[tempName],comp);
    //         }
    //     }
    // },
    // "removeYieldContent" : function(node,){
    //     if(!direct){
    //         for(let tempName in node._tempList) {
    //             this.removeDynamicNodes(node._tempList[tempName]);
    //         }
    //     }
    // },
    "insertBeforeNative" : function(parent, newNode, refNode) {
        parent.insertBefore(newNode, refNode);
    },
    "insertBefore" : function(referenceNode, newNode, parentNode) {
        _LC.setIgnoreDisconnect(true);
        if(!parentNode) {
            if(!referenceNode) {
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC005");
                //@Slicer.developmentEnd
                _LC.setIgnoreDisconnect(false);
                return;
            } else {
                parentNode = referenceNode.parentNode;
            }
        }
        _LC.insertBeforeNative(parentNode , newNode, referenceNode ? referenceNode : null);
        _LC.setIgnoreDisconnect(false);
    },
    "insertAfter" : function() {
        var argumentsArr = Array.from(arguments);
        var referenceNode = argumentsArr.shift();
        _LC.setIgnoreDisconnect(true);
        referenceNode.after.apply(referenceNode, argumentsArr);
        _LC.setIgnoreDisconnect(false);
    },
    "executeMethod" : function() {
        let args = Array.prototype.slice.call(arguments, 1);
        var methodName = _LC.String.toCamelCase(arguments[0]);
        if(!this._methods[methodName]) {
            //@Slicer.developmentStart
            let app = _LC.getNearestParentApp(this);
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this, "LC005", methodName, this.$node.tagName);
            //@Slicer.developmentEnd
            return;
        }
        return this._methods[methodName].apply(this, args);
    },
    // "getProperData" : function(obj) {
    //     var dataType = obj.dataType;
    //     var attr = obj.attr;
    //     var newValue = obj.newValue;
    //     var tagName = obj.tagName;
    //     switch(dataType) {
    //     case "boolean" : 
    //         {
    //             if(!newValue || newValue === "false") {
    //                 newValue= false;
    //             } else {
    //                 newValue = true;
    //             }
    //         }
    //         break;
    //     case "object" : 
    //         try{
    //             newValue = JSON.parse(newValue);
    //             if(!(newValue instanceof Object)) {
    //                 Lyte.warn("data type of the value provided for attribute "+ attr + " of " + tagName + " is not valid");
    //             }
    //         } catch(e) {
    //             Lyte.warn("attribute "+attr+ " is not a valid JSON string.");
    //             return;
    //         }
    //         break;
    //     case "array":
    //         try{
    //             newValue = JSON.parse(newValue);
    //             if(!(newValue instanceof Array)) {
    //                 Lyte.warn("data type of the value provided for attribute "+ attr + " of " + tagName + " is not valid");
    //             }
    //         } catch(e) {
    //             Lyte.warn("attribute "+attr+ " is not a valid JSON string.");
    //             return;
    //         }
    //         break;
    //     case "number":
    //         {
    //     let numValue = +newValue;
    //         if(newValue === numValue+"") {
    //             newValue = numValue;
    //         } else {
    //             Lyte.warn("data type of the value provided for attribute "+ attr + " of " + tagName + " is not valid");
    //             return;
    //         }
    //     }
    //         break;
    //     }
    //     obj.newValue = newValue;
    //     return true;
    // }, 
    "cssEscape" : function(string) {
        if(string) {
            return string.replace(/['"]/g, "\\$&");    
        } else {
            return string;
        }
        
    },
    "arrayOverride" : function(){
        let allArrFns = ["push","pop","splice","shift","unshift","concat","replaceAt","shiftObject","remove","removeAt","removeObject","removeObjects","unshiftObject","unshiftObjects","insertAt"];
        for(let i=0; i<allArrFns.length; i++){
            let fnName = allArrFns[i];
            Array.prototype[fnName] = (function() {
                return function() {
                    let actArgs = Array.from(arguments);
                    if(_LC){
                        let opt = {fromOverride : true};
                        actArgs.$unshift(opt,this,fnName);
                        return _LC.aF.apply(_LC, actArgs);
                    }else{
                        return this["$"+fnName].apply(this,actArgs);
                    }
                };
            })();
        }
    },
    "render" : function(componentName, data, outlet, _lyteOptions) {
        var component;
        ltCf.fromV4Render = true;
        let oldLyteV4 = ltCf.lyteV4;
        let setInnerHTML = false;
        let registryScope = this.isComponentRegistry && this.isComponentRegistry();
        if(_LC.validateRenderData(data,_lyteOptions)) {
            var currentReg = _LC.getCurrentRegistryIns();
            var currentRegClass = _LC.getCurrentRegistryIns();
            _LC.setCurrentRegistryIns(_lyteOptions && _lyteOptions.registryInstance ? _lyteOptions.registryInstance : registryScope ? this : undefined);
            _LC.setCurrentRegistry(_lyteOptions && _lyteOptions.registry ? _lyteOptions.registry : registryScope ? this.constructor : undefined);
            if(data && data.lyteV4){
                ltCf.lyteV4 = true;
            }
            if(componentName && typeof componentName == "string") {
                if(_lyteOptions && _lyteOptions.setInnerHTML){
                    setInnerHTML = true;
                    if(outlet) {
                        let actOutlet;
                        if(typeof outlet == "string"){
                            actOutlet = document.querySelector(outlet);
                        }else{
                            actOutlet = outlet;
                        }
                        if(actOutlet) {
                            actOutlet.innerHTML = componentName;
                        } else {
                            _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.error("LC008", outlet);
                        }
                    }
                }else{
                    //@Slicer.developmentStart
                    if(registryScope && !this._registeredComponents[componentName]){
                        _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Invalid Lyte Component name : '" + componentName + "'. It is not registered with Lyte.");
                    }
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Passing component name '" +componentName+ "' as string is depricated.");
                    //@Slicer.developmentEnd
                    component = createElement(componentName);
                }
            } else if(componentName && componentName._compName) {
                componentName._lyteOptions = _lyteOptions;
                component = createElement(componentName._compName);
                if(componentName._lyteOptions && componentName._lyteOptions.attributes && componentName._lyteOptions.attributes.length){
                    componentName._lyteOptions.attributes.forEach(function(attr){
                        if(typeof attr == "object"){
                            component.setAttribute(attr.name,attr.value);
                        }else{
                            component.setAttribute(attr,"");
                        }
                    })
                }
                componentName._lyteOptions = undefined;
            } else {
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC003");    
                //@Slicer.developmentEnd
                _LC.setCurrentRegistryIns(currentReg); 
                _LC.setCurrentRegistry(currentRegClass);
                ltCf.fromV4Render = false;
                ltCf.lyteV4 = oldLyteV4
                return;
            }
        } else {
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC0010");
            //@Slicer.developmentEnd
            _LC.setCurrentRegistryIns(currentReg);
            _LC.setCurrentRegistry(currentRegClass)
            ltCf.fromV4Render = false;
            ltCf.lyteV4 = oldLyteV4
            return;
        }
        
        if(data && !setInnerHTML){ 
            component.setData(data);
        }
        if(!setInnerHTML && _lyteOptions && _lyteOptions.methods){
            component.setMethods(_lyteOptions.methods);
        }
        if(_lyteOptions && _lyteOptions.attributes){
            _lyteOptions.attributes.forEach(function(attr){
                if(typeof attr == "object"){
                    component.setAttribute(attr.name,attr.value);
                }else{
                    component.setAttribute(attr,"");
                }
            })
        }
        if(_lyteOptions && _lyteOptions._route){
            component._route = _lyteOptions._route;
        }
        let initialDelayAll = _LC.getDelayDidConnect();
        let delayAll;
        if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue){
            delayAll = _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue.renderYield(_lyteOptions, component);
            if(delayAll){
                _LC.setDelayDidConnect(delayAll);
            }
        }
        if(!setInnerHTML && outlet) {
            let actOutlet;
            if(typeof outlet == "string"){
                actOutlet = document.querySelector(outlet);
            }else{
                actOutlet = outlet;
            }
            if(actOutlet) {
                if(_lyteOptions && _lyteOptions.clearOutlet){
                    actOutlet.innerHTML = "";    
                }
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._renderPosition(actOutlet, component, _lyteOptions);
                component._callee = component.getCallee ? component.getCallee(actOutlet) : undefined;
            } 
            //@Slicer.developmentStart
            else {
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ApiError.error("LC004", outlet);
            }
            //@Slicer.developmentEnd
        }
        _LC.setCurrentRegistryIns(currentReg);
        _LC.setCurrentRegistry(currentRegClass);
        ltCf.fromV4Render = false;
        ltCf.lyteV4 = oldLyteV4
        if(delayAll){
            _LC.setDelayDidConnect(initialDelayAll);
        }
        return component;
    },
    // "renderHTML" : function(string,outlet,options){
    //     var currentReg = _LC.getCurrentRegistryIns();
    //     _LC.setCurrentRegistryIns(options && options.registryInstance ? options.registryInstance : this)
    //     if(typeof string == "string") {
    //         if(outlet) {
    //             let actOutlet;
    //             if(typeof outlet == "string"){
    //                 actOutlet = document.querySelector(outlet);
    //             }else{
    //                 actOutlet = outlet;
    //             }
    //             if(actOutlet) {
    //                 actOutlet.innerHTML = string;
    //             } else {
    //                 Lyte.error("LC008", outlet);
    //             }
    //         }
    //     }
    //     _LC.setCurrentRegistryIns(currentReg);
    // },
    "removeForContent" : function(node){
        for(let s=0;s<node._forContent.length;s++) {
            for(let i=0;i<node._forContent[s].length; i++ ) {
                node._forContent[s][i].remove();
            }
        }
    },
    "removeForInContent" : function(node){
        for(var ind in node._forContent) {
            for(let i=0;i<node._forContent[ind].length; i++ ) {
                node._forContent[ind][i].remove();
            }
        }
    },
    "removeIfContent" : function(node){
        for(let i=0;i<node._caseContent.length; i++ ) {
            node._caseContent[i].remove();
        }
    },
    "removeIfCaseContent" : function(comp,node,direct){
        if(!direct){
            for(let tempName in node._tempList) {
                this.removeDynamicNodes(node._tempList[tempName],comp);
            }
        }
    },
    "removeYieldContent" : function(node){
        if(!direct){
            for(let tempName in node._tempList) {
                this.removeDynamicNodes(node._tempList[tempName]);
            }
        }
    },
    "removeDynamicNodes" : function(node,comp){
        for(let key in node._dynamicProperty) {
            if(node._dynamicProperty[key].isActualNode) {
                node._dynamicProperty[key].isActualNode._helperNodes[del](node);
            }else {
                let helperNodes = comp.getProperty(key)._helperNodes;
                if(helperNodes) {
                    helperNodes.delete(node);
                }
            }
        }
        node._dynamicProperty = {};
    },
    "insertInDom" : function(placeHolder,lastNode,toAppendMain){
        if(placeHolder) {
            _LC.insertBeforeNative(placeHolder.parentNode, toAppendMain, placeHolder);                	
        } else {
            _LC.insertBeforeNative(lastNode.parentNode,toAppendMain, lastNode);                
        } 
    },
    "appendInDom" : function(comp,content){
        comp.appendChild(content);
    },
    "setAttribute" : function(node,name,val){
        node.setAttribute(name,val);
    },
    "getErrorStructure": function (componentIns, key) {
        var cmpData = componentIns.data, data_Error = componentIns.data.errors;
        if (key.includes("$")) {
            key = key.replace("$.", "")
        }
        var cmpDataKey = key.split(".")
        var keyData = cmpData[cmpDataKey[0]];
        var _scpId = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.__nestScp__.get(keyData);
        if (_scpId) {
            var cmpDataIns = _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.nestScp[_scpId];
            var propInfo = cmpDataIns.PropsInfo || undefined;
            if (propInfo) {
                for (var i_err = 0; i_err < propInfo.length; i_err++) {
                    var _cp = propInfo[i_err];
                    if (_cp._cmpErr == data_Error && _cp.key == cmpDataKey[0]) {
                        if (_cp.Error[_cp.key] && _cp.Error[_cp.key].nested && cmpDataKey.length > 1) {
                            key = key.replaceAll("[", ".").replaceAll("]", "");
                            var errorPath = ""
                            cmpDataKey.splice(0, 1)
                            errorPath = cmpDataKey.join(".")
                            return _LC.get(_cp.Error[_cp.key].nested, errorPath)
                        }
                        else if (data_Error[cmpDataKey[0]]) {
                            return data_Error[cmpDataKey[0]]
                        }
                    }
                }
            }
        }
        // if(data_Error.hasOwnProperty(key)){
        //     var errorData=data_Error[key];
        //     if(errorData.hasOwnProperty("nested")){
        //         var nestedArrayError = [];
        //         _LC.collectErrorMessage(errorData.nested,nestedArrayError,key,"");
        //         return nestedArrayError;
        //     }else{
        //         return data_Error[key]
        //     }
        // }else{
        //     return;
        // }
    },
   "addLink" : function(comp,outlet,style,options,nearByApp){
        if(style.localName == "link"){
            let app = nearByApp || _LC.getNearestParentApp(comp.component);
            if(app.addLink){
                app.addLink.apply(app,[outlet,style,options]);
                return;
            }
        }
        _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._renderPosition(outlet, style);
    },
    "getBoundValue" : function(dataName){
        let ind = dataName.search(/\W/);
        let boundValue;
        if(ind !== -1) {
            boundValue = dataName.substring(0, ind);
        } else {
            boundValue = dataName;
        }
        return boundValue;
    },
    "map" : {
        "component" : new WeakMap(),
        "helper" : new WeakMap(),
        "registry" : new WeakMap(),
    },
    "iterator" : function(isType, data, def){
        if(isType == "for"){
            data.forEach(function(item , index){
                def.apply(this,[item,index]);
            })
        }else if(isType == "forIn"){
            for(let index in data){
                let item = data[index];
                def.apply(this,[item,index]);
            }
        }
    },
    "array" : {
        initalize : function(data, key){
            if(!data[key]){
                data[key] = [];
            }
        },
        checkAndPush : function(arr, val){
            if(arr.indexOf(val) == -1){
                arr.push(val);
            }
        }
    },
    "object" : {
        initalize : function(data, key){
            if(!data[key]){
                data[key] = {};
            }
        }
    },
    "nextTick" : function(callback){
        if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue){
            setTimeout(function(){
                callback.apply(this);
            },0)
        }else{
            callback.apply(this);
        }
    },
    "getYieldName" : function(node){
        if(node.getAttribute("yield-name")){
            return node.getAttribute("yield-name");
        }else if(node._attributes && node._attributes["yield-name"]){
            return node._attributes["yield-name"];
        }else if(node._initProperties){
            let yd = node._initProperties.yield;
            if(yd){
                if(yd.getAttribute("yield-name")){
                    return yd.getAttribute("yield-name");
                }else if(yd._attributes["yield-name"]){
                    return yd._attributes["yield-name"];
                }
            }
        }
    },
    "debugPerf" : function( app , id , cBname , cmpName ){
        
        if(typeof id == "object" ){
            var scope = _LC.getNearestParentApp(app.component);
            if(scope.config && (scope.config.performance || scope.config.debug)){
                if( id.render == 1 ){
                    var _id = Math.floor(Math.random() * 900000000).toString();
                    app.__renderId = _id;
                    app.setAttribute("__renderId__" , _id);
                    scope.log("Component - "+app.localName + " (" +app.$registry.constructor.name+ ") ->  " + app.__renderId  , "color:CornflowerBlue")
                    if(scope.config && scope.config.performance){
                        scope.time("["+app.__renderId + "] renderTime for " +app.localName);
                    }            
                }
                else if (id.render == 0){
                    if(scope.config && scope.config.performance){
                        scope.time("["+app.__renderId + "] renderTime for " +app.localName);
                    }
                }
            }
            return;
        }
        else if(app.config && app.config.performance){
            app.time("[" + id +"] " + cBname + " of " + cmpName +" component");
        }
        else if (app.config && app.config.debug){
            app.log("[" + id +"] " + cBname + " of " + cmpName +" component");
        }
    },
    "$variables" : function(comp){
        let initProperties = comp._initProperties;
        comp._initMethods = comp._initMethods || {};
        var $supportObj = {
            "$methods" : comp._initMethods,
            "$yields" : comp._yields,
            "$data" : comp._initProperties
        };
        for(let $name in $supportObj){
            let wholeObj = initProperties[$name];
            if(wholeObj){
                if(typeof wholeObj === "object") {
                    switch($name){
                        case "$methods" : {
                            let initPropArr = Object.keys(comp._initProperties);
                            let $methodIndex = initPropArr.indexOf($name);
                            for(let key in wholeObj) {
                                let itemIndex = initPropArr.indexOf(key);
                                if($methodIndex > itemIndex){
                                    let callee = comp._callee;
                                    if(callee){
                                        let newFunc = function(){
                                            var _meth = wholeObj[key].apply(callee.component,arguments);
                                            return _meth;
                                        }
                                        $supportObj[$name][key] = newFunc;
                                    }
                                }
                            }
                        }
                        break;
                        case "$yields":{
                            for(let key in wholeObj) {
                                $supportObj[$name][key] = wholeObj[key];
                            }
                        }
                        break;
                        case "$data":{
                            for(let key in wholeObj) {
                                if(key != "$methods" && key != "$yields"){
                                    $supportObj[$name][key] = wholeObj[key];
                                }
                            }
                        }
                    }
                }else{
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(comp,"LC018", comp.localName, $name);
                }
                delete initProperties[$name];
            }
        }
    },
    "$unbound" :function(attrObj, obj){
        if(attrObj){
            if(attrObj.name == "unbound"){
                obj.unbound = true;
            }else{
                attrObj.value.args.forEach(function(arg){
                    if(typeof arg == "object"){
                        _LC.$unbound(arg.value, obj);
                    }
                })
            }
        }
    }
    
}
// let _LCHD = _LC.directive.hide;

_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._renderPosition = function(outlet, newNode, options) {
    if(options && options.position){
        switch(options.position){
            case "before" : {
                if(options.hasOwnProperty('refNode')){
                    outlet.insertBefore(newNode, options.refNode);
                }else{
                    outlet.parentNode.insertBefore(newNode, outlet);
                }
            }
            break;
            case "after" : {
                outlet.after(newNode);
            }
            break;
            case "append" : {
                outlet.appendChild(newNode);
            }
            break;
            case "prepend" : {
                outlet.prepend(newNode);
            }
            break;
            case "replace" : {
                outlet.replaceWith(newNode);
            }
            break;
            case "default" : {
                outlet.appendChild(newNode);
            }
        }
    }else{
        outlet.appendChild(newNode);
    }
}
_LC.setData = function(arg0, arg1) {
    this._initProperties = this._initProperties || {};
    if(typeof arg0 === "string") {
        this._initProperties[arg0] = arg1
    } else if(typeof arg0 === "object") {
        for(let key in arg0) {
            this._initProperties[key] = arg0[key];
        }
    }
}
_LC.setMethods = function(arg0, arg1) {
    this._initMethods = this._initMethods || {};
    if(typeof arg0 === "string") {
        this._initMethods[arg0] = arg1
    } else if(typeof arg0 === "object") {
        for(let key in arg0) {
            this._initMethods[key] = arg0[key];
        }
    }
}
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._duplicateStyle = [];
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv._duplicateStyle = [];
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._compList = [];
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._shadowChild = [];
var div = document.createElement("div");
div.setAttribute("id","lessDiv");
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._lessDiv = div;
div._impNames = [];

_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._component.init = function(lyteIns){
    lyteIns.addEventListener("afterRouteTransition", function() {
        _LC.chromeBugFix();
    });
}
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._component.didConnect = function(lyteIns){
    //will be called after afterlookups
    if(lyteIns.getDefaultRegistry){
        let reg = lyteIns.getDefaultRegistry();
        if(typeof reg == "function"){
            if(_LC.validateRegistryClass(reg.name)){
                if(reg._instanceList.length){
                    reg = reg._instanceList[0];
                }else{
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC012", reg.name ,localName);    
                    return;
                }
            }else{
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC011", reg.name, localName);
                return;
            }
        }else if(typeof reg == "string"){
            let regClass = _LC.validateRegistryClass(reg,true)
            if(regClass){
                if(regClass._instanceList.length){
                    reg = regClass._instanceList[0];
                }else{
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC014", reg, localName);    
                    return;
                }
            }else{
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC013",reg, localName);
                return;
            }
        }
        else if(!_LC.isValidReg(reg)){
            let name =  reg ? reg.constructor ? reg.constructor.name : reg : reg ;
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC008", name, name);
            //@Slicer.developmentEnd
            return;
        }
        _LC.setDefaultRegistryIns(reg);
        _LC.setDefaultRegistry(reg.constructor);
        _LC.gotDefaultRegistryFrom = "user";
    }
    ComponentRegistry._registeredRegistries.forEach(function(obj){
        let regClass = obj.class;
        let name = obj.name;
        if(regClass._instanceList){
            regClass._instanceList.forEach(function(regIns){
                _LC.setAddedRegistries(regIns,regClass.name);
            })
        }
    })
}
//var toArrayLyte = "toArrayLyte";
//var bindStr = "_bindings";
//var compStr = "component";
//var forHelperStr = "_forHelpers";
//var dynamicNodesStr = "_dynamicNodes";
//var calleeStr = "_callee";
//var getAttributeStr = "getAttribute";
//var hasAttributeStr = "hasAttribute";
//var removeAttributeStr ="removeAttribute";
//var setAttributeStr = "setAttribute";
//var parentNodeStr = "parentNode";
//var nodeNameStr = "nodeName";
//var ownerElementStr = "ownerElement";
var globalDOMEvents = [
    "focus",
    "focusin",
    "focusout",
    "resize",
    "scroll",
    "click",
    "dblclick",
    "mousedown",
    "mouseup",
    "mousemove",
    "mouseover",
    "mouseout",
    "change",
    "select",
    "submit",
    "keydown",
    "keypress",
    "keyup",
    "contextmenu"
];
// var registerHelperStr = "registerHelper";
var delStr = "delete";

function defProp() {
	Object.defineProperty.apply(Object, arguments);
}

function makeSet(obj, key) {
	if(!obj[key]) {
		defProp(obj, key, {
			value : new Set(),
			enumerable : false,
			writable : true,
			configurable : true
		})
	}
}

function addBindings(obj, property) {
    if(property){
        let bindings = obj._bindings;
        if(!bindings){
            makeSet(obj, "_bindings");
            bindings = obj._bindings;    
        }
        bindings.add(property);
    }
}

function makeArray(obj, key) {
	if(!obj[key]) {
		defProp(obj, key, {
			value : [],
			enumerable : false,
			writable : true,
			configurable : true
		})
	}
}

function createDocFragment() {
	return document.createDocumentFragment();
}

function createElement(elm) {
	return document.createElement(elm);
}

// function insertBefore(parent, newNode, refNode) {
// 	parent.insertBefore(newNode, refNode);
// }

function deleteBindingCheckSize(obj, key, valToDelete) {
	obj[key][delStr](valToDelete);
	if(!obj[key].size) {
		delete obj[key];
	}
}

/*	IE Browser
	Lyte._ie 
	Edge Browser
	Lyte._ed 
	Replace with needed;
	Lyte._rwpf 
	IE / Edge Browser
	Lyte._ms
*/

let userAgent = navigator.userAgent;
// //temporary fix for IE 11
// if(userAgent.match(/rv:11\.0/) && userAgent.match(/Trident/)) {
// 	Lyte._ie = true;
//     window.action = function() {
//         return;
//     }
// }
// if(userAgent.match('Edge')) {
//     var s = createElement("div");
//     s.innerHTML= "<template><div>c</div></template>";
//     if(s.querySelector("template").childNodes.length) {
//         Lyte._ie = true;
//     } else {
//         Lyte._ed = true;    
//     }
//     s.remove()
// }

class Test extends HTMLElement {
    constructor() {
        super();
        if(!this.attributes.t) {
            _LC.frSpecial = true;
        }
    }
}

// if(Lyte._ie || Lyte._ed) {
// 	var doc = createDocFragment();
// 	doc.appendChild(document.createTextNode("  dummy "));
// 	doc.childNodes[0].replaceWith(document.createTextNode("changed"));
// 	if(doc.childNodes[0].textContent !== "changed") {
// 		Lyte._rwpf = true;
// 	}
// 	Lyte._ms = true;
// }

// Lyte.Component = {}//mainClass;
// Lyte.compile = {};//af check
// var _LyteComponent = Lyte.Component;

// let arrayUtils =  function() {
// 	//arrayFunctions
//     return _LC.aF.apply(_LC, arguments);
// };
// let objectUtils =  function() {
// 	//objectFunctions
//     return _LC.oF.apply(_LC, arguments);
// }

_LC.errorNodeDetails = function(node) {
    var str = node.cloneNode(true);
    str.innerHTML = ".....";
    return str.outerHTML;
}
// _LyteComponent.register = function() {
//     _LC.registerComponent.apply(_LC, arguments);
// }
// _LyteComponent.directive = {
//     register : function() {
//         _LC.registerDirective.apply(_LC, arguments);
//     }
// }
// _LyteComponent.destroy = function() {
//     _LC.destroy.apply(_LC, arguments);
// }

// _LyteComponent.registerDirective = function() {
//     _LC.registerDirective.apply(_LC, arguments);
// }

// _LyteComponent.registerHelper = function() {
//     _LC.registerHelper.apply(_LC, arguments);
// }
// _LyteComponent.set = function() {
//     _LC.set.apply(_LC, arguments);
// }
// _LyteComponent.registeredHelpers = {};
// _LyteComponent.registeredComponents = {};
// _LyteComponent.registeredDirectives = [];
function noop() {

}
_LC.customPropHandlersList = {};
_LC.registerCustomPropHandler = function(propName) {
    let dasherized = _LC.String.dasherize(propName);
    propName = _LC.String.toCamelCase(propName);
    if(this.customPropHandlers.indexOf(propName) === -1) {
        this.customPropHandlers.$push(propName);
        _LC.customPropHandlers.$push(propName);
        let customPropHanlderFn = customElementPrototype.prototype[propName] = function() {
            _LC.customPropRegex = this.component._registryClass.customPropRegex;
            let argsLength = arguments.length;
            let arg0 = arguments[0];
            let options = arguments[2];
            let compData = _LC.getCmpData(this.component.data);
            if(!arg0) {
                //Read all the values
                let obj = {};
                for(let key in compData) {
                    if(key.startsWith(propName)) {
                        let objKey = key.substring(propName.length);
                        objKey = _LC.String.lowerCaseFirstLetter(objKey);
                        obj[objKey] = compData[key];
                    }
                }
                return obj;
                
            } else if(typeof arg0 === "string") {
                if(argsLength > 1) {
                    //Set a value
                    this.set(propName+ _LC.String.upperCaseFirstLetter(arg0), arguments[1], options);
                } else {
                    //Read a value
                    let actKey = propName + _LC.String.upperCaseFirstLetter(arg0);
                    return compData[actKey];
                }
            } else if(typeof arg0 === "object") {
                //Write a set of values
                for(let key in arg0) {
                    let objKey = propName + _LC.String.upperCaseFirstLetter(key);
                    this.set(objKey, arg0[key], arguments[1]);
                }
            }
            _LC.customPropRegex  = "";
        }
        _LC.customPropHandlersList[propName] = customPropHanlderFn;
        this.customPropRegex = new RegExp("^(" + this.customPropHandlers.join("|")+ ")");
    }
}
_LC.unregisterComponent = function(componentName) {//af check
    if(typeof componentName != "string"){
        componentName = componentName._compName;
        if(!componentName){
            return;
        }
    }
    if(componentName && this._registeredComponentClass[componentName]) {
        var compClass = this._registeredComponentClass[componentName];
        var commonClass = this._registeredCommonClass[componentName];
        if(compClass.activeInstances > 0) {
            //@Slicer.developmentStart
            _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn(
                "There are active instances of the component " + componentName + " and hence cannot be unregistered"
            );
            //@Slicer.developmentEnd
        } else {
            // comp._properties = {};
            let ind = commonClass.component.list.indexOf(compClass);
            if(ind != -1){
                commonClass.component.list.splice(ind,1);
            }
            delete commonClass.componentClass;
            compClass._actions = compClass._template = compClass._dynamicNodes = compClass.__observers = null;            
            compClass._callBacks = {};
            compClass._observers = [];
            compClass._data = undefined;
            compClass._methods = {};
            compClass._classFunc = null;
            compClass._registered = false
            compClass.prototype.get = noop;
            compClass.prototype.set = noop;
            Object.defineProperty(commonClass.prototype, "setData", {
                configurable : true, 
                writable : true, 
                value : function(arg0, arg1) {
                    this._initProperties = this._initProperties || {};
                    if(typeof arg0 === "string") {
                        this._initProperties[arg0] = arg1
                    } else if(typeof arg0 === "object") {
                        for(let key in arg0) {
                            this._initProperties[key] = arg0[key];
                        }
                    }
                }
            });

            Object.defineProperty(commonClass.prototype, "setMethods", {
                configurable : true, 
                writable : true, 
                value : function(arg0, arg1) {
                    this._initMethods = this._initMethods || {};
                    if(typeof arg0 === "string") {
                        this._initMethods[arg0] = arg1
                    } else if(typeof arg0 === "object") {
                        for(let key in arg0) {
                            this._initMethods[key] = arg0[key];
                        }
                    }
                }
            });

            delete this._registeredComponentClass[componentName];
            this._instanceList.forEach(function(registryIns){
                delete registryIns._registeredComponents[componentName];
                delete registryIns.registeredComponents[componentName];
            })
            // var template = document.querySelector("template[tag-name="+componentName+ "]")
            var lyteComponentsDiv = _LC.getComponentsDiv(_LC.lyteComponentsDiv, this.name);
            var template = lyteComponentsDiv.querySelector("template[tag-name="+componentName+ "]");
            if(template) {
                template.remove();
            }
            // lyteht -> lyteHelperTemplates
            var helperTemplate = compClass._helperTemplate;
            if(helperTemplate) {
                helperTemplate.remove();
            }
            if(commonClass._depthTemp) {
                commonClass._depthTemp.remove();    
            }
            if(compClass._style){
                compClass._style = undefined;
            }
        }
        ComponentRegistry._unRegisteredComponents[componentName] = true;
    } 
    //@Slicer.developmentStart
    else {
        _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Component "+ componentName + " not yet registered");
    }
    //@Slicer.developmentEnd
}

var elementPrototype = typeof HTMLElement !== "undefined" ? HTMLElement : Element;

_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.appendTemplateDiv = function() {
    document.body.appendChild(_LC.tDiv);
    document.body.appendChild(_LC.h1Div);
    document.body.appendChild(_LC.hiddenComponentsDiv);
    document.body.appendChild(_LC.hiddenYieldsDiv);
}
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.getAllHiddenComponents=function(){
    return _LC.hiddenComponentsDiv.content.childNodes;
}

function onDomContentForLyte() {
    // if(!Lyte._ie){//_ie
    	document.body.appendChild(_LC.lyteComponentsDiv);
        //document.body.appendChild(_LC.tDiv);
    // }
    document.body.appendChild(_LC.hDiv);
    let bodyEvents = globalDOMEvents;    
    for(let i=0; i<bodyEvents.length; i++){    
        var evnt = bodyEvents[i];    
        document.body.addEventListener(evnt,globalEventHandler, true);    
    }    
    
    let comp = _LC.toBeRegistered;    
    if(comp.length){    
        for(let j=0; j<comp.length;j++){
            customElements.define(comp[j].name, comp[j].def, undefined, comp[j]._lyteOptions);    
        }    
        _LC.toBeRegistered = [];    
    }
    if(/iPad|iPhone|iPod/.test(userAgent) && !window.MSStream) {
        var style = createElement("style");
        style.innerHTML = "* { cursor : pointer}";
        document.head.appendChild(style);
    }
    document.head.appendChild(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._lessDiv);
}

var changeEventhandler = function(event) {
	var target = event.target || event.srcElement;
	if(!target._attributeDetails) {
		return;
	}
	var attributeName = "value";
	if(target.type === "checkbox" || target.type=== "radio") {
		attributeName = "checked";
	}
	let contextSwitchArray = [];
	var attrNode, saveAttr ;
	var attrDetail = target._attributeDetails[attributeName]
    
	if(!attrDetail || !attrDetail.isLbind && !saveAttr) {
		return;
	}
	//attrNode = (attributeName === "checked") ? target._attributeDetails[attributeName].bindedNode : target.getAttributeNode(attributeName);
	var callee = target;
	if(!target._callee){
		while(callee && !_LC.isCustomElement(callee) && callee.tagName !== "LYTE-YIELD") {
			if(callee.tagName === "BODY") {
				callee = null;
				break;
			}
			callee = callee.parentNode;
		}
		if(callee && callee.tagName === "LYTE-YIELD"){
			target._callee = callee._registerYield._callee;
		} else {
			target._callee = callee;
		}
	}
	let self = target._callee;
    
	if(target) {
		_LC.adCx(target, contextSwitchArray);
	}
    let obj = _LC.getNew(self.component.data, attrDetail.dynamicValue);
    if(!obj.context){
        return;
    }
    let lastKeyIndex = +obj.lastKey;
    if(Array.isArray(obj.context) && typeof lastKeyIndex == "number") {
        let callReplaceAt = lastKeyIndex < obj.context.length;
        if(obj.context[lastKeyIndex] !== target[attributeName] || !callReplaceAt){
            _LC.aF(obj.context, callReplaceAt ? "replaceAt" : "insertAt", lastKeyIndex, target[attributeName]);
        }
    } else {
        _LC.set(obj.context, obj.lastKey, target[attributeName]);
    }
	// _LC.set(obj.context, obj.lastKey, target[attributeName]);
	if(target) {
		_LC.rmCx(target, contextSwitchArray);
	}		
}
document.addEventListener("change", changeEventhandler);
var globalEventHandler = function(ev){
	var evnt = ev.type;
    var target = ev.target,toRemove;
    if(ev.target.shadowRoot){
        return;
    }
    if(/^(click|dblclick|mouseover|mouseout|mousemove|mousedown|mouseup|contextmenu|keydown|keyup|keypress|submit|reset|focus|blur|input|change|select|load|resize|scroll|unload|beforeunload|DOMContentLoaded|readystatechange|touchstart|touchmove|touchend|touchcancel|play|pause|ended|volumechange|durationchange|ratechange|dragstart|drag|dragenter|dragleave|dragover|drop|dragend)$/g.test(evnt) && target.getAttribute && target.getAttribute("lyte-state") != null){
        ev.preventDefault();
		return;
	}
    if(ev.currentTarget !== document.body && !_LCSD.getHostElement(ev.currentTarget)) {
        target = ev.currentTarget;
    }
	if(!window.event){
		toRemove = true;
		window.event = ev;
	}
    let eventStopped = false;
    while(target && target.getAttribute && (!target.getAttribute(evnt) || (!['mouseenter', 'mousemove', 'mouseover', 'mouseout', 'mouseleave'].includes(ev.type) && target.hasAttribute("disabled") && evnt != "blur")) && target.tagName != "BODY"){
		if(_LC.hasLyteEvents(target, evnt)) {
            eventStopped = _LC.handleLyteEvents(target, ev);
            if(eventStopped) {
                break;
            }
        }
        target = target.parentNode;
	}
    if(eventStopped || !target) {
        return;
    }
 	var callee = target;
    if((target._callee && target._callee.component && target._callee.component.constructor._v3) || (target.component && target.component.constructor && target.component.constructor._v3)){
        return;
    }
	if(!target._callee){
		while(callee && !_LC.isCustomElement(callee) && callee.tagName !== "LYTE-YIELD") {
			if(callee.tagName === "BODY") {
				callee = null;
				break;
			}
			callee = callee.parentNode;
		}
		if(callee && callee.tagName === "LYTE-YIELD"){
			target._callee = callee._registerYield._callee;
		} else {
			target._callee = callee === target ? undefined : callee;
		}
	}
    if(target._callee && !target._callee.component){
        _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Component of the target node is destroyed and so "+ evnt +" wont be triggered.");    
        return;
    }
    event.$lTarget = target;
	if(target._evBoundEvents && target._evBoundEvents[evnt]) {
        //Not needed - but check and remove
		// let actions = target._callee? target._callee.component.constructor._actions : target.component.constructor._actions ;
        //let actions = target.component.constructor._actions;
		let actObj = target._evBoundEvents[evnt];
        let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj);
        _LC.skipArgProcessing(cloneActObj, ev, target);
		_LC.throwAction.call(target,target,evnt,cloneActObj, undefined, undefined, target, ev, undefined, true);
	} else if(target.getAttribute && target.getAttribute(evnt) && target._boundEvents && target._boundEvents[evnt]){
		// let actions = target._callee.component.constructor._actions;
        // let func = target.getAttribute(evnt).split(" => ")[1];
        let actObj = target._boundEvents[evnt];
		let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj);
		_LC.skipArgProcessing(cloneActObj, ev, target);
		_LC.throwAction.call(target._callee,target._callee,evnt,cloneActObj, undefined, undefined, target, ev);
	}
    //@Slicer.$hideStart
    ComponentRegistry.hide && ComponentRegistry.hide.triggerGlobalEvent(target,evnt,ev);
    //@Slicer.$hideEnd
	if(target.tagName === "LABEL"){
		var input = target.querySelector("input");
		if(input && input.getAttribute(evnt)){
			// let actions = target._callee.component.constructor._actions;
			// let func = input.getAttribute(evnt).split(" => ")[1];
			//	let actObj = target._callee.constructor.getHelper(func);
            let actObj = target._boundEvents[evnt];
            let cloneActObj = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj);
			_LC.skipArgProcessing(cloneActObj, ev, target);
			_LC.throwAction.call(target._callee,target._callee,evnt,cloneActObj, undefined, undefined, input,ev);
		}
	}
	if(toRemove){
		window.event = undefined;
	}
}
class LyteYield extends HTMLElement {
    constructor() {
        super();   
        this.decideConstructor();
    }
    decideConstructor(){
        if(ltCf.versionBridge){
            let V3Registry = ltCf.versionBridge.component.V3Registry;
            let foundInV3List = V3Registry.allList.v3[this.localName]
            let foundInV4List = V3Registry.allList.v4[this.localName]
            if(foundInV3List){
                let _callee = this._callee || _LC.getCallee(this.parentNode, this);
                if(!foundInV4List || (!(this.hasAttribute("lyte-v4") || this.hasAttribute("lyte-registry") || (_callee && _callee.__v4)) && !ltCf.lyteV4 && !ltCf.v4Render && (ltCf.fromV3Render || _LC.getCurrentRegistry() == V3Registry || foundInV3List))){
                    //v3
                    let v3CompClass = foundInV3List;
                    let self = this;
                    LyteYield._V3InsApi.forEach(function(apiName){
                        self[apiName] = v3CompClass.prototype[apiName];
                    })
                }
            }
        }
    }
    connectedCallback(){
        this._connectedCallback();
    }
    _connectedCallback() {
        this._callee = this._callee || _LC.getCallee(this.parentNode, this);
        //@Slicer.$hideStart
        let _LCHD = this._callee.getDirectiveIns("hide");
        _LCHD && _LCHD.yield.connect(this);
        //@Slicer.$hideEnd
        let registry,shadowObj,directiveObj;
        if(this._callee){
            registry = this._callee.$registry;
           if(registry){
            shadowObj = registry._shadow;
            directiveObj = registry._directive;
           }
        }
        if(directiveObj){
            this._transitionAppend = []
        }
        if(shadowObj && this.shadowRoot){
            shadowObj.insertInLessDiv(this._shadowParent,this._lessDiv)
            shadowObj.updateLessDiv(this._lessDiv,this.shadowRoot._compList);
        }
        var yieldName = _LC.getYieldName(this);
        if(!this.hasAttribute("yield-name")){
            this.setAttribute("yield-name", yieldName);
        }
        if(!this._registerYield && this._callee) {
            if(this._callee._fR && this._callee._fR._yieldCallee) {
                this._registerYield = {"_callee" : this._callee._fR._yieldCallee.component.$node};
            } else if(this._callee._yields && yieldName && this._callee._yields[yieldName]) {
                this._registerYield = {"_callee" : this._callee._yields[yieldName]._callee};
            }
        }
    }
    disconnectedCallback(){
        this._disconnectedCallback();
    }
    getData(){
        return this.component.data;
    }
    _disconnectedCallback(){
        if(_LC.shouldIgnoreDisconnect() || this._deleted || this._ignoreDisconnect) {
            return;
        }
        //@Slicer.$hideStart
        let _LCHD = this._callee.getDirectiveIns("hide");
        _LCHD && _LCHD.yield.disConnect(this);
        //@Slicer.$hideEnd
        let shadowObj = this._callee.getDirectiveIns("shadow");
        shadowObj && shadowObj.destroyRef(this);
        let registry = this._callee.$registry;
        let directiveObj = registry._directive;
        this._deleted = true;
        let yieldName = this.getAttribute("yield-name");
        let callee = this._callee
        if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue){
            _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue.yieldDisconnectedCallback(this, callee);
        }
        if(!this._properties) {
            return;
        }
        var nodeContextSwitchArray = [];
        let cmpData = _LC.getCmpData(this.component.data);
        _LC.adCx(this, nodeContextSwitchArray);
        _LC.removeSelectedBindingDeep(this._properties, cmpData, true);
        let node = this._registerYield;
        if(!node) {
        _LC.rmCx(this, nodeContextSwitchArray);
        return;
        }
        var toAppendContextSwitchArray = [];
        this._parentHelper = null;
        //newContext not needed
        var del = "delete";//for ie 11.0
        _LC.adCx(node, toAppendContextSwitchArray);
        for(let key in this._dynamicProperty) {
            if(this._dynamicProperty[key].isActualNode) {
                this._dynamicProperty[key].isActualNode._helperNodes[del](this);
            }else {
                let helperNodes = node._callee.getProperty(key)._helperNodes;
                if(helperNodes) {
                    helperNodes[del](this);
                }
            }
        }
        this._dynamicProperty = {};
        for(let i=0;i<this._helpers.length;i++) {
            node._callee.removeHelpers(this._helpers[i]);
        }
        
        this._helpers = [];
        _LC.rmCx(node, toAppendContextSwitchArray);
        _LC.rmCx(this, nodeContextSwitchArray);
        directiveObj && directiveObj.destroyPromises(this);
        // var self = this;
        // setTimeout(function() {
        //     self._registerYield = null
        //     self._callee = null;
        // },0);
    }
    getProperty(key) {
            var arr = key.match(/([^[\].]+|\[\])/g);
            let property = this;
            if(!property._properties[arr[0]]) {
                    property._properties[arr[0]] = {};
            } 
            property = property._properties[arr[0]];
        
            defProp(property, '_path', {enumerable: false, value : arr[0]});
            for(let i=1;i<arr.length;i++) {
                if (arr[i].startsWith("'") || arr[i].startsWith('"')) {//added check
                    arr[i] = arr[i].substring(1, arr[i].length -1);
                }
                    if(!property[arr[i]]) {
                        property[arr[i]] = {};
                        defProp(property[arr[i]], '_path', {enumerable: false, value : property._path + "." + arr[i]});
                }
                property = property[arr[i]];
            }
            return property;
        }
}
LyteYield._V3InsApi = ["_connectedCallback", "getProperty","_disconnectedCallback"]; //no i18n
ltCf._LyteYield = LyteYield;
customElements.define("lyte-yield", LyteYield, undefined, {v4 : true});
ltCf.v3 && !ltCf.instanctiatedBridge && ltCf.instanciateBridge();
//v4 class code
class LyteCustomElement extends HTMLElement {
    constructor(){
        super();
        this.decideConstructor();
    }
    decideConstructor(){
        if(ltCf.versionBridge){
            let V3Registry = ltCf.versionBridge.component.V3Registry;
            let foundInV3List = V3Registry.allList.v3raw[this.localName];
            let foundInV4List = V3Registry.allList.v4raw[this.localName];
            if(foundInV3List){
                let _callee = this._callee || _LC.getCallee(this.parentNode, this);
                if(!foundInV4List || (!(this.hasAttribute("lyte-v4") || this.hasAttribute("lyte-registry") || (_callee && _callee.__v4)) && !ltCf.lyteV4 && !ltCf.fromV4Render && (ltCf.fromV3Render || _LC.getDecidedRegistry() == V3Registry || foundInV3List))){
                    //v3
                    let v3CompClass = V3Registry.allList.v3raw[this.localName];
                    let self = this;
                    LyteCustomElement._V3InsApi.forEach(function(apiName){
                        self[apiName] = v3CompClass.prototype[apiName];
                    })
                }
            }
        }
        //v4 
        this.actualConstructor();
    }
    actualConstructor(){
        this.__lyteCustomElement = true;
        if(this.isClonedNode()){
            this.__cloned = true;
            let attrList = this.attributes;
            for(let i=0; i<attrList.length; i++){
              let attrNode = attrList[i];
              attrNode._lyte = {cloned : true,userCloned : true, name : attrNode.nodeName ,value : attrNode.nodeValue};
            }
        }
        if(!_LC.getDefaultRegistry() && !_LC.getCurrentRegistry()){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC009");
            //@Slicer.developmentEnd
            return;
        }
        var obj = {compName : this.localName, type : "customComponent"};
        _LC.getCompRegistry(obj,this);
        if(!_LC.verifyDetails(obj)){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC010", "Raw Component", this.localName);
            //@Slicer.developmentEnd
            return
        }
        var lIns = obj.lIns ,compClass = obj.compClass ,regIns = obj.regIns;
        var compInstance;
        lIns.scopedInstance(compClass,[this],function(ins){
            compInstance = ins;
        },[regIns])
        if(!compInstance.$component){
            // they have not passed component in lookups  
            Object.defineProperty(compInstance, "$component", {
                get : function() {
                    //@Slicer.developmentStart
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn(
                        "Accessing $component from component instance is deprecated. Use $registry instead."
                    );
                    //@Slicer.developmentEnd
                    return regIns;
                }
            })
            Object.defineProperty(this, "$component", {
                get : function() {
                    //@Slicer.developmentStart
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Accessing $component from node is deprecated. Use $registry instead.");
                    //@Slicer.developmentEnd
                    return regIns;
                }
            })
        }
        this.$registry = regIns;
        compInstance.$registry = regIns;
        compInstance._registryClass = compClass._registryClass;
        this._registryClass = compClass._registryClass;
        _LC.setAddedRegistries(compInstance,this.localName);
        this.component = compInstance;
        compInstance.$node = this;
        let options = compClass._options;
        if(options && options.clone){
            this._allCallbacks = options.clone.allCallbacks;
            if(typeof options.clone.attributeChangedCallback == "object"){
                this._duringClone = options.clone.attributeChangedCallback.duringClone;
                this._afterClone = options.clone.attributeChangedCallback.afterClone;
            }else{
                this._allAttributesCallback = options.clone.attributeChangedCallback;
            }
            this.__connectedCallback = options.clone.connectedCallback;
            this.__disconnectedCallback = options.clone.disconnectedCallback;
        }
        this._actualConnectedCallback = compClass.prototype.connectedCallback;
        this._actualAttributeChangedCallback = compClass.prototype.attributeChangedCallback;
        this._actualDisconnectedCallback = compClass.prototype.disconnectedCallback;
        this._reconnectedCallback = compClass.prototype.reconnectedCallback;
        this._removedCallback = compClass.prototype.removedCallback;
    }
    executeLyteCallbacks(callBack,argArr){
        if(callBack) {
            callBack.apply(this.component, Array.from(argArr));
        }
    }
    isClonedNode(){
        if(this.hasAttribute("lyte-rendered-ce") && !this.LyteConnected || this.__cloned){
            return true;
        }
        return false;
    }
    _isClonedAttribute(attrName,oldVal,newVal){
        let attrNode = this.attributes[attrName];
        if(attrNode._lyte && attrNode._lyte.cloned && attrNode._lyte.name == attrName && attrNode._lyte.value == newVal && oldVal === null){
            return true;
        }
        return false;
    }
    isClonedAttribute(attrName){
        let attrNode = this.attributes[attrName];
        if(attrNode._lyte && attrNode._lyte.userCloned){
            return true;
        }
        return false;
    }
    connectedCallback(){
        this._connectedCallback();
    }
    _connectedCallback(){
        if(_LC.shouldIgnoreDisconnect() || this.__lyteIgnore){
            if(this._reconnectedCallback){
                this.executeLyteCallbacks(this._reconnectedCallback,arguments);
            }
            return;
        }
        if(this.isClonedNode() && (this._allCallbacks == false || this.__connectedCallback == false)){
            return;
        }
        this.executeLyteCallbacks(this._actualConnectedCallback,arguments);
        this.setAttribute("lyte-rendered-ce", "");
        this.LyteConnected = true;
    }
    static get observedAttributes() {
        return this._observedAttributes;	
    }
    attributeChangedCallback(attr, oldValue, newValue) {
        this._attributeChangedCallback(attr, oldValue, newValue);
    }
    _attributeChangedCallback(attr, oldValue, newValue){
        var cloneStatus;
        var clonedNode = this.isClonedNode();
        if(clonedNode){
            if(this._allCallbacks == false || this._allAttributesCallback == false){
                return;
            }else{
                let attrName = arguments[0];
                let oldVal = arguments[1];
                let newVal = arguments[2];
                let attrNode = this.attributes[attrName];
                cloneStatus = this.attributes[arguments[0]]._lyte.userCloned;
                let isClonedAttribute = this._isClonedAttribute(attrName,oldVal,newVal,attrNode);
                if(isClonedAttribute){
                    this.attributes[arguments[0]]._lyte.userCloned = true;
                    if(this._duringClone == false){
                        return;
                    }
                }else{
                    this.attributes[arguments[0]]._lyte.userCloned = false;
                    if(this._afterClone == false){
                        return;
                    }
                }
            }
        }
        this.executeLyteCallbacks(this._actualAttributeChangedCallback,arguments);
        if(clonedNode && this.attributes[arguments[0]] && this.attributes[arguments[0]]._lyte){
            this.attributes[arguments[0]]._lyte.userCloned = cloneStatus;
        }
    }
    disconnectedCallback() {
        this._disconnectedCallback();
    }
    _disconnectedCallback(){
        if(_LC.shouldIgnoreDisconnect() || this.__lyteIgnore){
            if(this._removedCallback){
                this.executeLyteCallbacks(this._removedCallback,arguments);
                this._parentHelper = null;
            }
            return;
        }
        if(this.isClonedNode() && (this._allCallbacks == false || this.__disconnectedCallback == false)){
            return;
        }
        this.executeLyteCallbacks(this._actualDisconnectedCallback,arguments);
    }
}
LyteCustomElement._V3InsApi = ["_connectedCallback", "_attributeChangedCallback" ,"_disconnectedCallback","actualConstructor","executeCallbacks","isNewComp"];
ltCf._LyteCustomElement = LyteCustomElement;
ltCf.v3 && !ltCf.instanctiatedBridge && ltCf.instanciateBridge();
//v4 class code
class customElementPrototype extends elementPrototype {
    constructor() {
        super();
        this.decideConstructor();
    }
    decideConstructor(){
        let self = this;
        if(ltCf.versionBridge){
            let V3Registry = ltCf.versionBridge.component.V3Registry;
            let foundInV3List = V3Registry.allList.v3[this.localName]
            let foundInV4List = V3Registry.allList.v4[this.localName]
            if(foundInV3List){
                let _callee = this._callee || _LC.getCallee(this.parentNode, this);
                if((!foundInV4List) || (!(this.hasAttribute("lyte-v4") || this.hasAttribute("lyte-registry") || (_callee && _callee.__v4)) && !ltCf.lyteV4 && !ltCf.fromV4Render && (ltCf.fromV3Render || _LC.getDecidedRegistry() == V3Registry || foundInV3List ))){
                    //v3
                    let v3CompClass = foundInV3List;
                    customElementPrototype._V3InsApi.forEach(function(apiName){
                        self[apiName] = v3CompClass.prototype[apiName];
                    })
                    self.component = self.constructor.component = v3CompClass.component;
                    this.setData = v3CompClass.component.prototype.setData;
                    this.setMethods = v3CompClass.component.prototype.setMethods;
                }else if(foundInV4List){
                    //v4
                    let v4CompClass = foundInV4List;
                    customElementPrototype._v4RegClassApi.forEach(function(apiName){
                        self.constructor[apiName] = v4CompClass[apiName];
                    })
                    customElementPrototype._v4RegProtoApi.forEach(function(apiName){
                        self[apiName] = v4CompClass.prototype[apiName];
                    })
                }
            }
        }
        //v4 
        this.actualConstructor();
    }
    doRegistration(componentName,customCrmComponent,componentClass, registryClass, registryInstance){
        if(!this.constructor.componentClass.__isRegistered){
            var registryName = registryClass.name, idleScheduler = registryClass.lazyScheduler, perf = registryInstance.$app && registryInstance.$app.config.performance;
            if(idleScheduler.tasks.get(componentName, registryName) || (idleScheduler.currentTask && idleScheduler.currentTask.id === registryName+"$"+componentName)){
                // console.log("idleScheduler task handling in component ", componentName);
                if(idleScheduler.currentTask && idleScheduler.currentTask.id !== registryName+"$"+componentName){
                //   console.log("idleScheduler pending task handling in component ",idleScheduler.currentTask.id);
                  if(perf){
                    var _p1 = performance.now();
                  }
                  var sval = idleScheduler.tasks.get(idleScheduler.currentTask.id, registryName);
                    if(sval && idleScheduler.isGenerator(sval.handler)){
                        var gnxt = sval.handler.next(), gval;
                        while(gnxt.done == false){
                            gval = gnxt.value;
                            if(typeof gval == "function"){
                                gval();
                            }
                            gnxt = sval.handler.next()
                        }
                        idleScheduler.dequeueTask(idleScheduler.currentTask.id, registryName);
                    }
                    if(perf){
                        var _p2 = performance.now();
                        var oldPerf = registryClass.registerPerf[idleScheduler.currentTask.id];
                        registryClass.registerPerf[idleScheduler.currentTask.id] = (oldPerf ? oldPerf : 0) + (_p2 - _p1);
                    }                        
                }
                if(perf){
                    var p1 = performance.now();
                }
                var obj = idleScheduler.dequeueTask(componentName, registryName);
                var gen = obj.handler;
                if(idleScheduler.isGenerator(gen)){
                    var gnxt = gen.next(), gval;
                    while(gnxt.done == false){
                        gval = gnxt.value;
                        if(typeof gval == "function"){
                            gval();
                        }
                        gnxt = gen.next()
                    }
                }
                if(perf){
                    var p2 = performance.now();
                    var oldPerf = registryClass.registerPerf[componentName];
                    registryClass.registerPerf[componentName] = (oldPerf ? oldPerf : 0) + (p2 - p1);
                }
            }
            else{
                if(perf){
                    var p1 = performance.now();
                }
                customCrmComponent._registerComponent(componentName,customCrmComponent,componentClass, registryClass, registryInstance);
                // _LC.postRegistration(componentName,customCrmComponent);
                idleScheduler.dequeueTask(componentName, registryName);
                if(perf){
                    var p2 = performance.now();
                    var oldPerf = registryClass.registerPerf[componentName];
                    registryClass.registerPerf[componentName] = (oldPerf ? oldPerf : 0) + (p2 - p1);
                }
            }
        }
    }
    actualConstructor() {
        // this.setData = _LC.setData;
        // this.setMethods = _LC.setMethods;
        if(this.hasAttribute("lyte-rendered") || this._ccCalled) {//aaf check
            this.__lyteIgnore = true;
        	return;
        } else if(ComponentRegistry._unRegisteredComponents[this.localName]){
            this.__lyteIgnore = true;
            var origClass = ComponentRegistry._registeredCommonClass[this.localName];
            origClass._pendingComponents = origClass._pendingComponents || [];
            origClass._pendingComponents.push(this);
            return;
        }
        if(!_LC.getDefaultRegistry() && !_LC.getCurrentRegistry()){
            //@Slicer.developmentStart
            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC009");
            //@Slicer.developmentEnd
            return;
        }
        this.__v4 = true;
        
        let fastRenderIndex;
        let lytePropAttr = this.attributes._lyteprop;
        lytePropAttr = lytePropAttr ? lytePropAttr.nodeValue : undefined;
        let fastRenderedProp = (fastRenderIndex = lytePropAttr) ? _LC.fRP[fastRenderIndex] : undefined;
        let compInstance;// = this.component = new this.constructor.component.list[0]();//this.component = fastRenderedProp ? fastRenderedProp.component : new this.constructor.component();
        var self = this;
        if(!fastRenderedProp){
            var obj = {compName : this.localName, type : "component"};
            _LC.getCompRegistry(obj,this);
            if(!_LC.verifyDetails(obj)){
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC010", "Component", this.localName);
                //@Slicer.developmentEnd
                return
            }
            var lIns = obj.lIns ,compClass = obj.compClass ,regIns = obj.regIns;
            lIns.scopedInstance(compClass,[],function(ins){
                compInstance = self.component = ins
            },[regIns])
            if(!compInstance.$component){
                // they have not passed component in lookups  
                Object.defineProperty(compInstance, "$component", {
                    get : function() {
                        //@Slicer.developmentStart
                        // Lyte.warn("Accessing $component from instance is deprecated. Use $registry instead.");
                        //@Slicer.developmentEnd
                        return regIns;
                    }
                })
                Object.defineProperty(this, "$component", {
                    get : function() {
                        //@Slicer.developmentStart
                        // Lyte.warn("Accessing $component from node is deprecated. Use $registry instead.");
                        //@Slicer.developmentEnd
                        return regIns;
                    }
                })
            }
            this.$registry = regIns;
            compInstance.$registry = regIns;
            compInstance._registryClass = compClass._registryClass;
            this._registryClass = compClass._registryClass;
            _LC.setAddedRegistries(compInstance,this.localName);
            
        }else{
            compInstance = self.component = fastRenderedProp.component;
            var compClass = this.constructor.component.list[0];
            this.$registry = compInstance.$registry;
            this._registryClass = compInstance._registryClass;
        }
        if(this.constructor.componentClass._registryClass.lazyRegister){
            this.doRegistration(this.localName, this.constructor, this.constructor.componentClass, this.$registry.constructor, this.$registry);
        }
        
        this._properties = {};
        this._compClass = compClass;
        this._tagDirectives = [];
        for(let propName in _LC.customPropHandlersList){
            this[propName] = _LC.customPropHandlersList[propName];
        }
        _LC.directive.setTagDirectives(compClass,this);
        _LC.directive.setAttrFromRender(this,this._tagDirectives);
        compInstance._methods = {};
        
        compInstance._config = this.constructor._config;
        compInstance.actions = Object.assign({},compClass._actions);
        //@Slicer.developmentStart
        var act = compInstance.actions;
        Object.defineProperty(compInstance,"actions",{
            get : function() {
        		_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Accessing actions directly is deprecated. Move the required common code from action block to a common function and access it.");
        		return act;
        	}   
        });
        var met = compInstance.methods;
        Object.defineProperty(compInstance,"methods",{
            get : function() {
        		_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn("Accessing methods directly is deprecated. Kindly make use of 'this.executeMethod' Api.");
        		return met;
        	}   
        });
        //@Slicer.developmentEnd
        compClass.activeInstances++; 
        let compData;
        
        if(compInstance.$node) {    
            for(var key in compInstance.$node) {
                if(key != "localName"){
                    this[key] = compInstance.$node[key];
                }
            }
        }
        compInstance.$node = this;
        this._yields = {};
        if(!fastRenderedProp) {
            compData = compInstance.data = {};
            let data = compClass._data ? compClass._data.apply(compInstance) : {};
            var def = "default";
            defProp(compData, '__component__', {
                value : this,
                configurable : true,
                writable: true,
                enumerable : false
            });
            compInstance.__data = data;
            compData.errors = {};
            this._hideAttr = {};
            let globalRegdata = this.$registry.__data;
            for(let key in globalRegdata) { 
                if(!data.hasOwnProperty(key)) {
                    data[key] = globalRegdata[key];
                }
            }
            let isFreezedComp = _LC.freeze ? _LC.freeze.component(compInstance.constructor) : false;
            for(let key in data) {
                var obj = data[key];
                compData[key] = obj[def];
                var customDtype = false;
                if(typeof obj.type == "function"){
                    var customDatatype = obj.type;
                    if(/^(object|array)$/.test(customDatatype.type) && (customDatatype.hasOwnProperty("properties") || customDatatype.hasOwnProperty("items") ) ){
                        customDtype = true;
                    }
                }
                if(isFreezedComp){
                    _LC.object.freezeData(data, obj);
                    continue;
                }
                if((/^(object|array)$/.test(obj.type) && (obj.watch || (obj.hasOwnProperty("items") || obj.hasOwnProperty("properties")) ) || customDtype)){
                    (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.establishObjectBinding)(compData, key, false,undefined,undefined,obj.watch?true:undefined);
                }
                if(obj.hasOwnProperty("hideAttr")){
                    this._hideAttr[key] = obj.hideAttr;
                }
            }
            this.$data=compData;
            if(this._initMethods){
                let arr=Object.keys(this._initMethods);
                compData.$methodAttributes = arr.reduce((obj, key) => {
                    obj[key] = true;
                    return obj;
                  }, {});
            }
            if(this._initProperties){
                let arr=Object.keys(this._initProperties).filter((ele)=>{
                    if(!compData.$methodAttributes){return true};
                 return !compData.$methodAttributes[ele];
                });
                compData.$dataAttributes=arr.reduce((obj, key) => {
                    obj[key] = true;
                    return obj;
                  }, {});
            }
            let observed_attributes=this.component.constructor._observedAttributes;
            if(observed_attributes && this.attributes){
                Object.values(this.attributes).forEach(value => {
                    if(observed_attributes.includes(_LC.String.toCamelCase(value.name))){
                        compData.$dataAttributes=compData.$dataAttributes?compData.$dataAttributes:[];
                        compData.$dataAttributes[_LC.String.toCamelCase(value.name)]=true;
                    }
                });
            }
            observed_attributes=[];
            compInstance.$dataAttributes=compData.$dataAttributes;
            compInstance.$methodAttributes=compData.$methodAttributes;
            if(compInstance.$dataAttributes){
                Object.freeze(compInstance.$dataAttributes);
                Object.freeze(compData.$dataAttributes);
            }
            if(compInstance.$methodAttributes){
                Object.freeze(compInstance.$methodAttributes);
                Object.freeze(compData.$methodAttributes);
            }
            compData.errors = {};
            compInstance.__data = data;
            compData.$data = compData;
            compData.$methods = compInstance._methods;
            compData.$yields = this._yields;
        }
        
        for(let key in compClass._methods) {
            compInstance._methods[key] = compClass._methods[key];
        }
        let _overrides;
        let _config_flag;
        if(compInstance._ssr && compInstance._ssr.config){
            if( compInstance._ssr.config.clientLifeCycleHooks != undefined ){
                _config_flag = compInstance._ssr.config.clientLifeCycleHooks == true || ( typeof compInstance._ssr.config.clientLifeCycleHooks == 'object' ? compInstance._ssr.config.clientLifeCycleHooks.includes('init') : false );
            }
        }
        
        if(!fastRenderedProp) {
            // this.callback("constructor"); //af check // no need
            this._actions = this._actions ? this._actions : {};
            this._callee = this._callee || this.getCallee(this.parentNode);

            //check if it is called from server || to ssr bind
            
            //check if it has ssr contructor for ssr
            //ssrcode_client
            if( _config_flag || !this.hasAttribute( 'server-rendered' ) ){
                if( _overrides && _overrides.constructor && this.serverCall ){
                    _overrides.constructor.apply( this.component );
                }
            } 

            //checking lyte.attr ytpe and given default value type
            for (var key in compData) {
                var field = compData.__component__.component.__data[key];
                
                var error = _LC.handleValidation(compData, key, compData[key], compInstance ,true);
                if (error) {
                    compData[key] = undefined;
                    var field = compData.__component__.component.__data[key]; 
                    if(field && /^(object|array)$/.test(field.type)){
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.establishObjectBinding)(compData, key, undefined,undefined,undefined,field.watch ? true:undefined);
                    }
                }
            }
            // if(compData.errors && Object.keys(compData.errors).length){
            //     Lyte.error("Error in data passed to component '"+ this.localName +"' for the properties - "+Object.keys(compData.errors).toString());
            // }
        } else {
            this._fR = fastRenderedProp;
            delete _LC.fRP[fastRenderIndex];
            // _LC.$pushFrc(fastRenderIndex);
        }
    }
    getDirectiveObj(){
        if(!_LC.directive.bundled){
            return false;
        }
        return _LC.directive;
    }
    getDirectiveIns(name){
        return this.$registry["_"+name];
    }
    getDrObj(name){
        if(this.$registry["_" + name]){
            return this.$registry["_" + name];
        }
        return false;
    }
    getShadowObj(){
        if(this.$registry._shadow){
            return this.$registry._shadow;
        }
        return false;
    }
    getFastObj(){
        if(this.$registry["_turbo"]){
            return this.$registry["_turbo"];
        }
        return false;
    }
    getViewObj(){
        if(this.$registry["_view"]){
            return this.$registry["_view"];
        }
        return false;
    }
    getMethods(arg0) {
        return this.component.getMethods(arg0);
    }
    hasAction(arg0) {
        return this.component.hasAction(arg0);
    }
    setActions(arg0,arg1){
        return this.component.setActions(arg0,arg1);
    }
    getActions(arg0) {
        return this.component.getActions(arg0);
    }
    getObservers() {
        return this.component.getObservers();
    }
    setMethods(arg0, arg1) {
        return this.component.setMethods(arg0, arg1);
    }
    // getData(arg0) {
    //     return this.component.getData(arg0);
    // }
    // setData(arg0, arg1 ,options) {
    //     return this.component.setData(arg0, arg1, options);
    // }
    getCallee(callee){
        return _LC.getCallee(callee, this);
    }
    afterConnected(fastRenderProp,ssrBind) {
        let constr = this.component.constructor;
        var appScp = this.$app || this.$registry.$app || this.$registry.$addon;
        if((appScp && appScp.$mutate)|| (appScp && appScp.$mutate)){
            this.component.data = appScp.$mutate.create(this.component.data);
        }
        //initProperties is used because, we may have cases where the component wouldn't have been registered but 
        //it would be in dom already with some attributes. In those cases we can store the data in _initProperties as key, value.
        //These properties would then be applied to the component, once it gets instantiated. 
        
        //This is done, in order to solve when on a string value update of an if helper, the binding in the true or false case must be established. 
        //Without this, we won't establish the _properties in the component to the actual Data. 
        if(this.getAttribute("_lyteprop")) {
        	return "";
        }
        let obsattr = this.constructor._observedAttributes;
        for(let i=0;i<obsattr.length;i++){
            let key = obsattr[i];
            if(_LC.freeze && this.component.__data[key].freeze){
                continue;
            }
            let prop = this.getProperty(key);
            defProp(prop, '__fromComponent', {
            	value : true,
                enumerable : false
            });
        }
        this.getProperty("errors");
        let app = this.component.getAppOrAddon();
        var $lg = app.__gl, 
        compInstance = this.component,
        compData = compInstance.data;
        compData.$lg = $lg;
        this.component.constructor.prototype.$lg = $lg;
        if(this._initProperties) {
            let initProperties = this._initProperties;
            _LC.$variables(this);
            var eKeys = [];
            for(let key in initProperties) {
                let actVal;
                let field = compInstance.__data[key]; 
                if(field && field.type !== _LC.getDataType(initProperties[key]) && (initProperties[key] !== undefined  || field.type === "boolean")) {
                    actVal = _LC.typeCast(initProperties[key], compInstance.__data[key].type);
                } else {
                     actVal = initProperties[key];
                }
                let error = _LC.handleValidation(compData, key, actVal, compInstance ,true);
                if(!error) {
                    compData[key] = actVal; 
                    var customDtype = false;
                    if(field && typeof field.type == "function"){
                        var customDatatype = field.type;
                        if(/^(object|array)$/.test(customDatatype.type) && (customDatatype.hasOwnProperty("properties") || customDatatype.hasOwnProperty("items"))){
                            customDtype = true;
                        }
                    }   
                    if((field && /^(object|array)$/.test(field.type) && (field.watch || (field.hasOwnProperty("items") || field.hasOwnProperty("properties"))) )|| customDtype){
                        (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.establishObjectBinding)(compData, key, undefined,undefined,undefined,field.watch ? true : undefined);
                    }
                }
                else{
                    eKeys.push(key);
                }
            }
            this._initProperties = undefined;
            //@Slicer.developmentStart
            if(eKeys.length){
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this,"LC006",this.component.$node.localName,eKeys.toString())
            }
            //@Slicer.developmentEnd
        }
        if(this._initMethods) {
            let initMethods = this._initMethods;
            for(let key in initMethods) {
                compInstance._methods[key] = initMethods[key];
            }
            this._initMethods = undefined;
        }
        
        let _config_flag;
        if( compInstance._ssr && compInstance._ssr.config ){
            if( compInstance._ssr.config.clientLifeCycleHooks != undefined ){
                _config_flag = compInstance._ssr.config.clientLifeCycleHooks == true || ( typeof compInstance._ssr.config.clientLifeCycleHooks == 'object' ? compInstance._ssr.config.clientLifeCycleHooks.includes('init') : false );
            }
        }
        let _overrides;      
        
        //ssrcode client        
        ssrBind && this.cmpBind(fastRenderProp);
        if( _config_flag || !this.hasAttribute( 'server-rendered' ) )
        {
            if( _overrides && _overrides.init && this.serverCall ){
                _overrides.init.apply( this.component );
            }else{        
                this.callback('init');
            }
            this.onCallBack('init');
        }
        // this.callback('init');
        // this.onCallBack('init');
        let templateAttributes = this.component.constructor._templateAttributes;
        if(templateAttributes && templateAttributes.a) {
        	for(let key in templateAttributes.a) {
//        		let attr = templateAttributes.attr[i];
				let attr = templateAttributes.a[key];
        		if((!this.hasAttribute(attr.name) && !compData.hasOwnProperty(attr.name) ) || attr.globalEvent) {
        			if(attr.helperInfo && attr.helperInfo.name === "action") {
                        this._evBoundEvents = this._evBoundEvents || {};
                        let actionName = attr.helperInfo.args[0];
					    let boundName;
					    if(actionName.startsWith('"') || actionName.startsWith("'")) {
						boundName = actionName.substring(1, actionName.length - 1);
					    } else {
						//Lyte.warn("Deprecation warning. Action name should be in quotes");
						boundName = actionName;
                        }
                        let actualAttrName = attr.globalEvent ? attr.name : (attr.name.indexOf("-") !== -1)? attr.name : attr.name.substr(2);
                        this._evBoundEvents[actualAttrName] = {"name" : boundName, "args" : attr.helperInfo.args, "from" : "component"};
                        let prevAttribute = this.getAttribute(attr.name);
                        let currentAttribute = this.component.constructor._template.getAttribute(attr.name);
                        //this.setAttribute("ev:"+attr.name, this.component.constructor._template.getAttribute(attr.name));
                        this.setAttribute(attr.name, currentAttribute + (prevAttribute ? " ; " + prevAttribute : ""));
                    }
                    if(!attr.globalEvent) {
                        attr.from = "component";
                        if(attr.staticValue) {
                            this.setAttribute(templateAttributes.a[key].name, attr.staticValue);
                        }
                        else if(_LC.directive.isDirectiveNode(attr)){
                            let actAttrName = attr.name;
                            let attrName = actAttrName;
                            let duplicateFound;
                            if(!this._specialAttributeDetails){
                                this._specialAttributeDetails = [];
                            }else{
                                this._specialAttributeDetails.forEach(function(oldAttr){
                                    if(oldAttr.hookName == actAttrName){
                                        duplicateFound = true;
                                    }
                                })
                            }
                            if(attr.hasOwnProperty("stringValue")){
                                
                                if(!duplicateFound){
                                    let strAttr = {
                                        hookName : attr.hookName,
                                        hookNode : attr.hookNode,
                                        name : attr.name,
                                        stringValue : attr.stringValue
                                    }
                                    this._specialAttributeDetails.push(strAttr);
                                }
                            }else if(attr.hasOwnProperty("dynamicValue")){
                                this.setAttribute(attr.hookName, "{{dummy}}");
                                let attrNode = this.attributes.getNamedItem(attr.hookName);
                                attr._isDirectiveNode = true;
							    this.bindNode(attrNode, [], undefined, {}, attr, undefined, undefined, true );
                                if(!this._specialAttributeDetails){
                                    this._specialAttributeDetails = [];
                                }
                                let dynAttr = {
                                    hookName : attr.hookName,
                                    hookNode : attr.hookNode,
                                    name : attr.name,
                                    dynamicValue : attr.dynamicValue
                                }
                                this._specialAttributeDetails.push(dynAttr);
                            }
                            
                        }
                        //@Slicer.developmentStart
                        else if(key.startsWith("@")){
                            _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this,"LC007")
                        }
                        //@Slicer.developmentEnd
                        else{
                            this.__lyteIgnore = true;
                            this.setAttribute(templateAttributes.a[key].name, "{{dummy}}");
                            this.__lyteIgnore = false;
							this.bindNode(this.attributes.getNamedItem(templateAttributes.a[key].name), [], undefined, {}, templateAttributes.a[key], undefined, undefined, true );
                        }
                    }
        			
        		}
        	}
        }
        // this._yields = this._yields || {};
        this.registerYields();
        let dumFlg;
        if(compData.lyteUnbound && !_LC.migratedv2(app)) {
        	_LC.unbound = true;
            dumFlg = true
        }
        let unboundDirective = _LC.directive.getTransitionArg(this,"unbound");
        if(unboundDirective){
            _LC.unbound = true;
        }
        if(!dumFlg){
            this.component.data.lyteUnbound = unboundDirective;//add in future   
        }
        let content = "";
        let fastRender,fastRenderSupported
        let unboundBeta = compData.lyteFastRender;
        fastRender = _LC.directive.getTransitionArg(this,"turbo");
        this.component.data.lyteFastRender = fastRender;
        if(fastRender){
            fastRenderSupported = _LC.directive.getTransitionArg(this,"unbound-supported") || _LC.directive.getTransitionArg(this,"turbo-supported");
            if(!fastRenderSupported){
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this, "LC008", this.localName)
                //@Slicer.developmentEnd
                this.component.data.lyteFastRender = false;
            }
        }
        
        let fastObj = this.getDirectiveIns("turbo");
        if((unboundBeta && !(_LC.migratedv2(app))) || (fastRender && fastRenderSupported && fastObj)){
            content = fastObj.renderFast(constr._dynamicNodes, constr._sta, compInstance, undefined, this);
            requestAnimationFrame(function() {
                requestAnimationFrame(function() {
                    _LC.callCC();
                });
            });
        }
        else {
            content = this.renderNodes(constr._template, constr._dynamicNodes,undefined,undefined, undefined, undefined, this.component.constructor._tC,undefined,true);
        }
        return content;
    }

    //RN
    renderNodes(toAppend, dynamicNodes, helperNode, options, establishBindings, returnVal, templateContent ,yieldComp, initialRender, _yields) {
        options = options || {};
        let content;
        let constr = this.constructor;
        // let Compile = this.component.constructor.Compile;
        let directiveObj = this.getDirectiveObj();
        /*if(Lyte._ie){
            let newFrag = toAppend.cloneNode(true, "lyte");
            this.constructor.splitTextNodes(newFrag);
            content = newFrag;
        }
        else{
            content = toAppend.cloneNode(true, "lyte");
        }*/
        let templateDepthHandlingNeeded = false;
        if (!toAppend) {
            templateDepthHandlingNeeded = true;
            content = _LC.getContentForIE(templateContent , constr);
        } else if(toAppend.hasAttribute("depth")) {
            templateDepthHandlingNeeded = true;
            content = _LC.getContentForIE(toAppend , constr);
        } else {
            content = toAppend.content.cloneNode(true, "lyte");
        }
        let updateHelpers = [],processLast = [],helperFunc,stoppedNode;
        let cmpData = _LC.getCmpData(this.component.data);
        
        let toBeInsMap = new Map();
        for(let i=0;i<dynamicNodes.length;i++) {
            let info = dynamicNodes[i], type = info.t, pos = info.p, dynamicN = content, helperInfo;
            dynamicN = getDynamicNode(dynamicN,pos);
            if(info.trans){
                dynamicN._hooksPresent = true;
            }
            if(!dynamicNodes._cache){
                dynamicNodes._cache = {};
            }
            if(type ===  "cD") {
                if(helperNode){
                    dynamicN._parentHelper = helperNode;
                }
                if(options.node) {
                    dynamicN._cx = options;
                } else if(helperNode) {
                    dynamicN._cx	= helperNode._cx;
                }
                  
                if(directiveObj){
                    directiveObj.infoCD(this,info,dynamicN,helperNode,options);
                }
                
                // dynamicN._callee = this//af check
            } 
            else if(type === "tX"){
                 this.bindNode(dynamicN, undefined, helperNode, options, dynamicNodes[i], processLast, establishBindings,undefined,dynamicNodes._cache,type,undefined,toBeInsMap);
            }
            else if(type === "a"){
                dynamicN._attributeDetails = info.a;            	
                if(dynamicN.nodeName === "LYTE-YIELD") {
                    dynamicN._callee = this;
                    dynamicN.component = {}
                    dynamicN.component.data = {};
                    defProp(dynamicN.component.data, "__component__", {
                        value : dynamicN,
                        configurable : true,
                        writable : true,
                        enumerable : false
                    });
                    dynamicN._properties = {};
                    for(let j=0;j<dynamicN.attributes.length;j++) {
                        let attr = dynamicN.attributes[j];
                        if(attr.nodeName !== "is" && attr.nodeName !== "yield-name") {
                            dynamicN._properties[_LC.String.toCamelCase(attr.nodeName)] = {};
                        }
                    }
                }
                let toBeRemoved = [];
                let processDirectiveLazy = [];
                for(let key in info.a) {
                    let attr = info.a[key];
                    attr._depthTemp = info._depthTemp;
                    let attrName = key;
                    let node;
                    if(attr && (attr.dynamicValue || attr.helperInfo)) {
                        if(options.node) {
                                dynamicN._cx = options;
                        }
                        else if(helperNode) {
                            dynamicN._cx = helperNode._cx;
                        }
                        let actionName, boundName;
                        if(attr.helperInfo && attr.helperInfo.name === "action") {
                            dynamicN._boundEvents = dynamicN._boundEvents || {};
                            actionName = attr.helperInfo.args[0];
                            if(actionName.startsWith('"') || actionName.startsWith("'")) {
                                boundName = actionName.substring(1, actionName.length - 1);
                            } else {
                                // Lyte.warn("Deprecation warning. Action name " + actionName + " must be in quotes");
                                boundName = actionName;
                            }
                            let actualAttrName = attr.globalEvent ? attrName : (attrName.indexOf("-") !== -1)? attrName : attrName.substr(2);
                            dynamicN._boundEvents[actualAttrName] = {"name" : boundName, "args" : attr.helperInfo.args};
                            //to bind actions in ssr components
                            let id;
                               
                        }            
                        if(!attr.globalEvent) {
                            /*this.bindNode(dynamicN.getAttributeNode(attrName), toBeRemoved, helperNode, options, attr, undefined, establishBindings);
                             */
                            if(!dynamicN.hasAttribute(attrName)) {
                                dynamicN.setAttribute(attrName, "{{dummy}}");
                            }
                            node = this.bindNode(dynamicN.attributes.getNamedItem(attrName), toBeRemoved, helperNode, options, attr, undefined, establishBindings,undefined,dynamicNodes._cache,type,i);
                            if(node !== dynamicN.attributes.getNamedItem(attrName)) {
                                dynamicN._removedAttributes = dynamicN._removedAttributes || {};
                                dynamicN._removedAttributes[attrName] = node;
                            }
                        }
                    }
                    if(attr.hookNode){
                        if(attr.hookName == "unbound" || attr.hookName == "turbo" || attr.hookName == "shadow" || attr.hookName == "shadow-style" || attr.hookName == "shadow-deep" || attr.hookName =="hide-tag"){
                            if(!dynamicN._transitionAppend){
                                dynamicN._transitionAppend = []
                            }
                            if(yieldComp && !yieldComp._transitionAppend){
                                yieldComp._transitionAppend = []
                            }
                            if(this && !this._transitionAppend){
                                this._transitionAppend = []
                            }
                            _LC.directive.infoA(this,info,dynamicN,helperNode,attr,yieldComp,options);
                        }
                        
                        else if(directiveObj){
                            directiveObj.infoA(this,info,dynamicN,helperNode,attr,yieldComp,options);
                        }
                    }
                }
                
                //Added now
                if(info.a && Object.keys(info.a).length) {
                    dynamicN._callee = this;
                }
                for(let d=0;d<toBeRemoved.length;d++) {
                    dynamicN.removeAttribute(toBeRemoved[d]);
                }
            }
            else if(/^(f|fI|cM|eM)$/.test(type)){
                dynamicN._cases = info.c;
                if(options.node) {
                    dynamicN._cx = options;
                } else if(helperNode) {
                    dynamicN._cx	= helperNode._cx;
                }
                if(directiveObj){
                    directiveObj.infoF(this,info,dynamicN,helperNode,type,options);
                }
                dynamicN._dynamicNodes = info.dN;
                // if(Lyte._ms) {
                //     dynamicN._tC = info.templateContent;  
                // }
                if(info.actualTemplate) {
                    dynamicN._tC = info.actualTemplate;
                }
                if(!dynamicN._hiddenTemplate){
                    dynamicN._hiddenTemplate = [];
                 }
                let returnVal;
                switch(type) {
                case "f" : 
                    dynamicN._ht = info._ht;
                	returnVal = this.updateForHelper(dynamicN,{"type" : "default"} , options.node? options : undefined, establishBindings, info._sta ,yieldComp, initialRender, _yields);
                	break;
                case "fI" : 
                    dynamicN._ht = info._ht;
                	returnVal = this.updateForInHelper(dynamicN,{"type" : "default"} , options.node? options : undefined, establishBindings ,yieldComp, initialRender, _yields);
                	break;
                case "cM" : 
                	returnVal = this.updateDynamicComponent(dynamicN, false, options.node ? options : undefined, establishBindings ,yieldComp, initialRender, _yields);
                    break;
                
                }
                if(returnVal) {
                    updateHelpers.$push(returnVal);
                }
            }
            else if(/^(e|s)$/.test(type)){
                var def ="default";
                dynamicN._cases = info.c; 
                dynamicN._hd = info.hd;
                dynamicN._co = info.co
                dynamicN._default = info.d;
                dynamicN._ht = info._ht;
                if(options.node) {
                    dynamicN._cx = options;
                } else if(helperNode) {
                    dynamicN._cx	= helperNode._cx;
                }
                if(directiveObj){
                    directiveObj.infoE(this,helperNode,dynamicN,info,options);
                }
                if(info.actualTemplate) {
                    dynamicN._tC = info.actualTemplate;
                    if(!dynamicN._origTemplate) {
                        // if(Lyte._ie) {
                        // 	dynamicN._origTemplate = createElement("template")
                        // } else {
                            dynamicN._origTemplate = info._depthTemp.cloneNode(true);
                        // }
                        if(dynamicN.hasAttribute("value")) {
                            dynamicN._origTemplate.setAttribute("value", dynamicN.getAttribute("value"));
                        }
                        if(dynamicN.hasAttribute("l-c")) {
                            dynamicN._origTemplate.setAttribute("l-c", dynamicN.getAttribute("l-c"));
                        }
                    }
                }
                if(!dynamicN._hiddenTemplate){
                    dynamicN._hiddenTemplate = [];
                }
                let id,obj;
                
                
                let returnVal = this.updateSwitchHelper(type, dynamicN, options.node ? options : undefined, undefined, establishBindings ,yieldComp, _yields ,undefined,helperNode,id,i);
                if( !_LC.unbound && ( this._ssrBind || ( this._callee && this._callee._ssrBind ) )){
                    // delete obj.node;
                    // obj.case = dynamicN._currentCase || dynamicN._currentScope; 
                    if(dynamicN.getAttribute("l-c")){
                        obj._new = dynamicN.getAttribute("_new") ? true:undefined;
                        obj.lc = true;
                        obj.case = dynamicN._currentScope;
                    }else{
                        obj.case = dynamicN._currentCase; 
                    }
                }
                if(returnVal) {
                    updateHelpers.$push(returnVal);
                    let isBreak = returnVal.toAppendMain.querySelector("template[is=break]");
                    if(isBreak) {
                        dynamicN._isStopped = "break";
                        content = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getTrimmedContent(content, info.p,undefined);
                        stoppedNode = info.p;
                        break;
                    }
                    let isContinue = returnVal.toAppendMain.querySelector("template[is=continue]");
                    if(isContinue) {
                        dynamicN._isStopped = "continue";
                        content = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getTrimmedContent(content, info.p,undefined);
//                		stoppedNode = info.position;
                        break;
                    }
                }
            } else if(type === "r") {
                dynamicN._childPromise = [];
                
                dynamicN._dynamicNodes = info.dN;
                dynamicN._sta = info._sta;
                dynamicN._ht = info._ht;
                // if(Lyte._ms) {
                //     dynamicN._tC = info.templateContent;  
                // }
//                updateHelpers.$push(dynamicN);
                if(directiveObj){
                    dynamicN._childPromise = [];
                    directiveObj.infoE(this,helperNode,dynamicN,info,options);
                }
                if(options.node) {
                    dynamicN._cx = options;
                }
                else if(helperNode) {
                    dynamicN._cx = helperNode._cx;
                }
                //Added now                
                dynamicN._callee = this;
            } else if(type === "i") {
                if(helperNode){
                    dynamicN._parentHelper = helperNode;
                }
                if(options.node) {
                    dynamicN._cx = options;
                } else if(helperNode) {
                    dynamicN._cx	= helperNode._cx;
                }
                if(!dynamicN._hiddenTemplate){
                    dynamicN._hiddenTemplate = [];
                }
                
                dynamicN.component = dynamicN.component || {"data" : {}};
                dynamicN._properties = dynamicN._properties || {};
                if(directiveObj){
                    directiveObj.infoI(this,info,dynamicN,helperNode,options);
                }
                let dData = _LC.getCmpData(dynamicN.component.data);  
                for(let x=0; x<dynamicN.attributes.length; x++) {
                    let attrObj = dynamicN.attributes[x];
                    let attrName = attrObj.name;
                    let attrValue = attrObj.value;
                    if(attrName !== "yield-name") {
                        if(dynamicN._properties && !dynamicN._properties[attrName]){
                            dynamicN._properties[attrName] = {};
                        }
                        if(dynamicN._attributeDetails && !dynamicN._attributeDetails[attrName]){
                            dData[attrName] = attrValue;
                        }
                    }
                }
                this.updateYield(dynamicN, false, options.node? options : undefined,helperNode, _yields);
                if(info.chld){
                    dynamicN._chld = info.chld;
                }
                if(info.sibl){
                    dynamicN._sibl = info.sibl;
                }
            }
        }
        dynamicNodes._cache = undefined;
        for(let i=0;i<processLast.length;i++) {
             let dynamicPosition = processLast[i].dynamicPositions;
                 let processNode = dynamicPosition.initialNode;
                 let nodeValue = dynamicPosition.dynamicNodeValue;
                 let childLen = nodeValue.childNodes.length;
                 if(!childLen) {
                    nodeValue.appendChild(document.createTextNode(""));
                        childLen = 1;
                 }
                 let startingNode = nodeValue.childNodes[0];
                 if(processNode.parentNode.nodeName === "#document-fragment") {
                    while(nodeValue.childNodes.length) {
                        _LC.insertBeforeNative(processNode.parentNode, nodeValue.childNodes[0], processNode);
                    }
                    processNode.remove();
                 } else {
                     processNode.replaceWith.apply(processNode,nodeValue.childNodes);
                 }
                 processLast[i].dynamicPositions = {startingNode : startingNode, length: childLen}
        }
        if(toBeInsMap.size){
            toBeInsMap.forEach(function(val, key){
                val.pN.insertBefore(key, val.cN);
                if(val.cN.nextSibling){
                    val.pN.insertBefore(document.createElement("textend"), val.cN.nextSibling);
                }
                else{
                    val.pN.appendChild(document.createElement("textend"));
                }
            })
        }  
        if(stoppedNode) {
            returnVal = returnVal || {};
            returnVal.stop = true;
        }
        if(helperNode) {
            if(options.type) {
                helperNode._helpers[options.itemIndex] = updateHelpers;
                if(templateDepthHandlingNeeded) {
                    content = constr.createDocFragment1(content);  
                }
                return content;
            } else {
                helperNode._helpers = helperNode._helpers || [];
                helperNode._helpers.$push.apply(helperNode._helpers, updateHelpers);
                if(templateDepthHandlingNeeded) {
                    content = constr.createDocFragment1(content);  
                }
                return content;
            }
        }
        this.executeBlockHelpers(updateHelpers);
        if(templateDepthHandlingNeeded) {
            content = constr.createDocFragment1(content);  
        }
        return content;
    }

    executeBlockHelpers(updateHelpers, node) {
	    for(let i=0;i<updateHelpers.length;i++) {
            var lastNode = updateHelpers[i].lastNode;
            var parentNode = lastNode.parentNode; 
                
            if(lastNode._placeHolder) {
                lastNode = lastNode._placeHolder;
                parentNode = lastNode.parentNode;
            }
            //parentNode = updateHelpers[i].lastNode.parentNode || updateHelpers[i].lastNode._placeHolder.parentNode;
            _LC.insertBeforeNative(parentNode, updateHelpers[i].toAppendMain, lastNode);
            updateHelpers[i] = updateHelpers[i].lastNode;
	    		// updateHelpers[i].lastNode.parentNode.insertBefore(updateHelpers[i].toAppendMain, updateHelpers[i].lastNode);
	    		// updateHelpers[i] = updateHelpers[i].lastNode;
	    		//updateHelpers[i]._parentIf = node;
//	    		if(!updateHelpers[i]._cx && node) {
//	    			updateHelpers[i]._cx = node._cx;
//	    		}
	    }	
    }

    updateBlockHelpers(updateHelpers,contextSwitchInfo){
        for(let i=0;i<updateHelpers.length;i++) {
            switch(updateHelpers[i].getAttribute('is')) {
            case "f" :
                this.updateForHelper(updateHelpers[i], {"type" : "default"},contextSwitchInfo);
                break;
            case "e" : 
                this.updateSwitchHelper("e",updateHelpers[i],contextSwitchInfo);
                break;
            case "fI" : 
                this.updateForInHelper(updateHelpers[i] , {"type" : "default"},contextSwitchInfo);
                break;
            case "s" :
                this.updateSwitchHelper("s",updateHelpers[i],contextSwitchInfo);
                break;
            case "cM" : 
                this.updateDynamicComponent(updateHelpers[i], false, contextSwitchInfo);
                break;
            case "i" : 
//              this.updateYield(updateHelpers[i], false, contextSwitchInfo);
                break;
            case "yield" : 
            case "r" : 
                if(contextSwitchInfo) {
                    updateHelpers[i]._cx = contextSwitchInfo;
                }
                break;
            default: 
                if(updateHelpers[i].tagName === "LYTE-YIELD") {
                    this.updateYield(updateHelpers[i], false, contextSwitchInfo);
                }
                
            }
         }
    }
    //AttributeChangedCallback will be called for the attributes mentioned in the this._observedAttributes array. 
    static get observedAttributes() {
    		let newArr = [];
            for(let i=0;i<this._observedAttributes.length;i++) {
                newArr[i] = _LC.String.dasherize(this._observedAttributes[i]);
            }
            this.componentClass._registryClass._observedAttributes.forEach(function(name){
                newArr.push(_LC.String.dasherize(name));
            })
            let customPropHandlers;
            if(this.componentClass){
                customPropHandlers = this.componentClass._registryClass.customPropHandlers
            }else{
                customPropHandlers = _LC.customPropHandlers;
            }
            customPropHandlers.forEach(function(item,index) {
                newArr.$push(_LC.String.dasherize(item));
            })
            return newArr;	
    }

    //Callback from browser, whenever any of the observed attribute changes. 
    //We call the component set, in order to affect the related changes. 
    attributeChangedCallback(attr, oldValue, newValue) {
        this._attributeChangedCallback(attr, oldValue, newValue);
    }
    _attributeChangedCallback(attr, oldValue, newValue){
        if(!this._registryClass || !this.$registry.registeredComponents[this.localName]) {
            return;
        }
        let constr = this.constructor;
       	if((constr._observedMethodAttributes && constr._observedMethodAttributes[attr]) || this.__lyteIgnore) {
   		 	return;
    	}
        let actualAttr = _LC.String.toCamelCase(attr);
        let isCustomProp = this.component._registryClass.customPropHandlers.indexOf(actualAttr);
        if(isCustomProp !== -1) {
            let propValue = this.component._registryClass.customPropHandlers[isCustomProp];
            let lyteProps = newValue;
            if(lyteProps) {
                try{
                    lyteProps = JSON.parse(lyteProps);
                    for(let key in lyteProps) {
                        let actKey = propValue + _LC.String.upperCaseFirstLetter(key);
                        this.set(actKey, lyteProps[key]);
                    }
                } catch (e) {
                    //@Slicer.developmentStart
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this, "LC001", attr, this.localName);
                    //@Slicer.developmentEnd
                }
            }
            return;
        }
        if(oldValue === newValue) {
            return;
        }
        var attrNode = this.attributes.getNamedItem(attr);
        if(attrNode) {
            if(attrNode.__lyteIgnore) {
                this.attributes.getNamedItem(attr).__lyteIgnore = false;
                return;    
            }
        } else if(this["__"+attr]) {
            this["__"+attr] = false;
            return;
        }
        let compInstance = this.component;
        let dataType = compInstance.__data[actualAttr].type;
        if(dataType !== "string") {
        	let obj = {"attr" : attr, "tagName" : this.tagName};
        	newValue = _LC.typeCast(newValue, dataType, obj);
        	if(obj.isError) {
                //@Slicer.developmentStart
        		_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn(
                    "data type of the value provided for attribute "+attr+ " of " + this.tagName + " is not valid"
                );
                //@Slicer.developmentEnd
        		return;
        	}
        }
        let compData = compInstance.data;
        if(compData[actualAttr] !== newValue) {
            // Null check is done because when we do a removeAttribute directly on a component, the corresponding value expected is that of undefined and not null.
            this.set(actualAttr, newValue === null ? undefined: newValue);
        } else {
            _LC.clearError(compData, actualAttr);
        }
    }

    //Used to remove helpers of specific index in a for helper. 
    removeHelpersSpecificIndex(node, index,totalProms,fakeRemove, previousPromise,destroyChild, type) {
        let isType = node.getAttribute("is");
        if(node.hc){
            fakeRemove = true;
        }
        if(node._helpers[index]) {
            for(let j=0;j<node._helpers[index].length;j++) {
                    this.removeHelpers(node._helpers[index][j],undefined,undefined,totalProms,fakeRemove,previousPromise, index, node, type);
            }
        }
        
        //@Slicer.$hideStart
        let _LCHD = this.getDirectiveIns("hide");
        _LCHD && _LCHD.rmHelpersSpecificIndex(node, index);
        //@Slicer.$hideEnd        
        let directiveObj = this.getDirectiveObj();
        if(directiveObj && node.hc && node._forContent[index] && node._specialNodes){
            directiveObj.removeForIndexContent(this,node,totalProms,previousPromise,index);
        }else if(node._forContent[index]) {
            if(!fakeRemove){
                // this.rmOtherNodes(node,node._forContent[index],totalProms);
                // if(node._helpers[index]) {
                //     for(let j=0;j<node._helpers[index].length;j++) {
                //         directiveObj.removeFromDomNormal(node._helpers[index][j]);
                //     }
                // }
                if(node._forContent[index]) {
                    for(let i=0;i<node._forContent[index].length; i++ ) {
                        node._forContent[index][i].remove();
                    }
                }
            }
            var self = this;
            Object.keys(node._items[index]._dynamicProperty).forEach(function(key) {
                node._dynProps[key]--;  
                if(!node._dynProps[key]) {
                    let prop  = self.getProperty(key);
                    if(prop._helperNodes) {
                        prop._helperNodes[delStr](node);
                    }
                }
            });
            node._items[index] = {"_dynamicProperty" : {}, "itemProperty" : {}, "indexProperty": {}, "propProperty" : {},"propPropertyDyn" : {}, "propNodes" : {}};
        }
    }
    //Used to remove all the helpers within an helper. 
    removeHelpers(node, update, direct,totalProms,fakeRemove,previousPromise,updateIndex, parentNode, type) {
        let isType = node.getAttribute("is");
        if(!direct) {
            node.remove();
            var helpersObj = node.getAttribute("is") === "component" ? this.__dc : this.__h;
            let nodeId = node.__lyteId;
            delete helpersObj[nodeId];
        }
        if(direct && node.hc){
            fakeRemove = true;
        }

        //@Slicer.$hideStart
        let _LCHD = this.getDirectiveIns("hide");
        //@Slicer.$hideEnd      
        var del = "delete";
        let parent;
        var contextSwitchArray = [];
        let directiveObj = this.getDirectiveObj();
        
        _LC.adCx(node, contextSwitchArray);
        if(node._forContent) {
            if(node.getAttribute("is") === "for") {
            if(node._helpers) {
                for(let i=0;i<node._helpers.length;i++) {
                    for(let j=0;j<node._helpers[i].length;j++) {
                            this.removeHelpers(node._helpers[i][j],undefined,undefined,totalProms,fakeRemove,previousPromise, updateIndex, parentNode, type);
                    }
                    directiveObj && directiveObj.checkFakeForAndRemove(fakeRemove,node,i);
                }
            }
            
            
            if(directiveObj && node.hc && node._specialNodes && node._specialNodes.length){
                directiveObj.removeForContent(this,direct,fakeRemove,node,totalProms);
            }else{
                if(!fakeRemove){
                    _LC.removeForContent(node);
                }
            }
            let key = node.getAttribute("item");
            if(node._items.length) {
                let prop = node._items[0].itemProperty;
                for(let i=0;i<node._items.length;i++) {
                    let dynProp = node._items[i]._dynamicProperty;
                    for(let dP in dynProp) {
                        let property = this.getProperty(dP); 
                        if(property._helperNodes){
                            property._helperNodes[del](node);                    
                        }
                    }    
                }
                if(prop) {
                    for(let i=0;i<node._items.length;i++) {
                        this.removeBindings({[key] : node._items[i].itemProperty}, {[key] : node._attributes.items[i]});
                    }
                }
            }
            if(!update) {
                if(node._actualBinding) {
                    if(node._attributes.items && node._attributes.items._bindings && node._actualBinding._createdBinding) {
                        node._attributes.items._bindings[del](node._actualBinding);
                    } 
                    if(node._actualBinding._forHelpers) {
                        node._actualBinding._forHelpers[del](node);
                    }
                    var multiplePrpty = node._removedAttributes.items._multipleProperty;
                    if(node._removedAttributes && node._removedAttributes.items && !node._removedAttributes.items.helperValue && multiplePrpty && multiplePrpty[0].actProp._forHelpers) {
                        multiplePrpty[0].actProp._forHelpers[del](node);
                    }
                }
            }
            node._items = [];
        } else {
            if(node._helpers) {
                let keys = Object.keys(node._helpers);
                for(let i=0;i<keys.length;i++) {
                    for(let j=0;j<node._helpers[keys[i]].length;j++) {
                        this.removeHelpers(node._helpers[keys[i]][j],undefined,undefined, totalProms, fakeRemove, previousPromise, updateIndex, parentNode, type);
                    }
                    directiveObj && directiveObj.checkFakeForAndRemove(fakeRemove,node,keys[i]);
                }
            }

            
            
            if(directiveObj && node.hc && node._specialNodes && Object.keys(node._specialNodes).length){
                directiveObj.removeForInContent(this,direct,fakeRemove,node,totalProms);
            }else{
                if(!fakeRemove){
                    _LC.removeForInContent(node);
                }
            }
            let items = node._items;
            let key = node.getAttribute("key");
            for(let index in items){
                let item = items[index];
                let prop = item.itemProperty;
                let dynamicProp = item._dynamicProperty;
                for(let dP in dynamicProp) {
                    let property = this.getProperty(dP); 
                    if(property._helperNodes){
                        property._helperNodes[del](node);                    
                    }
                }
                if(prop) {
                    this.removeBindings({[key] : node._items[index].itemProperty}, {[key] : node._attributes.object[index]});
                }

            }
            if(!update) {
                if(node._actualBinding) {
                    if(node._attributes.object && node._attributes.object._bindings && node._actualBinding._createdBinding) {
                        node._attributes.object._bindings[del](node._actualBinding);
                    } 
                    if(node._actualBinding._forHelpers) {
                        node._actualBinding._forHelpers[del](node);
                    }
                }
                var multiplePrpty = node._removedAttributes.object._multipleProperty;
                if(node._removedAttributes.object && !node._removedAttributes.object.helperValue && multiplePrpty && multiplePrpty[0].actProp._forHelpers) {
                    multiplePrpty[0].actProp._forHelpers[del](node);
                }
                if(node._propBindingObject && node._attributes.object && node._attributes.object._bindings) {
                    node._attributes.object._bindings[del](node._propBindingObject);
                }
            }
            node._items= {};
          }
          
          //@Slicer.$hideStart
          _LCHD && _LCHD.forRmHelpers(node);
          //@Slicer.$hideEnd
        } else if(node._caseContent || node._yieldContent) {
            if(node._helpers) {
                for(let j=0;j<node._helpers.length;j++) {
                    this.removeHelpers(node._helpers[j],undefined,undefined, totalProms, fakeRemove, previousPromise);
                }
                directiveObj && directiveObj.checkFakeIfAndRemove(fakeRemove,node);
            }
            
            
            if(directiveObj){
                directiveObj.removeIfContent(this,direct,fakeRemove,node,totalProms,previousPromise,this);
            }else{
                _LC.removeIfContent(node)
                _LC.removeIfCaseContent(this,node,direct);
                if(node._yieldContent){
                    for(let i=0;i<node._yieldContent.length; i++ ) {
                        node._yieldContent[i].remove();
                    }
                }
            }
            if(node._caseContent){
                for(let key in node._dynamicProperty) {
                    if(node._dynamicProperty[key].isActualNode) {
                        node._dynamicProperty[key].isActualNode._helperNodes[del](node);
                    }else {
                        let helperNodes = this.getProperty(key)._helperNodes;
                        if(helperNodes) {
                            helperNodes[del](node);
                        }
                    }
                }
            }
            //@Slicer.$hideStart
            _LCHD && _LCHD.ifRmHelpers(node);
            //@Slicer.$hideEnd
            node._dynamicProperty = {};
            let viewObj = this.getDirectiveIns("view")
            if(viewObj){
                viewObj.rmCaseContent(node)
            }
        } else if(node._renderedComponent) {
            for(let key in node._renderedComponent) {
                if(node._renderedComponent[key]) {
                    var renderedComponent = node._renderedComponent[key];
                    if(directiveObj && direct && renderedComponent._hooksPresent){
                        directiveObj.removeRenderedComponent(comp,node,previousPromise);
                    }else{
                        if(!fakeRemove){
                            node._renderedComponent[key].remove();
                            node._renderedComponent[key] = null;
                        }
                    }
                }
            }
        }
          _LC.rmCx(node, contextSwitchArray);
          if(!fakeRemove){
            node._helpers = [];
            
          }
    }
    updateYield(node, update, contextSwitchInfo,helperNode, _yields) {
        let shadowObj = this.getDirectiveIns("shadow");
        let directiveObj = this.getDirectiveObj();
        let app = this.component.getAppOrAddon();
        
        if(directiveObj){
            node._childPromise = [];
            node._specialNodes = [];
        }
        let constr = this.constructor;
        if(!node._callee) {
            node._callee = this;
        }        
        let actYields = _yields || node._callee._yields;
        let toAppend = actYields[_LC.getYieldName(node)];
        if(!toAppend) {
            if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue){
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue.updateYield(this, node);
            }
            return;
        }
        node._registerYield = toAppend;
        //ADded now
        let parentScope = toAppend._callee || node._callee._callee;
        if(!parentScope) {
            // if(Lyte._ms) {
            //     var div = createElement("div");
            //     div.innerHTML = toAppend.outerHTML;
            //     // if(Lyte._ie){
            //     //     div.firstChild.innerHTML = toAppend.innerHTML;
            //     //  }
            //     var content1 = div.childNodes[0];
            //     constr.splitTextNodes(content1);
            //     content1 = constr.createDocFragment1(content1);
            //     if(shadowObj && directiveObj && directiveObj.getTransitionArg(this,"shadow-supported") && directiveObj.getTransitionArg(this,"shadow") == true){
            //         shadowObj.updateYield(this,node,toAppend,content1);
            //     }else if(content1){
            //         node.appendChild(content1);
            //     }
            //     else{
            //         node.appendChild(toAppend.content.cloneNode(true, "lyte"));
            //     }
            // } else {
                 if(shadowObj && directiveObj && directiveObj.getTransitionArg(this,"shadow-supported") && directiveObj.getTransitionArg(this,"shadow") == true){
                    shadowObj.updateYield(this,node,toAppend);
                }else{
                    node.appendChild(toAppend.content.cloneNode(true, "lyte"));
                }
            // }
            
            node._helpers = [];
            return;
        }    
        if(!toAppend._callee) {
            toAppend._callee = parentScope;
        } 
        node._dynamicProperty = node._dynamicProperty || {};
        //set values from child component. 
        let obj = {},contextSwitchingArray = {},self = this,contextSwitchArray = [];
        _LC.adCx(toAppend, contextSwitchArray);
        var cmpData = _LC.getCmpData(parentScope.component.data);
        Object.keys(node._properties).forEach(function(key) {
            contextSwitchingArray[key] = {};
            contextSwitchingArray[key].value = cmpData[key];
            contextSwitchingArray[key].property = parentScope._properties[key];
            parentScope._properties[key] = node._properties[key];
            let nData = _LC.getCmpData(node.component.data);
            cmpData[key] = nData[key];
        }); 
        // htA -> helpertemplateApplied
        if(toAppend._ht && !toAppend._htA) {
            toAppend._htA = true;
            toAppend.content.append(toAppend._ht.content.cloneNode(true));
        }
        let yieldComp = node._callee;
        let content = parentScope.renderNodes(toAppend, toAppend._dynamicNodes || [], node, {"node" : node}, true, undefined, toAppend._tC , yieldComp, _yields);
        directiveObj && directiveObj.updateSpecialNodeRef(this,node,toAppend,helperNode);
        if(!_LC.directive.getTransitionArg(node,"unbound")) {
            let nData = _LC.getCmpData(node.component.data);  
            _LC.establishBindings(node._properties, nData);
        }else if(!node.component.data.lyteUnbound && !_LC.migratedv2(app)) {     
            let nData = _LC.getCmpData(node.component.data);     	
            _LC.establishBindings(node._properties, nData);
        }
        parentScope.executeBlockHelpers(node._helpers);
        Object.keys(node._properties).forEach(function(key) {
            cmpData[key] = contextSwitchingArray[key].value;
            parentScope._properties[key] = contextSwitchingArray[key].property;
        });
        _LC.rmCx(toAppend, contextSwitchArray); 
        if(shadowObj  && _LC.directive.getTransitionArg(this,"shadow-supported") && _LC.directive.getTransitionArg(this,"shadow") == true){
            shadowObj.updateYield(this,node,toAppend,content,parentScope);
        }else if(content){
            node.appendChild(content);
        }
        else{
            node.appendChild(toAppend.content.cloneNode(true, "lyte"));
        }
    }
    //upddc
    updateDynamicComponent(node, update, contextSwitchInfo, establishBindings,idx,helperNode) {
        if(!node._specialNodes){
            node._specialNodes = [];
        }
        let returnVal;
        // let registryClass = this._registryClass;
        let directiveObj = this.getDirectiveObj();
        node._callee = this;
        let keepAlive = node.hasAttribute("lyte-keep-alive");
        if(!node._placeHolder){
            let emptyTextNode = document.createTextNode("");
            _LC.replaceWithPf(node, emptyTextNode);
            node._placeHolder = emptyTextNode;
            emptyTextNode._helper = node;
            // node._placeHolder.__lytehelper = node._placeHolder._helper;
            node._placeHolder._actTemplate = node;
            _LC.tDiv.content.appendChild(node);
            if(this._removedTemplate){
                this._removedTemplate.push(node);
            }else{
                this._removedTemplate=[];
                this._removedTemplate.push(node);
            }
        }
        if(!node._renderedComponent) {
            node._renderedComponent = {};
            let id = _LC.createLyteId(this);
            this.__dc[id] = node;
            node.__lyteId = id;
        }
        
        node._cx = contextSwitchInfo || node._cx;
        node._dynamicProperty = node._dynamicProperty || {};
        let componentName = node.getAttribute("component-name") || (node._attributes ? node._attributes["component-name"] : undefined);
        let componentClass =  node._attributes ? node._attributes["component-class"] : undefined;
        // let registryInstance = (node._initProperties ? node._initProperties.lyteRegistry : undefined) || (node._attributes ? node._attributes["lyte-registry"] : undefined);
        let registryInstance = _LC.getRegFromAttr(node, "dynamicComponent");
        let regClass;
        if(!componentName && !componentClass) {
            return;
        }
        //@Slicer.developmentStart
        // else if(componentName){
        //     ComponentError.warn("LC016",componentName);
        // }
        //@Slicer.developmentEnd
        else if(componentClass){
            componentName = componentClass._compName;
            regClass = componentClass._registryClass
        }
        if(directiveObj){
            node._childPromise = []
            directiveObj.checkOngoingPromises(node);
            node._totalPromise = [];
        }
        let component,newComponent = false;
        if(update) {
            if(keepAlive) {
                _LC.setIgnoreDisconnect(true);
            }
            if(node._renderedComponent[node._currentComponent]) {
                var activeComponent = node._renderedComponent[node._currentComponent];
                if(activeComponent){
                    if(directiveObj && activeComponent._specialNodes && activeComponent._specialNodes.length>0){
                        directiveObj.updateRenderedComp(this,activeComponent,node,keepAlive)
                    }else{
                        if(keepAlive){
                            _LC.hDiv.content.appendChild(activeComponent) 
                        }else{
                            activeComponent.remove();
                        }
                        
                    }
                }
            }
            _LC.setIgnoreDisconnect(false);
            if(!keepAlive) {
                node._dynamicProperty = {};
            }
            if(node._renderedComponent[componentName] && keepAlive) {
                component = node._renderedComponent[componentName];
            } else {
                if(registryInstance){
                    component = registryInstance.render(componentClass,{});
                }else if(regClass && regClass._instanceList && regClass._instanceList[0]){
                    component = regClass._instanceList[0].render(componentClass,{});
                }
                else{
                    component = createElement(componentName);
                }
                newComponent = true;
            }
        }  else {
            // var flag = false;
            // if(node._initProperties && node._initProperties.lyteRegistry){
            //     _LC.setCurrentRegistryIns(node._initProperties.lyteRegistry);
            //     flag = true;
            // }
            // component = createElement(componentName);
            // if(flag){
            //     _LC.setCurrentRegistryIns(undefined);
            // }
            if(registryInstance){
                component = registryInstance.render(componentClass,{});
            }else if(regClass && regClass._instanceList && regClass._instanceList[0]){
                component = regClass._instanceList[0].render(componentClass,{});
            }
            else{
                component = createElement(componentName);
            }
            newComponent = true;
        }
        if(!keepAlive && node._currentComponent) {
            node._renderedComponent[node._currentComponent] = null;
        }
        if(newComponent) {
//          let componentData = {};
            for(let i=0;i<node.attributes.length;i++) {
                if(node.attributes[i].name !== "is" && node.attributes[i].name !== "component-name" && node.attributes[i].name !== "component-class" && node.attributes[i].name !== "lyte-keep-alive" && !node.attributes[i].name.startsWith("@")) {
                    component.setAttribute(node.attributes[i].name, node.attributes[i].value);
                }
            }
//          componentData = component._attributes;
            if(node._attributes) {
                _LC.$variables(node);
                for(var key in node._attributes) {
                    if(key == "$data"){
                        component.setData(node._attributes[key]);
                    }
                    else if(key!== "component-name" && key!== "component-class") {
                        component.setData(_LC.String.toCamelCase(key), node._attributes[key]);
                    }
                }
            }
            let toAppend = this.renderNodes(node, node._dynamicNodes, node, undefined, establishBindings, undefined, node._tC);
            component.appendChild(toAppend);
            if(directiveObj && activeComponent){
                component._dependentPromise = activeComponent._totalPromise;
            }
            component._toRegEvnts = node._toRegEvnts;
        }
        if(directiveObj){
            let placeHolder,placeHolderParent;
            if(!node._placeHolder){
                placeHolder = node;
                placeHolderParent = node.parentNode;
            }else{
                placeHolder = node._placeHolder;
                placeHolderParent = node._placeHolder.parentNode;

            } 
            returnVal = directiveObj.updateDynamicComp(this,update,component,activeComponent,node,newComponent,placeHolder,placeHolderParent);
        }else{
            if(!update) {
                returnVal = {"toAppendMain" : component, "lastNode" : node};
            } else {
                _LC.setIgnoreDisconnect(true);
                if(!node._placeHolder){
                    _LC.insertBeforeNative(node.parentNode,component, node);
                }else{
                    _LC.insertBeforeNative(node._placeHolder.parentNode,component, node._placeHolder); 
                } 
                _LC.setIgnoreDisconnect(false);
            }
        }
        component._dynComp = true;
        node._renderedComponent[componentName] = component;
        node._currentComponent = componentName;
        component._callee = this;
        
        component._actions = node._actions;
        component.setMethods(node._initMethods);
        component._attributeDetails = node._attributeDetails;
        component._boundEvents = node._boundEvents;
        component._cx = node._cx;
        return returnVal;
    }
    // It constructs/updates the for helper. 
    updateForHelper(node, options, contextSwitchInfo, establishBindings, staticTempArr,yieldComp, initialRender, _yields) {
        
        let directiveObj = this.getDirectiveObj();
        directiveObj && directiveObj.instanciateForPromises(node);
        
        let app = this.component.getAppOrAddon();
        if(node.tagName !== "TEMPLATE") {
            Object.keys(node).forEach(function(item) {
            	if(item !== "innerHTML" && item !== "innerText") {
	                node._origTemplate[item] = node[item];            	
            	}
            });
            // if(Lyte._ie) {
            // 	var div = createElement("div");
            //     div.innerHTML = node._tC;
            //     node._origTemplate.innerHTML = div.children[0].innerHTML;
            //     this.constructor.splitTextNodes(node._origTemplate);
            //     if(node.hasAttribute("depth")) {
            //         node._origTemplate.setAttribute("depth", node.getAttribute("depth"));    
            //     }
            // }
            let placeHolder = node._origTemplate._placeHolder = document.createTextNode("");
            node._origTemplate.setAttribute("item", node.getAttribute("item"));
            node._origTemplate.setAttribute("index", node.getAttribute("index"));
            
            if(!_LC.migratedv2(app) && node.hasAttribute("unbound")) {
                //What if unbound is dynamic attribute ? It will be set in _attributes of node._origTemplate -> So No worries (y)
                node._origTemplate.setAttribute("unbound", "true");
            }
            //node.replaceWith(node._origTemplate._placeHolder);
            _LC.replaceWithPf(node, node._origTemplate._placeHolder);
            // node = _LC.replaceWithOrigTemplate(node)
            node = node._origTemplate;
            node.setAttribute("is", "for");
            _LC.apdNode(node, this);
            placeHolder._helper = node;
            
        } else if(!node._placeHolder){
            var emptyTextNode;
            
                emptyTextNode = document.createTextNode("");
                
            //node.replaceWith(emptyTextNode);
            _LC.replaceWithPf(node, emptyTextNode)
            node._placeHolder = emptyTextNode;
            emptyTextNode._helper = node;
            _LC.apdNode(node, this);
        }
        node._sta = node._sta || staticTempArr;
        let callee = this;
        node._callee = this;
        node._attributes = node._attributes || {};
        if(options.type === "update" && node._currentItems === node._attributes.items && (!options || !options.force)) {
            return {};
        }
        node._cx = contextSwitchInfo || node._cx;
        let  indexValue = node.getAttribute("index");
        if(!indexValue) {
            node.setAttribute("index", "index");
            indexValue = "index";
        }
        let itemValue = node.getAttribute("item");
        if(!itemValue) {
            node.setAttribute("item", "item");
            itemValue = "item";
        }
        
        let cmpData = _LC.getCmpData(callee.component.data);
        let initialItemValue = cmpData[itemValue],initialIndexValue = cmpData[indexValue];
        let initialItemProp = callee._properties[itemValue],initialIndexProp = callee._properties[indexValue];
        callee._properties[itemValue] = callee._properties[indexValue] = {};
        let items = node._attributes.items,content = node.content,dynamicNodes = node._dynamicNodes,lastNode = node;
        if(!node._items) {
            node._items = [];
        }
        node._dynProps = node._dynProps || {};
        let lastIndexForIteration;
        let firstIndexForIteration;
        let firstIndex = options.firstIndex;
        let secondIndex = options.secondIndex;
        let thirdIndex = options.thirdIndex;
        var totalNodeIndex = firstIndex + secondIndex;
        var updateIndexProperty = true;
        var indexPropertyStartIndex = 0;
        if(options) {
            switch(options.type) {
            case "remove"  :{
                lastIndexForIteration = firstIndex;
                indexPropertyStartIndex = firstIndex;
                let totalProms = node._totalPromise;
                for(let i=secondIndex; i>0; i--) {
                    let ind = firstIndex + i-1;
                    directiveObj && directiveObj.onGoingForPromise(node,ind)
                    this.removeHelpersSpecificIndex(node, ind, totalProms, undefined,totalProms && totalProms.length ? totalProms[totalProms.length-1] : undefined);
                }
                
                //ln
                /*for(let i=(firstIndex)?firstIndex-secondIndex:firstIndex;i<node._items.length;i++) {
                    let forItem = node._items[i].itemProperty;
                    if(forItem._helperNodes){
                        for (var item of forItem._helperNodes){
                            let ind = item._cx.itemIndex;
                            item._cx.itemIndex = (ind)? ind- secondIndex : ind;
                        }
                    }
                }*/
                for(let i=firstIndex + secondIndex; i<node._items.length;i++) {        
                    node._items[i]._cx.itemIndex = node._items[i]._cx.itemIndex - secondIndex;        
                }
                node._items.$splice(firstIndex, secondIndex);
                node._helpers.$splice(firstIndex, secondIndex);
                
                
                node._forContent.$splice(firstIndex, secondIndex);
                node._specialNodes && node._specialNodes.$splice(firstIndex, secondIndex);
                break;
            }
            case "insert" : {
                firstIndexForIteration = firstIndex;
                lastIndexForIteration = secondIndex;
                indexPropertyStartIndex = firstIndex + secondIndex;
                if(node._forContent[firstIndex]) {
                    lastNode = node._forContent[firstIndex][0];
                }
                let newArr = [], newObj = [], newArr1 = [], newPropObj = [];
                for(let v=secondIndex, k=firstIndex;v>0;v--, k++) {
                    newArr.$push([]);
                    newObj.$push({});
                    newArr1.$push([]);
                    newPropObj.push({__dummy : true});
                }
                
                node._helpers.$splice.apply(node._helpers, [firstIndex, 0].$concat(newArr));
                
                node._items.$splice.apply(node._items, [firstIndex, 0].$concat(newObj));
                
                //ln
//                  for(let i=firstIndex + secondIndex;i<node._items.length;i++) {
//                      let forItem = node._items[i].itemProperty;
//                      for (var item of forItem._helperNodes){
//                          item._cx.itemIndex = item._cx.itemIndex + secondIndex;
//                      }
//                  }
                for(let i=firstIndex + secondIndex; i<node._items.length;i++) {
                    node._items[i]._cx.itemIndex = node._items[i]._cx.itemIndex + secondIndex;
                }
                node._forContent.$splice.apply(node._forContent, [firstIndex, 0].$concat(newArr1));
                node._specialNodes && node._specialNodes.$splice.apply(node._specialNodes,  [firstIndex, 0].$concat(newArr1));
            }
                break;
            case "replace" : {
                firstIndexForIteration = firstIndex;
                lastIndexForIteration = secondIndex;
                indexPropertyStartIndex = firstIndex + secondIndex;
                let totalProms = node._totalPromise;
                directiveObj && directiveObj.onGoingForPromise(node,firstIndex)
                this.removeHelpersSpecificIndex(node, firstIndex, totalProms, undefined, totalProms && totalProms.length ? totalProms[totalProms.length-1] : undefined);
                let toAppendMain = createDocFragment();
                if(node._forContent[firstIndex+1]) {
                    lastNode = node._forContent[firstIndex+1][0];
                }
                let newArr = [], newObj = [], newArr1 = [], newPropObj = [];
                for(let v=secondIndex, k=firstIndex;v>0;v--, k++) {
                    newArr.$push([]);
                    newArr1.$push([]);
                    newObj.$push({});
                    newPropObj.push({__dummy : true});
                }
                
                node._helpers.$splice.apply(node._helpers,[firstIndex, 1].$concat(newArr));
                
                node._items.$splice.apply(node._items, [firstIndex, 1].$concat(newObj));
                
//                  for(let i=firstIndex + secondIndex;i<node._items.length;i++) {
//                      let forItem = node._items[i].itemProperty._forItem;
//                      forItem.itemIndex = forItem.itemIndex + secondIndex - 1 ;
//                  }
                for(let i=firstIndex + secondIndex; i<node._items.length;i++) {
                    node._items[i]._cx.itemIndex = node._items[i]._cx.itemIndex + secondIndex - 1;
                }
                node._forContent.$splice.apply(node._forContent, [firstIndex, 1].$concat(newArr1));
                node._specialNodes && node._specialNodes.$splice.apply(node._specialNodes,  [firstIndex, 1].$concat(newArr1));
                if(options.secondIndex == 1){
                    updateIndexProperty = false;
                }
                break;
            }
            case "splice" : {
                firstIndexForIteration = firstIndex;
                lastIndexForIteration = secondIndex;
                indexPropertyStartIndex = firstIndex + secondIndex;
                let newArr = [], newObj = [], newArr1 = [], newPropObj = [];
                for(let v=secondIndex, k=firstIndex;v>0;v--, k++) {
                    newArr.$push([]);
                    newArr1.$push([]);
                    newObj.$push({});
                    newPropObj.push({__dummy : true});
                }
                let totalProms = node._totalPromise;
                for(let i=thirdIndex;i>0;i--) {
                    let ind = i + firstIndex-1;
                    directiveObj && directiveObj.onGoingForPromise(node,ind)
                    this.removeHelpersSpecificIndex(node, ind, totalProms, undefined, totalProms && totalProms.length ? totalProms[totalProms.length-1] : undefined);
                }
                
                let toAppendMain = createDocFragment();
                if(node._forContent[firstIndex+thirdIndex]) {
                    lastNode = node._forContent[firstIndex+thirdIndex][0];
                }
                node._helpers.$splice.apply(node._helpers,[firstIndex, thirdIndex].$concat(newArr));
                
                node._items.$splice.apply(node._items, [firstIndex, thirdIndex].$concat(newObj));
                
//                  for(let i=firstIndex + secondIndex;i<node._items.length;i++) {
//                      let forItem = node._items[i].itemProperty._forItem;
//                      forItem.itemIndex = forItem.itemIndex + secondIndex - 1 ;
//                  }
                for(let i=firstIndex + secondIndex; i<node._items.length;i++) {
                    node._items[i]._cx.itemIndex = node._items[i]._cx.itemIndex + secondIndex - thirdIndex;
                }
                node._forContent.$splice.apply(node._forContent, [firstIndex, thirdIndex].$concat(newArr1));
                node._specialNodes && node._specialNodes.$splice.apply(node._specialNodes,  [firstIndex, thirdIndex].$concat(newArr1));
                if(options.secondIndex == options.thirdIndex){
                    updateIndexProperty = false;
                }
                break;
            }
            break;
            case "update" : 
            {
                let key = node.getAttribute("item");
//                  this.removeHelpers(node, true);
                let totalProms = node._totalPromise;
                for(let i=node._items.length-1;i>=0;i--) {
                    directiveObj && directiveObj.onGoingForPromise(node,i)
                    this.removeHelpersSpecificIndex(node, i, totalProms, undefined,totalProms && totalProms.length ? totalProms[totalProms.length-1] : undefined);
                    
                }
//                  if(node._attributes.items) {
//                      for(let i=0;i<node._attributes.items.length && node._items[i];i++) {
//                          _LC.removeSelectedBindingDeep(node._items[i].itemProperty[key], node._attributes.items[i]);
//                      }
//                  }
                node._items = [];
        }
            case "default" : 
            {
                node._forContent = [];
                if(directiveObj){
                    node._specialNodes = [];
                }
                node._propNodes = {};
                node._helpers = [];
                
                firstIndexForIteration = 0;
                lastIndexForIteration = items? items.length : 0 ;
                indexPropertyStartIndex = items? items.length : 0 ;
            }
            //@Slicer.developmentStart
            break;
            default: 
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(node,"LC009","updateForHelper")
            //@Slicer.developmentEnd
            }
        }
        if(!lastNode) {
            lastNode = node;
        }
        if(lastNode != node && node._helpers.length > 0  && (options.type == "insert" ||"replace"|| 0) ){
            lastNode = _LC.findLastNodeL(lastNode,totalNodeIndex,node);
        }
        let returnVal;
        var localUnbound = false;
        var initialUnbound = _LC.unbound;
        if((node.hasAttribute("unbound") || (node._attributes && node._attributes.unbound && node._attributes.unbound !== "false")) && !_LC.migratedv2(app)) {
            localUnbound = true;
            _LC.unbound = true;
        }
        if((!node._fRender && (node.getAttribute("unbound") == "lyteFastRender" || (node._attributes && node._attributes.unbound == "lyteFastRender"))) && !_LC.migratedv2(app)) {
            node._fRender = true;
        }
        let unboundDirective = _LC.directive.getTransitionArg(node,"unbound");
        if(unboundDirective){
            if(unboundDirective == "lyteFastRender"){
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(node, "LC010", _LC.errorNodeDetails(node))
                //@Slicer.developmentEnd
            }else{ // if(unboundDirective !== "false")
                localUnbound = true;
                _LC.unbound = true;
            }
        }
        let fastDirective = _LC.directive.getTransitionArg(node,"turbo");
        if(fastDirective){
            if(!node._fRender ){//&& fastDirective !== "false"
                node._fRender = true;
                localUnbound = true;
                _LC.unbound = true;
            }
        }
        node._currentItems = items;        
        if((lastIndexForIteration - firstIndexForIteration) > 0 || items instanceof Promise) {
            if(node._ht && !node._htA) {
                node.content.append(node._ht.content.cloneNode(true));
                node._htA = true;
            }
        }
        let currentCaseName;
        
        let trueNode = node;
        if(options.type !== "remove") {
            var totalString = "";
            var domArr = [];
            var toAppendMain = createDocFragment();
            
            
            for(let k = firstIndexForIteration,v=lastIndexForIteration;v>0; k++, v--) {
                node._helpers[k] = [];
                
                if(directiveObj){
                    node._specialNodes[k] = [];
                }
                
                node._items[k] = {"_dynamicProperty" : {}, "itemProperty" : {}, "indexProperty": {}, "propProperty" : {},"propPropertyDyn" : {}, "propNodes" : {}};
                cmpData[itemValue] = items[k];
                cmpData[indexValue] = k;
                var cacheData = {};
                cacheData[itemValue]={}
                cacheData[itemValue]._data = items[k];
                cacheData[itemValue]._dyn = [itemValue];
                cacheData[indexValue]={}
                cacheData[indexValue]._data = k;
                cacheData[indexValue]._dyn = [indexValue];
                callee._properties[itemValue] = {};
                callee._properties[indexValue] = {};
                let optns = {"itemValue" : itemValue, "itemIndex" : k, "type" : "for", "node" : node, "indexValue" : indexValue};
//                  defProp(callee._properties[itemValue], '_forItem', {
//                      enumerable: false, 
//                      writable: true, 
//                      configurable: true, 
//                      value : optns
//                  });
                node._items[k]._cx = optns;
                let breakCheck = {};
                let toAppend;
                if(node._fRender) {
                    let fastObj = this.getDirectiveIns("turbo");
                    let str = fastObj.renderFast(dynamicNodes, node._sta, this.component, undefined, this);
                    totalString = totalString + str;
                    // var template = document.createElement("template");
                    // template.innerHTML = str;
                    // toAppend = template.content;
                } else {
                    dynamicNodes._cache = cacheData;
                    toAppend = this.renderNodes(trueNode.hasAttribute("depth") ? trueNode._depthTemp : trueNode, dynamicNodes, node, optns, establishBindings, breakCheck, node._tC,yieldComp,_yields);
                }
                //to bind in ssr components
                
                let dynProps = Object.keys(node._items[k]._dynamicProperty);
                for(let d=0;d<dynProps.length;d++) {
                    let key = dynProps[d];
                    node._dynProps[key] ? node._dynProps[key]++ : (node._dynProps[key] = 1);    
                }
                node._items[k].itemProperty = this.getProperty(itemValue);
                node._items[k].indexProperty = this.getProperty(indexValue);    
//                  if(options.type !== "default") {
                if(!_LC.unbound) {
                	_LC.establishBindings({[itemValue] : node._items[k].itemProperty},{[itemValue]:node._attributes.items[k]});
                }
//                  }
                if(!node._fRender){
                    node._forContent[k] = Array.from(toAppend.childNodes);
                } else {
                    domArr.$push(node._forContent[k] = []);
                }
                //Needs to revisit this and make sure it happen within renderNodes function itself;
//                  if(options.type !== "update") {
                this.executeBlockHelpers(node._helpers[k], node);
                if(!node._fRender) {
                    toAppendMain.appendChild(toAppend);
                }
                if(breakCheck.stop) {
                    break;
                }
            }
            if(node._fRender) {
                var s = document.createElement("template");
                s.innerHTML = totalString;
                toAppendMain.appendChild(s.content);
                _LC.processAction(toAppendMain);
                var childrenArr;
                // if(Lyte._ie) {
                //     childrenArr = [];
                //     var childNodes = toAppendMain.childNodes;
                //     for(var i=0;i<childNodes.length;i++) {
                //         if(childNodes[i].nodeType == 1) {
                //             childrenArr.$push(childNodes[i]);
                //         }
                //     }
                // } 
                // else {
                    childrenArr = toAppendMain.children;
                // }
                for(var i=0;i<domArr.length;i++) {
                    domArr[i].$push(childrenArr[i]);
                }
                requestAnimationFrame(function() {
                    requestAnimationFrame(function() {
                        _LC.callCC();
                    });
                });
            }
            //Provided so that before appending the component to DOM it is reset to previous value
            _LC.unbound = initialUnbound;
            if(options.type === "default") {
                returnVal = {"toAppendMain" : toAppendMain, "lastNode" : lastNode};
            } else {
                if(directiveObj){
                    var dependentPromises = [];
                    directiveObj.getAllDependentPromises(this,dependentPromises,node._totalPromise);
                    directiveObj.insertInDom(this,lastNode._placeHolder,lastNode,toAppendMain,undefined,undefined,dependentPromises);
                }else{
                    _LC.insertInDom(lastNode._placeHolder,lastNode,toAppendMain);
                }
            }
            if(!localUnbound && node._removedAttributes && node._removedAttributes.items && !node._removedAttributes.items.helperValue && node._removedAttributes.items._multipleProperty && node._removedAttributes.items._multipleProperty[0]) {
            	_LC.establishBindings({"items" : node._removedAttributes.items._multipleProperty[0].actProp}, {"items" : items});
            }
        }
        _LC.unbound = initialUnbound;
        if(updateIndexProperty){
            for(let i=indexPropertyStartIndex; i<node._items.length; i++) {
                if(node._items[i].indexProperty) {
                    _LC.affectChanges(node._items[i].indexProperty);
                }
            }
        }
        cmpData[itemValue] = initialItemValue;
        cmpData[indexValue] = initialIndexValue;
        callee._properties[itemValue] = initialItemProp;
        callee._properties[indexValue] = initialIndexProp;
        
        node._currentItems = items;
        return returnVal;
    }

    //It constructs/updates forIn Helper.
    //updFIH
    updateForInHelper(node, options, contextSwitchInfo, establishBindings,yieldComp,initialRender,_yields) {
        
        let directiveObj = this.getDirectiveObj();
        directiveObj && directiveObj.instanciateForPromises(node);
        
        let app = this.component.getAppOrAddon();
        if(node.tagName !== "TEMPLATE") {
            Object.keys(node).forEach(function(item) {
                if(item !== "innerHTML" && item !== "innerText") {
	                node._origTemplate[item] = node[item];            	
            	}
            });
            // if(Lyte._ie) {
            // 	var div = createElement("div");
            //     div.innerHTML = node._tC;
            //     node._origTemplate.innerHTML = div.children[0].innerHTML;
            //     this.constructor.splitTextNodes(node._origTemplate);
            //     if(node.hasAttribute("depth")) {
            //         node._origTemplate.setAttribute("depth", node.getAttribute("depth"));
            //     }
            // }
            let placeHolder = node._origTemplate._placeHolder = document.createTextNode("");
            node._origTemplate.setAttribute("key", node.getAttribute("key"));
            node._origTemplate.setAttribute("value", node.getAttribute("value"));
            if(node.hasAttribute("unbound") && !_LC.migratedv2(app)) {
                //What if unbound is dynamic attribute ? 
                node._origTemplate.setAttribute("unbound", "true");
            }
            
            
            //node.replaceWith(node._origTemplate._placeHolder);
            _LC.replaceWithPf(node, node._origTemplate._placeHolder)
            // node = _LC.replaceWithOrigTemplate(node)
            node = node._origTemplate;
            node.setAttribute("is", "forIn");
            _LC.apdNode(node, this);
            placeHolder._helper = node;
        } else if(!node._placeHolder){
            var emptyTextNode;
            
                emptyTextNode = document.createTextNode("");
                
            //node.replaceWith(emptyTextNode);
            _LC.replaceWithPf(node, emptyTextNode);
            node._placeHolder = emptyTextNode;
            _LC.apdNode(node, this);
            emptyTextNode._helper = node;
        }
        let callee = this;
        node._callee = this;
        node._attributes = node._attributes || {};
        if(options.type === "update" && node._currentObject === node._attributes.object && (!options || !options.force)) {
            return {};
        }
        contextSwitchInfo = contextSwitchInfo ? contextSwitchInfo : node._cx;
        node._cx = contextSwitchInfo;
        let key = node.getAttribute("key");
        if(!key) {
            key = "key";
            node.setAttribute("key", "key");
        }
        let value = node.getAttribute("value");
        if(!value) {
            value = "value";
            node.setAttribute("value", "value");
        }
        
        let cmpData = _LC.getCmpData(callee.component.data);
        let initialKeyValue = cmpData[key];
        let initialValueValue = cmpData[value];
        let initialKeyProp = callee._properties[key];
        let initialValueProp = callee._properties[value];
        callee._properties[key] = callee._properties[value] = {};
        let object = node._attributes.object;
        let content = node.content;
        let dynamicNodes = node._dynamicNodes;
        let lastNode = node;
        let keysArray = [];
        if(!node._items) {
            node._items = {};
        }
        node._dynProps = node._dynProps || {};
        if(options) {
            switch(options.type) {
            case "delete"  :{
                let totalProms = node._totalPromise;
                directiveObj && directiveObj.onGoingForPromise(node,options.property)
                this.removeHelpersSpecificIndex(node, options.property, totalProms, undefined,totalProms && totalProms.length ? totalProms[totalProms.length-1] : undefined, undefined, options.type);
                
                var delIndex = node._keysArray.indexOf(options.property);
                if(delIndex > -1) {
                  node._keysArray.$splice(delIndex,1);
                }
                if(node._helpers) {
                    delete node._helpers[options.property];    
                }
                
                delete node._propBindingObject[options.property];
                node._forContent[options.property] = null;
                delete node._forContent[options.property];
            }
            break;
            case "add" : {
                let replace = false;
                keysArray = [options.property];
                replace = node._keysArray.indexOf(options.property) != -1 ? true : false;
                !replace && node._keysArray.push(options.property);
                
            }
            break;
            case "update" : 
            {
                let totalProms = node._totalPromise;
                let keyObjArr = node._keysArray;
                for(let i=keyObjArr.length-1; i>=0; i--){
                    directiveObj && directiveObj.onGoingForPromise(node,keyObjArr[i])
                    this.removeHelpersSpecificIndex(node, keyObjArr[i], totalProms, undefined,totalProms && totalProms.length ? totalProms[totalProms.length-1] : undefined, undefined, options.type);
                }
                
                node._keysArray = keysArray = object ? Object.keys(object) : [];
                node._helpers = {};
                
                node._items = {};
                node._propBindingObject = {};
            }
            // break;
            case "default" : 
            {
                node._forContent = {};
                if(directiveObj){
                    node._specialNodes = {};
                }
                node._propNodes = {};
                node._helpers = {};
                
                node._keysArray = keysArray = object? Object.keys(object) : [];
//                  keysArray = Object.keys(object);

            }
            //@Slicer.developmentStart
            break;
            default: 
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(node,"LC009","updateForInHelper")
            //@Slicer.developmentEnd

            }
        }
        let returnVal;
        if(!object) {
            let toAppendMain = createDocFragment();
            if(options.type !== "default") {
            	_LC.insertBeforeNative(lastNode.parentNode,toAppendMain, lastNode);
            } else {
                returnVal = {"toAppendMain" : toAppendMain, "lastNode" : lastNode};
            }    
        }
        let localUnbound = false;
        let initialUnbound = _LC.unbound;
        if((node.hasAttribute("unbound") || (node._attributes && node._attributes.unbound)) && !_LC.migratedv2(app)) {
            localUnbound = true;
            _LC.unbound = true;
        }
        let unboundDirective = _LC.directive.getTransitionArg(node,"unbound")
        if(unboundDirective){
            localUnbound = true;
            _LC.unbound = true;
        }
        node._currentObject = object;
        if((keysArray.length ||  object instanceof Promise) && node._ht && !node._htA) {
            node.content.append(node._ht.content.cloneNode(true));
            node._htA = true;
        }
        let currentCaseName;
        let trueNode = node;
        
        if(object && options.type !== "remove") {
            var toAppendMain = createDocFragment();
            node._propBindingObject = node._propBindingObject || {};
            
            
            keysArray.forEach(function(itemKey, index, array) {
                node._helpers[itemKey] = [];
                node._items[itemKey] = {"_dynamicProperty" : {}, "itemProperty" : {}, "propProperty" : {}, "propPropertyDyn" : {}, "propNodes" : {}};
                
                if(directiveObj){
                    node._specialNodes[itemKey] = [];
                }
                
                // node._items[itemKey] = {"_dynamicProperty" : {}, "itemProperty" : {}};
                cmpData[key] = itemKey;
                cmpData[value] = object[itemKey];
                callee._properties[key] = {};
                callee._properties[value] = {};
                var cacheData = {};
                cacheData[key] = {}
                cacheData[key]._data = itemKey;
                cacheData[key]._dyn = [key];
                cacheData[value] = {}
                cacheData[value]._data = object[itemKey];
                cacheData[value]._dyn = [value];
                let optns = {"itemIndex" : itemKey, "itemValue" : value, "keyValue" : key, "type" : "forIn", "node" : node};
                node._items[itemKey]._cx = optns;
                dynamicNodes._cache = cacheData;
                let toAppend = this.renderNodes(trueNode.hasAttribute("depth") ? trueNode._depthTemp : trueNode, dynamicNodes, node, optns, establishBindings, undefined, node._tC,yieldComp,_yields);
                Object.keys(node._items[itemKey]._dynamicProperty).forEach(function(key) {
                    node._dynProps[key] ? node._dynProps[key]++ : (node._dynProps[key] = 1);    
                })
                node._items[itemKey].itemProperty = this.getProperty(value);
                node._propBindingObject[itemKey] = node._items[itemKey].itemProperty;
                node._forContent[itemKey] = Array.from(toAppend.childNodes);
//                    this.updateBlockHelpers(node._helpers[itemKey], optns);
                this.executeBlockHelpers(node._helpers[itemKey], node);
                //to bind in ssr components
                
                toAppendMain.appendChild(toAppend);
            }, this); 
//              if(options.type !== "update") {
                if(!_LC.unbound && typeof node._attributes.object =='object' && !Array.isArray(node._attributes.object)) {
                    addBindings(node._attributes.object,node._propBindingObject);
                    _LC.establishBindings(node._propBindingObject, node._attributes.object);
            	}
//              }
				_LC.unbound = initialUnbound;
                if(options.type !== "default") {
                    if(directiveObj){
                        let dependentPromises = [];
                        directiveObj.getAllDependentPromises(this,dependentPromises,node._totalPromise);
                        directiveObj.insertInDom(this,lastNode._placeHolder,lastNode,toAppendMain,undefined,undefined,dependentPromises);
                    }else{
                        _LC.insertInDom(lastNode._placeHolder,lastNode,toAppendMain);
                    }
                } else {
                    returnVal = {"toAppendMain" : toAppendMain, "lastNode" : lastNode};
                }

        }
        _LC.unbound = initialUnbound; 
        cmpData[key] = initialKeyValue;
        cmpData[value] = initialValueValue;
        callee._properties[key] = initialKeyProp;
        callee._properties[value] = initialValueProp;
        
        node._currentObject = object;
        return returnVal;
    }
    rmCaseBindings(node,templInd,currentInd){
        let casesList = node._co;
        for(let i=templInd+1;i<=currentInd;i++){
            let temp = node._tempList[casesList[i]];
            if(temp){
                _LC.removeDynamicNodes(temp,this);
                node._bindedList[casesList[i]] = false;
            }
        }
    }
    getCaseTemplate(node,nodeTemp,caseName,caseStr){
        var dummyTemp;
        var obj = {};//check
        if(!node._tempList[caseName]){
            // if(Lyte._ms){
            //     dummyTemp = _LC.getContentForIE(node._cases[caseName].templateContent , this.constructor,undefined,obj);
            //     if(obj._content){
            //         dummyTemp = obj._content.childNodes[0];
            //     }
            // }else{
                dummyTemp = nodeTemp.content.querySelector(caseStr+_LC.cssEscape(caseName)+'\']');
            // }
            node._tempList[caseName] = dummyTemp;
        }else{
            dummyTemp = node._tempList[caseName];
        }
        return dummyTemp;
    }
    getCaseIndexScope(node,templateCaseNode,casesList,caseName,switchValue,lyteConvertedSwitch){
        var caseName = templateCaseNode.getAttribute("lc-id");
        var templInd = casesList.indexOf(caseName);
        let val = templateCaseNode._attributes.case;
        var currentInd = casesList.indexOf(node._currentScope);
        var scope;
        
        if(node._currentScope != "default"){
            if(templInd == currentInd){
                if(val){
                    if(lyteConvertedSwitch){
                        return false;
                    }else if(val==templateCaseNode._prevDataVal){ //second check for lyteconvertedtemp
                        return false;
                    }
                    else if(val == switchValue){
                        scope  = node._cases[caseName];
                    }
                }
            }
            else if(templInd > currentInd){
                return false;
            }
            else if(templInd < currentInd){
                if(val){
                    this.rmCaseBindings(node,templInd,currentInd);
                    scope  = node._cases[caseName];
                    scope.cn = caseName;
                }else{
                    return false;
                }
            }
        }else{
            this.rmCaseBindings(node,templInd,casesList.length-1);//templInd+1
        }
        return scope;
    }
    getSwitchScope(node,templateCaseNode,scope,nodeTemp,helperNode,contextSwitchInfo,switchValue,lyteConvertedSwitch,idx,id){
        var caseStr;
        var casesList = node._co;
        var currentInd = -1;//casesList.indexOf(node._currentScope);
        if(templateCaseNode){
            scope = this.getCaseIndexScope(node,templateCaseNode,casesList,caseName,switchValue,lyteConvertedSwitch);
            if(scope == false){
                return false;
            }
        }
        // else{
        //     currentInd = -1;
        // }
        if(!scope){
            var indexStart = node._currentScope ? currentInd + 1 : 0;
            for(var i=indexStart; i<casesList.length; i++){
                var caseName = casesList[i];
                var dummyTemp;
                if(node._cases[caseName].dcn){
                    caseStr = '[lc-id=\'';
                    dummyTemp = this.getCaseTemplate(node,nodeTemp,caseName,caseStr);
                    dummyTemp._parentSwitch = node;
                    dummyTemp._callee = this;
                    if(node._bindedList[caseName]){
                        if((lyteConvertedSwitch && dummyTemp._attributes.case) || (!lyteConvertedSwitch && dummyTemp._attributes.case == switchValue)){
                            scope  = node._cases[caseName];
                            scope.cn = caseName;
                            dummyTemp._prevDataVal = dummyTemp._attributes.case;
                            if(!templateCaseNode && !lyteConvertedSwitch){
                                this.rmCaseBindings(node,i,casesList.length);
                            }
                            break;
                        }
                    }else{
                        var toBeRemoved = [];
                        dummyTemp._dynamicProperty = dummyTemp._dynamicProperty || {};
                        if(lyteConvertedSwitch && helperNode && i==0){
                            this.bindNode(dummyTemp.attributes.getNamedItem("case"), toBeRemoved,  helperNode , helperNode.getAttribute("is")!="switch" && contextSwitchInfo ? contextSwitchInfo :{}, node._cases[caseName].cdp.a.case,undefined,undefined,undefined,undefined,lyteConvertedSwitch?"l":undefined,idx,undefined,id);
                        }else{
                            this.bindNode(dummyTemp.attributes.getNamedItem("case"), toBeRemoved, dummyTemp ,{}, node._cases[caseName].cdp.a.case,undefined,undefined,undefined,undefined,lyteConvertedSwitch?"l":undefined,idx,undefined,id);
                        }
                        // if(this._ssrBind && textNode){
                        //     textNode.setAttribute( "placeHolderId", dummyTemp.getAttribute( 'bindId' ) );
                        // }
                        
                        // debugger;
                        // if(helperNode){
                        //     helpId = helperNode.getAttribute("bindId");
                            
                        //     obj._helperId = helpId;
                        //     if(/^(for|forIn)$/.test(helperNode.getAttribute("is"))){
                        //         obj._forType = this.getssrType(helperNode.getAttribute("is"));
                        //         obj._forIndex = dynamicN._cx.itemIndex;
                        //     }
                        // }
                        // this.component.data.bindIds.push( obj );
                        dummyTemp._cx = contextSwitchInfo;
                        node._bindedList[caseName] = true;
                        dummyTemp._parentSwitch = node;
                        let val;
                        if(dummyTemp.getAttribute("case")=== "" || dummyTemp.getAttribute("case")) {
                            val = dummyTemp.getAttribute("case");
                        }if(dummyTemp._attributes) {
                            val = dummyTemp._attributes.case;
                        }
                        
                        if((lyteConvertedSwitch && val) || (!lyteConvertedSwitch && val == switchValue)){
                            scope  = node._cases[caseName];
                            scope.cn = caseName;
                            dummyTemp.cn = val;
                            break;
                        }
                    }
                }else if(!lyteConvertedSwitch){
                    //might br stattic caseinbetween
                    caseStr = "[case='";
                    dummyTemp = this.getCaseTemplate(node,nodeTemp,caseName,caseStr);
                        let val = dummyTemp.getAttribute("case");
                        if(val == switchValue){
                            scope  = node._cases[caseName];
                            scope.cn = caseName;
                            dummyTemp._prevDataVal = val;
                            break;
                        }
                }
            }      
        }  
        return scope;
    }
    //updSH
    updateSwitchHelper(type,node, contextSwitchInfo, update, establishBindings,yieldComp, _yields,templateCaseNode,helperNode,id,idx){
         var lyteConvertedSwitch = node.getAttribute("l-c");
         let directiveObj = this.getDirectiveObj();
         
         if(directiveObj){
             directiveObj.checkOngoingPromises(node);
             directiveObj.instanciateForPromises(node);//af
         }
         if(!node._specialNodes){
             node._specialNodes = [];
         }
         if(node._ht && !node._htA) {
             node.content.append(node._ht.content.cloneNode(true));
             node._htA = true;
         }
         if(node.tagName !== "TEMPLATE") {
             Object.keys(node).forEach(function(item) {
                 if(item !== "innerHTML" && item !== "innerText") {
                     node._origTemplate[item] = node[item];
                 }
             });
             // if(Lyte._ie) {
             // 	var div = createElement("div");
             //     div.innerHTML = node._tC;
             //     node._origTemplate.innerHTML = div.children[0].innerHTML;
             //     this.constructor.splitTextNodes(node._origTemplate);
             //     if(node.hasAttribute("depth")) {
             //         node._origTemplate.setAttribute("depth", node.getAttribute("depth"));    
             //     }
             // }
             let placeHolder = node._origTemplate._placeHolder = document.createTextNode("");
             //node.replaceWith(node._origTemplate._placeHolder);
             _LC.replaceWithPf(node, node._origTemplate._placeHolder);
             node = node._origTemplate;
             // node = _LC.replaceWithOrigTemplate(node)
             node.setAttribute("is", _LC.mappy[type]);
             _LC.apdNode(node, this);
             placeHolder._helper = node;
         } else if(!node._placeHolder){
             var emptyTextNode;
             
                 emptyTextNode = document.createTextNode("");
                 
             //node.replaceWith(emptyTextNode);
             _LC.replaceWithPf(node, emptyTextNode);
             node._placeHolder = emptyTextNode;
             emptyTextNode._helper = node;
             _LC.apdNode(node, this);
         }
         let isNew = false;
         let lastNode = node;
         if(!node._callee) {
             node._callee = this;
             isNew = true;
         }
         node._bindedList = node._bindedList || {};
         node._tempList = node._tempList || {};
         contextSwitchInfo = contextSwitchInfo ? contextSwitchInfo : node._cx;
         node._cx = contextSwitchInfo;
         node._dynamicProperty = node._dynamicProperty ? node._dynamicProperty : {};
         var switchValue;
         if(node.getAttribute("value")=== "" || node.getAttribute("value")) {
             switchValue = node.getAttribute("value");
         } else if(node._attributes) {
             switchValue = node._attributes.value;
         }
         if(!lyteConvertedSwitch && !node._hd){
             
                 if(switchValue) {
                     switchValue = switchValue.toString();
                 } else {
                     switch(switchValue) {
                         case undefined : 
                             switchValue = "undefined";
                             break;
                         case null : 
                             switchValue = "null";
                             break;
                         case false: 
                             switchValue = "false";
                             break;
                         case "": 
                             switchValue = '""';
                             break;
                         case 0 : 
                             switchValue = '0';
                     }
                 }
             
         }
         if((!lyteConvertedSwitch && !node._hd) && switchValue === node._currentCase) {
             return;
         }
         let scope,caseStr;
         node._currentCase = switchValue;
         var nodeTemp = node._depthTemp || node;
         if(node._hd){
             scope = this.getSwitchScope(node,templateCaseNode,scope,nodeTemp,helperNode,contextSwitchInfo,switchValue,lyteConvertedSwitch,idx,id);
             if(scope){
                 node._currentCase = scope.cn;
             }
         }
         else{
             caseStr = '[case=\''
             scope = node._cases[switchValue];
         }
         if(scope === false || (!scope && node._previousScope == "default")){
             return;
         }
         if(node._caseContent && node._caseContent.length) {
             this.removeHelpers(node, undefined, true,node._totalPromise);//, node._hd ?{start:templInd+1,end:node._currentScope != "default"?currentInd:casesList.length-1}:false
         }
         node._currentScope = scope && scope.cn;
         let defaultContent;
         if(!scope){
             scope = node._default;
             node._currentScope = "default";
             defaultContent = nodeTemp.content.querySelector('[default]');
             if(scope._ht && !defaultContent._htA) {
                 defaultContent._htA = true;
                 defaultContent.content.append(scope._ht.content.cloneNode(true));
             }
             node._isDefault = true; 
             if(!defaultContent) {
                 // if(node._caseContent && node._caseContent.length) {
                 //     this.removeHelpers(node, undefined, true ,node._totalPromise);
                 // }
                 // node._previousCase = node._currentCase;
                 node._caseContent = [];
                 let emptyTextNode = document.createTextNode("");
                 node._caseContent.$push(emptyTextNode);
                 node._currentCaseName = switchValue;
                 node._helpers = node._helpers || [];
                 if(update) {
                     //lastNode.parentNode.insertBefore(emptyTextNode, node);
                     if(lastNode._placeHolder) {
                         _LC.insertBeforeNative(lastNode._placeHolder.parentNode,emptyTextNode, lastNode._placeHolder);                    
                     } else {
                         _LC.insertBeforeNative(lastNode.parentNode,emptyTextNode, node);
                     }
                     return;
                 } else {
                     let toAppendMain = createDocFragment();
                     toAppendMain.append(emptyTextNode);
                     return {lastNode : lastNode, toAppendMain : toAppendMain};
                 }
             }
         }
         
 //        if(currentCase) {
 //              if(currentCase.tagName === "TEMPLATE" && !currentCase.getAttribute("is")){
 //                  currentCase = currentCase.content;
 //              } else {
 //                  let temp = createElement('template');
 //                  let clone = currentCase.cloneNode(true);
 //                  temp.content.appendChild(clone);
 //                  currentCase.removeAttribute('slot');
 //                  currentCase = temp.content;
 //              }
 //              scope.content = currentCase;
 //        }
  
         // if(node._caseContent && node._caseContent.length) {
         //     this.removeHelpers(node, undefined, true ,node._totalPromise);
         // }
         let dummyScope = scope;
         let additionalContentArr = [];
         let cnt=0;
         let dummyCaseName = switchValue;
         let template;
         if(defaultContent) {
             template = defaultContent
         } else {
             template = nodeTemp.content.querySelector((scope.dcn ? '[lc-id=\'' : '[case=\'') +_LC.cssEscape(scope.cn ? scope.cn : dummyCaseName)+'\']');
         }
         // if(!template._callee){
         //     template._callee = this;
         // }
         let contentArr = [];
         if(directiveObj){
             node._specialNodes = [];
         }
         let nextCaseName;
         while(dummyScope) {
             if(dummyScope._ht && !template._htA) {
                 template._htA = true;
                 template.content.append(dummyScope._ht.content.cloneNode(true));
             }
             let dynamicNodes = dummyScope.dN;
             if(nextCaseName && node._cases[nextCaseName].dcn){
                 caseStr = '[lc-id=\'';
                 var tempNode = this.getCaseTemplate(node,nodeTemp,nextCaseName,caseStr);
                 if(!node._bindedList[nextCaseName]){
                     var toBeRemoved = [];
                     tempNode._dynamicProperty = tempNode._dynamicProperty || {};
                     this.bindNode(tempNode.attributes.getNamedItem("case"), toBeRemoved, tempNode ,{}, node._cases[nextCaseName].cdp.a.case);
                     tempNode._cx = contextSwitchInfo;
                     node._bindedList[nextCaseName] = true;
                     tempNode._parentSwitch = node;
                 }
             }
             let processedContent = this.renderNodes(template, dynamicNodes, node, undefined, establishBindings, undefined, dummyScope.templateContent,yieldComp,_yields);
             
             contentArr.$push(processedContent);
             if(dummyScope.additional) {
                 if(node._cases[dummyScope.additional.next] && node._cases[dummyScope.additional.next].dcn){
                     caseStr = '[lc-id=\'';
                 }else{
                     caseStr = '[case=\''
                 }
                 if(dummyScope.additional.next) {
                     nextCaseName = dummyScope.additional.next;
                     template = node.content.querySelector(caseStr+_LC.cssEscape(dummyScope.additional.next)+'\']');
                     dummyScope = node._cases[dummyScope.additional.next];
                 } else {
                     template = node.content.querySelector('[default]');
                     dummyScope = node._default;
                 }
             } else {
                     break;
             }
         }
         node._caseContent = [];
         let toAppendMain = createDocFragment();
         for(let i=0;i<contentArr.length;i++) {
             if(contentArr[i].nodeType == 11){
 //                for(let j=0;j<contentArr[i].childNodes.length;j++) {
 //                    node._caseContent.$push(contentArr[i].childNodes[j]);
 //                }
                 node._caseContent = node._caseContent.$concat(Array.from(contentArr[i].childNodes));
             }
             else{
                 node._caseContent.$push(contentArr[i]);
             }
                 toAppendMain.append(contentArr[i]);
 //            node.parentNode.insertBefore(contentArr[i], node);
         }
           this.executeBlockHelpers(node._helpers, node);
           if(update) {
               let returnVal;
               if(toAppendMain.querySelector("template[is=break]")) {
                   returnVal = "break";
               } else if(toAppendMain.querySelector("template[is=continue]")) {
                   returnVal = "continue";
               }
               if(directiveObj){
                 let dependentPromises = [];
                 directiveObj.getAllDependentPromises(this,dependentPromises,node._totalPromise);
                 directiveObj.insertInDom(this,node._placeHolder,node,toAppendMain,node._previousCase ?false:true,undefined,dependentPromises);
               }else{
                 _LC.insertInDom(node._placeHolder,node,toAppendMain);
               }

               node._previousCase = node._currentCase;
               node._previousScope = node._currentScope;
               return returnVal;
           } else {
                 node._previousCase = node._currentCase;
                 node._previousScope = node._currentScope;
               return {"toAppendMain" : toAppendMain, "lastNode" : node};
           }
     }

    dummy(a,b) {
        let dummy = this.component.constructor._properties[boundValue].observer;
    }

    callObservers(boundValue, key) {
        let property = this.component.constructor._properties[boundValue];
        let observers = property?property.observer: undefined;
        if(observers) {
            for(let i=0;i<observers.length;i++) {
                if(key.indexOf('.') === -1 || observers[i].path === key) {
                    this["_"+observers[i].functionName].apply(this);
                }
            }
        }
    }

    // static updateValue(property, path, value) {
    //     let pathVals = path.split('.');
    //     let context = property;
    //     for(let i=0;i<pathVals.length -1;i++) {
    //         context = context[pathVals[i]];
    //     }
    //     context[pathVals[i]] = value;
    // }

    //    static createDocFragment(template){
    //        let childNodes = template.cloneNode(true,"lyte").childNodes;
    ////           let childNodes = template.childNodes;
    //        let frag = document.createDocumentFragment();
    //        let len = childNodes.length;
    //        for(let i=0; i<len; i++){
    //            frag.appendChild(childNodes[0]);
    //        }
    //        return frag;
    //    }

    static createDocFragment1(template) {
        if(template.content) {
            return template.content;
        }
    	let childNodes = template.childNodes;
    	let frag = createDocFragment();
    	let len = childNodes.length;
    	for(let i=0; i<len; i++){
            frag.appendChild(childNodes[0]);
        }
    	return frag;
    }
    static seperateStyle(componentClass,componentsDiv,nearByApp){
        componentClass._template.replace(/\\'/g,"'");
        let div = createElement("div");
        div.innerHTML = componentClass._template;
        while(div.firstChild){
            if(div.firstChild.nodeName === "STYLE" || div.firstChild.nodeName === "LINK") {
                // if(div.firstChild.nodeName === "LINK" && nearByApp && nearByApp.processLinkTag){
                //     nearByApp.processLinkTag(div.firstChild);
                // }
                if(div.firstChild.nodeName == "LINK" && nearByApp.constructor._preloadLInkTag){
                    let preloadLinkTag = div.firstChild.cloneNode(true);
                    preloadLinkTag.setAttribute("rel","preload");
                    preloadLinkTag.setAttribute("as","style");
                    _LC.addLink(undefined, _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv, preloadLinkTag, undefined, nearByApp);
                }
                componentClass._style = div.firstChild.outerHTML;
                div.firstChild.remove();
            } else {
                // div.firstChild.setAttribute("lyte-registry",componentClass._registryClass.name);
                componentsDiv.appendChild(div.firstChild);
            }
        }
    }
    static* _registerComponentFn(a,b,componentClass,registry,registryInstance) {
        let componentsDiv = _LC.getComponentsDiv(_LC.lyteComponentsDiv ,registry.name);
        let origTemplateValue = componentClass._template;
        let app = _LC.getAppOrAddon(registryInstance);
        let nearByApp = _LC.getNearestParentApp(registryInstance);
        var clonedDummyTemp, s, hasUnbound, fastRenderClass, newCompile;
        var returnRegister = false;
        yield () => {
            if(registry._reRegisteredComponents.indexOf(a) != -1){
                if(b.componentClass._template.startsWith('"')){
                    b.componentClass._template = JSON.parse(b.componentClass._template);
                    // if(registry.lazyRegister){
                        registry._reRegisteredComponents.splice(registry._reRegisteredComponents.indexOf(a), 1);
                    // }
                }
            }
        }
        yield () => {
            if(componentClass._template && typeof componentClass._template === "string"){
                this.seperateStyle(componentClass,componentsDiv,nearByApp);
            }else if(componentClass.template && typeof componentClass.template === "object"){
                componentClass._template = componentClass.template._template;
                componentClass._dynamicNodes = componentClass.template._dynamicNodes;
                componentClass.template = undefined;
                this.seperateStyle(componentClass,componentsDiv,nearByApp);
            }
            componentClass._template = componentsDiv.querySelector("template[tag-name='"+a+"']");
            componentClass._helperTemplate = document.createElement("template");
            componentClass._helperTemplate.setAttribute("_lyteht", a );
            _LC.h1Div.content.appendChild(componentClass._helperTemplate);
            if(!componentClass._template) {
                //@Slicer.developmentStart
                console.error("Template not found for the component : '"+a+"'");
                //@Slicer.developmentEnd
                // return;
                returnRegister = true;
                return;
            }
            if((_compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().needDummyComponentsDiv)) {
                // if(Lyte._ie) {
                //     let temp = Compile.getTemplateFromString(origTemplateValue);
                //     // let dummyLyteComponentsDiv = _LC.setComponentsDiv(_LC.dummyLyteComponentsDiv,registry.name);
                //     _LC.getComponentsDiv(_LC.dummyLyteComponentsDiv,registry.name)
                //     dummyLyteComponentsDiv.appendChild(temp);
                // } else {
                    clonedDummyTemp = componentClass._template.cloneNode(true);   
                // }
            }
            if(componentClass._template && !componentClass._template.content){
    //            var frag = document.createDocumentFragment();
    //            let childNodes = this._template.cloneNode(true,"lyte").childNodes;
    //            //let childNodes = this._template.childNodes;
    //            let len = childNodes.length;
    //            for(let i=0; i<len; i++){
    //                frag.appendChild(childNodes[0]);
    //            }
    //            this._template.content = frag;
            }
            // var s = Lyte._ie ? componentClass._template : componentClass._template.content;//)?this._template.content:document.createDocumentFragment(this._template);
            s = componentClass._template.content; //)?this._template.content:document.createDocumentFragment(this._template);
            //This is used to split text nodes which contain multiple dynamic values 
            //Eg." Name is {{name}} and age is {{age}} "
            //This is used to find the dynamicNodes and helper nodes for the given component. 
        }
        if(returnRegister){
            return;
        }
        if(!componentClass._dynamicNodes){
            yield () => {
                if((_compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getDynamicNodes)) {
                    var returnVal = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getDynamicNodes(a);
                    if(returnVal.errors){
                        //@Slicer.developmentStart
                        console.error("Error in the component", returnVal.componentName, returnVal.errors);
                        //@Slicer.developmentEnd
                        return false;
                    }else{
                        componentClass._dynamicNodes = returnVal.dynamicNodes;
                        if(componentClass._dynamicNodes && componentClass._dynamicNodes.length && componentClass._dynamicNodes[componentClass._dynamicNodes.length-1].type == "dc"){
                            b.dc = componentClass._dynamicNodes.$pop();
                        }
                    }
                } 
                //@Slicer.developmentStart
                else {
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC002", a);  
                }
                //@Slicer.developmentEnd
            }
	    } 
        if(componentClass._dynamicNodes) {
            yield () => {
                this.setTemplateAttributes(a, componentClass, clonedDummyTemp);
                hasUnbound = this.getFastRenderSupported(componentClass._templateAttributes);
                fastRenderClass = registry.getDirectiveIns("turbo");
                // var hasUnbound = b._observedAttributes.indexOf("lyteUnbound") !== -1;
                if(!hasUnbound && !_LC.migratedv2(app)){
                    hasUnbound = b._observedAttributes.indexOf("lyteUnbound") !== -1;
                }
                if(hasUnbound) {
                    if(fastRenderClass){
                        newCompile = fastRenderClass.getNewCompile(componentClass);
                    }
                    //@Slicer.developmentStart
                    // else{
                    //     ComponentError.error("LC011")
                    // }
                    //@Slicer.developmentEnd
                    // if(Lyte._ie) {
                    //     newCompile = document.createElement("div");
                    //     newCompile.innerHTML = componentClass._template.outerHTML;
                    //     newCompile = newCompile.childNodes[0];
                    // } else {
                    //     newCompile = componentClass._template.cloneNode(true);
                    // }
                }
            }
            yield () => {
                this.splitTextNodes(s);
                if(hasUnbound && fastRenderClass) {
                    this.splitTextNodes(newCompile);
                }
            }   
            yield* doCompile(s, componentClass._dynamicNodes, a, b, newCompile ? newCompile.content : undefined, componentClass, fastRenderClass);
            // if(Lyte._ed) { 
            //     componentClass._tC = componentClass._template.outerHTML;
            // } 
            // else if(Lyte._ie) {
            //     componentClass._tC = s.outerHTML;
            // }
        }
        yield () => {
            if(clonedDummyTemp){
                let dummyRegDiv = _LC.getComponentsDiv(_LC.dummyLyteComponentsDiv,registry.name);
                if(!dummyRegDiv){
                    _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default()._registryNameList.forEach(function(regName){
                        let div = document.createElement("div");
                        div.setAttribute("id",regName);
                        _LC.dummyLyteComponentsDiv.appendChild(div);
                        if(regName == registry.name){
                            dummyRegDiv = div;            
                        }
                    })
                    ;(_compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default()._registryNameList) = [];
                }
                dummyRegDiv.appendChild(clonedDummyTemp);
            }
            componentClass._sta = newCompile ? _LC.processStatic(newCompile) : undefined;
        } 

        yield () => {
            this.componentClass.__isRegistered = true;
            let depthTemp = componentClass._depthTemp;
            if(depthTemp && depthTemp.content.childNodes.length) { //removed _ie
                depthTemp.setAttribute("data-id", "depthTemp_" + a);
                let lyteComponentsDiv = _LC.getComponentsDiv(_LC.lyteComponentsDiv,registry.name);
                lyteComponentsDiv.appendChild(depthTemp);
            } else {
                delete componentClass._depthTemp;
            }
            // _LC.postRegistration(a, b);
        }
    }
    static _registerComponent(a,b, componentClass,registry,registryInstance) {
        var gen = this._registerComponentFn(a,b, componentClass,registry,registryInstance);
        var gnxt = gen.next(), gval;
        while(gnxt.done == false){
            gval = gnxt.value;
            if(typeof gval == "function"){
                gval();
            }
            gnxt = gen.next()
        }
    }
    static getFastRenderSupported(templateAttributes){
        if(templateAttributes && templateAttributes.a){
            for(let attrName in templateAttributes.a){
                if(attrName == "@unbound-supported" || attrName == "@turbo-supported" ){
                    return true;
                }
            }
        }

    }
    static setTemplateAttributes(a, componentClass,clonedDummyTemp){
        componentClass._templateAttributes = {t : "a", "a" : {}, p: []};
        var ta = [componentClass._templateAttributes];
        let gen = doCompile(componentClass._template, ta, a, this, undefined, componentClass);
        let gnxt = gen.next(), gval;
        while(gnxt.done == false){
            gval = gnxt.value;
            if(typeof gval == "function"){
                gval();
            }
            gnxt = gen.next()
        }
        componentClass._templateAttributes = ta[0];
        if(componentClass._templateAttributes && componentClass._templateAttributes.a) {
            var attributesT = componentClass._template.attributes;
            try{
                for(let i=0;i<attributesT.length;i++) {
                    let attrName = attributesT[i].name;
                    if(!componentClass._templateAttributes.a[attrName] && attrName !== "tag-name" && attrName !== "use-strict" && attrName !== "lyte-registry" && attrName !== "__vp") {
                        componentClass._templateAttributes.a[attrName] = {"name" : attrName, "staticValue" :  attributesT[i].value};
                        componentClass._template.removeAttribute(attrName);
                        clonedDummyTemp && clonedDummyTemp.removeAttribute(attrName);
                        i--;
                    }
                }    
            } catch(e) {
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC012")
                //@Slicer.developmentEnd
            }
        }
    }
    //This is used to split text nodes which contain multiple dynamicNodes. 
    static splitTextNodes(node) {
        if(node && node.childNodes && node.childNodes.length) {
            for(let i=node.childNodes.length-1;i>=0;i--) {
                this.splitTextNodes(node.childNodes[i]);
            }
        }
        if(node.tagName === "TEMPLATE") { // && !Lyte._ie
            this.splitTextNodes(node.content);
        }
        if(node.nodeType === node.TEXT_NODE) {
            let nodeValue = node.nodeValue;
            if(nodeValue){
                let mustacheValues = nodeValue.match(/{{[^}]*?(?:(?:('|")[^\1]*?\1)[^}]*?)*}}/g); //'
                if(!mustacheValues) {
                    return;
                }
                let newNodeArray = [];
                for(let i=0;i<mustacheValues.length;i++) {
                    let mustacheStartIndex = nodeValue.indexOf(mustacheValues[i]);
                    let mustacheEndIndex = mustacheStartIndex + mustacheValues[i].length;
                    if(mustacheStartIndex) {
                            newNodeArray.$push(document.createTextNode(nodeValue.substring(0, mustacheStartIndex)));
                    }
                    newNodeArray.$push(document.createTextNode(nodeValue.substring(mustacheStartIndex, mustacheEndIndex)));
                    nodeValue = nodeValue.substring(mustacheEndIndex);
                }
                if(!(!nodeValue.trim() && node.nextSibling && node.nextSibling.nodeType === 3 && !node.nextSibling.nodeValue.trim())) {
                    newNodeArray.$push(document.createTextNode(nodeValue));
                }
                //Fix for IE Edge issue with higher versions where node.replaceWith is not working when the parent is a doc fragment.
                if(node.parentNode.nodeName === "#document-fragment") {
                	for(let i=0;i<newNodeArray.length;i++) {
                		_LC.insertBeforeNative(node.parentNode,newNodeArray[i],node);
                      }
                    node.remove();
                } else {
                	node.replaceWith.apply(node, newNodeArray);
                } 
            }
        }
    }
    //It registers the binding of the node with the properties with which the dynamicNode depends. 
    bindNode(node, toBeRemoved, helperNode, options, nodeInfo, processLast, establishBindings, isTemplate,cache,type,idx,toBeInsMap,bId) {
        let lyteConvertedSwitch = false;
        if(type == "l"){
            lyteConvertedSwitch = true;
            type = "a";
        }
        let itemValue = options.itemValue;
        let forIndex = options.itemIndex;
        let forType = options.type;
        let indexValue = options.indexValue;
        let dynamicValue = nodeInfo.dynamicValue;
        let directiveValue = nodeInfo.directiveValue;
        let helperFunc = nodeInfo.helperInfo;
        let nodeValue, ownerElement = node.ownerElement;
        let dynamicValuesArray = [];
        let cmpData = _LC.getCmpData(this.component.data);
        let processDetails = {};
        let isDirectiveNode;
        let directiveObj = this.getDirectiveObj();
        
//        if(node.nodeType === 2 && _LC.isCustomElement(node.ownerElement,true) ) {
//          node = {nodeName : node.nodeName, ownerElement: ownerElement, nodeType : 2, nodeValue : node.nodeValue};
//        }
        if(node.nodeType === 2) {
            node = {nodeName : node.nodeName, ownerElement : node.ownerElement, nodeType : 2, nodeValue : node.nodeValue, _attributeDetails : node._attributeDetails, _isDirectiveNode : nodeInfo._isDirectiveNode};
            // if(directiveObj){
            isDirectiveNode = _LC.directive.isDirectiveNode(node)
            // }
        }
        //to get bindings in ssr components
        
        node._callee = this;
        let isHelper = false;
        let deepValues = [];
        if(helperFunc && Object.keys(helperFunc).length) {
            isHelper = true;
            if(helperFunc._t){
                node._sq = true;
            }
            let attrName = node.nodeName;
            nodeValue = node.helperValue = helperFunc;
            let helperArgs = [];
            if(helperFunc.name === "action"){
                let actName = helperFunc.args[0];
                helperFunc.args[0] = actName.startsWith("'")? actName.replace(/'/g,''):  actName;
                // if(forType) {
                //     ownerElement._cx = options;
                // } else if(helperNode) {
                //     ownerElement._cx = helperNode._cx;
                // }
                actName = helperFunc.args.slice(0,1)[0];
                let args = helperFunc.args.slice(1,helperFunc.args.length), isCustom = false;
                let attrName = node.nodeName;
                if(attrName.indexOf("-") != -1){
                    isCustom = true;
                }
                helperArgs = [ownerElement,attrName,isCustom,{name:actName,args:args}];
                this.processHelper(this, {"name" : helperFunc.name, "args" : helperArgs}, node);
                return;
            } else{
                if(helperFunc.name === "method") {
                    helperArgs = helperFunc.args;
                         
                } else {  
                    helperArgs = this.processArgs(this,{"helperInfo" : helperFunc} ,dynamicValuesArray, deepValues,undefined,node,false,helperFunc._t ?undefined:cache, undefined, processDetails);
                }
            }
            if(helperFunc._t == "sq"){
                nodeValue = this.processArray(this,{"name" : helperFunc.name, "args" : helperArgs},dynamicValuesArray,deepValues,helperFunc.extra,event, node, undefined, processDetails);
            }
            else{
                nodeValue = this.processHelper(this, {"name" : helperFunc.name, "args" : helperArgs}, node, processDetails);
            }
            if(helperFunc.name === "unescape"){
//              let test = node.replaceWith.apply(node,nodeValue.childNodes);
                let obj = {
                    initialNode : node,
                    dynamicNodeValue : nodeValue
                };
                node = {dynamicPositions : obj, "_callee" : node._callee, helperValue : node.helperValue};
                nodeValue = undefined;
                processLast.$push(node);
            }
        } else {
            helperFunc = {};
            
            node.syntaxValue = dynamicValue;
            let dynamicValues = [];
            nodeValue = _LC.get(cmpData, dynamicValue, dynamicValues,cache);
            dynamicValuesArray.$push(dynamicValues);
        }
        //if(node.nodeType === 2 && ( (typeof nodeValue !== "string" && (_LC.isCustomElement(node.ownerElement,true) || typeof nodeValue === "boolean") ) || _LC.isControlHelper(node.ownerElement) )) {
        //	let bindedNode = node;
            //node = {nodeName : node.nodeName, ownerElement: ownerElement, nodeType : 2, nodeValue : node.nodeValue, _callee : this, syntaxValue : node.syntaxValue, helperValue : node.helperValue, _attributeDetails : node._attributeDetails};
        if(node.nodeType == 2) {
            let tagName = node.ownerElement.tagName;
            if(tagName == "INPUT" || tagName == "TEXTAREA" || (tagName == "DIV" && node.ownerElement.hasAttribute("contenteditable") )) {
                var rA = node.ownerElement._rA = node.ownerElement._rA || [];
                node.ownerElement._rA.$push(node);
            }
            if(!ownerElement._origTemplate) {
                if(( ownerElement.hasAttribute("lyte-for") || ownerElement.hasAttribute("lyte-if") || ownerElement.hasAttribute("lyte-switch") || ownerElement.hasAttribute("lyte-forin") ) && ownerElement.tagName !== "TEMPLATE") {
                    // if(Lyte._ie) {
                    //     node.ownerElement = createElement("template");
                    //     node.ownerElement.setAttribute("is", nodeInfo._depthTemp);
                    // } else {
                        node.ownerElement = nodeInfo._depthTemp.cloneNode(true);
                    // }
                    ownerElement._origTemplate = node.ownerElement;
                }
            } else {
                node.ownerElement = ownerElement._origTemplate;
            }
        //	node.ownerElement._attributeDetails[node.nodeName].bindedNode = node;
        //}
        }
        // let estuh = false;
        let estuhObj = {
            doHp : undefined,
            estuh : false
        };
        let actMultiProp; 
        if(!_LC.unbound) {
            let dynamicProp;
            if(helperNode) {
                dynamicProp = forType? helperNode._items[forIndex]._dynamicProperty : helperNode._dynamicProperty;
            }
            let obj, helperId;
            for(let d=0;d<dynamicValuesArray.length;d++) {
            	let dynamicValues = dynamicValuesArray[d];
                
                
                let firstBoundValue, nodeFreezed;
            	for(let v=0;v<dynamicValues.length;v++) {
                    //to get binding in ssr components
                    if(nodeFreezed){
                        continue;
                    }
                    var _nes = false;
                    
                    if(node.__lq){
                        if(!processDetails.lqDyn[d][0]){
                            continue;
                        }
                    }
                    let ind = dynamicValues[v].search(/\W/);
                    if(dynamicValues[v].startsWith('$dataAttributes') || dynamicValues[v].startsWith('$methodAttributes')){
                        nodeFreezed=true;
                        continue;
                    }
                    let boundValue;
                    if(ind !== -1) {
                        boundValue = dynamicValues[v].substring(0, ind);
                    } else {
                        boundValue = dynamicValues[v];
                    }
                    if(v == 0){
                        firstBoundValue = boundValue
                    }
                    if(_LC.freeze && _LC.freeze.prop(this, boundValue)){
                        nodeFreezed = true;
                        continue;
                    }
            		let actProperty = this.getProperty(dynamicValues[v]);
                    
            		if(helperNode) {
                        estuhObj.doHp = true;
                        
                        helperNode._deepValues = deepValues;
                        if( estuhObj.doHp && boundValue !== itemValue && boundValue !== indexValue && (!options.node || !options.node._properties || !options.node._properties[boundValue])) {
            				//to bind for in ssr
                            
                            makeSet(actProperty, "_helperNodes");
            				actProperty._helperNodes.add(
            						helperNode
            				);
            				dynamicProp[dynamicValues[v]] ? dynamicProp[dynamicValues[v]].$push(node): (dynamicProp[dynamicValues[v]] = []).$push(node);
            			} 
            			else {
                            node._deepValues = deepValues;
            				node._cx = options;
//            				if(!actProperty._dynamicNodes) {
//            					actProperty._dynamicNodes = [];
//            					defProp(actProperty, '_dynamicNodes', {
//            						value: [],
//            						enumerable: false, 
//            						writable: true, 
//            						configurable: true
//            					});
//            				}
            				makeArray(actProperty, "_dynamicNodes");
                            if(actProperty._dynamicNodes.indexOf(node) == -1){
            				    actProperty._dynamicNodes.$push(node);
                            }
            				if(boundValue !== indexValue) {
            					actMultiProp = actProperty;
            				}
            			}
            		} else {
//            			if(!actProperty._dynamicNodes) {
//            				defProp(actProperty, '_dynamicNodes', {
//            					value : [], 
//            					enumerable: false, 
//            					writable: true,
//            					configurable: true
//            				});
//            			}
                        node._deepValues = deepValues;
            			makeArray(actProperty, "_dynamicNodes");
                        if(actProperty._dynamicNodes.indexOf(node) == -1){
            			    actProperty._dynamicNodes.$push(node);
                        }
            		}
            		if ((ownerElement && (ownerElement.hasAttribute("lyte-for") || ownerElement.hasAttribute("lyte-if") || ownerElement.hasAttribute("lyte-switch") || ownerElement.hasAttribute("lyte-forin") )) || (ownerElement && ownerElement.tagName === "TEMPLATE" && /^(for|forIn)$/.test(ownerElement.getAttribute("is")) && !isHelper)) {
            			let type= ownerElement.getAttribute("is");
            			if( (type=== "for" && node.nodeName === "items") || (type==="forIn" && node.nodeName === "object")) {
            				if(!actProperty._forHelpers) {
            					makeSet(actProperty, "_forHelpers");
            				}
                            //to bind for in ssr
                            
            				node.ownerElement._actualBinding = actProperty;
            				actProperty._forHelpers.add(node.ownerElement);
            			}
            		}
            		if(establishBindings) {
            			_LC.establishSelectedBinding(actProperty, cmpData, this );
            		}
                    
                    //to get ssr bindings 
                     
            	}
                
                if((dynamicValues.length > 1 || helperFunc._t) && !nodeFreezed &&!(_LC.freeze && this.component.__data[firstBoundValue] && this.component.__data[firstBoundValue].freeze )) {
            		node._multipleProperty = node._multipleProperty || [];
            		node._multipleProperty.$push({"dynamicProp" : actMultiProp ? undefined : dynamicProp, "actProp" : this.getProperty(dynamicValues[0]), "helperNode" : helperNode, "dynamicValues" : dynamicValues, index:d});
            	}
                
                
            }
        }
        nodeValue = !typeof nodeValue === "boolean" && !typeof nodeValue === "number" ? (nodeValue? nodeValue : ""): nodeValue;
        if(isDirectiveNode){
            if(!directiveValue){ //af check
                _LC.directive.setNodeArgs(node, nodeValue, this);
            }
        }
        else if(node.nodeType === 2) {
            let parentNode = node._parentNode? node._parentNode : node.ownerElement;
            if(parentNode.tagName) {
                let is = parentNode.getAttribute("is");
            }
            let isCustomElement = _LC.isCustomElement(parentNode,true);
            
            if(isCustomElement && !isDirectiveNode) {
                 if(parentNode.set) {
                    parentNode.set(_LC.String.toCamelCase(node.nodeName), nodeValue);
                 } else {
                    parentNode._initProperties = parentNode._initProperties || {};
                     parentNode._initProperties[_LC.String.toCamelCase(node.nodeName)] =nodeValue;
                }
            }
            let origNodeValue = nodeValue;
            // if(directiveNode){
            //     node.ownerElement._transitionArgs = origNodeValue;
            //     node.ownerElement.removeAttribute(node.nodeName);
            //     node.nodeName = node.nodeName.slice(1,node.nodeName.length);
            // }
            //!== "string"
            if(isCustomElement && typeof nodeValue !== "string" && !isTemplate) {
                parentNode._attributes = parentNode._attributes || {};
                parentNode._attributes[node.nodeName] = nodeValue;
                if(parentNode.nodeName === "TEMPLATE" && isHelper) {
                    if((parentNode.getAttribute("is") === "for" && node.nodeName === "items") || (parentNode.getAttribute("is") === "forIn" && node.nodeName === "object") && !_LC.unbound) {
                        //node._actualBinding = {"_forHelpers" : new Set().add(parentNode)};
                        node.ownerElement._actualBinding = {
                            "_forHelpers" : new Set().add(parentNode),
                            "_createdBinding" : true
                        };
                        if(nodeValue && typeof nodeValue !== "number"){
                            addBindings(nodeValue,node.ownerElement._actualBinding);
                        }
                    }
                }
                toBeRemoved.$push(node.nodeName);
            }
            else {
                
                if(typeof nodeValue === "boolean") {
                    parentNode._attributes = node.ownerElement._attributes || {};
                    parentNode._attributes[node.nodeName] = nodeValue;
                    if(!nodeValue) {
//                      node.ownerElement.removeAttribute(node.nodeName);
                        toBeRemoved.$push(node.nodeName);
                    } else {
                        parentNode.setAttribute(node.nodeName, "");
                    }
                } else {
                    if(nodeValue && typeof nodeValue === "object"){
                        var res;
                        if(typeof Record != "undefined" && nodeValue instanceof Record){
                            res = JSON.stringify(nodeValue.$.toJSON())
                        }
                        else{
                            try{
                                res = JSON.stringify(nodeValue)
                            }
                            catch(exp){
                                //@Slicer.developmentStart
                                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(
                                    node,
                                    "LC013",
                                    node.nodeName,
                                    node.ownerElement.nodeName.toLocaleLowerCase(),
                                    node.ownerElement.nodeName.toLocaleLowerCase()
                                )
                                //@Slicer.developmentEnd
                            }
                        }
                    }
                    else if(nodeValue== null || nodeValue == undefined){
                        res = ""
                    }
                    else{
                        res = nodeValue;
                    }
                    nodeValue = res;
                    if(_LC.isControlHelper(node.ownerElement)) {
                    	 parentNode._attributes = node.ownerElement._attributes || {};
                         parentNode._attributes[node.nodeName] = nodeValue;
                         toBeRemoved.$push(node.nodeName);
                    } else {
                         let locNodeVal = nodeValue === undefined ? "" : nodeValue;
                         //if(node.nodeName === "style") {
                         //   node.ownerElement.setAttribute("style",locNodeVal);
                         //} else {
                         //   node.nodeValue = locNodeVal;
                         //}
                         node.ownerElement.setAttribute(node.nodeName, locNodeVal);
                    }
                    
                }
            }
            if(parentNode.tagName === "LYTE-YIELD" /*parentNode.getAttribute("is") === "insertYield"*/) {
                let pData = _LC.getCmpData(parentNode.component.data);
                pData[_LC.String.toCamelCase(node.nodeName)] = origNodeValue;
            }
            if (/^(INPUT|TEXTAREA|SELECT)$/.test(parentNode.nodeName)) {
                        if (node.nodeName === "value") {
                            parentNode.value = (nodeValue === undefined) ? "" : nodeValue;
                        } else if (node.nodeName === "checked") {
                            parentNode.checked = nodeValue;
                        }
            }
            if(!nodeValue && _LC.booleanAttrList.indexOf(node.nodeName) !== -1) {
                toBeRemoved.$push(node.nodeName);
            }
        } 
        else {
            node.nodeValue = nodeValue === undefined ? '' : nodeValue;
        }
	return node;
    }

    debounce(func, threshold) {
        var timeout;
        return function debounced() {
            var obj = this, args = arguments;
            function delayed() {
            	func.apply(obj, args);
                timeout = null;
            };
            if (timeout){
            	clearTimeout(timeout);
            }
            timeout = setTimeout(delayed, threshold || 100);
            //console.log(timeout,threshold);
        };
    }
    getProperty(key) {
        var arr = key.match(/([^[\].]+|\[\])/g);
        let property = this;
        if(!property._properties[arr[0]]) {
            property._properties[arr[0]] = {};
        } 
        property = property._properties[arr[0]];
        
        defProp(property, '_path', {enumerable: false, value : arr[0]});
        for(let i=1;i<arr.length;i++) {
            if (arr[i].startsWith("'") || arr[i].startsWith('"')) {//added check
			    arr[i] = arr[i].substring(1, arr[i].length -1);
		    }
            if(!property[arr[i]]) {
                property[arr[i]] = {};
                defProp(property[arr[i]], '_path', {enumerable: false, value : property._path + "." + arr[i]});
            }
            property = property[arr[i]];
        }
        return property;
    }
    //updN
    updateNode(node, updatePath, oldValue, newValue, exactProp, options) {
        let compInstance = this.component;
        
        var del = "delete";
        let multiplePropNode = [];
        let multipleDeepNode = [];
        let multipleProp;
        let nodeHasHelperNode;
        let isDirectiveNode;
        let processDetails = {}
        if(node.nodeType == 2){
            isDirectiveNode = _LC.directive.isDirectiveNode(node)
        }
        
        if(node._multipleProperty) {
        	for(var i=0;i<node._multipleProperty.length;i++) {
        		if(node._multipleProperty[i]  && (node._multipleProperty[i].dynamicValues.lastIndexOf(updatePath) > 0 || node.__lq)) {
                    var dynStartIndex = node.__lq ? node._multipleProperty[i].dynamicValues.length : node._sq ? node._multipleProperty[i].dynamicValues.lastIndexOf(updatePath) : 1;
                    multiplePropNode[i] = multipleDeepNode[i] = false;
                    multipleProp = node._multipleProperty[i];
		            let nodes;
                    for(var j=0;j<dynStartIndex;j++){
                        var pathName = node.__lq || node._sq ? multipleProp.dynamicValues[j] : multipleProp.actProp._path;
                        if(multipleProp.dynamicProp){
                            nodeHasHelperNode = true;
                            if(!node._sq || multipleProp.dynamicProp[multipleProp.dynamicValues[j]]){
                                multiplePropNode[i] = multipleProp;
                                nodes = multipleProp.dynamicProp[pathName];
                                if(nodes  && pathName.indexOf('.') != -1) {
                                    let index = nodes.indexOf(node);
                                    if(index != -1){
                                        nodes.$splice(index, 1);
                                    }
                                }
                                let helperNode = multipleProp.helperNode;
                                if(nodes.length === 0) {
                                    if(helperNode.getAttribute("is") === "if" || helperNode.getAttribute("is") === "switch" || (helperNode.hasAttribute("lc-id") && helperNode.getAttribute("is") === "case")) {
                                        multipleProp.actProp._helperNodes[del](helperNode);
                                        delete multipleProp.dynamicProp[multipleProp.actProp._path];
                                    } else {
                                        delete multipleProp.dynamicProp[multipleProp.actProp._path];
                                        if(helperNode._items) {
                                            let removeHelper = true;
                                            for(let i=0;i<helperNode._items.length;i++) {
                                                if(helperNode._items[i]._dynamicProperty && helperNode._items[i]._dynamicProperty[pathName]) {
                                                    removeHelper = false;
                                                    break;
                                                }
                                            }
                                            if(removeHelper) {
                                                multipleProp.actProp._helperNodes[del](helperNode);
                                                //console.log('for helper is removed');
                                            }
                                        }
                                    }
                                }
                            }
                        }
                        if(!nodeHasHelperNode) {
                            let deepNodeArr = [];
                            if(multiplePropNode[i] == false){
                                multiplePropNode[i] = ["dynamicNodes"];
                                multipleDeepNode[i] = [deepNodeArr];
                            }else{
                                multiplePropNode[i].$push("dynamicNodes");
                                multipleDeepNode[i].$push(deepNodeArr);
                            }
                            var prop = this.getProperty(pathName);
                            nodes = multipleProp.actProp._dynamicNodes;
                            if(nodes) {
                                let index = nodes.indexOf(node);
                                if(index != -1 && pathName!=updatePath && pathName.indexOf('.') != -1){
                                    nodes.$splice(index, 1);
                                    if(!nodes.length) {
                                        delete multipleProp.actProp._dynamicNodes;
                                    }
                                }
                            }
                            _LC.deep.delete(this, node, multipleProp.dynamicValues, pathName, deepNodeArr);
                        }else{
                            nodeHasHelperNode=false; 
                        }
                    }
		        }
			}
		}
        if(!node.syntaxValue && !node.helperValue) {
            return;
        }
        let contextSwitchInfo;
        let isYieldContext;
        if(node._cx || (node.nodeType === 2 && node.ownerElement._cx)) {
            contextSwitchInfo = node._cx || node.ownerElement._cx;
            var contextSwitchArray = [];
            _LC.changeContext(contextSwitchInfo.node, contextSwitchArray, contextSwitchInfo );
        } else if(node.tagName === "LYTE-YIELD" && node._callee._cx) {
        	isYieldContext = true;
        	contextSwitchInfo = node._callee._cx;
        	var contextSwitchArray = [];
        	_LC.changeContext(contextSwitchInfo.node, contextSwitchArray, contextSwitchInfo, true);
        }
        let nodeValue;
        let dynamicValues = [];
        let deepValues = [];
        var isHelper = false;
        let helperRetVal;
        if(node.helperValue){
            isHelper = true;
            nodeValue = node.helperValue;
                    let helperFunc = nodeValue;
                    if(helperFunc._t){
                        node._sq = true;
                    }
                    let helperArgs = this.processArgs(this,{"helperInfo" : helperFunc} ,dynamicValues, deepValues,undefined,node, undefined,undefined,undefined,processDetails, {oldValue : oldValue, newValue : newValue, path : updatePath});    
                    if(helperFunc._t == "sq"){
                        helperRetVal = this.processArray(this,{"name" : helperFunc.name, "args" : helperArgs},dynamicValues, deepValues,helperFunc.extra,undefined, node, undefined, processDetails);
                    }else{
                        helperRetVal = this.processHelper(this,{"name" : helperFunc.name, "args" : helperArgs}, node, processDetails);
                    }
                    nodeValue = helperRetVal;
                    if(helperFunc.name === "unescape") {
                        let oldDynamicPosition = node.dynamicPositions;
                        let oldStartingNode = oldDynamicPosition.startingNode;
                        let oldChldLen = oldDynamicPosition.length;
                        while(oldChldLen > 1) {
                            let next = oldStartingNode.nextSibling;
                            oldStartingNode.remove();
                            oldStartingNode = next;
                            oldChldLen--;
                        }
                        let childLen = nodeValue.childNodes.length;
                        if(!childLen) {
                            nodeValue.appendChild(document.createTextNode(""));
                            childLen = 1;
                        }
                        let startingNode = nodeValue.childNodes[0];
                        oldStartingNode.replaceWith.apply(oldStartingNode,nodeValue.childNodes);
                        let obj = {startingNode : startingNode, length: childLen};
                        node.dynamicPositions = obj;
                        nodeValue = undefined;
                    }
        } else {
            let boundValue = node.syntaxValue;
            let path;
            if(boundValue.indexOf('.') !== -1 || boundValue.indexOf('[') !== -1) {
                path = boundValue;
                boundValue = boundValue.substring(0,boundValue.indexOf('.'));
            }
            let cmpData = _LC.getCmpData(compInstance.data);
            let value = path ? _LC.get(cmpData, path, dynamicValues) : cmpData[boundValue]; 
            nodeValue = !typeof value === "boolean" && !typeof value === "number" ? (value? value : ""): value;
        }
        let origNodeValue = nodeValue;
		if(!(dynamicValues[0] instanceof Array)) {
        	dynamicValues = [dynamicValues];
		}
        
        if(multiplePropNode) {
        	for(var i=0;i<multiplePropNode.length;i++) {
        		if(multiplePropNode[i]) {
                    var ind = node.__lq ? node._multipleProperty[i].dynamicValues.length : node._sq ? node._multipleProperty[i].dynamicValues.lastIndexOf(updatePath) : 1;
					let multipleProp = node._multipleProperty[i];
                    let dynamicValIndex = node._multipleProperty[i].index;
                    for(var j=0;j<ind;j++){
                        if(node.__lq){
                            let processDynVal = processDetails.lqDyn[dynamicValIndex][0];
                            if(!processDynVal){
                                continue;
                            }
                        }
                        if(node._sq && dynamicValues[dynamicValIndex][j].indexOf('.') == -1){
                            continue;
                        }
                        
                        let prop = this.getProperty(dynamicValues[dynamicValIndex][j]);
                        let totalProp = this.getProperty(node.__lq ? dynamicValues[dynamicValIndex][j] : dynamicValues[dynamicValIndex][j].substring(0, dynamicValues[dynamicValIndex][j].indexOf('.')));
                        var value = this.getData(dynamicValues[dynamicValIndex][j].substring(0, dynamicValues[dynamicValIndex][j].indexOf('.')));                            
                        if(Array.isArray(multiplePropNode) && multiplePropNode[i] && multiplePropNode[i][j] && (multiplePropNode[i][j] === "dynamicNodes")) {
                            makeArray(prop, "_dynamicNodes");
                            if(prop._dynamicNodes.indexOf(node) == -1){
                                prop._dynamicNodes.$push(node);
                            }
                            node._deepValues = deepValues;
                            _LC.deep.add(this, node, dynamicValues[dynamicValIndex], pathName, multipleDeepNode[i][j]);
                        } else {
                            makeSet(prop, "_helperNodes");
                            prop._helperNodes.add(
                                    multipleProp.helperNode
                            );
                            let dynamicProp = multipleProp.dynamicProp;
                            dynamicProp[prop._path] ? dynamicProp[prop._path].$push(node): (dynamicProp[prop._path] = []).$push(node);
                            multipleProp.helperNode._deepValues = deepValues;
                        }
                        if(j==0){
                            if(node.ownerElement && (node.ownerElement.hasAttribute("lyte-for") || node.ownerElement.hasAttribute("lyte-if") || node.ownerElement.hasAttribute("lyte-switch") || node.ownerElement.hasAttribute("lyte-forIn")) || node.ownerElement && node.ownerElement.tagName === "TEMPLATE" && /^(for|forIn)$/.test(node.ownerElement.getAttribute("is")) && !isHelper) {
                                var type = node.ownerElement.getAttribute("is");
                                if (type === "for" && node.nodeName === "items" || type === "forIn" && node.nodeName === "object") {
                                    if(multipleProp.actProp._forHelpers.has(node.ownerElement)){
                                        multipleProp.actProp._forHelpers.delete(node.ownerElement);
                                    }
                                    if(!multipleProp.actProp._forHelpers.size) {
                                        delete multipleProp.actProp._forHelpers;
                                    }
                                    if (!prop._forHelpers) {
                                        makeSet(prop, "_forHelpers");
                                    }
                                    node.ownerElement._actualBinding = prop;
                                    prop._forHelpers.add(node.ownerElement);
                                }
                            }
                            if(value != undefined){
                                _LC.establishBindings(totalProp, value);
                                
                            }
                            
                            node._multipleProperty[i].actProp = prop;
                            node._multipleProperty[i].dynamicValues = dynamicValues[dynamicValIndex];
                        }
                    }
	        	}
	        }
		}
        if(isDirectiveNode){
            _LC.directive.setNodeArgs(node,origNodeValue);
            let dirIns = node.ownerElement._directiveIns;
            if(dirIns && dirIns.onConfigChange){
                let obj = {
                    type : "change",
                    oldValue : dirIns.config,
                    newValue : origNodeValue
                }
                dirIns.config = origNodeValue;
                node.ownerElement._directiveIns.onConfigChange.apply(node.ownerElement._directiveIns,[obj]);
            }
            if(node.ownerElement.hasAttribute("comp-in-parent")){
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Component.set(node.ownerElement.$data, _LC.String.toCamelCase(node.nodeName), nodeValue);
            }
            
            if(node.ownerElement._initialClassValue){
                _LC.directive.class.updateValue(node.ownerElement);
            }
        }
        else if(node.nodeType === 2) {
        	let parentNodes = [];
            let pN = node._parentNode ? node._parentNode :  node.ownerElement;
            if(pN.tagName === "TEMPLATE" && pN.getAttribute("is") === "component" && node.nodeName !== "component-name" && node.nodeName !== "component-class") {
            	let isKeepAlive = pN.hasAttribute("lyte-keep-alive");
            	if(isKeepAlive) {
            		for(var key in pN._renderedComponent) {
            			parentNodes.$push(pN._renderedComponent[key]);
            		}
            	} else {
                    var compName = pN._currentComponent;
                    if(compName && pN._renderedComponent[compName]){
                        parentNodes.$push(pN._renderedComponent[compName]);
                    }
            	}
            }
            parentNodes.$push(pN);
            for(let i=0;i<parentNodes.length;i++) {
            	let parentNode = parentNodes[i];
                let nodeName = node.nodeName;
                let $data = nodeName == "$data" ? true : false;
                if($data && exactProp){
                    nodeValue = nodeValue[exactProp];
                    nodeName = exactProp;
                }
            	if(parentNode.set) {
                    // if(parentNode._dynComp && node.nodeName == "component-data"){
                    //     parentNode.set(nodeValue , undefined, undefined,true);    
                    // }else{
                        // parentNode.set(_LC.String.toCamelCase(node.nodeName), nodeValue, undefined,true);
                    // }
                        if(nodeName == "$data"){
                            for(let key in nodeValue){
                                parentNode.set(_LC.String.toCamelCase(key), nodeValue[key], undefined,true);
                            }
                        }else{
                            parentNode.set(_LC.String.toCamelCase(nodeName), nodeValue, undefined,true);   
                        }
                    } else {
                        parentNode._initProperties = parentNode._initProperties || {};
                        parentNode._initProperties[_LC.String.toCamelCase(nodeName)] = nodeValue;
                    }
                    let pData = parentNode && parentNode.tagName === "LYTE-YIELD" ? (_LC.getCmpData(parentNode.component.data)) : undefined;
                    if(parentNode.tagName === "LYTE-YIELD" && pData && nodeName && pData[nodeName] !== nodeValue /*parentNode.getAttribute("is") === "insertYield"*/) {
                        _LC.set(pData, _LC.String.toCamelCase(nodeName), nodeValue,undefined , undefined, parentNode);
                    }
                    parentNode._attributes = parentNode._attributes || {};
                    //!== "string"
                    let isCustomElement = _LC.isCustomElement(parentNode, true)
                    
                    if(isCustomElement && typeof nodeValue !== "string") {
                        if(node.ownerElement.nodeName === "TEMPLATE") {
                            if(node.helperValue) {
                            	if((node.ownerElement.getAttribute("is") ===  "for" && nodeName === "items") || (node.ownerElement.getAttribute("is") ===  "forIn" && nodeName === "object")) {
                            		let oldValue = node.ownerElement._attributes[nodeName];
                            		let newValue = nodeValue;
                            		_LC.removeSelectedBindingDeep(node.ownerElement._actualBinding, oldValue);
                            		if(newValue  && typeof newValue !== "number") {
                                        addBindings(newValue,node.ownerElement._actualBinding);
                            			_LC.establishBindings(node.ownerElement._actualBinding, newValue);
                            		}
                            		if(nodeName === "object") {
                            			_LC.removeSelectedBindingDeep(node.ownerElement._propBindingObject, oldValue);
                            		}
                            		//console.log("old Value ", oldValue, " new Value ", newValue);
                            	}
                            }else{
                                if((node.ownerElement.getAttribute("is") ===  "for" && nodeName === "items") || (node.ownerElement.getAttribute("is") ===  "forIn" && nodeName === "object")) {
                            		let oldValue = node.ownerElement._attributes[nodeName];
                            		if(nodeName === "object") {
                            			_LC.removeSelectedBindingDeep(node.ownerElement._propBindingObject, oldValue);
                            		}
                            	}
                            }
                            parentNode["__"+nodeName] = true;
                            if(nodeName != "case"){
                                parentNode.removeAttribute(nodeName);
                            }
                        } else {
                            //Needs revisiting
                            //parentNode.removeAttribute(nodeName);
                        }

                    } else {
                        if(typeof nodeValue === "boolean") {
                            parentNode._attributes = parentNode._attributes || {};
                            parentNode._attributes[nodeName] = nodeValue;
                            if(!nodeValue) {
                                parentNode.removeAttribute(nodeName);
                            } else {
                                parentNode.setAttribute(nodeName, "");
                            }
                        } else {
                            if(nodeValue && typeof nodeValue === "object"){
                                var res;
                                if(typeof Record != "undefined" && nodeValue instanceof Record){
                                    res = JSON.stringify(nodeValue.$.toJSON())
                                }
                                else{
                                    try{
                                        res = JSON.stringify(nodeValue)
                                    }
                                    catch(exp){
                                        //@Slicer.developmentStart
                                        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(
                                            node,
                                            "LC013",
                                            nodeName,
                                            node.ownerElement.nodeName.toLocaleLowerCase(),
                                            node.ownerElement.nodeName.toLocaleLowerCase()
                                        )
                                        //@Slicer.developmentEnd
                                    }                                
                                }
                            }
                            else if(nodeValue== null || nodeValue == undefined){
                                res = ""
                            }
                            else{
                                res = nodeValue
                            }
                            nodeValue = res;
                            let locNodeVal = nodeValue === undefined ? "" : nodeValue;
                            // if(nodeName === "style") {
                            //     node.ownerElement.setAttribute("style",locNodeVal);
                            // } else {
                            //     node.nodeValue = locNodeVal;
                            // }

                            // if(node instanceof Node) {
                            //Check safari issue once
                            let oE = node.ownerElement;
                            if(oE.hasAttribute(nodeName)) {
                                if(oE.tagName != "INPUT" || nodeName != "value" || !oE.validity.badInput) {
                                    oE.setAttribute(nodeName, locNodeVal);
                                }
                            } else {
                                node.nodeValue = locNodeVal;                              
                            }
                        }
                    }
                    parentNode._attributes[nodeName] = nodeValue;
                    if(/^(INPUT|TEXTAREA|SELECT)$/.test(parentNode.nodeName)) {
                        if(nodeName === "value") {
                             let val = (nodeValue === undefined) ? "" : nodeValue;
                             if(parentNode.value !== val) {
                                parentNode.value = val;
                             } 
                        } else if(nodeName === "checked") {
                            parentNode.checked = nodeValue;
                        }
                    }
                    if(!nodeValue && _LC.booleanAttrList.indexOf(nodeName) !== -1) {
                        parentNode.removeAttribute(nodeName);
                    }
                    let isStopped = parentNode._isStopped;
                    let result;
                    switch(parentNode.getAttribute("is")) {
                        case "for" :
                            if(!options){
                                options = {"type" : "update"}
                            }else{
                                options.type = "update"
                            }
                            this.updateForHelper(parentNode, options);                            break;
                        case "if" : 
                            result = this.updateSwitchHelper("e",parentNode, undefined, true, true);
                            break;
                        case "case" : 
                            result = this.updateSwitchHelper("s",parentNode._parentSwitch, undefined, true, true,undefined,undefined,parentNode);
                            break;
                        case "forIn" : 
                            if(!options){
                                options = {"type" : "update"}
                            }else{
                                options.type = "update"
                            }
                            this.updateForInHelper(parentNode , options);                            break;
                        case "switch" :
                            this.updateSwitchHelper("s",parentNode, undefined, true, true);
                            break;
                        case "component" : 
                            if(nodeName === "component-name" || nodeName === "component-class") {
                                this.updateDynamicComponent(parentNode, "update");    
                            }
                            break;
                        default:            
                    }
//                 	let handleBreakOptions;
//                 	if(isStopped &&  isStopped !== result) {
//                 		//console.log("new value is stopped");
//                 		if(!result) {
//                 			//console.log("new value is not stopped");
//                 			if(isStopped === "break") {
//                 				handleBreakOptions = "SM"
//                 			} else {
//                 				handleBreakOptions = "SS"
//                 			}
//                 		} else if(result === "break") {
//                 			handleBreakOptions = "MS";
//                 			//console.log("old value is continue and new value is break");
//                 		} else {
//                 			handleBreakOptions = "SM";
//                 			//console.log("old value is break and new value is continue");
//                 		}
//                 	} else if(result === "break") {
//                 		handleBreakOptions = "MS";
//                 		//console.log("old value not stopped and new value is break");
// //                		this.handleBreak(parentNode._cx, "break");
//                 	} else if(result === "continue") {
//                 		handleBreakOptions = "SS";
//                 		//console.log("old value not stopped and new value is continue");
// //                		this.handleBreak1(parentNode._cx , "continue");
//                 	}
//                 	if(handleBreakOptions) {
//                 		this.handleBreak(parentNode._cx, handleBreakOptions);
//                 	}
            }
        }
        else {
            node.nodeValue = nodeValue === undefined ? '' : nodeValue;
        }
        
        if(contextSwitchInfo) {
            _LC.removeContext(contextSwitchInfo.node, contextSwitchArray, contextSwitchInfo, isYieldContext);
        }
    }

    // handleBreak(contextSwitchInfo, options) {
    // 	if(contextSwitchInfo) {
    // 		let forTemplate = contextSwitchInfo.node;
    // 		let breakIndex = contextSwitchInfo.itemIndex;
    // 		let itemValue = forTemplate.getAttribute("item");
    // 		let forContent = contextSwitchInfo.node._forContent;
    // 		let endIndex = options[0] === "M" ? forContent.length : breakIndex + 1;
    // 		for(let j=breakIndex;j<endIndex;j++) {
    // 			let currentForContent = forContent[j];
    // 			for(let i=0;i<currentForContent.length;i++) {
    // 				currentForContent[i].remove();
    // 				if(currentForContent[i]._forContent || currentForContent[i]._caseContent) {
    // 					this.removeHelpers(currentForContent[i]);
    // 				}
    // 			}	
    // 				forContent[j] = [];
    // 				_LC.removeSelectedBindingDeep(forTemplate._items[j].itemProperty, forTemplate._attributes.items[j]);
    // 				forTemplate._helpers[j] = [];
    // 				forTemplate._items[j] = {"_dynamicProperty" : {}, "itemProperty" : {}, "indexProperty": {}};
    // 		}
    // 		let length = forTemplate._attributes.items.length;
    // 		if(options[1] === "M") {
    // 			this.updateForHelper(forTemplate, {firstIndex : breakIndex, secondIndex : length - breakIndex, "type" : "replace"}, undefined, {});
    // 		} else {
    // 			this.updateForHelper(forTemplate, {firstIndex : breakIndex, secondIndex : 1, "type" : "replace"}, undefined, {});
    // 		}
    // 	}
    // }

    createCustomEvent(eventName, parentNode, actObj){
        const customEvent = new CustomEvent(eventName);
        parentNode._actions[eventName] = customEvent;
        parentNode._actions[eventName].processAction = actObj;
    }

    isEmptyString(str){
        return (!(typeof str === "string") || str === "" );
    }
    setLyteSequence(){
        let registryIns = this.$registry;
        let appAddonIns = this.component.getAppOrAddon();
        if(registryIns.constructor.lyteSequence || appAddonIns.constructor.lyteSequence || this.component.constructor.lyteSequence){
            this.__lq = true;
        }
    }
    processArgs(scope,dynN,dynamicValues,deepValues,event, node, newCompile,cache,parentSq,processDetails,observer,unboundFlag){
        unboundFlag=dynN.helperInfo.name=='unbound'?true:unboundFlag;
        let args = dynN.newHelperInfo && newCompile ? dynN.newHelperInfo.args : dynN.helperInfo.args;
        if(dynN.helperInfo && dynN.helperInfo._t == "sq")   {
            parentSq = true;
        }
        dynamicValues = dynamicValues || [];
        processDetails.lqDyn = processDetails.lqDyn || [];
        let lqDyn = processDetails.lqDyn;
        let previousArg;
        let tempSkipIndex;
        let skipAll = dynN.helperInfo.skipAll;
        let scpData = _LC.getCmpData(scope.component.data);
        args = (Array.isArray(args)) ? Array.from(args) : args;
        for(let i=0; i<args.length; i++){
            if(args[i] && args[i].type){
                let addSkipAll = false
                if(node && node.__lq && (skipAll || tempSkipIndex == i)){
                    args[i].value.skipAll = true;
                    addSkipAll = true;
                }
                if(args[i].type == "sq"){
                    this.internalArray(scope, args, i, dynamicValues, deepValues, event, node, newCompile,parentSq, processDetails);
                }else{
                    this.internalHelpers(scope, args, i, dynamicValues, deepValues, event, node, newCompile,cache,parentSq, processDetails,unboundFlag);
                }
                if(addSkipAll){
                    dynN.helperInfo.args[i].value.skipAll = false;
                }
            } else {
                if(!this.isEmptyString(args[i])) {
                    if(args[i].startsWith("'") && args[i].endsWith("'")){
                        args[i] = args[i].substr(1,args[i].length-2);   
                        if(this.__lq){
                            if(!node.__lq){
                                processDetails.lqDyn = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(dynamicValues);
                                node.__lq = true;
                            }
                            switch(args[i]){
                                case "||" : {
                                    if(previousArg){
                                        processDetails.skipHelper = true;
                                        tempSkipIndex = i+1;
                                    }
                                }
                                break;
                                case "&&" : {
                                    if(!previousArg){
                                        processDetails.skipHelper = true;
                                        tempSkipIndex = i+1;
                                    }
                                }
                                break;
                                case "?:" : {
                                    if(previousArg){
                                        tempSkipIndex = i+2;
                                    }else{
                                        tempSkipIndex = i+1;
                                    }
                                    processDetails.skipHelper = true;
                                }
                            }
                        }
                    } else {
                        args[i] = args[i].trim();
                        if(args[i] === "event" && event) {
                            args[i] = event;
                        } else if(args[i] === "this" && node) {
                            args[i] = node.nodeType === 2 ? node.ownerElement : node;
                        } else {
                            let dynamicVals = [];
                            let argStr = args[i];
                            args[i] = _LC.get(scpData,args[i],dynamicVals,cache,deepValues);
                            if(argStr.endsWith(".*")){
                                args[i] = _LC.deep.data(argStr, observer, scpData);
                            }
                            if(!parentSq || dynamicValues.length == 0){
                                if(unboundFlag!==true){
                                    dynamicValues.$push(dynamicVals);
                                    if(node && node.__lq && lqDyn){
                                        lqDyn.$push(skipAll || tempSkipIndex == i ? [false] : dynamicVals);
                                    }
                                }  
                            }else{
                                if(dynamicVals.length > 1){
                                    for(let s=0; s<dynamicVals.length; s++){
                                        if(unboundFlag!==true){
                                            dynamicValues[dynamicValues.length-1].$push(dynamicVals[s]);
                                            if(node && node.__lq && lqDyn){
                                                lqDyn[lqDyn.length-1].$push(skipAll || tempSkipIndex == i ? false : dynamicVals[s]);
                                            }
                                        }
                                    }
                                }else{
                                    if(unboundFlag!==true){
                                        dynamicValues[dynamicValues.length-1].$push(dynamicVals[0]);
                                        if(node && node.__lq && lqDyn){
                                            lqDyn[lqDyn.length-1].$push(skipAll || tempSkipIndex == i ? false : dynamicVals[0]);
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else if(newCompile) {
                    if(args[i] instanceof Array) {
                        args[i] = _LC.getDD(scpData.data, args[i]);
                    }
                }
            }
            previousArg = args[i];
        }
        return args;
    }

    internalHelpers(scope,args,i,dynamicValues, deepValues, event, node, newCompile,cache,parentSq, processDetails,unboundFlag){
        let helperFunc = args[i].value;
        var helperVal =  this.processHelper(scope,{"name" : helperFunc.name, "args" : this.processArgs(scope,{"helperInfo" : helperFunc},dynamicValues, deepValues, event, node, newCompile,cache,parentSq, processDetails,undefined,unboundFlag)},undefined, processDetails);
        args[i] = helperVal;
    }
    internalArray(scope, args, i, dynamicValues, deepValues, event, node, newCompile,parentSq, processDetails) {
        var helperFunc = args[i].value;
        if(!parentSq){
            dynamicValues.$push([]);
            let lqDyn = processDetails.lqDyn
            if(node && node.__lq && lqDyn){
                lqDyn.$push([]);
            }
        }
        var helperVal =  this.processArray(this,{"name" : helperFunc.name, "args" : this.processArgs(scope,{"helperInfo" : helperFunc},dynamicValues, deepValues, event, node, newCompile,undefined,true, processDetails)},dynamicValues, deepValues, helperFunc.extra ? Array.from(helperFunc.extra):undefined,event, node, newCompile, processDetails);
        args[i] = helperVal;
    }
    processArray(scope,helperFunc,dynamicValues, deepValues ,extra,event, node, newCompile, processDetails){
        var arrVal;
        dynamicValues = dynamicValues || [];
        var dynVal = [];
        var arr = helperFunc.name;
        var len = helperFunc.args.length;
        var str="";
        let skipAll = helperFunc.skipAll;
        let lqDyn = processDetails.lqDyn;
        for(var i=0; i<len; i++){
            str = str + "['"+ helperFunc.args[i] + "']"
        }
        var fullData = arr+str;
        if(extra && extra.length){
            for(var i=0; i<extra.length; i++){
                if(typeof extra[i] == "string"){
                    fullData += extra[i];
                }else{
                    if (extra[i] && extra[i].type) {
                        if (extra[i].type == "sq") {
                            this.internalArray(scope, extra, i, dynamicValues, deepValues, event, node, newCompile, true, processDetails);
                        } else {
                            this.internalHelpers(scope, extra, i, dynamicValues, deepValues, event, node, newCompile, undefined, true, processDetails);
                        }
                        fullData += extra[i];
                    } 
                }
            }
        }
        if(!helperFunc.name.startsWith('.')){
            arrVal = _LC.get(scope.component.data, fullData, dynVal, deepValues);
            if(dynamicValues.length == 0){
                dynamicValues.$push(dynVal);
                if(node && node.__lq && lqDyn){
                    lqDyn.$push(skipAll ? [false] : dynVal);
                }

            }else{
                dynamicValues[dynamicValues.length-1].$unshift(dynVal[0]);
                if(node && node.__lq && lqDyn){
                    lqDyn[lqDyn.length-1].$unshift(skipAll ? false : dynVal[0]);
                }
            }
            return arrVal;
        }else{
            return fullData;
        }
        
    }
    processHelper(scope,helperFunc, node, processDetails){
        if(processDetails && processDetails.skipHelper){
            return;
        }
        let args = [];
        let helperName = helperFunc.name;
        let helperObj = {compClass : scope.$registry.registeredHelpers[helperName]};
        if(!helperObj.compClass){
            let obj = {compName : helperName, type : "helper"};
            // _LC.getCompRegistry(obj,this);
            _LC.traverseRegistries([scope.$registry], obj, this);
            if(!_LC.verifyDetails(obj)){
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.RegistryError.error("LC010", "Helper", helperName);
                //@Slicer.developmentEnd
                return;
            }else{
                helperObj.compClass = obj.compClass;
            }
	    }
        switch(helperFunc.name){
            case "method" : 
                args.$push(this, node);
                break;
            case "lbind" :
                args.$push(this, node.ownerElement);
                break;
            case "lyteViewPort" :
                if(node && node.ownerElement){
                    args.$push(node.ownerElement);
                    if(!helperFunc.args.length){
                        args.$push(false);
                    }
                }else{
                    return true;
                }
        }
        return helperObj.compClass.apply(scope.component,args.$concat(helperFunc.args));
        // return _LyteComponent.registeredHelpers[helperFunc.name].apply(this,args.$concat(helperFunc.args));
    }

    // getActionProperty(prop){
    //     let hostProp = this._properties;
    //     let value = (hostProp)?hostProp[prop].value:undefined;
    //     return value;
    // }

    // hasInternalBindings(content){
    //     return content.match(/[(]{1}[^)]+[)]{1}/);
    // }

    // getArgValues(argNames, properties) {
    //     let argValueArray = [];
    //     for(let i=0;i<argNames.length;i++) {
    //         argValueArray.$push(properties[argNames[i]].value);
    //     }
    //     return argValueArray;
    // }

    createEventListeners(node,actionType,actObj){
        let self = this;
        if(!node._callee && node !== this) {
            node._callee = this;
        }
        if(globalDOMEvents.indexOf(actionType) == -1){
            let infoAttr = actionType.substr(2);
            let infoAttrVal = node.getAttribute(infoAttr);
            // var evntListener = function(event) {
            // 	var toRemove;
            //     if(!window.event) {
            //         window.event = event;
            //         toRemove = true;
            //     }
            //     _LC.throwAction.call(self,self,actionType.substr(2),actObj, undefined, undefined, node, event);
            //     if(toRemove) {
            //         window.event = undefined;
            //     }
            // };
            if ((ComponentRegistry._registeredCommonClass[node.localName] && !node.component) || (node.tagName === "TEMPLATE" && node.getAttribute("is") === "component")) {
                node._toRegEvnts = node._toRegEvnts || {};
                node._toRegEvnts[actionType.substr(2)] = {"listener" : globalEventHandler , "attrVal" : this.tagName.toLowerCase()+" => "+actObj.name};
            } else {
                node.setAttribute(infoAttr, this.tagName.toLowerCase()+" => "+actObj.name);
                //Event is not in capture phase because, in capture phase, multiple event listeners in hierarchy are called from parent to child (since registration is done in that order)
                node.addEventListener(actionType.substr(2), globalEventHandler);
            }
            if(node.hasAttribute(actionType)){
                node[actionType] = undefined;
            }
            node.removeAttribute(actionType);
        }
    }
    registerParentYield(yieldName){
        let locYield,parentYield = this._callee._yields[yieldName];
        if(parentYield){
            // if(Lyte._ie) {
            //     locYield = document.createElement("div");
            //     locYield.innerHTML = parentYield.outerHTML;
            //     locYield = locYield.childNodes[0];
            //     this.constructor.splitTextNodes(locYield);
            // } else {
                locYield = parentYield.cloneNode(true);
            // }
            Object.keys(parentYield).forEach(function(item) {  //eslint-disable-line no-loop-func
                locYield[item] = parentYield[item];
            });
            this._yields[yieldName] = locYield;
        }
    }

    registerYields() {
        // this._yields = {};
        let yields = this.querySelectorAll('template[is=registerYield],template[is=yield]');
        let lazyYields = [];
        for(let i=0;i<yields.length;i++) {
            while(yields[i].hasChildNodes()) {
                yields[i].content.appendChild(yields[i].childNodes[0]);
            }
            let yieldName = _LC.getYieldName(yields[i]);
            let def = yields[i].hasAttribute("default");
            if(def && this._yields[yieldName]){
                continue;
            }
            if(yields[i].hasAttribute("from-parent") && this._callee) {
                if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue){
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue.addDynamicYield(this, this._callee, yieldName);
                }
                if(this._callee._yields){
                    this.registerParentYield(yieldName);
                }
                else{
                    lazyYields.push(yields[i]);
                }
            } else {
                this._yields[yieldName] = yields[i];
            }
            //@Slicer.$hideStart
            let _LCHD = this.getDirectiveIns("hide");
            _LCHD && _LCHD.setTxtNode(this, yields[i]);
            //@Slicer.$hideEnd
        }
        if(lazyYields.length && this._callee){
            var self = this;
            this.lazyYield = function(){
                if(self._callee){
                    for(let j=0;j<lazyYields.length;j++){
                        let lYield = lazyYields[j];
                        self.registerParentYield(_LC.getYieldName(lYield));
                    }
                    self._callee.removeEventListener("onReady", self.lazyYield);
                    delete self.lazyYield;
                }
            };
            this._callee.addEventListener("onReady", this.lazyYield);
        }
    }

    connectedCallback() {
        this._connectedCallback();
    }
    _connectedCallback(){
        if(this.hasAttribute("lyte-rendered") || this._ccCalled || !this._registryClass || !this.$registry.registeredComponents[this.localName]) {
            return;
        }
        let fastRenderProp = this._fR;
        if(!fastRenderProp) {
            this._callee = this._callee || this.getCallee(this.parentNode);
        }
        this.__h = {};
        this.__counter = 0;
        this.__dc = {};
        if(fastRenderProp) {
            if(_LC.shouldIgnoreDisconnect()){
                return;
            }
            let methods = fastRenderProp._methods;
            if(methods) {
                this.setMethods(methods);
            }
            _LC.ccDelay.$push(this);
            this._ccCalled = true;
        } else {
            this.actualConnectedCallback();
        }
        let viewObj = this.getDirectiveIns("view")
        if(viewObj){
            viewObj.connectedCallback(this)
        }
    }
    cmpBind(fastRenderProp){
        let compData = _LC.getCmpData(this.component.data);
        if(!_LC.unbound && !fastRenderProp && !compData.lyteFastRender) {
            _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.establishObserverBindings.call(this,this.component.constructor._observers,undefined,undefined,undefined,_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte,_LC);
            _core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.establishWatchScope.call(this,this.constructor._deepWatchProperties)
            //this.establishObserverBindings();
            addBindings(compData, this._properties);
            _LC.establishBindings(this._properties, compData);
        }
    }
    actualConnectedCallback(){
        this._transitionHelperNodes = [];
        this._specialNodes = [];
        this._propProperty = {};
        this._propList = [];
        this._propNodes = {};
        this._hiddenTemplate = [];
        //@Slicer.developmentStart
        _LC.debugPerf(this, { render : 1 });
        //@Slicer.developmentEnd
        this._connectedCalled = true;
        let compData = _LC.getCmpData(this.component.data);
        if(this.component.constructor.dc){
            this.dc = this.component.constructor.dc.p;
        }
        let directiveObj = this.getDirectiveObj();
        directiveObj && directiveObj.instanciatePromises(this)
        let fastRenderProp = this._fR;
        if(fastRenderProp) {
            this._callee = this._callee || this.getCallee(this.parentNode);
        }
        for(let key in this._toRegEvnts) {
            this.addEventListener(key, this._toRegEvnts[key].listener);
            if(this.hasAttribute(key)) {
                this.setAttribute(key, this.getAttribute(key) + " ; "+ this._toRegEvnts[key].attrVal);
            } else {
                this.setAttribute(key, this._toRegEvnts[key].attrVal);
            }
        }
        this._toRegEvnts = {};
        let initialUnbound = _LC.unbound;
        let ssrBind  = this._ssrData || this.getAttribute("ssrbindservernode");
        if(ssrBind && !this.component.data.lyteUnbound){
            this.bindServerData();
            this.removeAttribute("ssrbindservernode");
        }
        this.setLyteSequence();
        let content =  this.afterConnected(fastRenderProp,ssrBind);
        !ssrBind && this.cmpBind(fastRenderProp);

        _LC.unbound = initialUnbound;
        var tagName = this.tagName;
        let dependentPromises = [];
        directiveObj && directiveObj.getDependentPromises(this,dependentPromises,this._dependentPromise);
        if(typeof content === "string") {
            if(content) {
                if(_LC.frSpecial) {
                    let temp = document.createElement("template");
                    temp.innerHTML = content;
                    this.innerHTML = "";
                    this.appendChild(temp.content);
                } else {
                    this.innerHTML = content;
                }
            }
            _LC.processAction(this);
        }
        else{
            let viewObj = this.getDirectiveIns("view")
            if(viewObj){
                viewObj.actualConnectedCallback(this,content)
            }
            let shadowObj = this.getDirectiveIns("shadow");
            let shadowDeep,shadowMode,shadowParent;
            var shadowSupported = _LC.directive.getTransitionArg(this,"shadow-supported")
            if(shadowObj){
                _LC.directive.setAttrFromRender(this,this._tagDirectives);
                // if(shadowSupported){
                    if(this.$registry._defaultDirectives && this.$registry._defaultDirectives.indexOf("shadow") != -1){
                        var shadowVal = _LC.directive.getTransitionArg(this,"shadow")
                        shadowMode = shadowVal;
                        this.component.data.lyteShadow = shadowVal; //af??
                    }
                    shadowParent = this;
                    if(this.parentNode.shadowRoot){
                        _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.warn(this.tagName +" should not be the direct child of a shadow component "+this.parentNode.tagName)
                    }
                    let rtObj = shadowObj.getShadowParent(this,directiveObj);
                    shadowParent = rtObj.shadowParent;
                    shadowDeep = rtObj.shadowDeep;
                // }
            }
            if(shadowObj && shadowMode == true){
                if(shadowSupported){
                    shadowObj.applyShadow(this,content,shadowParent,dependentPromises);
                }
                //@Slicer.developmentStart
                else{
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this,"LC017",this.localName)
                }
                //@Slicer.developmentEnd
            }
            else if(shadowObj && shadowParent){
                if(shadowMode == undefined && shadowDeep == true && this.constructor._observedAttributes.indexOf("lyteShadow")!=-1){
                    shadowObj.applyShadow(this,content,shadowParent,dependentPromises);
                }
                else{
                    this._hasShadowParent=true;
                    this._sw = [];
                    this.component.data.lyteShadow = false;//need to check
                    shadowObj.attachStyleToParentShadow(this, shadowParent, content);
                    if(directiveObj){
                        directiveObj.appendInDom(this,content,true,true,dependentPromises);
                    }else{
                        _LC.appendInDom(this,content);
                    }
                }
            }
            else{
                this.component.data.lyteShadow = false;//need to check
                if(this._compClass._style && _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv._duplicateStyle.indexOf(tagName) == -1){
                    var style = _LCSD.stringToStyle(this._compClass._style);
                    style.setAttribute("lyte-id","global-style-"+tagName);
                    if(this._ssrBind){
                        style.setAttribute("from-ssrComponent",tagName);
                    }
                    // if(style.tagName=="LINK"){
                    //     window.dispatchEvent(new CustomEvent("__onBeforeInject__",{detail:{link:style}}))
                    // }
                    // Lyte.$.assetsDiv.appendChild(style);
                    _LC.addLink(this, _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv, style);
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv._duplicateStyle.$push(tagName);
                }
                if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._compList.indexOf(tagName) == -1){
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv._compList.$push(tagName);
                }
                if(directiveObj){
                    directiveObj.appendInDom(this,content,true,true,dependentPromises);
                }
                //@Slicer.$hideStart
                if(_LC.directive.getTransitionArg(this,"hide-tag")){
                    let hideTagSupported=_LC.directive.getTransitionArg(this,"hide-tag-supported");
                    if(hideTagSupported){
                        let _LCHD = this.getDirectiveIns("hide");
                        _LCHD.connect(this, content);
                    }else{
                        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this,"LC020",this.localName)
                    }  
                }
                else{
                //@Slicer.$hideEnd
                    _LC.appendInDom(this, content);
                //@Slicer.$hideStart
                }
                //@Slicer.$hideEnd
            }
        }
        if(directiveObj){
            this._transitionAppend = []
        }
        let dataDef = this.component.__data;
        let attributes = this.attributes;
        this.__lyteIgnore = true;
        for(let i=attributes.length-1;i>-1;i--) {
            let camelCase = _LC.String.toCamelCase(attributes[i].nodeName);
            if(dataDef[camelCase] && dataDef[camelCase].hideAttr) {
            this.removeAttribute(attributes[i].nodeName);
            }
        }
        for(let dataName in this._hideAttr){
            let attrName = _LC.String.dasherize(dataName);
            let dataVal = this._hideAttr[dataName];
            if(dataVal == false){
                if(this.getAttribute(attrName) != this.component.data[dataName]){
                    this.setAttribute(attrName, this.component.data[dataName])
                }
            }
        }
        this.__lyteIgnore = false;
        if( this._callee && this._callee.serverCall ){
            this.serverCall = true;
        }
        //to bind in ssr
        if( this.serverCall && !this.hasAttribute( "server-rendered") ){
            this.setAttribute( "server-rendered", "");
            var newData = _LC.sendtoclient.call(this,this,true,this.component.__data);
            this.setAttribute("component-data", JSON.stringify( newData ) );
        }
        if( !this.hasAttribute( "server-rendered" ) )
        {        
            this.setAttribute("lyte-rendered", "");
        }
        
        if(!fastRenderProp && !_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._ignoreOnReady) {
            this.dispatchEvent(new CustomEvent("onReady"));
        }
        
        let _config_flag;
        if( this.component._ssr && this.component._ssr.config ){
            if( this.component._ssr.config.clientLifeCycleHooks != undefined ){
                _config_flag = this.component._ssr.config.clientLifeCycleHooks == true || ( typeof this.component._ssr.config.clientLifeCycleHooks == 'object' ? this.component._ssr.config.clientLifeCycleHooks.includes('didConnect') : false );
            }
        }
        
        let _overrides = this.component._ssr ? this.component._ssr.overrides : undefined;
        if( !this.hasAttribute( "server-rendered" ) || this.serverCall ||  _config_flag ){
            if( _overrides && _overrides.didConnect && this.serverCall ){
                _overrides.didConnect.apply( this.component );
            }else{
                if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue && (this.__delayDidConnect || _LC.getDelayDidConnect())){
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue.delayDidConnect(this, this.callback);
                }else{
                    this.callback("didConnect");
                }
            }
            if(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue && (this.__delayDidConnect || _LC.getDelayDidConnect())){
                _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue.delayDidConnect(this, this.onCallBack);
            }else{
                this.onCallBack("didConnect");
            }
        }

        if( this.serverCall == undefined ){
            this.callback( "didRender" );
        }            
        if(fastRenderProp) {
            this.removeAttribute("_lyteprop");
        }
        //@Slicer.developmentStart
        _LC.debugPerf(this, { render : 0 });
        //@Slicer.developmentEnd
    }
    onCallBack(name){
        let callbacks = this.component.constructor._callBacks[name];
        if(callbacks){
            for(let i=0;i<callbacks.length;i++){
                try{
                    callbacks[i].value.call(this.component);    
                } catch(e) {
                    //@Slicer.developmentStart
                    _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this.component, e);
                    //@Slicer.developmentEnd
                }
                
            }
        }
    }
    callback(name){
        var func = this.component[name];
        var args;
        if(func){
            //@Slicer.developmentStart
            var __cmpId = this.__renderId || undefined;
            var app = _LC.getNearestParentApp(this.component);
            _LC.debugPerf(app, __cmpId , name , this.constructor._compName);
            //@Slicer.developmentEnd
            if(arguments.length > 1) {
                args = Array.from(arguments);
                args.$splice(0,1)
            }
            try{
                func.apply(this.component, args || []);    
            } catch(e) {
                //@Slicer.developmentStart
                _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(this.component, e);
                //@Slicer.developmentEnd
            }
            //@Slicer.developmentStart
            _LC.debugPerf(app, __cmpId, name, this.constructor._compName);
            //@Slicer.developmentEnd
        }
    }
    establishObserverBindings() {
        let observers = this.component.constructor._observers;
        for(let i=0;i<observers.length;i++) {
            let props = observers[i].properties;
            for(let j=0;j<props.length;j++) {
                let actProp;
                let isArrayObserver = false;
                if(props[j].indexOf('.[]') !== -1) {
                    isArrayObserver = true;
                    actProp = this.getProperty(props[j].substring(0, props[j].indexOf('.[]')));
                } else {
                    actProp = this.getProperty(props[j]);
                }
                makeSet(actProp, "_observers");
                actProp._observers.add({callee : this, observer: observers[i], isArrayObserver : isArrayObserver});
            }
        }
    }
    removeBindings(properties, actualData) {
        var del = "delete";
        for(let i in properties) {
            let actData = actualData[i];
            if(actData && actData._bindings) {
                actData._bindings[del](properties[i]);
                //Error while trying to delete _bindings from actData when actData is of type Array
                /*  if(!actData._bindings.size) {
                    delete actData._bindings;
                } */
            }
            if(typeof properties[i] === "object" && actData) {
                this.removeBindings(properties[i], actData);
            }
        }
    }
    actualDisconnected() {
        try{
            var self = this;
            if(!self.component) {
                return;
            }
            self._cx = null;
            self._callee = null;
            self.component.$node = null;
            self.component.__data = null;
            if(self.component.data.__target__){
                self.component.data.__target__.__component__ = null;
            }
            else{
                self.component.data.__component__ = null;
            }
            self.component.data = null;
            self.component = null;
            self.__dc = self.__dc || {};
            self.__h =  self.__h || {};
            for (key in self.__dc) {
                var helper = self.__dc[key];
                //helper.remove();
                if(helper.hasAttribute("lyte-keep-alive")) {
                    var objKeys = Object.keys(helper._renderedComponent);
                    for(var j=0;j<objKeys.length;j++) {
                        let key = objKeys[j];
                        if(key !== helper._currentComponent) {
                            // Will remove from hDiv.
                            let comp = helper._renderedComponent[key];
                            comp.constructor.prototype._disconnectedCallback.call(comp);
                            comp.remove();
                        }
                    }
                }
            }
            for (key in self.__h) {
                self.__h[key].remove();
            }
            self.__h = {};
            self.__dc = {};
            let yields = self._yields;
            for(var key in yields) {
                yields[key]._callee = null;
            }
            self = null;
        }
        catch(ex){
            console.error("Errorrrr",ex);
        }
    }
    disconnectedCallback() {
        this._disconnectedCallback();
    }
    _disconnectedCallback(){
        if(_LC.shouldIgnoreDisconnect() || !this.component || this.__lyteIgnore || this._ignoreDisconnect) {
            return;
        }
        if(Array.from(_LC.hiddenComponentsDiv.content.childNodes).indexOf(this)!=-1){
            _LC.hiddenComponentsDiv.content.removeChild(this)
        }
        //@Slicer.$hideStart
        if(this._hiddenTemplate && this._hiddenTemplate.length){
            let _LCHD = this.getDirectiveIns("hide");
            _LCHD && _LCHD.callDisconnectedCallback(this);
        } 
        //@Slicer.$hideEnd
        if(this._removedTemplate){
            this._removedTemplate.forEach(el=>{
                if(_LC.tDiv.content.contains(el)){
                    _LC.tDiv.content.removeChild(el);
                }
            })
            this._removedTemplate=[];
        }
        let shadowObj = this.getDirectiveIns("shadow");
        let directiveObj = this.getDirectiveObj();
        shadowObj && shadowObj.destroyRef(this);
        let viewObj = this.getDirectiveIns("view")
        if(viewObj){
            viewObj.disconnectedCallback(this)
        }
        this._destroyed = true;
        if(this.__toRemoveLazy){
            for(var __key in this.__toRemoveLazy){
                var __arr = this.__toRemoveLazy[__key];
                __arr.forEach(function(id){
                    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.toRemoveFromRequiredServices(id);
                });
            }
            delete this.__toRemoveLazy;
        }
        this.component._bindings = null;
        this._parentHelper = null;
        var scpObj = this.__scpObj;
        let cmpData = _LC.getCmpData(this.component.data);
        if(scpObj){
            for(var key in scpObj){
                var propData = cmpData[key], id = scpObj[key], idArr = id.split("_");
                if(propData){
                    (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.removeNestScp)(cmpData[key], idArr[0], idArr[1], undefined, this);
                }
            }
        }
        if(!this._fR) {
            _LC.removeSelectedBindingDeep(this._properties, cmpData, true);
        }
        if(directiveObj){
            directiveObj.destroyHelperPromises([this.__dc,this.__h]);
        }
        var h = this.__h;
        for (key in h) {
            if(h[key]._actualBinding || (h[key]._hiddenTemplate && h[key]._hiddenTemplate.length)){
                this.removeHelpers(h[key]);
            }
        }
        h = {};
        for(key in this._properties) {
            this._properties[key] = {};
        }
        
        this._specialNodes = null;        
        
        this.callback('didDestroy');
        this.onCallBack('didDestroy');
        this._didDestroyCalled = true;
        this.component.constructor.activeInstances--;
        if(!_LC.dcc) {
            _LC.dcc = [];
            requestAnimationFrame(function() {
                requestAnimationFrame(function() {
                    if(viewObj){
                        viewObj.actualDisconnected(this)
                    }
                    try{
                        for(var i=0,item;item=_LC.dcc[i];i++) {
                            item.actualDisconnected();
                        }
                    }
                    catch(e) {

                    }
                    _LC.dcc = undefined;
                });
            });
        }
        _LC.dcc.$push(this);
        directiveObj && directiveObj.destroyPromises(this);
        if(this.lazyYield){
            this.removeEventListener("onReady", this.registerParentYield);
        }   
        // var self = this;
        // setTimeout(function() {
        //     self.actualDisconnected();
        // },0);
        // this.constructor.activeInstances--;
    }
}
customElementPrototype._V3InsApi = ["getData","cmpBind","component","actualConstructor","_connectedCallback","actualConnectedCallback", "getMethods","hasAction", "setActions", "setMethods", "getCallee", "afterConnected", "renderComponent", "renderFast", "formatValue", "renderNodes", "executeBlockHelpers", "updateBlockHelpers", "_attributeChangedCallback" , "removeHelpersSpecificIndex", "removeHelpers", "updateYield", "updateDynamicComponent","updateForHelper", "updateForInHelper", "updateSwitchHelper", "callObservers","bindNode","debounce", "getProperty", "updateNode", "handleBreak", "createCustomEvent", "isEmptyString", "processArgs", "internalHelpers", "processHelper", "getActionProperty", "hasInternalBindings", "getArgValues", "createEventListeners", "registerParentYield", "registerYields", "onCallBack", "callback", "establishObserverBindings", "removeBindings", "actualDisconnected" ,"_disconnectedCallback","throwAction","get","set","initializeMethod"];
customElementPrototype._V3StaticApi = ["_observers","_callBacks","_properties","activeInstances","_depthTemp","_bindsIds","_ssr","_config","_mixins","_serviceToBeUsed","_actions","_template","_dynamicNodes","_templateAttributes","_observedAttributes","_observedMethodAttributes","_data","_methods","_pendingComponents","splitTextNodes" ,"_registerComponent", "createDocFragment1" , "updateValue"];
customElementPrototype._v4RegClassApi = ["_reg","_compName","componentClass","_observedAttributes"]; //component
customElementPrototype._v4RegProtoApi = ["setData", "getData", "setMethods", "get", "set", "_pendingComponents"];
customElementPrototype.version = {v3 : {component : undefined}};
ltCf._customElementPrototype = customElementPrototype;
ltCf.v3 && !ltCf.instanctiatedBridge && ltCf.instanciateBridge();
// window._customElementPrototype = customElementPrototype;
// var onObj = function(){
//     return {"type": "callBack", "value":(this.type === "observer") ? this.value:this , "properties":arguments, "observes":(this.type === "observer" ? this: undefined)}
// }
// var observesObj = function() {
//     return {"type" : "observer", "value" : this, "properties" : arguments, "on":Function.prototype.on}
// }
// var computedObj = function() {
//     return {"type" : "computed", "value" : this, "properties" : arguments}
// }
// var fnProto = Function.prototype;
// fnProto.on ? fnProto.lyteOn = onObj : fnProto.on = onObj;
// fnProto.observes ? fnProto.lyteObserves = observesObj : fnProto.observes = observesObj;
// fnProto.computed ? fnProto.lyteComputed = computedObj : fnProto.computed = computedObj;


// BaseCompiler(Lyte,_LC);
// if(compConfig.compiler){
//     compConfig.compiler(_LC);
// }

_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.typeCast = _LC.typeCast;
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.getDataType = _LC.getDataType;
// Lyte.Component._get = _LC.get;
_LC.chromeBugFix = function() {
    var version = userAgent.match(/Chrom(e|ium)\/([0-9]+)\./);;
    version = version ? parseInt(version[2], 10) : 0;
    if(version > 62) {
        this.chI = [];
        document.addEventListener("focus", function(event) {
            var target = event.target;
            if(target.tagName === "INPUT" || target.tagName === "TEXTAREA" || target.tagName === "DIV") {
                if(_LC.chI.indexOf(target) == -1) {
                    _LC.chI.$push(target);
                }
            }
        } , true);
        this.chromeBugFix = function() {
            var tags = _LC.chI;
            var tagsL = tags.length;
            var toRemove = ["_callee", "_attributeDetails", "_attributes", "_removedAttributes", "_yields" , "_rA", "_cx"];//no i18n
            var toBeRemoved = [];
            var keepAliveInputs = [];
            document.querySelectorAll("[lyte-keep-alive]").forEach(function(item,index){
                for(var key in item._renderedComponent) {
                    keepAliveInputs.$push.apply(keepAliveInputs, Array.from(item._renderedComponent[key].querySelectorAll("input")));
                }
            });
            _LC.tDiv.content.querySelectorAll("[lyte-keep-alive]").forEach(function(item,index){
                for(var key in item._renderedComponent) {
                    keepAliveInputs.push.apply(keepAliveInputs, Array.from(item._renderedComponent[key].querySelectorAll("input")));
                }
            });
            for(var i= tagsL-1, item;item=tags[i];i--) {
                if((document.compareDocumentPosition(item) % 2) && (keepAliveInputs.indexOf(item) == -1)) {
                    tags.$splice(i,1);
                    item.remove();
                    toBeRemoved.$push(item);
                    item._rA = item._rA || [];
                    item._rA.forEach(function(remAttr) { //eslint-disable-line no-loop-func
                        remAttr.ownerElement = undefined;
                    });
                    toRemove.forEach(function(key) {//eslint-disable-line no-loop-func
                        item[key] = undefined;
                    });
                    Array.from(item.attributes).forEach(function(itemVal) {//eslint-disable-line no-loop-func
                        item.removeAttribute(itemVal.nodeName);
                    });
                }
            }
        }
        // Lyte.addEventListener("afterRouteTransition", function() {
        //     _LyteComponent.chromeBugFix();
        // });
        if(!_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Router) {
            setInterval(function() {
                // _LC.chromeBugFix();
                _LC.String.cache_c = {};
                _LC.String.cache_d = {};
            },300000)
        }
    }
}
_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.chromeBugFix = _LC.chromeBugFix;

// _LC._directives = {};
if(!customElements.get("lyte-safari-test-component")){
    customElements.define("lyte-safari-test-component", Test, undefined, {v4 : true});
}
var divTest = document.createElement("div");
divTest.innerHTML = "<lyte-safari-test-component t></lyte-safari-test-component>";

let _LCSD = _LC.shadow;
// _LyteComponent.render = _LC.render;
//Change it in v3.0 - Remove from _LC scope. 
// _LyteComponent.insertBefore = _LC.insertBefore;
// _LyteComponent.insertAfter = _LC.insertAfter;
// _LyteComponent.replaceWith = _LC.replaceWith;
// _LyteComponent.appendChild = _LC.appendChild;
_LC.tDiv = createElement("template");
_LC.tDiv.setAttribute("id", "dummy-templates-div");
// _LC.tDiv.setAttribute("style", "display:none");
_LC.hDiv = createElement("template");
_LC.hDiv.setAttribute("id", "keep-alive-div");
// _LC.hDiv.setAttribute("style", "display:none");
_LC.h1Div = createElement("template");
_LC.h1Div.setAttribute("id", "lyte-helper-div");
// _LC.h1Div.setAttribute("style", "display:none");

// ComponentRegistry.Compile.componentsDiv = 
_LC.lyteComponentsDiv = createElement("div");
_LC.lyteComponentsDiv.setAttribute("id", "lyte-components-div");
_LC.setComponentsDiv(_LC.lyteComponentsDiv,ComponentRegistry.name);


_LC.dummyLyteComponentsDiv = document.createElement("div");
_LC.dummyLyteComponentsDiv.setAttribute("id", "dummy-lyte-components-div");
_LC.setComponentsDiv(_LC.dummyLyteComponentsDiv,ComponentRegistry.name);

_LC.hiddenComponentsDiv=createElement("template");
_LC.hiddenComponentsDiv.setAttribute("id","hidden-component-div");

_LC.hiddenYieldsDiv=createElement("template");
_LC.hiddenYieldsDiv.setAttribute("id","hidden-yields-div");

// if(!Lyte._ie) {
if (document.readyState === "complete" || document.readyState === "interactive") {     
    document.body.appendChild(_LC.dummyLyteComponentsDiv);  
    document.head.appendChild(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv);
    document.head.appendChild(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv);
}else{
    document.addEventListener("DOMContentLoaded", function(){
        document.body.appendChild(_LC.dummyLyteComponentsDiv);  
        document.head.appendChild(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.assetsDiv);
        document.head.appendChild(_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.$.shadowDiv);
    },true);
}
// }

Set.prototype.toArrayLyte = function() {
    if(this.constructor.name === "Set"){
        return Array.from(this);
    }
    else{
        return Array.from(this._values);
    }
}
//_LC.registerListener(function() {
//  
//});

if(document.readyState === "complete" || document.readyState === "interactive") {
    onDomContentForLyte();
} else {
    document.addEventListener("DOMContentLoaded", function(e){
        onDomContentForLyte();
    },true);
}
_LC.mappy = {
    t: "type",
    p: "position",
    dN: "dynamicNodes",
    c: "cases",
    d: "default",
    a: "attr",
    f: "for",
    fI: "for-in",
    e: "if",
    s: "switch",
    i: "insertYield",
    r: "registerYield",
    cM: "component",
    cD: "componentDynamic",
    a: "attr",
    tX: "text"
    // dcn - dynamicCaseName
    // cn - caseName
    // hd - hasDynamicCase
    // hc - hasChildTrans
    // in - indNew
    // l-c - lyte-convertion
    // lc-id - lyte-case-id
    // lc_id - lyte_case_id
    // cdp - casesDeepNodes
    // co - casesOrder
    // dc - _dChild
}

function createSvgDepth(actualTemplate,type){
    var svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    for(var s=0; s<actualTemplate.childNodes.length; s++){
        svg.appendChild(actualTemplate.childNodes[s].cloneNode(true));
    }
    var depthTemp = document.createElement("template");
    depthTemp.setAttribute("is", type);
    for(var s=0; s<svg.childNodes.length; s++){
        depthTemp.content.appendChild(svg.childNodes[s]);
        s--;
    }
    return depthTemp;
}
function createDepth(actualTemplate,type){
    var depthTemp = document.createElement("template");
    depthTemp.setAttribute("is", type);
    depthTemp.innerHTML = actualTemplate.innerHTML;
    return depthTemp;
}
function appendDepth(depthTemp,constr,componentClass){
    constr.splitTextNodes(depthTemp);
    componentClass._depthTemp.content.appendChild(depthTemp);
}
function setHT(dynN,info,dynNewCompile,constr){
    var flag  = true;
    if(dynN.tagName == "TEMPLATE") {
        info._ht = dynN.innerHTML;
        flag = false;
    }
    if(info._ht) {
        if(flag){
            dynN.innerHTML = info._ht;
            constr.splitTextNodes(dynN);
        }
        if(dynNewCompile && dynNewCompile.innerHTML !== info._ht) {
            dynNewCompile.innerHTML = info._ht;
            constr.splitTextNodes(dynNewCompile);
        }
    }
}
function* doCompileHandling(dynamicN, dynamicNodes, componentName, constr, newCompile, componentClass, fastRenderClass, j, Q, callback, qInd, nestedQ, postQ, c_dynN) {
    let info = dynamicNodes[j], type = info.t, pos = info.p, helperInfo;
    let dynN = getDynamicNode(dynamicN,pos);
    let dynNewCompile;
    if(newCompile && fastRenderClass) {
        dynNewCompile = fastRenderClass.getDynNewCompile(newCompile,pos);
    }
    switch(type) {
    case "tX" : {
        dynN.nodeValue = _LC.deep.replace(dynN.nodeValue);
        var syn = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().syntaxCheckWorkerNew(dynN.nodeValue);
        let mustache = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getMustache(dynN.nodeValue,syn);
        if(mustache){
            mustache = _LC.deep.replaceBack(mustache);
            actObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getArray(mustache);  
        }
        if(!actObj && mustache){
            actObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getHelper(mustache);  
        }
        dynN.nodeValue = _LC.deep.replaceBack(dynN.nodeValue);
        let dynamic = mustache;
        if(actObj){
            info.helperInfo = actObj;
            dynNewCompile && fastRenderClass.caseTx(dynNewCompile,mustache,info,j);
       }
        else if(dynamic){
            //deepNodes.$push({type: "text", position:deepN.slice(), dynamicValue: dynamic});
            info.dynamicValue = dynamic;
            info.newDynamicValue = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getDV(dynamic);
            if(dynNewCompile) {
                fastRenderClass.replaceWithPf(dynNewCompile,j)
            }
//              LN to do
//              deepNodes.$push({type: "text", position:deepN.slice(), dynamicValue: getDV(dynamic)});                    
        }
    }
    break;
    case "i" : {
        dynNewCompile && fastRenderClass.caseI(dynNewCompile,info,j);
    }
    break;
    case "cD" : {
        dynNewCompile && fastRenderClass.caseCD(dynNewCompile,info,j);
    }
    break;
    case "a" : {
        let add = false, toBeRemoved = [],toBeAdded = [];
        let node = dynN;
        let attr = info.a = info.a || {};
        for(let i=0;i<node.attributes.length;i++) {
            if(node.attributes[i].nodeValue.indexOf("{{") !== -1) {
                node.attributes[i].nodeValue = _LC.deep.replace(node.attributes[i].nodeValue);
                var val = node.attributes[i].nodeValue;
                var syn = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().syntaxCheckWorkerNew(val);
                var actObj,actValue,multipleAttr=false;
                var splittedMus = val.split("{{");
                var splittedMusLen = splittedMus.length;
                if(syn.mustache >0){
                    splittedMusLen = splittedMusLen - syn.mustache;
                }
                if((splittedMusLen > 2 || !/^{{/.test(val) || !/}}$/.test(val)) && /{{.*}}/.test(val) && !/\\{{.*}}/.test(val)){
                    actObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().splitMixedText(val);
                    multipleAttr = true;
                }
                else{
                    actValue = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getMustache(val,syn);
                    if(actValue){
                        actValue = _LC.deep.replaceBack(actValue);
                        actObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getArray(actValue);  
                    }
                    if(!actObj && actValue){
                        actObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getHelper(actValue);  
                    }
                }
                node.attributes[i].nodeValue = _LC.deep.replace(node.attributes[i].nodeValue);
                if( actObj && (actObj.name === "action" || actObj.name === "method") && /^(onfocus|onfocusin|onfocusout|onresize|onscroll|onclick|ondblclick|onmousedown|onmouseup|onmousemove|onmouseover|onmouseout|onchange|onselect|onsubmit|onkeydown|onkeypress|onkeyup|oncontextmenu|__focus|__focusin|__focusout|__resize|__scroll|__click|__dblclick|__mousedown|__mouseup|__mousemove|__mouseover|__mouseout|__change|__select|__submit|__keydown|__keypress|__keyup|__contextmenu)$/.test(node.attributes[i].name)){
                        var newActObj;
                        if(actValue){
                            newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getArray(actValue);  
                        }
                        if(!newActObj && actValue){
                            newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getHelper(actValue,true);  
                        }
                        attr[node.attributes[i].name.substr(2)] = {
                            name:node.attributes[i].name.substr(2),
                            camelCase : _LC.String.toCamelCase(node.attributes[i].name.substr(2)),
                            helperInfo: actObj,
                            newHelperInfo : newActObj,
                            globalEvent: true
                        };
                        let actArgs = (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.deepCopyObject)(actObj.args);
                        let actName = actArgs.$splice(0,1)[0];
                        actName = actName.startsWith("'")? actName.replace(/'/g,''):  actName;
                        let actString = getArgString(actName, actArgs);
                        node.setAttribute(node.attributes[i].name.substr(2),componentName+" => "+ actString);
                        if(dynNewCompile) {
                            fastRenderClass.setAttribute(dynNewCompile, node.attributes[i].name.substr(2),componentName+" => "+ actString)
                        }
                        toBeRemoved.$push(node.attributes[i].name);                            
                }
                else{
                    if(actObj || actValue) {
                        let attrToPush = {};
                        if(node.attributes[i].name.startsWith("lbind:")) {
                            toBeRemoved.$push(node.attributes[i].name);
                            toBeAdded.$push({"name" : node.attributes[i].name.substring(6), "value": node.attributes[i].nodeValue});
                            attrToPush.isLbind = true;
                            attrToPush.name = node.attributes[i].name.substring(6);
                            attrToPush.camelCase = _LC.String.toCamelCase(attrToPush.name);
                        }
                        else {
                            attrToPush.name = node.attributes[i].name;
                            attrToPush.camelCase = _LC.String.toCamelCase(attrToPush.name);
                        }
                        if(actObj) {
                            if(actObj.name === "lbind") {
                                attrToPush.dynamicValue = actObj.args[0];
                                attrToPush.newDynamicValue = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getDV(actObj.args[0]);
                                attrToPush.isLbind = true;
                            }
                            else {
                                attrToPush.helperInfo = actObj;
                                var newActObj;
                                if(multipleAttr){
                                    newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().splitMixedText(val);
                                }
                                else{
                                    if(actValue){
                                        newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getArray(actValue);  
                                    }
                                    if(!newActObj && actValue){
                                        newActObj = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getHelper(actValue,true);  
                                    }
                                }
                                attrToPush.newHelperInfo = newActObj;
                            }
                        } 
                        else {
                            attrToPush.dynamicValue = actValue;
//                              LN to do
                            attrToPush.newDynamicValue = _compiler_cli_lyte_base_compile_js__WEBPACK_IMPORTED_MODULE_5___default().getDV(actValue);
                        }
                        add = true;
                        attr[attrToPush.name] = attrToPush;
                    }                  
                }
            }
            if(node.attributes[i].name.startsWith("@")){
                let specialAttr;
                add = true;
                node._special = true;
                let attrToPush = {};
                attrToPush.name = node.attributes[i].name;
                if(attr[attrToPush.name] && attr[attrToPush.name].dynamicValue){
                    attrToPush.dynamicValue = attr[attrToPush.name].dynamicValue;
                }else if(attr[attrToPush.name] && attr[attrToPush.name].helperInfo){
                    attrToPush.helperInfo = attr[attrToPush.name].helperInfo;
                }else{
                    attrToPush.stringValue = node.attributes[i].nodeValue;
                    let ndName = node.attributes[i].nodeName
                    toBeRemoved.push(ndName);
                    toBeAdded.push({name : "lyte-directive-" + ndName.slice(1,ndName.length), value : ""});
                }
                attr[attrToPush.name] = attrToPush;
                attrToPush.hookNode  = true;
                let hookName = node.attributes[i].name.slice(1,node.attributes[i].name.length);
                attrToPush.hookName = hookName;
                specialAttr = true;
            }
        }
        if(toBeRemoved.length){
            for(let i=0; i<toBeRemoved.length;i++){
                node.removeAttribute(toBeRemoved[i]);
            }
        }
        if(dynNewCompile) {
            fastRenderClass.removeAttributeArr(dynNewCompile, toBeRemoved);
            fastRenderClass.removeAttributeObj(dynNewCompile, attr, j)
        }
        if(toBeAdded.length) {
            for(let i=0;i<toBeAdded.length;i++) {
                node.setAttribute(toBeAdded[i].name, toBeAdded[i].value);
            }
        }
    } 
    break;
    case "f" : 
    case "fI" : 
    case "r" : 
        setHT(dynN,info,dynNewCompile,constr);
    case "eM":    
    case "cM" : {
        let actualTemplate = dynN.content;
        if(!dynNewCompile && (type == "r" || dynN.hasAttribute("unbound"))) { //af check
            if(fastRenderClass){
                dynNewCompile = fastRenderClass.cMBefore(dynN, info);
            }
        }
        let depthTemp;
        let dnNode;
        let actualTemplateNewCompile = dynNewCompile ? fastRenderClass.cMBefore2(dynNewCompile, info, constr) : undefined; 
        if(info.actualTemplate) {
            actualTemplate = _LC.getContentForIE(info.actualTemplate, constr, undefined, _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._ms? info : undefined);
        }
        let _lastInd = false;
        if(dynN.hasAttribute("has-child")){
            let casesArr = Object.keys(info.c);
            if(casesArr.length){
                casesArr.forEach(function(key){
                    let contentNewCompile;
                    var content = _LC.getContentForIE(actualTemplate.querySelector("[case='"+_LC.cssEscape(key)+ "']"), undefined, true);
                    setHT(content,info.c[key],contentNewCompile,constr);
                    var casesDn = info.c[key].dN;
                    if(casesDn.length){
                        _lastInd = key;
                        for(let i =0; i<casesDn.length; i++){
                            if(i == casesDn.length-1){
                                nestedQ.push( { dynN: dynN,  __case__:"1_1", __from__: qInd,__isInternal__:true, arguments:[content.tagName === "TEMPLATE" ? content.content : content, info.c[key].dN,componentName, constr, contentNewCompile, componentClass, fastRenderClass], __idx__: i, __callback__: (dynN) => {
                                    if(info.c[key]._ht) {
                                        info.c[key]._ht = content.cloneNode(true);
                                        componentClass._helperTemplate.content.append(info.c[key]._ht)
                                        content.innerHTML = "";
                                        postQ.push(content);
                                    }
                                    if(_lastInd === key){
                                        postForCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile, actualTemplateNewCompile, actualTemplate,componentClass, fastRenderClass);
                                    }
                                }});
                            }
                            else{
                                nestedQ.push( { dynN: dynN,  __case__:"1_2", __from__: qInd,__isInternal__:true, arguments:[content.tagName === "TEMPLATE" ? content.content : content, info.c[key].dN,componentName, constr, contentNewCompile, componentClass, fastRenderClass], __idx__: i});
                            }
                        };
                    }
                    else{
                        postSwitchCaseCompile(info, constr, j, key, contentNewCompile, content, actualTemplateNewCompile,componentClass);  
                    }
                });
            }
            else{
                
                if(dynNewCompile) {
                    info._args = {};
                    if(type === "registerYield") {
                        _LC.replaceWithPf(dynNewCompile, document.createTextNode("__**--Lyte"+j+"__**"));
                    } else if(type === "component") {
                        _LC.replaceWithPf(dynNewCompile, document.createTextNode((dynNewCompile.hasAttribute("_lyteattr") ? dynNewCompile.getAttribute("_lyteattr"): "" )+ "__**--Lyte"+j+"__**"));
                    } else {
                        //dynNewCompile.replaceWith(document.createTextNode("__**--Lyte"+j+"__**"))
                        _LC.replaceWithPf(dynNewCompile, document.createTextNode("__**--Lyte"+j+"__**"));
                    }
                    Array.from(dynNewCompile.attributes).forEach(function(item) { //eslint-disable-line no-loop-func
                        info._args[item.nodeName] = item.nodeValue;
                    });
                }
                if(info.actualTemplate) {
                    depthTemp = type;
                        if(info.svg){
                            depthTemp = createSvgDepth(actualTemplate,type)
                        }else{
                            depthTemp = createDepth(actualTemplate,type)
                        }
                        appendDepth(depthTemp, constr, componentClass);
                    if(dynamicNodes[j-1] && (dynamicNodes[j-1].p.toString() === dynamicNodes[j].p.toString())) {
                        dnNode = dynamicNodes[j-1];
                    } else {
                        dnNode = dynamicNodes[j];
                    }
                    dnNode._depthTemp = depthTemp;
                }
                if(info._ht) {
                    info._ht = dynN.cloneNode(true);
                    componentClass._helperTemplate.content.append(info._ht)
                    dynN.innerHTML = "";
                }
            }
        }else{
            var dN = info.dN;
            if(dN.length){
                _lastInd = true;
                for(let i =0; i<dN.length; i++){
                    if(i == dN.length-1){
                        nestedQ.push( { dynN: dynN,  /*__case__:"2_1", __from__: qInd,*/__isInternal__:true, arguments:[actualTemplate, info.dN, componentName, constr, actualTemplateNewCompile, componentClass, fastRenderClass], __idx__: i, __callback__:(dynN) => {
                            postForCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile, actualTemplateNewCompile, actualTemplate,componentClass, fastRenderClass);
                        }});
                    }
                    else{
                        nestedQ.push( { dynN: dynN, /*__case__:"2_2", __from__: qInd,*/__isInternal__:true, arguments:[actualTemplate, info.dN, componentName, constr, actualTemplateNewCompile, componentClass, fastRenderClass], __idx__: i});
                    }
                };
            }   
        }
        if(_lastInd == false){
            postForCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile, actualTemplateNewCompile, actualTemplate, componentClass, fastRenderClass);
        }
    }
    break;
    case "e" : 
    case "s" : {
        
        setHT(dynN,info,dynNewCompile,constr);
        let depthTemp;
        let actualTemplateNewCompile;
        if(info.actualTemplate) {
            dynN = _LC.getContentForIE(info.actualTemplate, constr, undefined);
        }
        if(dynNewCompile){
            actualTemplateNewCompile = fastRenderClass.caseES(dynNewCompile, info, constr);
        }
        var asyncCompile = false, def = "default";
            var dynNContent = dynN.content || dynN,  casesArr = Object.keys(info.c), _lastInd = false;
            if(info.d.dN) {
                let contentNewCompile;
                if(actualTemplateNewCompile) {
                    contentNewCompile = _LC.getContentForIE(actualTemplateNewCompile.querySelector("[default]"));
                }
                let content = _LC.getContentForIE(dynNContent.querySelector("[default]"), undefined, true);
                setHT(content,info.d,contentNewCompile,constr);
                let dN = info.d.dN;
                if(dN && dN.length){
                    for(let i =0; i<dN.length; i++){
                        if(i == dN.length-1){
                            nestedQ.push( { dynN: dynN,  /*__case__:"7_1", __from__: qInd,*/ __isInternal__:true, arguments:[content.tagName === "TEMPLATE" ? content.content : content, info.d.dN,componentName, constr, contentNewCompile, componentClass, fastRenderClass], __idx__: i, __callback__:(dynN) => {
                                postSwitchDefCompile(info, constr, contentNewCompile, content, actualTemplateNewCompile, def,componentClass);                                                           
                            }});
                        }
                        else{
                            nestedQ.push( { dynN: dynN,  /*__case__:"7_2", __from__: qInd,*/ __isInternal__:true, arguments:[content.tagName === "TEMPLATE" ? content.content : content, info.d.dN,componentName, constr, contentNewCompile, componentClass, fastRenderClass], __idx__: i});
                        }
                    }
                }
                else{
                    postSwitchDefCompile(info, constr, contentNewCompile, content, actualTemplateNewCompile, def,componentClass); 
                }
            }
            if(casesArr.length){
                casesArr.forEach(function(key){
                   let caseStr = info.c[key].dcn ? "[lc-id='" : "[case='";
                    let contentNewCompile;
                    if(actualTemplateNewCompile) {
                        contentNewCompile = _LC.getContentForIE(actualTemplateNewCompile.querySelector(caseStr+_LC.cssEscape(key)+ "']"));
                    }
                    var content = _LC.getContentForIE(dynNContent.querySelector(caseStr+_LC.cssEscape(key)+ "']"), undefined, true);
                    setHT(content,info.c[key],contentNewCompile,constr);
                    let dN = info.c[key].dN;
                    if(info.c[key].dcn){
                        let ndN=[info.c[key].cdp];
                        if(ndN && ndN.length){
                            asyncCompile = true;
                            _lastInd = key;
                            for(let i =0; i<ndN.length; i++){
                                if(i == ndN.length-1){
                                    nestedQ.push( { dynN: dynN, /*__case__:"5_1", __from__: qInd,*/ __isInternal__:true, arguments:[dynNContent, [info.c[key].cdp], componentName, constr, undefined,componentClass, fastRenderClass], __idx__: i, __callback__:(dynN) => {
                                        if(!dN.length){
                                            // var content = _LC.getContentForIE(dynNContent.querySelector(caseStr+_LC.cssEscape(key)+ "']"), undefined, true);
                                            // postSwitchCaseCompile(info, constr, j, key, contentNewCompile, content, actualTemplateNewCompile,componentClass);
                                            if(_lastInd == key){
                                                postSwitchCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile,componentClass,fastRenderClass);
                                            }
                                        }
                                    }});
                                }
                                else{
                                    nestedQ.push( { dynN: dynN,  /*__case__:"6_2", __from__: qInd,*/ __isInternal__:true, arguments:[dynNContent, [info.c[key].cdp], componentName, constr, contentNewCompile? _LC.getContentForIE(contentNewCompile) : undefined,componentClass, fastRenderClass], __idx__: i});
                                }
                            };
                        }
                    }
                    if(dN && dN.length){
                        asyncCompile = true;
                        _lastInd = key;
                        for(let i =0; i<dN.length; i++){
                            if(i == dN.length-1){
                                nestedQ.push( { dynN: dynN, /*__case__:"5_1", __from__: qInd,*/ __isInternal__:true, arguments:[content.tagName === "TEMPLATE" ? content.content : content, info.c[key].dN, componentName, constr, contentNewCompile? _LC.getContentForIE(contentNewCompile) : undefined,componentClass, fastRenderClass], __idx__: i, __callback__:(dynN) => {
                                    var content = _LC.getContentForIE(dynNContent.querySelector(caseStr+_LC.cssEscape(key)+ "']"), undefined, true);
                                    postSwitchCaseCompile(info, constr, j, key, contentNewCompile, content, actualTemplateNewCompile,componentClass);
                                    if(_lastInd == key){
                                        postSwitchCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile,componentClass,fastRenderClass);
                                    }
                                }});
                            }
                            else{
                                nestedQ.push( { dynN: dynN,  /*__case__:"6_2", __from__: qInd,*/ __isInternal__:true, arguments:[content.tagName === "TEMPLATE" ? content.content : content, info.c[key].dN, componentName, constr, contentNewCompile? _LC.getContentForIE(contentNewCompile) : undefined,componentClass, fastRenderClass], __idx__: i});
                            }
                        };
                    }
                    else{
                        postSwitchCaseCompile(info, constr, j, key, contentNewCompile, content, actualTemplateNewCompile,componentClass);
                    }
                });
            }
        // }
        if(asyncCompile === false){
            postSwitchCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile,componentClass,fastRenderClass);
        }
    }
    break;    
    }
    if(typeof callback == "function"){
        callback(c_dynN);
    }
}

function postForCompile( dynN, info, constr, j, type, dynamicNodes, dynNewCompile, actualTemplateNewCompile, actualTemplate,componentClass, fastRenderClass) {
    let depthTemp;
    let dnNode;
    if(actualTemplateNewCompile) {
        let staticTemp = info.actualTemplate ? actualTemplateNewCompile : dynNewCompile;
        info._sta = _LC.processStatic(staticTemp);                      
    }
    if(dynNewCompile){
        fastRenderClass.cMAfter(actualTemplateNewCompile, info, type, j, dynNewCompile);
    }
    if(info.actualTemplate) {
        depthTemp = type;
            if(info.svg){
                depthTemp = createSvgDepth(actualTemplate,type)
            }else{
                depthTemp = createDepth(actualTemplate,type)
            }
            appendDepth(depthTemp,constr,componentClass);
        
        if(dynamicNodes[j-1] && (dynamicNodes[j-1].p.toString() === dynamicNodes[j].p.toString())) {
            dnNode = dynamicNodes[j-1];
        } else {
            dnNode = dynamicNodes[j];
        }
        dnNode._depthTemp = depthTemp;
    }
    if(info._ht) {
        info._ht = dynN.cloneNode(true);
        componentClass._helperTemplate.content.append(info._ht)
        dynN.innerHTML = "";
    }
}
function postSwitchDefCompile(info, constr, contentNewCompile, content, actualTemplateNewCompile, def,componentClass) {
    if(actualTemplateNewCompile) {
        info.d._sta = _LC.processStatic(contentNewCompile);
    }
    if(info.d._ht) {
        info.d._ht = content.cloneNode(true);
        componentClass._helperTemplate.content.append(info.d._ht)
        content.innerHTML = "";
    }  
}
function postSwitchCaseCompile(info, constr, j, key, contentNewCompile, content, actualTemplateNewCompile,componentClass) {
    if(actualTemplateNewCompile) {
        info.c[key]._sta = _LC.processStatic(contentNewCompile);
    }
    if(info.c[key]._ht) {
        info.c[key]._ht = content.cloneNode(true);
        componentClass._helperTemplate.content.append(info.c[key]._ht)
        content.innerHTML = "";
    }
}
function postSwitchCompile(dynN, info, constr, j, type, dynamicNodes, dynNewCompile,componentClass,fastRenderClass) {   
    let depthTemp;
    if(info.actualTemplate) {
        depthTemp = type;
            if(info.svg){
                depthTemp = createSvgDepth(dynN.content,type)
            }else{
                depthTemp = createDepth(dynN,type)
            }
            appendDepth(depthTemp,constr,componentClass);
        if(dynamicNodes[j-1] && (dynamicNodes[j-1].p.toString() === dynamicNodes[j].p.toString())) {
            dynamicNodes[j-1]._depthTemp = depthTemp;
        } else {
            dynamicNodes[j]._depthTemp = depthTemp;
        }
    }
    if(dynNewCompile) {
        fastRenderClass.replaceWithPf(dynNewCompile,j);
    }
    if(info._ht) {
        info._ht = dynN.cloneNode(true);
        componentClass._helperTemplate.content.append(info._ht);
        dynN.innerHTML = "";
    } 
}
function* doCompile(dynamicN, dynamicNodes, componentName, constr, newCompile, componentClass, fastRenderClass) {
    let lastUsedAttrPosition ;
    if(dynamicNodes && dynamicNodes.length){
        let Q = Array.from(dynamicNodes), intCount = 0, postQ = [];
        for(let j=0;j<Q.length;j++) {
            let qItem=Q[j],nestedQ=[];
            if(Q[j].__isInternal__){
                intCount++;
                if(qItem.arguments && qItem.arguments[1] && qItem.arguments[1].length){
                    var argArr = [...qItem.arguments, ...[qItem.__idx__, Q, qItem.__callback__, j, nestedQ, postQ, qItem.dynN]];
                    yield* doCompileHandling(...argArr);
                }else if(qItem.callback && typeof qItem.callback == "function"){
                    qItem.callback(qItem.dynN);
                }
            }else{
                yield* doCompileHandling(dynamicN, dynamicNodes, componentName, constr, newCompile,componentClass,fastRenderClass, j-intCount, Q, undefined, j, nestedQ, postQ, qItem.dynN)
            }
            if(nestedQ.length){
                let newQ = [];
                nestedQ.forEach((item) => {
                    if(item.__callback__){
                        var nObj = {callback: item.__callback__, dynN: item.dynN, __isInternal__: true};
                        delete item.__callback__;
                        delete item.dynN;
                        newQ.push(item);
                        newQ.push(nObj);
                    }
                    else{
                        newQ.push(item);
                    }
                });
                Q.splice(j+1, 0, ...newQ);
            }
        }
    }
    else{
        yield "";
    }
}

//This is the function where the actual rendering takes place. 
//It takes the template, finds the actual dynamic nodes uwing dynamicNodes argument and then binds each node with the associated
//property by calling bindNode. 

function getDynamicNode(content, positions){
    let dynamicN = content;
    for(var i=0; i<positions.length; i++){
        dynamicN = (dynamicN.tagName != "TEMPLATE") ? dynamicN.childNodes[positions[i]] : dynamicN.content.childNodes[positions[i]];
    }
    return dynamicN;
}

function getArgString(name, array) {
    let retString;
    for(let i=0;i<array.length;i++) {
        if(array[i] && typeof array[i] === "object") {
            array[i] = getArgString(array[i].value.name, array[i].value.args);
        }
    }
    if(name) {
        retString = name +  "(" + array.toString() + ")";
    } else {
        retString = array.toString();
    }
    return retString;
}

var defHelpers = ComponentRegistry._defaultHelpers;
defHelpers["unbound"] = function(value){
     return value;
}
defHelpers["action"] = function(parentNode,attrName,isCustom,actObj){
    if(isCustom){
        parentNode._actions = parentNode._actions? parentNode._actions : {};
        if(!parentNode._actions[attrName]){
            this.$node.createCustomEvent(attrName, parentNode, actObj); 
            parentNode.removeAttribute(attrName);
        }
    }
    else{
        
        this.$node.createEventListeners(parentNode,attrName,actObj);    
    }
};

defHelpers["lbind"] = function(name){
	return this.getData(name);
};

defHelpers["method"] = function(parentComponent, attributeNode, functionName) {
    var parentComponent = arguments[0];
    var attributeNode = arguments[1];
    var functionName = arguments[2];
    var self = arguments[0].component;
    var childComponent = attributeNode? attributeNode.ownerElement : null;
    var attributeName = arguments[1].nodeName;
    attributeNode = null;
    var args = Array.prototype.slice.call(arguments, 2);
    var newFunc = function() {
        let node = this.$node;
        let contextSwitchArray = [];
        _LC.adCx(node, contextSwitchArray);
        let processDetails = {};
        let processedArgs = this.$node.processArgs(this.$node._callee,{"helperInfo" : {"args" : args}}, [], [], undefined, this.$node, undefined, undefined, undefined, processDetails);
        let functionName1 = processedArgs.$splice(0,1)[0];
        _LC.rmCx(node, contextSwitchArray);
        let customArgs = Array.from(arguments);
        let mainArgs = processedArgs.$concat(customArgs);
        if(self._methods[functionName1]) {
            var appScope = _LC.getNearestParentApp(this);
           //@Slicer.developmentStart
           _LC.debugPerf(
               appScope,
               node.__renderId,
               "Method "+functionName1,
               self.constructor._compName
           );
           //@Slicer.developmentEnd
           //@Slicer.catchAllErrorsStart
           try{
            //@Slicer.catchAllErrorsEnd 
            var _meth = self._methods[functionName1].apply(self, mainArgs);
            //@Slicer.developmentStart
            _LC.debugPerf(
                appScope,
                node.__renderId,
                "Method "+functionName1,
                self.constructor._compName
            );
            //@Slicer.developmentEnd
            return _meth;
            //@Slicer.catchAllErrorsStart
           }catch(err){
            appScope.appError ?  appScope.appError.error(self,err):_utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(self,err);
           }
           //@Slicer.catchAllErrorsEnd
        }
        //@Slicer.developmentStart
        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error(self, "LC005", functionName, self.$node.tagName);
        //@Slicer.developmentEnd
    }
    if(childComponent) {
        if(!childComponent.set) {
            childComponent.setMethods(_LC.String.toCamelCase(attributeName), newFunc);
        } else {
            childComponent.component._methods[_LC.String.toCamelCase(attributeName)] = newFunc;
        }
    } else {
        return newFunc;
    }
};
defHelpers["stringifyHTML"] = function(){// unescapeAttr renderString printHTML showString showHTML returnHTML stringifyHTML
    defHelpers.unescape.apply(this,arguments).innerHTML;
};

defHelpers["unescapeAttr"] = function(){
    return defHelpers.unescape.apply(this,arguments).innerHTML;
}
//@Slicer.$bundlePurifierStart
defHelpers["unescape"] = function(value, additionalObject, userInstance, options) {
    if(_LC.ffr) {
        return value;
    }
    let divEle = document.createElement("div");
    if(options && !options.force && options.ignoreSanitizer){
        divEle.innerHTML = value;
        return divEle;
    }
    else{
        if(additionalObject && Object.keys(additionalObject) && Object.keys(additionalObject).length > 0){
            _utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_6__.Security.initializeConfig(additionalObject);
            _utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_6__.Security.removeConfig(additionalObject);
            _utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_6__.Security.addLyteComponents(additionalObject);
        }
        let clean;
        let ins = userInstance && Object.keys(userInstance).length ? userInstance._sanitzer : this.$registry._ourSanitizerInstance_._sanitzer;
        if(additionalObject && Object.keys(additionalObject).length){
            clean = _utils_lyte_sanitizer_js__WEBPACK_IMPORTED_MODULE_6__.Security.sanitizeWithConfig(value ,additionalObject, ins);
        }else{
            clean = ins.sanitize(value);
        }
        divEle.innerHTML = clean;
        return divEle;
    }
};
//@Slicer.$bundlePurifierEnd


defHelpers["debugger"] = function() {
    debugger;
};

defHelpers["log"] = function() {
    console.log.apply(window, Array.from(arguments));
};

defHelpers["ifEquals"] = function(arg1, arg2) {
    if(arg1 === arg2) {
        return true;
    } else {
        return false;
    }
};

defHelpers["if"] = function(value, trueValue, falseValue) {
    if(value) {
        return trueValue;
    } else {
        return falseValue;
    }
};

defHelpers["negate"] = function(arg1) {
    return !arg1;
};


defHelpers["ifNotEquals"] = function(arg1, arg2) {
    if(arg1 === arg2) {
        return false;
    } else {
        return true;
    }
};


defHelpers["slyteImport"] = function(src, basePath){
    return (0,_core_src_lyte_utils_js__WEBPACK_IMPORTED_MODULE_4__.slyteImport)(src,basePath || (this.$app && this.$app.staticPath));
}

defHelpers['concat'] = function(){
	var resp = '';
	var argLength = arguments.length;
	for(var i=0;i<argLength;i++){
		if(arguments[i] != undefined){
			resp += arguments[i];
		}
	}
	return resp;
};

defHelpers['toString'] = function(obj){
    if(obj === null || obj === undefined){
        return "";
    }
    else if(typeof obj == "object"){
        return JSON.stringify(obj);
    }
    return obj;
};

defProp(HTMLElement.prototype, 'setData', {
    configurable : true, 
    writable : true,
    value : function(arg0, arg1) {
        this._initProperties = this._initProperties || {};
        if(typeof arg0 === "string") {
            this._initProperties[arg0] = arg1
        } else if(typeof arg0 === "object") {
            for(let key in arg0) {
                this._initProperties[key] = arg0[key];
            }
        }
    }
});

_core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.Compatibility.vue = {
    yieldDisconnectedCallback : function(comp , callee){
        let yieldName = comp.getAttribute("yield-name");
        if( callee && callee._dynYields && callee._dynYields[yieldName]){
            let dynamicToAppend = callee._dynYields[yieldName];
            dynamicToAppend.apply(comp, [comp, comp.getData(),  { "remove" : true }]);
        }
    },
    updateYield : function(comp, node){
        if(node._callee._dynYields){
            let dynamicToAppend = node._callee._dynYields[_LC.getYieldName(node)];
            if(dynamicToAppend){
                dynamicToAppend.apply(comp, [node, node.component.data,  { "add" : true }]);
            }
        }
    },
    addDynamicYield : function(currentComp, parentComp, yieldName){
        if(parentComp._dynYields && parentComp._dynYields[yieldName]){
            if(!currentComp._dynYields){
                currentComp._dynYields = {};
            }
            currentComp._dynYields[yieldName] = parentComp._dynYields[yieldName];
        }
    },
    renderYield : function(_lyteOptions, component){
        if(_lyteOptions && _lyteOptions.registerYield && _lyteOptions.registerYield.length){
            // _lyteOptions.registerYield.forEach(function(obj){
            //     let yieldName = obj.yieldName;
            //     let callBack =  obj.callback;
            //     component.registerYield.apply(component,[yieldName, callBack]);
            // });
            component.registerYield.apply(component, [_lyteOptions.registerYield]);
            component.__delayDidConnect = true;
            return true;
        }
    },
    delayDidConnect : function(comp, callBack){
        setTimeout(function(){
            callBack.apply(comp, ["didConnect"]);
        },1)
    }
}
window.__lyteConfig.render = _LC.render;
defProp(HTMLElement.prototype, 'registerYield', {
    configurable : true, 
    writable : true,
    value : function(arr) {
        var self = this;
        arr.forEach(function(obj){
            self._dynYields = self._dynYields || {};
            self._dynYields[obj.yieldName] = obj.callback;
        })
    }
});

defProp(HTMLElement.prototype, 'addAction', {
    configurable : true, 
    writable : true,
    value : function() {
        let args = Array.from(arguments);
        _LC.addAction.apply(this,[this].concat(args));
    }
});

defProp(HTMLElement.prototype, 'setMethods', {
    configurable : true, 
    writable : true,
    enumerable: false, 
    value : function(arg0, arg1) {
        this._initMethods = this._initMethods || {};
        if(typeof arg0 === "string") {
            this._initMethods[arg0] = arg1
        } else if(typeof arg0 === "object") {
            for(let key in arg0) {
                this._initMethods[key] = arg0[key];
            }
        }
    }
});

defHelpers['encAttr'] = function(val) {
    return Encoder.encodeForHTMLAttribute(encodeURIComponent(val));
}

defHelpers['expHandlers'] = function(leftOperand,operator,rightOperand,nextOperand){
    var argLen = arguments.length;
    if(operator == '++' ){
    	if(rightOperand == "postfix"){
    	   return (leftOperand++);
    	} else if(rightOperand == "prefix"){
    		return (++leftOperand);
    	}
    } else if(operator == "--"){
    	if(rightOperand == "postfix"){
    	   return (leftOperand--);
    	} else if(rightOperand == "prefix") {
    		return (--leftOperand);
    	}
    } else if((operator == "==")){
    	return leftOperand == rightOperand;
    }
     else if((operator == "===")){
    	return leftOperand === rightOperand;
    }
    
    else if((operator == "!=")) {
        return leftOperand != rightOperand;
    }
    else if((operator =="!==")) {
        return leftOperand !== rightOperand;
    }
    else if( operator == "&&") {
    	return leftOperand && rightOperand;
    } else if(operator == "||") {
    	return leftOperand || rightOperand;
    } else if(operator == "+"){
        if(argLen > 2){
        	return leftOperand+rightOperand;
        }
        return leftOperand;
    } else if(operator == '-'){
         if(argLen > 2){
        	return leftOperand-rightOperand;
        }
        return (-leftOperand);
    } else if(operator == '*'){
    	return leftOperand * rightOperand;
    } else if(operator == "/"){
    	return leftOperand / rightOperand;
    } else if(operator == "%"){
    	return leftOperand % rightOperand;
    } else if(operator == "<"){
    	return leftOperand < rightOperand;
    } else if(operator == ">") {
    	return leftOperand > rightOperand;
    } else if(operator == "<=") {
    	return leftOperand <= rightOperand;
    } else if(operator == ">=") {
    	return leftOperand >= rightOperand;
    } else if(operator == '|') {
        return leftOperand | rightOperand;
    } else if(operator == '&') {
        return leftOperand & rightOperand;
    }
    else if(operator == "!" ){
        return (!leftOperand);
    } else if(operator == '=') {
        leftOperand = rightOperand;
        return leftOperand;
    } else if(operator == "+=") {
        return leftOperand += rightOperand;
    } else if(operator == '-=') {
        return leftOperand -= rightOperand;
    } else if(operator == "*=") {
        return leftOperand *= rightOperand;
    } else if(operator == '/=') {
        return leftOperand /= rightOperand;
    } else if(operator == '?:') {
        return (leftOperand ? rightOperand : nextOperand);
    }
 };

ComponentRegistry.registerCustomPropHandler("ltProp");

_LC.shouldIgnoreDisconnect = function() {
    return _LC.ignoreDisconnect || _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.ignoreDisconnect || ltCf.ignoreDisconnect;
}
_LC.setIgnoreDisconnect = function(val) {
    _LC.ignoreDisconnect = _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.ignoreDisconnect = ltCf.ignoreDisconnect = val;
}
_LC.setDelayDidConnect = function(val) {
    _LC.delayDidConnect = _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte.delayDidConnect = ltCf.delayDidConnect = val;
}
_LC.getDelayDidConnect = function(val) {
    return _LC.delayDidConnect;
}

_LC.addAction = function(element, eventName, func, context) {
    element._lyteEvents = element._lyteEvents || {};
    element._lyteEvents[eventName] = element._lyteEvents[eventName] || []; 
    var ind = element._lyteEvents[eventName].$push({"func" : func, "fromEventListener" : true, "context" : context});
    return eventName + "-" + ind; 
  }

ComponentRegistry.prototype.removeAction = function(element, listenerId) {
    if(!listenerId) {
        //@Slicer.developmentStart
        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC014")
        //@Slicer.developmentEnd
        return;
    }
    var split = listenerId.split('-');
    var eventName = split[0];
    var index = parseInt(split[1]);
    if(!element._lyteEvents || !element._lyteEvents[split[0]] || isNaN(index)) {
        //@Slicer.developmentStart
        _utils_lyte_errors_js__WEBPACK_IMPORTED_MODULE_2__.ComponentError.error("LC015")
        //@Slicer.developmentEnd
        return;
    }
    element._lyteEvents[split[0]][split[1] - 1] = {};
}


_LC.hasLyteEvents = function(element, eventName) {
    if(element._lyteEvents && element._lyteEvents[eventName]) {
        return true;
    } else {
        return false;
    }
}

_LC.handleLyteEvents = function(element, event) {
    var funcs = element._lyteEvents[event.type];
    var ret;
    var eventStopped;
    for(var i=0;i<funcs.length;i++) {
        if(funcs[i].func) {
            ret = funcs[i].func.call(funcs[i].context ? funcs[i].context : window, event);
            if(ret === false || event.cancelBubble) {
                eventStopped = true;
                break;
            } 
        }
    }
    if(eventStopped) {
        event.stopPropagation();
    }
    return eventStopped;
}
_LC.executeObservers = function(compClass){
    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._preRegister();
    let __observers = compClass.observers();
    _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._postRegister();
    return __observers;
}
_LC.chromeBugFix();
_LC.booleanAttrList = ["async","autocomplete","autofocus","autoplay","border","challenge","checked1","compact","contenteditable","controls","default","defer","disabled","formNoValidate","frameborder","hidden","indeterminate","ismap","loop","multiple","muted","nohref","noresize","noshade","novalidate","nowrap","open","readonly","required","reversed","scoped","scrolling","seamless","selected","sortable","spellcheck","translate"]
_LC.core = {};
_LC.core._constructor = customElementPrototype;
_LC.core._registerComponent = customElementPrototype._registerComponent;
_LC.core.registerComponent = _LC.registerComponent;
_LC.core.executeBlockHelpers = customElementPrototype.prototype.executeBlockHelpers;
_LC.core.updateForHelper = customElementPrototype.prototype.updateForHelper;
_LC.core.updateForInHelper = customElementPrototype.prototype.updateForInHelper;
_LC.core.updateSwitchHelper = customElementPrototype.prototype.updateSwitchHelper;

ComponentRegistry.prototype.set = _LC.set;
ComponentRegistry.prototype.get = _LC.get;
_LC.aF._name = "arrayUtils";
_LC.oF._name = "objectUtils";
_core_src_Utils_js__WEBPACK_IMPORTED_MODULE_10__.Utils.addMethods([_LC.set,_LC.get,_LC.arrayUtils,_LC.objectUtils])
let appendChild = _LC.appendChild;
let insertAfter = _LC.insertAfter;
let insertBeforeFn = _LC.insertBefore
let replaceWith = _LC.replaceWith;
let render = _LC.render;
let nextTick = _LC.nextTick;
var _addLyteAction = _LC.addAction;
let shouldIgnoreDisconnect = _LC.shouldIgnoreDisconnect;
let _setIgnoreDisconnect = _LC.setIgnoreDisconnect;
_LC.globalDOMEvents = globalDOMEvents;
_LC.globalEventHandler = globalEventHandler;
_LC.changeEventhandler = changeEventhandler;
_LC.doCompile = doCompile;
_LC.getDynamicNode = getDynamicNode;
function getNearestApp(node){
    while(node){
      if(node.component){
        return node.component.getApp();
      }
      node = node.parentElement;
    }
    return _core_index_js__WEBPACK_IMPORTED_MODULE_7__.Lyte._getDefaultAppIns();
}



/***/ }),

/***/ 4585556:
/*!********************************************************************!*\
  !*** ./node_modules/@slyte/component/src/utils/custom-elements.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "CustomElements": () => (/* binding */ CustomElements)
/* harmony export */ });
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../core/index.js */ 21046606);

class CustomElements {
    static define(name,customElementClass,options){
      if(options && options.clone){
        var allCallbacks = options.clone.allCallbacks;
        var duringClone,afterClone,allAttributesCallback;
        if(typeof options.clone.attributeChangedCallback == "object"){
          duringClone = options.clone.attributeChangedCallback.duringClone;
          afterClone = options.clone.attributeChangedCallback.afterClone;
        }else{
          allAttributesCallback = options.clone.attributeChangedCallback;
        }
        var connectedCallback = options.clone.connectedCallback;
        var disconnectedCallback = options.clone.disconnectedCallback;
      }
      var actualConnectedCallback = customElementClass.prototype.connectedCallback;
      var actualAttributeChangedCallback = customElementClass.prototype.attributeChangedCallback;
      var actualDisconnectedCallback = customElementClass.prototype.disconnectedCallback;
      var reconnectedCallback = customElementClass.prototype.reconnectedCallback;
      var removedCallback = customElementClass.prototype.removedCallback;

      class LyteCustomElement extends customElementClass {
        constructor(){
          super();
          if(this.isClonedNode()){
            this.__cloned = true;
            let attrList = this.attributes;
            for(let i=0; i<attrList.length; i++){
              let attrNode = attrList[i];
              attrNode._lyte = {cloned : true,userCloned : true, name : attrNode.nodeName ,value : attrNode.nodeValue};
            }
          }
        }
        connectedCallback(){
          if(_core_index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.ignoreDisconnect){
            if(reconnectedCallback){
              this.executeLyteCallbacks(reconnectedCallback,arguments);
            }
            return;
          }
          if(this.isClonedNode() && (allCallbacks == false || connectedCallback == false)){
              return;
          }
          this.executeLyteCallbacks(actualConnectedCallback,arguments);
          this.setAttribute("lyte-rendered-ce", "");
          this.LyteConnected = true;
        }
        attributeChangedCallback(){
          var cloneStatus;
          var clonedNode = this.isClonedNode();
          if(clonedNode){
            if(allCallbacks == false || allAttributesCallback == false){
              return;
            }else{
              let attrName = arguments[0];
              let oldVal = arguments[1];
              let newVal = arguments[2];
              let attrNode = this.attributes[attrName];
              cloneStatus = this.attributes[arguments[0]]._lyte.userCloned;
              let isClonedAttribute = this._isClonedAttribute(attrName,oldVal,newVal,attrNode);
              if(isClonedAttribute){
                this.attributes[arguments[0]]._lyte.userCloned = true;
                if(duringClone == false){
                  return;
                }
              }else{
                this.attributes[arguments[0]]._lyte.userCloned = false;
                if(afterClone == false){
                  return;
                }
              }
            }
          }
          this.executeLyteCallbacks(actualAttributeChangedCallback,arguments);
          if(clonedNode && this.attributes[arguments[0]] && this.attributes[arguments[0]]._lyte){
            this.attributes[arguments[0]]._lyte.userCloned = cloneStatus;
          }
        }
        disconnectedCallback(){
          if(_core_index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.ignoreDisconnect){
            if(removedCallback){
              this.executeLyteCallbacks(removedCallback,arguments);
            }
            return;
          }
          if(this.isClonedNode() && (allCallbacks == false || disconnectedCallback == false)){
            return;
          }
          this.executeLyteCallbacks(actualDisconnectedCallback,arguments);
        }
      }
        
      customElementClass.prototype.executeLyteCallbacks = function(callBack,argArr){
        if(callBack) {
          callBack.apply(this, Array.from(argArr));
        }
      }
      customElementClass.prototype.isClonedNode = function(){
        if(this.hasAttribute("lyte-rendered-ce") && !this.LyteConnected || this.__cloned){
          return true;
        }
        return false;
      }
      customElementClass.prototype._isClonedAttribute = function(attrName,oldVal,newVal){
        let attrNode = this.attributes[attrName];
        if(attrNode._lyte && attrNode._lyte.cloned && attrNode._lyte.name == attrName && attrNode._lyte.value == newVal && oldVal === null){
          return true;
        }
        return false;
      }
      customElementClass.prototype.isClonedAttribute = function(attrName){
        let attrNode = this.attributes[attrName];
        if(attrNode._lyte && attrNode._lyte.userCloned){
          return true;
        }
        return false;
      }
      
      customElements.define(name,LyteCustomElement);

    }
}


/***/ }),

/***/ 22956926:
/*!***********************************************************!*\
  !*** ./node_modules/@slyte/component/src/utils/freeze.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../index.js */ 26591616);

_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.freeze = {};
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.object.freezeData = function(data, prop){
    if(prop.freeze && data && typeof data == "object" && !Object.isFrozen(data)){
        Object.freeze(data);
    }
}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.freeze.prop = function(comp, propName){
    if(comp){
        if(comp.localName == "lyte-yield"){
            comp = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getCallee(comp);
        }
        if(comp && comp.component && comp.component.__data[propName] && comp.component.__data[propName].freeze){
            return true;
        }
    } 
}
_index_js__WEBPACK_IMPORTED_MODULE_0__._LC.freeze.component = function(compClass,data, obj){
    let compOptions = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.map.component.get(compClass);
    if(compOptions && compOptions.freeze){
        // _LC.object.freezeData(data, obj);
        return true;
    }
}
let freeze = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.freeze;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (freeze);

/***/ }),

/***/ 35495860:
/*!*******************************************************************!*\
  !*** ./node_modules/@slyte/component/src/utils/load-component.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "loadComponent": () => (/* binding */ loadComponent)
/* harmony export */ });
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../core/index.js */ 21046606);
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../index.js */ 26591616);


function loadComponent(componentName,registryClass) {
    if(!registryClass){ //  || !registryClass.__lyteOptionsMap.get(registryClass).hash
        registryClass = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.getDefaultRegistry();
    }
    if(typeof componentName === "string") {
      componentName = [componentName];
    }
    var promises = [];
    var downloadType = "component";
    for(var i=0;i<componentName.length;i++) {
        let cmpName = componentName[i];
        let hash = _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.map.registry.get(registryClass).hash;
        if(typeof cmpName == "object"){
            hash = cmpName.registry ? _index_js__WEBPACK_IMPORTED_MODULE_0__._LC.map.registry.get(cmpName.registry).hash : hash;
            cmpName = cmpName.name;
        }
        if(cmpName.indexOf("/") != -1){
            downloadType = "path";
        }
        if(_core_index_js__WEBPACK_IMPORTED_MODULE_1__.Lyte._componentMap){
            var nameMap = _core_index_js__WEBPACK_IMPORTED_MODULE_1__.Lyte._componentMap[downloadType][hash];
            if(nameMap){
                var arr = nameMap[cmpName];
                if(arr && arr.length){
                    for(let j=0; j<arr.length;j++){
                        var script = document.createElement("script");
                        var src = arr[j]; //need to remove once cli gave full path
                        script.setAttribute("src", src);
                        document.head.appendChild(script);
                        promises.push(createPromiseScript(script))
                    }
                }else{
                    _core_index_js__WEBPACK_IMPORTED_MODULE_1__.Lyte.error("No component mapping available for component name "+ cmpName + "in the registry" + registryClass.name + " .");
                }
            }else{
                _core_index_js__WEBPACK_IMPORTED_MODULE_1__.Lyte.error("No mapping available for " + registryClass.name + "  registry.");
            }
        }else{
            _core_index_js__WEBPACK_IMPORTED_MODULE_1__.Lyte.error("No mapping JSON available");
        }
    }
    return Promise.all(promises);
}
function createPromiseScript(script) {
    return new Promise(function(resolve, reject) {
        script.onload = script.onreadystatechange = function() {
            if(!this.readyState || this.readyState == "loaded" || this.readyState == "complete") {
                resolve();
            }
        }
    });
}



/***/ }),

/***/ 59935577:
/*!****************************************************************!*\
  !*** ./node_modules/@slyte/component/src/utils/lyte-errors.js ***!
  \****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ApiError": () => (/* binding */ ApiError),
/* harmony export */   "ComponentError": () => (/* binding */ ComponentError),
/* harmony export */   "RegistryError": () => (/* binding */ RegistryError)
/* harmony export */ });
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../core/index.js */ 10533647);
//ignorei18n_start


class ComponentError extends _core_index_js__WEBPACK_IMPORTED_MODULE_0__.Logger {
    constructor(){
        super(...arguments);
    }
}

ComponentError.errorCodes = {
    LC001: "Error while parsing custom prop handler attribute {0} to the component {1}. Check if the value provided is a valid JSON",
    LC002: "{0} Component is not compiled. Please compile using Lyte CLI",
    LC003: "Helper named {0} is not defined",
    LC004: "Action named {0} doesn't exists",
    LC005: "Method named {0} doesn't exists in {1} component",
    LC006: "Error in data passed to component '{0}' for the properties - {1}",
    LC007: "Directive syntax is used but source not loaded",
    LC008: "{0} component not supporting turbo render. please add @turbo-supported in the component's template tag.",
    LC009: "Error in updateForHelper",
    LC010: "Passing lyteFastRender to unbound option in 'for' helper is depricated. So please make use of '@turbo' directive in the node - {0}",
    LC011: "Fast render source not bundled",
    LC012: "Error with templateAttributes.",
    LC013: "Data passed for property '{0}' of '{1}' cannot be serialized. The component '{2}' might not be registered before its use.",
    LC014: "No listenerId provided",
    LC015: "Invalid listenerId / listener is not available",
    LC016: "Deprecation Warning! pass the component class to component-class attribute, instead of passing '{0}' string component name to component-name attribute.",
    LC017: "{0} component not supporting shadow dom rendering. please add @shadow-supported in the component's template tag.",
    LC018: "Expected to pass object for {1} on the component {0}.",
    LC019: "A datatype mismatch has occurred for custom DataType '{0}'",
    LC020: "{0} component not supporting hide-tag rendering. please add @hide-tag-supported in the component's template tag."
};
ComponentError.register();

class ApiError extends _core_index_js__WEBPACK_IMPORTED_MODULE_0__.Logger {
    constructor(){
        super(...arguments);
    }
}

ApiError.errorCodes = {
    LC001: "Lyte.objectUtils doesn't support {0} function",
    LC002: "Lyte.arrayUtils doesn't support {0} function",
    LC003: "Component not specified in Lyte.Component.render",
    LC004: "Specified outlet {0} doesn't exists - Lyte.Component.render",
    LC005: "Parent Node / reference Node not provided for insertBefore method",
    LC006: "Invalid registry instance passed - Lyte.Component.render",
    LC007: "Uninstantiated registry class passed - Lyte.Component.render",
    LC008: "Unregistered component class passed - Lyte.Component.render",
    LC009: "Sanitizer instance not passed to sanitize api",
    LC010: "Invalid data passed in Lyte.Component.render"

};
ApiError.register();

class RegistryError extends _core_index_js__WEBPACK_IMPORTED_MODULE_0__.Logger {
    constructor(){
        super(...arguments);
    }
}

RegistryError.errorCodes = {
    LC001: "'{0}' registry is not registered with Lyte.",
    LC002: "'{0}' - '{1}' already registered with '{1}' registry.",
    LC003: "'{0}' Component class should extend a ComponentRegistry's Component property directly or extend through base component.",
    LC004: "Registry class extended in the '{0}' '{1}' is not registered with Lyte",
    LC005: "Invalid registry instance '{0}' passed in addRegistries hook of {1}",
    LC006: "Invalid registry instance '{0}'",
    LC007: "'{0}' Registry linked recursively in the app/addon.",
    LC008: "Invalid registry instance '{0}' passed in setDefaultRegisty hook of '{1}'",
    LC009: "Default Registry as well as Current Registry not found.",
    LC010: "{0} : {1} Not found in any registry.",
    LC011: "Invalid registry class '{0}' passed to lyte-registry in the component '{1}'",
    LC012: "Uninstantiated registry class '{0}' passed to lyte-registry in the component '{1}'",
    LC013: "Invalid registry name '{0}' passed to lyte-registry in the component '{1}'",
    LC014: "Uninstantiated registry class '{0}' passed to lyte-registry in the component '{1}'",
    LC015: "Invalid registry instance '{0}' passed in {1}"
};
RegistryError.register();


//ignorei18n_end

/***/ }),

/***/ 20426923:
/*!*******************************************************************!*\
  !*** ./node_modules/@slyte/component/src/utils/lyte-sanitizer.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Sanitizer": () => (/* binding */ Sanitizer),
/* harmony export */   "Security": () => (/* binding */ Security)
/* harmony export */ });
/* harmony import */ var _zoho_SecurityJS_dist_security_html_sanitizer_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../@zoho/SecurityJS/dist/security-html-sanitizer.js */ 20488676);
/* harmony import */ var _zoho_SecurityJS_dist_security_html_sanitizer_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_zoho_SecurityJS_dist_security_html_sanitizer_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _lyte_errors_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-errors.js */ 59935577);
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../index.js */ 26591616);
/* harmony import */ var _compatibility_compatibility_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../compatibility/compatibility.js */ 32483767);
/* harmony import */ var _compatibility_compatibility_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_compatibility_compatibility_js__WEBPACK_IMPORTED_MODULE_0__);
//ignorei18n_start




let Security = {
    "_userSanitizerInstance_":{}, 
    "_eM" : {
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#x27;',
        '`': '&#x60;',
        '=': '&#x3D;'
        }, 
    "_eR" : /[&<>"'`=]/g,
    "_eF" : function(str) {
        return this._eM[str];
    }, 
    "escape" : function(string) {
        if (typeof string !== 'string') {
            string = '' + string;
        }
        return string.replace(this._eR, this._eF.bind(this));
    },
    defaultTags : ["link-to"],
    defaultAttr : ["is","yield-name","lt-prop-route", "lt-prop-dp", "lt-prop-fragment", "lt-prop-qp", "lt-prop", "lt-prop-class", "lt-prop-id", "lt-prop-rel", "lt-prop-title", "lt-prop-style", "lt-prop-target","lt-prop-td","lt-prop-custom","lt-prop-target","lt-prop-id","lt-prop-class","lt-prop-style","lt-prop-rel","lt-prop-title"],
    sanitizeHTML : function(obj){
        let clean;
        let divEle = document.createElement("div");
        let html = obj.html;
        let instance = obj.instance;
        let additionalObject = obj.additionalObject ? obj.additionalObject : {};
        if(instance && Object.keys(instance).length){
            if(additionalObject && Object.keys(additionalObject).length){
                this.initializeConfig(additionalObject);
                this.removeConfig(additionalObject);
                this.addLyteComponents(additionalObject);
                clean = this.sanitizeWithConfig(html ,additionalObject, instance);
            }
            else{
                clean = instance.sanitize(html);
            }
        }else{
            //@Slicer.developmentStart
            _lyte_errors_js__WEBPACK_IMPORTED_MODULE_1__.ApiError.error("LC009");
            //@Slicer.developmentEnd
            return;
        }
        if(obj && obj.getInnerHTML){
            return clean;
        }
        divEle.innerHTML = clean;
        return divEle;
    },
    createSanitizer : function (obb) {
        if(obb && obb.__target__){
            obb = obb.__target__;
        }
        this.initializeConfig(obb);
        this.addConfig(obb);
        this.addLyteComponents(obb);
        let instance = (0,_zoho_SecurityJS_dist_security_html_sanitizer_js__WEBPACK_IMPORTED_MODULE_2__.HTMLPurifier)(obb);
        instance._GLOBAL_TAGS = obb.GLOBAL_TAGS;
        instance._GLOBAL_ATTRIBUTES = obb.GLOBAL_ATTRIBUTES;
        instance._FORBID_TAGS = obb.FORBID_TAGS;
        instance._FORBID_ATTR = obb.FORBID_ATTR;
        return instance;
    },
    sanitizeWithConfig : function(html , additionalObject, instance){
        this.addGlobalObject(instance,additionalObject);
        let clean = instance.sanitize(html);
        this.removeGlobalObject(instance,additionalObject);
        return clean;
    },
    initializeConfig : function(obb){
        if (!obb.GLOBAL_ATTRIBUTES) {
            obb.GLOBAL_ATTRIBUTES = [];
        }
        if (!obb.FORBID_TAGS) {
            obb.FORBID_TAGS = [];
        }
        if (!obb.FORBID_ATTR) {
            obb.FORBID_ATTR = [];
        }
        if (!obb.GLOBAL_TAGS) {
            obb.GLOBAL_TAGS = [];
      }
    },
    addConfig : function(obb){
        this.defaultAttr.forEach(function(item){
            obb.GLOBAL_ATTRIBUTES.push(item);
        })
        this.defaultTags.forEach(function(item){
            obb.GLOBAL_TAGS.push(item);
        })
    },
    removeConfig : function(obb){
        if(obb && Object.keys(obb) && Object.keys(obb).length > 0){
            if(obb.GLOBAL_ATTRIBUTES && obb.GLOBAL_ATTRIBUTES.length > 0){
                this.defaultAttr.forEach(function(item){
                    var index = obb.GLOBAL_ATTRIBUTES.indexOf(item);
                    if(index != -1){
                        obb.GLOBAL_ATTRIBUTES.splice(index,1);
                    }
                })
            }
            if(obb.GLOBAL_TAGS && obb.GLOBAL_TAGS.length > 0){
                this.defaultTags.forEach(function(item){
                    var index = obb.GLOBAL_TAGS.indexOf(item);
                    if(index != -1){
                        obb.GLOBAL_TAGS.splice(index,1);
                    }
                })
            }
        }
    },
    addLyteComponents : function(obb){
        var globalTagArr = Array.from(obb.GLOBAL_TAGS);
        var attr = [];
        for(var a=0; a<globalTagArr.length; a++){
            let regComps = _index_js__WEBPACK_IMPORTED_MODULE_3__.ComponentRegistry._registeredCommonClass;
            let rawComps = _index_js__WEBPACK_IMPORTED_MODULE_3__.ComponentRegistry._registeredCommonCeClass;
            if(regComps[globalTagArr[a]]){
                attr = regComps[globalTagArr[a]].observedAttributes;
            }else if(rawComps[globalTagArr[a]]){
                if(rawComps[globalTagArr[a]].observedAttributes){
                    attr = rawComps[globalTagArr[a]].observedAttributes;
                }
            }
            for(var i=0; i<attr.length; i++){
                if(obb.GLOBAL_ATTRIBUTES.indexOf(attr[i]) == -1){
                    obb.GLOBAL_ATTRIBUTES.push(attr[i]);
                }
            }
        }
    },
    addGlobalObject : function(instanceObj,additionalObj){
        for (var property in additionalObj) {
                if(Array.isArray(additionalObj[property])){
                    additionalObj[property].forEach(function(item){
                        if(instanceObj["_"+property].indexOf(item) == -1){
                            instanceObj["_"+property].push(item);
                        }
                    });
            }
        }
    },
    removeGlobalObject : function(instanceObj,additionalObj){
        for (var property in additionalObj) {
            if(Array.isArray(additionalObj[property])){
                additionalObj[property].forEach(function(item){
                    var index = instanceObj["_"+property].indexOf(item);
                    if(index != -1){
                        instanceObj["_"+property].splice(index,1);
                    }
                });
        }
        };
    }
}
class Sanitizer {
    constructor(config){
        this._sanitzer = Security.createSanitizer(config);
    }
    clean(config){
        if(typeof config == "string"){
            config = {
                html : config,
                instance : this._sanitzer
            }
        }else{
            config.instance = this._sanitzer;
        }
        return Security.sanitizeHTML(config);
    }
    sanitize(html){
        this.clean(html);
    }
}
// if(!window.__lyteConfig){
//     window.__lyteConfig = {
//         _definedBeforeBridging : [],
//         _alreadyDefinedBeforeBridging : [],
//         v4 : true,
//         Sanitizer : Sanitizer,
//         _pendingV3Components : {},
//         _firstRegisteredComp : {}
//     }
// }else{
    window.__lyteConfig.Sanitizer = Sanitizer;
// }

//migarte changes
// Lyte.Security.ignoreSanitizer -> throw error - sanitizing HTML is mandatory
// Lyte.Security.createSanitizer() -> new Sanitizer()
//ignorei18n_end

/***/ }),

/***/ 10494547:
/*!****************************************************************!*\
  !*** ./node_modules/@slyte/component/src/utils/v3-registry.js ***!
  \****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "V3Registry": () => (/* binding */ V3Registry)
/* harmony export */ });
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../index.js */ 26591616);
/* harmony import */ var _core_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../core/index.js */ 70549578);

// import { Lyte } from "@slyte/core";

class V3Registry extends _core_index_js__WEBPACK_IMPORTED_MODULE_0__.Service {
    render(componentName, data, outlet, _lyteOptions){
        var component;
        let decideReg = _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.getDecidedRegistry();
        if(componentName && _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.validateRenderData(data)) {
            _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.setDecidedRegistry(V3Registry);
            if(typeof componentName == "string") {
                component = document.createElement(componentName);
            } else {
                Lyte.error("LC007");  //no i18n  
                _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.setDecidedRegistry(decideReg);
                return;
            }
        } else {
            Lyte.error("LC007");//no i18n
            _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.setDecidedRegistry(decideReg);
            return;
        }
        if(data){ 
            component.setData(data);
        }
        if(outlet) {
            let actOutlet;
            if(typeof outlet == "string"){
                actOutlet = document.querySelector(outlet);
            }else{
                actOutlet = outlet;
            }
            if(actOutlet) {
                if(_lyteOptions && _lyteOptions.clearOutlet){
                    actOutlet.innerHTML = "";    
                }
                actOutlet.appendChild(component);
                component._callee = component.getCallee ? component.getCallee(actOutlet) : undefined;
            } else {
                Lyte.error("LC008", outlet);//no i18n
            }
        }
        _index_js__WEBPACK_IMPORTED_MODULE_1__._LC.setDecidedRegistry(decideReg);
        return component;
    }
}


/***/ }),

/***/ 35155520:
/*!**************************************************!*\
  !*** ./node_modules/@slyte/core/src/DataType.js ***!
  \**************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "DataType": () => (/* binding */ DataType)
/* harmony export */ });
/* harmony import */ var _lyte_error__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./lyte-error */ 10533647);
/* harmony import */ var _lyte_utils__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-utils */ 70416156);



class DataType {
    static register(opts){
        this._name = this.name.replace(/([a-zA-Z0-9])(DataType)$/g, '$1');
        var clsExt = this.extends || this.type;
        this.type = clsExt;
        if(clsExt == undefined || DataType.types.indexOf(clsExt) == -1){
            _lyte_error__WEBPACK_IMPORTED_MODULE_0__.Logger.error("Not a valid field type - "+clsExt);
            return;
        }  
        if(this.prototype.serialize){
            this.serialize = this.prototype.serialize;
        }      
        if(this.prototype.deserialize){
            this.deserialize = this.prototype.deserialize;
        }
        var rHash = opts.refHash;
        var currCls = this, 
        appMap = DataType.lyte = DataType.lyte || new Map(),  
        dtMap,
        currClsName = currCls.name;
        if(!appMap.has(rHash)){
            appMap.set(rHash, new Map());
        }
        dtMap = appMap.get(rHash);
        if(!dtMap.has(currClsName)){
            dtMap.set(currCls.name, currCls);
        }
        DataType.triggerEvent(rHash, currCls);
    }
    static registerInApp(parent){
        var name = this._name = this.name || this.name.replace(/([a-zA-Z0-9])(DataType)$/g, '$1');
        var clsExt = this.extends || this.type;
        this.type = clsExt;
        if(clsExt == undefined || DataType.types.indexOf(clsExt) == -1){
            _lyte_error__WEBPACK_IMPORTED_MODULE_0__.Logger.error("Not a valid field type - "+clsExt);
            return;
        }  
        if(this.prototype.serialize){
            this.serialize = this.prototype.serialize;
        }      
        if(this.prototype.deserialize){
            this.deserialize = this.prototype.deserialize;
        }
        var name = this._name = this._name || this.name.replace(/([a-zA-Z0-9])(DataType)$/g, '$1');
        if(!parent.dataType.hasOwnProperty(name)){
            parent.dataType[name] = this;
            if(parent.dataType.triggerEvent){
                parent.dataType.triggerEvent("add", name, this);
            }
        }
        else{
            _lyte_error__WEBPACK_IMPORTED_MODULE_0__.Logger.warn("DataType with the same name - "+ name+ " already present in the app");
        }
    }
}
(0,_lyte_utils__WEBPACK_IMPORTED_MODULE_1__.defProp)(DataType, "types", {
    value: ["string", "object", "number", "boolean", "array"]
});
(0,_lyte_utils__WEBPACK_IMPORTED_MODULE_1__.extendEventListeners)(DataType);


/***/ }),

/***/ 76043102:
/*!***********************************************************!*\
  !*** ./node_modules/@slyte/core/src/IdleTaskScheduler.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "IdleTaskScheduler": () => (/* binding */ IdleTaskScheduler)
/* harmony export */ });
/* harmony import */ var _lyte_error__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./lyte-error */ 10533647);
/* harmony import */ var _lyte_utils__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-utils */ 70416156);



// class PriorityQ {
//   constructor() {
//       this.queue = [];
//   }
//   enqueue(name, priority, scope) {
//       const queueElement = { name, priority, scope };
//       let added = false;
//       for (let i = 0; i < this.queue.length; i++) {
//           if (queueElement.priority < this.queue[i].priority) {
//               this.queue.splice(i, 0, queueElement);
//               added = true;
//               break;
//           }
//       }
//       if (!added) {
//           this.queue.push(queueElement);
//       }
//   }
//   dequeue(name, scope) {
//       if (!this.isEmpty()) {
//         if(!name){
//           return this.queue.shift();
//         }
//         else{
//           let dequeuditem;
//           this.queue = this.queue.filter(item => {
//             if (item.name === name && item.scope === scope) {
//               dequeuditem = item;
//               return false; // Remove the item from the queue
//             }
//             return true; // Keep the item in the queue
//           });
//           return dequeuditem;
//         }
//       }
//   }
//   get(){
//     return this.queue;
//   }
//   isEmpty() {
//       return this.queue.length === 0;
//   }
//   size() {
//       return this.queue.length;
//   }
// }

class IdleTaskScheduler {
    constructor(obj){
      this.idleCallback = this.idleCallback.bind(this);
      this.timeout = 50;
      this.chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
      this.tasks = new Map();
      this.idleCallbackRequested = false;
      this.delayInit = false;
      this.useRequestIdleCallback = true;
      this.priorityQueue = [];
      this.processTime = {};
      this.perf = false;
      if(obj!= undefined){      
        if(obj.useRequestIdleCallback !== undefined){
          this.useRequestIdleCallback = obj.useRequestIdleCallback;
        }
        if(obj.delayInit !== undefined){
            this.delayInit = obj.delayInit;
        }
        if(obj.timeout !== undefined){
          this.timeout = obj.timeout;			
        }
        if(obj.perf !== undefined){
          this.perf = obj.perf;
        }
      }
    }
    isGenerator(obj) {
      return (
          obj !== null &&
          typeof obj === "object" &&
          typeof obj.next === "function" &&
          typeof obj[Symbol.iterator] === "function"
      );
    }
    init(obj){
      if(this.delayInit == true){
        if(obj !== undefined){ 
          if(obj.useRequestIdleCallback !== undefined){
            this.useRequestIdleCallback = obj.useRequestIdleCallback;
          }
          if(obj.timeout !== undefined){
            this.timeout = obj.timeout;
          }
          if(obj.perf !== undefined){
            this.perf = obj.perf;
          }
        }
        this.delayInit = false;
        this.tasksScheduler();
      } 
    }
    getUniqueKey(len){
      var result;
      while(result == undefined || this.tasks.has(result)){
          result = undefined;
          result = this.generateUniqueKey(len);
      }
      return result;
    }
    generateUniqueKey(length){
      let result = '', clen = this.chars.length;
      for (let i = 0; i < length; i++) {
          result += this.chars.charAt(Math.floor(Math.random() * clen));
      }
      return result;
    }
    enqueueTask(handler, data, id, scope = "slyte"){
      if(!id){
          id = this.getUniqueKey(6);
      }
      const key = `${scope}$${id}`;
      if(this.tasks.has(key)){
          _lyte_error__WEBPACK_IMPORTED_MODULE_0__.Logger.error("Task with id-"+key+" is alreaded queued for execution.");
          return; 
      }
      this.tasks.set(key, {handler: handler, data: data});
      this.tasksScheduler();
      return key;
    }
    deleteTask(id, scope = "slyte"){
      if(id){
          const key = `${scope}$${id}`;
          var val = this.tasks.get(key);
          this.tasks.delete(key);
          if(this.priorityQueue instanceof PriorityQ){
            this.priorityQueue.dequeue(id, scope);
          }
          return val;
      }
    }
    getPriorityQObj(){
      var id, i=0, pObj;
      if(this.priorityQueue.length){
        var len =this.priorityQueue.length;
        while(i<len){
          id = this.priorityQueue[i];
          if(this.tasks.has(id)){
            pObj = this.tasks.get(id);
            break;
          }
          i++;
        }
        if(pObj){
          return {id:id, obj:pObj, ind:i};
        }
      }
      return false;   
    }
    dequeueTask(id, scope = "slyte"){
      if(id){
          const key = `${scope}$${id}`;
          var val = this.tasks.get(key);
          // console.log("dequeud-",id);
          this.tasks.delete(key);
          return val;
      }
      else if(this.tasks.size || this.priorityQueue.length){
        var pqObj = this.getPriorityQObj(), gnxt, gval, obj, isPriority = false, ind;
        if(pqObj !== false){
          id = pqObj.id;
          obj = pqObj.obj;
          ind = pqObj.ind;
          isPriority = true;
        }
        else{
          if(!this.tasks.size){
            return;
          }
          var val = this.tasks.entries().next().value;
          id = val[0]
          obj = val[1];
        }
        // console.log("dequeud-",id);
        this.currentTask = {
          id:id,
          handler: obj.handler
        };
        (0,_lyte_utils__WEBPACK_IMPORTED_MODULE_1__.extendEventListeners)(this.currentTask);
        if(this.isGenerator(obj.handler)){
          gnxt = obj.handler.next();
          if(gnxt.done == false){
            gval = gnxt.value;
            if(typeof gval == "function"){
              return gval;
            }
          }
          else{
            this.currentTask.triggerEvent("done", id);
            if(isPriority){
              this.priorityQueue.splice(ind,1);
            }
            this.tasks.delete(id);
            return this.dequeueTask();
          }
        }
        else{
          this.currentTask.triggerEvent("done", id);
          if(isPriority){
            this.priorityQueue.splice(ind,1);
          }
          this.tasks.delete(id);
        }
        return obj;
      }
    }
    tasksScheduler(){
      if(this.delayInit == false){
        if(this.tasks.size){
          if(this.idleCallbackRequested == false){
            if('requestIdleCallback' in window && this.useRequestIdleCallback){
                this.idleCallbackRequested = true;
                requestIdleCallback(this.idleCallback);
            }   
            else{
                this.requestIdleCallback(this.idleCallback);
            }
          }
        }
        else{
          if('cancelIdleCallback' in window && this.useRequestIdleCallback){
            cancelIdleCallback(this.idleCallback);
          }
        }
      }
    }
    idleCallback(deadline){
      var task, remTime = deadline.timeRemaining();
      while(remTime>0 && this.tasks.size){
        task = this.dequeueTask();
        if(this.perf){
          var p1 = performance.now();
        }
        if(typeof task == "function"){
              // setTimeout(function(){
                  task();
              // },1);
        }
        if(typeof task == "object"){
          // setTimeout(function(){
          var data = task.data || [];
          if(typeof task.handler == "function"){
            task.handler(...data);	
          }
          // },1);
        }
        if(this.perf){
          var p2 = performance.now();
          var id = this.currentTask.id;
          this.processTime[id] = (this.processTime.hasOwnProperty(id) ? this.processTime[id] : 0) + (p2-p1);
        }
        remTime = deadline.timeRemaining();
      }
      this.idleCallbackRequested = false;
      this.tasksScheduler();
    }
    requestIdleCallback(callback){
      var self = this;
      setTimeout(function(){
        setTimeout(function(){
          var startTime = Date.now();
          callback({
            timeRemaining: function(){
              var diffTime = Date.now() - startTime;
              if(diffTime > self.timeout){
                  return 0;
              }
              return self.timeout-diffTime;
            }
          });
        }, 1);
      }, 50);
    }
  }  
  

/***/ }),

/***/ 72493624:
/*!**************************************************!*\
  !*** ./node_modules/@slyte/core/src/JsonPath.js ***!
  \**************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Jwalk": () => (/* binding */ pathFinder)
/* harmony export */ });
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../index.js */ 21046606);


function pathFinder (scope,path){
    if(path){
        var Json = scope;
        if(typeof scope == "object"){
            return parsePath(scope,path)
        }
    }
    else{
        _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Invalid JsonPath - " + path)
    }
}
function parsePath (scope,path){
    if(path.includes(".")){
        path=path.replace(/ /g,"");
        var pathArr =path.split(".")
        if(pathArr[0] == "$"){
            pathArr.splice(0,1)
        }
    }
    return GetData(scope,pathArr)
}
function GetData (scope,path,DeepTraverse){
    if(DeepTraverse){
        var arr =[];
        if(Array.isArray(scope)){
            path.splice(0,1);
            for(var i_deep=0; i_deep<scope.length ; i_deep++){
            var value = deepBranches(scope[i_deep],path,DeepTraverse)
            if(Array.isArray(value) && value.length ==0){
                value = undefined;
            }
            if(value){
                arr.push(value)
            }
            }
            return arr;
        }
    }
    else{
        return deepBranches(scope,path,DeepTraverse)
    }
}
function deepBranches (obj,path,DeepTraverse){
    for(var pathIndex =0 ; pathIndex<path.length; pathIndex ++){
        var key = path[pathIndex];
        var nextKey = path[pathIndex+1],_nextKey;
        if(nextKey){
            _nextKey = keyParser(nextKey);
        }
        if(key.includes("[") && key.includes("]")){
            var utilScope = key.match(/\[([^\]]+)\]/g)[0]
            var utilKey = key.match(/^(.+?)\[/g)
            if(utilKey){
                utilKey=utilKey[0].replace(/\[/g,"");
            }
            if(!Array.isArray(obj) && utilKey){
                obj = obj[utilKey];
            }
            obj = (!Array.isArray(obj) && utilScope) ? undefined : obj;
            if(Array.isArray(obj)){
                if(utilScope && !utilScope.includes("@")){
                    if(utilScope.includes(":")){
                        utilScope = utilScope.replace(/\[/g,"").replace(/\]/g,"");
                        var slice = utilScope.split(":");
                        var start = slice[0]==""?undefined:parseInt(slice[0]);
                        var end = slice[1]==""?undefined:parseInt(slice[1]);
                        obj = obj.slice(start,end)
                    }
                    else if(utilScope.includes("*")){
                        continue
                    }
                    else if(utilScope){
                        var key = utilScope.match(/\[(.*?)\]/)[1]
                        obj = obj[key]
                    }
                }
            }
        }
        else if(key == ""){
            if(_nextKey == undefined){
                _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Invalid JsonPath - " + path + "expected : Path string should not end with the  '..' ")
            }
            obj = GetAllKeys(obj,_nextKey)
            if(path.length-1 != pathIndex){
                if(Array.isArray(obj)){
                    DeepTraverse =1;
                    var utilScope = nextKey.match(/\[([^\]]+)\]/g)
                    path.splice(0,pathIndex+1)
                    if(utilScope!=null){
                        utilScope = utilScope[0];
                        utilScope = utilScope.match(/\[(.*?)\]/)[1]
                        obj = separateArray(obj,utilScope);
                    }
                    if(path.length>1){
                        obj = GetData(obj,path,DeepTraverse)
                        break;
                    }
                }
            }
            if(nextKey && !nextKey.includes("[") && !nextKey.includes("]")){
                pathIndex++;
            }
        }
        else if(key == "*"){
            continue;
        }
        else{
            if(path[pathIndex-1] && (path[pathIndex-1] == "*" || path[pathIndex-1].includes("*"))){
                if(Array.isArray(obj)){
                    var newArray=[];
                    var ind=0;
                    for(var arr_index = 0; arr_index<obj.length; arr_index++){
                        if(obj[arr_index][key]){
                            newArray[ind] = obj[arr_index][key];
                            ind++;
                        }
                    }
                    obj=newArray;
                }
                else if (typeof obj == "object"){
                    obj = obj[key]
                }
            }
            else{
                obj = obj[key]?obj[key]:undefined;
            }
        }

    }
    return obj
}
function keyParser (key){
    if(key.includes("[") && key.includes("]")){
        var utilScope = key.match(/\[([^\]]+)\]/g)[0]
        var utilKey = key.match(/^(.+?)\[/g) 
        if(utilKey){
            utilKey=utilKey[0].replace(/\[/g,"");
            return utilKey;
        }
    }
    return key;
}
function GetAllKeys (scope,key){
    var arr= []; 
    getRecurssive(scope,key,arr);
    return arr
}
function getRecurssive (scope,key,arr){
    if(typeof scope == "object"){
        for(var v in scope){
            if(v == key){
                arr.push(scope[key])
            }
            if(typeof scope[v] == "object"){
                getRecurssive(scope[v],key,arr)
            }
        }
    }
    else if(Array.isArray(scope)){
        for( var index = 0 ; index<scope.length ; index++ ){
            var obj = scope[index];
            getRecurssive(obj,key,arr);
        }
    }
}
function separateArray (scope,index){
    var Arr =[];
    for (var i=0; i< scope.length; i++){
        if(scope[i][index]){
            Arr.push(scope[i][index]);
        }
    }
    return Arr;
}



/***/ }),

/***/ 57625180:
/*!***************************************************!*\
  !*** ./node_modules/@slyte/core/src/LyteAddon.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "LyteAddon": () => (/* binding */ LyteAddon)
/* harmony export */ });
/* harmony import */ var _lyte__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./lyte */ 21046606);
/* harmony import */ var _service__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./service */ 70549578);
/* harmony import */ var _lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-utils.js */ 70416156);



// import { resolvePromises } from './rsvp';
// import { Logger } from './lyte-error';
/*convert to custom class*/
class LyteAddon extends _service__WEBPACK_IMPORTED_MODULE_0__.Service {
    constructor(config){
        super();
        this.config = config;
        (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__._lyteInit)(LyteAddon,this);
        (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__._lyteDidConnect)(LyteAddon,this);
    }
    static register(options){
        if(options){
          if(options._migration){
            this._migration = options._migration;
          }
        }
    }
    lookups() {
        return [];
    }

    scopedInstance() {
        return _lyte__WEBPACK_IMPORTED_MODULE_2__.__scopedInstance.apply(this, Array.from(arguments));
    }

    get __isAddon() {
        return true;
    }
    
}
// LyteAddon.prototype.resolvePromises = function(promises) {
//     return new Promise(function(res, rej) {
//       resolvePromises(promises).then(function(data) {
//         res(data);
//       },function(data) {
//         rej(data);
//       })
//     })
// }
LyteAddon._component = {};
// LyteAddon.error = Logger.error.bind(Logger);
// LyteAddon.warn = Logger.warn.bind(Logger);
// LyteAddon.errorCodes = Logger.errorCodes;



/***/ }),

/***/ 99289921:
/*!***********************************************!*\
  !*** ./node_modules/@slyte/core/src/Mixin.js ***!
  \***********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Mixin": () => (/* binding */ Mixin)
/* harmony export */ });
/* harmony import */ var _lyte_utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./lyte-utils.js */ 70416156);
/* harmony import */ var _lyte_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte.js */ 21046606);


let registeredMixin = {};
let Mixin = (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_0__.createCustomClass)(function(arg1,overrides){
    class Mixin extends (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_0__.getClass)([],arg1) {
        static register(options){
            if(this.__lMod) {
                return super.register.apply(this, Array.from(arguments));
            }
            if(options){
                if(options.hash){
                    this._hash = options.hash;
                }
                if(options.refHash){
                    this._refHash = options.refHash;
                    var mobj = Mixin.registeredMixin[options.refHash] = Mixin.registeredMixin[options.refHash] || {};
                    mobj[this.name] = this;
                    Mixin.getAppClass(options, this);
                }
            }
        }
        static getAppClass(options, mixinCls){
            let registryClass;
            _lyte_js__WEBPACK_IMPORTED_MODULE_1__.Lyte._instances.forEach(function(appIns){
                let appClass = appIns.constructor;
                if(appClass._hash == options.refHash){
                    appIns.registeredMixins[mixinCls.name] = mixinCls;
                }
            });
        }
    }
    Mixin.registeredMixin = registeredMixin;
    return overrides(Mixin)
})



/***/ }),

/***/ 97229384:
/*!***********************************************!*\
  !*** ./node_modules/@slyte/core/src/Utils.js ***!
  \***********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Utils": () => (/* binding */ Utils)
/* harmony export */ });
/* harmony import */ var _lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-utils.js */ 70416156);
/* harmony import */ var _service_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./service.js */ 70549578);


/*convert to custom class */
class Utils extends _service_js__WEBPACK_IMPORTED_MODULE_0__.Service {
    static addMethods(data){
        if(Array.isArray(data)){
            data.forEach(function(fn){
                var name = fn._name ? fn._name : fn.name;
                if(!Utils.prototype[name]){
                    Utils.prototype[name] = fn;
                }
            });
        }
    } 
}
Utils.addMethods([_lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__.deepCopyObject, _lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__.extendEventListeners]);



/***/ }),

/***/ 10533647:
/*!****************************************************!*\
  !*** ./node_modules/@slyte/core/src/lyte-error.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Logger": () => (/* binding */ Logger)
/* harmony export */ });
/* harmony import */ var _lyte_utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./lyte-utils */ 70416156);


const level = 0;
class Logger extends Error {
    static get level(){
        return level;
    }
    constructor(){
        super(...arguments);
    }
    static register(){
        var context = this;
        while(context !== Logger){
            if(!context.hasOwnProperty("addEventListener")){
                (0,_lyte_utils__WEBPACK_IMPORTED_MODULE_0__.extendEventListeners)(context);
            }
            context = Object.getPrototypeOf(context);
        }
        if(!Logger.triggerEvent){
            (0,_lyte_utils__WEBPACK_IMPORTED_MODULE_0__.extendEventListeners)(Logger);
        }
    }
    static registerErrorCodes(obj, defineProperty){
        if(obj && typeof obj == "object"){
            for(var key in obj){
                var message = obj[key];
                if(defineProperty){
                    Object.defineProperty(this.errorCodes, key, {
                        value: message
                    });
                }
                else{
                    this.errorCodes[key] = message;
                }
            }
        }
    }
    static getErrorMessage(code, withCode){
      var args = Array.from(arguments).slice(1);
      var message = this.errorCodes[code];
      if(typeof message == "function"){
         return code+": "+message.apply(this, args);
      }
      else if(message) {
          var msg = message.replace(/{(\d+)}/g, function(t, i) {
              return args[i];
          });
          return code+": "+msg;
      } else {
          return code;
      }
    }
    static setErrorMessage(code, message, defineProperty){
        if(defineProperty){
            Object.defineProperty(this.errorCodes, code, {
                value: message
            });
        }
        else{
            this.errorCodes[code] = message;
        }
    }
    static error(){
        var args = Array.from(arguments), arg0 = arguments[0], ins, errorObj, logLevel = Logger.level, scIns;
        if(arg0.__isApp || arg0.__isAddon){
            ins = arg0;
            args.splice(0,1);
            if(ins.hasOwnProperty("logLevel")){
                logLevel = ins.logLevel;
            }
        }
        else if(arg0.$app || arg0.$addon){
            scIns = arg0;
            ins = arg0.$app || arg0.$addon;
            args.splice(0, 1);
            if(ins.hasOwnProperty("logLevel")){
                logLevel = ins.logLevel;
            }
        }
        if(logLevel < 3){
            errorObj = args[0];
            var parse = errorObj.stack, 
            context = this;
            errorObj = parse ? errorObj : new this(this.getErrorMessage.apply(this, args));
            while(context !== Logger){
                if (context.hasOwnProperty("onerror")) {
                    context.onerror.call(context, errorObj, args[1], this, ins, scIns);
                }
                if(context.hasOwnProperty("triggerEvent")){
                    context.triggerEvent.call(context, "error", errorObj, args[1], this, ins, scIns);
                }
                context = Object.getPrototypeOf(context);
            }
            if (Logger.hasOwnProperty("onerror")) {
                Logger.onerror.call(Logger, errorObj, args[1], this, ins, scIns);
            }
            if(Logger.hasOwnProperty("triggerEvent")){
                Logger.triggerEvent.call(Logger, "error", errorObj, args[1], this, ins, scIns);
            }
            if(scIns){
                // if(scIns.onerror) {
                //     scIns.onerror.call(scIns, errorObj, args[1], this, ins, scIns);
                // }
                // if(scIns.triggerEvent){
                //     scIns.triggerEvent.call(scIns, "error", errorObj, args[1], this, ins, scIns);
                // }
                var scCls = scIns.constructor;
                if(scCls.onerror) {
                    scCls.onerror.call(scCls, errorObj, args[1], this, ins, scIns);
                }
                if(scCls.triggerEvent){
                    scCls.triggerEvent.call(scCls, "error", errorObj, args[1], this, ins, scIns);
                }
                if(scIns.$registry || scIns.$router){
                    var Ins = scIns.$registry || scIns.$router;
                    // if(Ins.onerror) {
                    //     Ins.onerror.call(Ins, errorObj, args[1], this, ins, scIns);
                    // }
                    // if(Ins.triggerEvent){
                    //     Ins.triggerEvent.call(Ins, "error", errorObj, args[1], this, ins, scIns);
                    // }
                    var regcls = Ins.constructor;
                    if(regcls.onerror) {
                        regcls.onerror.call(regcls, errorObj, args[1], this, ins, scIns);
                    }
                    if(regcls.triggerEvent){
                        regcls.triggerEvent.call(regcls, "error", errorObj, args[1], this, ins, scIns);
                    }
                }
            }
            if(ins){
                if(ins.onerror) {
                    ins.onerror.call(ins, errorObj, args[1], this, ins, scIns);
                }
                if(ins.triggerEvent){
                    ins.triggerEvent.call(ins, "error", errorObj, args[1], this, ins, scIns);
                }
                var insCls = ins.constructor;
                if(insCls.onerror) {
                    insCls.onerror.call(insCls, errorObj, args[1], this, ins, scIns);
                }
                if(insCls.triggerEvent){
                    insCls.triggerEvent.call(insCls, "error", errorObj, args[1], this, ins, scIns);
                }
            }
            var safari = errorObj.stack && errorObj.stack.indexOf(errorObj.message) == -1
            if (parse && !safari) {
                errorObj = JSON.parse(JSON.stringify(errorObj, Object.getOwnPropertyNames(errorObj)));
            }
            if(args[1]) {
                console.error(errorObj.stack ? (safari ? errorObj : errorObj.stack) : errorObj.message, args[1]);
            } else {
                console.error(errorObj.stack ? (safari ? errorObj : errorObj.stack) : errorObj.message);    
            }
        }
    }
    static warn(){
        var args = Array.from(arguments), arg0 = arguments[0], ins, errorObj, logLevel = Logger.level;
        if(arg0.__isApp || arg0.__isAddon){
            ins = arg0;
            args.splice(0,1);
            if(ins.hasOwnProperty("logLevel")){
                logLevel = ins.logLevel;
            }
        }
        else if(arg0.$app || arg0.$addon){
            ins = arg0.$app || arg0.$addon;
            args.splice(0, 1);
            if(ins.hasOwnProperty("logLevel")){
                logLevel = ins.logLevel;
            }
        }
        if(logLevel < 2){
            var errorObj = args[0];
            errorObj = errorObj.stack ? JSON.parse(JSON.stringify(errorObj, Object.getOwnPropertyNames(errorObj))) : new this(this.getErrorMessage.apply(this, args));
            console.warn(errorObj.stack ? (errorObj.stack.indexOf(errorObj.message) != -1 ? errorObj.stack.replace("Error: ", "Warn: ") : errorObj) : errorObj.message);
        }
    }
    static log(){
        var args = Array.from(arguments), arg0 = arguments[0], ins, errorObj, logLevel = Logger.level;
        if(arg0.__isApp || arg0.__isAddon){
            ins = arg0;
            args.splice(0,1);
            if(ins.hasOwnProperty("logLevel")){
                logLevel = ins.logLevel;
            }
        }
        else if(arg0.$app || arg0.$addon){
            ins = arg0.$app || arg0.$addon;
            args.splice(0, 1);
            if(ins.hasOwnProperty("logLevel")){
                logLevel = ins.logLevel;
            }
        }
        if(logLevel == 0){
            var errorObj = args[0];
            errorObj = errorObj.stack ? JSON.parse(JSON.stringify(errorObj, Object.getOwnPropertyNames(errorObj))) : new this(this.getErrorMessage.apply(this, args));
            console.log(errorObj.stack ? (errorObj.stack.indexOf(errorObj.message) != -1 ? errorObj.stack.replace("Error: ", "Log: ") : errorObj) : errorObj.message);        
        }
    }
}
Logger.errorCodes = {
    ERR02 : "Mandatory prop cannot be empty", 
    ERR03 : "Type of value does not match the specified data type",
    ERR04 : "Value is greater than the maximum value allowed",
    ERR05 : "Value is less than the minimum value allowed", 
    ERR06 : "Length of string/array is greater than the maximum limit allowed", 
    ERR07 : "Length of string/array is less than the minimum limit allowed",
    ERR08 : "String does not match the specified pattern", 
    ERR09 : "Values in array are not unique", 
    ERR10 : "Value is not equal to the specified constant", 
    ERR29: "Property not defined in the object",
    ERR30: "Property's value is not an instanceof the mentioned class", 
    ERR31: "Type definition for nested properties / items does not match with the value" ,     
    L001: "{0} {1} is already registered"
    // /* move to component */
    // LC001: "Error while parsing custom prop handler attribute {0}. Check if the value provided is a valid JSON",
    // LC002: "{0} Component is not compiled. Please compile using Lyte CLI",
    // LC003: "Helper named {0} is not defined",
    // LC004: "Action named {0} doesn't exists",
    // LC005: "Lyte.objectUtils doesn't support {0} function",
    // LC006: "Lyte.arrayUtils doesn't support {0} function",
    // LC007: "Component name not specified in Lyte.Component.render",
    // LC008: "Specified outlet {0} doesn't exists - Lyte.Component.render",
    // LC009: "Method named {0} doesn't exists in {1} component",
    // LC010: "Parent Node / reference Node not provided for insertBefore method",
};
(0,_lyte_utils__WEBPACK_IMPORTED_MODULE_0__.extendEventListeners)(Logger);



/***/ }),

/***/ 70416156:
/*!****************************************************!*\
  !*** ./node_modules/@slyte/core/src/lyte-utils.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "__nestScp__": () => (/* binding */ __nestScp__),
/* harmony export */   "_defineProperty": () => (/* binding */ _defineProperty),
/* harmony export */   "_get": () => (/* binding */ _get),
/* harmony export */   "_lyteDidConnect": () => (/* binding */ _lyteDidConnect),
/* harmony export */   "_lyteInit": () => (/* binding */ _lyteInit),
/* harmony export */   "addEventListener": () => (/* binding */ addEventListener),
/* harmony export */   "addLink": () => (/* binding */ addLink),
/* harmony export */   "addStateToMap": () => (/* binding */ addStateToMap),
/* harmony export */   "arrayUtils": () => (/* binding */ arrayUtils),
/* harmony export */   "bindObj": () => (/* binding */ bindObj),
/* harmony export */   "checkEstablishingSCP": () => (/* binding */ checkEstablishingSCP),
/* harmony export */   "checkNestedProp": () => (/* binding */ checkNestedProp),
/* harmony export */   "checkProperty": () => (/* binding */ checkProperty),
/* harmony export */   "checkWatchPath": () => (/* binding */ checkWatchPath),
/* harmony export */   "cmpData": () => (/* binding */ cmpData),
/* harmony export */   "cmpObjs": () => (/* binding */ cmpObjs),
/* harmony export */   "copyObject": () => (/* binding */ copyObject),
/* harmony export */   "createCustomClass": () => (/* binding */ createCustomClass),
/* harmony export */   "deepCopyObject": () => (/* binding */ deepCopyObject),
/* harmony export */   "defProp": () => (/* binding */ defProp),
/* harmony export */   "defProps": () => (/* binding */ defProps),
/* harmony export */   "defineRelation": () => (/* binding */ defineRelation),
/* harmony export */   "estObjScp": () => (/* binding */ estObjScp),
/* harmony export */   "establishObjectBinding": () => (/* binding */ establishObjectBinding),
/* harmony export */   "establishObserverBindings": () => (/* binding */ establishObserverBindings),
/* harmony export */   "establishWatchScope": () => (/* binding */ establishWatchScope),
/* harmony export */   "extendEventListeners": () => (/* binding */ extendEventListeners),
/* harmony export */   "extendMixin": () => (/* binding */ extendMixin),
/* harmony export */   "extendService": () => (/* binding */ extendService),
/* harmony export */   "getClass": () => (/* binding */ getClass),
/* harmony export */   "getConfig": () => (/* binding */ getConfig),
/* harmony export */   "getCurrentRouterInstance": () => (/* binding */ getCurrentRouterInstance),
/* harmony export */   "getModuleInfo": () => (/* binding */ getModuleInfo),
/* harmony export */   "getProperty": () => (/* binding */ getProperty),
/* harmony export */   "getSuperClass": () => (/* binding */ getSuperClass),
/* harmony export */   "globalsGet": () => (/* binding */ globalsGet),
/* harmony export */   "globalsSet": () => (/* binding */ globalsSet),
/* harmony export */   "includeMixins": () => (/* binding */ includeMixins),
/* harmony export */   "injectServiceToModules": () => (/* binding */ injectServiceToModules),
/* harmony export */   "isEntity": () => (/* binding */ isEntity),
/* harmony export */   "isInheritedClass": () => (/* binding */ isInheritedClass),
/* harmony export */   "many": () => (/* binding */ many),
/* harmony export */   "nestScp": () => (/* binding */ nestScp),
/* harmony export */   "nestScpId": () => (/* binding */ nestScpId),
/* harmony export */   "nestScpRemove": () => (/* binding */ nestScpRemove),
/* harmony export */   "newGetSuperClass": () => (/* binding */ newGetSuperClass),
/* harmony export */   "objectUtils": () => (/* binding */ objectUtils),
/* harmony export */   "observes": () => (/* binding */ observes),
/* harmony export */   "one": () => (/* binding */ one),
/* harmony export */   "prop": () => (/* binding */ prop),
/* harmony export */   "refreshParentRoute": () => (/* binding */ refreshParentRoute),
/* harmony export */   "registerErrorCodes": () => (/* binding */ registerErrorCodes),
/* harmony export */   "removeEventListener": () => (/* binding */ removeEventListener),
/* harmony export */   "removeNestScp": () => (/* binding */ removeNestScp),
/* harmony export */   "removeStateFromMap": () => (/* binding */ removeStateFromMap),
/* harmony export */   "slyteImport": () => (/* binding */ slyteImport),
/* harmony export */   "toAddSuper": () => (/* binding */ toAddSuper),
/* harmony export */   "toBeUsedServices": () => (/* binding */ toBeUsedServices),
/* harmony export */   "triggerEvent": () => (/* binding */ triggerEvent),
/* harmony export */   "types": () => (/* binding */ types),
/* harmony export */   "validateData": () => (/* binding */ validateData)
/* harmony export */ });
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../index.js */ 21046606);
/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../index.js */ 70549578);
/* harmony import */ var _lyte_error__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-error */ 10533647);

// import { errorCodes } from "@slyte/core/src/errors.js";


// import { resolvePromises } from './rsvp';
// const _keywords = {
//   "component" : ["init", "didConnect", "didDestroy", "constructor", "onError", "actions", "methods", "data"],
//   "adapter" : ["namespace", "actionNamespace", "host", "withCredentials", "buildURL", "methodForRequest", "headersForRequest", "reloadRecord", "reloadRecord", "reloadAll", "processRequest", "parseResponse", "parseRequest", "super"],
//   "serializer" : ["normalize", "normalizeResponse", "serialize", "serializeKey", "deserializeKey", "extractMeta", "payloadKey", "serializeRecord", "normalizeRecord", "super"],
//   "route" : ["getDependencies", "getResources", "beforeModel", "model", "afterModel", "redirect", "renderTemplate", "afterRender", "beforeExit", "didDestroy", "queryParams", "title", "routeName", "component", "parent", "currentModel", "forceFetch", "setTitle", "setQueryParams", "getQueryParams", "setDynamicParam", "getDynamicParam", "removeFromCache", "refresh", "transitionTo", "replaceWith","actions"]
// };  
// var arrayUtils =  function() {
//     let lc = window.CrmComponentRegistry._instanceList[0]._getLyteComponent();
//     return lc.aF.apply(lc, arguments);
// };
// var objectUtils =  function() {
//     let lc = window.CrmComponentRegistry._instanceList[0]._getLyteComponent();
//     return lc.oF.apply(lc, arguments);
// }
// var set = function(){
//   return LyteComponent.set.apply(LyteComponent,arguments);
// }
// var get = function(){
//   return LyteComponent.get.apply(LyteComponent,arguments);
// }
// var render = function() {
//   return LyteComponent.render.apply(LyteComponent, arguments);
// }
// var modifyTemplate = function(){
//   return LyteComponent.modifyTemplate.apply(LyteComponent,arguments);
// }
// var compileDynamicTemplate = function(){
//   return LyteComponent.compileDynamicTemplate.apply(LyteComponent,arguments);
// }
// var doDomProcessing = function(){
//   return LyteComponent.doDomProcessing.apply(LyteComponent,arguments);
// }
// var getComponentTemplate = function(){
//   return LyteComponent.getComponentTemplate.apply(LyteComponent,arguments);
// }
// var addLyteEventListener = function(){
//   return Lyte.Component.addLyteEventListener.apply(Lyte.Component,arguments);
// }
// var removeLyteEventListener = function(){
//   return Lyte.Component.removeLyteEventListener.apply(Lyte.Component,arguments);
// }
// function getNearestApp(node){
//     while(node){
//       if(node.component){
//         return node.component.getApp();
//       }
//       node = node.parentElement;
//     }
//     return Lyte._getDefaultAppIns();
// }
function defProp() {
	Object.defineProperty.apply(Object, arguments);
}

function defProps() {
	Object.defineProperties.apply(Object, arguments);
}

function observes(){
  var args = Array.from(arguments), 
  func = args.pop(), 
  properties = [];
  args.forEach(function(observerValue) {
      let valueDetails = observerValue.split("::"), 
      observerType = valueDetails[0], 
      observerTo = valueDetails[1];
      switch(observerType){
          // case "event":{
          //     customCrmComponent._callbacks[observerType].push(observerFunction);
          // }
          // break;
          // case "action":
          // case "method":
          // {
          //     customCrmComponent._callbacks[observerType].push({[observerTo] : observerFunction});
          // }
          // break;
          default :{
              properties.push(observerType);
          }
      }
  })
  if(properties.length){
    var observerObj = {
        properties:properties,
        type:"observer",
        value:func
    }
    return observerObj;
  }
}

// function isKeyword(key, scope){
//   var arr = ["component", "adapter", "serializer", "route"];
//   if(scope){
//     arr = [scope];
//   }
//   var len = arr.length;
//   for(var i=0;i<len;i++){
//     var keyArr = _keywords[arr[i]];
//     if(keyArr){
//       var res = keyArr.indexOf(key);
//       if(res != -1){
//         return true;
//       }
//     }
//   }
//   return false;
// }

function injectServiceToModules(scp, services){
  var mdlObj = scp.$.injectServices;
  for(var mKey in mdlObj){
    var mdl = mdlObj[mKey];
    for(var name in services){
      mdl(name, serv[name], mKey);
    }
  }
}

// function addToInstance(ins) {
//   if(ins.is != "app"){
//     if(!__instances[ins.is].hasOwnProperty(ins.name))
//     {
//       __instances[ins.is][ins.name] = ins;
//     }
//     else{
//       Lyte.warn("L001", ins.is, ins.name);
//     } 
//   }
//   else{
//     if(!__instances[ins.is]){
//       __instances[ins.is] = ins;
//     }
//     else{
//       Lyte.warn("L001", ins.is, "");
//     }
//   }
// }

// function createEngineInstance(engineName,config){
//   var ins = new Lyte('engine',engineName,config);
//   ins = Object.assign(ins,__instances.engine[engineName]);
//   return ins;
// }

function registerErrorCodes(obj, lyte) {
    Object.assign(errorCodes, obj);
}


function isEntity(object){
  if(object && object.$ && object.$.hasOwnProperty("isModified")) {
    return true;
  }
  return false;
}
function getVal(key,target){
  return target && target.__config && (target.__config[key]?"true":(target.__config[key] == false?"false":undefined));
}
function getConfig(key,target){
  if(!target){
    target = LyteCls._instances[0].$router.getRouteInstance();
    return getVal(key,target.$component)||
          ((!target._callee)?getVal(key,target.$app): getConfig(key,target._callee));
  }
  else{
    return getVal(key,target.component)||
          getVal(key,target.$component)||
          ((!target._callee)?getVal(key,target.$app): getConfig(key,target._callee));
  }
}
function getCurrentRouterInstance(){
  return LyteCls._instances[0].$router.getRouteInstance();
}
// function getConfig(key,regIns){
//   var configObj = window.__config;
//   if(configObj){
//       return {value:configObj[key],...((regIns)?{regIns:window.__config.getIns}:{})};
//   }
// }

function triggerEvent() {
  var args = Array.prototype.slice.call(arguments, 1)
  var eventName = arguments[0];
  var stopEvent = false;
  var s = this.__lyteRegisteredEvents[eventName];
    if(!s) {
      s = this.__lyteRegisteredEvents[eventName] = {"listeners" : new Map()};
    } else {
      var mp = s.listeners, keys = Array.from(mp.keys()), kLen = keys.length;    
      for(var i=0;i<kLen;i++) { 
        var func = mp.get(keys[i]);
        if(func) {
           var ret = func.apply(this, args);
           if(ret === false) {
             stopEvent = true;
             break;
           }
        }
      }
    }
    var customEvent = new CustomEvent(eventName, {"detail" : args});
    if(!stopEvent) {
     document.dispatchEvent(customEvent); 
    } 
 }

function addEventListener(eventName, func) {
  if(typeof func !== "function") {
      _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Second parameter to Lyte.Component.addGlobalEventListener() must be a function");
      return;
  }
  var s = this.__lyteRegisteredEvents[eventName];
  if(!s) {
    s = this.__lyteRegisteredEvents[eventName] = {"listeners" : new Map()};
  }
  var id = genMapId(s.listeners);
  var d = s.listeners.set(id, func);
  return eventName + "-" + id;
}

function removeEventListener(id) {
  if(!id) {
    _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("listener unique id not specified");
    return;
  }
   var arr = id.split("-"), evid = arr.pop(), name = arr.join("-");
   var s = this.__lyteRegisteredEvents[name];
   if(!s || !s.listeners.get(evid)) {
     _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("No such listener registered");
     return;
   }
   s.listeners.delete(evid);
}

function genMapId(map){
  var id = Math.floor(Math.random()*10000000000 + 1);
  while(map.get(id)){
      id = Math.floor(Math.random()*10000000000 + 1)
  }
  return id.toString();
}

// function checkNestedProp (id, path, dtype, wobj, object, property, value, check, fromStore) {
//   if (dtype && path.length != 0){
//       var extend;
//       if(Lyte.Transform[dtype.type]){
//           extend = true;
//       }
//       if (dtype.type == 'array' || (extend && Lyte.Transform[dtype.type].extends == "array")) {
//           if (dtype.items && !isNaN(path[0]) && _typeof(dtype.items) == 'object') {
//               path = path.slice(1);
//               checkNestedProp(id, path, dtype.items, wobj, object, property, value, check);
//           }
//       } else if (dtype.type == "object" || (extend && Lyte.Transform[dtype.type].extends == "object")) {
//           if (dtype.properties){
//               var k = path[0];
//               path = path.slice(1);
//               checkNestedProp(id, path, k == "" ? dtype :dtype.properties[k], wobj, object, property, value, check);
//           }
//       }
//   // } else if (dtype.properties && Lyte.isRecord(value) && !check.hasOwnProperty("warn")) {
//   //     check.warn = { warn: true, message: "Nested property will not support for the record" };
//   } 
//   else {
//       var err;
//       if (dtype && (dtype.properties || dtype.items) && !check.hasOwnProperty("warn")) {
//           var component = {};component.__data = {};
//           if (dtype.properties) {
//               component.__data[property] = dtype.properties[property];
//           } else {
//               component.__data[property] = dtype;
//           }
//           var cp = validateData(object, property, value, component);
//           var errKey = wobj.attr ? wobj.attr : wobj.key,_path = wobj.path.split(".");
//           if (cp && (typeof cp === 'undefined' ? 'undefined' : _typeof(cp)) == "object" && cp.code) {
//               cp.value = value;
//               cp.path = wobj.path !=""?wobj.path:property;
//               if(wobj.isRec && fromStore && wobj.key ==  undefined && wobj.path!=""){
//                   errKey = wobj.path.split(".")[0]
//                   _path.shift();
//                   _path = (_path.length == 1 && _path[0] == property) ? [] : _path;
//               }
//               wobj.Error = {};
//               wobj.Error && wobj.Error[errKey] ? wobj.Error[errKey].code && wobj.Error[errKey].nested ? wobj.Error[errKey].nested : wobj.Error[errKey].nested = {} : wobj.Error[errKey] = { nested: {} };
//               wobj.Error[errKey].code = "ERR34"
//               wobj.Error[errKey].message = Logger.errorCodes.ERR34;
//               err = wobj.Error[errKey].nested;
//               wobj._cmpErr[errKey].code = "ERR34";
//               wobj._cmpErr[errKey].message = Logger.errorCodes.ERR34 ;
//               var cmpErrPath = wobj._cmpErr[errKey].path ? wobj._cmpErr[errKey].path : [] ;
//               if(wobj.path !=""){
//                   _path.forEach(function (k) {
//                       if (err && !err[k]) {
//                           err = err[k] = {};
//                       } else {
//                           err = err[k];
//                       }
//                   });
//               }
//               wobj.index != undefined ? err[wobj.index] = cp : err[property] = cp;
//               cmpErrPath.push(wobj.index == undefined && property? wobj.path+"."+property : wobj.index !=undefined ? wobj.path+"."+wobj.index : wobj.path)
//               wobj._cmpErr[errKey].path = cmpErrPath;
//           } else{
//               if(wobj.isRec && fromStore && wobj.key ==  undefined){
//                   errKey = wobj.path.split(".")[0]
//                   _path.shift();
//                   _path = (_path.length == 1 && _path[0] == property) ? [property] : _path;
//               }
//               err = wobj.Error[errKey] && wobj.Error[errKey].nested ? wobj.Error[errKey].nested : undefined;
//               //var p = object.$.error[property];
//               if(err){
//                   var key = wobj.index || property; 
//                   var flag = true,
//                   p = wobj.path !=""?_path:[property];
//                   wobj.index ? p.push(wobj.index) : p;
//                   var prev;
//                   p.forEach(function (r) {
//                       if (err[r] && err[r].code) {
//                           delete err[r];
//                           if(err && Object.keys(err).length == 0){
//                               flag = true;
//                           }
//                           else{
//                               flag = false;
//                           }
//                       }
//                       err = err[r];
//                   });
//                   if (flag == true && wobj.Error[errKey] && wobj._cmpErr[errKey]) {
//                       delete wobj.Error[errKey];
//                       delete wobj._cmpErr[errKey]
//                   }
//               }

//           }
//           if((check.value && check.value.hasOwnProperty("code") && cp.code) || !check.value){
//               check.value = cp;
//           }
//       }
//   }
//   wobj.path && delete wobj.path;
//   wobj.index && delete wobj.index;
//   wobj.attr && delete wobj.attr;
// };

function checkNestedProp(id,path,dtype,wobj,object,property,value,check){
  if (dtype.type instanceof Function){
    dtype = dtype.type;
  }
  if(path.length!=0){
      if(dtype.type == 'array'){
          if(dtype.items && !isNaN(path[0]) && typeof(dtype.items)=='object'){
              path= path.slice(1);
              checkNestedProp(id,path,dtype.items,wobj,object,property,value,check)
          }
      }
      else if( dtype.type == "object"){
          if(dtype.properties && dtype.properties.hasOwnProperty(path[0])){
              var k = path[0];
              path = path.slice(1);
              checkNestedProp(id,path,dtype.properties[k],wobj,object,property,value,check);
          }
      }
  }
  else {
    var err;
    if (dtype && (dtype.properties || dtype.items) && !check.hasOwnProperty("warn")) {
      var component = {};component.__data = {};
      if (dtype.properties) {
          component.__data[property] = dtype.properties[property];
      } else if(dtype.items) {
        component.__data[property] = dtype.items;      
      } else
      {
          component.__data[wobj.index] = dtype;
      }
      var cp = validateData(object, property, value, component);
      var errKey = wobj.attr ? wobj.attr : wobj.key,_path = wobj.path.split(".");
      var PathWay = wobj.index == undefined && property? wobj.path+(wobj.path!="" ?".":"")+property : wobj.index !=undefined ? wobj.path+(wobj.path!="" ?".":"")+wobj.index : wobj.path;
      if (cp && (typeof cp === 'undefined' ? 'undefined' : typeof(cp)) == "object" && cp.code) {
          cp.value = value;
          cp.path = wobj.path !=""?wobj.path:property;
          // if(wobj.isRec && fromStore && wobj.key ==  undefined && wobj.path!=""){
          //     errKey = wobj.path.split(".")[0]
          //     _path.shift();
          //     _path = (_path.length == 1 && _path[0] == property) ? [] : _path;
          // }
          // wobj.Error ? true : wobj.Error = {}
          wobj.Error && wobj.Error[errKey] ? wobj.Error[errKey].code && wobj.Error[errKey].nested ? wobj.Error[errKey].nested : wobj.Error[errKey].nested = {} : wobj.Error[errKey] = { nested: {} };
          wobj.Error[errKey].code = "ERR34"
          wobj.Error[errKey].message = _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.errorCodes.ERR31;
          err = wobj.Error[errKey].nested;
          wobj._cmpErr[errKey] ? true : wobj._cmpErr[errKey]={};
          wobj._cmpErr[errKey].code = "ERR34";
          wobj._cmpErr[errKey].message = _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.errorCodes.ERR31;
          var cmpErrPath = wobj._cmpErr[errKey].path ? wobj._cmpErr[errKey].path : [] ;
          if(wobj.path !=""){
              _path.forEach(function (k) {
                  if (err && !err[k]) {
                      err = err[k] = {};
                  } else {
                      err = err[k];
                  }
              });
          }
          wobj.index != undefined ? err[wobj.index] = cp : err[property] = cp;
          if(!cmpErrPath.includes(PathWay)){
            cmpErrPath.push(PathWay)
          }
          wobj._cmpErr[errKey].path = cmpErrPath;
      } else{
          // if(wobj.isRec && fromStore && wobj.key ==  undefined){
          //     errKey = wobj.path.split(".")[0]
          //     _path.shift();
          //     _path = (_path.length == 1 && _path[0] == property) ? [property] : _path;
          // }
          err = wobj.Error[errKey] && wobj.Error[errKey].nested ? wobj.Error[errKey].nested : undefined;
          var cmpErr = wobj._cmpErr[errKey] || undefined;
          if(cmpErr && cmpErr.path && cmpErr.path.includes(PathWay)){
            var ind = cmpErr.path.indexOf(PathWay)
            cmpErr.path.splice(ind,1)
          }
          //var p = object.$.error[property];
          if(err){
              var key = wobj.index || property; 
              var flag = true,
              p = PathWay.split(".");
              p.forEach(function (r) {
                  if (err[r] && err[r].code) {
                      delete err[r];
                      if(err && Object.keys(err).length == 0){
                          flag = true;
                      }
                      else{
                          flag = false;
                      }
                  }
                  else{
                    err = err[r]
                  }
              });
              if (flag == true && wobj.Error[errKey] && wobj._cmpErr[errKey]) {
                  delete wobj.Error[errKey];
                  delete wobj._cmpErr[errKey]
              }
          }

      }
      if((check.value && check.value.hasOwnProperty("code") && cp.code) || !check.value){
          check.value = cp;
      }
  }
}
  wobj.index && delete wobj.index; 
}

var types = ["string", "object", "number", "boolean", "array", "component"];
function prop(type, opts){
  var obj = {};
  obj.type = type;
  obj._type = "prop";
  if(opts == undefined){
    opts = {};
  }
  Object.assign(obj,opts);
  return obj;
}

function one(name,opts){
  return defineRelation(name,"belongsTo",opts);
}

function many(name,opts){
  return defineRelation(name,"hasMany",opts);
}

function extendEventListeners(scp){
  if(scp && !scp.hasOwnProperty("__lyteRegisteredEvents")){
      Object.defineProperties(scp, {
          __lyteRegisteredEvents : {
              value : {}
          },
          addEventListener : {
              value : addEventListener
          },
          removeEventListener : {
              value : removeEventListener
          },
          triggerEvent : {
              value : triggerEvent
          }
      });
  }
}

function getSuperClass(obj, getName){
	var cons = obj, isInstance = (typeof obj === "object");
	if(isInstance){
		cons = obj.constructor;
	}
	var bClass = Object.getPrototypeOf(cons);
	if((!(bClass instanceof Function)) || bClass === _index_js__WEBPACK_IMPORTED_MODULE_2__.Service){
		if(getName){
			return obj.name;
		}
		return obj;
	} 
	return getSuperClass(bClass, getName);
}

function isInheritedClass(baseObj, inheritedClass){
  var cons = baseObj, isInstance = (typeof obj === "object");
  if(isInstance){
    cons = baseObj.constructor;
  }
  var bClass = Object.getPrototypeOf(cons);
  if((bClass.hasOwnProperty("__lyteOrigClass") && bClass.__lyteOrigClass == inheritedClass) || (bClass == inheritedClass)){
    return true;
  }
  else if((!(bClass instanceof Function)) || bClass === _index_js__WEBPACK_IMPORTED_MODULE_2__.Service){
    return false;
  }  
  return isInheritedClass(bClass, inheritedClass); 
}

function newGetSuperClass(obj, getName, toBeCompared, getClass) {
  var cons = obj, isInstance = (typeof obj === "object");
  if(isInstance){
    cons = obj.constructor;
  }
  var bClass = Object.getPrototypeOf(cons);
  if(bClass.__lyteOrigClass == toBeCompared){
    return true;
  }
  else if((!(bClass instanceof Function)) || bClass === _index_js__WEBPACK_IMPORTED_MODULE_2__.Service){
    // return false;
    if(getName){
      return obj.name;
    } else if(getClass){
      return obj;
    }
    return obj;
  }  
  return newGetSuperClass(bClass, getName, toBeCompared);    
}

function includeMixins(arr,mainClass){
  if(arr.length == 0) {
    return mainClass;
  } 
  var currentClass = mainClass;
  for(var i=0;i<arr.length;i++) {
    var classVar = arr[i];
    var classFunction = classVar._classFunc;
    currentClass = classFunction(currentClass, classFunction.overrides,classFunction._proxy);
  }
  return currentClass;
}
function getClass(arr, mainClass,clsObj) {
  if(arr.length == 0) {
      return mainClass;
  }
  var currentClass = mainClass;
  for(var i=0;i<arr.length;i++) {
    var classVar = arr[i];
    var classFunction = classVar._classFunc;
    currentClass = classFunction(currentClass, classFunction.overrides,classFunction._proxy);
  }

  return currentClass;
}
Object.defineProperty(Object.prototype,"getMixinPropertyNames",{
  value:function(){
    return Object.getOwnPropertyNames(this);
  }
});

Function.prototype.tempApply=Function.prototype.apply;
Function.prototype.tempCall=Function.prototype.call;
Function.prototype.tempBind=Function.prototype.bind;
var classObj = {};
function createCustomClass(classDefFunc, fromInside) {

  class LyteModule {
      constructor() {
          if(window.ssss1) {
              this.constructor = window.ssss1;
              window.ssss1 = undefined;
          }
      }
    static actions(arg1) {
      return arg1 ? arg1 : {};
    }
    static methods(arg1) {
      return arg1 ? arg1 : {};
    }
    static observers(arg1) {
      return arg1 ? arg1 : {};
    }
    data(arg1) {
      return arg1 ? arg1 : {};
    }
   
  }
    var dummyObj={};

    var proxy = new Proxy(class {
        static [Symbol.hasInstance](instance) {  // checks if the object's prototype chain contains the [Symbol.hasInstance] method
          console.log("inside  custom instanceof check")
          return instance instanceof dummyObj.arg1._initialCopyClass;
        }
      },{
      "construct" : function(a,b,c) {
        if(c==proxy) {
          window.ssss1 = dummyObj.retClass;
          let constr=Reflect.construct(dummyObj.retClass._initialCopyClass, b);
          if(constr._afterConstructor){
            constr._afterConstructor();
          }
          return constr;
        } else {
          return Reflect.construct(dummyObj.arg1, b,c);
        }
          
      }, 
      "get" : function(target, prop,value) {
        if(prop == "prototype") {
          return Reflect.get(...arguments);
        } else if(prop == "hasOwnProperty" ) {
          return function(prop1) {
              return dummyObj.arg1.hasOwnProperty(prop1);
          }  
        }
        else if (prop=="getMixinPropertyNames"){
          return function(prop1){
            return Object.getOwnPropertyNames(dummyObj.arg1);
          }
        }
        else if(dummyObj.arg1.hasOwnProperty(prop)) {
          return dummyObj.arg1[prop];
        }
        else if(prop.toString()==='Symbol(Symbol.hasInstance)'){
          return Reflect.get(target, prop,value);
        } 
        else {
          return Reflect.get(dummyObj.arg1, prop,value);
        }
      }, 
      "set" : function(target, prop, value, receiver) {
        dummyObj.arg1._updateStaticVal(prop, value, receiver);
          return true;
      }, 
      "getPrototypeOf": function(target){
        return Reflect.getPrototypeOf(dummyObj.arg1); 
      },
      "defineProperty": function(target, key, descriptor) {
        dummyObj.arg1._defineProperty(target,key,descriptor);
          return true;
        },
        
      "getOwnPropertyDescriptor":function(target,prop){
          if( Object.hasOwn(dummyObj.arg1,prop)){
            return {
              value:dummyObj.arg1[prop],
              enumerable : true,
              configurable : true,
              writable : true
          };
          }
          else {
            return Reflect.getOwnPropertyDescriptor(...arguments)  ;
          }
        } ,
        "deleteProperty":function(target,prop,value){
          return dummyObj.arg1._deleteProperty(target,prop);
        }
      });
  var retClass = classDefFunc(LyteModule, function(arg1,cacheObj) {
      if(arg1.name[0]=='_'){
          Object.defineProperty(arg1,"name",{value:arg1.name.slice(1)});
      }
      dummyObj.arg1 = arg1;
      arg1._lyteClasses = [];
    arg1._lyteClasses = [];  //keep track of classes that have been extended from retClass.
  
    arg1._updatePrototypeVal = function(prop, value) {
       arg1.prototype[prop]=value;
       this._lyteClasses.forEach(function(item){
        item.prototype[prop]=value;
       });  
  }
  arg1._deleteProperty=function(target,prop){
    if(arg1[prop]){
      delete arg1[prop];
      this._lyteClasses.forEach(function(item){
        delete item[prop];
      })
    }
    delete arg1.prototype[prop];
    this._lyteClasses.forEach(function(item){
      delete item.prototype[prop];
    });
  }

  arg1._defineProperty = function(target, prop, descriptor) {
  Object.defineProperty(arg1,prop,descriptor);
    this._lyteClasses.forEach(function(item) {
      if(descriptor==undefined){
        item.prototype[prop]=undefined;
      }else{
        Object.defineProperty(item, prop, descriptor);
      }
    });

  }

  arg1._updateStaticVal = function(prop, value, receiver) {
    //updates the static values of arg1 and all the classes in _lyteClasses with the provided prop and value.
    // arg1[prop]=value;
    this[prop] = value;
    if(cacheObj && cacheObj.hasOwnProperty(prop)){
      cacheObj[prop]=value;
    }
    this._lyteClasses.forEach(function(item) {
      item[prop] = value;
    });
  }
  arg1._hasOwnProperty=function(target,prop,receiver){
    return arg1.prototype.hasOwnProperty(prop)
  }
  arg1._getMixinPropertyNames=function(target,prop,receiver){
    return Object.getOwnPropertyNames(arg1.prototype);
  }

     let tempProxy=new Proxy({} /*proxy.prototype*/, {

      "get" : function(target, prop,receiver) {
        //  if(Object.hasOwn(arg1._initialCopyClass.prototype,prop))
        //    return arg1.prototype[prop];
        if(prop === "deleteProp") {
            return function(prop1) {
                 return arg1._deleteProperty(target, prop1, undefined);
                 };
          }
          else if(prop=="hasOwnProperty"){
            return function(prop1){
                return arg1._hasOwnProperty(target,prop1);
            }
        }
        else if (prop=="getMixinPropertyNames"){
          return function(prop1){
            return arg1._getMixinPropertyNames(target,prop1,receiver);
          }
        }
        else {
          if(arg1.prototype[prop] && typeof arg1.prototype[prop]=="function" ) {
              arg1.prototype[prop].apply = function() {
                  let tempArgument=arguments[0];
                  while(tempArgument){
                      if(arg1._proxy==tempArgument.constructor){
                          return arg1._initialCopyClass.prototype[prop].tempApply(tempArgument, arguments[1]);
                      }
                      tempArgument=Object.getPrototypeOf(tempArgument);
                  }
                return  arg1.prototype[prop].tempApply(arguments[0],arguments[1]);
              }
              arg1.prototype[prop].call = function() {
                  let tempArgument=arguments[0];
                  while(tempArgument){
                      if(arg1._proxy==tempArgument.constructor){
                          return arg1._initialCopyClass.prototype[prop].tempCall(tempArgument, arguments[1]);
                      }
                      tempArgument=Object.getPrototypeOf(tempArgument);
                  }
                return  arg1.prototype[prop].tempCall(arguments[0],arguments[1]);
              }
              arg1.prototype[prop].bind = function() {
                  let tempArgument=arguments[0];
                  while(tempArgument){
                      if(arg1._proxy==tempArgument.constructor){
                          return arg1._initialCopyClass.prototype[prop].tempBind(tempArgument, arguments[1]);  
                      }
                      tempArgument=Object.getPrototypeOf(tempArgument);
                  }
                return  arg1.prototype[prop].tempBind(arguments[0],arguments[1]);
              }
          }
            return arg1.prototype[prop];
      }
          
      }, 
      "set" : function(target, prop, value) {    
          arg1._updatePrototypeVal(prop, value);
          return true;
      }, 
      "defineProperty" : function(target, prop, value) {
        arg1._defineProperty(target, prop, value);
      },
      "getPrototypeOf": function(target){
        return Reflect.getPrototypeOf(arg1); 
      }, 
  })  ;
   Object.setPrototypeOf(proxy.prototype,tempProxy);                                        
  return proxy;
  },proxy);
  retClass._proxy = proxy;
  dummyObj.retClass = retClass;
  classDefFunc._proxy = proxy;
  
  // var initialCopyClass = classDefFunc(LyteModule, overrides,proxy);
  var initialCopyClass=overrides(dummyObj.arg1);
  function overrides(cls) {
      if(cls.name[0]=='_'){
          Object.defineProperty(cls,"name",{value:cls.name.slice(1)});
      }
      retClass._lyteClasses.push(cls);
      cls.prototype.__origConstructor = cls;
      cls.__lyteOrigClass = retClass;
      if(retClass._lyteClasses.length>1){
          let baseCls=retClass._lyteClasses[0];
          let arr=Object.getOwnPropertyNames(retClass._lyteClasses[0]);
          for(let i=0;i<arr.length;i++){
              if(!cls.hasOwnProperty(arr[i])){
                  cls[arr[i]]=baseCls[arr[i]];
              }
          }
      let arr1=Object.getOwnPropertyNames(retClass._lyteClasses[0].prototype);
          for(let i=0;i<arr1.length;i++){
              if(!cls.prototype.hasOwnProperty(arr1[i])){
                  cls.prototype[arr1[i]]=baseCls.prototype[arr1[i]];
              }
          }
      }
    return cls;
  }


  classDefFunc.overrides = overrides;
  retClass._classFunc = classDefFunc;
  initialCopyClass._classFunc=classDefFunc;   //new changes
  // retClass._initialCopyClass = initialCopyClass; //new changees
  dummyObj.arg1._initialCopyClass = initialCopyClass;
  classDefFunc._mainClass = retClass;
  if(initialCopyClass.name[0]=='_'){
      Object.defineProperty(initialCopyClass,"name",{value:initialCopyClass.name.slice(1)});

  }   
  return retClass;
}

function getCustomData(scp, fieldVal, fromDb){
  if(fromDb){
    if(scp && scp.dataType && scp.dataType.hasOwnProperty(fieldVal)){
      return scp.dataType[fieldVal];
    }
    scp = scp.lyte;
  }
  if(scp && scp.dataType && scp.dataType.hasOwnProperty(fieldVal)){
    return scp.dataType[fieldVal];
  }
}
function checkProperty(property, dataVal, key, fieldVal, record, name, scp, errorCodes ,init, field, fromDb){
  // var errorCodes = scp.errorCodes;
  var exts = "extends";
  switch(property){
    case "type" : errorCodes
      if(typeof fieldVal !== "string"){
        var _fld = getCustomData(scp, fieldVal._name, fromDb);
        if (_fld && dataVal !== undefined && dataVal !== null) {
          var _ret = true;
          if (Array.isArray(dataVal)) {
              if (_fld.type != "array") {
                  return { code: "ERR03", message: errorCodes.ERR03, expected: fieldVal };
              }
              if(_fld.hasOwnProperty("items")){
                  _ret = checkProperty("items", dataVal, key, _fld.items, undefined, undefined, scp, errorCodes);
              }
          }else if(typeof dataVal === "object" && _fld.type == "object" && _fld.hasOwnProperty("properties")){
              _ret = checkProperty("properties", dataVal, key, _fld.properties, undefined, undefined, scp, errorCodes);
          } 
          else if (fieldVal && _fld.type != typeof(dataVal)) {
            return { code: "ERR03", message: errorCodes.ERR03, expected: fieldVal };
          }
          if(_ret !== true){
              return Object.assign(_ret, { code:"ERR31", message:errorCodes.ERR31, value: dataVal});
          }
        }
      }
      else if(dataVal !== undefined && dataVal !== null){
        if(Array.isArray(dataVal)){
          if(fieldVal != "array"){
              return {code : "ERR03", message : errorCodes.ERR03, expected : fieldVal};
          }
        }
        else if(dataVal && dataVal._compName){
          if(fieldVal != "component"){
            return {code : "ERR03", message : errorCodes.ERR03, expected : fieldVal};
          }
        }
        else if(fieldVal != (typeof dataVal)){
          return {code : "ERR03", message : errorCodes.ERR03, expected : fieldVal};
        }
      }
      break;
    case "mandatory":
        let validateFlag = fieldVal;
        if(typeof fieldVal == "object"){
          if(init && fieldVal.skipValidationOnInit == true){
            validateFlag = false;
          }
        }
        if (validateFlag && (dataVal == undefined || dataVal == null || dataVal === "")) {
          return { code: "ERR02", message: errorCodes.ERR02 };
        }
      break;
    case "maximum" :
      if((typeof dataVal == "number") && dataVal > fieldVal){
        return {code : "ERR04", message : errorCodes.ERR04, expected : fieldVal};
      }
      break;
    case "minimum" :
      if((typeof dataVal == "number") && dataVal < fieldVal){
        return {code : "ERR05", message : errorCodes.ERR05, expected : fieldVal};
      }
      break;
    case "maxLength" :
    case "maxItems" :
      if(dataVal && dataVal.length > fieldVal){
        return {code : "ERR06", message : errorCodes.ERR06, expected : fieldVal};
      }
      break;
    case "minLength" :
    case "minItems" :
      if(dataVal && dataVal.length < fieldVal){
        return {code : "ERR07", message : errorCodes.ERR07, expected : fieldVal};
      }
      break;
      case "pattern" :
        if(typeof fieldVal == "string"){
            var _scp = scp;
            if(fromDb){
              _scp = scp.lyte;
            }
            if(_scp){ 
              if(_scp.patterns && _scp.patterns.hasOwnProperty(fieldVal)){
                fieldVal = _scp.patterns[fieldVal];
              }
              else{
                _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.warn("Pattern with name - "+ fieldVal+" is not registered");
                break;
              }
            }
          }
          if( typeof dataVal == "string" && !(new RegExp(fieldVal).test(dataVal))){
            return {code : "ERR08", message : errorCodes.ERR08, expected : fieldVal};
          }
          break;
      case "uniqueItems" :{
      if(Array.isArray(dataVal) && fieldVal){
        var newArr = [];
        for(var i=0; i<dataVal.length; i++){
          var val = dataVal[i];
          if(newArr.indexOf(val) != -1){
            return {code : "ERR09", message : errorCodes.ERR09};
          }
          newArr.push(val);
        }         
      }
      break;        
    }
    case "constant" :
      if(Array.isArray(dataVal)){
        var resp = dataVal.length==fieldVal.length && dataVal.every(function(v,i) { return v === fieldVal[i]});
        if(!resp){
          return {code : "ERR10", message : errorCodes.ERR10, expected : fieldVal};
        }
      }
      else if(typeof dataVal == "object"){
        var resp = db.connector.$.compareObjects(dataVal, fieldVal);
        if(!resp){
          return {code : "ERR10", message : errorCodes.ERR10, expected : fieldVal};
        }
      }
      else if(dataVal && dataVal != fieldVal){
        return {code : "ERR10", message : errorCodes.ERR10, expected : fieldVal};
      }
      break;
    case "items" :{
      if(Array.isArray(dataVal)){
        var _resp = true;
        for(var i=0; i<dataVal.length; i++){
          for(var property in fieldVal){
            var resp = checkProperty(property, dataVal[i], i, fieldVal[property], undefined, undefined, scp, errorCodes, undefined, fieldVal);
            if(resp != true){
              var _path = resp.path || [""], plen = _path.length;
              for(var l=0; l<plen; l++){
                var path = _path[l];
                path = path ? i + "." + path : i.toString();
                if(_resp !== true){
                  _resp.path.indexOf(path) == -1 ? _resp.path.push(path) : undefined; 
                }
                else{
                  _resp = { path: [path]};
                }
              }
              break;
            }
          }
        }
        return _resp;         
      }
      break;        
    }
    case "properties" :
      if(typeof dataVal == "object" && !Array.isArray(dataVal)){
        var _resp = true
        for (var key in dataVal) {
          if(fieldVal.hasOwnProperty(key)){
              var fld = fieldVal[key]; 
              for (var property in fld) {
                  var resp = checkProperty(property, dataVal[key], key, fld[property], undefined, undefined, scp, errorCodes, undefined, fld);
                  if (resp != true) {
                    var _path = resp.path || [""], plen = _path.length;
                    for(var l=0; l<plen; l++){
                      var path = _path[l];
                      path = path ? key + "." + path : key.toString();
                      if(_resp !== true){
                        _resp.path.indexOf(path) == -1 ? _resp.path.push(path) : undefined; 
                      }
                      else{
                        _resp = { path: [path]};
                      }
                    }
                    break;
                  }
              }
          }
          // else if(field.exact){
            //     return { code : "ERR29", message: errorCodes.ERR29, property: key };
            // }
          }
          return _resp;
      }
      break;
    case "validation" :{
      var validator = fieldVal;
      if(validator){
        var resp =  validator.validate.apply(record, [key, dataVal, name]);
        if(resp != true){
          return resp;
        }
      }       
    }
    break;
    case "instanceof": 
    {
        if(typeof dataVal === "object" && !Array.isArray(dataVal) && !(dataVal instanceof fieldVal)){
          return { code: "ERR30", message: errorCodes.ERR30, property: key, instanceof: fieldVal};
        }
    }
  }
  return true;
}

function validateData(object, key, value, component,scp ,init) {
  var definition = component.__data[key];
  var isError = false;
  var type = definition ? definition.type : undefined;
  for(var defKey in definition) {
    let app = component.getApp ? component.getApp() : undefined;
    isError =  checkProperty(defKey, value, key, definition[defKey], object, undefined, app, _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.errorCodes, init, definition);
    if(isError !== true) {
      return isError;
    }
  }
  return false;
}

function deepCopyObject(obj){
  return copyObject(obj, true);
}

function copyObject( obj, internal )  {
  var targetVal = Array.isArray(obj) ? [] : Object.create(Object.getPrototypeOf(obj));
  var current, copies = [{source : obj, target : targetVal}], keys, propertyIndex, descriptor, nextSource, indexOf, sourceReferences = [obj];
  var cloneObject = copies[0].target, targetReferences = [cloneObject];
  while(current = copies.shift()){
      keys = Object.keys(current.source);
      if(current.source instanceof Map){
        keys = current.source.keys().toArray()
        for(MapKeys = 0 ; MapKeys < keys.length ; MapKeys++){
            if( typeof current.source.get(keys[MapKeys]) != "object"){
              current.target.set(keys[MapKeys] , current.source.get(keys[MapKeys])) 
            }else{
              current.target.set(keys[MapKeys],copyObject(current.source.get(keys[MapKeys]))) 
            }
        }
        continue;
      }
      for(propertyIndex = 0; propertyIndex < keys.length; propertyIndex++){
          descriptor = Object.getOwnPropertyDescriptor(current.source, keys[propertyIndex]);
          if(!descriptor){
              continue;
          }
          if(!descriptor.value || typeof descriptor.value != "object"){
              Object.defineProperty(current.target, keys[propertyIndex], descriptor);
              continue;
          }
          nextSource = descriptor.value;
          if(!(descriptor.value instanceof Promise)){
        descriptor.value = Array.isArray(nextSource) ? [] : nextSource instanceof Set ? new Set() : nextSource instanceof Map ? new Map() : Object.create(Object.getPrototypeOf(nextSource));
          }
          indexOf = sourceReferences.indexOf(nextSource);
          if(indexOf != -1){
              descriptor.value = targetReferences[indexOf];
              Object.defineProperty(current.target, keys[propertyIndex], descriptor);
              continue;
          }
          sourceReferences.push(nextSource);
          targetReferences.push(descriptor.value);
          Object.defineProperty(current.target, keys[propertyIndex], descriptor);
          copies.push({source : nextSource, target : descriptor.value});
      }
      if(internal){
        if(isEntity(current.source)){
          Object.defineProperty(current.target, "$", {
            value: {}
          });
          current.target.$.isNew = current.source.$.isNew;
          current.target.$.isModified = current.source.$.isModified;
          current.target.$.isDeleted = current.source.$.isDeleted;
          current.target.$.pK = current.source.$.pK;
          current.target.$._attributes = current.source.$._attributes;
          current.target.$.isCloned = true;
          if(current.source.$.hasOwnProperty("deepNest")){
            current.target.$.deepNest = current.source.$.deepNest;
          }
          if(current.source.$.hasOwnProperty("partialType")){
            current.target.$.partialType = current.source.$.partialType;
          }
        }
        if(Array.isArray(current.source)){
          if(current.source.partial){
            Object.defineProperty(current.target, "partial", {
              value: current.source.partial
            });
          }
        }
      }
  }
  return cloneObject;
}

function defineRelation(name,type,opts){
  var relation = {type : "relation", relType : type, relatedTo : name};
  if(opts){
    relation.opts = opts;
  }
  return relation;
}

function establishObserverBindings(observers,fromStore,properties,model,lyteScp,_LC) {
  var scope = this;
  var watchProps = model && fromStore ? model._fldGrps.JsonPathWatch : scope.constructor._deepWatchProperties;
  if(fromStore){
    scope = fromStore;      
  }
  for(var i=0;i<observers.length;i++) {
    var props = observers[i].properties;
    var obsAttr = {},obsDuplicate=false;
    var Jpath={}
    for(var j=0;j<props.length;j++) {
      var actProp;
      var isArrayObserver = false;
      var isObjectObserver = false;
      if(typeof props[j] == "string"){
        if(_LC && _LC.freeze && scope.component && _LC.freeze.prop(scope, props[j])){
          continue;
        }
        if(props[j].search(/^\$\./g)!=-1){
         
          var JsonPath = props[j];

          var CmpPropertyPath = JsonPath.match(/[^\$.][^\.]*/g)[0]
          if(CmpPropertyPath.search(/\[[0-9*]\]/g)!=-1){
            CmpPropertyPath = CmpPropertyPath.split(/\[[0-9*]\]/g)[0];
          }
          if(watchProps[CmpPropertyPath]==undefined){
            watchProps[CmpPropertyPath]=[];
          }

          if(Jpath[CmpPropertyPath] == undefined){
            Jpath[CmpPropertyPath]=[];
          }
          
          var JSONPATH ;
          var reg = "^\\$\\."+CmpPropertyPath+"\\.?"
          reg = new RegExp(reg);
          JSONPATH = props[j].replace(reg,"$.")
          if(!watchProps[CmpPropertyPath].includes(JSONPATH)){
            watchProps[CmpPropertyPath].push(JSONPATH)
          }
          
          if(!Jpath[CmpPropertyPath].includes(JSONPATH)){
            Jpath[CmpPropertyPath].push(JSONPATH)
          }


          CmpPropertyPath =CmpPropertyPath+".*";
          if(!obsAttr.hasOwnProperty(CmpPropertyPath)){
            actProp= getProperty.call(this,CmpPropertyPath,fromStore,properties)
            obsAttr[CmpPropertyPath]=true;
            obsDuplicate = false;
          }
          else{
            obsDuplicate = true;
          }
        }
        else if(props[j].indexOf('.[]') !== -1) {
          isArrayObserver = true;
          actProp = getProperty.call(this,props[j].substring(0, props[j].indexOf('.[]')),fromStore,properties);
        }
        else if(props[j].indexOf('.{}') !== -1) {
          isObjectObserver = true;
          let objObbName = props[j].substring(0, props[j].indexOf('.{}'))
          actProp = this.getProperty(objObbName);
          Object.defineProperty(actProp, '_objectObservers', {
            value: true,
            enumerable: false,
            writable: true,
            configurable: true
          });
			  }
        else {
          if(props[j].indexOf('.*') !== -1) {
            var prop = props[j].split(".")[0];
            var isDeepObs = (!fromStore && this.component.__data[prop] && this.component.__data[prop].watch) || (fromStore && model && model.fieldList && model.fieldList[prop] && model.fieldList[prop].watch) ? true : false;
            if(!isDeepObs){
              continue;
            }
          } 
          actProp = getProperty.call(this,props[j],fromStore,properties);
        }
      if(!obsDuplicate){
        if(!actProp._observers) {
          Object.defineProperty(actProp, '_observers', {
            value : new Set(),
            enumerable: false, 
            writable: true,
            configurable: true
          });
        }
       actProp._observers.add({callee : scope, observer: observers[i], isArrayObserver : isArrayObserver,isObjectObserver:isObjectObserver });
      }
    }else{
      _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.warn.call(lyteScp,"ERR27",JSON.stringify(props[j]),observers[i].fnName,this.tagName);
    }
  }
  observers[i].Jpath  = Jpath;
  obsAttr={};
}
}
// getProperty(key) {
//   var arr = key.match(/([^[\].]+|\[\])/g);
// let property = this;
// if(!property._properties[arr[0]]) {
//       property._properties[arr[0]] = {};
//   } 
//   property = property._properties[arr[0]];

// defProp(property, '_path', {enumerable: false, value : arr[0]});
// for(let i=1;i<arr.length;i++) {
//       if (arr[i].startsWith("'") || arr[i].startsWith('"')) {//added check
//           arr[i] = arr[i].substring(1, arr[i].length -1);
//       }
//       if(!property[arr[i]]) {
//           property[arr[i]] = {};
//           defProp(property[arr[i]], '_path', {enumerable: false, value : property._path + "." + arr[i]});
//       }
//     property = property[arr[i]];
//   }
//  return property;
// }
function getProperty(key,fromStore,properties) {
  var arr = key.match(/([^[\].]+|\[\])/g);
  let property = this;
  if(fromStore){
    property = properties;
    if(!properties[arr[0]]){
        properties[arr[0]] = {};
    }
    property = properties[arr[0]];
  }
  else {                      
    if(!property._properties[arr[0]]) {
        property._properties[arr[0]] = {};
    } 
    property = property._properties[arr[0]];
  }

  Object.defineProperty(property, '_path', {enumerable: false, value : arr[0]});
  for(let i=1;i<arr.length;i++) {
    if (arr[i].startsWith("'") || arr[i].startsWith('"')) {//added check
      arr[i] = arr[i].substring(1, arr[i].length -1);
    }
    if(!property[arr[i]]) {
        property[arr[i]] = {};
        Object.defineProperty(property[arr[i]], '_path', {enumerable: false, value : property._path + "." + arr[i]});
    }
    property = property[arr[i]];
  }
  return property;
}

let nestScpId = 1;
const nestScp = {};
const __nestRef__ = {};
const __nestScp__ = new Map();
function establishObjectBinding(data, attr, fromStore, update, storeRecord, watch) {
  var model, fld, nestObj,customDtype;
  var checkAttrs=data.__component__ && data.__component__.component.__data?data.__component__.component.__data[attr]:undefined, db;
  if (fromStore || storeRecord) {
    if(data.$ && data.$.schema){
      model = data.$.schema;
      db = model.db;
    }
      fld = model && model.fieldList && model.fieldList.hasOwnProperty(attr)? model.fieldList[attr] : {};
      if(fld){
        var _checkDtype = false ; 
        if(fld.type && fld.type instanceof Function){
          var _dtype = fld.type;
          if(_dtype.hasOwnProperty("properties") || _dtype.hasOwnProperty("items")){
            _checkDtype = true;
          }
        }
      }
      if (fld && (fld.type !== "relation" && fld.watch) || _checkDtype){
        checkAttrs=fld;
        watch = fld.watch ? fld.watch : watch;
    }
    else{
      if(data._scp){
         var keys = Array.from(data._scp.keys());
          keys.forEach(function (id) {
            var _mpObj = data._scp.get(id), 
            mpObj = _mpObj.paths;
            var _dtype = nestScp[id] || undefined;
            watch = _dtype && _dtype.watch?_dtype.watch:watch;
              for (var key in mpObj) {
                  var path = key ? key.split(".") : [];
                  path.push(attr);
                  bindObj(data, attr, id, path, new Map(),undefined,checkAttrs,watch,true);
              }
         });
      }
     return;
   }
  }
  if (update && data._scp && data._scp.size) {
      var keys = Array.from(data._scp.keys());
      keys.forEach(function (id) {
        var _mpObj = data._scp.get(id), 
        mpObj = _mpObj.paths;
        var _dtype = nestScp[id] || undefined;
        watch = _dtype && _dtype.watch?_dtype.watch:watch;
          for (var key in mpObj) {
            var path = key ? key.split(".") : [];
            path.push(attr);
            bindObj(data, attr, id, path, new Map(),checkAttrs,watch , true);
          }
      });
  }
  var customDtype = false;
  if(checkAttrs && checkAttrs.type && checkAttrs.type instanceof Function){
    var _dt = checkAttrs.type;
    if(_dt.hasOwnProperty("properties") || _dt.hasOwnProperty("items")){
     customDtype = true;
      checkAttrs = _dt; 
    }
  }
  if ((!fromStore || fld.watch || fld.properties || fld.items || watch || customDtype) && data[attr]) {
      var _scpObj, kmpKey;
      if (data && data.__component__) {
          data.__component__.__scpObj || Object.defineProperty(data.__component__, "__scpObj", {
              value: {}
          });
          kmpKey = data.__component__;
          _scpObj = data.__component__.__scpObj;
      } else if (isEntity(data)) {
          data.$.__scpObj || Object.defineProperty(data.$, "__scpObj", {
              value: {}
          });
          kmpKey = data;
          _scpObj = data.$.__scpObj;
      }
      var obj = _scpObj,
      id;
      if (_scpObj) {
          var __nestScp1Set__ = false, __nestScp2Set__ = false;
          if (fromStore) {
              if(data[attr] && !__nestScp__.has(data[attr])){
                  nestObj = { db: db, model: model._name, attr: attr, pK: data.$.pK , Error : {} };
                  __nestScp1Set__ = true;
              }
              else{
                // var kmp = __nestScp__[kid] = __nestScp__[kid] || new Map();
                // data ? kmp.set(data, true) : undefined;
                var __nId = __nestScp__.get(data[attr])
                var refMp = __nestRef__[__nId] = __nestRef__[__nId] || new Map();
                var refMpId = genMapId(refMp);
                refMp.set(refMpId, true);
                setRecBindMap(nestScp[__nId], {db:db, model:model._name, attr: attr, pK: data.$.pK , Error : {} });
                obj[attr] = __nId+"_"+refMpId;
              }
          } else {
            if(data[attr] && !__nestScp__.has(data[attr])){
              // nestObj = { data: data[attr] };
              var ErrMap = [],cmpDataKeys = {};
              cmpDataKeys.key=attr;
              cmpDataKeys.dtype = data.__component__.component.__data[attr];
              cmpDataKeys._cmpErr = data.errors;
              cmpDataKeys.Error={};
              ErrMap.push(cmpDataKeys);
              nestObj = { data: data[attr], PropsInfo : ErrMap };
              if(cmpDataKeys.dtype.watch){
                nestObj.watch = true;
              }
                __nestScp2Set__ = true;
            }
            else{
                var __nId = __nestScp__.get(data[attr])
                var scope = nestScp[__nId];
                if(scope){
                  var errMp = scope.PropsInfo;
                  var cmpDataKeys = {};
                  cmpDataKeys.key=attr;
                  cmpDataKeys.Error = {}
                  cmpDataKeys._cmpErr = data.errors;
                  cmpDataKeys.dtype = data.__component__.component.__data[attr];
                  if(cmpDataKeys.dtype.watch){
                    scope.watch = true;
                  }
                  errMp.push(cmpDataKeys);
                }
                var refMp = __nestRef__[__nId] = __nestRef__[__nId] || new Map();
                var refMpId = genMapId(refMp);
                refMp.set(refMpId, true);
                obj[attr] = __nId+"_"+refMpId;
            }
        }
        if(nestObj){
          nestScpId++;
          var refMp = __nestRef__[nestScpId] = __nestRef__[nestScpId] || new Map();
          var refMpId = genMapId(refMp);
          refMp.set(refMpId, true);
          __nestScp__.set(data[attr], nestScpId);
          if(__nestScp1Set__){
              nestScp[nestScpId] = {};
              if(db){
                nestScp[nestScpId].db = db;
              }
              setRecBindMap(nestScp[nestScpId], nestObj);
              nestScp[nestScpId].Error = nestObj.Error
              //     setRecBindMap(model._name, attr, data.$.pK, nestScpId); 
          }
          if(__nestScp2Set__){   
            nestScp[nestScpId] = nestObj;                 
          }
          nestScp[nestScpId]._data = data[attr];
          obj[attr] = obj[attr] || nestScpId+"_"+refMpId;
          var path = [];
          // fromStore ? path.push(attr) : undefined;
          bindObj(data, attr, nestScpId, path, new Map(), checkAttrs, watch);
        }
    }
}
};

function establishWatchScope(watchProps,model){
  var scope = this,object;
  for (var v in watchProps){
    var property = v;
    var watch = watchProps[v]
    if(scope.component && scope.component.__data && scope.component.__data[property]){
      object = scope.getData(property);
      watch = scope.component.__data[property].watch ? true : watch;
      if(typeof object == "object"){
        establishObjectBinding(scope.component.data,v,false,undefined,undefined,watch)
      }
      var dataDef = scope.component.__data;
      if( nestScp[__nestScp__.get(object)]){
        // nestScp[__nestScp__.get(object)].dtype.watch = watch;
        dataDef[v].watch = watch;        
      }
    }
    else{
      if(model){
        var fieldList = model.fieldList;
        if(fieldList[v] && fieldList[v].watch == undefined){
          fieldList[v].watch = watch ;
          model._fldGrps.watch[v] = fieldList[v];
        }
      }
    }
  }
}

function removeNestScp2Bind(mp, mdlObj){
  var mp = mp.model;
  if (mp.has(mdlObj.model)) {
    var mp1 = mp.get(mdlObj.model);
    if (mp1.has(mdlObj.pK)) {
        var mp2 = mp1.get(mdlObj.pK);
        if (mp2.has(mdlObj.attr)) {
            mp2.delete(mdlObj.attr);
        }
        !mp2.size ? mp1.delete(mdlObj.pK) : undefined; 
    }
    !mp1.size ? mp.delete(mdlObj.model) : undefined;
  }
}

function setRecBindMap(obj, nestObj){
  var mp = obj.model = obj.model || new Map(), modelName = nestObj.model, pK = nestObj.pK, attr = nestObj.attr;
  if(modelName && !mp.has(modelName)){
    mp.set(modelName, new Map());
  }
  mp = mp.get(modelName);
  if(pK && !mp.has(pK)){
    mp.set(pK, new Map());
  }
  mp = mp.get(pK);
  if(attr && !mp.has(attr)){
    mp.set(attr, true);
  }
}


function bindObj(data, key, id, path, mp, checkAttrs, watch) {
  mp = mp || new Map();
  var value = key != undefined ? data[key] : data;
  var cyclic = false;
  if(!path.length && __nestScp__.get(data) === id){
    delete nestScp[id].cyclic;
  }
  if (path.length > 1 && value && nestScp[id] && nestScp[id].hasOwnProperty("data") && nestScp[id].data === value) {
    if(mp.get(value)){
      cyclic = true;
    }
    if (checkAndAddBind(value, id, path, cyclic)) {
      estObjScp(value, id, path, cyclic);
    }
    return;
  }
  var attrs,nestedSCP;
  if(checkAttrs){
      if((checkAttrs.hasOwnProperty("items")||checkAttrs.hasOwnProperty("properties"))){
          attrs=checkAttrs.items || checkAttrs.properties;
          nestedSCP = true;
      }
      else if(watch == undefined && checkAttrs.watch == true){
          watch = checkAttrs.watch 
      }
  }
  attrs=watch?undefined:attrs;
  if (Array.isArray(value)) {
    if (!mp.get(value)) {
      mp.set(value, true);
      value.forEach(function (val, idx) {
        path.push(idx);
        if(watch || (checkAttrs && checkAttrs.hasOwnProperty("items") && typeof(checkAttrs.items) == "object" )){
          bindObj(value, idx, id, path, mp,attrs,watch);
        }  
        // bindObj(value, idx, id, path, mp);
        path.pop();
      });
    } else {
      cyclic = true;
    }
    if (checkAndAddBind(value, id, path, cyclic)) {
      var _establish;
      if(Array.isArray(watch)){
        _establish = checkEstablishingSCP(value,path,watch)
      }
      if(_establish || (typeof watch =="boolean" && watch ) || nestedSCP){
          estObjScp(value, id, path, cyclic);
      }
    }
  } else if (value && typeof(value) == "object") {
    var cyclic = false;
    if (!mp.get(value)) {
      mp.set(value, true);
      for (var str in value) {
        path.push(str);
        if(watch || (attrs && attrs.hasOwnProperty(str))){
          bindObj(value, str, id, path, mp,attrs?attrs[str]:undefined,watch);
        }  
        // bindObj(value, str, id, path, mp);
        path.pop();
      }
    } else {
      cyclic = true;
    }
    if (checkAndAddBind(value, id, path, cyclic)) {
      var _establish;
      if(Array.isArray(watch)){
        _establish = checkEstablishingSCP(value,path,watch)
      }
      if(_establish || (typeof watch =="boolean" && watch ) || nestedSCP ){
          estObjScp(value, id, path, cyclic);
      }
    }
  }
};

function checkAndAddBind(value, id, path, cyclic){
    if(!value._scp || !value._scp.size){
        return true;
    }
    if(value._scp.size){
      if(value._scp.has(id)){
          var _obj = value._scp.get(id);
          var obj = _obj.paths;
          if(obj.hasOwnProperty(path.join("."))){
            if(cyclic){
              nestScp[id].cyclic = true;
            }                      
            return false;
          }
      }
    }
    return true;
}   

function estObjScp(value, id, path, cyclic){
  if(!value._scp){
    Object.defineProperty(value, "_scp", {
      value : new Map(),
      enumerable : false,
      configurable : true
    });
  }
  var mp = value._scp;
  if(!mp.has(id)){
    mp.set(id, {});
  }
  var obj = mp.get(id),
  path = path ? path.join(".") : path;
  var pathObj = obj.paths = obj.paths || {};
  pathObj[path] = true;
  var nestObj = nestScp[id];
  nestObj.cyclic = true;
}

function cmpObjs(obj1, obj2){
    if(Object.keys(obj1).length !== Object.keys(obj2).length){
        return false;
    }
    for(var key in obj1){
        if(!obj2.hasOwnProperty(key)){
            return false;
        }
        var ret = cmpData(obj1[key], obj2[key]);
        if(ret == false){
            return false;
        }
    }
    return true;
}	


function cmpData(data1, data2){
  if(Array.isArray(data1)){
    if((!Array.isArray(data2)) || data1.length !== data2.length){
      return false;
    }
    var len = data1.length, ret;
    for(var i=0;i <len; i++){
      ret = cmpData(data1[i], data2[i]);
      if(ret == false){
        return false;
      }
    }
  }
  else if(data1 && data2 && typeof data1 == "object" && typeof data2 == "object"){
    return cmpObjs(data1,data2);
  }
  else if(isEntity(data1) && isEntity(data2)){
    if(data1.$.schema){
      if( (data1.$.schema._name !== data1.$.schema._name) || (str$.getpKVal(data1) !== str$.getpKVal(data2)) ){
        return false;
      }
    }
    else{
      if( (data1.$.model._name !== data1.$.model._name) || (str$.getpKVal(data1) !== str$.getpKVal(data2)) ){
        return false;
      }
    }
  }
  else if(data1 !== data2){
    return false;
  }
  return true;
}

function nestScpRmPath(obj, path){
  if(obj && obj.paths){
      obj = obj.paths;
      for(var key in obj){
          if(key.startsWith(path+".")){
              delete obj[key];
          }
      }
  }
}

function nestScpRemove(data, id, path){
  var mp = data._scp;
  if(mp && mp.size){
      if(path){
        nestScpRmPath(mp.get(id), path);
        var _obj = mp.get(id); 
        var obj = _obj ? _obj.paths : undefined;
        if(obj && !Object.keys(obj).length){
            mp.delete(id);
        }  
      }
      else{
          mp.delete(id);
      }
      if(!mp.size){
        delete data._scp;
      }
  }
}

function rmNestScp(value, id, mp, data, path){
  if (Array.isArray(value)) {
      if (!mp.get(value)) {
          mp.set(value, true);
          value.forEach(function (val, idx) {
              rmNestScp(val, id, mp, undefined, path);
          });
      }
      nestScpRemove(value, id, path);
  } else if (value && typeof(value) == "object") {
      if (!mp.get(value)) {
          mp.set(value, true);
          for (var str in value) {
              rmNestScp(value[str], id, mp, undefined, path);
          }
      }
      nestScpRemove(value, id, path);
  }
}

function removeNestScp(value, id, mpId, path, context, mp, data, recObj) {
  mp = mp || new Map();
  id = Number.parseInt(id);
  var obj = nestScp[id], kmp = __nestRef__[id];
  if(recObj && obj.model){
    removeNestScp2Bind(obj, recObj);
  }
  if(context){
      if(kmp && kmp.has(mpId)){
          kmp.delete(mpId);
      }
      if(!kmp || (kmp && !kmp.size)){
          delete __nestRef__[id];
          if(__nestScp__.has(value)){
              __nestScp__.delete(value);
          }
          rmNestScp(value, id, mp, true, path);
          if(!data){
              delete nestScp[id];
          }
      }
  }
  else{
    rmNestScp(value, id, mp, true, path);
  }
};

//@3055
// function addStateToMap(event, target, XHR, stateName){
//   var mp = window.__nodeXHRMap = window.__nodeXHRMap || new Map();
//   var nodeMap = mp.get(target);
//   if(!nodeMap){
//       mp.set(target, new Map());
//   }
//   nodeMap = mp.get(target);
//   var sr = target.lyteState = target.lyteState || [], type = stateName || XHR;
//   if(sr.indexOf(type) == -1){
//       sr.push(type);
//   }
//   target.setAttribute("lyte-state", "");
//   var evMap = nodeMap.get(event);
//   if(!evMap){
//       nodeMap.set(event, []);
//       evMap = nodeMap.get(event);
//   }
//   if(stateName){
//       evMap.push({state:stateName});
//       return {target: target, event: event};        
//   }
//   else{
//       evMap.push({isXHR:true, xhr:XHR});
//       var callback = function(arg){
//         if(XHR.readyState == 4){
//             removeStateFromMap(XHR, event, target);
//             XHR.removeEventListener("readystatechange", callback);
//         }
//     }
//     XHR.addEventListener("readystatechange", callback);
//   }
// }

// function removeStateFromMap(type, event, target){
//   var mp = window.__nodeXHRMap; 
//   var nodeMap = mp.get(target);
//   if(!nodeMap){
//       return;
//   }
//   var evMap = nodeMap.get(event);
//   if(!evMap){
//       return;
//   }
//   if(evMap){
//       var arr = evMap;
//       var ind = -1;
//       arr.every(function(itm, idx){
//           if((itm && itm.isXHR && itm.xhr == type)|| (typeof type == "string" && itm.state == type)){
//               ind = idx;
//               return false;
//           }
//           return true;
//       });
//       if(ind != -1){
//           arr.splice(ind,1);
//           var sind = target.lyteState.indexOf(type);
//           target.lyteState.splice(sind, 1);
//           if(!arr.length){
//               nodeMap.delete(event);
//               var tyInd = target && Array.isArray(target.lyteState) ? target.lyteState.indexOf(type) : -1; 
//               tyInd != -1 ? target.lyteState.splice(tyInd, 1) : undefined;
//               if(target && target.lyteState && target.lyteState.length == 0){
//                   target.lyteState = null;
//                   target.removeAttribute("lyte-state");
//               } 
//           }
//           if(!nodeMap.size){
//               mp.delete(target);
//           }
//       }
//   }
// }

function toBeUsedServices(obj){
  var serviceToBeUsed = Object.assign({},obj.Lyte.toBeInjectedServices);
  var arr = obj.services;
  if(arr){
    arr.forEach(function(service){
        if(typeof service == "string"){
            serviceToBeUsed[service] = service;
        }
        else if(service && typeof service == "object"){
            for(var key in service){
                serviceToBeUsed[key] = service[key];
            }
        }
    })
  }
  return serviceToBeUsed;
}

function extendService(obj){
  var servObj = obj.serviceToBeUsed || toBeUsedServices(obj), name;
  for(var serv in servObj){
    name = servObj[serv];
    if(obj.Lyte.registeredServices.hasOwnProperty(name)){
      obj.scope[serv] = obj.Lyte.registeredServices[name];
    }else{
      obj.Lyte.$.requiredServices(serv, name, obj.callback);
    }
  }  
}

function extendMixin(obj){
  var scp = obj.Lyte, self = obj.scope;
  obj.mixins.forEach(function(item){
    if(scp.Mixin.exists(item)){
      var mixin = scp.registeredMixins[item];
      for(var key in mixin){
        self[key] = mixin[key];
      }
    }
    else{
      scp.$.requiredMixins(item, obj.callback);
    }
  });
}

function toAddSuper(scp, key, name, self){
  if(scp.__toAddSuper && scp.__toAddSuper.hasOwnProperty(name)){
    var addSuper = scp.__toAddSuper[name];
    for(var i=0; i<addSuper.length; i++){
      var child = scp[addSuper[i]];
      if(child && child.is == key){
        child.$super = self;
        self.__extendedBy.push(addSuper[i]);
        var index;
        if(name != "application")
        {	
          if(scp.__toAddSuper.application)
          {
            index = scp.__toAddSuper.application.indexOf(child.__name);
            if(index > -1)
            {
              scp.__toAddSuper.application.splice(index,1);
            }
          }
          if(scp.application)
          {
            index = scp.application.__extendedBy.indexOf(child.__name);
            if(index > -1)
            {
              scp.application.__extendedBy.splice(index,1);
            }
          }
        }
      }
    }
    delete scp.__toAddSuper[name];
  }
}

function _get(cacheObj, key) {
  return cacheObj[key];
}
function globalsSet(scope,value){
  let set = this.$utils.set;
  if(set){
      return set(this.__gl,scope,value);
  }
  else{
      this.error("Globals set will be supported only if component registry is imported in the app");
  }
};
function globalsGet(scope){
  let get = this.$utils.get;
  if(get){
      return get(this.__gl,scope);
  }
  else{
      elf.error("Globals get will be supported only if component registry is imported in the app");
  }
};
function arrayUtils(){
  let arrayUtils = this.$utils.arrayUtils;
  if(arrayUtils){
      return arrayUtils.apply(arrayUtils, arguments);
  }else{
      this.error("arrayUtils will be supported only if component registry is imported in the app");
  }
}
function objectUtils(){
  let objectUtils = this.$utils.objectUtils;
  if(objectUtils){
      return objectUtils.apply(objectUtils, arguments);
  }else{
      this.error("objectUtils will be supported only if component registry is imported in the app");
  }
}
function _lyteDidConnect(LClass,ins){
  if(LClass._component.didConnect){
    LClass._component.didConnect(ins);
  }
}
function _lyteInit(LClass,ins){
  ins.__gl = {};
  ins.Globals = {};
  // ins.triggerEvent = triggerEvent;
  // ins.addEventListener = addEventListener;
  // ins.removeEventListener = removeEventListener; 
  ins.extendEventListeners = extendEventListeners;
  extendEventListeners(ins);
  extendEventListeners(LClass);
  if(LClass._component.init){
    LClass._component.init(ins);
  }
  // ins.arrayUtils = arrayUtils;
  // ins.objectUtils = objectUtils;
  ins.arrayUtils = function() {
    return arrayUtils.apply(ins ,arguments);
  }
  ins.objectUtils = function(){
      return objectUtils.apply(ins ,arguments);
  }
  ins.Globals.set = function(){
      return globalsSet.apply(ins ,arguments);
  }
  ins.Globals.get = function(){
      return globalsGet.apply(ins ,arguments);
  }
  ins.lyteError = _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger;
  ins.error = _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.error.bind(_lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger);
  ins.warn = _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.warn.bind(_lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger);
  ins.errorCodes = _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.errorCodes;
  defProp(ins, "patterns", {
    value: {
      email : new RegExp(/^([A-Za-z0-9._%\-'+/]+@[A-Za-z0-9.-]+\.[a-zA-Z]{2,22})$/),
      url : new RegExp(/(^(ht|f)tp(s?):\/\/[0-9a-zA-Z][-.\w]*(:[0-9])*(\/?)([a-zA-Z0-9\-.?,:'/\\+=&amp;%$#_[\]@!()*;~]*)?$)/),
      ampm : new RegExp(/^(AM|PM|am|pm)$/),
      hour : new RegExp(/^(0?[0-9]|1[0-9]|2[0-4])$/),
      minute : new RegExp(/^(0?[0-9]|[1-5][0-9]|60)$/),
      boolean : new RegExp(/^(true|false|TRUE|FALSE)$/),
      alphaNumeric : new RegExp(/([a-zA-Z0-9])+/),
      alphabetsOnly : new RegExp(/([a-zA-Z])+/),
      numeric : new RegExp(/([0-9])+/),
      phoneNo : new RegExp(/^[0-9a-zA-Z+.()\-;\s]+$/)
    }
  });
  ins.deepCopyObject = function(obj){
    return copyObject(obj);
  }
  ins.prop = function(type, opts){
    var obj = {};
    obj.type = type;
    if(opts == undefined){
      opts = {};
    }
    // if(this.types.indexOf(type) == -1 && !this.Transform.hasOwnProperty(type)){
    //   throw new Error("Not a valid field type - "+type);
    // }
    Object.assign(obj, opts);
    return obj;
  }
  _lyte_error__WEBPACK_IMPORTED_MODULE_1__.Logger.addEventListener("error", function(){  
    var arr = Array.from(arguments);
    if (LClass.onerror) {
      LClass.onerror.apply(LClass, arr);
    }
    if(LClass.triggerEvent){
      arr.unshift("error");
      LClass.triggerEvent.apply(LClass, arr);
    }
  });
  ins.types = types;
  ins.log = function (text, src, color) {
    if (this.config && this.config.debug) {
        if(color) {
            console.log("%c" + text,'color:' + color);
        } else {
          console.log(text);      
        }
    }
  };
  ins.isComponent = function(object) {
      if(object && object.$node && object.__data) {
        return true;
      }
      return false;
  }
  ins.Transform = {};
  ins.one = one;
  ins.many = many;

  ins.registerDataType = function(fieldTypeName, properties){
      var exts = "extends";
      if(ins.Transform.hasOwnProperty(fieldTypeName)){
        LClass.error("Custom Field Type - "+fieldTypeName+" -  already exists.");
        return;
      }
      if(properties[exts] == undefined || ins.types.indexOf(properties[exts]) == -1){
        LClass.error("Not a valid field type - "+properties[exts]);
        return;
      }
      ins.Transform[fieldTypeName] = properties;
  }
  ins.registerPattern = function(patternName, pattern){
    ins.patterns[patternName] = pattern;
  }
  ins.injectResources = function (files, every, completed, options) {
    var successFiles = [],
    errorFiles = [],
    scope = ins;
    every = every || function () {};
    completed = completed || function () {};
    return new Promise(function (resolve,reject) {
      processRequirements(files, function () {
        if (options && options.defer) {
          options.defer({
            injectJS: injectJS,
            files: files,
            errorFiles: errorFiles
          });
          resolve();
        } else {
          injectJS(files, function () {
            completed(successFiles, errorFiles);
            if(errorFiles.length) {
                reject(successFiles, errorFiles);
              } else {
                resolve(successFiles, errorFiles);
              }
          });
        }
      }.bind(ins));
    });
  
    function injectJS(files, resolve, execFiles) {
      execFiles = execFiles || []
      if (!files) {
        resolve(successFiles, errorFiles);
      } else {
        if (!Array.isArray(files)) {
          files = [files];
        }
        if (!files.length) {
          resolve(successFiles, errorFiles);
        }
        var len = -files.length;
        files.forEach(function (file) {
          if (typeof file == "string") {
             var fileSplit = file.split('.'),
            type = fileSplit[fileSplit.length - 1];
            if (type && type == "js" && execFiles.indexOf(file) == -1) {
              execFiles.push(file);
              createScript(file, function () {
                loaded();
              });
            } else {
              loaded();
            }
          } else if (Array.isArray(file)) {
            new Promise(function (r) {
              injectJS(file, r);
            }).then(function () {
              loaded();
            });
          } else {
            len--;
            new Promise(function (r) {
              injectJS(file.parent, r);
            }).then(function () {
              new Promise(function (r) {
                injectJS(file.child, r);
              }).then(function () {
                loaded();
              });
              loaded();
            });
          }
        });
      }
  
      function loaded() {
        len++;
        if (len == 0) {
          resolve(successFiles, errorFiles);
        }
      }
    }
    
    function createScript(file, resolve) {
      var ev = scope.injectResources.respObj[file];
      if (!scope.injectResources.availableTags[file] || scope.injectResources.availableTags[file].tag.tagName == "LINK") {
        var tag = document.createElement('script');
        tag.setAttribute('type', "text/javascript");
        ev.getAttributeNames().forEach(function (attr) {
          if (["href", "as", "rel"].indexOf(attr) == -1) {
            tag.setAttribute(attr, ev.getAttribute(attr));
          }
        });
        tag.setAttribute('src', file);
        tag.onerror = tag.onload = function(event) {
          if (event.type == "error") {
            errorFiles.push(event)
          } else {
            successFiles.push(event)
          }
          scope.injectResources.availableTags[file].tag.remove();
          scope.injectResources.availableTags[file] = { tag: tag, event: { type: event.type == "error" ? "error" : "load"} };
          resolve();
        }
        _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.$.assetsDiv.appendChild(tag);
      } else {
        resolve();
      }
    }
  
    function processRequirements(files, resolve) {
      if (!files) {
        resolve();
      } else {
        if (!Array.isArray(files)) {
          files = [files];
        }
        if (!files.length) {
          resolve();
        }
        var len = -files.length;
        files.forEach(function (file) {
          if (typeof file == "string") {
            requestFile.call(scope, file, scope.injectResources.availableTags[file], function () {
              loaded();
            });
          } else if (Array.isArray(file)) {
            new Promise(function (r) {
              processRequirements(file, r);
            }).then(function () {
              loaded();
            });
          } else {
            len--;
            new Promise(function (r) {
              processRequirements(file.parent, r);
            }).then(function () {
              loaded();
            });
            new Promise(function (r) {
              processRequirements(file.child, r);
            }).then(function () {
              loaded();
            });
          }
        });
      }
  
      function loaded() {
        len++;
        if (len == 0) {
          resolve();
        }
      }
  
      function requestFile(file, cached, resolve) {
        if(ins.$.reqFiles[file]) {
          ins.$.reqFiles[file].push(resolve);
        } else {
          var scope = ins;
          ins.$.reqFiles[file] = [resolve];
          if (cached && cached.event.type != "error") {
            if (ins.removeFromCache.arr.indexOf(file) != -1) {
              ins.removeFromCache.arr.splice(scope.removeFromCache.arr.indexOf(file), 1);
            }
            fileLoaded.call(cached.tag, file, cached.event, true);
            resolve();
          } else {
            makeRequest(file, function (event) {
              scope.injectResources.respObj[file] = ins;
              scope.$.reqFiles[file].forEach(function (resolve) {
                resolve();
              });
              // filesObj[file] = ins;
              fileLoaded.call(ins, file, event);
              every.call(ins, event);
            });
          }
        }
      }
  
      function fileLoaded(file, event, cached) {
        delete scope.$.reqFiles[file];
        if (!cached) {
          if (scope.injectResources.availableTags[file]) {
            scope.injectResources.availableTags[file].tag.remove();
            delete scope.injectResources.respObj[file];
          }
          ins.onerror = ins.onload = undefined;
          scope.injectResources.availableTags[file] = { tag: ins, event: { type: event.type } };
        }
      }
    }
  
    function makeRequest(file, callBack) {
      var tag,
          ev = every.internal || {},
          fileSplit = file.split('.'),
          type = fileSplit[fileSplit.length - 1];
      ev.file = file;
      if (fileSplit.length == 1) {
        _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error('Type of file is not specified in injectResources.');
        return;
      }
      tag = document.createElement('link');
      tag.setAttribute('href', file);
      ev.tag = tag;
      if (type == 'css') {
        tag.setAttribute('type', "text/css");
        tag.setAttribute('rel', "stylesheet");
        tag.onerror = tag.onload = function (event) {
          // if (event.type == "error") {
          //   errorFiles.push(event);
          // } else {
          //   successFiles.push(event);
          // }
          callBack.call(ins, event);
        };
      } else {
        tag.setAttribute('as', "script");
        tag.setAttribute('rel', "preload");
        tag.onerror = tag.onload = function (event) {
          // if (event.type != "error") {
          //   tag.status = 200;
          // }
          callBack.call(ins, event);
        };
      }
      _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.triggerEvent("onBeforeInject", ev);
      _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.$.assetsDiv.appendChild(tag);
    };
  };
  ins.injectResources.availableTags = [];
  ins.injectResources.respObj = [];

  ins.removeFromCache = function(arr) {
    var scope = ins;
    ins.removeFromCache.assign.call(ins,arr);
    if(ins.removeFromCache.arr.length) {
      ins.removeFromCache.arr.forEach(function(file) {
        if(scope.injectResources.availableTags[file]) {
          scope.injectResources.availableTags[file].tag.remove();
          delete scope.injectResources.availableTags[file];  
        }
      });
      ins.removeFromCache.arr = [];
    }
  }
  ins.removeFromCache.arr = [];
  ins.removeFromCache.assign = function(arr) {
    arr = arr == "*" ? Object.keys(ins.injectResources.availableTags) : (Array.isArray(arr) ? arr : [arr]); 
    ins.removeFromCache.arr = ins.removeFromCache.arr.concat(arr);
    return;
  }
  ins.resolvePromises = function(promises) {
    return new Promise(function(res, rej) {
      resolvePromises(promises).then(function(data) {
        res(data);
      },function(data) {
        rej(data);
      })
    })
  }
  // ins.setState = function(str){
  //   if(!str){
  //     console.error("Please provide a state name");
  //   }
  //   var evnt = window.event;
  //   if(/^(click|dblclick)$/.test(evnt.type)){
  //       var target = evnt.target;
  //       if(getConfig("stateHandling") == true && target && target.getAttribute && target.getAttribute("lyte-state-handling") != "false"){
  //           var state = target.getAttribute("lyte-state");
  //           if(!state){
  //               var mp = window.__stateMap = window.__stateMap || new Map();
  //               var mpobj = mp.get(str);
  //               if(!mpobj){
  //                   var obj = addStateToMap(evnt.type, target, undefined, str);
  //                   mp.set(str, obj);
  //               }
  //               else{
  //                   console.error("There is already a open state by the name",str);
  //               }
  //           }
  //       }        
  //   }
  // }
  // ins.removeState = function(str){
  //   if(!str){
  //       console.error("Please provide a state name");
  //   }
  //   var mp = window.__stateMap;
  //   if(mp){
  //       var obj = mp.get(str);
  //       if(obj){
  //           mp.delete(str);
  //           removeStateFromMap(str, obj.event, obj.target);
  //       }
  //   }
  // }

  ins.setConfig = function(key, value) {
    var configObj = window.__config = window.__config || {};
    configObj[key] = value;
  }
  ins.getConfig = getConfig;
}


function _defineProperty(exportsObj,specsObj){
  let actualObj = {};
  let defineProperty = function(obj,property){
    Object.defineProperty(exportsObj,property,{
      set : function(newValue){
        actualObj[property] = newValue;
      },
      get : function(){
        if(!actualObj[property]){
          actualObj[property] = obj();
        }
        return actualObj[property];
      }

    })
  }
  for(let key in specsObj){
    defineProperty(specsObj[key],key)
  }
}

function checkEstablishingSCP(value,path,watch){
  if(Array.isArray(watch)){
      var _path ='';
      _path = path.join(".")
      if(checkWatchPath(_path,watch,true) && typeof value != "object"){
          return true
      }
      var _key = Object.keys(value);
      for(var i_scp =0 ; i_scp<_key.length ; i_scp++){
          var spiePath = _key[i_scp];
         var finalaUth =  _path == ""?spiePath : _path+"."+spiePath;
          if(checkWatchPath(finalaUth,watch,true)){
              return true
          }
      }
  }
}
function checkWatchPath(actualPath,watchArr,establishBind){
  if(typeof watchArr == "boolean" && watchArr === true){
      return true
  }
  else if(Array.isArray(watchArr)){
      for(var i_watch=0; i_watch < watchArr.length ; i_watch++){
          var path = watchArr[i_watch];
          path =path.replace(/ /g,"");
          path = path.replace(/\$\.\./g,"..");
          path = path.replace(/\$\./g,"")
          var weirdPath = path.search(/\.\./g) != -1 || path.search(/\[|\\]|\{|\}/g)!=-1 ? true :  false || path.includes("*");
          if(actualPath == path){
              return true;
          }
          else if (weirdPath){
              if(establishBind){
                if(path.search(/\.\./) != -1 ){
                  path = path.replace(/\.\.\S+/g,"..*")
                  if(path == "..*"){
                    return true;
                  }
                } 
              }
              path = path.search(/\[/) == 0? path.replace("[",""):path;
              path = path.search(/\.\.\[/)==0 ? path.replace("..[",".."):path
              path = path.replace(/\.*\[/g,".");
              path = path.replace(/\]/g,"");
              var _watchPath = checkWeirdPath(path,actualPath);
              if(_watchPath){
                  return true
              }
          }
      }
  }
}
function checkWeirdPath(watchPath,actualPath){
  if(actualPath == watchPath){
      return true;
  }
  else if(watchPath.includes("..") || watchPath.includes("*")) {
      var cmpPath = watchPath;
      cmpPath = cmpPath.replace(/\*/g, "$$")
      cmpPath = cmpPath.replace(/\.\./g,"::")
      // cmpPath = cmpPath.replace("[","\\[")
      cmpPath = cmpPath.replace(/\./g, "\\.")
      cmpPath=cmpPath.replace(/\:\:/g,"\\.?.*\\.?");
      cmpPath=cmpPath.replace(/\$/g,".*");
      var _wildCard = watchPath.split(".")
      var _wClenght = _wildCard.length;
      if(_wildCard[_wClenght-2] == "" && _wildCard[_wClenght-1]!="*"){
        cmpPath = cmpPath+"$";
      }
      var regularExp = new RegExp(cmpPath);
      if(regularExp.test(actualPath)){
          return true;
      }
      return false;
  }
}
function removeStateFromMap(type, event, target) {
  var mp = window.__nodeXHRMap;
  var nodeMap = mp.get(target);
  if (!nodeMap) {
      return;
  }
  var evMap = nodeMap.get(event);
  if (!evMap) {
      return;
  }
  if (evMap) {
      var arr = evMap;
      var ind = -1;
      arr.every(function (itm, idx) {
          if ((itm && itm.isXHR && itm.xhr == type) || (typeof type == "string" && itm.state == type)) {
              ind = idx;
              return false;
          }
          return true;
      });
      if (ind != -1) {
          arr.splice(ind, 1);
          var sind = target.lyteState.indexOf(type);
          target.lyteState.splice(sind, 1);
          if (!arr.length) {
              nodeMap.delete(event);
              var tyInd = target && Array.isArray(target.lyteState) ? target.lyteState.indexOf(type) : -1;
              tyInd != -1 ? target.lyteState.splice(tyInd, 1) : undefined;
              if (target && target.lyteState && target.lyteState.length == 0) {
                  target.lyteState = null;
                  target.removeAttribute("lyte-state");
              }
          }
          if (!nodeMap.size) {
              mp.delete(target);
          }
      }
  }
}

function addStateToMap(event, target, XHR, stateName) {
  var mp = window.__nodeXHRMap = window.__nodeXHRMap || new Map();
  var nodeMap = mp.get(target);
  if (!nodeMap) {
      mp.set(target, new Map());
  }
  nodeMap = mp.get(target);
  var sr = target.lyteState = target.lyteState || [],
      type = stateName || XHR;
  if (sr.indexOf(type) == -1) {
      sr.push(type);
  }
  target.setAttribute("lyte-state", "");
  var evMap = nodeMap.get(event);
  if (!evMap) {
      nodeMap.set(event, []);
      evMap = nodeMap.get(event);
  }
  if (stateName) {
      evMap.push({
          state: stateName
      });
      return {
          target: target,
          event: event
      };
  } else {
      evMap.push({
          isXHR: true,
          xhr: XHR
      });
      var callback = function () {
          if (XHR.readyState == 4) {
              removeStateFromMap(XHR, event, target);
              XHR.removeEventListener("readystatechange", callback);
          }
      }
      XHR.addEventListener("readystatechange", callback);
  }
}
function getModuleInfo(jsClass){
  if(!jsClass || typeof jsClass !== "function"){
    //@Slicer.developmentStart
    _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Invalid argument passed to the getModuleInfo api");
    //@Slicer.developmentEnd
    return;
  }
  let name = jsClass.__lMod;
  return name ? { name : name } : false;
}
function addSlash(value) {
  return ((value[0] != "/") ? "/" : "") + value;
}
function slyteImport(src){
  if(src!=undefined){
      let entry = addSlash(src);
      if (window.LyteFingerPrint && LyteFingerPrint.fingerPrint && LyteFingerPrint.fingerPrint[entry]) {
          return addSlash(LyteFingerPrint.fingerPrint[entry].file.trim());
      }
      return src;
  }
  throw new Error("Missing path argument!!!");
}
function addLink(appIns,outlet,style,options,observerFlag){
  let replaceFn = function(linkurl){
    if(linkurl.indexOf('$')){
        linkurl = linkurl.replace(/\$([a-zA-Z0-9_]+)/g, (match, key) => {
            if(appIns.env[key] !== undefined){
              return appIns.env[key]
            }else{
              _index_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error(key+" is not available in env object");
              return match
            }
            //return appIns.env[key] !== undefined ? appIns.env[key] : match//Add the error console here;
        });
        return linkurl;   
    }
  }
  let getFingerPrintedVal = function(linkurl){
      let fingerPrintedVal = window.LyteFingerPrint && window.LyteFingerPrint.get(linkurl);
      if(fingerPrintedVal){
          linkurl = fingerPrintedVal.file;
      }
      return linkurl;
  }
  let getStaticPath = function(linkurl){
      if(appIns.cssStaticPath){
          linkurl = appIns.cssStaticPath+linkurl;
      }
      return linkurl;
  }
  if(!observerFlag){
      let linkurl
      if(typeof style == "string"){
          linkurl = style
          style = document.createElement("link");
          style.setAttribute("rel","stylesheet");
      }else{
          linkurl = style.getAttribute("href");
      }
      let originalLinkUrl = linkurl;
      linkurl = getStaticPath(getFingerPrintedVal(replaceFn(linkurl)));
      style.setAttribute('href',linkurl);
      let idVal = "dynamicStyle-"+appIns.__styleStack.length;
      style.setAttribute('id',idVal);
      outlet.appendChild(style); // Need to update for prepend cases
      appIns.__styleStack.push({id:idVal,replaceFilePath : linkurl,filePath : originalLinkUrl}) //
  }else{
      let arr = appIns.__styleStack;
      let len = arr.length;
      for(let i=0;i<len;i++){
          let {id,replaceFilePath,filePath} = arr[i];
          let newUrl = getStaticPath(getFingerPrintedVal(replaceFn(filePath)));
          if(replaceFilePath != newUrl){
              let node = document.querySelector('#'+id);
              arr[i].replaceFilePath = newUrl;
              node.setAttribute('href',newUrl);
          }
      }
  }
}
function refreshParentRoute(appIns,observerFlag){
  if(appIns){
    let routeInstance = appIns.$router.getRouteInstance();
    let nodeToRefresh;
    while(routeInstance){
      let dynamicVars = routeInstance.dynamicVars;
      if(dynamicVars && dynamicVars[observerFlag] == "r"){
        nodeToRefresh = routeInstance;
      }
      routeInstance = routeInstance.parent;
    }
    if(nodeToRefresh){
      nodeToRefresh.refresh({refreshTemplate:true});
    }
  }
}



/***/ }),

/***/ 21046606:
/*!**********************************************!*\
  !*** ./node_modules/@slyte/core/src/lyte.js ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Lyte": () => (/* binding */ Lyte),
/* harmony export */   "__getCurrentContext": () => (/* binding */ __getCurrentContext),
/* harmony export */   "__getCurrentFunc": () => (/* binding */ __getCurrentFunc),
/* harmony export */   "__scopedInstance": () => (/* binding */ __scopedInstance),
/* harmony export */   "__setCurrentContext": () => (/* binding */ __setCurrentContext),
/* harmony export */   "toLowerCase": () => (/* binding */ toLowerCase)
/* harmony export */ });
/* harmony import */ var _lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./lyte-utils.js */ 70416156);
/* harmony import */ var _service__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./service */ 70549578);
/* harmony import */ var _lyte_error__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./lyte-error */ 10533647);
/* harmony import */ var _Utils_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./Utils.js */ 97229384);
/* harmony import */ var _DataType_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./DataType.js */ 35155520);
/* harmony import */ var _Mixin_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./Mixin.js */ 99289921);


// import { resolvePromises } from './rsvp';





// var __instances = { engine : {}, addon: {}};
// var __componentsMap = {};
// var __servicesMap = {}; // Map for containing the services needed for every engine
// var __enginesMap = new Map(); //Map for containing the engines associated with every service
var d = document;
// //change to defProp later
// Function.prototype.observes = function(){//af
//   return {
//     type: "observer",
//     value: this,
//     properties: arguments,
//     on: Function.prototype.on
//   }
// }
// Function.prototype.on = function(){
//   return {
//     type: "callBack",
//     value: "observer" === this.type ? this.value : this,
//     properties: arguments,
//     observes: "observer" === this.type ? this : void 0
// }
// }
var currentContext;
var currFunc;

function __setCurrentFunc(func) {
    currFunc = func;
}

function __getCurrentFunc() {
    return currFunc;
}
function __setCurrentContext(self) {
    currentContext = self;
}

function __getCurrentContext() {
    return  currentContext;
}

function toLowerCase(string) {
    return string.charAt(0).toLowerCase() + string.slice(1)
}

function __scopedInstance(cls, args, func, extendArgs) {
  var oldcontext = __getCurrentContext();
  __setCurrentContext(this);
  var self = this;
  __setCurrentFunc(function(ins) {
      __setCurrentFunc();
      if(extendArgs && extendArgs.length){
        extendArgs.forEach(function(itm){
          // let itm__lyte = __getLyte(itm);
          // itm.__lookups
          if(itm.__lookups && itm.__lookups.length){
            (0,_service__WEBPACK_IMPORTED_MODULE_0__.__handleLookups)(itm.__lookups, self, ins);
          }
        });
      }
      func(ins);
  });
  var ins = Reflect.construct(cls, args);
  __setCurrentContext(oldcontext);
  return ins;
}

class Lyte {
  static dataType(type){
    var self = this;
    class dataType extends self.DataType {}
    dataType.type = type;
    return dataType;
  }

  lookups() {
    return [];
  }

  scopedInstance() {
    return __scopedInstance.apply(this, Array.from(arguments));
  }
  static componentMap(map){
    Lyte._componentMap = map;
  }
  static register(options){
    if(options){
      if(options.app == true){
        Lyte._setDefaultApp(this);
        if(options.clientConfig && options.clientConfig.performance){
          this.__performance = true;
          Lyte.__performance = true;
        }
      }
      if(options.preload){
        this._preloadLInkTag = true;
      }
      if(options._migration){
        this._migration = options._migration;
      }
      if(options.hash){
        this._hash = options.hash;
      }
      if(options.refHash){
          this._refHash = options.refHash;
      }
    }
    var DT = this.DataType = (function() {
      class DataType extends _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType {
        static register(){
          _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.registerInApp.apply(this, [DT]);
        }
      }
      return DataType;
    })();
    DT.dataType = {};
    (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.extendEventListeners)(DT.dataType);
    // DT.register = function(){
    //     DataTypeClass.register.apply(this, [DT]);
    // }    
    var hash = options ? options.hash : undefined;
    if(hash && _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType && _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.app && _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.app.has(options.hash)){
      var dMap = _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.app.get(hash);
      if(dMap.size){
        dMap.forEach(function(cls, name){
          _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.registerInApp.apply(cls, [DT]);
          dMap.delete(name);
        });
        if(!dMap.size){
          _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.app["delete"](hash);
        }
        if(!_DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.app.size){
          delete _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.app;
        }
      }
    }
    if(hash){
      _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.addEventListener(hash, function(dCls){
        _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.registerInApp.apply(dCls, [DT]);
        if(_DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.lyte.has(hash)){
            var dMap = _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.lyte.get(hash); 
            if(dMap.has(dCls.name)){
              dMap.delete(dCls.name);
            }
            if(!dMap.size){
              _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.lyte["delete"](hash);
            }
            if(!_DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.lyte.size){
              delete _DataType_js__WEBPACK_IMPORTED_MODULE_1__.DataType.lyte;
            }
        }
      });
    }
  }
  constructor(config) {
    var self = this;
    let defApp = Lyte._getDefaultApp();
    if(!Lyte._getDefaultAppIns() &&  defApp && defApp == this.constructor){
      Lyte._setDefaultAppIns(this);
    }
    if(this.constructor.appError){
      this.appError =  this.constructor.appError;
    }
    Lyte._instances.push(this);
    // extendEventListeners(this);
    // this.lyteError = Logger;
    // this.error = Logger.error.bind(Logger);
    // this.warn = Logger.warn.bind(Logger);
    // this.errorCodes = Logger.errorCodes;
    // this.addEventListener = addEventListener;
    // this.removeEventListener = removeEventListener;
    // this.triggerEvent = triggerEvent;
    var consoleTime = [],
    wLyte = Lyte;
    var type = this.is = config.__lp && config.__lp.type ? config.__lp.type :  "app";
    this.config = config;
    this.version = "4.0.0";
    this.$ = {
      isApp : type == "app",
      // isSubApp : type == "subApp",
      isAddon : type == "Addon",
      reqFiles : {},
      injectServices : {},
      modules : {}
    };
    (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.extendEventListeners)(this.$.modules);
    this.$.modules.addEventListener("add", function(name, ins){
      if(!self.$.modules.hasOwnProperty(name)){
        self.$.modules[name] = [];
      }
      self.$.modules[name].push(ins);
    });
    (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.defProps)(this, {
      "__mixins__":{
        value: new Map()
      },
      "registeredServices":{
        value:{}
      },
      "registeredMixins":{
        value:{}
      },
      // "_registeredComponents":{
      //   value: {}
      // },
      "Service":{
        value:{}
      },
      "toBeInjectedServices": {
        value:{}
      },
      "toBeRegistered": {
        value:{}
      },
      "dataType":{
        value:{}
      }
    });
    (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.defProps)(this, { "customValidator":{
      value: new Map()
    } });
    this.constructor.DataType.dataType.addEventListener("add",function(name, def){
      self.dataType[name] = def;
    });
    var dTypeDef = this.constructor.DataType.dataType;
    for(var dKey in dTypeDef){
      this.dataType[dKey] = dTypeDef[dKey];
    }
    // this.registeredCustomComponent = {};
    this.updateMixinsInApp();
    // this.Globals = {}
    // this.__lyteRegisteredEvents = {};
    // this.patterns = {
    //   email : /^([A-Za-z0-9._%\-'+/]+@[A-Za-z0-9.-]+\.[a-zA-Z]{2,22})$/,
    //   url : /(^(ht|f)tp(s?):\/\/[0-9a-zA-Z][-.\w]*(:[0-9])*(\/?)([a-zA-Z0-9\-.?,:'/\\+=&amp;%$#_[\]@!()*;~]*)?$)/,
    //   ampm : /^(AM|PM|am|pm)$/,
    //   hour : /^(0?[0-9]|1[0-9]|2[0-4])$/,
    //   minute : /^(0?[0-9]|[1-5][0-9]|60)$/,
    //   boolean : /^(true|false|TRUE|FALSE)$/,
    //   alphaNumeric : /([a-zA-Z0-9])+/,
    //   alphabetsOnly : /([a-zA-Z])+/,
    //   numeric : /([0-9])+/,
    //   phoneNo : /^[0-9a-zA-Z+.()\-;\s]+$/
    // };
    // this.$.requiredMixins  = {};
    // this.$.requiredServices = {};
    var self = this;
    this.time = function(fn) {
      if(this.config.performance) {
        var index;
        if((index = consoleTime.indexOf(fn)) != -1) {
          consoleTime.splice(index,1);
          console.timeEnd(fn);
        } else {
          consoleTime.push(fn)
          console.time(fn);
        }
      }
    }

    // this.arrayUtils = arrayUtils;
    // this.objectUtils = objectUtils;
    // this.Globals.set = function(){
    //   return globalsSet.apply(self ,arguments);
    // }
    // this.Globals.get = function(){
    //   return globalsGet.apply(self ,arguments);
    // }
    // if(Lyte._component.init){
    //   Lyte._component.init(this);
    // }
    // if(config.compiler){
    //   config.compiler.compiler(this);
    // }
    ;(0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__._lyteInit)(Lyte, this);
    this.Globals._name = "Globals";
    _Utils_js__WEBPACK_IMPORTED_MODULE_3__.Utils.addMethods([this.Globals])

    // if(config && config.engines){
    //   for(var engine in config.engines){
    //     var dependencies = config.engines[engine];
    //     if(dependencies.dependencies.services){
    //       __servicesMap[engine] = dependencies.dependencies.services;
    //     }
    //   }
    // }

    // if(type == 'engine' && name){
    //   if(__servicesMap[name]){
    //     __servicesMap[name].forEach(function(itm){
    //       if(config.dependencies && (engServ = config.dependencies.services)){
    //         if(itm && typeof itm == 'object'){
    //           for(var key in itm){
    //             if(engServ.includes(key)){
    //               if(__instances.app.registeredServices.hasOwnProperty(itm[key])){
    //                 self.registeredServices[itm[key]] = __instances.app.registeredServices[itm[key]]
    //               }
    //               // else{
    //               //   self.$.requiredServices(key,itm[key],callback.bind(self));
    //               // }
    //             self.toBeInjectedServices[key] = itm[key];
    //               if(!__enginesMap.has(itm[key])){
    //                 __enginesMap.set(itm[key],[]);
    //               }
    //               __enginesMap.get(itm[key]).push(self.name);
    //             }
    //           }
    //         }else if(itm){
    //           if(engServ.includes(itm)){
    //             if(__instances.app.registeredServices.hasOwnProperty(itm)){
    //               self.registeredServices[itm] = __instances.app.registeredServices[itm]
    //             }
    //             // else{
    //             //   self.$.requiredServices(itm,itm,callback.bind(self));
    //             // }
    //             self.toBeInjectedServices[itm] = itm;
    //             if(!__enginesMap.has(itm)){
    //               __enginesMap.set(itm,[]);
    //             }
    //             __enginesMap.get(itm).push(self.name);
    //             }
    //           }
    //         }
    //     })
    //   }
    // }

    // this.__lyteRegisteredEvents = {};

    this.addEventListener("navigationStart", function(obj){
      var trans = obj.nextTrans;
      if(trans && obj.prevTrans){
          if(window.event && /^(click|dblclick|mouseover|mouseout|mousemove|mousedown|mouseup|contextmenu|keydown|keyup|keypress|submit|reset|focus|blur|input|change|select|load|resize|scroll|unload|beforeunload|DOMContentLoaded|readystatechange|touchstart|touchmove|touchend|touchcancel|play|pause|ended|volumechange|durationchange|ratechange|dragstart|drag|dragenter|dragleave|dragover|drop|dragend)$/g.test(window.event.type)){
              trans.ev = window.event;
              if(obj.prevTrans.ev){
                delete obj.prevTrans.ev;
              }
          }
          else if((obj.prevTrans.state == 409 || obj.prevTrans.state == 308) && obj.prevTrans.ev){
            trans.ev = obj.prevTrans.ev;
            delete obj.prevTrans.ev;
        }
      }
    });

    this.addEventListener("afterRouteNavigation", function(obj){
        if(obj && obj.trans){
            var mp = window.__transXHRMap;
            if(obj.trans.state == 409 || obj.trans.state == 308){
                if(mp){
                    var mpObj = mp.get(obj.trans);
                    if(mpObj){
                        removeStateFromMap(mpObj.XHR, mpObj.currentAction.type, mpObj.currentAction.target);
                        mp.delete(obj.trans);
                    }
                }
            }
            else if(obj.trans.state == 200){
                mp ? mp.delete(obj.trans) : undefined;
                obj.trans.ev ? delete obj.trans.ev : undefined;
            }
        }
    });

    this.addEventListener("afterRouteTransition",function(obj){
      if(false) {}
    })

    this.addEventListener("beforeRouteNavigation", function(obj){
        if(obj && obj.prevTrans && obj.prevTrans.nested){
            obj.trans.ev = obj.prevTrans.ev;
            obj.trans.fromPrevTrans = true; // temp check
        }
    });

    this.includes = function(modules) {
      for(var module in modules) {
        var mod = this[module] = modules[module];
        mod.lyteInit ? mod.lyteInit(this) : undefined;
      }
    }
    // this.Security = {};
    // this.Security.$scp = this;
    // this.Security.createSanitizer = Lyte.Security.createSanitizer;
    // addToInstance(this);
    // window.Lte = Lyte.$scp[type][this.name] = this;
    //window.Lte = Lyte.$scp[type][this.name] = this;
    // if(__instances.app && false) { /* remove this code. Move this to component module */
    //   for(var compName in __globalElements.Component.registeredComponents){
    //     __componentsMap[compName] = {
    //           'is' : __instances.app.is,
    //           'name' : __instances.app.name
    //     };
    //   }
    // }

    // if(false) { /* check code to move to compoennt module*/
    //   this.Component._registeredComponents = Object.assign(this.Component._registeredComponents , __globalElements.Component._registeredComponents);
    //   this.Component.registeredComponents = Object.assign(this.Component.registeredComponents , __globalElements.Component.registeredComponents);
    //   this.Component.registeredHelpers = Object.assign(this.Component.registeredHelpers , __globalElements.Component.registeredHelpers);
    //   this.Component.customPropRegex =  __globalElements.Component.customPropRegex;
    //   this.Component.customPropHandlers = Array.from(__globalElements.Component.customPropHandlers);
    // }
    // this.$.requiredMixins.component = Lyte.Component.requiredMixins.component;
    // this.Security = {};
    // this.Security.$scp = this;
    // this.Security.createSanitizer = Lyte.Security.createSanitizer;

    if(config && config.init){
      config.init.apply(this);
    }
    Object.defineProperty(this, "__lyte", {
      enumerable : false, 
      writable : true, 
      value : {"lookupMap" : new Map()}
    });
    //Assigning env obj and adding observer to env object
    this.addLink = function(outlet, style, options, observerFlag) {
      if(self.__instantiated){
        (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.addLink)(this,outlet,style,options,observerFlag);
      }
      else{
        self.addEventListener("ready", function(){
          (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.addLink)(self,outlet,style,options,observerFlag);
        });
      }
    }
    this.refreshParentRoute = function(observerFlag){
      if(self.__instantiated){
        (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.refreshParentRoute)(this,observerFlag);
      }
      else{
        self.addEventListener("ready", function(){
          (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.refreshParentRoute)(self,observerFlag);
        });
      }
    }
    this.removeCss = function(fileList) {
      fileList.forEach(function(fileName){
        let styleStack = this.__styleStack || [];
        let len = styleStack.length;
        for(let index = 0;index <len;index++) {
          let iStyleStack = styleStack[index];
          if(iStyleStack.filePath == fileName) {
            let id = iStyleStack.id;
            let node = document.getElementById(id);
            styleStack.splice(index,1);
            node.remove();
            break;
          }
        }
      }.bind(this));
    }
    if(!this.env){
      this.env = {};
    }
    if(!this.__styleStack){
      this.__styleStack = [];
    }
    let dummyObj = Object.assign({},this.env);
    let appIns = this;
    let newProxy = function(val) {
      return new Proxy(val,{
          get(target,prop) {
              return target[prop];
          },
          set(obj,prop,value) {
              obj[prop] = value;
              appIns.refreshParentRoute(prop);
              appIns.addLink(null,null,null,true);
              return true;
          }
      });
    }
    let proxy = newProxy.call(this, dummyObj);
    Object.defineProperty(this,"env",{
        get() {
            return proxy;
        },
        set(newVal) {
            console.log("set call");
            proxy = newProxy.call(this, newVal);
            appIns.addLink(null,null,null,true);
        }
    })
    ;(0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.extendEventListeners)(this.__lyte.lookupMap);
    __setCurrentContext(this);
    (0,_service__WEBPACK_IMPORTED_MODULE_0__.__handleLookups)([_Utils_js__WEBPACK_IMPORTED_MODULE_3__.Utils], this, this);
    var lookups = this.lookups();
    // if(Array.isArray(lookups)){
    //   lookups.unshift(Utils);
    // }
    // else{
    //   lookups = [Utils];
    // }
    (0,_service__WEBPACK_IMPORTED_MODULE_0__.__handleLookups)(lookups, this, this);
    __setCurrentContext(undefined)   
    // if(Lyte.instantiateComponent){
    //   Lyte.instantiateSecurity(this);
    // }
    // if(Lyte._component.didConnect){
    //   Lyte._component.didConnect(this);
    // }
    ;(0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__._lyteDidConnect)(Lyte, this);

    this.__instantiated = true;
    //this.triggerEvent("ready"); // Added the ready event in app constructor through cli
  }
  updateMixinsInApp(){
    var hash = this.constructor._hash;
    if(hash){
      var mObj = _Mixin_js__WEBPACK_IMPORTED_MODULE_4__.Mixin.registeredMixin[hash];
      if(mObj){
        for(var key in mObj){
          let mixinCls = mObj[key];
          if(this.constructor._hash == mixinCls._refHash){
            this.registeredMixins[mixinCls.name] = mixinCls;
          }
        }
      }
    }
  }
  static isEntity(obj){
    (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.isEntity)(obj);
  }
  get __isApp() {
    return true;
  }
}

Lyte.Compatibility = {};

Lyte._instances = [];

Lyte.removeLink = function(providedParameter){
  if(providedParameter!=null){
    let linkTagParent=document.getElementById("lyteAssetsDiv");
        linkTagParent.removeChild(linkTagParent.querySelector(`a[href*=${providedParameter}]`))
  }
};

var fnProto = Function.prototype;
if(!fnProto.on){
    fnProto.on = function(){
        Lyte.warn("'.on()' method with the argument '" + arguments[0] + "' cannot be executed outside component scope.");
        return { "type": "callBack", "value": this.type === "observer" ? this.value : this, "properties": arguments, "observes": this.type === "observer" ? this : undefined };
    };
}
if(!fnProto.observes){
    fnProto.observes = function(){
        Lyte.warn("'.observes()' method with the arguments '" + arguments[0] + "' cannot be executed outside component scope.");
        return { "type": "observer", "value": this, "properties": arguments, "on": Lyte._onObj, "lyteOn": Lyte._onObj };
    };
}
if(!fnProto.computed){
    fnProto.computed = function(){
        Lyte.warn("'.computed()' method with the arguments '" + arguments[0] + "' cannot be executed outside component scope.");
        return { "type": "computed", "value": this, "properties": arguments };
    };
}
Lyte._onObj = function(){
  return {"type": "callBack", "value":(this.type === "observer") ? this.value:this , "properties":arguments, "observes":(this.type === "observer" ? this: undefined)}
}
Lyte._observesObj = function() {
    return {"type" : "observer", "value" : this, "properties" : arguments, "on": Lyte._onObj, "lyteOn" : Lyte._onObj}
}
Lyte._computedObj = function() {
    return {"type" : "computed", "value" : this, "properties" : arguments}
}
Lyte._preRegister = function(){
  Lyte._actualFnProtoOn = Function.prototype.on;
  Lyte._actualFnProtoObserves = Function.prototype.observes;
  Lyte._actualFnProtoComputed = Function.prototype.computed;
  let fnProto = Function.prototype;
  fnProto.on = fnProto.lyteOn = Lyte._onObj;
  fnProto.observes = fnProto.lyteObserves = Lyte._observesObj;
  fnProto.computed = fnProto.lyteComputed = Lyte._computedObj;
}
Lyte._postRegister = function(){
  let fnProto = Function.prototype;
  fnProto.on = Lyte._actualFnProtoOn;
  fnProto.observes = Lyte._actualFnProtoObserves;
  fnProto.computed = Lyte._actualFnProtoComputed;
}

Object.defineProperty(Lyte, "_singleTonLookupMap", {
  enumerable : false, 
  writable : true, 
  value : new Map()
});
(0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.extendEventListeners)(Lyte._singleTonLookupMap);
// Lyte.prototype.arrayUtils = function(){
//   if(this.$.modules.component && this.$.modules.component.length){
//     let lc = this.$.modules.component[0]._getLyteComponent();
//     return lc.aF.apply(lc, arguments);
//   }else{
//     this.error("arrayUtils will be supported only if component registry is imported in the app");
//   }
// }
// Lyte.prototype.objectUtils = function(){
//   if(this.$.modules.component && this.$.modules.component.length){
//     let lc = this.$.modules.component[0]._getLyteComponent();
//     return lc.oF.apply(lc, arguments);
//   }else{
//     this.error("objectUtils will be supported only if component registry is imported in the app");
//   }
// }
(0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_2__.extendEventListeners)(Lyte);
// Logger.addEventListener("error", function(){  
//   var arr = Array.from(arguments);
//   if (Lyte.onerror) {
//     Lyte.onerror.apply(Lyte, arr);
//   }
//   if(Lyte.triggerEvent){
//     arr.unshift("error");
//     Lyte.triggerEvent.apply(Lyte, arr);
//   }
// });

window.addEventListener("getLyteIns", function(ev){
  if(ev && ev.detail && ev.detail.init){
    ev.detail.init(Array.from(Lyte._instances));
  }
});
// Lyte.prototype.Compile = {};
window.LyteCls = Lyte;/* global lyte class */
Lyte.error = _lyte_error__WEBPACK_IMPORTED_MODULE_5__.Logger.error.bind(_lyte_error__WEBPACK_IMPORTED_MODULE_5__.Logger);
Lyte.warn = _lyte_error__WEBPACK_IMPORTED_MODULE_5__.Logger.warn.bind(_lyte_error__WEBPACK_IMPORTED_MODULE_5__.Logger);
Lyte.errorCodes = _lyte_error__WEBPACK_IMPORTED_MODULE_5__.Logger.errorCodes;
Lyte.browser = {};
Lyte._component = {};
Lyte.appList = [];
Lyte._setDefaultApp = function(appClass){
  Lyte._defaultApp = appClass;
}
Lyte._setDefaultAppIns = function(appIns){
  Lyte._defaultAppIns = appIns;
}
Lyte._getDefaultApp = function(){
  return Lyte._defaultApp;
}
Lyte._getDefaultAppIns = function(){
  return Lyte._defaultAppIns;
}
var userAgent = navigator.userAgent;
//temporary fix for IE 11
if (userAgent.match(/rv:11/)) {
  Lyte.browser.ie = true;
  window.action = function () {
    return;
  };
}
if (userAgent.match('Edge')) {
  var s = createElement("div");
  s.innerHTML = "<template><div>c</div></template>";
  if (s.querySelector("template").childNodes.length) {
    Lyte.browser.ie = true;
  } else {
    Lyte.browser.edge = true;
  }
  s.remove();
}

Lyte.reg = function(fn,options) {
  let type = options.type;
  let name = options.name;
  if(Lyte.$scp[type][name] && Lyte.$sscp[type][name]){
    fn(Lyte.$scp[type][name],Lyte.$sscp[type][name]);
  }
  else{
    Lyte.$scp[type][name] = new Lyte(type,name);
    Lyte.$sscp[type][name] = new Lyte.Store();
    fn(Lyte.$scp[type][name],Lyte.$sscp[type][name]);
  }
}
Lyte.$scp = {app : {}, addon : {}, subApp : {}};
Lyte.$sscp = {app : {},subApp : {}}
// Lyte.__lyteRegisteredEvents = {};
// Lyte.addEventListener = addEventListener;
// Lyte.removeEventListener = removeEventListener;
// Lyte.triggerEvent = triggerEvent;
Lyte.toBeRegistered = [];
Lyte.nestScp = {};
Lyte.nestScpId = 1;
Lyte.addons = {};
// Lyte.registeredCustomComponent = {};
// Lyte.$ = {
//   assetsDiv : document.createElement("div"),
//   shadowDiv : document.createElement("div")
// }
// Lyte.$.assetsDiv.setAttribute("id", "lyteAssetsDiv");
// Lyte.$.shadowDiv.setAttribute("id", "lyteShadowDiv");

Lyte.domContentLoaded = function(callback) {
  if(d.readyState === "complete" || d.readyState === "interactive") { 
    callback();
  } else {
    window.addEventListener('DOMContentLoaded', function() {
      callback();
    });
  }
}

// Lyte.createApplication = function(name, config){
//   var ins = new Lyte("app", name, config);
//   setTimeout(function() {
//     config.includes.forEach(function(module) {
//       module.init && module.init(ins);
//     });
//   },0)
//   if(Lyte.Store) {
//     var sins = new Lyte.Store();
//     Lyte.$sscp.app[name] = sins;
//   }
//   // window.Lte = Lyte.$scp.app[name] = ins;
//   return ins;
// }

// Lyte.createEngine = function(name, config){
//   var ins = new Lyte("engine", name, config);
//   // var sins = new Lyte.Store();
//   // Lyte.$sscp.engine[name] = sins;
//   Lyte.$scp.engine[name] = ins;
//   return ins;
// }

// Lyte.createAddon = function(name, config){
//   var ins = new Lyte("addon", name, config);
//   Lyte.$scp.addon[name] = ins;
//   return ins;
// }

// Lyte.prototype.types = ["string", "object", "number", "boolean", "array"];

// Lyte.prototype.deepCopyObject = function(obj){
//   return copyObject(obj);
// }

// Object.defineProperty(Lyte, 'debug', {
//   set : function(data) {
//     Lyte._debug = Lyte.prototype.debug = data;
//     return data;
//   },
//   get : function() {
//     return Lyte._debug;
//   }
// });

// Lyte.prototype.log = function (text, src, color) {
//   if (this.config.debug) {
//       if(color) {
//           console.log("%c" + text,'color:' + color);
//       } else {
//         console.log(text);      
//       }
//   }
// };

// Lyte.prototype.isComponent = function(object) {
//   if(object && object.$node && object.__data) {
//     return true;
//   }
//   return false;
// }
// Lyte.prototype.Transform = {};
// Lyte.prototype.prop = function(type, opts){
//   var obj = {};
//   obj.type = type;
//   if(opts == undefined){
//     opts = {};
//   }
//   // if(this.types.indexOf(type) == -1 && !this.Transform.hasOwnProperty(type)){
//   //   throw new Error("Not a valid field type - "+type);
//   // }
//   Object.assign(obj,opts);
//   return obj;
// }

// Lyte.prototype.one = function(name,opts){
//   return defineRelation(name,"belongsTo",opts);
// }

// Lyte.prototype.many = function(name,opts){
//   return defineRelation(name,"hasMany",opts);
// }

// Lyte.prototype.registerDataType = function(fieldTypeName, properties){
//   var exts = "extends";
//   if(this.Transform.hasOwnProperty(fieldTypeName)){
//     this.error("Custom Field Type - "+fieldTypeName+" -  already exists.");
//     return;
//   }
//   if(properties[exts] == undefined || this.types.indexOf(properties[exts]) == -1){
//     this.error("Not a valid field type - "+properties[exts]);
//     return;
//   }
//   this.Transform[fieldTypeName] = properties;
// }

// Lyte.prototype.registerPattern = function(patternName, pattern){
//   this.patterns[patternName] = pattern;
// }

// Lyte.prototype.injectResources = function (files, every, completed, options) {
//   var successFiles = [],
//   errorFiles = [],
//   scope = this;
//   every = every || function () {};
//   completed = completed || function () {};
//   return new Promise(function (resolve,reject) {
//     processRequirements(files, function () {
//       if (options && options.defer) {
//         options.defer({
//           injectJS: injectJS,
//           files: files,
//           errorFiles: errorFiles
//         });
//         resolve();
//       } else {
//         injectJS(files, function () {
//           completed(successFiles, errorFiles);
//           if(errorFiles.length) {
//               reject(successFiles, errorFiles);
//             } else {
//               resolve(successFiles, errorFiles);
//             }
//         });
//       }
//     }.bind(this));
//   });

//   function injectJS(files, resolve, execFiles) {
//     execFiles = execFiles || []
//     if (!files) {
//       resolve(successFiles, errorFiles);
//     } else {
//       if (!Array.isArray(files)) {
//         files = [files];
//       }
//       if (!files.length) {
//         resolve(successFiles, errorFiles);
//       }
//       var len = -files.length;
//       files.forEach(function (file) {
//         if (typeof file == "string") {
//            var fileSplit = file.split('.'),
//           type = fileSplit[fileSplit.length - 1];
//           if (type && type == "js" && execFiles.indexOf(file) == -1) {
//             execFiles.push(file);
//             createScript(file, function () {
//               loaded();
//             });
//           } else {
//             loaded();
//           }
//         } else if (Array.isArray(file)) {
//           new Promise(function (r) {
//             injectJS(file, r);
//           }).then(function () {
//             loaded();
//           });
//         } else {
//           len--;
//           new Promise(function (r) {
//             injectJS(file.parent, r);
//           }).then(function () {
//             new Promise(function (r) {
//               injectJS(file.child, r);
//             }).then(function () {
//               loaded();
//             });
//             loaded();
//           });
//         }
//       });
//     }

//     function loaded() {
//       len++;
//       if (len == 0) {
//         resolve(successFiles, errorFiles);
//       }
//     }
//   }

//   function createScript(file, resolve) {
//     var ev = scope.injectResources.respObj[file];
//     if (!scope.injectResources.availableTags[file] || scope.injectResources.availableTags[file].tag.tagName == "LINK") {
//       var tag = document.createElement('script');
//       tag.setAttribute('type', "text/javascript");
//       ev.getAttributeNames().forEach(function (attr) {
//         if (["href", "as", "rel"].indexOf(attr) == -1) {
//           tag.setAttribute(attr, ev.getAttribute(attr));
//         }
//       });
//       tag.setAttribute('src', file);
//       tag.onerror = tag.onload = function(event) {
//         if (event.type == "error") {
//           errorFiles.push(event)
//         } else {
//           successFiles.push(event)
//         }
//         scope.injectResources.availableTags[file].tag.remove();
//         scope.injectResources.availableTags[file] = { tag: tag, event: { type: event.type == "error" ? "error" : "load"} };
//         resolve();
//       }
//       Lyte.$.assetsDiv.appendChild(tag);
//     } else {
//       resolve();
//     }
//   }

//   function processRequirements(files, resolve) {
//     if (!files) {
//       resolve();
//     } else {
//       if (!Array.isArray(files)) {
//         files = [files];
//       }
//       if (!files.length) {
//         resolve();
//       }
//       var len = -files.length;
//       files.forEach(function (file) {
//         if (typeof file == "string") {
//           requestFile.call(scope, file, scope.injectResources.availableTags[file], function () {
//             loaded();
//           });
//         } else if (Array.isArray(file)) {
//           new Promise(function (r) {
//             processRequirements(file, r);
//           }).then(function () {
//             loaded();
//           });
//         } else {
//           len--;
//           new Promise(function (r) {
//             processRequirements(file.parent, r);
//           }).then(function () {
//             loaded();
//           });
//           new Promise(function (r) {
//             processRequirements(file.child, r);
//           }).then(function () {
//             loaded();
//           });
//         }
//       });
//     }

//     function loaded() {
//       len++;
//       if (len == 0) {
//         resolve();
//       }
//     }

//     function requestFile(file, cached, resolve) {
//       if(this.$.reqFiles[file]) {
//         this.$.reqFiles[file].push(resolve);
//       } else {
//         var scope = this;
//         this.$.reqFiles[file] = [resolve];
//         if (cached && cached.event.type != "error") {
//           if (this.removeFromCache.arr.indexOf(file) != -1) {
//             this.removeFromCache.arr.splice(scope.removeFromCache.arr.indexOf(file), 1);
//           }
//           fileLoaded.call(cached.tag, file, cached.event, true);
//           resolve();
//         } else {
//           makeRequest(file, function (event) {
//             scope.injectResources.respObj[file] = this;
//             scope.$.reqFiles[file].forEach(function (resolve) {
//               resolve();
//             });
//             // filesObj[file] = this;
//             fileLoaded.call(this, file, event);
//             every.call(this, event);
//           });
//         }
//       }
//     }

//     function fileLoaded(file, event, cached) {
//       delete scope.$.reqFiles[file];
//       if (!cached) {
//         if (scope.injectResources.availableTags[file]) {
//           scope.injectResources.availableTags[file].tag.remove();
//           delete scope.injectResources.respObj[file];
//         }
//         this.onerror = this.onload = undefined;
//         scope.injectResources.availableTags[file] = { tag: this, event: { type: event.type } };
//       }
//     }
//   }

//   function makeRequest(file, callBack) {
//     var tag,
//         ev = every.internal || {},
//         fileSplit = file.split('.'),
//         type = fileSplit[fileSplit.length - 1];
//     ev.file = file;
//     if (fileSplit.length == 1) {
//       Lyte.error('Type of file is not specified in injectResources.');
//       return;
//     }
//     tag = document.createElement('link');
//     tag.setAttribute('href', file);
//     ev.tag = tag;
//     if (type == 'css') {
//       tag.setAttribute('type', "text/css");
//       tag.setAttribute('rel', "stylesheet");
//       tag.onerror = tag.onload = function (event) {
//         // if (event.type == "error") {
//         //   errorFiles.push(event);
//         // } else {
//         //   successFiles.push(event);
//         // }
//         callBack.call(this, event);
//       };
//     } else {
//       tag.setAttribute('as', "script");
//       tag.setAttribute('rel', "preload");
//       tag.onerror = tag.onload = function (event) {
//         // if (event.type != "error") {
//         //   tag.status = 200;
//         // }
//         callBack.call(this, event);
//       };
//     }
//     Lyte.triggerEvent("onBeforeInject", ev);
//     Lyte.$.assetsDiv.appendChild(tag);
//   };
// };

// if(Lyte.browser.ie) { //remove no ie support // af
//   Lyte.prototype.injectResources = function (files, every, completed) {
//     var successFiles = [],
//     errorFiles = []; 
//     every = every || function() {};
//     completed = completed || function() {};
//     return new Promise(function(resolve) {
//       processRequirements(files, resolve);   
//     }).then(function() {
//       completed(successFiles,errorFiles);  
//     });

//     function processRequirements(files, resolve) {
//       if(!files) {
//         resolve();
//       } else {
//         if(!Array.isArray(files)) {
//           files = [files];
//         }
//         if(!files.length) {
//           resolve();
//         }
//         var len = -(files.length);
//         files.forEach(function(file) {
//           if(typeof file == "string"){
//             requestFile.call(scope,file, scope.injectResources.availableTags[file], function() {
//               loaded();
//             });  
//           } else if(Array.isArray(file)) {
//             new Promise(function(r){
//               processRequirements(file, r);
//             }).then(function(){
//               loaded();
//             })
//           } else {
//             new Promise(function(r){
//               processRequirements(file.parent, r);
//             }).then(function(){
//               new Promise(function(r1){
//                 processRequirements(file.child, r1)
//               }).then(function(){
//                 loaded();
//               })
//             })
//           }
//         })
//       }

//       function loaded() {
//         len++;
//         if(len == 0) {
//           resolve();
//         }
//       }

//       function requestFile(file,cached,resolve) {
//         if(this.$.reqFiles[file]) {
//           this.$.reqFiles[file].push(resolve)
//         } else {
//           this.$.reqFiles[file] = [resolve];
//           if(cached && cached.event.type != "error") {
//             if(this.removeFromCache.arr.indexOf(file) != -1) {
//               this.removeFromCache.arr.splice(this.removeFromCache.arr.indexOf(file),1);
//             }
//             fileLoaded.call(cached.tag,cached.event,true);
//             resolve();
//           } else {
//             makeRequest(file,
//               function(event) {
//                 this.$.reqFiles[file].forEach(function(resolve) {
//                   resolve();
//                 });
//                 fileLoaded.call(this,event);
//                 every.call(this,event);
//               }
//             );
//           }
//         }
//       }

//       function fileLoaded(event,cached) {
//         var file = this.getAttribute('src') || this.getAttribute('href');
//         delete scope.$.reqFiles[file];
//         if(!cached) {
//           if(scope.injectResources.availableTags[file]) {
//             scope.injectResources.availableTags[file].tag.remove();
//           }
//           this.onerror = this.onload = undefined;
//           scope.injectResources.availableTags[file] = {tag : this, event : {type : event.type}};
//         }
//       }
//     }

//     function makeRequest(file,callBack) {
//       var tags = { ".js": 'script', ".css" : 'link' },
//       type = file.match(/\.[a-zA-Z]+(?=\?|$)/),
//       tag = document.createElement(tags[type]);
//       if (!type) {
//         Lyte.error('Type of file is not specified in injectResources.');
//         return;
//       } else if (type == '.css') {
//         tag.setAttribute('href', file);
//         tag.setAttribute('type', "text/css");
//         tag.setAttribute('rel', "stylesheet");
//       } else {
//         tag.setAttribute('src', file);
//       }
//       tag.onerror = tag.onload = function (event) {
//         if(event.type == "error") {
//           errorFiles.push(event);  
//         } else {
//           successFiles.push(event);
//         }
//         if(callBack) {
//           callBack.call(this,event);
//         }
//       };
//       var ev = every.internal || {};
//       ev.file = file;
//       ev.tag = tag;
//       Lyte.triggerEvent("onBeforeInject", ev);
//       Lyte.$.assetsDiv.appendChild(tag);
//     };
//   };
// }

// Lyte.prototype.injectResources.availableTags = [];
// Lyte.prototype.injectResources.respObj = [];

// Lyte.prototype.removeFromCache = function(arr) {
//   var scope = this;
//   this.removeFromCache.assign.call(this,arr);
//   if(this.removeFromCache.arr.length) {
//     this.removeFromCache.arr.forEach(function(file) {
//       if(scope.injectResources.availableTags[file]) {
//         scope.injectResources.availableTags[file].tag.remove();
//         delete scope.injectResources.availableTags[file];  
//       }
//     });
//     this.removeFromCache.arr = [];
//   }
// }

// Lyte.prototype.removeFromCache.arr = [];

// Lyte.prototype.removeFromCache.assign = function(arr) {
//     arr = arr == "*" ? Object.keys(this.injectResources.availableTags) : (Array.isArray(arr) ? arr : [arr]); 
//     this.removeFromCache.arr = this.removeFromCache.arr.concat(arr);
//     return;
// }

// Lyte.prototype.triggerEvent = triggerEvent;

// Lyte.prototype.addEventListener = addEventListener;

// Lyte.prototype.removeEventListener = removeEventListener; 

// Lyte.prototype.extendEventListeners = extendEventListeners;

// Lyte.prototype.resolvePromises = function(promises) {
//   return new Promise(function(res, rej) {
//     resolvePromises(promises).then(function(data) {
//       res(data);
//     },function(data) {
//       rej(data);
//     })
//   })
// }

// Lyte.createCustomElement = function (customElementName, definition) {
//   var constructor = definition.constructor;
//   delete definition.constructor;
//   var connectedCallback = definition.connectedCallback;
//   delete definition.connectedCallback;
//   var attributeChangedCallback = definition.attributeChangedCallback;
//   delete definition.attributeChangedCallback;
//   var disconnectedCallback = definition.disconnectedCallback;
//   delete definition.disconnectedCallback;

//   this.defProperty = function(obj, key, val) {
//     var obj1 = {};
//     if(val.get) {
//       obj1.get = val.get
//     }
//     if(val.set) {
//       obj1.set = val.set
//     }
//     Object.defineProperty(obj, key, obj1);
//   }
//   class classDef extends HTMLElement {
//     constructor() {
//       super();
//       if(this.isNewComp(customElementName)){
//         this.executeCallbacks(constructor,arguments);
//       }else{
//         this.__lyteIgnore = true;
//       }
//     }

//     connectedCallback(){
//       let _LC = Lyte._instances[0].$.modules.component[0]._getLyteComponent();//temp fix
//       if(!_LC.ignoreDisconnect && !this.__lyteIgnore) {
//         this.executeCallbacks(connectedCallback,arguments);
//         this.setAttribute("lyte-rendered-ce", "");
//       }
//     }
//     attributeChangedCallback(){
//       if(!this.__lyteIgnore) {
//         this.executeCallbacks(attributeChangedCallback,arguments);
//       }
//     }
//     disconnectedCallback(){
//       let _LC = Lyte._instances[0].$.modules.component[0]._getLyteComponent();//temp fix
//       if(!_LC.ignoreDisconnect && !this.__lyteIgnore) {
//         this.executeCallbacks(disconnectedCallback,arguments);
//       }
//     }
//     executeCallbacks(callBack,argArr){
//       if(callBack) {
//         callBack.apply(this, Array.from(argArr));
//       }
//     }
//     isNewComp(customElementName){
//       if(this.hasAttribute("lyte-rendered-ce")) {
//         return false;
//       }
//       return true;
//     }
//   }
//   var staticDef = definition.static;
//   if(staticDef) {
//     for(var key in staticDef) {
//       if(typeof staticDef[key] === "object") {
//         this.defProperty(classDef, key, staticDef[key]);
//       } else {
//         Object.defineProperty(classDef, key, {
//           value : staticDef[key]
//         });
//       }
//     }
//     delete definition.static;
//   }
//   for(var key in definition) {
//     if(typeof definition[key] === "object") {
//       this.defProperty(classDef.prototype, key, definition[key]);
//     } else {
//       Object.defineProperty(classDef.prototype, key, { writable: true, value : definition[key]});
//     }
//   }
//   definition.static = staticDef;
//   definition.constructor = constructor;
//   definition.connectedCallback = connectedCallback;
//   definition.attributeChangedCallback = attributeChangedCallback;
//   definition.disconnectedCallback = disconnectedCallback;
//   if (document.readyState === "complete" || document.readyState === "interactive") {     
//     // document is already ready to go
//     customElements.define(customElementName,classDef);
//   }
//   else{
//     // ***
//     //Change the Lyte to instance
//     Lyte.toBeRegistered.push({name:customElementName, def: classDef});
//   }
//   Lyte.registeredCustomComponent[customElementName] = classDef;
// }

// function domContentLoaded1() {
// document.head.appendChild(Lyte.$.assetsDiv);
// document.head.appendChild(Lyte.$.shadowDiv);
// let comp = Lyte.toBeRegistered;    
// if(comp && comp.length){    
//     for(let j=0; j<comp.length;j++){
//         customElements.define(comp[j].name, comp[j].def);    
//     }    
//     Lyte.toBeRegistered = [];    
// }
// }

// if(document.readyState === "complete" || document.readyState === "interactive") {
//     domContentLoaded1();
// } else {
//     document.addEventListener("DOMContentLoaded", function(e){
//         domContentLoaded1();
//     },true);
// };

//@3055
// var XHRSend = XMLHttpRequest.prototype.send;
// XMLHttpRequest.prototype.send = function(){
//     var event = window.event;  
//     var type, target = event ? event.target : undefined; 
//     let regIns = getConfig("stateHandling",true)
//     if(getConfig("stateHandling")){
//         if(event){
//             type = event.type;
//             /^(click|dblclick)$/.test(type) && target && target.getAttribute && target.getAttribute("lyte-state-handling") != "false" ? addStateToMap(type,target,this) : undefined;
//         }
//         else{
//             var router = Lyte && Lyte.Router ? Lyte.Router.getRouteInstance() : undefined;
//             if(router){
//                 var currentAction = router.transition ? router.transition.ev : undefined;
//                 if(currentAction && currentAction.target && currentAction.target.getAttribute && currentAction.target.getAttribute("lyte-state-handling") != "false"){
//                     var mp = window.__transXHRMap = window.__transXHRMap || new Map();
//                     mp.set(router.transition, {currentAction : currentAction, XHR:this});
//                     addStateToMap(currentAction.type, currentAction.target, this);
//                 }
//             }
//         }
//     }
//     XHRSend.apply(this, arguments);
// }

// Lyte.prototype.setState =   function(str){
//   if(!str){
//     console.error("Please provide a state name");
//   }
//   var evnt = window.event;
//   if(/^(click|dblclick)$/.test(evnt.type)){
//       var target = evnt.target;
//       if(getConfig("stateHandling") == true && target && target.getAttribute && target.getAttribute("lyte-state-handling") != "false"){
//           var state = target.getAttribute("lyte-state");
//           if(!state){
//               var mp = window.__stateMap = window.__stateMap || new Map();
//               var mpobj = mp.get(str);
//               if(!mpobj){
//                   var obj = addStateToMap(evnt.type, target, undefined, str);
//                   mp.set(str, obj);
//               }
//               else{
//                   console.error("There is already a open state by the name",str);
//               }
//           }
//       }        
//   }
// }

// Lyte.prototype.removeState = function(str){
//     if(!str){
//         console.error("Please provide a state name");
//     }
//     var mp = window.__stateMap;
//     if(mp){
//         var obj = mp.get(str);
//         if(obj){
//             mp.delete(str);
//             removeStateFromMap(str, obj.event, obj.target);
//         }
//     }
// }

// Lyte.prototype.setConfig = function(key, value){
//     var configObj = window.__config = window.__config || {};
//     configObj[key] = value;
// }

// Lyte.prototype.getConfig = getConfig;  @3055


// Lyte.errorCodes = errorCodes;
// Lyte.registerErrorCodes = registerErrorCodes;
// Lyte.getErrorMessage = getErrorMessage;
window.addEventListener("onBeforeInject", function(ev) {
  Lyte.triggerEvent("onBeforeInject",ev.detail, ev);
})






/***/ }),

/***/ 70549578:
/*!*************************************************!*\
  !*** ./node_modules/@slyte/core/src/service.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Service": () => (/* binding */ Service),
/* harmony export */   "__checkIfService": () => (/* binding */ __checkIfService),
/* harmony export */   "__getLyte": () => (/* binding */ __getLyte),
/* harmony export */   "__handleLookups": () => (/* binding */ __handleLookups)
/* harmony export */ });
/* harmony import */ var _lyte_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./lyte.js */ 21046606);
/* harmony import */ var _lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lyte-utils.js */ 70416156);


/*convert to custom class*/
class Service { 
    static actions(arg1) {
      if(super.actions){
        return super.actions(arg1 ? arg1 : {});
      }
      return arg1 ? arg1 : {};
    }
    static methods(arg1) {
      if(super.methods){
        return super.methods(arg1 ? arg1 : {});
      }
      return arg1 ? arg1 : {};
    }
    static observers(arg1) {
      if(super.observers){
        return super.observers(arg1 ? arg1 : {});
      }
      return arg1 ? arg1 : {};
    }
    data(arg1) {
      if(super.data){
        return super.data(arg1 ? arg1 : {});
      }
      return arg1 ? arg1 : {};
    }
    constructor() {
        var context = (0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.__getCurrentContext)();
        // if(context){
        if(context.__isAddon) {
            if(this.__isAddon){
                this.$parent = context;
                this.$app = context.$app;
            }else{
                this.$addon = context;
            }
            if((0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.toLowerCase)(this.constructor.name) !== "utils"){
                this.$utils = context.$utils;
            }
        } else {
            this.$app = context;
            if(this.__isAddon){
                this.$parent = context;
            }
            if((0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.toLowerCase)(this.constructor.name) !== "utils"){
                this.$utils = context.$utils;
            }
        }
        var func = (0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.__getCurrentFunc)();
        var lookupMap;
        if(this.__isAddon){
            Object.defineProperty(this, "__lyte", {
                enumerable : false, 
                writable : true, 
                value : {"lookupMap" : new Map()}
            });
            // lookupMap = this.__lyte.lookupMap;
            (0,_lyte_utils_js__WEBPACK_IMPORTED_MODULE_1__.extendEventListeners)(this.__lyte.lookupMap);
        }
        // else{
            if(this.$addon){
                lookupMap = this.$addon.__lyte.lookupMap;
            }else if(this.$app){
                lookupMap = this.$app.__lyte.lookupMap;
            }
        // }
        if(func) {
            func(this);
        }
        var oldContext = (0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.__getCurrentContext)();
        if(this.__isAddon) {
            (0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.__setCurrentContext)(this);
        }
        
        if(lookupMap.get(this.constructor)){
            lookupMap.set(this.constructor,this);
        }
        else if(this.constructor.singleTon){
            _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte._singleTonLookupMap.set(this.constructor,this);
        }

        var lookups = this.lookups();
        // if(this.$addon){ //commented since lookups returned array - we are not using or holding it for anny purpose
        //     this.$addon.__lyte.lookupMap.lookups = lookups;
        // }else{
        //     this.$app.__lyte.lookupMap.lookups = lookups;
        // }
        __handleLookups(lookups, this.__isAddon ? this : context, this);
        this.__lookups = lookups;
        if(this.__isAddon) {
            (0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.__setCurrentContext)(oldContext);
        }
        // }else{
            // debugger
        // }
    }
    
    lookups() {
        return [];
    }

    onLookup() {
        
    }
}
function __getLyte(service){
    return service.$addon ? service.$addon.__lyte : service.__lyte ? service.__lyte : service.$app.__lyte;
}
function __handleLookups(lookups, app, curr) {
    if(lookups && lookups.length){
        var appLookupMap = app.__lyte.lookupMap;
        var currLookupMap = __getLyte(curr).lookupMap;
        lookups.forEach(function(item) {
            if(!item){
                _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Invalid class/instance passed in "+ curr.constructor.name + "'s lookup hook.")
                return
            }
            if(typeof item == "string") {
                if(curr) {
                    if(item.length){
                        if(appLookupMap.get(item)){
                            curr["$" + item] = appLookupMap.get(item);
                            __callOnLookup(curr["$" + item], curr);
                        }
                        else{
                            var id = app.__lyte.lookupMap.addEventListener(item, function(ins){
                                curr["$" + item] = ins;
                                __callOnLookup(ins, curr);
                                app.__lyte.lookupMap.removeEventListener(id);
                            });
                        }
                    }
                    else{
                        _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Lookup cannot be added for empty(undefined) key");
                    }
                }
            } else {
                if(__checkIfService(item)) {
                    instantiateService(item, app, curr);
                } else if(Object.keys(item).length == 1) {
                    for(var onlyKey in item){
                        var val = item[onlyKey];
                        if(onlyKey.length){
                            if(/^(utils|node|lg|app|addon)$/.test(onlyKey)){
                                _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Lookup cannot be added for the predefined key - "+ onlyKey);
                                return;
                            }
                            let name = "$" + onlyKey;
                            if(__checkIfService(val)) {
                                instantiateService(val, app, curr, name);
                            } else {
                                if(val && typeof val == "object"){
                                    curr[name] = val;
                                    __callOnLookup(val, curr);
                                    if(!appLookupMap.get(onlyKey)){
                                        appLookupMap.set(onlyKey, val); //how singleton af ?
                                        appLookupMap.triggerEvent(onlyKey, val);
                                    }
                                }
                                else if(typeof val == "string"){
                                    if(appLookupMap.get(val)){
                                        curr[name] = appLookupMap.get(val);
                                        __callOnLookup(curr[name], curr);
                                    }
                                    else{
                                        var id = appLookupMap.addEventListener(val, function(ins){
                                            curr[name] = ins;
                                            __callOnLookup(ins, curr);
                                            appLookupMap.removeEventListener(id);
                                        });
                                    }
                                }
                                currLookupMap.set(onlyKey, val);
                            }
                        }
                        else{
                            _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte.error("Lookup cannot be added for empty(undefined) key");
                        }
                    }
                }
            }
            
        });
    }
}

function instantiateService(cls, app, curr, name) {
    name = name || ("$" + (0,_lyte_js__WEBPACK_IMPORTED_MODULE_0__.toLowerCase)(cls.name));
    let appLookupMap = app.__lyte.lookupMap;
    // let currLookupMap = curr.__lyte.lookupMap;
    let lookupValue = appLookupMap.get(cls);
    if(lookupValue) {
        if(lookupValue instanceof Promise) {
            lookupValue.then(function(val) {
                curr[name] = val;
                __callOnLookup(val, curr);
            });
        } else {
            curr[name] = lookupValue;
            __callOnLookup(lookupValue, curr);
        }
    } else {
        if(cls.singleTon){
            let singleTonclass = _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte._singleTonLookupMap.get(cls)
            if(singleTonclass){
                curr[name] = singleTonclass;
                return;
            }else{
                _lyte_js__WEBPACK_IMPORTED_MODULE_0__.Lyte._singleTonLookupMap.set(cls,cls);
            }
        }else{
            appLookupMap.set(cls, cls);
        }
        let obj = curr[name] = new cls();
        __callOnLookup(obj, curr);
        appLookupMap.triggerEvent(name, obj);
    }
}
function __checkIfService(item) {
    var isService = false;
    while(item != null) {
        if(item == Service || item.__lyteOrigClass) {
            isService = true;
            break;
        } else {
            item = Object.getPrototypeOf(item);
        }
    }
    return isService;
}

function __callOnLookup(service, module) {
    if(service && service.onLookup) {
        service.onLookup(module);
    }
}



/***/ }),

/***/ 53275862:
/*!*********************************************************!*\
  !*** ./node_modules/@slyte/data/src/ValidationError.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ValidationError": () => (/* binding */ ValidationError)
/* harmony export */ });
/* harmony import */ var _commonUtils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./commonUtils */ 5730373);



class ValidationError {
    constructor(lyte, attr, obj, message){
        Object.defineProperties(this, {
            $ : {
                value : {isError : true , error:message?message:{}}
            }
        });
        if(attr){
            ValidationError.setError(lyte,this,attr,obj);
        }    
    }

    static setError(lyte,err,attr,codeObj){
        if(err.$.hasOwnProperty("error")){
            if(Array.isArray(attr)){
                attr.forEach(function(itm){
                    (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, err.$.error, itm, codeObj);
                });
            }
            else{
                (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, err.$.error,attr,codeObj);
            }
        }
        else{
            ValidationError.error(lyte,"LD03",err,attr);
        }
    }

    static setRecErr(ent$, field, code, value){
        var lyte = ent$.schema.Lyte;
        (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, ent$, "isError", true);
        var errObj = code;
        if(typeof errObj == "object"){
            (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, ent$.error,field,errObj);
        }
        else{
            if(typeof code == "string"){
                var errMes = ValidationError.errorCodes[code];
                if(errMes){
                    errObj = {code : code, message : ValidationError.errorCodes[code]};
                }
                else{
                    errObj = code;
                }
            }
            (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, ent$.error, field, errObj);
            if(value){
                (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, ent$.error[field],"value",value);
            }				
        }
		var db = ent$.schema.db;
        (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.emit)(db,"error",ent$.entity,field,errObj);
    }

    static clrRecErr(ent$, field, code){
        var lyte = ent$.schema.Lyte;
        var objUtl = lyte.objectUtils;
        var $err = ent$.error;
        if(code){
            if(ent$.error.code == code){
                if(typeof objUtl != "undefined"){
                    objUtl($err,"delete",field);
                }
                else{
                    delete $err[field];                        
                }
            }
        }
        else if(field){
            if(typeof objUtl != "undefined"){
                objUtl($err,"delete",field);
            }
            else{
                delete $err[field];                       
            }
        }
        else{
            if(typeof objUtl != "undefined"){
                for(var err in $err){
                    objUtl($err,"delete",err);
                }
            }	
            else{
                ent$.error = {};
            }   
        }
        if(Object.keys(ent$.error).length == 0){
            (0,_commonUtils__WEBPACK_IMPORTED_MODULE_0__.cmpSet)(lyte, ent$, "isError", false);
        }
    }
}


ValidationError.errorCodes = {
    ERR01 : "Primary key cannot be modified", 
    ERR02 : "Mandatory prop cannot be empty", 
    ERR03 : "Type of value does not match the specified data type",
    ERR04 : "Value is greater than the maximum value allowed",
    ERR05 : "Value is less than the minimum value allowed", 
    ERR06 : "Length of string/array is greater than the maximum limit allowed", 
    ERR07 : "Length of string/array is less than the minimum limit allowed",
    ERR08 : "String does not match the specified pattern", 
    ERR09 : "Values in array are not unique", 
    ERR10 : "Value is not equal to the specified constant", 
    ERR11 : "Schema of related field is not defined",
    ERR12 : "Schema of backward relation is not defined", 
    ERR13 : "Entity not found", 
    ERR14 : "Schema does not match the schema defined in the related field", 
    ERR15 : "Error in creating a entity as a relation",
    ERR16 : "Entity with primary key already exists", 
    ERR17 : "Value cannot be changed because entity has been deleted", 
    ERR18 : "Action not defined", 
    ERR19 : "Schema not defined",
    ERR20 : "Key not specified", 
    ERR21 : "'one' relationship expects a single object/id", 
    ERR22 : "Type not specified for polymorphic relation", 
    ERR23: "Primary Key value not present", 
    ERR24: "Error while relating entities", 
    ERR25: "Backward relation not present",
    ERR26: "Primary key value cannot be undefined or null",
    ERR27: "Observer can observe only string data type value, '{0}' value cannot be observed in the function named '{1}' in the component '{2}'",
    ERR29: "Property not defined in the object",
    ERR30: "Property's value is not an instanceof the mentioned class"      
};


/***/ }),

/***/ 5730373:
/*!*****************************************************!*\
  !*** ./node_modules/@slyte/data/src/commonUtils.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {


__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "cmpSet": () => (/* binding */ cmpSet),
/* harmony export */   "emit": () => (/* binding */ emit)
/* harmony export */ });
function cmpSet(lyte, obj, key, value, opts, fromStore){
    if(lyte && lyte.$utils && lyte.$utils.set){
        lyte.$utils.set(obj, key, value, opts, fromStore);
    }
    else{
        obj[key] = value;
    }
}

function emit(db, type, record, attr, err){
	record.$.emit(type, [record,attr,err]);
	record.$.schema.emit(type, [record, attr, err]);
	db.emit(type, [record.$.schema._name, record, attr, err]);
}

 

/***/ })

}]);